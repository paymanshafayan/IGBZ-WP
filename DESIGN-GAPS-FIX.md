# رفع شکاف‌های تأییدشده — سند اجرا

**وضعیت:** ✅ شکاف‌های امنیتی این سند در کد و تست پوشش داده شده‌اند؛ قرارداد حقوقی، گیت پرداخت،
گیت مسیرهای اصلی، کول‌داون OTP و اتصال اولیهٔ پادو اجرا شده‌اند. گزارش اجرای جاری در §۷ است.
**تاریخ:** ۱۴۰۵/۰۵/۳۱
**دستور کارفرما:** «موارد بند الف رو به صورت یک سند دربیار که یکجا همشو انجام بدیم،
اولویتش هم در آخر بذار.»

---

## ۰. این سند چیست

چهار موردی که در بازبینی ۱۴۰۵/۰۵/۳۱ پیدا شدند و **نقص‌اند، نه سلیقه**. هر چهار مورد با
اجرای واقعی روی کد تأیید شده‌اند — هیچ‌کدام حدس نیست.

| # | شکاف | جدیت | روش تأیید |
|---|---|---|---|
| ۱ | قرارداد دیجیتال حقوقی: جدول هست، کد نیست | 🔴 حقوقی | `grep` روی کل `src/` |
| ۲ | مسیرهای هستهٔ وردپرس و ووکامرس بی‌محافظ‌اند | 🔴 نشت داده | خواندن `Authenticator.php:137` |
| ۳ | کول‌داون OTP بر اساس IP اعمال نمی‌شود | 🟠 هزینه | خواندن `OtpService::send()` |
| ۴ | بدهی سند `DESIGN-PADO.md` | 🟠 گمراهی | مقایسه با تصحیح کارفرما |

---

## ۱. 🔴 قرارداد دیجیتال حقوقی — جدول هست، کد نیست

### ۱.۱ شکاف

`DESIGN-LEGAL-AUTH.md §۳` می‌گوید اگر ادمین «تطابق کد ملی» را خاموش کند، باید **قرارداد
دیجیتال حقوقی** را تأیید کند که «تمام عواقب آن تنها متوجه شخص اوست».

جدولش در اسکیما **هست** (`Schema.php:1161`):

```sql
CREATE TABLE {$p}ig_legal_agreements (
    id, tenant_id, type DEFAULT 'payment_without_nid', version,
    accepted_by, accepted_at, ip, content_hash,
    UNIQUE KEY tenant_type (tenant_id,type)
)
```

ولی `grep` روی کل `src/` نشان داد **هیچ فایلی جز `Schema.php` این جدول را نمی‌شناسد**.
نه نوشتنی، نه خواندنی، نه بررسی‌ای.

### ۱.۲ ریسک واقعی

ابتدای `DESIGN-LEGAL-AUTH.md` هشدار داده: «عدم رعایت ثبت‌نام/احراز قانونی می‌تواند منجر
به **پروندهٔ قضایی و جبران خسارت‌های سنگین** شود.»

امروز این حالت ممکن است: پرداخت بانکی بدون تطابق کد ملی انجام شود **و هیچ قرارداد
امضاشده‌ای پشتش نباشد**. یعنی نه محافظت فنی، نه محافظت حقوقی.

### ۱.۳ آنچه باید ساخته شود

۱. **سرویس قرارداد** — خواندن وضعیت، ثبت پذیرش، و نسخه‌گذاری متن.
۲. **رابط تأیید در پنل ادمین فروشگاه** — نمایش متن کامل و دکمهٔ پذیرش صریح.
۳. **گیت پیش از پرداخت** ← مهم‌ترین بخش:

```
اگر legal.national_id_check خاموش است
    و قرارداد تأییدشدهٔ معتبر برای این tenant نیست
        → پرداخت بانکی فعال نشود
```

۴. **ثبت `content_hash`** — تا بعداً معلوم باشد ادمین دقیقاً **کدام متن** را پذیرفته.
   اگر متن قرارداد عوض شد، پذیرش قبلی برای نسخهٔ تازه معتبر نیست.
۵. **ثبت در دفتر رویداد** (`igbz_audit`) — پذیرش قرارداد یک رویداد حقوقی است.

### ۱.۴ قید

`UNIQUE KEY tenant_type` یعنی هر مستأجر فقط **یک** رکورد از هر نوع دارد. با نسخه‌گذاری
تداخل دارد: اگر نسخهٔ ۲ قرارداد بیاید، رکورد قبلی باید به‌روز شود یا کلید یکتا عوض شود.
تصمیمش موقع پیاده‌سازی گرفته شود.

---

## ۲. 🔴 مسیرهای هستهٔ وردپرس و ووکامرس بی‌محافظ‌اند

### ۲.۱ شکاف — با نقل کد

`Authenticator::rest_authentication_errors()` خط ۱۳۷:

```php
$token = self::bearer_token();
if ( '' === $token ) {
    return $errors;   // ← بدون توکن، هیچ کنترلی اعمال نمی‌شود
}
```

یعنی محدودیت نرخ **فقط روی کاربران اپ** (که توکن Bearer دارند) اعمال می‌شود. درخواستی که
با **کوکی نشست وردپرس** یا **Application Password** بیاید، از این فیلتر رد می‌شود.

و `grep` تأیید کرد هیچ گیت مرکزی‌ای (`rest_pre_dispatch`) وجود ندارد.

### ۲.۲ درهای باز

| مسیر | چه می‌دهد |
|---|---|
| `GET /wp-json/wc/v3/customers` | **مشتریان با ایمیل، تلفن، آدرس** |
| `GET /wp-json/wc/v3/orders` | سفارش‌ها با اطلاعات کامل خریدار |
| `GET /wp-json/wp/v2/users` | فهرست کاربران |
| `xmlrpc.php` | بردار brute-force و تقویت حمله |
| `?author=N`، فید نویسنده، oEmbed | شمارش کاربران بدون REST |
| صادرات وردپرس (`Tools → Export`) | خروجی انبوه از پنل |
| `admin-ajax.php` | مسیرهای دلخواه افزونه‌ها |

### ۲.۳ ریسک در مقیاس ما

صدها فروشگاه × هزاران کاربر. یک حساب ادمین فروشگاه که لو برود، می‌تواند **کل پایگاه
مشتریان** را از `wc/v3/customers` بیرون بکشد — بدون سقف، بدون امضای بیومتریک
(`DESIGN-LEGAL-AUTH.md §۷.۶`)، و بدون هیچ ردی در دفتر رویداد.

مطابق OWASP API Security Top 10 (2023) این هم‌زمان مصداق **`API3` (افشای بیش از حد داده)**
و **`API6` (سوءاستفادهٔ خودکار از جریان کاری حساس)** است.

### ۲.۴ آنچه باید ساخته شود

**رویکرد: فهرست سفید، نه فهرست سیاه.** شمردن درهای پشتی محکوم به شکست است — افزونهٔ بعدی
یک در تازه باز می‌کند. پیش‌فرض باید **بسته** باشد.

۱. **گیت مرکزی روی `rest_pre_dispatch`** — نه `rest_authentication_errors` که فقط با
   توکن کار می‌کند. همهٔ مسیرها، از جمله هسته و ووکامرس، از آن رد شوند.
۲. **`xmlrpc.php`** — خاموش یا در nginx مسدود.
۳. **شمارش کاربران** — بستن `?author=N`، فیدهای نویسنده، و oEmbed.
۴. **Application Passwords** — خاموش شود؛ مسیر احراز موازی است که کنترل ما را دور می‌زند.
۵. **صادرات پنل وردپرس** — محدود به مدیر ارشد یا بسته.
۶. **`admin-ajax.php`** — بازبینی مسیرهای ثبت‌شده.

⚠️ **قید:** بعضی از این‌ها بهتر است در **nginx** بسته شوند تا اصلاً به PHP نرسند —
هم‌راستا با قاعدهٔ «هیچ کار سنگینی در مسیر درخواست» (`PROJECT-STATE.md §۵.۵`).

⚠️ **خطر خودزنی:** بستن این مسیرها می‌تواند خودِ پنل وردپرس یا ووکامرس را بشکند (مثلاً
بلوک‌ویرایشگر به `wp/v2` نیاز دارد). فهرست سفید باید با **تست زندهٔ پنل** همراه باشد، نه
فقط تست واحد.

---

## ۳. 🟠 کول‌داون OTP بر اساس IP اعمال نمی‌شود

### ۳.۱ شکاف

جدول `otp_codes` ستون `ip_hash` دارد (`Schema.php:475`) و `OtpService::send()` هنگام درج
**آن را می‌نویسد**. ولی در بررسی کول‌داون **خوانده نمی‌شود**:

```php
'SELECT created_at FROM otp_codes
 WHERE phone = %s AND purpose = %s ORDER BY id DESC LIMIT 1'
```

سهمیهٔ ساعتی (`within_hourly_quota`) هم فقط روی `phone` است. یعنی زیرساخت نیمه‌کاره رها
شده.

### ۳.۲ خواستهٔ کارفرما — شرط `AND`

> «بر اساس شماره موبایل **و** آی‌پی بررسی کنه» — «یعنی اگه **هر دو یکی باشن**.»

```sql
WHERE phone = %s AND ip_hash = %s AND purpose = %s
```

اگر هر دو یکی بودند و مهلت نگذشته بود → رد، با پیام «هنوز مهلت ارسال مجدد منقضی نشده» و
`retry_after`.

**نکته:** با `AND`، نگرانی NAT خودبه‌خود حل می‌شود — دو کاربر واقعی پشت یک IP شمارهٔ
متفاوت دارند.

### ۳.۳ دو الزام دیگر کارفرما

- **بررسی حتماً در بک‌اند**، نه فرانت‌اند: «با یه رفرش صفحه دور زده نشه». شرط باید داخل
  `OtpService::send()` بنشیند — نه کنترلر، نه شورت‌کد، نه جاوااسکریپت — چون آن تابع تنها
  مسیری است که همهٔ ورودی‌ها (سایت، REST اپ، مصرف‌کنندهٔ آینده) از آن می‌گذرند.
- **تولید کد با `Crypto`** — ✅ از قبل رعایت می‌شود (`Crypto::numeric_code()` روی
  `random_int()`). به‌عنوان قاعدهٔ دائمی ثبت شد.

### ۳.۴ کارهای جانبی

۱. **ایندکس:** کلید فعلی `phone_purpose` است. با افزودن `ip_hash` به شرط، ایندکس باید
   بازبینی شود (`DB_VERSION` بالا می‌رود).
۲. **پاسخ استاندارد:** طبق `PROJECT-STATE.md §۵.۵` باید `429` + `Retry-After` باشد.
۳. **رویداد امنیتی:** رسیدن به سقف باید به ویرا گزارش شود
   (`DESIGN-DEPLOY-VIRA.md §۱۴`).

---

## ۴. 🟠 بدهی سند `DESIGN-PADO.md`

### ۴.۱ شکاف

کارفرما در ۱۴۰۵/۰۵/۳۱ تصحیح کرد که **پادو سرویس ابری آماده است و ما آن را نمی‌سازیم**.
فقط دو جا تصحیح شد: `§۲.۵` (تازه) و تصمیم ۴ در `§۱۵`.

**کل سند بازبینی نشده** و جاهایی هنوز بر فرض «ما پادو را می‌سازیم» نوشته شده.

### ۴.۲ چرا مهم است

هر کس بعداً این سند را بخواند — از جمله خودِ من در جلسهٔ بعد — بر پایهٔ فرض غلط طراحی
می‌کند. همان اتفاقی که با «عامل فروشگاه» افتاد و یک بند کامل سند استقرار را باطل کرد.

### ۴.۳ آنچه باید انجام شود

بازبینی کامل سند و تفکیک صریح سه چیز که امروز قاطی‌اند:

| | چیست | چه کسی می‌سازد |
|---|---|---|
| **ویرا** | ابزار عاملِ ما | ✅ ما |
| **پادو** | سرویس ابری بیرونی | ❌ ادمین فروشگاه تهیه می‌کند |
| **اسکیل** | دستورالعمل روزانه برای پادو | ✅ ما |

⚠️ این کار **ابتدای کار پادو** انجام شود (کار دوم)، نه الان — چون آنجا سند به‌هرحال
باز می‌شود و دوباره‌کاری نمی‌شود.

---

## ۵. اولویت اجرا

طبق دستور کارفرما، اولویت در انتهای سند:

| ترتیب | مورد | چرا این جا |
|---|---|---|
| **۱** | **§۲ مسیرهای هسته** | بزرگ‌ترین سطح حمله. نشت داده برگشت‌ناپذیر است — برخلاف بقیه که ضررشان قابل جبران است |
| **۲** | **§۳ کول‌داون OTP** | کوچک، مستقل، و جلوی هزینهٔ واقعی پیامک را می‌گیرد |
| **۳** | **§۱ قرارداد دیجیتال** | ریسک حقوقی جدی است، ولی تا کلید شاهکار نیامده مسیر پرداخت بانکی به‌هرحال کامل فعال نیست |
| **۴** | **§۴ بدهی سند پادو** | همراه کار دوم (پادو)، نه اینجا |

### چرا §۲ قبل از §۱

هر دو 🔴 هستند. تفاوتشان در **برگشت‌پذیری** است:

- **نشت داده (§۲)** برگشت‌ناپذیر است. اطلاعات مشتری که بیرون رفت، برنمی‌گردد.
- **ریسک حقوقی (§۱)** جدی است ولی تا وقتی پرداخت بانکی واقعی فعال نشده، بالفعل نیست — و
  کلید شاهکار هنوز نیامده.

### نسبت با ترتیب کلی پروژه

هر سه مورد اول **بخشی از سند امنیت‌اند** که کارفرما آن را **کار اول** تعیین کرده
(`PROJECT-STATE.md §۵.۵`). پس این سند زیرمجموعهٔ آن است، نه موازی‌اش.

مورد چهارم داخل **کار دوم** (پادو) حل می‌شود.

---

## ۶. آنچه این سند تعیین تکلیف نمی‌کند

- **۱۳ لایهٔ امنیتی** — کارفرما گفت «در بخش امنیت توضیح می‌دم». این سند فقط شکاف‌های
  موجود را می‌بندد، معماری کامل امنیت را طراحی نمی‌کند.
- **کلیدهای واقعی سرویس‌ها** — دست کارفراست، نه عامل.
- **بازبینی مقیاس‌پذیری کد** با هفت قاعدهٔ `§۵.۵` — کار جدایی است در بند ب.


---

## ۷. گزارش اجرا (۱۴۰۵/۰۵/۳۱ — کامیت `06cbc0b`)

### آنچه ساخته شد

| شکاف | فایل | وضعیت |
|---|---|---|
| §۲ مسیرهای هسته | `src/Support/CoreSurfaceGuard.php` (تازه) | ✅ |
| §۳ کول‌داون OTP | `OtpService::send()` + ایندکس `phone_ip_purpose` | ✅ |
| §۱ قرارداد دیجیتال | `src/Modules/MultiTenant/Payments/LegalWaiverService.php` (تازه) | ✅ |
| §۴ بدهی سند پادو | — | ⏳ همراه کار دوم |

`IGBZ_DB_VERSION` از ۱۷ به **۱۸** رفت (ایندکس تازه). شمار جدول‌ها **۷۰** بدون تغییر.

### تأیید — سه لایه، نه فقط تست واحد

**۱. تست خودکار:** ۱۲۳۷ اظهارنظر در ۲۴ کیس · ۲۳۸ فایل بدون خطای نحوی.

**۲. جهش (mutation):** هر سه رفع عمداً به حالت باگ‌دار برگردانده شد و تست مربوطه **قرمز
شد** — یعنی تست‌ها واقعاً همان باگ را می‌گیرند، نه اینکه الکی سبز باشند.

| جهش | نتیجه |
|---|---|
| حذف `ip_hash` از شرط کول‌داون | ✗ `resend cooldown filters on phone AND ip_hash` |
| بی‌اثر کردن گیت قرارداد | ✗ ۲ تست قرارداد |
| حذف `/wc/v3/customers` از فهرست | ✗ `bulk route recognised` |

**۳. تأیید زنده روی سایت واقعی** (وردپرس ۷.۰.۴ + ووکامرس ۱۱.۰.۱):

```
wc/v3/customers  → HTTP 403  igbz_bulk_access_denied   ← در بسته شد
wp/v2/users      → HTTP 403  igbz_bulk_access_denied   ← در بسته شد
wp/v2/posts      → HTTP 200                            ← دست‌نخورده
wc/v3/products   → HTTP 401  (۴۰۱ خودِ ووکامرس)        ← دست‌نخورده
igbz/v1          → ۱۳۰ مسیر                            ← دست‌نخورده
سلامت سایت       → ۷۰/۷۰ جدول، ۵ ماژول فعال
```

### دو تصمیم که موقع اجرا گرفته شد

**۱. خواندن تک‌رکورد عمداً باز ماند.** `/wc/v3/customers/42` بسته نشد چون صفحات ادمین
خودمان مدام از آن استفاده می‌کنند و بستنش **خودزنی** بود بدون سود امنیتی — فراخوان از
قبل باید شناسه را می‌دانست. ولی `/batch` جزو گروهی شمرده می‌شود، چون گروهی است که شکل
تک‌رکورد به خود گرفته.

**۲. `content_hash` به‌جای یک بولین.** قرارداد با **هش متن** ذخیره می‌شود نه پرچم ساده،
چون بازنویسی متن یعنی شرایط تازه و رضایت قبلی به آن تعلق نمی‌گیرد.

### یک نقص جانبی که حین کار پیدا و رفع شد

`apply_filters` در `tests/bootstrap.php` یک **no-op** بود که مقدار را دست‌نخورده
برمی‌گرداند. یعنی هر رفتار مبتنی بر فیلتر عملاً **غیرقابل تست** بود: تست می‌توانست فیلتر
ثبت کند، هیچ اثری نبیند، و باز هم سبز شود. واقعی شد و `remove_filter` هم اضافه گردید.

### آنچه در سندباکس قابل تأیید نبود

- **`?author=N` و oEmbed:** منطقشان نوشته شد ولی مسیر بدون‌کوکی در این محیط قابل آزمون
  دقیق نبود.
- **رفتار با کاربر واقعیِ فروشگاه:** تأیید زنده با کاربر مدیر انجام شد. رفتار برای نقش
  «مدیر فروشگاه» وقتی چند مستأجر واقعی باشند، روی محیط کارفرما باید دیده شود.
- **`igbz-status` که ۴۰۳ می‌دهد:** با `git stash` بررسی شد و **با کد قدیمی هم ۴۰۳ بود** —
  پیش‌از‌این وجود داشته و ربطی به این تغییرات ندارد. جدا باید بررسی شود.
