# استقرار ویرا روی سرور — طراحی

> **ریشه نام (دهخدا):** ویرا (ص، از پهلوی): هوشمند، یادگیرنده، آموزنده، مدرک. شاهد از ابوالعباس مروزی: «یکی مرد ویرا بباید نخست / که گوید نیوشیده‌ها را درست.»

> **تطبیق معماری ۱۴۰۵/۰۶/۰۶:** ویرا runtime، proxy یا secret store پادو/DeepInfra/Zernio
> نیست. معماری جاری در ADR-0004 است و در تعارض با بخش‌های تاریخی این سند مقدم است.
> هیچ فایل داخل `vira/` برای این تصمیم بررسی یا تغییر نشده است.
>
> **وضعیت (به‌روز ۱۴۰۵/۰۵/۳۰):** این سند هنوز **پیاده نشده** است. ویرا در نسخهٔ ۰.۹.۶ فقط روی
> دستگاه خودِ کاربر اجرا می‌شود (تک‌کاربره، بدون دروازهٔ پنل و بدون چند مدیر). هفت سؤالِ
> باز پایین همین سند هنوز جواب کارفرما را می‌خواهند؛ بدون آن‌ها، استقرار سروری یعنی حدس‌زدن
> سیاست دسترسی — که در جایی که «فرستادن قیمت غلط برای پنج هزار مشتری» ممکن است، بدترین
> کار است.


**وضعیت: پیشنهاد، پیاده‌نشده.** (۱۴۰۵/۰۵/۲۷ · ۲۰۲۶-۰۸-۱۷)
**مبنا:** تصمیم‌های تأییدشدهٔ کارفرما در `DESIGN-PADO.md §۱۵`، و **وضعیت واقعی کد امروز**
که برای نوشتن این سند خوانده شد، نه از روی حافظه.

طبق قانون کار، تا اعلام صریح «شروع کن» هیچ کدی بر اساس این سند نوشته نمی‌شود.

این سند به یک سؤال جواب می‌دهد: **ویرا که امروز روی لپ‌تاپ یک نفر کار می‌کند، چطور روی
سرور و در دست چند مدیر با اختیارات نابرابر کار کند، بدون اینکه چیزی از دستمان در برود؟**

هاب پرووایدر (سند `vira/DESIGN-PROVIDER-HUB.md`) ساخته شده و اینجا تکرار نمی‌شود؛ فقط
جاهایی که استقرار روی آن اثر می‌گذارد گفته می‌شود.

---

## ۱. خلاصهٔ مدیریتی

ویرای امروز یک برنامهٔ **تک‌کاربره** است. این جمله‌ای نیست که از روی احتیاط بگویم؛ در کد
سه جای مشخص دارد و هر سه را خواندم:

- `runtime.agent` **یک** عامل است. تا نوبت تمام نشود، پیام دوم با ۴۰۹ رد می‌شود
  (`server.js:818`).
- جریان رویدادها به **همهٔ** کلاینت‌های متصل پخش می‌شود (`broadcast` روی `clients`).
- دروازهٔ تأیید فقط با **شناسه** جواب می‌گیرد (`POST /api/permission`)؛ کد نمی‌پرسد این
  شناسه مال کدام کاربر بود.

یعنی اگر همین امروز دو مدیر را پشت یک پروسه بنشانیم، **مدیر دوم گفتگوی مدیر اول را
می‌بیند و می‌تواند تأییدهایش را بزند**. این یک باگ نیست، یک فرضِ طراحیِ نانوشته است که با
استقرار چندنفره می‌شکند.

راه پیشنهادی من — با تفصیل در بند ۵ — **یک پروسهٔ ویرا به‌ازای هر مدیر** است، نه بازنویسی
هسته برای چندنشستی‌شدن. دلیلش در بند ۵.۳ آمده و خلاصه‌اش این است: جداسازی در سطح پروسه،
همان جداسازی‌ای است که سیستم‌عامل مجانی به ما می‌دهد و ما مجبور نیستیم دوباره و بدتر
بنویسیمش.

پنج چیز باید ساخته شود و ترتیبشان اهمیت دارد:

| فاز | چه چیزی | چرا این ترتیب |
|---|---|---|
| **الف** | قفل شنود روی لوکال‌هاست · نقش‌ها و دسترسی‌ها · پراکسی پنل · پروسه به‌ازای مدیر | تا این نباشد، هر چیز دیگری روی یک در باز ساخته می‌شود |
| **ب** | محدوده‌های فایل‌سیستم (فراتر از یک پوشهٔ کاری) | بدون این، «عیب‌یابی سرور» که خواستهٔ صریح است ممکن نیست |
| **پ** | کارگزار کلید · ابزار SSH · دفتر رویداد منتسب | این سه با هم معنا دارند: دسترسی، و ردِ پاکِ آنچه شد |
| **ت** | صف واقعی و کار بلندمدت | امروز هر کاری با بستن مرورگر می‌میرد |
| **ث** | مدیریت اسکیل‌های پادو و گزارش ماهانه | آخر. ~~عامل‌های فروشگاه~~ منتفی شد (بند ۱۱) — این فاز حالا سبک‌تر است |

---

## ۲. تصمیم‌هایی که مبنا هستند و بحث نمی‌شوند

از `DESIGN-PADO.md §۱۵`، تأییدشده در ۱۴۰۵/۰۵/۲۶:

| # | تصمیم |
|---|---|
| ۱ | ویرا یک **پروسهٔ جدا روی لوکال‌هاست** است؛ پنل به آن پراکسی می‌کند |
| ۲ | **دسترسی از بیرون کاملاً ممنوع**؛ تنها راه، پنل است |
| ۳ | **SSH مسیر اصلی** برای ماشین‌های خودمان، **API مکمل** برای سرویس‌های بدون شل |
| ۴ | ~~عامل‌های فروشگاه: یک پروسه به‌ازای هر فروشگاه~~ — ❌ **باطل شد (۱۴۰۵/۰۵/۳۱).** پادو سرویس ابریِ بیرونی است و هیچ پروسه‌ای روی سرور ما ندارد. بند ۱۱ |
| ۵ | «پلن کاری» موضوع پادو است، نه ویرا |
| ۶ | دادهٔ فروشگاه به مدل بیرونی: موضوع پادو؛ فعلاً ماسک PII |
| — | ابزار `bash` **می‌ماند** |
| — | مدیران چند نفرند با **اختیارات نابرابر**؛ ادمین فروشگاه هیچ رابطی با ویرا ندارد |
| — | در بخش مدیریت **هیچ تغییری بدون تأیید انسانی** انجام نمی‌شود |
| — | نسخهٔ پنل **همه‌فن‌حریف** است؛ تخصصی‌سازی با بستن مجوز است نه با کم‌کردن توانایی |

و یک تصمیم که موقع ساختن هاب گرفته شد و اینجا هم اثر دارد: **کلید مدل‌ها داخل ویرا،
کلید سرویس‌های بیرونی در پنل، رمزهای زیرساخت فقط دست مدیر ارشد.**

---

## ۳. توپولوژی

```
                    اینترنت
                       │
                   ┌───┴────┐
                   │ nginx  │   ← تنها چیزی که پورت باز دارد
                   └───┬────┘
                       │ 443
              ┌────────┴─────────┐
              │  وردپرس (پنل)    │
              │  igbz-suite      │
              └────────┬─────────┘
                       │ صفحهٔ «IGBZ → ویرا»
                       │
        ┌──────────────┴───────────────┐
        │   دروازهٔ ویرا (بند ۴)        │  ← نشست وردپرس + دسترسی + دفتر رویداد
        └──────────────┬───────────────┘
                       │ 127.0.0.1 فقط
        ┌──────────┬───┴────┐
        │          │        │
   vira:7801 vira:7802  …
   مدیر «الف»   مدیر «ب»
        │          │        │
        └──────────┴────────┘
                   │
          ┌────────┴─────────┐
          │  تعریف مشترک هاب │  ← فقط‌خواندنی برای همه جز مدیر ارشد
          │  کارگزار کلید    │
          │  دفتر رویداد     │
          └──────────────────┘
```

سه قاعدهٔ سختِ این نقشه:

1. **هیچ پروسهٔ ویرایی روی رابط شبکه گوش نمی‌دهد.** فقط `127.0.0.1`.
2. **هیچ مرورگری مستقیم به ویرا وصل نمی‌شود.** حتی از داخل شبکه.
3. **هیچ پروسهٔ ویرایی هویت خودش را نمی‌سنجد.** هویت مسئولیت پنل است و ویرا حق ندارد
   صفحهٔ ورود داشته باشد — چون آن‌وقت دو منبع حقیقت برای «چه کسی مجاز است» داریم.

---

## ۴. دروازه: پنل چطور به ویرا وصل می‌شود

اینجا یک تصمیم فنی هست که باید آگاهانه گرفته شود، چون هر دو راه معایبی دارند.

### ۴.۱ مسئلهٔ SSE

رابط ویرا روی **جریان رویداد (SSE)** بنا شده: یک اتصال باز که تا وقتی کاربر روی صفحه است
باز می‌ماند. اگر این جریان از داخل PHP پراکسی شود، **یک ورکر php-fpm به‌ازای هر مدیرِ باز
اشغال می‌ماند**. با `pm.max_children` معمولِ ۱۰ تا ۲۰، سه مدیر که پنجره‌شان باز است یک‌ششم
ظرفیت سایت را گرفته‌اند بی‌آنکه کاری بکنند. این یک ریسک واقعی است، نه نظری.

### ۴.۲ راه پیشنهادی: `auth_request` در nginx

```nginx
location /vira/ {
    auth_request /igbz-vira-authz;
    auth_request_set $vira_port $upstream_http_x_vira_port;
    auth_request_set $vira_user $upstream_http_x_vira_user;

    proxy_pass http://127.0.0.1:$vira_port/;
    proxy_set_header X-Vira-Admin $vira_user;

    # جریان رویداد نباید بافر شود
    proxy_buffering off;
    proxy_read_timeout 1h;
    proxy_http_version 1.1;
}

location = /igbz-vira-authz {
    internal;
    proxy_pass http://127.0.0.1:80/wp-json/igbz/v1/vira/authz;
    proxy_pass_request_body off;
    proxy_set_header Content-Length "";
    proxy_set_header X-Original-URI $request_uri;
}
```

وردپرس فقط یک درخواست **کوتاه** می‌گیرد (`authz`)، نشست و دسترسی را می‌سنجد، شمارهٔ پورت
پروسهٔ همان مدیر را در هدر برمی‌گرداند، و کنار می‌رود. جریان بلند را nginx خودش نگه می‌دارد.

**مزیت:** ظرفیت PHP دست‌نخورده می‌ماند؛ SSE طبیعی کار می‌کند.
**عیب:** به دسترسی به پیکربندی nginx نیاز دارد. روی هاست اشتراکی ممکن نیست.

### ۴.۳ راه جایگزین: پراکسی کامل در PHP

`GET|POST /wp-json/igbz/v1/vira/{path}` که خودش به `127.0.0.1:<port>` وصل می‌شود.
برای مسیرهای معمولی خوب است. برای SSE باید به **long-poll** تبدیل شود: به‌جای یک اتصال
دائم، درخواست‌های پیاپیِ حداکثر ۲۵ ثانیه‌ای که رویدادهای تازه را برمی‌گردانند.

این یعنی یک تغییر کوچک در سمت ویرا (یک مسیر `/api/events/poll?since=<n>` کنار SSE
موجود) و یک سوئیچ در رابط. تأخیرش محسوس نیست ولی ظرافت SSE را ندارد.

### ۴.۴ توصیه

**۴.۲ به‌عنوان مسیر اصلی، ۴.۳ به‌عنوان پشتیبان.** و long-poll به‌هرحال ساخته شود، چون
پشتیبانِ ارزانی است و در آینده اگر پشت CDN رفتیم به کار می‌آید.

> ### ✅ تصمیم گرفته شد (۱۴۰۵/۰۵/۳۱) — nginx، مسیر ۴.۲
>
> کارفرما اعلام کرد سرور هنوز گرفته نشده و انتخابش کاملاً آزاد است: **اول سرور مجازی
> (VPS)، و بعد از پیشرفت کار سرور اختصاصی — در هر دو حالت دسترسی به پیکربندی هست.**
>
> پس **nginx** انتخاب شد و مسیر ۴.۲ (`auth_request`) مسیر اصلی است. سه دلیل، به ترتیب
> اهمیت:
>
> ۱. **ظرفیت PHP.** در راه ۴.۳ هر پاسخ ویرا یک پروسهٔ PHP را تا پایان کار اشغال می‌کند و
>    پاسخ‌ها گاهی دقیقه‌ها طول می‌کشند؛ چند مدیر هم‌زمان می‌توانند ظرفیت را بخورند و
>    **فروشگاه برای مشتری کند شود**. در ۴.۲ وردپرس فقط یک `authz` کوتاه می‌گیرد و کنار
>    می‌رود.
> ۲. **SSE طبیعی کار می‌کند.** راه ۴.۳ نیازمند تبدیل به long-poll است — یعنی کد اضافه در
>    هر دو سمت (مسیر `/api/events/poll` در ویرا + سوئیچ در رابط).
> ۳. **پیکربندی از قبل آماده است** (بند ۴.۲)، پس این مسیر کد کمتری لازم دارد نه بیشتر.
>
> **قید:** فقط «nginx بودن» کافی نیست؛ دسترسی به فایل پیکربندی لازم است. VPS و سرور
> اختصاصی هر دو این را می‌دهند، **هاست اشتراکی نه**. اگر روزی به هاست اشتراکی مهاجرت شد،
> راه ۴.۳ نقشهٔ دوم است — به همین دلیل long-poll طبق توصیهٔ بالا به‌هرحال ساخته می‌شود.

### ۴.۵ آنچه دروازه در هر درخواست انجام می‌دهد

۱. نشست وردپرس معتبر است؟ (کوکی + `wp_verify_nonce` برای درخواست‌های نوشتنی)
۲. کاربر دسترسی `igbz_vira_use` دارد؟
۳. برای مسیرهای حساس (`/api/hub`, `/api/permissions`, `/api/git` با پوش)، دسترسی
   مخصوص همان کار را دارد؟
۴. پروسهٔ این مدیر بالاست؟ اگر نه، بالا بیاور (بند ۵.۴).
۵. **نام مدیر را در هدر `X-Vira-Admin` بگذار** — این همان چیزی است که سقف هزینهٔ
   «هر مدیر» و دفتر رویداد به آن نیاز دارند.
۶. اگر درخواست از جنس تغییر بود، در دفتر رویداد ثبت کن (بند ۸).

---

## ۵. چند مدیر، با اختیارات نابرابر

> ### ✅ تصمیم گرفته شد (۱۴۰۵/۰۵/۳۱) — «یک نفر الان، چند نفر بعداً»
>
> کارفرما گزینهٔ **ب** را انتخاب کرد: **معماری چندنفره ساخته شود، ولی فعلاً فقط یک مدیر
> ارشد فعال باشد.**
>
> یعنی هر چه در این بند آمده (راه دو: یک پروسه به‌ازای هر مدیر، تقسیم حالت روی دیسک،
> مالکِ تأیید) **پیاده می‌شود**، ولی روز اول فقط یک کاربر دسترسی `igbz_vira_use`
> می‌گیرد.
>
> **چرا این‌طور و نه تک‌نفرهٔ ساده:** سه اشتراکِ بند ۵.۱ (عامل سراسری، پخش جریان به همه،
> دروازهٔ تأیید بی‌صاحب) در حالت تک‌نفره **دیده نمی‌شوند ولی وجود دارند**. اگر بعداً نفر
> دوم اضافه شود و طراحی تک‌نفره باشد، باید `server.js` و `runtime.js` و `agent.js`
> بازنویسی شوند — همان‌هایی که امروز کار می‌کنند و تست دارند. هزینهٔ ساختنش امروز کم است،
> هزینهٔ مهاجرتش بعداً زیاد.
>
> **پیامد عملی:** بودجهٔ حافظه روز اول ~۱۲۰ مگابایت (یک مدیر) است، نه نیم گیگ؛ ولی سرور
> باید جا برای رشد داشته باشد. این ورودیِ سؤال «حافظهٔ سرور چقدر باشد» است.
>
> **باز مانده:** «اختیارات نابرابر» عنوانِ همین بند هنوز تصمیم‌گیری نشده — وقتی نفر دوم
> اضافه شد باید معلوم شود چه کسی حق پوش، تعریف اتصال، و تأیید دارد. تا آن روز، تنها
> مدیر ارشد همه‌کاره است.

### ۵.۰ ویرا مثل مغز است — ماژول‌ها ابزارهای اویند

> **تصحیح بنیادی کارفرما (۱۴۰۵/۰۵/۳۱).** این بند مبناست و بقیهٔ سند باید با آن خوانده شود.

> «ویرا مثل مغز انسان می‌مونه که ابزارهای مختلف داره، مثل چشم‌ها، گوش‌ها، عضلات. ویرا هم
> ابزارهای مختلفی داره که در قالب ماژول‌ها طراحی شدن. یکی مثلاً از این ماژول‌ها رابط
> کاربریه. وقتی می‌گم ویرا بالا اومد، منظورم ماژول رابط کاربریش برای مدیره، نه اینکه
> خودِ ویرا خاموش بوده حالا روشن می‌شه.»

| | نقش | چرخهٔ حیات |
|---|---|---|
| **هستهٔ ویرا** (مغز) | مدیریت پروژه، نظارت امنیتی، بازبینی، تصمیم | **همیشه بالا** |
| **ماژول رابط کاربری** (چشم و زبان، برای مدیر) | نمایش و تعامل | با نیاز می‌آید و می‌رود |
| **سایر ماژول‌ها** (حواس و عضلات) | ابزارها، گیت، تونل، هاب، امنیت… | بسته به کار |

**سه پیامدی که مستقیماً از این تعریف درمی‌آید:**

۱. **«ویرا بالا آمد» یعنی رابطش برای مدیر باز شد** — نه اینکه ویرا خاموش بوده و روشن شد.
   هر جای سند که این عبارت آمده باید با همین معنا خوانده شود.
۲. **کارهای بی‌رابط باید بدون هیچ مدیرِ آنلاین ادامه پیدا کنند:** نظارت امنیتی (بند ۱۴)،
   صف و کار بلندمدت (بند ۱۰)، و پایش سلامت. اینها به مغز وصل‌اند نه به چشم.
۳. **حافظه:** آنچه دائمی است هستهٔ مشترک است، نه یک پروسهٔ کامل به‌ازای هر مدیر. رابط هر
   مدیر می‌تواند بیاید و برود بدون اینکه هسته خاموش شود. بودجهٔ بند ۵.۷ باید بر همین مبنا
   خوانده شود.

**بازطراحی لازم:** بند ۵.۳ امروز می‌گوید «یک پروسه به‌ازای هر مدیر» و همان پروسه هم هسته
است هم رابط. با این تعریف، مرز درست **هسته / رابط** است نه **مدیر الف / مدیر ب**. جداسازی
بین مدیرها همچنان لازم است (سه اشتراک بند ۵.۱)، ولی نباید به قیمت خاموش‌شدن هسته تمام
شود. طراحی دقیقش با دستور جداگانهٔ کارفرما.

### ۵.۱ سه چیزی که امروز مشترک‌اند و نباید باشند

| چیز | امروز | چرا مشکل است |
|---|---|---|
| عامل | یک `runtime.agent` سراسری | مدیر دوم پشت ۴۰۹ می‌ماند |
| جریان رویداد | پخش به همهٔ کلاینت‌ها | مدیر ب گفتگوی مدیر الف را می‌بیند |
| دروازهٔ تأیید | `POST /api/permission` فقط شناسه می‌گیرد | مدیر ب می‌تواند تأیید مدیر الف را بزند |

سومی از دوتای اول بدتر است: **تأیید انسانی، ستون امنیتی این طراحی است** و اگر بشود
تأیید یکی را به‌جای دیگری زد، آن ستون توخالی است.

### ۵.۲ دو راه

**راه یک — چندنشستی‌کردن هستهٔ ویرا.** `Map<sessionId, Agent>`، جریان رویداد به‌ازای
نشست، مالکِ هر تأیید ثبت شود. تمیزتر است، ولی به `server.js` (۱۳۹۳ خط) و `runtime.js` و
`agent.js` دست می‌زند و هر سه امروز کار می‌کنند و تست دارند.

**راه دو — یک پروسه به‌ازای هر مدیر.** هیچ‌کدام از سه اشتراک بالا اصلاً وجود نخواهد داشت،
چون پروسه‌ها حافظهٔ مشترک ندارند.

### ۵.۳ توصیه: راه دو

- **جداسازی‌ای که سیستم‌عامل می‌دهد، از هر چیزی که ما بنویسیم قوی‌تر است.** یک باگ در
  مسیریابی نشست، دو گفتگو را قاتی می‌کند؛ دو پروسه هرچقدر هم باگ داشته باشند این کار را
  نمی‌کنند.
- خرجش شفاف است و حدس نزدم: پروسهٔ ویرای در حال اجرا روی همین سندباکس، با هاب روشن و یک
  گفتگوی باز، **۶۶ مگابایت** RSS دارد. با احتیاط ۱۲۰ مگابایت برای هر مدیر حساب کنیم،
  پنج مدیر می‌شود نیم گیگ. این را می‌شود خرید؛ ساعت‌های اشکال‌زدایی نشست‌های قاتی‌شده را
  نمی‌شود.

> ### ❌ قید «خاموشی با بیکاری» باطل شد (۱۴۰۵/۰۵/۳۱)
>
> ~~پروسه‌ها با بیکاری خاموش می‌شوند (پیش‌فرض پیشنهادی: ۳۰ دقیقه بدون درخواست).~~
>
> این یک **پیشنهادِ تأییدنشده** در نسخهٔ اول سند بود که برای صرفه‌جویی در حافظه نوشته شده
> بود. کارفرما آن را رد کرد و به تعریف روز اول برگرداند:
>
> > «روز اول که ویرا رو تعریف کردم گفتم هستهٔ هوشمند پروژه است، یعنی تمام چیزو باید
> > مدیریت کنه. این چطور خاموش می‌شه؟»
>
> **تناقض واقعی بود:** هسته‌ای که همه‌چیز را مدیریت می‌کند نمی‌تواند بعد از ۳۰ دقیقه
> بیکاری خاموش شود. با تصمیم بند ۱۴ (ویرا مسئول امنیت) این بدتر هم می‌شود — خاموشی یعنی
> **کور شدن**، و نگهبانی که می‌خوابد نگهبان نیست.
>
> **قاعدهٔ جایگزین:** ویرا **همیشه بالاست**. تفکیک درست در بند ۵.۰ آمده — آنچه می‌آید و
> می‌رود، **ماژول رابط کاربری** است نه خودِ ویرا.

### ۵.۴ تقسیم حالت روی دیسک

اینجا یک تلهٔ واقعی هست: امروز **همه‌چیز** در `VIRA_HOME` است — تنظیمات، کلید مدل‌ها،
نشست‌ها، چک‌پوینت‌ها، دفتر مصرف، و حالا هاب. اگر هر مدیر `VIRA_HOME` خودش را بگیرد،
**کلیدها و تعریف هاب هم به‌ازای هر مدیر تکرار می‌شوند** — که هم غلط است (تصمیم: فقط مدیر
ارشد اتصال تعریف می‌کند) و هم فاجعه‌بار (پنج کپی از کلیدها).

پس حالت به دو بخش تقسیم می‌شود:

```
/var/lib/vira/shared/          ← مالک: کاربر vira، فقط‌خواندنی برای پروسه‌های مدیران
    hub.json                     تعریف اتصال‌ها، مدل‌ها، ترکیب‌ها، کلیدها  (۶۰۰)
    hub-ledger.json              دفتر راه‌حل‌های عیب‌یاب
    scopes.json                  محدوده‌های فایل‌سیستم (بند ۶)
    hosts.json                   فهرست سفید میزبان‌های SSH (بند ۷)

/var/lib/vira/admins/<user_id>/    ← مالک: همان پروسه
    config.json                  حالت مجوز، پوشهٔ کاری فعلی، ظاهر
    sessions/  checkpoints/  usage.json
    hub-state.json               سلامت و یادگیریِ دیده‌شده از این پروسه
    spend/<pid>.jsonl            دفتر خرج، فقط‌افزودنی
```

**تغییر لازم در کد:** یک متغیر محیطی دوم، `VIRA_SHARED`. اگر ست شده باشد، `hub.json` و
`hub-ledger.json` از آنجا خوانده می‌شوند و نوشتنشان فقط وقتی مجاز است که پروسه با پرچم
`--superadmin` بالا آمده باشد. امروز `config.js` فقط یک `HOME` می‌شناسد.

**سقف هزینهٔ مشترک** با فایل‌های `spend/<pid>.jsonl` حل می‌شود: هر پروسه فقط به فایل
**خودش** می‌افزاید (افزودن کوتاه روی POSIX اتمیک است، پس قفل لازم نیست)، و موقع سنجش
سقف، همهٔ فایل‌های امروز خوانده و جمع می‌شوند. یک انحراف کوچک و کوتاه‌مدت بین پروسه‌ها
ممکن است؛ برای سقف هزینه قابل‌قبول است و از یک پایگاه‌دادهٔ مشترک ساده‌تر.

### ۵.۵ نقش‌ها و دسترسی‌ها

امروز ده دسترسی داریم (`Capabilities.php`). هفت‌تای تازه لازم است:

| دسترسی | یعنی |
|---|---|
| `igbz_vira_use` | اصلاً می‌تواند ویرا را باز کند |
| `igbz_vira_dev` | روی محیط توسعه کار کند |
| `igbz_vira_staging` | روی محیط آزمایشی |
| `igbz_vira_prod` | روی محیط زنده |
| `igbz_vira_ops` | ابزارهای سرور: SSH، خواندن لاگ، سرویس‌ها |
| `igbz_vira_providers` | اتصال و کلید مدل‌ها — **فقط مدیر ارشد** |
| `igbz_vira_agents` | مدیریت اسکیل‌های پادو و دیدن گزارش ماهانه (بند ۱۱.۳) |

### ۵.۶ و یک اشکال که باید همین‌جا بسته شود

```php
public static function current_user_can( string $cap ): bool {
    return current_user_can( $cap ) || current_user_can( 'manage_options' );
}
```

هر کاربری که `manage_options` دارد، **همهٔ** دسترسی‌ها را دارد. برای ماژول‌های فعلی
پذیرفتنی بوده؛ برای ویرا نیست، چون `manage_options` را در وردپرس هر «Administrator»
دارد و ما داریم دربارهٔ SSH و کلید و محیط زنده حرف می‌زنیم.

پیشنهاد — **بدون شکستن رفتار فعلی**:

```php
/** دسترسی‌هایی که هیچ‌وقت با manage_options باز نمی‌شوند. */
private const STRICT = [
    'igbz_vira_use', 'igbz_vira_dev', 'igbz_vira_staging', 'igbz_vira_prod',
    'igbz_vira_ops', 'igbz_vira_providers', 'igbz_vira_agents',
];

public static function current_user_can( string $cap ): bool {
    if ( in_array( $cap, self::STRICT, true ) ) {
        return current_user_can( $cap );   // بدون در پشتی
    }
    return current_user_can( $cap ) || current_user_can( 'manage_options' );
}
```

و **مدیر ارشد یک نفر مشخص است**، نه «هرکه ادمین است»: یک شناسهٔ کاربر در تنظیمات
(`vira.superadmin_user_id`) که فقط از راه WP-CLI یا `wp-config.php` عوض می‌شود. اگر از
داخل پنل قابل تغییر باشد، هر ادمینی می‌تواند خودش را مدیر ارشد کند و کل این جدول تزئینی
می‌شود.

---

### ۵.۷ بودجهٔ حافظه — ✅ تصمیم گرفته شد (۱۴۰۵/۰۵/۳۱)

**کارفرما: شروع با ۸ گیگابایت، و بعداً ارتقا تا ۳۲ گیگابایت.**

نکتهٔ مهم این است که این **ارتقای همان ماشین** است، نه مهاجرت به ماشین دیگر — یعنی
طراحی نباید فرض «سرور ثابت» بگذارد و نباید عددی را hard-code کند که با ارتقا غلط شود.

| مصرف‌کننده | تخمین |
|---|---|
| سیستم‌عامل | ~۴۰۰MB |
| MySQL | ۵۰۰MB – ۱GB |
| PHP-FPM (وردپرس + ووکامرس) | ۵۰۰MB – ۱GB |
| nginx | ~۵۰MB |
| **ویرا، به‌ازای هر مدیر** | **~۱۲۰MB** (اندازه‌گیری‌شده: ۶۶MB RSS با هاب روشن) |
| xray (اگر تونل روشن باشد) | ~۵۰MB |

**در ۸ گیگ:** وردپرس + ووکامرس + سه تا چهار مدیر هم‌زمان، با فضای تنفس. روز اول که یک
مدیر فعال است (تصمیم بند ۵)، فشاری نیست.

**در ۳۲ گیگ:** سقف مدیرها عملاً برداشته می‌شود.

> **اصلاح (۱۴۰۵/۰۵/۳۱):** نسخهٔ اول اینجا نوشته بود «جا برای عامل‌های فروشگاه باز
> می‌شود». آن فرض **باطل شد** (بند ۱۱): پادو سرویس ابریِ بیرونی است و روی سرور ما
> هیچ حافظه‌ای نمی‌گیرد. پس ۸ گیگِ ابتدایی **راحت‌تر** از این تخمین است.

**پیامدهای طراحی:**

۱. **سقف تعداد پروسه باید تنظیم‌شدنی باشد، نه ثابت.** با ارتقای ۸ → ۳۲ نباید کسی مجبور
   شود کد را عوض کند.
۲. ~~مهلت خاموشی با بیکاری در ۸ گیگ لازم است و باید بماند.~~ ❌ **باطل شد
   (۱۴۰۵/۰۵/۳۱):** هستهٔ ویرا **همیشه بالاست** (بند ۵.۰). آنچه می‌تواند بیاید و برود
   **ماژول رابط کاربری** است، نه هسته. نگرانی اصلیِ آن بند — پروسهٔ فراموش‌شده‌ای که
   هزینهٔ توکن می‌سازد — همچنان معتبر است، ولی جوابش **محافظ ضدحلقه**
   (`vira/DESIGN-PROVIDER-UI.md §۱۴.۴`) است نه خاموش‌کردن هسته.
۳. **انتخاب nginx (بند ۴.۴) اینجا هم کمک می‌کند:** چون پروسه‌های PHP برای پاسخ‌های بلند
   ویرا اشغال نمی‌مانند، بیشترین صرفه‌جویی دقیقاً در همان ۸ گیگِ ابتدایی است.

## ۶. فراتر از «یک پوشهٔ کاری»

### ۶.۱ چرا لازم است

`tools.js` هر مسیر را داخل یک ریشه محدود می‌کند:

```js
function resolveInside( ctx, p ) {
    const target = path.resolve( ctx.workspace, p || '.' );
    const root = path.resolve( ctx.workspace );
    if ( target !== root && ! target.startsWith( root + path.sep ) ) {
        throw new Error( `مسیر بیرون از پوشهٔ کاری است: ${ p }` );
    }
    return target;
}
```

این برای کار روی یک مخزن درست است. ولی خواستهٔ صریح کارفرما «عیب‌یابی سرور» است، و
عیب‌یابی سرور یعنی `/var/log/nginx/error.log` و `/etc/nginx/` و `systemctl status`.
با یک ریشه، جواب ویرا همیشه «مسیر بیرون از پوشهٔ کاری است» خواهد بود.

### ۶.۲ محدوده‌ها به‌جای یک ریشه

`scopes.json` در پوشهٔ مشترک، فقط دست مدیر ارشد:

```json
[
  { "id": "shop",  "root": "/var/www/shop",      "mode": "rw", "cap": "igbz_vira_prod" },
  { "id": "stage", "root": "/var/www/stage",     "mode": "rw", "cap": "igbz_vira_staging" },
  { "id": "logs",  "root": "/var/log",           "mode": "ro", "cap": "igbz_vira_ops" },
  { "id": "nginx", "root": "/etc/nginx",         "mode": "ro", "cap": "igbz_vira_ops" },
  { "id": "repo",  "root": "/srv/igbz-wp",       "mode": "rw", "cap": "igbz_vira_dev" }
]
```

`resolveInside` می‌شود `resolveInScope`: مسیر باید داخل **یکی** از محدوده‌هایی باشد که
این مدیر دسترسی‌اش را دارد، و نوشتن فقط در محدودهٔ `rw`.

### ۶.۳ فهرست سیاهی که بر همه‌چیز مقدم است

مستقل از محدوده‌ها، این‌ها هرگز خوانده یا نوشته نمی‌شوند:

```
/etc/shadow  /etc/gshadow  /root/.ssh  /home/*/.ssh  /var/lib/vira/shared/hub.json
~/.aws  ~/.kube  ~/.docker/config.json  *.pem  *.key  id_rsa*
```

و `wp-config.php` **خوانده می‌شود ولی ماسک‌شده**: مدیر باید بتواند بپرسد «`WP_DEBUG` روشن
است؟» بدون اینکه کلیدهای نمک و رمز دیتابیس به مدل برود. یعنی یک فیلتر روی خروجی خواندن،
نه یک ممنوعیت کامل.

### ۶.۴ سندباکس

`sandbox.enabled` امروز پیش‌فرض خاموش است و وقتی روشن شود شبکه را می‌بندد
(`network: 'none'`). برای «برو API صرافی را پیدا کن» بی‌فایده است.

پیشنهاد: سه پروفایل به‌جای یک کلید روشن/خاموش —
`off` (امروز) · `net` (کانتینر با شبکه، برای کار بیرونی) · `strict` (کانتینر بدون شبکه،
برای اجرای کد ناشناخته). انتخاب پروفایل، دسترسی می‌خواهد.

---

## ۷. رسیدن به ماشین‌های دیگر

### ۷.۱ SSH — مسیر اصلی

ابزار تازه: `ssh_run({ host, command })`. قیدهایی که تصمیم ۳ گذاشت و باید در کد باشند:

| قید | چطور |
|---|---|
| فهرست سفید میزبان | `hosts.json` مشترک؛ هر میزبان دیگری رد، حتی با اصرار مدل |
| کلید هرگز به مدل نرسد | `keyRef` → کارگزار کلید (بند ۹)؛ کلید در `ssh-agent` یا فایل ۶۰۰ بیرون از دید مدل |
| شل تعاملی وجود ندارد | هر فرمان یک فراخوانی جدا؛ نه `ssh -tt`، نه نشست باز |
| هر فرمان از دروازهٔ تأیید | قاعده‌ها مثل `ssh:web-1:systemctl status *` — همان موتور مجوز فعلی، با فرمان مرکبِ شکسته‌شده |
| ثبت کامل | فرمان، میزبان، مدیرِ تأییدکننده، زمان، کد خروج، خروجی پاک‌سازی‌شده |
| پاک‌سازی خروجی | قبل از رسیدن به مدل و به دفتر گفتگو |

نکتهٔ خوب: **موتور شکستن فرمان مرکب از قبل هست** (`permissions.js` با `splitCommand` و
`MULTI_WORD` و تشخیص `$()`), چون همان حفره را یک بار در `bash` بسته‌ایم. برای SSH فقط
باید همان مسیر صدا زده شود، نه اینکه از نو نوشته شود.

### ۷.۲ API — مسیر مکمل

برای پنل میزبانی، CDN، DNS، سرویس پایش، و وردپرسِ سالمِ فروشگاه دیگر. یا آداپتور
اختصاصی، یا کانکتور MCP. کلید از کارگزار، فهرست سفید دامنه، و همان دروازهٔ تأیید.

---

## ۸. دفتر رویداد منتسب

### ۸.۱ چرا `Logger` کافی نیست

`Logger.php` هست، ساختاریافته است، و راز را ماسک می‌کند. سه چیز ندارد:

1. **انتساب.** ستون `user_id` ندارد؛ فقط `tenant_id`.
2. **دوام.** `Cron::housekeeping()` هر روز `logger()->prune( log.retention_days )` را
   صدا می‌زند — یعنی دقیقاً همان چیزی که برای بازرسی می‌خواهیم، بعد از ۳۰ روز پاک می‌شود.
3. **غیرقابل‌ویرایش‌بودن.** یک `UPDATE` ساده هر ردیفی را عوض می‌کند.

### ۸.۲ جدول `igbz_audit`

جدا از `igbz_logs`، **هرگز prune نمی‌شود**:

```
id · created_at · user_id · user_login · ip · env(dev|staging|prod)
action        approve_tool | run_ssh | promote_patch | change_provider | change_scope | deploy
target        نام ابزار، میزبان، شناسهٔ اتصال…
payload       ورودی پاک‌سازی‌شده
result        ok | denied | failed  + کد خروج
prev_hash · hash
```

`hash = sha256(prev_hash + canonical(row))`. این «غیرقابل ویرایش» نمی‌کند — MySQL چنین
چیزی ندارد — ولی **ویرایش را قابل‌کشف** می‌کند: هر دستکاری زنجیره را می‌شکند و یک کار
روزانه آن را می‌سنجد و به مدیر ارشد خبر می‌دهد. صادق‌ترین چیزی است که بدون سخت‌افزار
یا سرویس بیرونی می‌شود ساخت، و باید همین‌طور هم توصیفش کنیم.

### ۸.۳ چه چیزهایی حتماً ثبت می‌شوند

هر تأیید یا رد ابزار · هر فرمان SSH · هر تغییر در اتصال یا کلید هاب · هر وصله‌ای که
ماندگار می‌شود · هر تغییر محدوده یا فهرست سفید · هر پوش گیت · هر خرجِ بالای آستانه.

> **اصلاح (۱۴۰۵/۰۵/۳۱):** «هر روشن/خاموش‌کردن عامل فروشگاه» از این فهرست حذف شد —
> عامل فروشگاه وجود ندارد (بند ۱۱). جایش این دو می‌نشیند، از تصمیم‌های همین تاریخ:
> **هر گزارش بازبینی که ویرا به مدیر ارشد می‌دهد** (بند ۱۳)، و **هر تماسی که از مسیر
> پولیِ اضطراری می‌رود** — چون آن مسیر عمداً سقف ندارد، دفتر رویداد تنها ردِ آن است.

### ۸.۴ مدت نگه‌داری — ✅ تصمیم گرفته شد (۱۴۰۵/۰۵/۳۱)

**کارفرما: هرگز پاک نشود (گزینهٔ الف).** اگر روزی حجم مسئله شد، بایگانی سالانه — نه حذف.

این همان چیزی است که بند ۸.۲ از قبل می‌گفت («هرگز prune نمی‌شود») و حالا تصمیم رسمی است.

سه دلیلی که ثبت شد:

۱. **حجم مسئله نیست.** هر ردیف چند صد بایت است؛ حتی با هزار رویداد در روز، سالی حدود
   صد مگابایت — ناچیز در برابر دیسک سرور.
۲. **ارزشش با گذشت زمان بیشتر می‌شود، نه کمتر.** سؤالی که یک سال بعد پیش می‌آید («این
   از کِی خراب شد؟») دقیقاً همان است که به دادهٔ قدیمی نیاز دارد.
۳. **حذف برگشت‌ناپذیر است.** کوتاه‌کردن نگه‌داری در آینده ضرری ندارد؛ بازگرداندن دادهٔ
   پاک‌شده ممکن نیست.

سه تصمیم دیگرِ همین تاریخ اهمیت این دفتر را بالا برده‌اند: ویرا **بازبین** است و گزارشش
مبنای merge می‌شود (بند ۱۳) · آزمایشی و زنده روی **یک ماشین**‌اند و مرز `scopes.json`
تنها محافظ سایت واقعی است · مسیر پولی **سقف ندارد**. در هر سه، دفتر رویداد تنها راه
فهمیدن «چه شد» است.

### ۸.۵ دادهٔ شخصی در دفتر دائمی — ماسک اجباری

نگه‌داری دائمی یک پیامد دارد که باید صریح بسته شود: **اگر دادهٔ شخصی مشتری وارد دفتر
شود، برای همیشه می‌ماند.** این معمولاً از راه مستقیم نمی‌آید، بلکه از `payload` یا متن
خطا نشت می‌کند.

**قاعده (تأیید کارفرما ۱۴۰۵/۰۵/۳۱):**

> پیش از نوشتن هر ردیف، `payload` و متن خطا از **ماسک PII** می‌گذرند: نام، تلفن، آدرس،
> ایمیل و کد ملی. این همان پیش‌فرض موقتی است که در `DESIGN-PADO.md §۱۵` (تصمیم ۶)
> آمده و اینجا **اجباری** می‌شود، نه اختیاری.

سه نکتهٔ پیاده‌سازی:

- ماسک **پیش از** محاسبهٔ `hash` اعمال شود، وگرنه زنجیرهٔ درستی‌سنجی با دادهٔ ماسک‌نشده
  بسته می‌شود و پاک‌سازی بعدی زنجیره را می‌شکند.
- `Logger.php` امروز راز (کلید/توکن) را ماسک می‌کند ولی **PII را نه**؛ این دو یکی نیستند
  و هر دو لازم‌اند.
- شناسهٔ **مدیر** (`user_id`, `user_login`, `ip`) ماسک **نمی‌شود** — انتساب کل هدف این
  دفتر است. ماسک فقط برای دادهٔ **مشتری** است.

---

## ۹. کارگزار کلید

در `tools.js` این خط هست:

```js
token: await ctx.secret?.( 'git.token' )
```

یعنی **شکل** کارگزار کلید از قبل در ذهن کد بوده — ولی هیچ‌کس `ctx.secret` را پر نمی‌کند.
امروز این قلاب همیشه `undefined` برمی‌گرداند.

پیشنهاد: پرکردن همین قلاب، بدون اختراع چیز تازه.

- پروسهٔ ویرا موقع بالا آمدن یک **توکن کوتاه‌عمر** می‌گیرد (از دروازه، در متغیر محیطی).
- `ctx.secret(ref)` با آن توکن به `POST /wp-json/igbz/v1/vira/secret` می‌زند و مقدار را
  می‌گیرد.
- پنل می‌سنجد که این مدیر حق **این** ارجاع را دارد، مقدار را با `Crypto` باز می‌کند،
  برمی‌گرداند، و در دفتر رویداد ثبت می‌کند.
- مقدار **فقط در لحظهٔ اجرای ابزار** استفاده می‌شود؛ نه در پرامپت می‌رود، نه در دفتر
  گفتگو، نه در خروجی — چون از همان مسیر `redact` رد می‌شود که برای گیت ساخته شد.

مزیتش این است که هم برای کلید SSH کار می‌کند، هم برای درگاه پرداخت و پیامک و صرافی —
همان چیزی که در بند ۱۲ سند هاب پیش‌بینی شده بود.

---

## ۱۰. صف و کار بلندمدت

### ۱۰.۱ دو ضعف امروز

- **شل‌های پس‌زمینه در حافظه‌اند.** `background.js` خروجی را در یک بافر نگه می‌دارد؛ با
  خاموش‌شدن پروسه همه‌چیز می‌رود.
- **`wp_cron` زمان‌بند نیست، یک قلاب روی ترافیک است.** بدون بازدیدکننده اجرا نمی‌شود و
  برای کاری که ده دقیقه طول می‌کشد ساخته نشده.

و در `Schema.php` هیچ جدولی از جنس صف وجود ندارد — نگاه کردم؛ ۷۲ جدول هست، هیچ‌کدام
`jobs` یا `queue` نیست.

### ۱۰.۲ پیشنهاد

جدول `igbz_jobs`: `id · type · payload · state(queued|running|done|failed) · run_at ·
attempts · locked_by · locked_until · created_by · result`.

اجراکننده یک **پروسهٔ دائمی** است (systemd)، نه `wp_cron`. برداشتن کار با
`UPDATE … SET locked_by=? WHERE state='queued' AND (locked_until IS NULL OR locked_until<NOW())
LIMIT 1` و بعد خواندن — الگویی که روی MySQL امن است. `wp_cron` می‌ماند برای همان
کارهای سبک فعلی.

ویرا از این صف دو استفاده می‌کند: کار بلندی که مدیر شروع می‌کند و پنجره را می‌بندد، و
عامل‌های خودکار فروشگاه (بند ۱۱).

---

## ۱۱. پادو و نقش نظارتی ویرا

> ### ⚠️ بازنویسی کامل — تصحیح کارفرما (۱۴۰۵/۰۵/۳۱)
>
> نسخهٔ قبلی این بند بر یک **فرض غلط** بنا شده بود: «عامل فروشگاه = یک پروسهٔ ویرا
> به‌ازای هر فروشگاه، روی سرور ما، با محدودهٔ `/var/www/<store>`». کارفرما این را رد کرد:
>
> > «من چیزی به عنوان عامل فروشگاه تعریف نکردم. ما یه پادو داریم که برای هر فروشگاه
> > مستقله و یه سرویس ابریه. ویرا فقط برای اینا نظارت می‌کنه.»
>
> نسخهٔ قبلی (پروسه به‌ازای فروشگاه، جدول محدوده، «SSH هرگز») **باطل است**.

### ۱۱.۱ پادو چیست — و چه چیزی نیست

**پادو یک سرویس ابری آمادهٔ بیرونی است، مثل Gemini یا ChatGPT.** ما آن را
**نمی‌سازیم و میزبانی نمی‌کنیم**. هر ادمین فروشگاه **خودش** اشتراکش را تهیه می‌کند.

**تنها چیزی که ما می‌سازیم یک «اسکیل» است** — بسته‌ای از دستورالعمل که به آن ابزار
هوشمند می‌گوید روزانه چه کارهایی برای فروشگاه انجام دهد.

| فرض غلط نسخهٔ قبلی | واقعیت |
|---|---|
| یک پروسه به‌ازای هر فروشگاه روی سرور ما | **هیچ پروسه‌ای روی سرور ما نیست** |
| محدودهٔ فایل‌سیستم `/var/www/<store>` | **دسترسی به فایل سرور اصلاً وجود ندارد** |
| «SSH: هرگز» | موضوعیت ندارد |
| ~۱۲۰MB حافظه به‌ازای هر فروشگاه | **صفر** |
| پورت `79xx` در نمودار بند ۳ | وجود ندارد |

### ۱۱.۲ طراحی قالب — بدون هیچ دسترسی به سرور

خواستهٔ ۱۷ کارفرما در `DESIGN-PADO.md` («طراحی قالب سایت فروشگاه») این نگرانی را
پیش آورده بود که سرویس ابری چطور می‌تواند فایل PHP/CSS بنویسد. کارفرما توضیح داد که
اصلاً چنین چیزی لازم نیست:

```
پادو در محیط خودش قالب را طراحی می‌کند
              ↓
      فایل zip را تحویل ادمین می‌دهد
              ↓
ادمین از بخش «قالب‌ها»ی وردپرس نصبش می‌کند
```

یعنی خروجی یک **فایل** است، نه یک تغییر در محل. طبق تصمیم متأخر
`ADR/ADR-0003-THREE-THEME-OUTPUTS.md` فایل می‌تواند یکی از سه قرارداد بلوکی، کلاسیک PHP یا
تمپلیت صفحه‌ساز باشد. در معماری Multisite نصب قالب عملیات مدیر شبکه است؛ PHP فقط پس از
خط لولهٔ بازبینی کد و صفحه‌ساز فقط به‌صورت الحاقی مجاز است. پادو همچنان هیچ تغییر مستقیمی
روی سرور انجام نمی‌دهد.

### ۱۱.۳ «نظارت» ویرا دقیقاً یعنی دو کار

کارفرما دامنهٔ نظارت را صریح محدود کرد — نه بیشتر:

| # | کار | جنس |
|---|---|---|
| ۱ | **به‌روزرسانی اسکیل‌ها** بنا به نیاز | نوشتن/ویرایش محتوای اسکیل |
| ۲ | **گزارش ماهانه از فعالیت فروشگاه‌ها** تا معلوم شود پادو کارش را درست انجام می‌دهد | جمع‌آوری و خلاصه‌سازی |

**آنچه نظارت نیست:** کنترل زندهٔ پادو، دیدن گفتگوهایش، دخالت در کارهای روزمره، یا
هر اتصال دائمی. هیچ‌کدام از این‌ها خواستهٔ کارفرما نیست.

### ۱۱.۴ پیامدها برای استقرار

۱. **سؤال ۴ بند ۱۷ («عامل‌های فروشگاه روی همین سرور یا جدا؟») منتفی است** — چیزی برای
   جادادن وجود ندارد.
۲. **بودجهٔ حافظهٔ بند ۵.۷ سبک‌تر از تخمین است.** آنجا نوشته شده بود «در ۳۲ گیگ جا برای
   عامل‌های فروشگاه باز می‌شود»؛ این دیگر موضوعیت ندارد. ۸ گیگِ ابتدایی راحت‌تر از چیزی
   است که حساب شده بود.
۳. **فاز «ث» در جدول بند ۱ سبک می‌شود** — از «عامل‌های فروشگاه و کنسول نظارت» به فقط
   «مدیریت اسکیل‌ها و گزارش ماهانه».
۴. **تصمیم ۴ بند ۲ («یک پروسه به‌ازای هر فروشگاه») باطل است.**

### ۱۱.۵ آنچه هنوز طراحی نشده

این‌ها موضوع **طراحی پادو** هستند که طبق قرار کارفرما فقط با دستور جداگانهٔ او آغاز
می‌شود:

- **گزارش ماهانه از کجا می‌آید؟** پادو سرویس ابریِ شخصِ ادمین است؛ چه چیزی داده را
  به ویرا می‌رساند — خودِ افزونه از روی داده‌های فروشگاه گزارش می‌سازد، یا ادمین چیزی را
  وارد می‌کند، یا اسکیل طوری نوشته می‌شود که خروجی‌اش قابل جمع‌آوری باشد؟
- **اسکیل‌ها چطور به دست ادمین‌ها می‌رسند و به‌روز می‌شوند؟** توزیع، نسخه‌گذاری، و اینکه
  ادمین چطور نسخهٔ تازه را اعمال می‌کند.
- **قالب اسکیل** برای کدام سرویس‌ها نوشته شود (هر سرویس فرمت خودش را دارد).

### ۱۱.۶ تأیید معماری در فاز ۰۲ — تطبیق ۱۴۰۵/۰۶/۰۶

ADR-0004 جانشین تصمیم اولیهٔ ADR-0002 است. مرجع نسخه‌دار جاری:

```text
ADR/ADR-0004-PADO-ZERNIO-SOCIAL-ARCHITECTURE.md
```

در نسبت با پادو، ویرا runtime عامل، proxy ترافیک DeepInfra، نگه‌دارندهٔ credential مستقل
فروشگاه، یا واسط کلید مرکزی Zernio نیست. مدیریت Playbook و گزارش تجمیعی فقط با قرارداد
حداقل‌داده و بدون گفت‌وگوی خام/secret می‌تواند بعداً طراحی شود. نقش‌های داخلی توسعه و
امنیت ویرا از این تصمیم حق دسترسی به دادهٔ خام پادو نمی‌گیرند. هیچ تغییری در پوشهٔ
`vira/` برای ثبت این تصمیم انجام نشده است.

---

## ۱۲. سرویس و نگه‌داری

### ۱۲.۱ قفل شنود — یک تغییر کوچک ولی واجب

امروز `--host 0.0.0.0` کار می‌کند. تصمیم ۲ می‌گوید در استقرار سرور باید **غیرفعال** باشد،
نه اینکه فقط پیش‌فرض نباشد. پیشنهاد:

```
VIRA_BIND=local    →  --host نادیده گرفته می‌شود و همیشه 127.0.0.1
```

و ویرا موقع بالا آمدن یک خط چاپ کند که روی چه چیزی گوش می‌دهد، تا اشتباه ساکت نماند.
این همان درسی است که از `npm install -g .` گرفتیم: تنظیم غلطی که چیزی نمی‌گوید، بدترین
نوع تنظیم غلط است.

### ۱۲.۲ واحد systemd

```ini
[Service]
User=vira
Environment=VIRA_BIND=local
Environment=VIRA_SHARED=/var/lib/vira/shared
Environment=VIRA_HOME=/var/lib/vira/admins/%i
ExecStart=/usr/bin/node /srv/vira/src/cli.js --port %i --no-open

NoNewPrivileges=yes
PrivateTmp=yes
ProtectSystem=strict
ProtectHome=yes
ReadWritePaths=/var/lib/vira /var/www/stage /srv/igbz-wp
RestrictAddressFamilies=AF_INET AF_INET6 AF_UNIX
MemoryMax=1G
```

`ReadWritePaths` **دومین** حصار است، مستقل از بند ۶. اگر روزی محدوده‌ها باگ داشتند،
سیستم‌عامل هنوز جلوی نوشتن بیرون از این فهرست را می‌گیرد. دو حصار مستقل، از یک حصارِ
دقیق بهترند.

### ۱۲.۳ ارتقا و پشتیبان

- ارتقا: `git pull && npm ci && systemctl restart vira@*`. هرگز `npm install -g .` —
  دلیلش در README ویرا هست و یک بار واقعاً وقت تلف کرد.
- پشتیبان: `hub.json` **کلید دارد** و باید رمزنگاری‌شده پشتیبان گرفته شود؛ `sessions/` و
  `checkpoints/` معمولی؛ جدول `igbz_audit` هرگز حذف نمی‌شود.
- سلامت: یک `GET /api/state` روی هر پورت. اگر جواب نداد، پروسه ریستارت شود.

---

## ۱۳. گردش کار توسعه و نقش ویرا در آن — ✅ تصمیم گرفته شد (۱۴۰۵/۰۵/۳۱)

### ۱۳.۱ گفتهٔ کارفرما

> «تمام کارها بر روی گیت‌هاب انجام می‌شه. توسعه‌دهنده‌ها بر روی سیستم‌های خودشون کار
> می‌کنند، بر روی برنچ خودشون پوش می‌کنند، مدیر ارشد بعد از بازبینی اجازهٔ مرج می‌ده.»
>
> «بررسی تمام تغییراتی که توسعه‌دهنده‌ها انجام می‌دن توسط ویرا انجام می‌شه که گزارش کار
> رو به مدیر می‌ده تا مدیر تصمیم بگیره که مرج انجام بشه یا نه. در ضمن ویرا خودش یک محیط
> آزمایشی داره بر روی سرور اصلی.»

### ۱۳.۲ گردش کار

```
توسعه‌دهنده (سیستم خودش)  →  push به برنچ خودش در گیت‌هاب
                                      ↓
                          ویرا تغییرات را بررسی می‌کند
                                      ↓
                            گزارش به مدیر ارشد
                                      ↓
                        مدیر ارشد: merge بله / خیر
```

### ۱۳.۳ نقش ویرا — بازبین، نه توسعه‌دهنده

این مهم‌ترین نکتهٔ این بند است و چند فرض قبلی را اصلاح می‌کند:

| | نقش ویرا |
|---|---|
| ✅ **بررسی تغییرات** برنچ‌های توسعه‌دهنده‌ها | کار اصلی |
| ✅ **گزارش به مدیر ارشد** تا او تصمیم merge بگیرد | خروجی کار |
| ✅ **محیط آزمایشی روی سرور اصلی** | جای امتحان کردن |
| ❌ تصمیم merge | **با مدیر ارشد است، نه ویرا** |

**ویرا حق merge ندارد.** تصمیم نهایی همیشه انسانی است — این همان «تأیید انسانی» است که
در تصمیم‌های مبنا (بند ۲) آمده و اینجا مصداق مشخصش معلوم شد.

### ۱۳.۴ محیط‌ها — جواب سؤال ۵ بند ۱۷

| محیط | کجاست |
|---|---|
| **توسعه** | روی **سیستم خودِ توسعه‌دهنده‌ها**، نه سرور |
| **آزمایشی** | روی **سرور اصلی** — ویرا خودش آن را دارد |
| **زنده** | روی **سرور اصلی** |

یعنی یک ماشین، با محیط آزمایشی و زنده کنار هم. نه SSH به ماشین دیگر لازم است، نه
`/var/www/dev` روی سرور معنا دارد.

### ۱۳.۵ پیامدها

۱. **دسترسی `igbz_vira_dev` بازتعریف می‌شود.** «محیط توسعه روی سرور» وجود ندارد؛ آنچه
   هست دسترسی ویرا به **مخزن گیت** برای بررسی برنچ‌ها (`/srv/igbz-wp` در `scopes.json`).
۲. **جداسازی آزمایشی از زنده روی یک ماشین حیاتی است.** چون هر دو روی سرور اصلی‌اند، مرز
   بین `stage` و `shop` در `scopes.json` تنها چیزی است که جلوی خراب‌شدن سایت واقعی را
   می‌گیرد. این مرز باید سخت‌گیرانه اعمال شود.
۳. **ابزار گیت ویرا (`/api/git`) هستهٔ این گردش کار است** و باید بتواند برنچ‌ها و
   diff بین آن‌ها را بخواند. امروز این وجود دارد (`vcs.diff`, `vcs.branches`, `vcs.log`).
۴. **دسترسی به گیت‌هاب لازم است** — نه فقط مخزن محلی. برای دیدن برنچ‌های
   توسعه‌دهنده‌ها ویرا باید بتواند `fetch` بزند.

### ۱۳.۶ آنچه هنوز طراحی نشده

- **قالب «گزارش بازبینی»**: مدیر ارشد دقیقاً چه می‌بیند؟ خلاصهٔ تغییرات، ریسک‌ها، نتیجهٔ
  تست‌ها، پیشنهاد merge؟
- **آیا بررسی خودکار است یا با درخواست مدیر؟** با هر push، یا وقتی مدیر بخواهد؟
- **رابطهٔ محیط آزمایشی با برنچ‌ها**: ویرا برنچ را روی محیط آزمایشی مستقر می‌کند تا
  عملاً امتحان شود، یا فقط کد را می‌خواند؟
- **احراز هویت گیت‌هاب** برای ویرا (توکن، دسترسی‌ها، و اینکه در کارگزار کلید کجا می‌نشیند).

---

## ۱۴. ویرا مسئول امنیت پروژه — تصمیم کارفرما (۱۴۰۵/۰۵/۳۱)

> **کار طراحی‌نشده.** اینجا فقط تصمیم و دامنه‌اش ثبت می‌شود تا گم نشود؛ طراحی و پیاده‌سازی
> با دستور جداگانهٔ کارفرما.

### ۱۴.۱ گفتهٔ کارفرما

> «ویرا مسئول امنیت پروژه است. تمام اجزای بک‌اند باید هر درخواست مشکوکی رو به ویرا گزارش
> کنند، مثل تعداد درخواست‌های خیلی زیاد و غیراستاندارد در یک محدودهٔ زمانی. برای ویرا هم
> باید تنظیمات و اسکیل‌های خیلی حرفه‌ای و قوی در مورد امنیت طراحی بشه.»

### ۱۴.۲ چرخش نقش — و چرا مهم است

تا امروز اسناد، ویرا را **موضوع** امنیت می‌دیدند: چیزی که باید پشت دروازه قفل شود (بند ۴)،
محدوده بگیرد (بند ۶)، و کارهایش در دفتر ثبت شود (بند ۸). این تصمیم یک نقش **فعال** اضافه
می‌کند: ویرا **ناظر امنیتی** هم هست.

هر دو نقش هم‌زمان معتبرند و هیچ‌کدام دیگری را لغو نمی‌کند. یعنی ویرا هم زندانی است هم
نگهبان — و این تناقض نیست، چون در دو لایهٔ متفاوت عمل می‌کنند.

**و این نقش فعال، منفعل نیست:** ویرا **لایهٔ سیزدهم** دفاع است و **اختیار سد کردن دارد**
(بند ۱۴.۵).

### ۱۴.۳ جریان گزارش‌دهی

```
اجزای بک‌اند (افزونه، REST، درگاه‌ها، OTP، وبهوک‌ها)
                    ↓  رویداد مشکوک
              کانال امنیتی ویرا
                    ↓
        تحلیل · همبسته‌سازی · تشخیص الگو
                    ↓
            هشدار به مدیر ارشد
```

**قاعده: «تمام اجزای بک‌اند».** یعنی گزارش‌دهی امنیتی نباید در چند نقطهٔ منتخب باشد، بلکه
باید مسیر مشترکی داشته باشد که همهٔ ماژول‌ها از آن استفاده کنند — وگرنه هر ماژول تازه‌ای
یک نقطهٔ کور می‌سازد.

### ۱۴.۴ نمونهٔ صریح کارفرما و خانوادهٔ آن

کارفرما یک نمونه داد: **«تعداد درخواست‌های خیلی زیاد و غیراستاندارد در یک محدودهٔ زمانی».**
خانوادهٔ طبیعی‌اش که باید در طراحی پوشش داده شود:

| نشانه | نمونهٔ ملموس در همین پروژه |
|---|---|
| نرخ غیرعادی درخواست | هجوم به مسیرهای REST یا صفحهٔ ورود |
| تلاش ناموفق پیاپی | OTP (امروز `otp.max_attempts` دارد) · ورود مدیر |
| الگوی غیراستاندارد | ترتیب فراخوانی‌ای که کاربر واقعی تولید نمی‌کند |
| دستکاری مالی | تکرار درخواست پرداخت، تلاش برای شکستن idempotency |
| وبهوک مشکوک | امضای نامعتبر، مبدأ ناشناس، تکرار پیام |
| رفتار غیرعادی خودِ ویرا | حلقهٔ تلاش روی مسیر پولی (بند ۱۴ سند `DESIGN-PROVIDER-UI.md`) |

⚠️ **نکتهٔ مهم:** امروز مکانیزم‌های پراکنده‌ای هست (`otp.max_attempts`، ماسک راز در
`Logger.php`) ولی **هیچ‌کدام به جایی گزارش نمی‌دهند** — محلی تصمیم می‌گیرند و رد می‌کنند.
تفاوت این تصمیم همین است: **تشخیص محلی، ولی دید مرکزی.** ده تلاش ناموفق در ده ماژول
مختلف، محلی بی‌معنی است و مرکزی یک الگوی روشن.

### ۱۴.۵ جایگاه ویرا: لایهٔ سیزدهم — ✅ تصمیم کارفرما (۱۴۰۵/۰۵/۳۱)

> ### ⚠️ نسخهٔ قبلی این بند باطل شد
>
> نسخهٔ اول نوشته بود «ویرا ناظر و تحلیل‌گر است، **نه دیوار آتش**» و سد کردن درخواست را
> از اختیارش بیرون گذاشته بود. **کارفرما این را رد کرد:**
>
> > «ما ۱۳ لایهٔ امنیتی خواهیم داشت که در بخش امنیت توضیح می‌دم. ویرا لایهٔ آخر هست. فقط
> > درخواست‌های خاص و فعالیت‌های مشکوک بهش ارجاع داده می‌شه، که برای هر کدوم تنظیمات و
> > اسکیل‌های پیش‌بینی‌شده داره. پس **می‌تونه جلوی درخواست رو بگیره**.»
>
> دلیل اولِ آن مرز از قبل باطل شده بود (فرض خاموشی، بند ۵.۰). دلیل دومش — «تأخیر، چون
> تحلیل هوشمند کند است» — با این تصمیم **بی‌موضوع می‌شود**: ویرا در مسیر **همهٔ**
> درخواست‌ها نمی‌نشیند، فقط چیزی که دوازده لایهٔ قبلی به او **ارجاع** داده‌اند.

### ۱۴.۵.۱ معماری ۱۳ لایه

```
لایه‌های ۱ تا ۱۲   →  دفاع سریع و خودکار (در بخش امنیت تعریف می‌شوند)
                          ↓  فقط موارد خاص و مشکوک ارجاع می‌شوند
لایهٔ ۱۳ = ویرا    →  تحلیل هوشمند + تصمیم + امکان سد کردن
```

**نکتهٔ کلیدی که تناقض ظاهری را حل می‌کند:** ایراد «ویرا برای مسیر بحرانی کند است» وقتی
درست بود که فرض می‌شد ویرا در مسیر **هر** درخواست می‌نشیند. در معماری لایه‌ای، ویرا فقط
**ته قیف** است — حجم ورودی‌اش کم است، پس هزینهٔ تحلیل هوشمند قابل پرداخت می‌شود و
گلوگاه نمی‌سازد.

### ۱۴.۵.۲ اختیارات ویرا در این جایگاه

| ✅ کار ویرا | توضیح |
|---|---|
| دریافت ارجاع از لایه‌های پیشین | نه همهٔ ترافیک — فقط موارد خاص و مشکوک |
| تحلیل، همبسته‌سازی، تشخیص الگو | همان دید مرکزیِ بند ۱۴.۴ |
| **سد کردن درخواست** | ✅ **دارد** — طبق تصمیم کارفرما |
| اقدام طبق اسکیل و تنظیمات از پیش تعریف‌شده | هر نوع رخداد، پاسخ پیش‌بینی‌شدهٔ خودش |
| هشدار به مدیر ارشد | همیشه، حتی وقتی خودش اقدام کرده |

**قاعده‌ای که همچنان معتبر است:** ویرا طبق **اسکیل و تنظیمات از پیش تعریف‌شده** عمل
می‌کند، نه با قضاوت آزاد در لحظه. این با بند ۸ خواسته‌های `DESIGN-PADO.md` هم‌راستاست
(«روند کاری از پیش تعریف‌شده = مزیت رقابتی، جلوگیری از هر نوع اشتباه»).

### ۱۴.۵.۳ ویرا «فکر نمی‌کند» — روند از پیش تعریف‌شده را اجرا می‌کند

> ### ⚠️ تصحیح کارفرما (۱۴۰۵/۰۵/۳۱) — نگرانی «مهلت زمانی» باطل شد
>
> نسخهٔ قبلی این بند سه نگرانی مطرح کرده بود: مهلت زمانی، رفتار هنگام تردید
> (fail-open/fail-closed)، و معلق‌ماندن درخواست. **کارفرما هر سه را رد کرد:**
>
> > «من می‌گم روند از پیش تعیین‌شده داره، تو می‌گید معطل بمونه. چطور می‌تونه معطل بمونه؟
> > مثلاً براش تو اسکیل تعریف شده: اول منبع درخواست رو شناسایی کن، اگه شناسایی کردی
> > اجازه بده، اگه نکردی به منبع پیام بفرست که از طریق مثلاً یک تأیید دوفاکتوری درخواست
> > رو تأیید کنه، اگه کرد باز اجازه می‌ده، در غیر این صورت اجازه نمی‌ده. **ویرا فقط با
> > سرعت چیزی که براش تعریف شده رو اجرا می‌کنه.**»
>
> **اشتباه تحلیل:** عامل «تحلیل» را به معنای استدلال مدل زبانی گرفته بود — که کند است و
> مهلت می‌خواهد. ولی آنچه در لایهٔ ۱۳ اجرا می‌شود یک **درخت تصمیمِ از پیش نوشته‌شده**
> است، نه استدلال آزاد. اجرایش در حد میلی‌ثانیه است.

**نمونهٔ کارفرما، به‌شکل روند:**

```
درخواست مشکوک از لایهٔ ۱۲ ارجاع می‌شود
        ↓
منبع درخواست را شناسایی کن
        ├─ شناخته شد        →  اجازه بده
        └─ شناخته نشد       →  به منبع پیام بفرست: تأیید دوفاکتوری
                                    ├─ تأیید کرد   →  اجازه بده
                                    └─ تأیید نکرد  →  اجازه نده
```

**سه نتیجه‌ای که از این تصحیح می‌آید:**

۱. **«معطل ماندن» موضوعیت ندارد.** چیزی برای فکر کردن وجود ندارد؛ فقط مسیری که از قبل
   مشخص شده دنبال می‌شود.
۲. **«رفتار هنگام تردید» هم موضوعیت ندارد.** تردیدی در کار نیست — هر شاخهٔ درخت جواب
   صریح خودش را دارد. آنچه عامل «fail-open یا fail-closed» می‌نامید، در این مدل
   **همان برگ‌های درخت** است که در اسکیل نوشته می‌شوند.
۳. **انتظار برای تأیید دوفاکتوری، کندیِ سیستم نیست.** در آن شاخه سیستم منتظر نیست؛
   **کاربر** باید جواب بدهد. این ذاتاً بخشی از خودِ روند است، نه تأخیر ناخواسته.

**قاعده‌ای که از این بیرون می‌آید و باید در طراحی رعایت شود:**

> در لایهٔ امنیتی، ویرا **مجری** است نه **متفکر**. هر رخدادِ ممکن باید در اسکیل شاخهٔ
> خودش را داشته باشد. اگر روزی موردی پیش بیاید که در اسکیل تعریف نشده، راه‌حل **اضافه
> کردن شاخه به اسکیل** است، نه واگذاشتنِ تصمیم به قضاوت لحظه‌ایِ مدل.

این مستقیماً همان بند ۸ خواسته‌های `DESIGN-PADO.md` است: «ابزارهای دیگر پلتفرم باز
دارند و نیاز به بازتعریف و اشکال‌زدایی دوباره و دوباره دارند؛ اما ما یک روند خاص و کارهای
مشخص داریم که می‌توان دقیقاً در خود ابزار مشخص کرد و **از هر نوع اشتباهی جلوگیری کرد**.»

### ۱۴.۵.۴ آنچه به این سند مربوط نیست

خودِ ۱۳ لایه، فهرست روندهای امنیتی، درخت تصمیم هر رخداد، و مرز اقدام‌های ماندگار
(مثل مسدودکردن دائمی IP) **موضوع این سند نیستند**.

کارفرما (۱۴۰۵/۰۵/۳۱) صریح گفت:

> «این مواردی که داری می‌پرسی اصلاً ربطی به موضوع الان نداره. این‌ها در **بخش امنیت و
> طراحی اسکیل‌ها و تنظیمات ویرا** بررسی می‌شه.»

آنچه در این سند ثبت می‌شود فقط **جایگاه** ویراست: لایهٔ سیزدهم، ارجاع‌گیرنده از
لایه‌های پیشین، مجریِ روند از پیش تعریف‌شده، با اختیار سد کردن. همین و بس.

### ۱۴.۶ اسکیل‌ها و تنظیمات امنیتی ویرا

خواستهٔ صریح: «تنظیمات و اسکیل‌های خیلی حرفه‌ای و قوی در مورد امنیت».

این با بند ۵ خواسته‌های `DESIGN-PADO.md` هم‌راستاست («نصب اسکیل‌های آماده، نه ساختن
اسکیل»). یعنی سرفصل‌های شناخته‌شدهٔ صنعت — بازبینی امنیتی کد، تحلیل لاگ، پاسخ به رخداد،
سخت‌سازی سرور — به شکل اسکیل بیایند، نه اینکه از صفر اختراع شوند.

**نقطهٔ اتصال روشن:** طبق بند ۱۳، ویرا **بازبین** تغییرات توسعه‌دهنده‌هاست. اسکیل بازبینی
امنیتی دقیقاً همان‌جا می‌نشیند — یعنی گزارش بازبینی باید بخش امنیتی داشته باشد، نه فقط
درستی عملکرد.

### ۱۴.۷ آنچه باید طراحی شود

- **قرارداد رویداد امنیتی:** شکل داده، سطح‌بندی شدت، و مسیر رسیدنش به ویرا (REST؟ جدول؟
  صف؟). با توجه به بند ۵.۳ که ویرا ممکن است خاموش باشد، **گزارش‌ها باید جایی بمانند تا
  ویرا بالا بیاید** — نه اینکه از دست بروند.
- **نقطهٔ مشترک گزارش‌دهی در افزونه** تا هر ماژول تازه خودبه‌خود پوشش بگیرد.
- **آستانه‌ها**: چه چیزی «خیلی زیاد» است؟ باید تنظیم‌شدنی باشد، نه ثابت در کد.
- **رابطهٔ این کانال با `igbz_audit`** (بند ۸): یک جدول یا دو تا؟ دفتر رویداد **کار مدیران**
  را ثبت می‌کند؛ این یکی **رفتار مشکوک بیرونی** را — احتمالاً دو چیز متفاوت‌اند.
- **ضدنویز:** یک حملهٔ ساده می‌تواند هزاران رویداد بسازد. اگر هر کدام یک هشدار شود، مدیر
  ارشد هشدارها را نادیده می‌گیرد و کل سیستم بی‌اثر می‌شود. تجمیع و کف‌گیری لازم است.
- **دسترسی تازه**: احتمالاً `igbz_vira_security` کنار بقیهٔ دسترسی‌های بند ۵.۵.

---

## ۱۵. یک تذکر نام‌گذاری

در افزونه از قبل چیزی به نام **Hub** هست: ماژول `Modules/Hub` و فضای نام
`igbz-hub/v1` که **هاب فروشگاه‌ها** (صفحهٔ فرود، فهرست فروشگاه‌ها، پلن‌ها) است.

«هاب پرووایدر» چیز کاملاً دیگری است. در کد و در مسیرها باید از هم جدا بمانند:

- مسیرهای ویرا زیر `igbz/v1/vira/...` بروند، **نه** `igbz-hub/v1`.
- در متن فارسی، هرجا ابهام هست بنویسیم «هاب مدل» یا «هاب پرووایدر»، نه «هاب» خالی.

الان هزینه‌اش صفر است؛ شش ماه دیگر که کسی دنبال باگ می‌گردد، گران می‌شود.

---

## ۱۶. چطور می‌سنجیم که کار می‌کند

هر بند، یک آزمون که یا سبز می‌شود یا نه:

| # | آزمون |
|---|---|
| ۱ | `curl http://<ip-سرور>:7801/` از بیرون → اتصال برقرار نشود (نه ۴۰۳، اصلاً پورتی باز نباشد) |
| ۲ | درخواست به `/vira/api/state` بدون کوکی وردپرس → ۴۰۱ |
| ۳ | مدیر بدون `igbz_vira_prod` روی محیط زنده → رد، و رد در دفتر رویداد ثبت شود |
| ۴ | دو مدیر هم‌زمان: هیچ‌کدام پیام دیگری را نبیند و نتواند تأییدش را بزند |
| ۵ | مدیر با `manage_options` ولی بدون `igbz_vira_ops` → ابزار SSH را اصلاً نبیند |
| ۶ | `ssh_run` به میزبانی بیرون از فهرست سفید → رد، حتی اگر مدل اصرار کند |
| ۷ | خواندن `/etc/shadow` از هر محدوده‌ای → رد |
| ۸ | خواندن `wp-config.php` → موفق، ولی نمک‌ها و رمز دیتابیس ماسک |
| ۹ | کلید در هیچ پرامپت، هیچ دفتر گفتگو و هیچ خروجی ابزاری دیده نشود (grep روی رونوشت) |
| ۱۰ | یک ردیف دفتر رویداد را دستی عوض کنیم → سنجش زنجیره خرابی را گزارش کند |
| ۱۱ | کار بلند شروع شود، مرورگر بسته شود → کار تمام شود و نتیجه‌اش بماند |
| ۱۲ | پروسهٔ مدیر با kill کشته شود → درخواست بعدی خودش بالا بیاورد و گفتگو سر جایش باشد |
| ۱۳ | سقف روزانهٔ مشترک با دو پروسهٔ هم‌زمان → مجموع درست شمرده شود |
| ۱۴ | ده مدیر متصل با پنجرهٔ باز → ورکرهای php-fpm آزاد بمانند |

بند ۱۴ مهم‌ترین آزمون این سند است، چون تنها راهی است که می‌فهمیم انتخاب بند ۴ درست بود.

---

## ۱۷. آنچه باید کارفرما تصمیم بگیرد

این‌ها را نمی‌شود از روی کد فهمید:

۱. ~~**وب‌سرور چیست؟**~~ — ✅ **بسته شد (۱۴۰۵/۰۵/۳۱): nginx، مسیر ۴.۲.** سرور هنوز گرفته
   نشده و انتخاب آزاد بود؛ اول VPS و بعد سرور اختصاصی، هر دو با دسترسی به پیکربندی.
   دلایل و قیدها در کادر تصمیم بند ۴.۴.
۲. ~~**مدیر ارشد کیست؟**~~ — ✅ **نیمه‌بسته (۱۴۰۵/۰۵/۳۱): «یک نفر الان، چند نفر بعداً»
   (گزینهٔ ب).** معماری چندنفرهٔ بند ۵ پیاده می‌شود ولی روز اول یک مدیر فعال است.
   **باقی‌مانده:** شناسهٔ کاربر وردپرسِ آن یک نفر هنوز اعلام نشده، و تقسیم اختیارات وقتی
   نفر دوم اضافه شود. کادر تصمیم در ابتدای بند ۵.
۳. ~~**حافظهٔ سرور چقدر است؟**~~ — ✅ **بسته شد (۱۴۰۵/۰۵/۳۱): شروع با ۸ گیگابایت، با
   مسیر ارتقا تا ۳۲ گیگابایت.** بودجهٔ حافظه و پیامدهایش در بند ۵.۷.
۴. ~~**عامل‌های فروشگاه روی همین سرور اجرا می‌شوند یا جدا؟**~~ — ❌ **منتفی شد
   (۱۴۰۵/۰۵/۳۱).** سؤال بر فرض غلط بنا شده بود؛ پادو سرویس ابریِ بیرونی است و چیزی
   برای جادادن روی سرور وجود ندارد. بند ۱۱.
۵. ~~**محیط‌ها روی یک ماشین‌اند یا سه ماشین؟**~~ — ✅ **بسته شد (۱۴۰۵/۰۵/۳۱).** توسعه روی
   سیستم خودِ توسعه‌دهنده‌هاست (نه سرور)؛ محیط آزمایشی و زنده هر دو روی **سرور اصلی**.
   گردش کار و نقش ویرا در بند ۱۳.
۶. ~~**عدد بودجهٔ ماهانهٔ توکن**~~ — ❌ **منتفی شد (۱۴۰۵/۰۵/۳۱).** سؤال بر فرض غلط بنا
   شده بود. کار روزمره از **هاب** و توکن‌های رایگان می‌گذرد (الگوی OmniRoute)؛ مسیر پولی
   فقط اضطراری است و **عمداً سقف ندارد** — «در موارد اضطراری نمی‌شه ریسک کرد». پرووایدر
   شخصیِ هر مدیر همان «پروفایل تک‌نفره» است و از هاب نمی‌گذرد. جایش دو کار طراحی‌نشده
   آمد (محافظ ضدحلقه، و گردش شارژ ماهانه): `vira/DESIGN-PROVIDER-UI.md §۱۴`.
۷. ~~**مدت نگه‌داری دفتر رویداد**~~ — ✅ **بسته شد (۱۴۰۵/۰۵/۳۱): هرگز پاک نشود**، با
   بایگانی سالانه اگر حجم مسئله شد. دادهٔ شخصی مشتری پیش از ثبت **ماسک اجباری** می‌شود.
   دلایل و قواعد در بند ۸.۴ و ۸.۵.

---

## ۱۸. ارجاع‌ها

- `DESIGN-PADO.md §۱۵` — تصمیم‌های استقرار (مبنای این سند)
- `DESIGN-PADO.md §۱۶` — اتصال گیت
- `vira/DESIGN-PROVIDER-HUB.md` — هاب پرووایدر (ساخته‌شده، ۰.۷)
- `API-KEYS.md` — سی‌ونه کلید و جایشان
- `igbz-suite/src/Support/Capabilities.php` · `Logger.php` · `Cron.php` · `Crypto.php`
- `vira/src/tools.js` (`resolveInside`, `ctx.secret`) · `permissions.js` ·
  `server.js` (`broadcast`, `/api/permission`) · `background.js` · `config.js`
