# فاز ۱۰ — سئو، ثبت خودکار در موتورهای جستجو و شبکه‌های تبلیغاتی: طراحی

**وضعیت:** طراحی تأییدشده توسط کارفرما. مبنای آن: راهکارهای سئو در اسناد nopCommerce
(تولید متا، فید یکتانت/تپسل، تریبون) + فهرست تکمیلی کارفرما (ثبت خودکار در Google Search
Console، Google Indexing، ایمالز، ترب، Bing Webmaster، Google Business Profile).

> **الگوی حاکم:** «ثبت خودکار هنگام ساخت فروشگاه» = در **Provisioning** (ساخت فروشگاه) یک گام
> «ثبت در سرویس‌های وب» اجرا می‌شود. هر سرویس یک آداپتور تنظیم‌پذیر دارد (مانند بقیهٔ پروژه).

---

## ۱. ثبت خودکار هنگام ساخت فروشگاه (Onboarding)

هنگامی که فروشگاه ساخته می‌شود (و دامنه/آدرس مشخص است)، پادو/سیستم به‌صورت خودکار در
این سرویس‌ها ثبت می‌کند:

| سرویس | روش | نکته |
|---|---|---|
| **Google Search Console** | API (Site Verification + sitemap submit) | نیازمند OAuth/سرویس‌اکانت گوگل |
| **Google Indexing** | Google Indexing API (تازه‌سازی/حذف URL) | برای ایندکس سریع صفحات |
| **ایمالز (Emalls)** | ثبت فروشگاه + فید محصول | API ایمالز |
| **ترب (Torob)** | ثبت لینک فید JSON (از قبل داریم) + ثبت فروشگاه | ترب فید را خودش می‌خواند |
| **Bing Webmaster** | ثبت سایت + sitemap | API بینگ |
| **Google Business Profile** | ثبت کسب‌وکار/فروشگاه | نیازمند تأیید؛ راهنمایی پادو |

- هر سرویس = یک **آداپتور** (`WebPresenceAdapterInterface`): `register(site)` و
  `submit_sitemap(site)`.
- **کلیدها/اعتبارنامه‌ها** در تنظیمات اپراتور (سراسری) — چون ثبت برای همهٔ فروشگاه‌هاست.
- **وضعیت ثبت** برای هر فروشگاه ذخیره می‌شود (کدام سرویس‌ها ثبت شدند، خطاها).
- **پادو** در سرویس‌هایی که نیاز به تأیید انسانی دارند (Google Business Profile)، ادمین را
  راهنمایی می‌کند.

## ۲. تولید و ذخیرهٔ متای خودکار (از اسناد nop)

- `SeoService` موجود: Title/Description/هشتگ از نام و توضیح محصول.
- **اصلاح مهم (رفع نقص nop):** متای تولیدشده **روی محصول ووکامرس ذخیره می‌شود**
  (`update_post_meta`: `_yoast_wpseo_title` در صورت وجود Yoast/RankMath، یا متای اختصاصی
  `igbz_seo_title`/`igbz_seo_description`)، نه فقط نمایش.
- دکمهٔ «تولید و ذخیرهٔ متا» در صفحهٔ igbz-seo + خودکار هنگام ثبت محصول.

## ۳. فیدهای تبلیغاتی (از اسناد nop)

- **فید یکتانت/تپسل** از کاتالوگ واقعی (`?igbz_feed=yektanet|tapsell`) ✅ موجود — تأیید شد که
  از کاتالوگ واقعی است (نه نمونهٔ Hardcode — رفع مورد ۱۵ ممیزی nop).
- **تریبون** (رپورتاژ) ✅ موجود (`AdNetworkService`).

## ۳ب. پرامپت‌های سئو برای پادو

مجموعهٔ «Free SEO Prompts» (۱۰ پرامپت حرفه‌ای در ۵ دسته: عنوان بلاگ، متا دیسکریپشن،
اوت‌لاین بر اساس قصد، ممیزی داخل صفحه، لینک‌سازی داخلی) در `PROMPTS-SEO-PADO.md`
ذخیره شده و باید به‌عنوان **Skill سئوی پادو** در دسترس باشد — با جایگذاری دادهٔ واقعی
فروشگاه به‌جای `[براکت‌ها]`.

## ۴. نقش پادو

- **در ثبت:** راهنمایی/اتوماسیون در هر سرویس؛ پیگیری وضعیت.
- **پس از ثبت:** نظارت بر ایندکس (Search Console)، پیشنهاد بهبود سئو، به‌روزرسانی sitemap،
  پاسخ به هشدارهای سرویس‌ها.
- **گزارش:** «وضعیت حضور وب» فروشگاه (کدام سرویس‌ها ثبت/فعال/خطا).

## ۵. جدول‌ها/تنظیمات
| مورد | محتوا |
|---|---|
| `ig_web_presence` | وضعیت ثبت هر فروشگاه در سرویس‌ها: `tenant_id`، `service`، `status`، `detail`، `updated_at` |
| تنظیمات سراسری | `webpresence.google_*`، `webpresence.bing_*`، `webpresence.emalls_*`، `webpresence.basalam?` (نه) |

## ۶. اندپوینت‌ها/کرون
```
POST /igbz/v1/admin/web-presence/register    (اجرای ثبت برای فروشگاه)
GET  /igbz/v1/admin/web-presence/status      (وضعیت)
کرون: به‌روزرسانی sitemap + بررسی وضعیت ایندکس
```

## ۷. فازبندی پیاده‌سازی (پس از تأیید)
| گام | محتوا |
|---|---|
| ۱ | `WebPresenceAdapterInterface` + آداپتورهای Google Search Console / Indexing / Bing |
| ۲ | آداپتورهای ایمالز / ترب / Google Business Profile (+ راهنمای پادو) |
| ۳ | ذخیرهٔ واقعی متا روی محصول (رفع نقص nop) |
| ۴ | جدول `ig_web_presence` + کرون + گزارش |
| ۵ | تست‌ها + تأیید زنده |
