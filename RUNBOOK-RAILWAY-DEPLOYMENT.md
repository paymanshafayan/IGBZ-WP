# راهنمای استقرار و اجرای IGBZ-WP روی Railway

تاریخ آخرین بازبینی: ۳۰ اوت ۲۰۲۶

این سند روش مرجع استقرار پروژه روی Railway است و برای جلوگیری از تکرار خطاهای راه‌اندازی، healthcheck، مهاجرت دیتابیس و سکرت‌ها نوشته شده است.

## ۱. معماری استقرار

- سرویس وب: تصویر رسمی `wordpress:php8.2-apache` با افزونهٔ IGBZ-WP و WooCommerce.
- دیتابیس: سرویس MySQL جداگانه با ولوم پایدار.
- ولوم وب: مسیر `/var/www/html/wp-content` برای افزونه‌ها، قالب‌ها، آپلودها و فایل‌های پایدار.
- منبع کد: شاخهٔ متصل به محیط Railway؛ پیش از هر Deploy باید Commit موردنظر در همان شاخه پوش شده باشد.
- پردازش PHP و workerها داخل سرویس وب انجام می‌شود؛ برای محیط تولید، اجرای worker/cron باید با متغیر `IGBZ_SERVER_CRON=1` فعال شود.

## ۲. اتصال مخزن و شاخه

۱. در سرویس، به مسیر `Settings → Source` بروید.
۲. مخزن `paymanshafayan/IGBZ-WP` را انتخاب کنید.
۳. شاخهٔ ثابت این جلسه را متصل کنید:

```text
arena/01a052e8-igbz-wp
```

۴. گزینهٔ `Auto deploys when pushed to GitHub` را فعال نگه دارید.
۵. در تب `Deployments`، پیش از پذیرش هر استقرار، مقدار `Source Commit` را با Commit پوش‌شده تطبیق دهید.

**هشدار:** اتصال صحیح شاخه به‌تنهایی کافی نیست؛ ممکن است Deployment قبلی یا snapshot قدیمی هنوز در حال اجرا باشد. پس از هر تغییر کد، باید یک Deployment جدید با Commit جدید ساخته شود.

## ۳. متغیرهای اجباری سرویس وب

در `Variables` همان سرویس و همان محیط، این متغیرها را ثبت کنید. مقدار سکرت‌ها در چت، مخزن یا لاگ قرار نمی‌گیرد.

```text
WP_ADMIN_PASSWORD=<رمز قوی و یکتا>
WP_ADMIN_USER=admin
WP_ADMIN_EMAIL=<ایمیل مدیر>
IGBZ_SERVER_CRON=1
```

برای دیتابیس عمومی یا سفارشی:

```text
IGBZ_DB_TLS=1
```

برای شبکهٔ خصوصی Railway معمولاً `IGBZ_DB_TLS=0` کافی است، چون ارتباط شبکهٔ خصوصی Railway رمزنگاری‌شده است.

برای آزمون مرحله‌ای providerها:

```text
WP_ENVIRONMENT_TYPE=staging
DEEPINFRA_API_KEY=<secret>
ZERNIO_API_KEY=<secret>
```

- کلیدها را به‌صورت `Sealed` ذخیره کنید.
- کلیدها نباید در Dockerfile، فایل `.env`، PHP، JavaScript، دیتابیس یا لاگ ذخیره شوند.
- `WP_ENVIRONMENT_TYPE=staging` عمداً لازم است تا fallback متغیرمحیطی فقط در محیط مرحله‌ای فعال باشد.
- در تولید، اولویت زرنیو با کلید رمزنگاری‌شده‌ای است که ادمین از پنل ثبت کرده است؛ کلید محیطی نباید جایگزین آن شود.
- دیپ‌اینفرا کلید را فقط برای همان درخواست مصرف می‌کند و در تنظیمات وردپرس ذخیره نمی‌کند.

پس از افزودن یا تغییر Variables، حتماً تغییرات را اعمال و `Redeploy` کنید؛ ذخیرهٔ متغیر بدون استقرار مجدد برای فرایند در حال اجرا کافی نیست.

## ۴. ولوم‌ها

دو ولوم جدا لازم است:

| سرویس | مسیر اتصال | دلیل |
|---|---|---|
| MySQL | `/var/lib/mysql` | حفظ دیتابیس و جلوگیری از ازبین‌رفتن داده‌ها |
| سرویس وب | `/var/www/html/wp-content` | حفظ افزونه، قالب، آپلود و فایل‌های وردپرس |

مسیر ولوم دیتابیس را با مسیر ولوم وب اشتباه نگیرید. پیش از حذف یا جابه‌جایی هر ولوم، از دیتابیس و فایل‌ها پشتیبان بگیرید.

## ۵. ترتیب صحیح ساخت و Deploy

۱. ابتدا سرویس MySQL و ولوم آن را آماده کنید.
۲. متغیرهای اتصال دیتابیس و متغیرهای اجباری وردپرس را تنظیم کنید.
۳. ولوم وب را روی `/var/www/html/wp-content` متصل کنید.
۴. مخزن و شاخه را متصل کنید.
۵. در `Settings → Build` بررسی کنید که builder برابر Dockerfile و مسیر آن برابر باشد:

```text
railway/Dockerfile
```

۶. یک Deploy تازه انجام دهید.
۷. تا پایان بوت صبر کنید؛ Redeployهای پشت‌سرهم می‌توانند مهاجرت را هم‌زمان و دیتابیس را قفل کنند.
۸. فقط پس از سبزشدن healthcheck وارد پنل یا سایت شوید.

## ۶. رفتار راه‌انداز پروژه

راه‌انداز `railway/entrypoint.sh` این مراحل را انجام می‌دهد:

۱. تنظیم MPM آپاچی روی `prefork`.
۲. راه‌اندازی اولیهٔ فایل‌های وردپرس توسط entrypoint رسمی تصویر وردپرس.
۳. بالا آوردن Apache برای پاسخ‌گویی به healthcheck، اما مسدودکردن درخواست‌های عادی هنگام بوت.
۴. پاسخ مستقل healthcheck با کد `۲۰۰` و وضعیت `ready:false` تا پایان بوت؛ این پاسخ وارد وردپرس یا دیتابیس نمی‌شود.
۵. همگام‌سازی افزونه و mu-pluginها با ولوم.
۶. انتظار برای آماده‌شدن دیتابیس.
۷. نصب یا شناسایی وردپرس.
۸. فعال‌سازی ووکامرس پیش از `igbz-suite`.
۹. اجرای مهاجرت‌های دیتابیس و آماده‌سازی فروشگاه.
۱۰. نصب بستهٔ زبان فارسی.
۱۱. ثبت وضعیت `ready:true` و حذف گیت بوت.
۱۲. آزادکردن ترافیک عادی و نگه‌داشتن همان فرایند Apache فعال.

**دو خطای مهم گذشته:**

- نباید Apache دوباره با `exec apache2-foreground` اجرا شود؛ چون یک Apache از قبل فعال است و این کار باعث خروج فرایند اصلی و خطای `۵۰۲` می‌شود.
- نباید فقط Apache را تا پایان بوت خاموش کرد؛ Railway در این حالت healthcheck را دریافت نمی‌کند و Deployment رد می‌شود.

## ۷. معیارهای موفقیت Deploy

در لاگ باید تقریباً این مراحل دیده شود:

```text
igbz: database ready
igbz: required plugins active
igbz: bootstrap done
igbz: ready (health 200, attempt N)
```

healthcheck مرجع:

```text
https://<دامنه-عمومی>/?igbz_health=1
```

پاسخ سالم باید شامل این موارد باشد:

```json
{
  "ok": true,
  "degraded": false,
  "db": true,
  "igbz_loaded": true,
  "igbz_tables": "100/100",
  "schema_expected_dbv": 48,
  "wc_active": true,
  "wc_version": "11.0.1"
}
```

وجود `AH00558` فقط هشدار نبودن `ServerName` آپاچی است و به‌تنهایی خطای استقرار محسوب نمی‌شود.

## ۸. عیب‌یابی سریع

### خطای `Lock wait timeout exceeded`

نشانه:

```text
DELETE/INSERT ... wp_options
_site_transient_theme_roots
```

علت معمول: درخواست وب، healthcheck و `wp-cli` هم‌زمان در زمان بوت به وردپرس رسیده‌اند.

اقدام:

۱. Redeploy پشت‌سرهم انجام ندهید.
۲. بررسی کنید Source Commit آخرین نسخهٔ راه‌انداز باشد.
۳. لاگ را از ابتدا بخوانید؛ باید healthcheck مستقل و گیت بوت دیده شود.
۴. اگر خطا ادامه داشت، وضعیت قفل و فضای دیتابیس را در سرویس MySQL بررسی کنید.

### خطای `disk is full`

نشانه:

```text
Create table/tablespace ... failed, as disk is full
```

علت: فضای ولوم یا فضای دیتابیس کافی نیست؛ خطاهای «ستون وجود ندارد» معمولاً پیامد مهاجرت ناقص هستند.

اقدام:

۱. فضای دیتابیس را افزایش دهید یا فضای غیرضروری را آزاد کنید.
۲. ولوم `/var/lib/mysql` را حذف نکنید.
۳. از دیتابیس پشتیبان بگیرید.
۴. پس از اطمینان از فضای کافی، فقط یک Redeploy انجام دهید.

### خطای `502 Bad Gateway` پس از `bootstrap done`

علت محتمل: راه‌انداز Apache فعال را دوباره با `exec` اجرا کرده و فرایند اصلی خارج شده است.

نشانهٔ لاگ:

```text
httpd (pid ...) already running
```

باید راه‌انداز همان PID Apache را با `wait` نگه دارد.

### خطای healthcheck

- `۵۰۲`: فرایند وب خارج شده یا Apache دوباره اجرا شده است.
- پاسخ خالی/timeout: Apache بالا نیست یا بوت‌استرپ قبل از پاسخ‌گویی گیر کرده است.
- `۵۰۳`: گیت بوت یا افزونه عمداً سرویس را آماده اعلام نکرده است؛ لاگ مهاجرت و دیتابیس بررسی شود.
- شاخه و Source Commit را در `Deployments` بررسی کنید؛ فقط به اتصال شاخه در Settings اکتفا نکنید.

## ۹. آزمون واقعی providerها

آزمون providerها فقط پس از سبزشدن healthcheck و اطمینان از فضای دیتابیس انجام شود.

ترتیب پیشنهادی:

۱. بررسی وجود متغیرها بدون چاپ مقدار آن‌ها.
۲. بررسی `WP_ENVIRONMENT_TYPE=staging`.
۳. اجرای یک درخواست کوچک و کم‌هزینهٔ دیپ‌اینفرا با مدل allowlistشده.
۴. بررسی اینکه کد پاسخ، شناسهٔ مدل، مصرف توکن و نتیجه ثبت شده، اما کلید در لاگ نیامده است.
۵. اجرای اتصال مرکزی زرنیو و دریافت/تطبیق profile مرحله‌ای.
۶. اجرای آزمون واقعی انتشار فقط با محتوای آزمایشی، profile آزمایشی و تأیید انسانی.
۷. بررسی دفتر audit و نتیجهٔ provider.

هرگز کلید را در URL، بدنهٔ پاسخ، اسکرین‌شات، گزارش مرورگر یا پیام خطا چاپ نکنید.

## ۱۰. روند بازگشت

- بازگشت کد: از Deployment سالم قبلی استفاده کنید؛ تغییرات ناقص را دوباره مستقر نکنید.
- بازگشت دیتابیس: طبق `RUNBOOK-BACKUP-RESTORE.md` از پشتیبان رمز‌شده استفاده کنید.
- مهاجرت MySQL: ابتدا روی دیتابیس موازی انجام شود؛ بازگشت با برگرداندن `DATABASE_URL` است.
- حذف ولوم یا ساخت دیتابیس تازه بدون پشتیبان ممنوع است.

## ۱۱. چک‌لیست کوتاه هر استقرار

- [ ] شاخهٔ درست متصل است.
- [ ] Source Commit با Commit موردنظر یکسان است.
- [ ] Variables اعمال و Redeploy شده‌اند.
- [ ] سکرت‌ها Sealed هستند.
- [ ] دو ولوم مسیر درست دارند.
- [ ] فضای دیتابیس کافی است.
- [ ] `database ready` دیده شد.
- [ ] `bootstrap done` دیده شد.
- [ ] `ready (health 200)` دیده شد.
- [ ] healthcheck پاسخ `ok=true` و `degraded=false` می‌دهد.
- [ ] پیش از آزمون provider، کلیدها در لاگ/پاسخ/اسکرین‌شات ظاهر نشده‌اند.
- [ ] برای هر آزمون provider از profile و دادهٔ آزمایشی استفاده شده است.

## منابع

- مستندات رسمی مدیریت سکرت‌های Railway: `https://docs.railway.com/guides/managing-secrets-on-railway`
- مستندات رسمی متغیرهای Railway: `https://docs.railway.com/variables`
- راهنمای داخلی مهاجرت LTS: `RUNBOOK-RAILWAY-LTS.md`
- گزارش پذیرش پروژه: `ACCEPTANCE-REPORT.md`
