# پرامپت ادامهٔ کار — چت تازه

> **این فایل را در چت جدید بفرست (یا بگو «`HANDOFF-PROMPT.md` را بخوان»).**
> به‌روزرسانی: ۱۴۰۵/۰۶/۰۶ (۲۸ اوت ۲۰۲۶) · شاخهٔ ثابت آخرین جلسه:
> `arena/01a04859-igbz-wp` (هر جلسه شاخهٔ arena خودش را دارد؛ فقط روی همان کار کن).
> **وضعیت: فازهای ۰۱ تا ۴۹ تمام و پوش‌شده؛ فاز بعدی ۵۰ است.** جزئیات دقیق در بخش
> «📍 نقطهٔ دقیق ادامهٔ کار» همین‌جاست.
> معماری جاری: پادوی Playbookمحور، DeepInfra مستقل هر فروشگاه و Zernio مرکزی/چندپروفایلی
> به‌عنوان تنها provider اجتماعی؛ Manus، ChatPlace، ManyChat و Ayrshare حذف‌اند و پیشنهاد session اینستاگرام در Agent Reach رد شده است.
> همیشه `git status` و انتهای همین سند را ملاک قرار بده؛ بخش‌های میانی تاریخچه‌اند.

> **🔴 تغییرنام رسمی (۱۴۰۶/۰۵/۳۱):**
> - **هوشا → ویرا** (به دهخدا: هوشمند، یادگیرنده، آموزنده، مدرک — شاهد: «یکی مرد ویرا بباید نخست / که گوید نیوشیده‌ها را درست» ابوالعباس مروزی). نام فنی/پوشه/کلاس/فایل‌هایش: `vira/`، `Vira`، `vira`، ویرا.
> - **هوشواره → پادو** (به دهخدا: پاکار، خدمتکارگونه و فرمانبر، شاگرد حجره تجار، کسی که برای کارهای سرپایی و دوندگی فرستاده می‌شود). نام فنی/پوشه/کلاس/فایل‌هایش: `Modules/Pado`، `Pado`، `pado`، پادو.
> همهٔ فایل‌ها، دایرکتوری‌ها، فیلدها، نام کلاس‌های PHP، مسیرها در اسناد و capabilityها به این دو نام به‌روزرسانی شدند. مرجع تعریف این دو در `DESIGN-DEPLOY-VIRA.md §۰` و `DESIGN-PADO.md §۰` درج شده.

---

## 📍 نقطهٔ دقیق ادامهٔ کار — وضعیت در پایان جلسهٔ ۱۴۰۵/۰۶/۰۶

**این بخش ملاک سریع چت تازه است؛ بقیهٔ سند تاریخچه و قواعد است.**

### تا کجا انجام شده

- **فازهای ۰۱ تا ۴۹ برنامه (`PROJECT-COMPLETION-PLAN.md §۲۳`) کامل، مستند و پوش شده‌اند.**
  تنها استثناء: فاز ۰۲ که ذاتاً «در جریان» است (تصمیم‌های ظرفیت/دیتابیس/ترجمه).
- آخرین کامیت پوش‌شده: `b9e1bac` (فاز ۴۹) روی شاخهٔ `arena/01a04859-igbz-wp`.
- خلاصهٔ آخرین فازها: ۴۵ آداپتورهای لجستیک · ۴۶ مارکت‌پلیس‌ها (اسکیمای ۳۴) · ۴۷ سئو و
  تبلیغات (۳۵) · ۴۸ ترجمه و تجارت بین‌المللی (۳۶) · ۴۹ اتصال Zernio و نگاشت پروفایل
  (۳۷، طبق بند ۵ سند `ADR-0004`).
- معیارهای سبز: تست **۲۵۲۹ اظهارنظر در ۶۷ کیس** · لینت **۳۲۶ فایل، صفر خطا** · دیتابیس
  **v37 با ۸۵ جدول** · اسموک زندهٔ پلی‌گراند پاس.

### از کجا باید ادامه داد

- **فاز بعدی: ۵۰ — مهاجرت به تنها provider اجتماعی.** شرح کامل در
  `PROJECT-COMPLETION-PLAN.md §۲۳` (بند «فاز ۵۰»): ساخت آداپتر رسمی Zernio، مهاجرت
  کنترل‌شدهٔ داده، و **حذف کامل** کد/تنظیمات/نقاط پایانی قدیمیِ Manus، ChatPlace، ManyChat و
  فالبک‌های اجتماعی از همه‌جا (کانتینر، فرم‌ها، UI، Runbook) + گارد معماری و آزمون برای
  جلوگیری از بازگشت کانال اینستاگرام مبتنی بر نشست.
- **هشدار اجرا:** فاز ۵۰ موج حذف است؛ تست‌های قدیمیِ وابسته به کلاینت‌های قدیم را باید همزمان
  به‌روزرسانی کرد، نه حذف بی‌ملاحظه. طبق قاعدهٔ ۱، پلن کوتاه فاز ۵۰ را بنویس، تأیید بگیر و
  منتظر «شروع کن» بمان.
- پس از ۵۰، فازهای ۵۱ تا ۵۵ (اینباکس، ثبت محصول ۱۳مرحله‌ای، انتشار و صدا، VIP، قرعه‌کشی) و
  سپس فازهای `PV-*` (تأیید زندهٔ پرووایدرها — نیازمند اعتبارنامهٔ واقعی از کارفرما) در برنامه‌اند.

### محیط و ابزار (پس از بازسازی سندباکس بازسازی شوند)

- اجرای تست: `node "$(find _devenv/.work/node_modules/@php-wasm/cli -maxdepth 1 -name '*.js' | head -1)" igbz-suite/tests/run.php`
- لینت: همان دستور با مسیر `_devenv/.work/lint.php igbz-suite`
- پلی‌گراند روی پورت ۹۰۰ یا ۹۴۰۰ با `nohup bash _devenv/run.sh`؛ کوکی ورود در `/tmp/cj.txt`.
- بازسازی سندباکس، `_devenv/.work` و `/tmp` را پاک و گیت را به پایه برمی‌گرداند ⇒ **بلافاصله
  بعد از هر کامیت، پوش کن.**

### نکته‌های ماندگار این بلوک کاری

- دستور قدیمی «فاز ۲۵» گاهی دوباره ارسال می‌شود — **کهنه است** (در `de6c31e` انجام شده)؛
  هرگز دوباره اجرا نشود.
- دابل‌های تست: جدول‌ها با پیشوند می‌آیند — تطبیق با پسوند نام منطقی؛ شمارندهٔ `next_id` بین
  جدول‌ها مشترک است؛ شعبه‌های خاص `get_var` پیش از عمومی.
- نقاط تماس هر اسکیمای جدید: `igbz-suite.php`، `DriftGuardTest` (نسخه + تعداد جدول)،
  `PHASE-01-INVENTORY.json`، مپ مهاجرت `Activator` + متد `migrate_to_vNN`، فهرست جدول‌های
  `Schema::tables()` و `TenantOffboarding`.

---

## ۰. اول از همه — قواعد کاری کارفرما

این‌ها **دستورهای دائمی**‌اند، نه سلیقهٔ یک جلسه. نقض هرکدام یعنی دوباره‌کاری:

| # | قاعده |
|---|---|
| **۱** | **پلن ← تأیید ← کلمات صریح «شروع کن» ← تغییر.** «خوبه» یا سکوت کافی **نیست**. خواندن، بررسی، تست و نوشتن پلن آزاد است. |
| **۲** | **سکوت تا پایان کامل هر فاز.** هیچ گزارش میانی؛ پس از به‌روزرسانی اسناد فقط گزارش نهایی طبق قاعدهٔ ۱۵ (یک خلاصهٔ ۴ تا ۵ خطی). |
| **۳** | **تست سبک** برای تغییر کوچک — تست فراگیر نگیر. |
| **۴** | **سؤال‌ها را تک‌تک، کوتاه و با گزینه بپرس.** تا پاسخ سؤال جاری نیامده، سؤال بعدی را مطرح نکن. |
| **۵** | **هر چیز را در سند مربوط به خودش ثبت کن** — نه فقط در `PROJECT-STATE.md` یا متن پاسخ. |
| **۶** | **از دامنهٔ موضوع جاری خارج نشو.** |
| **۷** | چیزی که در اسناد «پیشنهادی» است، تا تأیید کارفرما **مبنای استدلال بعدی نشود**. |
| **۸** | **مجوز هر فاز فقط برای همان فاز است.** خواندن/بررسی آزاد و ثبت سند طبق قاعدهٔ ۹ خودکار است؛ تغییر کد و اجرای فاز بعدی بدون دستور صریح ممنوع. اعلان GitHub نیز فقط با دستور تازهٔ کارفرما. |
| **۹** | **به‌روزرسانی خودکار اسناد.** هرگونه تغییر/افزودن در اسناد و کتب پروژه (شامل همین HANDOFF، PROJECT-STATE، DESIGN-*، PROMPT-*، READMEها و غیره) و همچنین **ثبت تصمیم‌هایی که کارفرما در جریان گفتگو می‌گیرد** در سند(های) مربوطه **به‌طور خودکار و بلافاصله** اعمال می‌شود و **نیازی به پلن، تأیید یا کلمهٔ «شروع کن» ندارد.** ⚠️ این قاعده **فقط برای اسناد** است؛ برای **تغییر در کد** (پلاگین، ویرا، اسکریپت‌های `_devenv`، فایل‌های اجرایی و هر چیزی که اجرا می‌شود) کماکان قاعدهٔ ۱ به‌قوت خود باقی است و نیاز به پلن ← تأیید ← «شروع کن» دارد. |
| **🔴 ۱۰ (سختگیرانه)** | **تحقیق اجباری پیش از هر کار — هرگز فقط به دانش درونی اکتفا نشود.** هر کاری که محول می‌شود (اعم از طراحی ویژگی جدید، انتخاب فناوری، تصمیم‌گیری معماری، نوشتن اسکیل/پرامپت، رفع باگ، و حتی به‌روزرسانی اسناد فنی) **باید قبل از شروع**، حتماً با ابزار `web_search` (با `depth=3` برای کارهای مهم) تحقیق و بررسی شود: (الف) مقالات و مستندات رسمی **به‌روزِ** موضوع؛ (ب) بهترین تجربیات و الگوهای پذیرفته‌شده در صنعت؛ (پ) پیاده‌سازی‌ها و پروژه‌های مشابه موفق و ناموفق — **نقاط قوت و ضعف هرکدام**؛ (ت) بازخوردها و نقدهای کاربران واقعی در issueها، فروم‌ها، Reddit/HN و غیره؛ (ث) تهدیدهای امنیتی/عملکردی شناخته‌شده (OWASP و…). **سپس** با جمع‌بندی این تحقیق + دانش درونی، پلن نوشته شود و به تأیید برسد. نقض این قاعده = دوباره‌کاری قطعی. ⚠️ این قاعده شامل انتخاب مدل/سرویس پادو هم می‌شود: انتخاب باید بر اساس **benchmarkها و نقد و بررسی‌های واقعی به‌روز** باشد، نه صرفاً شنیده‌ها یا دانش قدیمی. |
| **🔴 ۱۱ (سختگیرانه)** | **سؤال کردن فقط تک‌به‌تک، با گزینه‌های انتخابی + باکس پیشنهاد شخصی.** هرگز چند سؤال را پشت سر هم در یک پیام نپرس. سؤال‌ها را **یکی یکی و به ترتیب** بپرس. هر سؤال باید: (۱) کوتاه و در یک جمله ساده باشد، (۲) ۲–۴ گزینه ازپیش‌آمادهٔ کلیکی داشته باشد (هر گزینه ۱ خط توضیح کوتاه)، (۳) همیشه باکس «پیشنهاد/نظر خودم» با پاسخ آزاد داشته باشد (`allowCustomResponse: true` در ابزار `ask_user`). تا رسیدن جواب قطعی کارفرما برای سؤال فعلی، هیچ‌گاه سراغ سؤال بعدی نرو. نقض این قاعده = دوباره‌کاری قطعی. |
| **🔴 ۱۲ (سختگیرانه — ۱۴۰۶/۰۶/۰۲)** | **تست ویژوال اجباری برای هر تغییر.** از این پس **هر تغییری** علاوه بر تست‌های حسی/فنی همیشگی (unit، curl، DOM)، باید **به‌صورت ویژوال** هم تست شود: صفحه(های) متأثر با مرورگر واقعی (Chromium پلی‌رایت، §۷-ب) باز شود، **اسکرین‌شات تمام‌صفحه** گرفته شود و **با چشم (ابزار read_file روی تصویر) بازبینی گردد** — چیدمان، RTL، متن‌های شکسته، سرریز، تصاویر خراب، اجزای فرم. ابزار آماده: `browser-test/visual-crawl.cjs` (۳۴ صفحه: ۲۹ منوی ادمین IGBZ + ۵ صفحهٔ فروشگاه) و اسکرین‌شات‌ها/گزارش در پوشهٔ `visual-testing/` ریپو. دلیل وضع قانون: تست ویژوال ۱۴۰۶/۰۶/۰۲ **هفت مشکل واقعی** را پیدا کرد که همهٔ تست‌های قبلی (۱۲۴۸ اظهارنظر + curl سبز) از کنارشان رد شده بودند — از جمله دوباره‌رندر صفحهٔ Status و بایت backspace خراب در regex ارقام فارسی (جزئیات: `visual-testing/REPORT.md`). نقض این قاعده = دوباره‌کاری قطعی. |
| **🔴 ۱۳ (سختگیرانه — ۱۴۰۵/۰۶/۰۵)** | **صداقت مطلق در پاسخ‌گویی.** پاسخ به هر پرسش کارفرما باید فقط صادقانه و بر پایهٔ واقعیت‌های دانسته یا شواهد قابل‌بررسی باشد. ساختن پاسخ، پنهان‌کردن واقعیت مرتبط، وانمودکردن به انجام یا دانستن چیزی، و بیان حدس با قطعیت ممنوع است. هرجا اطلاعات کافی نیست یا نتیجه نامطمئن است، باید صریحاً «نمی‌دانم»، «اطلاعات کافی ندارم» یا میزان عدم‌قطعیت گفته شود. واقعیت، استنباط و پیشنهاد باید از هم تفکیک شوند و هر خطای کشف‌شده فوراً و شفاف اصلاح شود. هیچ ملاحظه‌ای مجوز پاسخ ناصادقانه یا گمراه‌کننده نیست. |
| **۱۴ (۱۴۰۵/۰۶/۰۵)** | پیام کارفرما که فقط شامل `.` باشد، معادل **«ادامه بده»** است. این میان‌بر فقط کارِ مجاز و در جریان را ادامه می‌دهد؛ چون کارفرما آن را جای «ادامه بده» تعریف کرده، جای عبارت صریح **«شروع کن»** برای آغاز تغییر کد یا فاز اجرایی تازه نیست. |
| **🔴 ۱۵ (سختگیرانه — ۱۴۰۵/۰۶/۰۶)** | **گزارش فقط یک خلاصهٔ ۴ تا ۵ خطی پس از انجام هر کار.** از گزارش‌های طولانی و پرجزئیات **جداً خودداری** شود؛ جزئیات کامل در سند مربوطه (طبق قاعدهٔ ۵) ثبت می‌شود، نه در چت. اگر کارفرما سؤال یا جزئیات بخواهد، خودش می‌پرسد. **از هر کاری که باعث سنگین‌شدن چت شود (پیام بلند، جدول بزرگ، خروجی خام، تکرار محتوا) اکیداً خودداری کن.** این قاعده قاعدهٔ ۲ را کامل می‌کند: مبنای گزارش نهایی، همین ۴ تا ۵ خط است. |

**دیگر الزام‌ها:**
- همهٔ پاسخ‌ها **فارسی**.
- **🔴 قالب پیام‌ها (۱۴۰۶/۰۶/۰۳ — دستور صریح کارفرما): پیام‌های چت باید فارسیِ تمیزِ راست‌به‌چپ باشند.** کارفرما گفت پیام‌های پر از کلمات لاتین «انقدر قاطی می‌شود» که اکثرشان قابل فهم نیستند. قواعد: (۱) تا جای ممکن از واژه‌های لاتین وسط جملهٔ فارسی پرهیز کن — معادل فارسی بنویس و فقط در صورت نیاز، اصل لاتین را یک بار داخل پرانتز بیاور؛ (۲) نام فایل‌ها/دستورها/کدها را فقط در بلوک کد یا انتهای جمله بیاور، نه وسط جمله؛ (۳) جدول‌هایی که ستون‌هایشان پر از متن لاتین است جهت سطر را به‌هم می‌ریزند — یا ستون لاتین را کم کن یا فهرست فارسی بنویس؛ (۴) اعداد فارسی؛ (۵) لینک‌ها و آدرس‌ها در سطر جدا یا بلوک کد.
- **سؤال اضافه نپرس تا وقتی کار تمام است** — «تا وقتی که کار تموم کنیم لازم نیست که انقدر سوال بپرسی».
- **بررسی محدودیت‌ها همیشه در بک‌اند، نه فرانت‌اند** — «که با یه رفرش صفحه دور زده نشه».
  این را کارفرما **دو بار مستقل** گفته (کول‌داون OTP، و امضای بیومتریک) ⇒ اصل پایدار پروژه.
- **کد یکبارمصرف همیشه با ماژول کریپتو** — هرگز `rand`/`mt_rand`.
- **مواردی که کد ندارند باید در بخش کارهای باقی‌مانده ثبت شوند.**
- **تأیید زنده** بعد از هر تغییر معنادار — `curl` و تست واحد کافی نیست.
- **مبنای امنیتی منتخب کارفرما:** `امنیت و مراقبت/README.md`؛ هر تصمیم امنیتی باید به
  منبع و محل استفاده‌اش در `REQUIREMENTS-TRACEABILITY.md` قابل ردیابی باشد.
- **پوشهٔ `vira/` مطلقاً خارج محدوده است** و بدون دستور صریح نباید خوانده یا تغییر کند.
- **هیچ اعلان GitHub** بدون دستور تازهٔ کارفرما ارسال نشود.
- فقط روی شاخهٔ **arena جاریِ همان جلسه**. هرگز شاخهٔ دیگر؛ در این جلسه فقط
  `arena/01a0435a-igbz-wp`.

---

## ۱. پروژه در یک نگاه

**`IGBZ-WP`** — افزونهٔ وردپرس/ووکامرس برای سرویس فروشگاه‌ساز اینستاگرامی، بازار ایران.

| | |
|---|---|
| مقیاس هدف | **صدها تا هزاران فروشگاه × صدها تا هزاران کاربر هرکدام** |
| اندازه | **۸۵ جدول** · `IGBZ_DB_VERSION=37` · اسکیمای نسخه‌بندی‌شده با مهاجرت‌های `Activator` |
| تست | **۲۵۲۹ اظهارنظر در ۶۷ کیس** · **لینت: ۳۲۶ فایل، صفر خطا** (ارقام ۱۴۰۵/۰۶/۰۶، پایان فاز ۴۹) |
| خارج دامنه | `vira/` بدون دستور صریح خوانده یا تغییر نمی‌شود |

### سه مفهوم که نباید قاتی شوند

| | چیست | چه کسی می‌سازد |
|---|---|---|
| **ویرا** | ابزار عاملِ خودمان (Node.js) + API Gateway واحد مدل‌ها | ✅ ما — **فعلاً دست کدکس** |
| **پادو** | لایهٔ هوشمند روی هستهٔ ویرا؛ مدل/سرویس توسط ما بر اساس بهترین عملکرد در اتوماسیون انتخاب می‌شود | ✅ ما (انتخاب مدل و پلتفرم) / ادمین فقط **کلید API** مدل منتخب را ثبت می‌کند |
| **اسکیل** | دستورالعمل روزانه برای پادو (فرمت SKILL.md) | ✅ ما |

> ⚠️ **`DESIGN-PADO.md` هنوز جاهایی بر فرض غلط «ما پادو را می‌سازیم» نوشته شده.**
> فقط `§۲.۵` و تصمیم ۴ تصحیح شدند. **بازبینی کل سند، اولین کارِ فاز پادو است.**

---

## ۲. ترتیب کار — تعیین‌شده توسط کارفرما

```
۱ ✅ تعیین تکلیف کارهای باقی‌مانده        — تمام شد
۲ 🟡 سند امنیت + مدیریت درخواست‌ها       — سه شکاف بسته شد، خودِ سند نوشته نشده
۳ ⏳ پادو — «یکجا تا پایان، نه تکه‌تکه»
۴ ⏳ ترجمهٔ فارسی (۲۰۹۰ رشته، همه‌چیز جز خطاها)
۵ ⏳ بازبینی مقیاس‌پذیری کد با هفت قاعدهٔ §۵.۵
۶ ⏳ شناسهٔ کاربر وردپرسِ مدیر ارشد — یک عدد از کارفرما
۷ ⏳ پاک‌سازی ۱۰ اتصال نمونهٔ sk-demo در ویرا
۸ ⏳ اپ‌های موبایل — آخر از همه، بعد از تکمیل بک‌اند و فرانت‌اند
```

**اپ‌ها معلق‌اند** چون کارفرما گفت: «تا از دوباره‌کاری جلوگیری کنه. در زمانش هم فایل کامل
اپلیکیشن رو ارسال می‌کنم.» ⇒ نیازی به دسترسی ریپوی اپ نیست.

---

## ۳. 🔴 نقطهٔ دقیق توقف — از اینجا ادامه بده

**فاز ۰۱ تمام شده است.** منبع خروجی آن:

```text
REQUIREMENTS-TRACEABILITY.md
```

فاز ۰۲ چهار ADR ثبت‌شده دارد و کدنویسی تازه‌ای برای معماری جدید انجام نشده است:

۱. **وردپرس چندسایتی + هستهٔ مدیریتی مشترک**؛ هر فروشگاه زیرسایت و data plane جدا دارد:
   `ADR/ADR-0001-MULTISITE-TENANCY.md`.
۲. **هر سه خروجی قالب به انتخاب ادمین**؛ بلوکی، کلاسیک PHP و تمپلیت صفحه‌ساز الحاقی، با
   خط لولهٔ امنیتی متفاوت:
   `ADR/ADR-0003-THREE-THEME-OUTPUTS.md`.
۳. **پادوی Playbookمحور + DeepInfra مستقل هر فروشگاه + Zernio مرکزی/چندپروفایلی**؛
   Zernio تنها provider اجتماعی و ADR-0004 جانشین ADR-0002 است:
   `ADR/ADR-0004-PADO-ZERNIO-SOCIAL-ARCHITECTURE.md`.

backlog معماری این حوزه: قرارداد و آزمون واقعی Zernio/DeepInfra، Business Discovery و
Hashtag Search، benchmark فارسی، TTS فارسی، sandbox امن PHP و migration providerهای legacy.
ظرفیت/SLO، دیتابیس تولید، دامنهٔ ترجمه و پیشنهادهای سایر حوزه‌ها همچنان جداگانه بازند.

**اقدام بعدی فنی:** بدون برنامهٔ تازه، تأیید و عبارت صریح «شروع کن» هیچ کدی تغییر نده. در
صورت صدور مجوز، migration باید کد Manus/ChatPlace/ManyChat را حذف، از ورود کانال Instagram
مبتنی بر session در Agent Reach جلوگیری، و Zernio، DeepInfra و چهار Playbook را با آزمون
فنی/زنده اضافه کند. مبنای امنیت همچنان:

```text
امنیت و مراقبت/README.md
```

### تاریخچهٔ رشتهٔ پادو — برای زمینه، نه نقطهٔ شروع جاری

**روش کار تعیین‌شده توسط کارفرما (۱۴۰۶/۰۵/۳۱):**
> «از این به بعد تا کامل شدن طراحی هوش‌ها اینجوری پیش میریم که هر بخشی رو که
> برای پادو تا الان طراحی شده را با هم بررسی می‌کنیم به این صورت که تو اول
> شرحی در رابطه با اون بخش میدی که چطور کار می‌کنه و چه کار می‌کنه بعد من
> نظراتم رو برای تغییر یا تکمیل اون به تو میدم درباره‌اش بحث می‌کنیم
> جمع‌بندی که کردیم میریم سراغ بخش بعدی و در انتها تغییراتو انجام میدیم.»

قواعد این روند:
- در هر نوبت **فقط یک بخش** شرح داده می‌شود (معماری کلی، یک ماژول، یک جریان
  داده، یک قرارداد پرامپت/خروجی، …).
- شرح باید شامل **چیستی، چرا این‌طور، چطور کار می‌کند، ورودی/خروجی، نقاط
  حساس، دام‌های شناخته‌شده، و کارهای باز** همان بخش باشد.
- طبق **قاعده ۱۰ (تحقیق اجباری)**: هر گاه حین بررسی به یک انتخاب فناوری/مدل/
  الگو می‌رسیم یا کارفرما تغییر/تکمیلی را مطرح می‌کند که نیازمند اطلاع از
  وضعیت به‌روز صنعت، نقاط ضعف/قوت راه‌حل‌های مشابه یا بازخورد کاربران
  است، **حتماً قبل از ارائهٔ پیشنهاد نهایی** با `web_search` (depth=3) تحقیق
  می‌شود. بازبینی‌های صرفاً توصیفی از طراحی موجود نیازی به تحقیق ندارند.
- تا جمع‌بندی یک بخش، ورود به بخش بعدی ممنوع.
- **تغییرات کد/اسناد در انتها** و پس از بازبینی همهٔ بخش‌ها اعمال می‌شوند
  (مگر اینکه قاعده ۹ — به‌روزرسانی خودکار اسناد برای تصمیم‌های کارفرما —
  اقتضای ثبت فوری را داشته باشد).

**ترتیب پیشنهادی بخش‌ها برای بازبینی (قابل تغییر به درخواست کارفرما):**
۱. ✅ جایگاه و معماری کلی (تصمیمات قطعی کارفرما: n8n هسته اورکستراسیون، ویرا
   موتور مدل/اسکیل، وردپرس منبع حقیقت — نگاه A)
۲. ✅ فرم «طراحی قالب» در پنل — **تک‌سؤالی شد** (شرح در بند ۵ رشته فعال).
۳. ✅ نوع خروجی قالب = **قالب فرزند FSE (Full Site Editing) روی قالب هسته وردپرس** (گزینه الف) — theme.json + CSS + تمپلیت‌های HTML، بدون PHP اجرایی، کم‌حجم (~۱۰۰-۳۰۰KB)، امن، قابل ویرایش در ویرایشگر خود وردپرس.
۳. تصمیم قالب بلوکی FSE فرزند در مقابل کلاسیک/قالب مادر
۴. سه‌بعدی دو پله (عمق CSS خالص + shader اختیاری)
۵. اسکیل `igbz-store-theme` (ساختار ۷ مرجع)
۶. قواعد ایران (فونت محلی، RTL واقعی، اعداد فارسی، …)
۷. پرامپت ثابت و قرارداد خروجی zip
۸. دروازه امنیتی `ThemeValidator` (ممنوعیت PHP، حجم، ساختار)
۹. مرحله «پیشنهاد طراحی یک‌صفحه‌ای» قبل از ساخت zip
۱۰. روند نصب قالب در فروشگاه (preview → live → بازگشت یک‌کلیک)
۱۱. ApprovalQueue جزئیات (چرخه create → list → claim → resolve)
۱۲. مراحل ۲ تا ۹ پادو (ثبت محصول، قیمت، محتوا، اینستاگرام، …)
۱۳. ~~API Gateway ویرا و دروازه مدل‌ها~~ — با connector مستقل DeepInfra در ADR-0004 جانشین شد

#### 🔴 نقطهٔ ادامهٔ دقیق — بخش‌های بازبینی شده: طراحی قالب (کامل) + سیستم حافظه پادو (کامل):
> **بخش‌های تکمیل‌شده در جلسه ۱۴۰۶/۰۶/۰۱:**
> - ✅ **تغییرنام رسمی سراسری: «هوشا → ویرا»، «هوشواره → پادو»** (معنی دهخدا برای هر دو نام در ابتدای `DESIGN-PADO.md` و `DESIGN-DEPLOY-VIRA.md` ثبت شد). تمام ۱۰۲ فایل مرتبط، پوشه‌ها (`hoosha/` → `vira/`، `igbz-suite/src/Modules/Hushvare/` → `Pado/`)، فایل‌ها (`HushvareModule.php` → `PadoModule.php`، `Admin/HushvarePage.php` → `Admin/PadoPage.php`، `DESIGN-HUSHVARE.md` → `DESIGN-PADO.md` و غیره)، نام‌های کلاس/کپبیلیتی/ثابت‌های PHP (`igbz_manage_hushvare` → `igbz_manage_pado`، `Modules::HUSHVARE` → `Modules::PADO`) به‌روزرسانی شدند. بررسی نهایی نشان می‌دهد هیچ موردی از نام‌های قدیمی باقی نمانده است.
>
> - ✅ **سیستم حافظه پادو — سه لایه اصلی + مکمل‌ها (طراحی کامل شد):**
>   ۱. **دانش (حافظه معنایی):** بخش‌بندی‌شده در ۷ حوزه متناظر با کارهای پادو (طراحی قالب، اقتصادی/کسب‌وکار، فنی/عملیات، بازاریابی/محتوا، مشتری/پشتیبانی، حقوقی/قوانین، امنیت). منابع پایه معرفی‌شده توسط کارفرما (۷ مرجع طراحی قالب شامل emilkowalski/skills، SKILL فرانت‌اند کلود، Step-Into-Wonder، MotionSites.ai، GetLayers.ai، Webild.io، Awwwards و ui-ux-pro-max) ثابت و تغییرناپذیر هستند. اگر پادو پاسخ در دانش موجود پیدا نکند، با تحقیق خود منبع معتبر می‌یابد، اعتبارسنجی می‌کند و با ثبت منبع به بخش مربوطه می‌افزاید (دانش به‌مرور انباشت می‌شود).
>   ۲. **تجربه (حافظه الگویی):** الگوهای تعمیم‌یافته از **چهار منبع**: رفتار/ترجیحات ادمین، واکنش واقعی مشتری (نتیجه کمپین، قیمت، طراحی، نرخ تبدیل، فروش)، عملکرد خود پادو (کدام روش‌ها موفق/ناموفق بود)، **و رفتار و نتایج رقبا** (کارهایی که رقبا نتیجه منفی گرفتند ما تکرار نمی‌کنیم، کارهای موفق را شبیه‌سازی/بهبود می‌دهیم). هدف مطلق: حذف آزمون‌وخطای مجدد. قبل از هر کار جدید الگوهای موفق تکرار و الگوهای ناموفق پرهیز می‌شوند.
>   ۳. **خاطره (حافظه بلندمدت دائمی):** از لحظه افتتاح فروشگاه تا تعطیلی آن تمام رخدادها ذخیره می‌شوند (رفتار/درخواست ادمین، رفتار/تعاملات مشتریان، کارهای روزمره پادو). به‌دلیل وجود اطلاعات بسیار حساس (اطلاعات شخصی مشتریان، داده‌های مالی)، مجهز به بالاترین سطح لایه امنیتی است (رمزنگاری کامل، ایزوله مطلق per-tenant، محدودیت دسترسی سختگیرانه، ردیابی کامل دسترسی).
>
>   **مکمل‌های ضروری (لایه مستقل نیستند):**
>   - **حافظه کاری (کوتاه‌مدت):** زمینه لحظه‌ای کارهای فعال (مثل جلسه ویرایش قالب یا کمپین در حال اجرا) را به‌صورت موقت نگه می‌دارد. پس از پایان کار فقط داده‌های ارزشمند به لایه‌های دانش/تجربه/خاطره منتقل و بقیه پاک می‌شوند تا حافظه حجیم نشود.
>   - **لایه خودبازبینی و نگهداشت دوره‌ای:** فرایند پس‌زمینه خودکار هفتگی/ماهانه که محتوای منقضی‌شده دانش را به‌روزرسانی/حذف می‌کند، الگوهای تجربه را با داده‌های جدید راستی‌آزمایی و الگوهای نادرست را پاک می‌کند، داده‌های خاطره را پس از اتمام دوره نگهداری قانونی حذف/ناشناس‌سازی می‌کند و هرگونه دسترسی غیرمجاز یا ناهنجاری را فوراً گزارش می‌دهد.
>
> **وضعیت تاریخی:** این نقطهٔ توقف با ADR-0004 جانشین شد؛ ادامهٔ فنی نیازمند برنامهٔ
> connector مستقل DeepInfra، Zernio، Playbook و migration کد legacy است.
>
> (تصمیمات قبلی بخش طراحی قالب کماکان به قوت خود باقی است: فرم تک‌سؤالی، سه سطح رایگان/نقره‌ای/طلایی، روند تولید→پیش‌نمایش→۵ ویرایش رایگان+شارژ ویرایش اضافی→تأیید نهایی، عناصر بصری ترکیبی، کد سفارشی Three.js/JS/CSS، تطبیق هوشمند موبایل، مراجع الهام الزامی، اصل taste و پرهیز از پیش‌فرض‌های AI.)

### رشتهٔ آماده — محیط تست مرورگری (منتظر دستور)

سرور وردپرس + پلی‌رایت + کرومیوم آماده است (دستورات سریع در §۷). طبق دستور صریح کارفرما:
**هیچ تستی ساخته نشده و هیچ سناریویی طراحی/اجرا نشده.** تست‌ها فقط وقتی اضافه شوند که
کارفرما بگوید.

### رشتهٔ معلق — سند طراحی امنیت و مدیریت درخواست‌ها

سه شکاف اجرایی‌اش بسته شد (`06cbc0b`)، ولی خودِ سند هنوز نوشته نشده. کارفرما در ۱۴۰۵/۰۶/۳۱
پرسش «۱۳ لایهٔ امنیتی چیست؟» را با «فعلاً موضوع دیگری در اولویت است» رد کرد و بحث به
پادو رفت. **توضیح ۱۳ لایه هنوز داده نشده** — بدون آن سند ناقص می‌ماند. تا خودش پیش
نکشد، سر همان رشتهٔ فعال (پادو) بمان (قاعدهٔ ۶).

### آنچه از قبل برای این سند آماده است

- ✅ هفت قاعدهٔ مدیریت درخواست — `PROJECT-STATE.md §۵.۵`
- ✅ پنج منبع استاندارد تحقیق‌شده — `امنیت و مراقبت/منابع/`
- ✅ سه شکاف بسته‌شده به‌عنوان پایه — `DESIGN-GAPS-FIX.md §۷`
- ✅ `§۱۴.۷` باز در `DESIGN-DEPLOY-VIRA.md`: قرارداد رویداد امنیتی · نقطهٔ گزارش ·
  آستانه‌ها · رابطه با `igbz_audit` · ضدنویز · دسترسی `igbz_vira_security`
- ⏳ **قرارداد سمت سرور برای امضای بیومتریک** (`DESIGN-LEGAL-AUTH.md §۷.۶`) — چون اپ
  معلق است، سرور نباید منتظرش بماند. شکل امضا، هدرها، ضدبازپخش (nonce + مهر زمانی)،
  و چرخهٔ حیات دستگاه باید تعریف شوند.

---

## ۴. کارهای جلسات اخیر

### جلسهٔ ۱۴۰۵/۰۵/۳۱ (قبلی)

#### الف) سه شکاف امنیتی بسته شد

| شکاف | راه‌حل |
|---|---|
| مسیرهای هستهٔ وردپرس/ووکامرس باز بودند | `src/Support/CoreSurfaceGuard.php` روی `rest_pre_dispatch` |
| کول‌داون OTP فقط با شماره | شرط `phone AND ip_hash` + ایندکس `phone_ip_purpose` |
| قرارداد حقوقی: جدول بود، کد نبود | `Modules/MultiTenant/Payments/LegalWaiverService.php` |

**تأیید سه‌لایه:** تست (۱۲۵۱ اظهارنظر) · **جهش** (هر سه رفع را خراب کردم، تست قرمز شد) ·
**زنده** روی وردپرس ۷.۰.۴ + ووکامرس ۱۱.۰.۱.

نتیجهٔ تأیید زنده:
```
wc/v3/customers → 403 ✓   wp/v2/posts    → 200 ✓ دست‌نخورده
wp/v2/users     → 403 ✓   wc/v3/products → 401 (۴۰۱ خود ووکامرس)
igbz/v1 → هر ۱۳۰ مسیر سرجایشان
```

**نکتهٔ طراحی:** خواندن تک‌رکورد (`/wc/v3/customers/42`) **عمداً باز ماند** — صفحات ادمین
خودمان از آن استفاده می‌کنند و بستنش خودزنی بود. ولی `/batch` جزو گروهی شمرده می‌شود.

#### ب) پوشهٔ «امنیت و مراقبت» ساخته شد

به دستور کارفرما: «اسنادی که ازشون استفاده کردی رو اونجا بذار که بتونم چک کنم با کاری
که انجام شده». شش سند، هرکدام با بخش «کجای پروژه استفاده شد».

#### ج) نقصی که حین کار پیدا شد

`apply_filters` در `tests/bootstrap.php` یک **no-op** بود — مقدار را دست‌نخورده برمی‌گرداند.
یعنی هر رفتار مبتنی بر فیلتر **غیرقابل تست** بود و تست می‌توانست فیلتر ثبت کند، اثری
نبیند، و **باز هم سبز شود**. واقعی شد و `remove_filter` اضافه گردید.

---

### جلسهٔ ۱۴۰۵/۰۶/۳۱ (این جلسه) — سند اتوماسیون + سرور وردپرس + محیط تست مرورگری

#### الف) سند معماری اتوماسیون — `DESIGN-AUTOMATION.md`

متن کامل را کارفرما فرستاد و گفت بدون تغییر ذخیره کن. ۱۵۷۱ خط عیناً ذخیره شد؛
کامیت `66a5924` روی `arena/01a025f2-igbz-wp` پوش شد — **هنوز روی `main` ادغام نشده.**
خلاصه: n8n هستهٔ orchestration · ووکامرس منبع حقیقت دادهٔ تجاری · پنج Agent
(SEO/Marketing/CRM/Support/Instagram) · Approval Center برای سه عملیات حساس (تغییر قیمت،
Refund، انتشار اینستاگرام) · PostgreSQL + pgvector · ۱۰ وردفلوی n8n (WF-001 تا WF-010) ·
۸ فاز پیاده‌سازی · MVP بدون اینستاگرام.

#### ب-مکمل) سند `DESIGN-AUTOMATION.md` — بررسی و بدهی‌های سندی (۱۴۰۶/۰۵/۳۱)

خودِ سند در ۱۴۰۵/۰۶/۳۱ عیناً از طرف کارفرما ثبت شد (کامیت `66a5924` روی `arena/01a025f2-igbz-wp`، هنوز روی `main` نیست). **۱۵۷۲ خط، ۴۱ بخش** — پلتفرم اتوماسیون کسب‌وکار با هسته n8n + ۵ عامل AI (SEO, Marketing, CRM, Support, Instagram) + Approval Center + PostgreSQL/pgvector + ۱۰ وردفلو.
**نقاط هم‌راستا با قواعد پروژه:** HITL برای عملیات حساس (قیمت، Refund، انتشار اینستاگرام) · Single Source of Truth روی ووکامرس برای داده‌های تجاری · ممنوعیت حدس‌زدن قیمت/موجودی/وضعیت سفارش · مخالفت با محتوای کم‌ارزش SEO · Guardrails و Audit Log.
**ناسازگاری‌هایی که باید در زمان پیاده‌سازی/بازبینی سند رفع شوند (بدهی سندی):**
- هسته Orchestration سند **n8n** است، اما بعداً **ویرا** (نسخه ۰.۹.۹) به‌عنوان هسته بومی Skills/MCP/Permissions/HITL ساخته شد؛ نیاز به تدقیق نقش هرکدام.
- این ناسازگاری در ADR-0004 بسته شد: IGBZ اتصال مستقیم Meta ندارد؛ Zernio تنها مسیر رسمی
  اجتماعی است و Manus/ChatPlace/Windsor در معماری هدف نیستند.
- عامل‌های درون‌چرخه n8n در تضاد با تصمیم «پادو سرویس بیرونی است؛ ما اسکیل می‌نویسیم و پروسه‌ای روی سرور ما به‌ازای فروشگاه اجرا نمی‌شود» (`DESIGN-PADO.md §۱۵`) است.
- قوانین بازار ایران (درگاه‌های ریالی، اینماد، شاهکار، COD، تومان/kg) در سند لحاظ نشده‌اند. تصمیم ۱۴۰۶/۰۵/۳۱: المنتور به‌دلیل حجم بالا (حدود ۱۰۸ مگابایت) از نصب پیش‌فرض حذف شد.
- به افزونه اصلی IGBZ-WP (۷۲ جدول، ۶ ماژول، `CoreSurfaceGuard`، صف در `Action Scheduler` که `PROJECT-STATE §۵.۵` الزام می‌کند) ارجاع داده نشده.

#### ب) سرور وردپرس با ابزار خودِ ریپو بالا آمد (`_devenv`)

`setup.sh` سپس `run.sh --port 9400` → WordPress Playground (PHP-Wasm 8.2.32) روی `0.0.0.0:9400`:
وردپرس 7.0.4 · ووکامرس 11.0.1 فعال · igbz-suite فعال با هر ۶ ماژول · ۷۲/۷۲ جدول.
سلامت: `/?igbz_health=1` (mu-plugin خود devenv — همیشه با کوكی‌جار و `-L`).
`--login` یعنی مرورگر خودکار با کاربر admin لاگین می‌شود؛ پیشخوان اولین بار روی
ویزارد راه‌اندازی ووکامرس باز می‌شود. دستورات دقیق: §۷.

#### ج) محیط Playwright + Chromium — `/home/user/browser-test` (خارج از ریپو)

به دستور کارفرما («مثل مهندس ارشد زیرساخت و DevOps») فقط محیط آماده شد؛ **هیچ تستی ساخته نشد.**

- **مانع:** فقط `registry.npmjs.org` باز است؛ `cdn.playwright.dev` · `playwright.azureedge.net`
  · `registry.npmmirror.com` · `deb.debian.org` · `storage.googleapis.com` · `wordpress.org` همه بلاک.
- **راه‌حل:** باینری Chromium از کانال npm — `@sparticuz/chromium` (باینری + libهای اشتراکی + فونت،
  همه داخل تاربال npm؛ نیازی به CDN ندارد).
- نسخه‌های پین‌شده: `playwright@1.62.1` · `@playwright/test@1.62.1` · `@sparticuz/chromium@149.0.0`
  → Chromium **149.0.7827.0** · `ldd` صفر lib گمشده (libها از `/tmp/al2023/lib`).
- فایل‌های محیط (**همه از قبل ساخته شده‌اند — دوباره نساز**):

| فایل | نقش |
|---|---|
| `env.sh` | قبل از هر کاری `source ./env.sh` — استخراج باینری/لایبرری/فونت به `/tmp` + تنظیم مسیرها |
| `bootstrap.cjs` | استخراج idempotent (`--ready` برای چک، `--exports` برای شل) |
| `playwright.config.ts` | `executablePath=/tmp/chromium` · `workers=1` · `timeout=120s` · `baseURL=$WP_BASE_URL` |
| `verify-env.cjs` | راستی‌آزمایی محیط (لانچ/نسخه/DOM/اسکرین‌شات) — تست نیست |
| `shot.js` + `shots/` | هارنس اسکرین‌شات قدیمی‌تر با puppeteer-core (نگه داشته شد؛ پلی‌رایت جایگزین اصلی است) |

- تأیید نهایی: smoke سبز (`browser.version()=149.0.7827.0` · DOM read-back ✓ · اسکرین‌شات ✓) ·
  `npx playwright test --list` → «Total: 0 tests» (وضعیت درست) · `git status` ریپو خالی ·
  سلامت وردپرس بدون تغییر.

#### د) منابع الهام طراحی قالب پادو — ✅ تأیید کارفرما (۱۴۰۶/۰۵/۳۱) و نصب اسکیل مرجع

> جزئیات کامل جمع‌بندی و نقطهٔ ادامهٔ بحث: **§۳ رشتهٔ فعال**.

کارفرما دستور داد: «با ووکامرس MCP کاری نداریم، فقط همون دو مورد اول باید برای پادو نصب بشن.»
پس از سه منبع معرفی‌شده، فقط دو منبع اول پذیرفته و نصب/ثبت شدند؛ سومی (MCP داخلی ووکامرس) به‌دلیل
هشدار امنیتی کنار گذاشته شد (یافته ۱ §۵).

- ✅ **`github.com/nextlevelbuilder/ui-ux-pro-max-skill`** (کامیت `bc826e2`) — هفت اسکیل Claude با
  ساختار `SKILL.md` + مراجع جدا (استایل/پالت/فونت/قواعد UX). اسکیل‌ها به‌صورت پیش‌فرض در ویرا
  نصب شده‌اند (پوشهٔ `.vira/skills/` همین مخزن، git-ignored؛ در استقرار سراسری به
  `~/.vira/skills/` می‌رود). **فقط الگو است**؛ وردپرس/ووکامرس و فونت فارسی ندارد و مستقیم
  برای تولید قالب فارسی استفاده نمی‌شود — صرفاً به‌عنوان الگوی ساختار SKILL.md و قواعد UX پایه.
- ✅ **`21st.dev`** — وب‌سایتی با کامپوننت‌های React/Tailwind مدرن (WebGL hero، liquid metal، wave،
  gradient بار، کارت شیشه‌ای، hoverهای زنده). ⚠️ این یک **مرجع بصری** است، نه اسکیل فایلی — چیزی
  در ویرا نصب نمی‌شود؛ الگوی «سه‌بعدی/عمق» در CSS (hero با shader، کارت شیشه‌ای، گرادیان بار و…)
  از آن الهام می‌گیرد.
- ❌ **MCP/Abilities داخلی ووکامرس** — از لیست منابع پادو **حذف شد**. احراز هویتش با کلید REST
  (`X-MCP-API-Key`) یک مسیر احراز موازی است که `CoreSurfaceGuard` را دور می‌زند و تا حل آن
  نباید در فروشگاه‌های تولیدی فعال شود (جزئیات در §۵ یافته ۱ و
  `امنیت و مراقبت/منابع/WordPress/WooCommerce-MCP-Abilities.md`).
- جمع‌بندی پیشنهادیِ مرحلهٔ ۱ (قالب بلوکی FSE فرزند · پرسش‌نامهٔ پنل ~۱۷ سؤال · اسکیل
  `igbz-store-theme` با ۷ مرجع · گارد بک‌اند برای zip · پرامپت ثابت + مرحلهٔ پیشنهاد طراحی)
  **هنوز در انتظار تأیید نهایی کارفرماست**؛ فقط گزینش و نصب دو منبع بالا انجام شد.
- تصمیم‌های باز (مثل قبل): قالب مادرِ خودمان + فرزند، یا قالب کامل هر بار؟ · مسیر تحویل zip
  (آپلود دستی ادمین یا از افزونه؟).
- معماری پذیرفته‌شده (ثبت‌شده در `PROJECT-STATE.md` و `DESIGN-DEPLOY-VIRA.md §۱۱`): پادو سرویس
  ابری بیرونی است؛ ما فقط اسکیل می‌نویسیم؛ zip را ادمین از بخش قالب‌های وردپرس نصب می‌کند.

---

### جلسهٔ ۱۴۰۶/۰۵/۳۱ (این جلسه) — فروشگاه نمونهٔ آرایشی + بوت‌استرپ + مستندسازی روند افزودن فروشگاه + حذف المنتور

> ⚠️ **تصمیم نهایی (۱۴۰۶/۰۵/۳۱ — همین جلسه):** المنتور و قالب Hello Elementor به‌دلیل حجم بالا
> (حدود ۱۰۸ مگابایت در ریپو) **کلاً حذف شدند.** دیگر پوشه‌های `_devenv/elementor/` و
> `_devenv/hello-elementor/` وجود ندارند، `BUNDLED_PLUGINS` و `BUNDLED_THEMES` خالی‌اند،
> و mu-plugin `030` اکنون به‌جای Hello Elementor، قالب پیش‌فرض هسته وردپرس (Twenty
> Twenty-Five در WP 6.4+) را فعال می‌کند. تصمیم‌های مرتبط با طراحی قالب پادو (FSE
> فرزند، سه‌بعدی، اسکیل `igbz-store-theme`) **بدون پیش‌نیاز المنتور** نگاشته می‌شوند.

#### الف) فروشگاه نمونه روی `_devenv` (دمو، کد محصول نیست)

- در نسخهٔ اولیه دو سورس‌زیپ المنتور/Hello Elementor به‌صورت آفلاین در `_devenv/` قرار
  گرفته بودند، اما به دستور کارفرما (حجم بالا) **حذف شدند.**
- `setup.sh` در نسخهٔ نهایی (v6):
  - `BUNDLED_PLUGINS=()` و `BUNDLED_THEMES=()` (بدون افزونه/قالب باندل اضافه)
  - تولید هفت mu-plugin: `000` (فعال‌سازی به‌ترتیب WC + igbz-suite)، `010` (ماژول‌ها)،
    `020` (health check)، `030` (بوت‌استرپ فروشگاه — نسخه v6، switch_theme در
    `setup_theme` به اولین قالب هسته موجود (twentytwentyfive → …)، تنظیمات IR/IRR،
    صفحات فارسی، RTL)، `040` (CSS راست‌چین)، `050` (ترجمه موقت رشته‌های ووکامرس + نماد
    «تومان» + اعداد فارسی)، `100` (بذر فروشگاه)
- `run.sh` مانت‌های elementor/hello-elementor را ندارد.
- فروشگاه نمونه:
  - **عنوان:** «شاپ بیوتی — فروشگاه نمونه آرایشی»
  - **۸ دسته:** مراقبت پوست، مراقبت مو، آرایش صورت، آرایش چشم، آرایش لب، عطر و ادکلن، بهداشت بدن، ابزار آرایش
  - **۲۵ محصول ساده** با SKUهای `SAMPLE-0001..0025`، قیمت ~۴۵۰هزار تا ۶٫۲۰۰٫۰۰۰ ریال، ~۶ محصول حراج
  - **۱۰۰ مشتری** (`customer001@example.com`..`customer100@example.com`، رمز `Customer123!`، نام/نام‌خانوادگی فارسی، تلفن/آدرس در تهران/کرج)
  - **۸ سفارش نمونه** با وضعیت‌های processing/completed/on-hold
  - واحد پول IRR (نمایش «تومان») · بدون اعشار · جداکننده هزارتایی `,` · کشور IR · وزن kg
  - تصویر شاخص گرادیانت سرخابی-صورتی تولیدشده با GD
  - راست‌چین (RTL) با mu-plugin های 030/040/050
  - صفحهٔ اصلی = صفحه فروشگاه · پرمالینک `/%postname%/` · `woocommerce_coming_soon=no`
- تأیید زنده: HTTP 200 · `dir="rtl"` · عنوان `<h1>فروشگاه</h1>` · محصولات قابل افزودن به سبد.

#### ب) به‌روزرسانی اسناد پس از حذف المنتور

- `PROJECT-STATE.md §۱`: بخش «پیش‌نیازهای نصب فروشگاه» به‌روزرسانی شد (المنتور حذف شد).
- `DESIGN-PADO.md §۸` و `§۱۷`: بلاک نقل‌قول پیش‌نیاز المنتور و یادداشت‌های وابسته حذف/بازنویسی شدند.
- `DESIGN-AUTOMATION.md §۲، §۳۳، §۳۵، §۴۲`: تمام ارجاع‌ها به المنتور به‌عنوان پیش‌فرض حذف شد و ممنوعیت جدید «عدم تحمیل صفحه‌ساز سنگین در پیش‌فرض» جایگزین گشت.
- انتخاب قالب نهایی (FSE فرزند روی قالب هسته یا قالب مادر خودمان) در سوالات بند ۷ پلن پادو از کارفرما پرسیده می‌شود.

#### ج) سند `HANDOFF-PROMPT.md §۷.۵` — روند افزودن فروشگاه

- بخش جدید **§۷.۵** «🏬 افزودن فروشگاه جدید — روند بوت‌استرپ» در همین فایل اضافه شد و پس
  از تصمیم حذف المنتور **بازنویسی گشت** (نسخه v6):
  - **پیش‌نیاز قطعی: هیچ صفحه‌ساز ثالثی پیش‌فرض نیست**؛ قالب پیش‌فرض = قالب هسته وردپرس.
  - **ترتیب ۱۴گام اجرای بوت‌استرپ** با هوک‌های دقیق (`setup_theme` برای switch_theme،
    `wp_loaded` با اولویت ۵۰ برای بقیه)
  - قواعد عمومی (idempotent بودن، ترتیب فعال‌سازی، عدم استفاده از `rand`/`mt_rand`)
  - جدول دام‌های شناخته‌شده (fatal switch_theme دیرهنگام، بنر Coming Soon، عناوین
    انگلیسی صفحات، … — دام کیت المنتور حذف شد)
  - فهرست فایل‌های مرجع کارکرده
  - یادداشت صریح «وضعیت کنونی کد» که بوت‌استرپ فقط در mu-plugin های devenv هست و
    هنوز در `igbz-suite/src/` کدنویسی نشده (کار باز).
- بخش **§۹ نقشهٔ فایل‌ها** به‌روزرسانی شد (حذف پوشه‌های elementor/hello-elementor، به‌روزرسانی شرح هفت mu-plugin).
- **§۱۰ جملهٔ شروع پیشنهادی** به‌روزرسانی شد.

> ℹ️ mu-plugin `050-fa-demo.php` (ترجمه موقت و اعداد فارسی) به‌صورت heredoc در setup.sh درج شد
> و هر بار با `bash _devenv/setup.sh` تولید می‌گردد. در تولید از بستهٔ رسمی زبان `fa_IR`
> استفاده می‌شود، نه از این mu-plugin — این فقط برای دمو/آفلاین است.

---

### جلسهٔ ۱۴۰۶/۰۶/۰۲ (۲۴ اوت ۲۰۲۶) — پیش‌نمایش زنده برای کارفرما: مشکل، رفع‌ها و دو راه‌حل نهایی

این جلسه کلاً صرف یک نیاز کارفرما شد: **«یک لینک لایو بده که خودم پیش‌نمایش فروشگاه/افزونه را ببینم.»**
سه مشکل جدا پیدا و رفع شد و در نهایت دو راه‌حل ماندگار برقرار گردید. جزئیات کامل در **§۷.۶**.

> **تکملهٔ ۱۴۰۶/۰۶/۰۲ (ادامهٔ همان روز) — تست ویژوال کامل + هفت رفع + قانون ۱۲:**
> به دستور کارفرما پوشهٔ `visual-testing/` در ریپو ساخته شد؛ تمام ۲۹ منوی پنل IGBZ
> و ۵ صفحهٔ فروشگاه با Chromium واقعی باز، اسکرین‌شات و چشمی بازبینی شدند
> (۳۵ تصویر + `_raw-report.json` + `REPORT.md`). نتیجهٔ نهایی: **۳۴ صفحه · ۰ خطا
> · ۰ هشدار**. هفت مشکل پیدا و رفع شد — دو باگ کد افزونه: (م۱) دوباره‌رندر صفحهٔ
> Status به‌خاطر کال‌بک تکراری زیرمنوی هم‌اسلاگ با والد در `StatusPage.php`؛
> (م۵) نبود `dir=rtl` در کارت پادو (`PadoPage.php`). پنج مشکل devenv: (م۲)
> **بایت backspace واقعی (0x08) به‌جای `\b`** در regex مبدل ارقام `050-fa-demo`
> ⇒ اسکریپت‌ها/اتریبیوت‌ها فارسی می‌شدند (SyntaxError، svg خراب، 404 تصاویر)؛
> (م۳) سرریز ۱۹.۵k پیکسلی تسویه (اینپوت مخفی `address_2` با left منفی در RTL)؛
> (م۴) کشور پیش‌فرض تسویه US (رفع: `woocommerce_default_customer_address=base`
> + پاکسازی سشن — mu جدید `031`)؛ (م۶) خرابی entityهای عددی HTML توسط مبدل
> ارقام؛ (م۷) تصاویر شکستهٔ CDN ایموجی `s.w.org` (mu جدید `015-no-emoji-cdn`).
> تست واحد: ۱۲۴۸ سبز، ۲ قرمز از قبل موجود (ModulesTest از ماژول ششم pado
> به‌روز نشده — با stash تأیید شد؛ کار باز). **قانون سختگیرانهٔ ۱۲ (تست ویژوال
> اجباری) در §۰ ثبت شد.** ابزار دائمی: `browser-test/visual-crawl.cjs`.
> ⚠️ کار باز: علت کارنکردن منوهای IGBZ روی playground.wordpress.net آنلاین
> (گزارش کارفرما) هنوز جداگانه دیباگ نشده — در این محیط همهٔ منوها سالم‌اند.

---

### جلسهٔ ۱۴۰۶/۰۶/۰۳–۰۴ (۲۵–۲۶ اوت ۲۰۲۶) — اتوماسیون جولز + استقرار ریل‌وی

#### الف) خط لولهٔ تمام‌خودکار «جولز» (Google Jules) — برقرار و آزموده ✅

کارفرما خواست عامل کدنویس با پلن رایگان؛ تحقیق شد و جولز (رایگان، ۱۵ تسک/روز،
سخاوتمندترین پلن رایگان در دستهٔ عامل‌های پس‌زمینه) انتخاب شد. معماری (گزینهٔ «ب»
تمام‌خودکار):

```
ایجنت فایل jules-tasks/TASK-*.md پوش می‌کند
  ← ورک‌فلوی .github/workflows/jules-dispatcher.yml (نصب دستی کارفرما)
  ← با سیکرت JULES_API_KEY یک session در API جولز می‌سازد (AUTO_CREATE_PR)
  ← مارکر ضد-تکرار در jules-tasks/.dispatched/ کامیت می‌شود
  ← جولز PR باز می‌کند ← بازبینی ایجنت + تست ویژوال ← تصمیم ادغام با کارفرما
```

- قالب تسک و قواعد: `jules-tasks/README.md` · نسخهٔ مرجع ورک‌فلو:
  `_devenv/github-workflow-jules-dispatcher.yml`
- **آزمون واقعی موفق:** تسک `TASK-001` (رفع ۲ تست قرمز از-قبل-موجود ModulesTest
  برای ماژول ششم pado) ارسال شد — session:
  `sessions/16522588723806306581` — و جولز **PR #9** را ساخت.
- 🔴 **وضعیت PR #9: دیف آن بازبینی شد و کاملاً درست است** (فقط
  `igbz-suite/tests/ModulesTest.php`: پنج←شش ماژول، پیش‌فرض
  `['multitenant','pado']`، پیام‌ها اصلاح). اما کارفرما آن را **بست (بدون ادغام)**
  — حین خطای ریل‌وی گمان کرد شاخهٔ جولز علت خطاست (نبود؛ خطای ریل‌وی بی‌ربط بود).
  **کار باز:** یا PR #9 را در گیت‌هاب Reopen و ادغام کنید، یا همان دو خط را
  ایجنت با تأیید کارفرما مستقیم اعمال کند. تا آن موقع همان ۲ تست قرمز می‌مانند.
- درس‌های API جولز: احراز با هدر `X-Goog-Api-Key` · نام source به شکل
  `sources/github/<owner>/<repo>` · برای شاخه `refType` لازم نیست ولی
  `startingBranch` مهم است · مصرف: هر session یک تسک از سهمیهٔ روزانه.

#### ب) پیش‌نمایش دائمی فروشگاه روی ریل‌وی — فایل‌ها آماده، استقرار ناتمام ⏳

کارفرما سرور Railway دارد و سیکرت `RAILWAY_TOKEN` را در مخزن ساخته. ساخته شد:

| فایل | نقش |
|---|---|
| `railway/Dockerfile` | وردپرس رسمی php8.2-apache + wp-cli + کپی igbz-suite از مخزن + رفع mpm |
| `railway/entrypoint.sh` | بوت‌استرپ بی‌ویزارد طبق §۷.۵: صبر دیتابیس، نصب هسته (رمز ادمین از env)، ووکامرس قبل از igbz-suite، بستهٔ زبان fa_IR واقعی، home/siteurl از `RAILWAY_PUBLIC_DOMAIN` |
| `railway/mu-plugins/` | همان mu-plugin های آزموده (۰۱۰/۰۲۰/۰۳۰/۱۰۰ — ماژول‌ها/سلامت/بوت‌استرپ/بذر) |
| `railway.json` (ریشه) | builder=DOCKERFILE با مسیر `railway/Dockerfile` |
| `_devenv/github-workflow-railway-volume.yml` | «نگهبان ولوم»: با CLI ریل‌وی چک می‌کند ولوم `/var/www/html/wp-content` روی سرویسِ هم‌نامِ مخزن هست یا نه؛ نبود ← می‌سازد. نصب دستی در `.github/workflows/railway-volume.yml` |
| `railway/README.md` | راهنمای فارسی گام‌به‌گام پنل (شامل جایگزین MySQL) |

نکته‌های کلیدی که به کارفرما آموزش داده شد (در راهنما هم هست):
- **MySQL در پنل ریل‌ویِ کارفرما نبود** (فقط پستگرس/مونگو) ← جایگزین: سرویس از
  ایمیج داکر `mysql:8.0` با نام `MySQL` + متغیرهای `MYSQL_*` + ولوم روی
  `/var/lib/mysql` + اتصال فروشگاه با `WORDPRESS_DB_HOST=mysql.railway.internal:3306`.
- **دو ولوم جدا لازم است:** دیتابیس `/var/lib/mysql` و فروشگاه
  `/var/www/html/wp-content` (نگهبان فقط دومی را پوشش می‌دهد).
- `RAILWAY_PUBLIC_DOMAIN` خودکار تزریق می‌شود ولی **فقط بعد از Generate Domain**؛
  اگر دامنه بعد از استقرار ساخته شد، یک Redeploy لازم است.

🔴 **وضعیت فعلی استقرار — خطا و علت دقیق:** بیلد ریل‌وی دو بار با
«Railpack could not determine how to build» شکست. علت از خود لاگ قطعی شد:
ریل‌وی **شاخهٔ `main`** را می‌سازد که فایل‌های railway را **ندارد** (در فهرست
درختی لاگ، `railway/` و `railway.json` غایب‌اند؛ هر دو بیلد هم snapshot یکسان
`sha256:912627…` داشتند یعنی تغییر شاخهٔ کارفرما اعمال نشده بود). **راه‌حل:
ادغام PR #8 در main** (کارفرما ادغام را به پایان این جلسه موکول کرد تا چت بسته
نشود) و سپس یک Deploy تازه. نشانهٔ درستی بیلد: در لاگ باید ساخت Dockerfile
دیده شود (`FROM wordpress:php8.2-apache`)، نه Railpack.

#### ج) قواعد/تصمیم‌های جدید این جلسه

- 🔴 قانون «پیام‌های چت فارسیِ تمیزِ راست‌به‌چپ» در §۰ ثبت شد (الزام‌های دیگر).
- کارفرما امکان پرداخت ارزی ندارد ⇒ ابزارهای پولی (کوپایلت، کدکس، کلود کد،
  دوین) منتفی؛ **جولز = انتخاب قطعی عامل کدنویس** (بهترین رایگان). گزینهٔ
  آیندهٔ بدون هزینهٔ اشتراک: OpenHands روی ریل‌وی + سهمیهٔ رایگان Gemini/Qwen.
- سرور کارفرما: **Railway** — علاوه بر پیش‌نمایش، مقصد آیندهٔ n8n
  (قالب یک‌کلیکی دارد) و استقرار نهایی است.
- 🔴 کشف پروکسی گیت‌هاب سندباکس (تکمیل §۷.۶-پ): هدر Authorization همهٔ
  درخواست‌ها به گیت‌هاب با توکن App سندباکس جایگزین می‌شود —**PAT کارفرما از
  داخل سندباکس بی‌اثر است** (۳ توکن تست شد: fine-grained بدون مجوز، fine-grained
  با مجوز، classic — همه شکست خوردند؛ با توکن قلابی هم ۲۰۰ گرفتیم = اثبات
  جایگزینی). سیکرت‌ها فقط در GitHub Actions قابل استفاده‌اند، نه در سندباکس.
  API ریل‌وی هم از سندباکس بسته است (`backboard.railway.com → 000`) ⇒ همهٔ
  کارهای ریل‌وی یا با ورک‌فلو یا دست کارفرما.

**خلاصهٔ رخدادها:**
1. **پیش‌نمایش سندباکس (`https://9400-<sandbox-id>.e2b.app`) اول کار نمی‌کرد** — دو علت جدا:
   - (الف) وردپرس `siteurl`/`home` را `http://127.0.0.1:9400` ذخیره کرده بود ⇒ از دامنهٔ
     پیش‌نمایش، همهٔ لینک‌ها/CSS/تصاویر به `127.0.0.1` می‌خوردند و می‌شکستند.
     **رفع:** mu-plugin جدید `005-dynamic-host.php` (هاست پویا — §۷.۶-الف).
   - (ب) mu-plugin دموی `050-fa-demo.php` ارقام داخل `href`/`src` را هم فارسی می‌کرد
     (`http://۱۲۷.۰.۰.۱:۹۴۰۰/...`) و URLها را می‌شکست. **رفع:** لایهٔ ایمنی در بافر
     خروجیِ `005` که ارقام داخل اتریبیوت‌های URL را به لاتین برمی‌گرداند (متن صفحه
     مثل قیمت‌ها فارسی می‌ماند).
2. **پیش‌نمایش سندباکس چند بار «Sandbox Not Found» داد** — عمر پیش‌نمایش e2b.app به عمر
   سندباکسِ جلسه گره خورده است؛ با خواب/مرگ سندباکس می‌میرد. **دورزدنی نیست** — فقط
   راه‌اندازی مجدد (§۷) یا استفاده از راه‌حل مستقل از سندباکس (§۷.۶).
3. **راه‌حل ماندگار:** تست آنلاین از خود گیت‌هاب با WordPress Playground رسمی — دو شکل:
   لینک `?blueprint-url=` (فوری) و دکمهٔ خودکار PR (اکشن رسمی v3). هر دو برقرار و
   تست‌شده‌اند (§۷.۶-ب و §۷.۶-پ).

---


## ۵. دو یافتهٔ مهم تحقیق که باید در طراحی لحاظ شوند

### 🔴 یافتهٔ ۱: MCP/Abilities داخلی ووکامرس — ❌ استفاده‌نشده تا رفع مانع امنیتی

`grep` روی همان زیپ ووکامرس ۱۱.۰.۱ که داریم: **۵۷ فایل MCP** و **هشت قابلیت آمادهٔ
دامنه‌ای** در `src/Internal/Abilities/Domain/`:

```
ProductsQuery · ProductCreate · ProductUpdate · ProductDelete
OrdersQuery · OrderUpdateStatus · OrderAddNote · AbstractDomainAbility
```

فقط یک پرچم می‌خواهد: `mcp_integration`. در صورت روشن‌بودن بخش بزرگی از فاز ۱ و ۵ پادو
را می‌پوشاند.

> **⚠️ تصمیم کارفرما (۱۴۰۶/۰۵/۳۱): فعلاً با این MCP کاری نداریم.**
> **دلیل:** احراز هویتش با کلید REST ووکامرس است (`X-MCP-API-Key: ck_...:cs_...`) —
> یک **مسیر احراز موازی** که `CoreSurfaceGuard` را دور می‌زند. اگر روشن شود،
> `OrdersQuery` داده‌های مشتری را بدون عبور از گیت ما بیرون می‌دهد.
> **این باید در سند امنیت حل شود، پیش از هر استفاده‌ای از MCP.** تا آن زمان، پرچم
> `mcp_integration` خاموش می‌ماند و در اسناد طراحی پادو به‌عنوان منبع الهام یا
> ابزار عملیاتی ارجاع داده نمی‌شود.

جزئیات: `امنیت و مراقبت/منابع/WordPress/WooCommerce-MCP-Abilities.md`

### ⭐ یافتهٔ ۲: چهار گاردریل که در سند پادؤ ما نیست

از پروژهٔ MIT ‏`nitin27may/e-commerce-agents` (۳۳۱ فایل پایتون، ۸۴ فایل تست):

| گاردریل | چرا لازم است |
|---|---|
| دفاع در برابر **تزریق پرامپت** | مشتری در دایرکت می‌تواند به عامل دستور بدهد |
| **سقف هزینهٔ توکن** | همان ریسک OWASP API4 |
| **پالایش خروجی** | «آبرو» در جدول ریسک سند |
| **grounding** | مهار توهم مدل در پاسخ به مشتری |

و مهم‌تر: **`shared/hitl.py`** — میان‌افزار تأیید انسانی پیش از اقدام پرریسک، با چرخهٔ
`create → list → claim → resolve → execute`. **دقیقاً همان «مشاهده ← پلن ← تأیید ادمین ←
اجرا ← گزارش»** که سند پادو می‌خواهد و قرار بود از صفر طراحی کنیم.

⚠️ کدش قابل استفاده نیست (پایتون + PostgreSQL + pgvector + تک‌مستأجری). **الگو است.**

جزئیات: `امنیت و مراقبت/منابع/AI-Commerce/e-commerce-agents-MIT.md`

---

## ۶. کارهای باز ثبت‌شده

### در `DESIGN-LEGAL-AUTH.md`
- **`§۷.۶`** امضای بیومتریک برای خروجی گروهی — قرارداد سمت سرور تعریف نشده
- **`§۷.۷`** بستن باقی درهای هسته: `xmlrpc` · `?author=N` · oEmbed · Application
  Passwords · صادرات پنل · `admin-ajax.php`. **کد `CoreSurfaceGuard` تنظیماتشان را دارد
  ولی در سندباکس تأیید زنده نشدند.**

### در `PROJECT-STATE.md`
- بازبینی هم‌خوانی کد فعلی با هفت قاعدهٔ `§۵.۵` — کوئری بدون `LIMIT`، کرون حلقه‌ای،
  سقف ثابت در کد
- ترجمهٔ فارسی · شناسهٔ مدیر ارشد · `sk-demo` · ادغام PR #5

### بررسی‌نشده
- **`nexscope-ai/eCommerce-Skills`** — اسکیل‌های آمادهٔ `SKILL.md`
- **`igbz-status` کد ۴۰۳ می‌دهد.** با `git stash` بررسی شد: **با کد قدیمی هم ۴۰۳ بود** ⇒
  مشکل از پیش موجود، ربطی به تغییرات این جلسه ندارد. باید جدا بررسی شود.

---

### محیط تست مرورگری (جلسهٔ ۱۴۰۵/۰۶/۳۱)
- محیط آماده است (§۷) ولی **هیچ تستی نوشته نشده** — به دستور صریح کارفرما.
  اولین بستهٔ تست‌ها باید بعد از دستور کارفرما و با تأیید او نوشته شود.


## ۷. ⚡ راه‌اندازی سریع محیط در چت تازه — سرور وردپرس + پلی‌رایت

وقتی کارفرما گفت «محیط تست را بالا بیاور» (یا برای هر تست مرورگری)، دقیقاً این ترتیب را
اجرا کن. مجموعاً ~۲ تا ۳ دقیقه. **این‌ها فقط بالا آوردن محیط است — تست و سناریو ممنوع تا
کارفرما بگوید.**

⚠️ **در چت تازه هیچ پروسه‌ای زنده نیست** — پروسه‌های جلسهٔ قبل با پایان آن مرده‌اند و `/tmp`
و `node_modules` ممکن است پاک شده باشند؛ هر دو بخش (الف و ب) را از صفر اجرا کن. اگر پورت
۹۴۰۰ اشغال بود (پروسهٔ زندهٔ قبلی)، اول همان را متوقف کن (ابزار stop_process یا `kill`)،
چون `run.sh` خودش حاضر نیست روی پورت اشغال‌شده بالا بیاید.

### الف) سرور وردپرس + ووکامرس + افزونه (از داخل خود ریپو، بدون اینترنت اضافه)

```bash
cd /home/user/IGBZ-WP
bash _devenv/setup.sh            # idempotent؛ ~۲۰ ثانیه (فقط npm باز است)
```

سپس سرور را به‌صورت **پروسهٔ پس‌زمینه** (ابزار start_process، نه bash) اجرا کن:

```bash
bash _devenv/run.sh --port 9400  # 0.0.0.0:9400 — برای پیش‌نمایش زندهٔ کارفرما
```

سلامت — **حتماً با کوكی‌جار و `-L`، وگرنه حلقهٔ 302**:

```bash
curl -s -c /tmp/cj.txt -b /tmp/cj.txt -L "http://127.0.0.1:9400/?igbz_health=1"
```

انتظار: `{"wp":"7.0.4","php":"8.2.32","wc_active":true,"wc_version":"11.0.1",
"igbz_loaded":true, ... "igbz_tables":"72/72","modules":[...هر ۶ ماژول...]}`

### ب) پلی‌رایت + کرومیوم (`/home/user/browser-test` — فایل‌ها از قبل موجودند)

```bash
cd /home/user/browser-test
[ -d node_modules ] || PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1 npm ci   # فقط اگر پاک شده باشد (~۵ ثانیه)
source ./env.sh                  # استخراج باینری/لایبرری/فونت به /tmp + تنظیم مسیرها
node verify-env.cjs              # انتظار: 149.0.7827.0 · DOM ✓ · screenshot ✓
```

### پ) آمادهٔ تست

- کانفیگ پلی‌رایت (`playwright.config.ts`) از قبل درست است: `baseURL=$WP_BASE_URL`
  (پیش‌فرض `http://127.0.0.1:9400`) · `workers=1` (سرور ۱ worker دارد) · `timeout=120s`.
- فایل‌های `*.spec.ts` را فقط وقتی کارفرما تست خواست بساز، در همان پوشه.
- **هرگز `npx playwright install` نزن** — CDN بلاک است و گیر می‌کند؛ باینری از
  `CHROMIUM_EXECUTABLE_PATH=/tmp/chromium` می‌آید (کانفیگ خودش می‌خواند).
- برای نشان‌دادن سریع صفحه‌ها به کارفرما: `node shot.js` (هارنس قدیمی پاپیت؛ خروجی در
  `shots/`) یا اسکرین‌شات پلی‌رایت در تست. یادت باشد تحلیل چشمی خودت ممکن است خاموش باشد —
  چک‌ها را با DOM/CSS بده (§۸).
- کوكی‌جار برای curl همان `/tmp/cj.txt` است؛ مرورگر (پلی‌رایت/کروم) خودش کوكی‌ها را
  می‌چپاند و ریدایرکت ۳۰۲ لاگین خودکار برایش مشکلی ندارد.

---


## ۷.۶ 🔴 پیش‌نمایش زنده برای کارفرما — دو راه‌حل + رفع‌های لازم (۱۴۰۶/۰۶/۰۲)

کارفرما می‌خواهد **خودش** پیش‌نمایش را در مرورگرش ببیند. دو راه وجود دارد؛ راه ۲
مستقل از سندباکس است و **هرگز نمی‌میرد** — راه ارجح برای کارفرماست.

### الف) راه ۱: پیش‌نمایش سندباکس (`https://9400-<sandbox-id>.e2b.app`)

سرور `run.sh --port 9400` با ابزار start_process بالا می‌آید و پلتفرم آن را به
کارفرما به‌صورت لینک پیش‌نمایش می‌دهد. **دو رفع لازم بود که بدون آن‌ها صفحه در
مرورگر کارفرما خراب است:**

**رفع ۱ — mu-plugin هاست پویا (`_devenv/.work/mu/005-dynamic-host.php`):**
وردپرسِ Playground گزینه‌های `home`/`siteurl` را `http://127.0.0.1:9400` ذخیره می‌کند؛
از دامنهٔ پیش‌نمایش همهٔ لینک‌ها/CSS/تصاویر به `127.0.0.1` اشاره می‌کنند و می‌شکنند.
این mu-plugin (فقط harness؛ git-ignored؛ در صورت پاک‌شدن `.work` باید دوباره ساخته
شود — نسخهٔ مرجع در تاریخچهٔ همین جلسه):
- `pre_option_home`/`pre_option_siteurl` را به «هاستِ همان درخواست» برمی‌گرداند
  (محلی 127.0.0.1 می‌ماند، از پروکسی، دامنهٔ پیش‌نمایش با https می‌شود).
- تشخیص TLS پروکسی با `X-Forwarded-Proto` (و پسوند `.e2b.app`) + ست‌کردن
  `$_SERVER['HTTPS']='on'` تا `is_ssl()` درست و حلقهٔ ریدایرکت قطع شود.
- فیلتر `upload_dir` برای بازنویسی URL آپلودها.
- **لایهٔ ایمنی بافر خروجی** (اولویت -۱ = بیرونی‌ترین بافر): URLهای جامانده را
  بازنویسی می‌کند **و ارقام فارسی‌شده داخل اتریبیوت‌های URL** (`href/src/srcset/…`)
  را به لاتین برمی‌گرداند — چون `050-fa-demo.php` ارقام داخل اتریبیوت‌ها را هم
  فارسی می‌کرد و URL می‌شکست (`http://۱۲۷.۰.۰.۱:۹۴۰۰/...png`). متن صفحه
  (قیمت «۶۲۰,۰۰۰ تومان») فارسی می‌ماند.

**رفع ۲ — تست از دید مرورگر واقعی:** `e2b.app` در **HSTS preload** مرورگرهاست —
`http://` روی آن دامنه خودکار به `https://` ارتقا می‌شود؛ تست محلی http ممکن نیست.
ابزارهای `preview-check.cjs` + `tls-proxy.cjs` در `/home/user/browser-test/` ساخته
شدند: پروکسی TLS محلی (شبیه‌ساز دقیق پروکسی E2B با `X-Forwarded-Proto`) + کرومیوم
با `--host-resolver-rules` و `--ignore-certificate-errors`. تأیید نهایی جلسه:
صفر لینک شکسته، wp-admin با auto-login باز شد، ۳۰ آیتم منوی IGBZ.

**⚠️ محدودیت ذاتی (سه بار برای کارفرما رخ داد):** عمر این پیش‌نمایش = عمر سندباکسِ
جلسه. با خواب/مرگ سندباکس، کارفرما «Sandbox Not Found» می‌بیند. **هیچ راه دورزدنی
از داخل سندباکس ندارد** — فقط راه‌اندازی مجدد سرور با start_process (پروسهٔ قبلی را
اول kill کن) یا استفاده از راه ۲.

### ب) راه ۲ (ارجح): تست آنلاین از گیت‌هاب — WordPress Playground رسمی

`playground.wordpress.net` کل وردپرس را در مرورگر کارفرما اجرا می‌کند (PHP-WASM؛
همان فناوری `_devenv`)، هیچ سروری لازم ندارد و **به سندباکس وابسته نیست**. فایل
Blueprint در خود ریپوست: **`_devenv/playground-blueprint.json`** — ووکامرس از مخزن
وردپرس + igbz-suite با `git:directory` مستقیم از گیت‌هاب (ریپو عمومی است، احراز
نمی‌خواهد) + فعال‌سازی هر ۶ ماژول + حافظه + خاموشی Coming Soon + ورود خودکار admin.

لینک برای کارفرما (پس از ادغام در `main`؛ تا آن موقع نام شاخه را جایگزین کن):

```
https://playground.wordpress.net/?blueprint-url=https://raw.githubusercontent.com/paymanshafayan/IGBZ-WP/main/_devenv/playground-blueprint.json
```

**دو خطای واقعی که کارفرما دید و رفع شد — تکرار نکن:**

| خطا | علت | رفع |
|---|---|---|
| `Git ref "main" not found` | در `git:directory` مقدار `refType` را `refname` گذاشته بودیم؛ برای نام شاخه باید **`branch`** باشد (خطای شایع ثبت‌شده در مستندات رسمی Playground) | `"refType": "branch"` |
| `PHP Fatal: Out of memory (allocated 50331648)` در `jetpack-autoloader` هنگام رندر فروشگاه | حافظهٔ پیش‌فرض PHP در Playground (~۴۸MB) برای ووکامرس+IGBZ کافی نیست | گام `defineWpConfigConsts` با `WP_MEMORY_LIMIT=256M`/`WP_MAX_MEMORY_LIMIT=512M` **+** گام `writeFile` که mu-plugin با `@ini_set('memory_limit','512M')` می‌سازد |

نکته: Notice مربوط به `_load_textdomain_just_in_time` (دامنهٔ woocommerce) **بی‌ضرر**
است — رفتار شناختهٔ خود ووکامرس روی WP جدید؛ ربطی به خرابی صفحه ندارد.

محدودیت‌های راه ۲: هر تب یک نمونهٔ تازه است (با بستن تب داده‌ها می‌روند) و بذر
فروشگاه نمونه (۲۵ محصول و…) آنجا نیست چون mu-pluginهای devenv است، نه بخشی از افزونه.

### پ) دکمهٔ خودکار پیش‌نمایش روی هر PR — برقرار و سبز ✅

ورک‌فلو **`.github/workflows/pr-playground-preview.yml`** (اکشن رسمی
`WordPress/action-wp-playground-pr-preview@v3`) روی ریپو فعال است: با هر PR
(opened/synchronize/reopened) دکمهٔ **«Preview in WordPress Playground»** به توضیحات
PR اضافه می‌شود و همان شاخهٔ PR را در مرورگر تست می‌کند. اولین اجرا روی PR #8 سبز
شد (۱۷ ثانیه) و دکمه ظاهر گردید.

**طراحی مهم ورک‌فلو:** Blueprint داخل ورک‌فلو نیست — از
`_devenv/playground-blueprint.json` همان شاخهٔ PR خوانده می‌شود و فقط `ref` آن با
`github-script` به شاخهٔ PR عوض می‌شود. ⇒ **هر اصلاح آیندهٔ Blueprint فقط ویرایش
همان فایل JSON است** (که ایجنت می‌تواند پوش کند) و هیچ‌وقت نیاز به دست‌زدن به
ورک‌فلو نیست.

**🔴 محدودیت کشف‌شدهٔ مهم (ثبت در §۸ هم):** توکن گیت‌هاب سندباکس مجوز `workflows`
ندارد و **پوش هر فایلی به `.github/workflows/` رد می‌شود**. مهم‌تر: **پروکسی
سندباکس هدر Authorization همهٔ درخواست‌های به گیت‌هاب را با توکن GitHub App خودش
جایگزین می‌کند** — حتی PAT شخصی کارفرما هم به گیت‌هاب نمی‌رسد (با توکن قلابی تست
شد: باز هم 200 گرفت). سه PAT از کارفرما گرفته شد و همه بی‌اثر بودند؛ در نهایت
کارفرما فایل ورک‌فلو را خودش از UI گیت‌هاب ساخت (کامیت `f6a590d`). **دیگر هرگز
برای این کار PAT نگیر — مستقیم دستور ساخت دستی از UI بده.** نسخهٔ مرجع ورک‌فلو
برای بازسازی: `_devenv/github-workflow-pr-preview.yml`.

### ت) فایل‌های مرتبط این بخش

| فایل | نقش |
|---|---|
| `_devenv/playground-blueprint.json` | Blueprint تست آنلاین — تنها نقطهٔ اصلاح آینده |
| `.github/workflows/pr-playground-preview.yml` | ورک‌فلو دکمهٔ PR (ساختهٔ دست کارفرما — از سندباکس قابل ویرایش **نیست**) |
| `_devenv/github-workflow-pr-preview.yml` | نسخهٔ مرجع/قالب همان ورک‌فلو (قابل پوش از سندباکس) |
| `_devenv/PLAYGROUND-LINKS.md` | سند لینک‌ها + دستورالعمل کامل برای کارفرما |
| `_devenv/LINK-FOR-EMPLOYER.txt` | لینک آمادهٔ کپی برای کارفرما |
| `_devenv/.work/mu/005-dynamic-host.php` | mu-plugin هاست پویا (harness، git-ignored) |
| `/home/user/browser-test/preview-check.cjs` · `tls-proxy.cjs` | راستی‌آزمایی پیش‌نمایش از دید مرورگر واقعی |

---

## ۷.۵ 🏬 افزودن فروشگاه جدید — روند بوت‌استرپ

این بخش روند **نصب و آماده‌سازی یک فروشگاهٔ تازه‌تأسیس** در سیستم چندمستأجری IGBZ را
توصیف می‌کند. الگوی مرجعِ کارکرده در mu-plugin‌های `_devenv` (۰۳۰ و ۱۰۰) موجود است؛
زمان پیاده‌سازی واقعی این روند در کد `igbz-suite/src/` (جریان ثبت‌نام در ماژول `hub`
یا بوت‌استرپ `multitenant`)، **همین ترتیب و همین دام‌ها** عیناً رعایت شوند.

### الف) پیش‌نیازهای قطعی (تصمیم کارفرما — ۱۴۰۶/۰۵/۳۱ ≈ ۲۲ اوت ۲۰۲۶)

هر فروشگاهٔ تازه در IGBZ **به‌صورت پیش‌فرض** فقط با این پشته بالا می‌آید:

- هستهٔ وردپرس + **WooCommerce** (الزامی)
- **افزونهٔ IGBZ-Suite** (الزامی)
- **قالب پیش‌فرض هسته وردپرس** (Twent Twenty-Five و نسخه‌های بعدی) — هیچ صفحه‌ساز
  ثالثی (از جمله Elementor) و هیچ قالب باندل‌شده‌ای به‌صورت پیش‌فرض نصب **نمی‌شود**.

مدیر فروشگاه می‌تواند بعداً هر قالب یا صفحه‌ساز دیگری (از جمله Elementor در صورت
تمایل) نصب کند، اما مرحلهٔ ۱ پادو («طراحی قالب») روی یک قالب فرزند FSE از قالب
هسته ساخته می‌شود و **به نصب صفحه‌ساز ثالث وابسته نیست.** این قاعده در
`PROJECT-STATE.md §۱` و `DESIGN-PADO.md §۸` و `DESIGN-AUTOMATION.md §۴۲` ثبت شده.

### ب) ترتیب اجرای بوت‌استرپ (الزامی)

این ترتیب از روی نسخهٔ v5 فایل `030-default-theme.php` (دمو) استخراج شده و هر
جابه‌جایی در آن در محیط واقعی **fatal** می‌دهد (بخش «د» را ببینید). مراحل باید
به‌صورت **یک‌باره در بوت اول** اجرا شوند و با یک **نگهبان آپشن**
(مثل `igbz_store_provisioned = v<N>`) از اجرای مجددشان جلوگیری شود. با تغییر محتوای
هر مرحله نسخهٔ نگهبان را یکی بالا ببرید.

| گام | هوک | کار |
|---|---|---|
| ۱ | — | ساخت رکورد مستأجر در `tenants` + `tenant_domains` + `tenant_members` (مدیر اول) |
| ۲ | — | کپی/لینک پوشه‌های باندل‌شده: فقط `woocommerce/` و `igbz-suite/` — **بدون صفحه‌ساز ثالث** |
| ۳ | `plugins_loaded` اولویت ۰ | فعال‌سازی پلاگین‌ها به ترتیب: **ووکامرس ← igbz-suite** (وابستگی به این ترتیب است) |
| ۴ | **`setup_theme`** اولویت ۰ | `switch_theme('<قالب هسته پیش‌فرض>')` (مثلاً `twentytwentyfive`) — این هوک **باید** قبل از لود `functions.php` قالب اجرا شود |
| ۵ | `wp_loaded` اولویت ۵۰ | (حذف شد — گام ساخت کیت Elementor دیگر لازم نیست) |
| ۶ | همان | تنظیم پرمالینک `/%postname%/` + `flush_rewrite_rules(false)` |
| ۷ | همان | `blogname` و `blogdescription` (نام فروشگاه از درخواست ثبت‌نام) |
| ۸ | همان | تنظیمات ووکامرس: `default_country=IR`، `currency=IRR`، `currency_pos=right_space`، `price_thousand_sep=,`، `price_decimal_sep=/`، `price_num_decimals=0`، `weight_unit=kg`، `dimension_unit=cm`، `calc_taxes=no`، فعال‌بودن COD (`woocommerce_cod_settings`) |
| ۹ | همان | **غیرفعال‌کردن بنر «Coming Soon»** ووکامرس ۱۱: `woocommerce_coming_soon=no` و `woocommerce_store_pages_only=no` |
| ۱۰ | همان | ساخت صفحات ووکامرس به فارسی (یا تغییرنام صفحات پیش‌ساختهٔ انگلیسی): فروشگاه / سبد خرید / تسویه حساب / حساب کاربری من؛ ست‌کردن آپشن‌های `woocommerce_*_page_id` |
| ۱۱ | همان | `show_on_front=page` و `page_on_front=<ID صفحه فروشگاه>` |
| ۱۲ | همان | `timezone_string=Asia/Tehran`، `WPLANG=fa_IR` |
| ۱۳ | همان | ساخت کاربر مدیر فروشگاه با رمز تولیدشده از `random_int()`/ماژول کریپتو (**هرگز `rand`/`mt_rand`** — قاعدهٔ دائمی) |
| ۱۴ | آخر هوک | `update_option('igbz_store_provisioned', 'v<N>')` تا مراحل هرگز دوباره اجرا نشوند |

### ج) قواعد عمومی بوت‌استرپ

- **Idempotent** نوشته شود: اجرای مجدد نباید صفحه، محصول یا کاربر تکراری بسازد و
  نباید fatal بدهد. برای هر موجودیت اول `term_exists`/`post_exists`/`email_exists`
  /آپشن را چک کنید.
- **ترتیب فعال‌سازی پلاگین‌ها مهم است**: ووکامرس باید قبل از هر پلاگینی که به آن
  وابسته است (از جمله igbz-suite) فعال شود. الگو در `000-activate.php`.
- **همهٔ تنظیمات با آپشن/هوک وردپرس** اعمال شوند — نه از راه دستکاری مستقیم جدول
  آپشن‌ها در SQL.
- **محتوای بذر اولیه (sample data)** برای فروشگاه تولید اختیاری است و جزو مراحل
  بوت‌استرپ نیست (برعکس محیط دمو که در ۱۰۰ بذر ۲۵ محصول/۱۰۰ مشتری/۸ سفارش
  می‌کارد).
- **چیزهای انگلیسی که در تولید باید ترجمه شوند** (Add to cart و …) با بستهٔ
  زبان `fa_IR` ترجمه می‌شوند، نه با mu-plugin موقت شبیه `050-fa-demo.php` (فقط
  برای محیط دمو/آفلاین است چون `downloads.wordpress.org` در سندباکس بلاک است).

### د) دام‌های شناخته‌شده (هر یک یک بار باعث fatal شده — تکرار نشود)

| علامت | علت | راه‌حل درست |
|---|---|---|
| `Call to undefined function <theme>_display_header_footer()` یا Fatal هنگام `get_header()` | `switch_theme()` روی هوک دیرهنگام (مثلاً `wp_loaded`) صدا زده شده — یعنی بعد از شروع رندر قالب قبلی، قبل از لود کامل `functions.php` قالب جدید | انتقال `switch_theme` به هوک **`setup_theme` با اولویت ۰** — همان‌طور که در `030-default-theme.php` هست |
| فروشگاه به‌جای کاتالوگ صفحهٔ «Coming Soon» ووکامرس ۱۱ را نشان می‌دهد | در WC 11 `woocommerce_coming_soon` پیش‌فرض `yes` است و سایت را از دید عموم پنهان می‌کند | `update_option('woocommerce_coming_soon', 'no')` در مرحله ۹ |
| صفحات ووکامرس با عنوان انگلیسی («Shop», «Cart») ساخته می‌شوند | ویزارد/نصب خودکار WC صفحات را با نام انگلیسی می‌سازد | یا قبل از فعال‌سازی WC فیلتری روی آپشن صفحات ست کنید، یا بعد از نصب با `wp_update_post` عنوان و `post_name` را به فارسی تغییر دهید (مرحله ۱۰) |
| رندر قالب جدید بدون CSS/با شکستگی layout | هوک دیرهنگام `switch_theme` و عدم `flush_rewrite_rules` | رعایت گام ۴ و ۶ |
| `rand()`/`mt_rand()` برای رمز مدیر اولیه یا PIN | در کد قدیمی دیده شده — ممنوع است | همیشه `random_int()` یا متدهای `Crypto` در `src/Support/Crypto.php` |

### هـ) منابع و مرجع کارکرده

| منبع | نقش |
|---|---|
| `_devenv/.work/mu/000-activate.php` | الگوی فعال‌سازی به‌ترتیب پلاگین‌ها |
| `_devenv/.work/mu/030-default-theme.php` (نسخهٔ v6) | **الگوی کامل بوت‌استرپ** — switch_theme در setup_theme به قالب هسته + تنظیمات + صفحات + RTL |
| `_devenv/.work/mu/040-rtl-demo.php` | CSS راست‌چین تزریقی (فقط دمو) |
| `_devenv/.work/mu/050-fa-demo.php` | ترجمهٔ موقت رشته‌ها و تبدیل ارقام به فارسی (فقط دمو — در تولید بستهٔ زبان fa_IR) |
| `_devenv/.work/mu/100-seed-sample-shop.php` | بذر فروشگاه نمونهٔ آرایشی — **الگوی idempotent برای seeder اختیاری آینده**، نه برای هر فروشگاه تولیدی |
| `PROJECT-STATE.md §۱` بند «پیش‌نیازهای نصب فروشگاه» | ثبت تصمیم نبودن صفحه‌ساز پیش‌فرض (المنتور حذف شد) |
| `DESIGN-PADO.md §۸` | طراحی قالب پادو بدون وابستگی به صفحه‌ساز ثالث |

> ⚠️ **وضعیت کنونی کد (۱۴۰۶/۰۵/۳۱ — به‌روز):** روند بوت‌استرپ **فقط** در mu-plugin‌های
> محیط توسعه پیاده شده است و **هنوز** در `igbz-suite/src/` (ماژول `hub` یا
> `multitenant`) جایی که ثبت‌نام واقعی فروشگاه رخ می‌دهد، کدنویسی نشده. هنگام
> پیاده‌سازی جریان «افزودن فروشگاه» در کد اصلی، همین روند (v6، بدون صفحه‌ساز
> ثالث) باید در نقطهٔ مناسب بوت‌استرپ مستأجر اعمال شود. این به‌عنوان **کار باز** در
> اینجا ثبت می‌شود و در اسناد مرتبط (DESIGN-PADO و PROJECT-STATE) تکرار شده.

---
## ۸. دام‌های محیطی — وقت‌گیرترین اشتباه‌ها

| مشکل | راه‌حل |
|---|---|
| **سندباکس تاریخچهٔ گیت را پاک می‌کند** (سه بار رخ داد) | **قبل از هر ادعا `git fetch origin`** و مقایسه با `FETCH_HEAD`. اگر push رد شد: `git fetch` → `git rebase FETCH_HEAD` → تعارض: `git checkout --theirs` → `--continue` |
| `_devenv/.work/` بین جلسه‌ها پاک می‌شود | `bash _devenv/setup.sh` (~۲۰ ثانیه) |
| `php` در PATH نیست | فقط از راه `_devenv/test.sh` (php-wasm) |
| بالا آمدن سایت | ۳۰–۹۰ ثانیه؛ `_devenv/run.sh --port 9400` |
| `curl` به سایت | `-c /tmp/cj.txt -b /tmp/cj.txt -L` وگرنه حلقهٔ ۳۰۲ |
| **`curl` سبز ≠ درست** | پنج بند قبلاً در `curl` سبز بودند و در مرورگر غلط |
| شمارش جدول‌ها | `grep -c '$sql\[\] = "CREATE TABLE'` ⇒ باید **۷۲** بماند |
| هر تغییر ویرا | نسخه در `package.json` **و** `package-lock.json` (خطوط ۳ و ۹) |
| **`npm ci` خودکار در راه‌انداز** | ضدالگو — دوباره اضافه نکن |
| **شبکهٔ سندباکس: فقط `registry.npmjs.org` باز است** | `cdn.playwright.dev` · `playwright.azureedge.net` · `registry.npmmirror.com` · `deb.debian.org` · `storage.googleapis.com` · `wordpress.org` همگی بلاک — هیچ‌وقت منتظرشان نمان |
| **`npx playwright install` کار نمی‌کند** | مرورگر Playwright دانلود نمی‌شود؛ باینری از `@sparticuz/chromium` (داخل npm) می‌آید: `source ./env.sh` در `/home/user/browser-test` |
| `require.resolve('@sparticuz/chromium/package.json')` خطا می‌دهد | `exports` پکیج قفل است — مسیر را دستی بساز: `path.join(__dirname,'node_modules','@sparticuz','chromium')` |
| Chromium با `libnspr4.so` بالا نمی‌آید | `LD_LIBRARY_PATH=/tmp/al2023/lib` — `env.sh` ست می‌کند؛ حذفش نکن |
| `/tmp` بین جلسات پاک می‌شود | فقط دوباره `source ./env.sh` (bootstrap idempotent است) |
| `node_modules` در snapshot ماندگار نیست | `PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1 npm ci` با `package-lock.json` موجود (~۵ ثانیه) |
| **بینایی تصویر در برخی جلسات خاموش است** | اسکرین‌شات بگیر و به کارفرما نشان بده؛ تحلیل «بصری» را با DOM/CSS انجام بده (`getBoundingClientRect` · استایل محاسبه‌شده · overflow موبایل · خطای console) |
| مرورگر روی `wp-admin` اولین بار کند است | طبیعی است (بوت PHP-Wasm) — timeout کانفیگ 120s است؛ `networkidle` صبر کند |
| **پیش‌نمایش e2b.app «Sandbox Not Found» می‌دهد** | عمر لینک = عمر سندباکس جلسه؛ دورزدنی نیست. سرور را دوباره با start_process بالا بیاور (پروسهٔ قبلی را اول kill کن) یا از لینک Playground (§۷.۶-ب) استفاده کن که دائمی است |
| **لینک پیش‌نمایش از دامنهٔ e2b همه‌چیز به 127.0.0.1 می‌خورد** | mu-plugin `005-dynamic-host.php` باید در `.work/mu/` باشد (§۷.۶-الف)؛ با پاک‌شدن `.work` از بین می‌رود — بازسازی کن |
| **`e2b.app` با http تست نمی‌شود** | دامنه در HSTS preload مرورگرهاست — فقط https؛ برای تست از `tls-proxy.cjs` + `preview-check.cjs` در browser-test استفاده کن |
| **پوش به `.github/workflows/` رد می‌شود** | توکن App سندباکس مجوز `workflows` ندارد. **پروکسی سندباکس هدر Authorization را با توکن خودش جایگزین می‌کند — حتی PAT کارفرما هم بی‌اثر است (با توکن قلابی اثبات شد).** هرگز PAT نگیر؛ فایل را در `_devenv/` بگذار و از کارفرما بخواه خودش از UI گیت‌هاب در `.github/workflows/` بسازد (§۷.۶-پ) |
| **Blueprint پلی‌گراند: `Git ref not found`** | برای نام شاخه `refType` باید `branch` باشد، نه `refname` (§۷.۶-ب) |
| **Blueprint پلی‌گراند: `Out of memory` در رندر فروشگاه** | حافظهٔ پیش‌فرض Playground کم است — `WP_MEMORY_LIMIT=256M` + mu-plugin با `ini_set('memory_limit','512M')` (§۷.۶-ب) |
| **پکیج `@sparticuz/chromium@149` کاملاً ESM شده** | `build/cjs/` دیگر نیست؛ از cjs با `import(pathToFileURL(...))` دینامیک لود کن. `al2023.tar.br` (libها مثل libnspr4) فقط در آمازون‌لینوکس خودکار باز می‌شود — دستی `mod.inflate(...al2023.tar.br)` صدا بزن (در `bootstrap.cjs` جدید انجام شده) |

**قواعد فنی افزونه:** SQL همیشه MySQL · هرگز `__()` قبل از `init` · لجر به‌جای موجودی
ذخیره‌شده · idempotency مالی · تست‌ها با `igbz_test_reset_settings()` · الگوی درگاه‌ها:
`$response = $this->post_json(...); $ok = (bool)$response['ok'];` — **هرگز destructuring**.

---


## ۹. نقشهٔ فایل‌ها

```
PROJECT-STATE.md          ← وضعیت جاری و نتیجهٔ فازها
PROJECT-COMPLETION-PLAN.md ← برنامهٔ مادر؛ §۲۳ شامل ۷۶ فاز پایه + فازهای PV
REQUIREMENTS-TRACEABILITY.md ← موجودی فاز ۰۱؛ نیاز ← سند ← کد ← آزمون/شکاف
PHASE-01-INVENTORY.json   ← artefact ماشینی؛ بازسازی با `_devenv/phase01_inventory.py`
DESIGN-GAPS-FIX.md        ← چهار شکاف + §۷ گزارش اجرا
DESIGN-LEGAL-AUTH.md      ← §۷.۵ OTP · §۷.۶ بیومتریک · §۷.۷ مسیرهای هسته
DESIGN-PADO.md        ← §۸ طراحی قالب بدون صفحه‌ساز ثالث · §۱۷ انتخاب مدل با ما · ⚠️ بدهی: فرض غلط «ما می‌سازیم»
DESIGN-DEPLOY-VIRA.md   ← §۸ دفتر رویداد · §۱۴ امنیت · §۱۴.۷ باز
DESIGN-APPS-ROADMAP.md    ← ⏸️ معلق تا تکمیل بک‌اند
DESIGN-AUTOMATION.md      ← سند معماری اتوماسیون (v1.2، ۱۴۰۶/۰۵/۳۱ — المنتور از پیش‌نیازها حذف شد)
امنیت و مراقبت/           ← README + منابع/{OWASP,WordPress,IETF,AI-Commerce}

igbz-suite/src/Support/CoreSurfaceGuard.php                      ← تازه
igbz-suite/src/Modules/MultiTenant/Payments/LegalWaiverService.php ← تازه
igbz-suite/tests/SecurityGapsTest.php                            ← تازه
igbz-suite/src/Modules/MultiTenant/Otp/OtpService.php            ← کول‌داون AND

_devenv/                   ← ابزارهای محیط توسعه؛ BUNDLED_PLUGINS/THEMES خالی است (المنتور حذف شد)
_devenv/playground-blueprint.json      ← Blueprint تست آنلاین Playground (§۷.۶-ب) — تنها نقطهٔ اصلاح
_devenv/github-workflow-pr-preview.yml ← نسخهٔ مرجع ورک‌فلو دکمهٔ PR (§۷.۶-پ)
_devenv/PLAYGROUND-LINKS.md            ← سند لینک‌های دائمی تست آنلاین + دستورالعمل
_devenv/LINK-FOR-EMPLOYER.txt          ← لینک آمادهٔ کپی برای کارفرما
.github/workflows/pr-playground-preview.yml ← ورک‌فلو فعال دکمهٔ PR (ساختهٔ دست کارفرما؛ از سندباکس قابل پوش نیست)
_devenv/.work/mu/          ← mu-plugin‌های harness (تولیدشده توسط setup.sh، git-ignored):
    000-activate.php       · فعال‌سازی اجباری WC+igbz-suite به‌ترتیب (المنتور دیگر نیست)
    005-dynamic-host.php   · هاست پویا برای پیش‌نمایش e2b (§۷.۶-الف) — دستی ساخته شد؛ با پاک‌شدن .work بازسازی شود
    010-enable-modules.php · روشن‌کردن هر ۶ ماژول
    020-healthcheck.php    · اندپوینت /?igbz_health=1
    030-default-theme.php  · بوت‌استرپ فروشگاه نمونه (نسخه v6) — الگوی §۷.۵ (قالب هسته)
    040-rtl-demo.php       · CSS راست‌چین (فقط دمو)
    050-fa-demo.php        · ترجمه موقت رشته‌ها + «تومان» + ارقام فارسی (فقط دمو)
    100-seed-sample-shop.php · بذر ۲۵ محصول آرایشی + ۱۰۰ مشتری + ۸ سفارش (دمو)

jules-tasks/               ← صف تسک‌های جولز (۱۴۰۶/۰۶/۰۳):
    README.md              · قالب و قواعد تسک‌ها + چرخهٔ dispatcher
    TASK-001-modules-test.md · اولین تسک (ارسال‌شده؛ خروجی: PR #9 — بسته‌شده به‌اشتباه، کار باز)
    .dispatched/           · مارکرهای ضد-تکرار (شمارهٔ session جولز)
.github/workflows/         ← هر سه را کارفرما دستی ساخته (از سندباکس قابل پوش نیست):
    pr-playground-preview.yml · دکمهٔ پیش‌نمایش Playground روی هر PR
    jules-dispatcher.yml     · ارسال خودکار تسک‌ها به جولز
    railway-volume.yml       · نگهبان ولوم ریل‌وی (اگر هنوز نساخته، از نسخهٔ مرجع بسازد)
railway/                   ← استقرار پیش‌نمایش دائمی روی Railway (۱۴۰۶/۰۶/۰۴):
    Dockerfile · entrypoint.sh · mu-plugins/ · README.md (راهنمای فارسی پنل)
railway.json               ← تنظیم بیلد ریل‌وی (builder=DOCKERFILE)

skills/                    ← اسکیل‌های پادو (مکان قطعی ۱۴۰۶/۰۵/۳۱):
                            · skills/igbz-*/           ← اسکیل‌های خودمان (کامیت می‌شوند)
                            · skills/_thirdparty/      ← اسکیل‌های شخص ثالث (git-ignored)
                              (ui-ux-pro-max-skill کامیت bc826e2 اینجا نصب می‌شود)
                            · skills/README.md         ← قوانین پوشه

/home/user/browser-test/   ← خارج از ریپو — محیط تست مرورگری (⚠️ بین جلسات پاک می‌شود؛ طبق §۷-ب بازسازی کن):
    env.sh · bootstrap.cjs (سازگار با chromium@149 ESM + inflate دستی al2023) · playwright.config.ts · verify-env.cjs
    preview-check.cjs · tls-proxy.cjs (راستی‌آزمایی پیش‌نمایش e2b — §۷.۶-الف)
    package.json + package-lock.json (نسخه‌های پین‌شده)
```

---

## ۱۰. جملهٔ شروع پیشنهادی برای چت تازه

> ⚠️ **قوانین جدید (۱۴۰۶/۰۵/۳۱):** (۱) به‌روزرسانی اسناد و ثبت تصمیم‌های کارفرما
> دیگر نیازی به پلن/تأیید/«شروع کن» ندارد — خودکار در سند مربوطه اعمال می‌شود. فقط
> برای تغییر **کد** همچنان باید پلن ← تأیید ← «شروع کن» رعایت شود (قاعده ۹ §۰).
> (۲) انتخاب مدل/سرویس پادو با ماست؛ ادمین فقط کلید API مدل منتخب را در پنل ثبت
> می‌کند (ثبت در `DESIGN-PADO.md §۱۷`).
> (۳) 🔴 **قاعدهٔ سختگیرانه ۱۰: تحقیق اجباری پیش از هر کار.** قبل از شروع هر کاری
> (طراحی، انتخاب فناوری، اسکیل/پرامپت، رفع باگ، حتی به‌روزرسانی اسناد فنی) حتماً
> با `web_search` (depth=3 برای کارهای مهم) مقالات به‌روز، مستندات رسمی، پروژه‌های
> مشابه (موفق/ناموفق)، بازخورد کاربران واقعی و نقاط ضعف/قوت آن‌ها را بررسی کن،
> بعد با جمع‌بندی تحقیق + دانش درونی پلن بنویس. هرگز فقط به دانش درونی اکتفا نکن.
> (۴) 🔴 **قاعدهٔ سختگیرانه ۱۱: سؤال تک‌به‌تک با گزینه کلیکی + باکس نظر آزاد.** هرگز
> چند سؤال را در یک پیام نپرس. سؤال‌ها یکی‌یکی و با ۲–۴ گزینه انتخابی + همیشه
> امکان پاسخ آزاد پرسیده شوند. تا جواب سؤال فعلی نگرفتی، سراغ سؤال بعدی نرو.
>
> «`HANDOFF-PROMPT.md` را بخوان. آخرین کار این جلسه (۱۴۰۶/۰۶/۰۱ ≈ ۲۳ اوت ۲۰۲۶):
> تغییرنام رسمی سراسری «هوشا → ویرا» و «هوشواره → پادو» روی **تمام ۱۰۲ فایل** پروژه
> اعمال شد (پوشه‌ها `hoosha/` → `vira/` و `Modules/Hushvare/` → `Modules/Pado/`،
> فایل‌های `HushvareModule.php` → `PadoModule.php`، سندها `DESIGN-HUSHVARE.md`
> → `DESIGN-PADO.md` و `DESIGN-DEPLOY-HOOSHA.md` → `DESIGN-DEPLOY-VIRA.md` و
> پرامپت‌ها `PROMPT-*-HUSHVARE.md` → `PROMPT-*-PADO.md`، کپبیلیتی
> `igbz_manage_pado`، ثابت `Modules::PADO`). بنر معنی دهخدا برای هر دو نام در بالای
> اسناد اصلی درج گردید.
> **سیستم حافظه سه‌لایه پادو طراحی کامل و در `DESIGN-PADO.md §۵` ثبت شد:** دانش
> (۷ بخش، منابع ثابت + انباشت تحقیق معتبر) · تجربه (چهار منبع: ادمین، مشتری، خود
> پادو، **رقبا** — برای حذف آزمون‌وخطای مجدد) · خاطره (بلندمدت از افتتاح تا
> تعطیلی فروشگاه، با لایه امنیتی فوق‌العاده برای داده‌های حساس). مکمل‌های ضروری
> (حافظه کاری کوتاه‌مدت، لایه خودبازبینی و نگهداشت دوره‌ای) نیز اضافه شدند.
>
> **ماژول پادو (S0) پیاده‌سازی شد** (`igbz-suite/src/Modules/Pado/`):
> صفحهٔ «مرکز پادو» با چهار تب (طراحی قالب / درخواست‌های مجوز / تاریخچه /
> تنظیمات) در پیشخوان IGBZ، `ApprovalRequestService` (صف واحد درخواست‌های مجوز
> برای **همهٔ** عملیات حساس از جمله چهار نوع پست اینستاگرام، قیمت، Refund،
> حذف انبوه، ارسال کمپین، تغییر سیاست، اعمال قالب)، `ThemeValidator`
> (دروازه اعتبارسنجی بک‌اند برای zip قالب)، `ThemeService` (ثبت artefact
> قالب). دو جدول جدید `igbz_approval_requests` و `igbz_themes` در نسخهٔ DB v19
> افزوده شد. پاسخ‌های هفت‌گانه پلن پادو در `DESIGN-PADO.md §۱۸` ثبت شد
> (نوع خروجی: هر سه به انتخاب ادمین · تحویل: مستقیم + zip · تأییدها: دسته‌ای ·
> فونت: وزیرمتن · مرکز پادو در S0 · اینستاگرام: درخواست مجوز روزانه برای هر
> چهار نوع پست؛ provider تاریخی آن بخش با Zernio در ADR-0004 جانشین شد · مکان اسکیل‌ها: `skills/`).
> اسکیل‌ها به `skills/` منتقل شدند (اسکیل‌های خودمان `igbz-*` کامیت می‌شوند،
> اسکیل‌های شخص ثالث در `skills/_thirdparty/` git-ignored). اسکلت اولیهٔ
> `skills/igbz-store-theme/SKILL.md` ایجاد شد. منبع `21st.dev` به‌عنوان مرجع الهام
> بصری و **عدم استفاده از MCP داخلی ووکامرس** به‌دلیل هشدار امنیتی در §۵ ثبت گردید.
> المنتور (~۱۰۸ مگابایت) کلاً از نصب پیش‌فرض حذف شد و قالب پیش‌فرض هسته وردپرس
> (Twenty Twenty-Five) جایگزین گردید.
> محیط تست مرورگری طبق §۷ آماده است؛ فروشگاه نمونه (۲۵ محصول آرایشی + ۱۰۰ مشتری
> + ۸ سفارش) با `bash _devenv/setup.sh && bash _devenv/run.sh --port 9400` بالا
> می‌آید (روی قالب پیش‌فرض هسته وردپرس، ماژول pado فعال، ۷۲ جدول، مرکز
> پادو در منوی IGBZ). رمز مشتریان نمونه: `Customer123!`.
> نکته: پیش‌نمایش زنده تونل E2B در این سشن کار نکرد؛ برای نشان‌دادن صفحه به
> کارفرما می‌توان HTML را با curl گرفت و با `present_file` باز کرد.
>
> **جلسهٔ ۱۴۰۶/۰۶/۰۲ — پیش‌نمایش زنده (جزئیات کامل در §۷.۶):** پیش‌نمایش
> سندباکس e2b.app با mu-plugin `005-dynamic-host.php` درست شد ولی عمرش به عمر
> سندباکس گره خورده («Sandbox Not Found» پس از خواب سندباکس — دورزدنی نیست).
> راه‌حل ماندگار برقرار شد: تست آنلاین از گیت‌هاب با WordPress Playground رسمی —
> (۱) لینک `?blueprint-url=` به `_devenv/playground-blueprint.json` و (۲) دکمهٔ
> خودکار «Preview in WordPress Playground» روی هر PR (ورک‌فلو
> `pr-playground-preview.yml`، اولین اجرا روی PR #8 سبز). دو خطای رفع‌شده:
> `refType` باید `branch` باشد (نه `refname`) و حافظهٔ Playground باید با
> `WP_MEMORY_LIMIT` + `ini_set` بالا برود (Fatal OOM در jetpack-autoloader).
> 🔴 مهم: پروکسی سندباکس Authorization گیت‌هاب را با توکن App خودش جایگزین
> می‌کند — PAT کارفرما بی‌اثر است و پوش به `.github/workflows/` ناممکن؛ فایل
> ورک‌فلو را کارفرما خودش از UI ساخت. **PR #8 باز است** (شامل blueprint +
> ورک‌فلو + اسناد) و باید ادغام شود تا لینک `main` کار کند.
>
> **جلسهٔ ۱۴۰۶/۰۶/۰۲ (ادامه) — تست ویژوال + قانون ۱۲:** پوشهٔ `visual-testing/`
> با ۳۵ اسکرین‌شات از همهٔ ۲۹ منوی ادمین + ۵ صفحهٔ فروشگاه؛ ۷ مشکل واقعی رفع شد
> (جزئیات: `visual-testing/REPORT.md` و §۴). قانون سختگیرانهٔ ۱۲ (تست ویژوال
> اجباری برای هر تغییر) در §۰ ثبت شد؛ ابزار: `browser-test/visual-crawl.cjs`.
>
> **جلسهٔ ۱۴۰۶/۰۶/۰۳–۰۴ — جولز + ریل‌وی (جزئیات کامل در §۴ بخش همین جلسه):**
> خط لولهٔ تمام‌خودکار جولز برقرار و آزموده شد (`jules-tasks/` + ورک‌فلوی
> dispatcher + سیکرت `JULES_API_KEY`). تسک آزمایشی PR #9 را ساخت؛ دیف آن درست
> بود ولی کارفرما به‌اشتباه بست ⇒ **کار باز: Reopen و ادغام PR #9 (یا اعمال
> مستقیم همان دو خط)**. جولز به‌دلیل نبود امکان پرداخت ارزی کارفرما، انتخاب
> قطعی است. فایل‌های استقرار ریل‌وی ساخته شد (`railway/` + `railway.json` +
> نگهبان ولوم) ولی **استقرار هنوز خطا می‌دهد چون ریل‌وی شاخهٔ main را می‌سازد
> که این فایل‌ها را ندارد** — کارفرما گفت PR #8 را پس از پایان آن جلسه خودش
> ادغام می‌کند.
>
> **🔴 نقطهٔ شروع این چت — به این ترتیب:**
> ۱. `git fetch origin` و بررسی اینکه PR #8 ادغام شده یا نه (`gh pr view 8`).
>    اگر ادغام شده: شاخهٔ جدید جلسه را از main بگیر و ادامه بده. اگر نه: از
>    کارفرما بخواه ادغام کند (یک کلیک) — بدون آن ریل‌وی درست نمی‌شود.
> ۲. استقرار ریل‌وی را با کارفرما تمام کن: پس از ادغام، Deploy تازه؛ نشانهٔ
>    بیلد درست = خط `FROM wordpress:php8.2-apache` در لاگ (نه Railpack).
>    سپس سلامت (`/?igbz_health=1`)، پیشخوان و فروشگاه + **تست ویژوال (قانون ۱۲)**.
> ۳. تعیین تکلیف PR #9 جولز (Reopen+ادغام یا اعمال دستی دو خط تست).
> ۴. سپس برگرد به رشتهٔ اصلی: بازبینی پادو — منتظر اعلام بخش بعدی از سوی
>    کارفرما (اسکیل igbz-store-theme / قواعد ایران / ThemeValidator /
>    ApprovalQueue / مراحل ۲-۹ پادو؛ gateway ویرا با ADR-0004 منسوخ شده است).
>
> **کارهای باقی‌ماندهٔ تاریخی S1 — با ADR-0004 نیازمند بازطراحی:** ساخت adapter مستقل
> DeepInfra و قرارداد connector (در S0 درخواست‌ها فقط در صف ثبت می‌شوند) · افزودن اسکیل کامل
> `igbz-store-theme` با هفت references (01-persian-rtl الی 07-output-contract) ·
> دروازه zip (آپلود/دریافت از Gateway + اکسترکت + اعتبارسنجی ThemeValidator +
> نصب preview + بازگشت یک‌کلیک) · پشتیبانی از سه نوع خروجی (FSE/کلاسیک/تمپلیت
> صفحه‌ساز) در اسکیل · اجراکننده‌های واقعی به ازای هر نوع درخواست
> (قیمت، Refund، چهار نوع پست اینستاگرام از طریق Zernio، حذف انبوه،
> ارسال کمپین، تغییر سیاست) که `ApprovalRequestService::decide()` صدا می‌زند ·
> دیباگ منوهای IGBZ روی Playground آنلاین · به‌روزکردن نگهبان ولوم برای پوشش
> ولوم دیتابیس (`/var/lib/mysql`).
>


---

## ثبت وضعیت این جلسه — ۱۴۰۵/۰۶/۰۵ (۲۷ اوت ۲۰۲۶)

این بخش برای چت بعدی افزوده شد و هیچ‌یک از مطالب قبلی حذف نشده است.

### تغییرات واقعاً انجام‌شده

- استقرار ریل‌وی تثبیت شد: رفع چند MPM آپاچی، اجرای درست راه‌انداز رسمی وردپرس، رفع رقابت زمانی راه‌انداز و پشتیبانی از گواهی خودامضای مار‌یاد‌بی در بررسی ابزار خط فرمان.
- بوت‌استرپ نمونهٔ ریل‌وی قابل‌تکرار شد: نصب وردپرس، ووکامرس و `igbz-suite`، ثبت لاگ مرحله‌ای و بذرگذاری idempotent محصولات.
- هشت تصویر محلی برای محصولات آزمایشی اضافه و ۲۵ محصول موجود به تصاویر دسته‌ای متصل شدند.
- مستأجر نمونه، اتصال محصولات به مستأجر، فیلتر کاتالوگ/سفارش/پیشخوان/REST و داشبورد مالک فروشگاه اضافه شد.
- ساخت مستأجر از مسیر ثبت‌نام و پنل، عضویت مالک و ایجاد زیر‌دامنهٔ اولیه به هم متصل شدند.
- نگاشت دامنهٔ مسیریابی (`tenant_domains`) با دفتر دامنهٔ ارائه‌دهنده (`ig_domains`) همگام شد و تأیید DNS واقعی شد.
- قرارداد حقوقی، پذیرش قرارداد، گیت پیش از پرداخت، بررسی کلید شاهکار و کنترل OTP شماره+IP پیاده شدند.
- صادرات وردپرس برای غیرمدیر ارشد بسته شد.
- مرکز پادو به سرویس بیرونی متصل شد؛ دریافت ZIP، اعتبارسنجی، ثبت، پیش‌نمایش، اعمال زنده و بازگشت قالب اضافه شد. اعمال و بازگشت از صف مجوز عبور می‌کنند.
- صف مجوز پادو برای مستأجرها محدوده‌بندی شد.
- رابط REST مدیریت محصولات، شمارش و صفحه‌بندی دقیق محصولات/سفارش‌ها، حذف محصول و بارگذاری تصویر اضافه شد.
- تست‌های محلی به‌روز شدند.

### نتیجهٔ آخرین تست

```text
۱۲۵۱ اظهارنظر موفق در ۲۴ کیس
۲۴۵ فایل PHP بدون خطای نحوی
```

### کارهای باقی‌ماندهٔ واقعی

۱. اتصال صف مجوز به تمام عملیات حساس: تغییر قیمت، Refund، حذف انبوه، کمپین و چهار نوع انتشار اینستاگرام؛ فعلاً زیرساخت صف و مسیر قالب فعال است.
۲. تکمیل چرخهٔ دامنهٔ پولی: پرداخت سفارش دامنه، ثبت نزد ثبت‌کننده، تمدید و callback.
۳. اجرای واقعی و آزمون با کلید Manus و ChatPlace برای تمام ۱۳ گام ثبت محصول و انتشار اینستاگرام؛ تست‌های موجود عمدتاً شبیه‌ساز دارند.
۴. تکمیل ترجمهٔ فارسی همهٔ رشته‌ها به‌جز پیام‌های خطا؛ فایل POT موجود است اما ترجمهٔ نهایی کامل نیست.
۵. بازبینی مقیاس‌پذیری تمام کوئری‌ها، cache، cron و workerها طبق قواعد بخش ۵.۵.
۶. آزمون واقعی شاهکار با کلید مدیر ارشد و ثبت نتیجهٔ مثبت/منفی سرویس بیرونی.
۷. تکمیل حافظه، Playbook، KPI و قراردادهای عملیاتی پادو طبق `DESIGN-PADO.md`؛ ویرا خارج از محدوده است و نباید تغییر کند.
۸. بازسازی محیط Chromium و اجرای قانون ۱۲ برای همهٔ صفحات متأثر، همراه اسکرین‌شات تمام‌صفحه و بازبینی چشمی.
۹. اصلاح همهٔ شمارش‌ها و وضعیت‌های قدیمی اسناد پس از تطبیق نهایی؛ هیچ کلاس، جدول یا آداپتور به‌تنهایی معادل قابلیت کامل نیست.

### نقطهٔ شروع چت بعدی

- `git fetch origin`، وضعیت شاخه و PR شمارهٔ ۱۰ بررسی شود.
- همین فهرست با همهٔ سندهای `DESIGN-*` و کد تطبیق داده شود.
- هر مرحلهٔ بزرگ کامل، تست‌شده و کامیت‌شده انجام شود؛ اعلان گیت‌هاب ارسال نشود مگر با دستور تازهٔ کارفرما.
- ویرا (`vira/`) مطلقاً بررسی یا تغییر داده نشود مگر با دستور صریح.
- شاخهٔ این جلسه: `arena/01a0435a-igbz-wp` (نام شاخه را همیشه از `git status` بگیر).
> تا نگفتم هیچ کاری و هیچ گزارشی نده.»

---

## ثبت ممیزی و برنامهٔ تکمیل — ۱۴۰۵/۰۶/۰۵ (۲۷ اوت ۲۰۲۶)

این بخش جدیدترین نقطهٔ شروع است و در تعارض با وضعیت‌های تاریخی PR شمارهٔ ۸/۹/۱۰ یا
شمارش‌های قبلی، بر آن‌ها مقدم است.

### وضعیت Git و ادغام

- شاخهٔ ثابت این جلسه: `arena/01a0435a-igbz-wp`.
- PR شمارهٔ ۱۰ با موفقیت در `main` ادغام شد؛ merge commit برابر `b160311` است.
- هنگام شروع ممیزی، `HEAD`، `origin/main` و `main` همگی روی همین commit بودند.
- بررسی PR شمارهٔ ۱۰، Preview و وضعیت Railway آن موفق بود. رکورد deployment تازهٔ Railway
  هنگام بررسی GitHub هنوز `in_progress` بود؛ بنابراین موفقیت نهایی خود deployment باید
  در شروع کار انتشار دوباره خوانده و سپس URL عمومی با مرورگر آزموده شود.
- PR شمارهٔ ۸ قبلاً ادغام و PR شمارهٔ ۹ بسته شده است؛ اصلاح تست موردنظر PR شمارهٔ ۹ در
  تغییرات نهایی PR شمارهٔ ۱۰ حاضر است، چون تست فعلی ۱۲۵۱/۱۲۵۱ سبز است. هیچ اقدام جداگانه‌ای
  برای reopen شمارهٔ ۹ لازم نیست.

### نتیجهٔ اجرای واقعی

```text
WordPress 7.0.4
PHP 8.2.32
WooCommerce 11.0.1
IGBZ فعال؛ ۶ ماژول؛ DB v19؛ ۷۲/۷۲ جدول
۱۲۵۱/۱۲۵۱ اظهارنظر موفق در ۲۴ کیس
۲۴۵ فایل PHP بدون خطای نحوی
POT: ۲۳۵۱ رشته، ۹ جمع، ۲۹۱۱ ارجاع از ۲۱۵ فایل
ویژوال: ۳۵ حالت، ۳۴ موفق، ۱ شکست ماشینی checkout country=US (انتظار IR)
بازبینی چشمی: یک نقص مستقل glyph/آیکن مربعی در پادو و واحد کیف پول
```

محیط مرورگر در `/home/user/browser-test` با Playwright 1.62.1 و Chromium
149.0.7827.0 بازسازی شد. خزش همهٔ ۲۹ صفحهٔ مدیریت و شش حالت فروشگاه انجام و تصاویر
تمام‌صفحه چشمی بازبینی شدند. سبد خرید پس از hydration کامل سالم بود. تنها شکست صریح،
کشور/ایالت تسویهٔ محیط نمونه (`US/CA`) است. گزارش منبع حقیقت:
`visual-testing/REPORT.md` و `visual-testing/_raw-report.json`.

### یافته‌های تازهٔ مسدودکننده

۱. **رازها:** از ۳۶ فیلد password پنل فقط ۱۴ مورد در رجیستری ۱۷تایی `Settings::SECRETS`
   مشترک‌اند و ۲۲ مورد عضو آن نیستند؛ plaintext ذخیره و
   مقدار واقعی‌شان در HTML فرم بازنمایش می‌شود. کلیدهای شاهکار، دامنه، FX و چند درگاه
   جزو آن‌ها هستند. ورود کلید واقعی تا اصلاح/مهاجرت ممنوع. فهرست: `API-KEYS.md §۵`.
۲. **قالب پادو و مستأجر:** `switch_theme()` سراسری است و قالب یک مستأجر را جدا نمی‌کند؛
   rollback نیز update بدون شرط tenant دارد. query پیش‌نمایش مصرف‌کننده‌ای ندارد.
۳. **دانلود/اجرای پادو:** URL خروجی بیرونی با محدودیت اندازه/stream و allowlist کامل
   دریافت نمی‌شود و Bearer ممکن است به میزبان ZIP ارسال شود؛ قالب FSE تولیدی نیز هنوز
   اجازهٔ PHP دارد و blacklist جای sandbox را نمی‌گیرد.
۴. **صف مجوز:** تصمیم و اجرا claim اتمیک ندارد و در race ممکن است عملیات دو بار اجرا شود.
   عملیات قیمت، Refund، چهار نوع انتشار، حذف انبوه و کمپین فقط label هستند و سیم‌کشی واقعی
   ندارند.
۵. **دامنه:** ثبت ارائه‌دهنده پیش از پرداخت انجام می‌شود؛ تمدید و callback وجود ندارد؛
   `verify_dns(id)` مالکیت شیء به مستأجر جاری را در query اعمال نمی‌کند.
۶. **مقیاس:** Action Scheduler اصلاً استفاده نشده؛ cronهای تجمیعی، batchهای سراسری بدون
   requeue/عدالت، DELETEهای نامحدود و چند فهرست بدون صفحه‌بندی باقی‌اند. HPOS فقط اعلام
   سازگاری شده و در نمونهٔ روشن آزموده نشده است.
۷. **پادو S1 و ترجمه:** اسکیل قالب هنوز placeholder ۳۴خطی است؛ حافظه/Playbook/KPI کد
   ندارد؛ `fa_IR.po/mo` کامل نیست. POT در این ممیزی بازسازی شد.

شرح فنی، جدول تطبیق ۹ محور و برنامهٔ کامل بسته‌های صفر تا شش در
`PROJECT-STATE.md §۱۰` ثبت شده است. جزئیات پادو در `DESIGN-PADO.md §۱۹` و دامنه در
انتهای `DESIGN-DOMAIN.md` ثبت شد.

### نقطهٔ شروع بعدی

۱. `git fetch origin`، `git status --short --branch` و وضعیت deployment/PR را تازه کن.
۲. اگر کارفرما برنامه را پذیرفت، منتظر عبارت صریح **«شروع کن»** بمان؛ تأیید مبهم یا سکوت
   مجوز تغییر کد نیست.
۳. نخستین بستهٔ پیشنهادی: **بستهٔ صفر، بخش رجیستری رازها و مهاجرت امن ۲۲ فیلد**. پیش از
   اجرا، تحقیق وب همان بسته را تازه و برنامهٔ فایل/تست دقیق ارائه کن.
۴. پس از هر تغییر: تست هدفمند متناسب، مرورگر واقعی، اسکرین‌شات تمام‌صفحه، بازبینی چشمی،
   ثبت سند و commit روی همین شاخه.
۵. هیچ اعلان GitHub ارسال نکن مگر دستور تازهٔ کارفرما.
۶. `vira/` مطلقاً خارج محدوده است و نباید بررسی یا تغییر داده شود.

تا این لحظه فقط ممیزی، تست، بازسازی artefactهای سندی/ویژوال و برنامه انجام شده است؛ هیچ
تغییر کد افزونه بدون مجوز کارفرما انجام نشده است.

### برنامهٔ جامع تازه — ۱۴۰۵/۰۶/۰۵

کارفرما برنامهٔ تکمیل کامل و تطبیق کد با همهٔ اسناد را درخواست و اجرای فاز ۰۱ آن را با
عبارت صریح «حالا شروع کن» مجاز کرد. برنامهٔ تفصیلی در `PROJECT-COMPLETION-PLAN.md` ثبت
شده و در جزئیات اجرا بر خلاصهٔ `PROJECT-STATE.md §۱۰.۵` مقدم است. این مجوز فقط فاز ۰۱ را
پوشش داد و به هیچ فاز فنی بعدی تسری ندارد. `vira/` خارج محدوده و اپ‌ها تا دریافت مخزن
کامل معلق‌اند.

### فازبندی تک‌نوبتی برنامهٔ جامع

کارفرما خواست برنامه بر اساس ظرفیت واقعی به فازهایی تقسیم شود که هر فاز بدون توقف
برنامه‌ریزی‌شده و بدون گزارش میانی تمام شود. `PROJECT-COMPLETION-PLAN.md §۲۳` اکنون ۷۶
فاز پایه دارد؛ هر provider فعال نیز یک فاز مستقل اضافه می‌کند و تعداد نهایی پس از تصمیم
فاز ۰۲ معلوم می‌شود. هر فاز فقط با پیش‌نیاز، معیار پذیرش و ورودی آماده شروع می‌شود و پس
از به‌روزرسانی اسناد، گزارش دو یا سه خطی می‌گیرد. فاز `۰۱` (موجودی و ماتریس ردیابی)
تمام شده و نخستین فاز کدنویسی امنیتی `۰۵` است؛ برای فازهای بعدی هنوز مجوز اجرایی صادر
نشده است.

### نتیجهٔ قطعی فاز ۰۱ — ۱۴۰۵/۰۶/۰۵

- ۲۱۵ فایل PHP منبع، ۱۴۳ فراخوانی ثبت REST در ۱۵ فایل، DB v19/۷۲ جدول، ۳۰۴ تنظیم، ۳۶
  فیلد password، ۱۷ عضو رجیستری secret، ۱۴ اشتراک و ۲۲ مورد جاافتاده ثبت شد.
- سه نقش، ۱۱ capability، ده shortcode، سه hook مرکزی cron و ۲۸ فایل تست/۲۴ کلاس اصلی
  ثبت شد؛ ۱۲ سند `ِDoc/` و گزارش nopCommerce فقط منبع تاریخی/پیشنهادی طبقه‌بندی شدند.
- `REQUIREMENTS-TRACEABILITY.md` شامل ۶۷ خانوادهٔ نیاز، ده تناقض و backlog نه‌گانهٔ فاز
  ۰۲ ساخته شد؛ `PHASE-01-INVENTORY.json` با استخراج‌گر قابل بازتولید همراه آن است.
  رجیستری امنیت نیز با محل استفاده و وضعیت جاری منابع به‌روز شد.
- کد محصول فقط برای موجودی خوانده شد و تغییر نکرد؛ هیچ فایل `vira/` خوانده یا تغییر داده
  نشد و هیچ اعلان GitHub ارسال نشد.

**نقطهٔ بعد:** سه تصمیم نخست فاز ۰۲ در ADR-0001 تا ADR-0003 ثبت شد. providerها باید
تک‌به‌تک تعیین شوند؛ پیش از هر provider تحقیق تازه و فقط یک سؤال کوتاه مطرح شود.

---

## جمع‌بندی قطعی معماری پادو و اینستاگرام — ۱۴۰۵/۰۶/۰۶

این بخش از همهٔ اشاره‌های تاریخی بالاتر به Manus، کانال Instagram مبتنی بر session در Agent Reach، ChatPlace، ManyChat،
Graph API مستقیم، gateway ویرا یا provider انتخاب‌نشده جدیدتر و مقدم است.

### تصمیم نهایی

۱. پادو دستیار کامل بازاریابی، تحلیل، برنامه‌ریزی، تولید و یادگیری هر فروشگاه است، اما
   runtime آن فقط Playbookهای محدود، نسخه‌دار، schemaدار و قابل‌آزمون را اجرا می‌کند.
۲. DeepInfra تنها provider هدف inference نسخهٔ اول است. حساب، کلید، اعتبار، سقف و هزینه
   مستقل هر فروشگاه است؛ credential در IGBZ ذخیره نمی‌شود. انتخاب مدل هر Playbook با تیم
   IGBZ و فعال‌سازی با benchmark فارسی و صلاحیت جغرافیایی/قراردادی است.
۳. Zernio تنها provider اجتماعی است. پرداخت با حساب مرکزی IGBZ انجام می‌شود، هر فروشگاه
   profile جدا دارد و هزینه در اشتراک همان فروشگاه منظور می‌شود. کلید مرکزی فقط در secret
   store بک‌اند است.
۴. WooCommerce منبع حقیقت تجارت و بک‌اند IGBZ مرجع هویت، مجوز، صف، idempotency، rate
   limit، اجرای قطعی و audit است. پادو یا Zernio تصمیم تجاری خارج از policy اجرا نمی‌کنند.
۵. مسیرهای اصلی شامل انتشار/زمان‌بندی، Inbox، comment-to-DM، Insight و صدای کاتالوگ‌شده
   Instagram از Zernio هستند. پادو از فروش واقعی، Insight و نتیجهٔ campaign یاد می‌گیرد.
۶. تحلیل رقبا در نسخهٔ اول فقط با دادهٔ رسمی محدود و ورودی مدیر است. Business Discovery و
   Hashtag Search تا ارائهٔ endpoint رسمی Zernio backlog/گیت هستند.
۷. scraper، cookie و session مرورگر Instagram ممنوع‌اند. Manus، ChatPlace و ManyChat در
   migration کد حذف می‌شوند؛ کانال Instagram مبتنی بر session در Agent Reach و ورود Ayrshare
   نیز ممنوع‌اند. OpenRouter fallback عادی نسخهٔ اول نیست.
۸. TTS فارسی و sandbox امن PHP نیز تا آزمون/پیاده‌سازی واقعی گیت‌اند، نه قابلیت موجود.

### مراجع منبع حقیقت

```text
ADR/ADR-0004-PADO-ZERNIO-SOCIAL-ARCHITECTURE.md
DESIGN-INSTAGRAM-PADO-ZERNIO.md
PROMPT-IG-GROWTH-PADO.md
DESIGN-PADO.md §۲۲
DESIGN-AUTOMATION.md §۴۶
```

### واقعیت پیاده‌سازی و نقطهٔ شروع بعدی

- این جمع‌بندی در این نوبت فقط تغییر سند است؛ کد افزونه و پوشهٔ `vira/` تغییر نکرده‌اند.
- adapterهای Zernio و DeepInfra، profile mapping، webhook امضاشده، چهار Playbook و حلقهٔ
  یادگیری هنوز کد ندارند.
- پیشنهاد کانال Instagram مبتنی بر session در Agent Reach رد شده و کد Manus/ChatPlace/ManyChat بدهی migration است و نباید قابلیت معماری
  جدید معرفی شود.
- پیش از هر تغییر کد: تحقیق تازه، برنامهٔ فایل/آزمون، تأیید کارفرما و عبارت صریح «شروع کن»
  لازم است. آزمون فنی و مرور واقعی/اسکرین‌شات تمام‌صفحه نیز اجباری است.
- `vira/` مطلقاً بررسی یا تغییر داده نشود مگر با دستور صریح.
- مرور بصری سه سند اصلی با Chromium واقعی انجام شده و گزارش/تصاویر در
  `visual-testing/PADO-ZERNIO-DOCS-REPORT.md` ثبت شده است.

---

## ثبت جلسهٔ ۱۴۰۵/۰۶/۰۶ (۲۸ اوت ۲۰۶) — قانون ۱۵ و اجرای فازهای ۰۳ تا ۰۵

- **قانون ۱۵** به §۰ افزوده شد: پس از هر کار فقط یک گزارش خلاصهٔ ۴-۵ خطی؛ پرهیز از هرچیز سنگین‌کنندهٔ چت؛ قاعدهٔ ۲ به آن ارجاع می‌دهد.
- **فاز ۰۳:** شمارش‌های کهنهٔ «وضعیت جاری» (نمونهٔ سلامت ۷۰/۷۰ و دام شمارش جدول‌ها در همین سند و `DESIGN-DEPLOY-VIRA.md`) با کد (۷۲ جدول) هم‌راستا شد؛ بخش‌های تاریخیِ دارای تاریخ دست‌نخورده ماندند.
- **فاز ۰۴:** `igbz-suite/tests/DriftGuardTest.php` (شمارش جدول‌ها، نسخهٔ دیتابیس، جفت‌شدگی فیلد password ↔ رجیستری) + گام drift در `_devenv/test.sh` با `phase01_inventory.py --check`. در اجرای اول ۲۶ واگرایی واقعی را قرمز کرد.
- **فاز ۰۵:** رجیستری `Settings::SECRETS` به ۴۰ عضو رسید (۲۲ فیلد ممیزی‌شده + `vip.media_hmac_secret` که حین کار پیدا شد)؛ مهاجرت v20 (`encrypt_legacy_secrets` در `Settings` + `migrate_to_v20` در `Activator`)؛ `rotate_secret()`؛ تست `SecretsTest`؛ تست ۱۳۵۴/۲۶ سبز؛ لینت ۲۴۷/۰؛ سلامت زنده ۷۲/۷۲؛ تست ویژوال ۱۶ صفحه در `visual-testing/phase-05/` + گزارش `visual-testing/PHASE-05-REPORT.md` با **۰ نشت plaintext** (نکته: فیلدهای راز به‌عمد `type=text` رندر می‌شوند + توضیح «Stored encrypted»؛ ابزار ویژوال بر همین مبنا پایش می‌کند).
- **نقطهٔ شروع بعدی:** فاز ۰۶ (نقش‌ها و درهای هسته)؛ پیش از آن تحقیق تازه (قاعدهٔ ۱۰) و پس از آن مجوز کارفرما لازم است. `vira/` همچنان خارج محدوده؛ هیچ اعلان GitHub ارسال نشد.

---

## ثبت ادامهٔ جلسهٔ ۱۴۰۵/۰۶/۰۶ — اجرای فاز ۰۶ (نقش‌ها و درهای هسته) ✅

- ماتریس ۱۱ capability و سطح ajax ناشناس (`igbz_otp` تنها عضو) در `DriftGuardTest` پین شد.
- oEmbed کامل خاموش (`security.disable_oembed`) + مهاجرت v21 برای هم‌راستایی فرم و رفتار روی نصب‌های قدیمی؛ XML-RPC/Application Passwords/شمارش کاربر/صادرات از قبل تنظیم‌شده بودند و این جلسه **زنده تأیید** شدند (oEmbed ‏۴۰۴، App Passwords ‏۴۰۳).
- مسیر اضطراری مدیر ارشد: `security.senior_admin_id` (فقط super admin می‌نویسد؛ هر استفاده از گیت‌ها audit می‌شود)؛ تست `DoorsTest`. شناسهٔ واقعی هنوز از کارفرما نگرفته‌ایم (کار باز §۲).
- تست ۱۳۷۱/۲۷ · لینت ۲۴۸/۰ · موجودی به‌روز · ویژوال: `visual-testing/PHASE-06-REPORT.md`.
- **نقطهٔ شروع بعدی:** فاز ۰۷ (مجوز مسیرهای تجاری چندمستأجری) با تحقیق تازه؛ `vira/` خارج محدوده؛ هیچ اعلان GitHub نشد.

---

## ثبت ادامهٔ جلسهٔ ۱۴۰۵/۰۶/۰۶ — اجرای فاز ۰۷ (مجوز شیء چندمستأجری / الگوی مالکیت) ✅

- با مجوز یکپارچهٔ کارفرما برای چند فاز پشت‌سرهم انجام شد. تحقیق: الگوی مالکیت سمت سرور
  برای هر خواندن/نوشتن با شناسه + آزمون منفی دسترسی (منابع عمومی امنیت وب چندمستأجری).
- همهٔ خواندن‌های «شیء با شناسه» در مسیرهای تجاری شرط `tenant_id` مستأجر جاری گرفتند:
  `LmsService` (دوره/درس/آزمون/ثبت‌نام + سه خواندن خام در پیشرفت و گواهی)، تمدید اشتراک
  در `PlanService`، قرارداد و اقساط در `BnplService`، همکار در `AffiliateService`، پرداخت و
  فهرست پرداخت کاربر در `PaymentService`، و دو عملیات نوشتاری پنل ادمین `LmsPage`.
- چهار استثنای تعمدی ثبت شد: کال‌بک درگاه با `authority` (راز دارنده‌کلید)، کد معرف عمومی
  همکار، خود شیء مستأجر، و کاتالوگ جهانی طرح‌ها. جزئیات: `DESIGN-LEGAL-AUTH.md §۷.۷.۷`.
- تست منفی جدید `TenantScopeTest` (شناسهٔ مستأجر ۲ به دادهٔ مستأجر ۱ نمی‌رسد) + به‌روزرسانی
  دابل `LmsDb` برای رعایت شرط مستأجر. نتیجه: تست ۱۳۸۵/۲۸ · لینت ۲۴۹/۰ · موجودی به‌روز.
- **نقطهٔ شروع بعدی:** فاز ۰۸ (مسیرهای جانبی: لجستیک، مارکت‌پلیس، سئو، ترجمه، گیمیفیکیشن،
  حقوقی، دامنه)؛ کار باز قبلی: شناسهٔ مدیر ارشد از کارفرما؛ `vira/` خارج محدوده؛ اعلان GitHub نشد.

---

## ثبت ادامهٔ جلسهٔ ۱۴۰۵/۰۶/۰۶ — اجرای فاز ۰۸ (مسیرهای جانبی چندمستأجری) ✅

- لجستیک: مرسوله‌ها مستأجر-محور؛ چت پیک فقط با گیت مالکیت مرسوله؛ مسیرهای مشتری «وضعیت/
  مکان زنده» با کد رهگیری به‌عنوان اثبات دارنده‌کلید؛ چاپ لیبل با پیوند گروه به مستأجر.
- مارکت‌پلیس: پیوند محصول↔کانال کلید مستأجر گرفت (شناسهٔ محصول جهانی دیگر نشت/بازنویسی
  نمی‌کند)؛ گیمیفیکیشن: اعتبار هوش مصنوعی و سبد رهاشده مستأجر(+کاربر)-محور شدند.
- پرداخت اصلی: ایدمپوتنسی `hold` و عملیات اختلاف/بازپرداخت مستأجر-محور شدند.
- دامنه (نقص شناخته‌شدهٔ تأیید): `verify_dns` و `sync_tenant_domain` مستأجر-محور شدند؛
  توکن تأیید در هر نگاشت با `Crypto::token` تازه است؛ کنترل‌پلین‌ها به‌طرح ذاتی سراسری‌اند.
- چهار سناریوی منفی تازه در `TenantScopeTest`. نتیجه: تست ۱۳۹۳/۲۸ · لینت ۲۴۹/۰ · موجودی به‌روز.
- **نقطهٔ شروع بعدی:** فاز ۰۹ (اینستاگرام، هاب، اف‌ایکس، پادو، موبایل‌ای‌پی‌آی + نقش‌های سرویس/
  پیک/کارمند)؛ کار باز قبلی: شناسهٔ مدیر ارشد از کارفرما؛ `vira/` خارج محدوده؛ اعلان GitHub نشد.

---

## ثبت ادامهٔ جلسهٔ ۱۴۰۵/۰۶/۰۶ — اجرای فاز ۰۹ (مجوز سایر ماژول‌ها) ✅

- اینستاگرام: محتوای مانوس، محصول ورودی (خواندن/حذف)، قیف، قرعه‌کشی و کامنت‌های پنل
  مستأجر-محور شدند؛ موبایل‌ای‌پی‌آی: کامنت/پست در `VipAdminController`؛ اف‌ایکس: تسویهٔ دستی
  و حساب‌های ارائه‌دهنده؛ پادو: تصمیم بازبینی با اسکوپ مستأجر.
- تفکیک نقش‌ها با گیت‌های مالکیت (پیک/مشتری/پنل/کنترل‌پلین) تأمین شد؛ استثناهای تعمدی
  (کرون، قلاب ناهمگام پرداخت، کنترل‌پلین‌ها) در `DESIGN-LEGAL-AUTH.md §۷.۷.۹` ثبت شدند.
- تست‌های منفی تازه در `TenantScopeTest`. نتیجه: تست ۱۳۹۷/۲۸ · لینت ۲۴۹/۰ · موجودی به‌روز.
- **نقطهٔ شروع بعدی:** فاز ۱۰ (درخواست‌های بیرونی و SSRF)؛ کار باز قبلی: شناسهٔ مدیر ارشد؛
  `vira/` خارج محدوده؛ اعلان GitHub نشد.

---

## ثبت ادامهٔ جلسهٔ ۱۴۰۵/۰۶/۰۶ — اجرای فاز ۱۰ (درخواست‌های بیرونی و مهار اجبار درخواست) ✅

- دروازهٔ متمرکز `UrlGuard`: اسکیم فقط http/https؛ رد نشانی‌های خصوصی/حلقه‌ای/پیوند-محلی/
  متادیتا (IPv4+IPv6) و بدفرم‌ها؛ رزولور فعال در تولید؛ فیلتر گریز مستند.
- لفاف `Http` پیش از هر درخواست از دروازه می‌گذرد؛ تغییرمسیر خاموش؛ سقف اندازهٔ پاسخ
  (پیش‌فرض ۲۰ مگابایت، کلید `max_bytes`)؛ دو دانلود رسانهٔ کاربرساخت و تبدیل گفتار هم
  جداگانه از دروازه عبور کردند.
- سیاست حامل: کلید پادو فقط به میزبان پیکربندی‌شدهٔ خودش فرستاده می‌شود (دانلود زیپ).
- محدودیت محیطی ثبت‌شده: رزولور سندباکس برای هر نامی نشانی خصوصی می‌دهد؛ آزمون‌ها قطعی
  (آی‌پی مستقیم + `.test`). نتیجه: تست ۱۴۳۶/۲۹ · لینت ۲۵۱/۰ · موجودی به‌روز.
- **نقطهٔ شروع بعدی:** فاز ۱۱ (بارگذاری/آرشیو: کنترل میم/اندازه/زیپ‌بمب/سم‌لینک/قرنطینه)؛
  کار باز قبلی: شناسهٔ مدیر ارشد؛ `vira/` خارج محدوده؛ اعلان GitHub نشد.

---

## ثبت ادامهٔ جلسهٔ ۱۴۰۵/۰۶/۰۶ — اجرای فاز ۱۱ (بارگذاری، آرشیو و artefact) ✅

- دروازهٔ مشترک `ArchiveGuard`: سقف تعداد ورودی و حجم بازشده (هم‌تراز `ThemeValidator`)،
  رد نام‌های ناامن (`..`/مطلق/بک‌اسلش)، بررسی امضای بایت جادویی پیش از بازکردن.
- دریافت قالب پادو به دروازه منتقل شد؛ مسیر پیش‌نمایش/فعال‌سازی که پیش‌تر بی‌بازبینی
  استخراج می‌کرد هم از همان دروازه می‌گذرد (دفاع در عمق در هر دو نقطه).
- بارگذاری‌های دیگر (محصول/پنل فروشگاه) از `wp_handle_upload` با فهرست میم و سقف‌های
  وردپرس استفاده می‌کنند؛ پویش کد ممنوع `ThemeValidator` بدون تغییر باقی ماند.
- `ArchiveGuardTest` با آرشیوهای واقعی (سالم/زیپ‌اسلیپ/سقف تعداد/پی‌اچ‌پی با نام zip).
  نتیجه: تست ۱۴۴۳/۳۰ · لینت ۲۵۳/۰ · موجودی به‌روز.
- **نقطهٔ شروع بعدی:** فاز ۱۲؛ کار باز قبلی: شناسهٔ مدیر ارشد؛ `vira/` خارج محدوده؛ اعلان
  GitHub نشد.

---

## ثبت ادامهٔ جلسهٔ ۱۴۰۵/۰۶/۰۶ — اجرای فاز ۱۲ (هویت، OTP، JWT و دستگاه) ✅

- نوسازی توکن اتمیک شد: تصاحب شرطی در `TokenService::refresh`؛ بازندهٔ رقابت حکم بازپخش
  می‌گیرد (ابطال کل دستگاه + ثبت امنیتی)؛ نقطهٔ `/auth/refresh` سقف تلاش گرفت.
- قرارداد سمت سرور امضای بیومتریک (§۷.۶): `BiometricGate` با پنجرهٔ زمانی، نانس
  یک‌بارمصرف و زمان ثابت؛ کلید دستگاه رمزگذاری‌شده در ستون تازهٔ `signing_key` (مهاجرت
  دیتابیس v22)؛ ثبت‌نام دستگاه کلید را می‌گیرد و هرگز بازنمی‌گرداند.
- چرخهٔ جفت توکن/کول‌داون شماره+نشانی/سقف تلاش تأیید از قبل برقرار بود و ممیزی شد.
- `BiometricGateTest` + استاب‌های پشتیبان. نتیجه: تست ۱۴۵۰/۳۱ · لینت ۲۵۵/۰ · دیتابیس 22.
- **نقطهٔ شروع بعدی:** فاز ۱۳ (حریم خصوصی داده، دفتر رویداد، حذف و خروج از سرویس)؛ کار باز
  قبلی: شناسهٔ مدیر ارشد؛ `vira/` خارج محدوده؛ اعلان GitHub نشد.

---

## ثبت ادامهٔ جلسهٔ ۱۴۰۵/۰۶/۰۶ — اجرای فاز ۱۳ (حریم خصوصی، دفتر رویداد و خروج) ✅

- ماسک دادهٔ شخصی در لحظهٔ ورود به لاگ: تلفن فقط چهار رقم آخر، ایمیل حرف اول+دامنه،
  نشانی/کد ملی/کارت جایگزین کامل؛ شامل آرایه‌های تودرتو.
- ماندگاری رویدادها (کرون روزانه با `log.retention_days`) و پالایش توکن‌ها/دستگاه‌های مرده
  از قبل فعال بود و ممیزی شد.
- `TenantOffboarding` با قلاب حذف مستأجر همهٔ جداول مستأجردار را با ثبت امنیتی جارو می‌کند؛
  `uninstall.php` از قبل با انتخاب صریح اپراتور کار می‌کرد.
- `OffboardingTest`. نتیجه: تست ۱۴۶۰/۳۲ · لینت ۲۵۷/۰ · دیتابیس 22.
- **نقطهٔ شروع بعدی:** فاز ۱۴ (رزولور و repository مستأجر)؛ کار باز قبلی: شناسهٔ مدیر ارشد؛
  `vira/` خارج محدوده؛ اعلان GitHub نشد.

## ثبت ادامهٔ جلسهٔ ۱۴۰۵/۰۶/۰۶ — اجرای فاز ۱۴ (resolver و repository مستأجر) ✅

- مسیر واحد `TenantScope::page_tenant_id`: مستأجر صفحه‌های ادمین از هویت معتبر می‌آید؛
  `tenant_id` ارسالی کلاینت فقط برای دارندهٔ `MANAGE_TENANTS` اعتبار دارد. بستن BOLA
  کیف‌پول/افیلیت/BNPL (نقش مالک این اجازه‌ها را داشت ولی نباید مستأجر دیگر را هدف بگیرد).
- `BaseController::scoped_tenant_id` از قبل همین الگو را داشت؛ صفحات هاب/فهرست مستأجرها
  ذاتاً `MANAGE_TENANTS` و ممیزی شدند.
- پیام‌رسانی وی‌آی‌پی: مستأجر رشتهٔ گفت‌وگو از خود پست مشتق می‌شود (نه پارامتر کلاینت)؛
  خواندن رشته/پیام با شرط `user_id` قفل است.
- `TenantResolutionTest` (سناریوی منفی). نتیجه: تست ۱۴۶۴/۳۳ · لینت ۲۵۹/۰ · دیتابیس 22.
- **نقطهٔ شروع بعدی:** فاز ۱۵ (کش، فایل، نرخ و سهمیه)؛ کار باز قبلی: شناسهٔ مدیر ارشد؛
  `vira/` خارج محدوده؛ اعلان GitHub نشد.

## ثبت ادامهٔ جلسهٔ ۱۴۰۵/۰۶/۰۶ — اجرای فاز ۱۵ (جداسازی کش، فایل و نرخ) ✅

- مسیر واحد `TenantScope::cache_key`: کلیدهای کش/ترنزینتِ دادهٔ مستأجر با مستأجر حل‌شده
  نام‌فضا می‌گیرند (فلش سئو، استودیو هوش مصنوعی، پوش، فلش حساب، نتیجهٔ آزمون، لینک وی‌آی‌پی
  هاب)؛ کد بدون مستأجر در فضای صریح `t0`.
- جریان‌های منی‌چت با هش کلید حساب کش می‌شوند (نه کلید سراسری).
- سقف تجمیعی دقیقه‌ای هر مستأجر (`api.tenant_rate_limit_per_minute` پیش‌فرض ۶۰۰) جدا از سقف
  هر توکن در `Authenticator`؛ فایل قالب پادو در زیرشاخهٔ مستأجر.
- استثناهای سراسری (کاتالوگ مستأجر، توکن گوگل، دایرکتوری هاب، آنتی‌بروت‌فورس) ممیزی شدند.
- `TenantIsolationTest` (برخورد کلید + هم‌زمانی). نتیجه: تست ۱۴۷۵/۳۴ · لینت ۲۶۰/۰ · دیتابیس 22.
- **نقطهٔ شروع بعدی:** فاز ۱۶ (ثبت‌نام و provision فروشگاه)؛ کار باز قبلی: شناسهٔ مدیر ارشد؛
  `vira/` خارج محدوده؛ اعلان GitHub نشد.

## ثبت ادامهٔ جلسهٔ ۱۴۰۵/۰۶/۰۶ — اجرای فاز ۱۶ (ثبت‌نام و provision فروشگاه) ✅

- اجرای مجدد امن: ثبت‌نام تکراری با همان ایمیل و نشانی، فروشگاه موجود را برمی‌گرداند؛ بدون
  خطا و بدون ردیف تکراری.
- Rollback جزئی: خطا در عضویت/زیردامنه/نقش/اشتراک، مستأجر تازه‌ساخت و عضویتش را پاک می‌کند
  (جاروی خروج فاز ۱۳ هم اجرا می‌شود)؛ شکست زیردامنه به‌تنهایی فقط تنزل است و ثبت می‌شود.
- `SignupTest` با دابل ماندگار (ساخت کامل، اجرای مجدد، نشانی متعلق به دیگری، خرابی شبیه‌سازی‌شده).
- نتیجه: تست ۱۴۹۲/۳۵ · لینت ۲۶۱/۰ · دیتابیس 22.
- **نقطهٔ شروع بعدی:** فاز ۱۷ (مسیریابی دامنهٔ مستأجر)؛ کار باز قبلی: شناسهٔ مدیر ارشد؛
  `vira/` خارج محدوده؛ اعلان GitHub نشد.

## ثبت ادامهٔ جلسهٔ ۱۴۰۵/۰۶/۰۶ — اجرای فاز ۱۷ (مسیریابی دامنهٔ مستأجر) ✅

- دو شاخهٔ سرویس‌دهی رزولور (دامنه/مسیر) فقط مستأجر قابل‌مسیریابی را حل می‌کنند (`is_active`:
  فعال یا آزمایشی نامنقضی)؛ معلق/بسته/در انتظار دیگر سرویس نمی‌گیرد. فال‌بک‌های پنل مدیریت
  عمداً بی‌قید ماندند.
- نگاشت دامنه یک‌به‌یک شد (دامنهٔ متعلق به دیگری رد می‌شود؛ افزودن مجدد idempotent) و ورودی
  دامنه فقط میزبانِ معتبر کوچک‌نوشته ذخیره می‌شود.
- ممیزی شد: `DomainVerifier` با TXT/CNAME/A واقعی تأیید می‌کند؛ رزولور `verified_at` را الزام
  می‌کند؛ هدر میزبان بهداشت دارد؛ درهای هاب با `MANAGE_TENANTS`.
- `DomainRoutingTest` پنج سناریو. نتیجه: تست ۱۵۰۴/۳۶ · لینت ۲۶۲/۰ · دیتابیس 22.
- **نقطهٔ شروع بعدی:** فاز ۱۸ (قالب مستقل هر مستأجر)؛ کار باز قبلی: شناسهٔ مدیر ارشد؛
  `vira/` خارج محدوده؛ اعلان GitHub نشد.

## ثبت ادامهٔ جلسهٔ ۱۴۰۵/۰۶/۰۶ — اجرای فاز ۱۸ (معماری قالب tenant-scoped) ✅

- حذف کامل `switch_theme()` سراسری: فعال‌سازی زنده، ستون `theme` مستأجر را می‌نویسد و قالب‌های
  زندهٔ قبلی همان مستأجر را بایگانی می‌کند؛ سایت مادر و بقیهٔ فروشگاه‌ها دست نمی‌خورند.
- `TenantThemeRouter` با فیلترهای `template`/`stylesheet` در فرانت‌اند قالب خود مستأجر را اعمال
  می‌کند؛ قالب نصب‌نشده هرگز سرویس نمی‌شود.
- `rollback` فقط قالب زندهٔ همان مستأجر را بایگانی می‌کند (رفع `UPDATE` بی‌شرط) و نشانهٔ قبلی
  را پاک می‌کند. پیش‌نمایش `igbz_theme_preview` فقط برای عضو مستأجر/ادمین پلتفرم و فقط روی
  فروشگاه خودش رندر می‌شود.
- باقی‌ماندهٔ ثبت‌شده: فایل قالب پس از حذف مستأجر روی دیسک می‌ماند (بک‌لاگ).
- `ThemeRoutingTest` چهار سناریو. نتیجه: تست ۱۵۲۰/۳۷ · لینت ۲۶۴/۰ · دیتابیس 22.
- **نقطهٔ شروع بعدی:** فاز ۱۹ (مهاجرت داده و نسخه‌بندی)؛ کار باز قبلی: شناسهٔ مدیر ارشد؛
  `vira/` خارج محدوده؛ اعلان GitHub نشد.

## ثبت ادامهٔ جلسهٔ ۱۴۰۵/۰۶/۰۶ — اجرای فاز ۱۹ (چارچوب مهاجرت) ✅

- چارچوب `Migrator`: قفل تک‌اجراکننده با تصاحب قفل کهنه، نقطهٔ بررسی پس از هر گام، ادامه‌دادن
  به‌جای اجرای مجدد، گزارش پیشرفت خواندنی و قرارداد ایدمپوتنس.
- `Activator::maybe_upgrade` پلکان گام‌ها را به چارچوب سپرد؛ شکست گام نسخه را بالا نمی‌برد و
  درخواست بعدی از نقطهٔ بررسی ادامه می‌دهد؛ رفتار تاریخی نسخهٔ صفر حفظ شد.
- راه بازگشت صادقانه: گام‌های اسکیمی رو‌به‌جلو هستند؛ نسخهٔ پیش از ارتقا نگه داشته می‌شود و
  بازیابی = بازگرداندن پشتیبان + نوشتن همان نسخه.
- `MigratorTest` پنج سناریو (شامل ارتقای مستقیم از نسخهٔ قدیمی). نتیجه: تست ۱۵۴۴/۳۸ ·
  لینت ۲۶۶/۰ · دیتابیس 22.
- **نقطهٔ شروع بعدی:** فاز ۲۰ (آخر: کوئری، ایندکس و صفحه‌بندی)؛ کار باز قبلی: شناسهٔ مدیر
  ارشد؛ `vira/` خارج محدوده؛ اعلان GitHub نشد.

## ثبت ادامهٔ جلسهٔ ۱۴۰۵/۰۶/۰۶ — اجرای فاز ۲۰ (کوئری، ایندکس و صفحه‌بندی) ✅ — پایان مجوز چندفازی

- حذف انبوه به مسیر واحد `Db::delete_batches` رفت (دسته‌های مرتب بر id با سقف تکرار): هرس
  کد/توکن/دستگاه، ماندگاری لاگ، پاک‌سازی ادمین و جاروی خروج مستأجر.
- فهرست‌های بی‌کران اصلاح شدند: کی‌ست برای حساب‌های اینستاگرام؛ سقف برای دستگاه‌های کاربر
  (۱۰۰)، پیوند مارکت‌پلیس (۵۰۰۰)، قیف (۵۰۰)، کاتالوگ اف‌ایکس (۵۰۰) و طرح وی‌آی‌پی (۱۰۰).
- ایندکس‌های مرکب تازه (انقضای توکن‌ها، last_seen دستگاه‌ها)؛ دیتابیس ۲۳؛ اعتبارسنجی
  `EXPLAIN` روی دادهٔ حجیم وظیفهٔ عملیاتی تولید (سندباکس دیتابیس واقعی ندارد).
- `BatchTest`. نتیجه: تست ۱۵۵۴/۳۹ · لینت ۲۶۷/۰ · دیتابیس 23 · سایت زنده سالم.
- **نقطهٔ شروع بعدی:** فاز ۲۱ (سازگاری کد با HPOS) — بیرون از مجوز این اجرا؛ نیازمند برنامه
  و تأیید تازهٔ کارفرما. کار باز قبلی: شناسهٔ مدیر ارشد؛ `vira/` خارج محدوده؛ اعلان GitHub نشد.

## ثبت ادامهٔ جلسهٔ ۱۴۰۵/۰۶/۰۶ — اجرای فاز ۲۱ (سازگاری کد با HPOS) ✅ — پایان مجوز چندفازی

- ممیزی سراسری: همهٔ دسترسی‌های سفارش از قبل فقط از لایهٔ CRUD ووکامرس هستند.
- کلاس واحد جدید `Support/WooCommerceCompat.php`: اعلام رسمی سازگاری
  (`custom_order_tables` + `cart_checkout_blocks`)، لینک ویرایش سفارش و نام جدول درآمد.
- باگ واقعی در راستی‌آزمایی زنده روی فروشگاه سندباکس با فعال‌سازی واقعی HPOS یافت و اصلاح شد:
  `OrderUtil::get_order_edit_link` در ووکامرس ۱۱ وجود ندارد → `get_order_admin_edit_url`.
- `HposOrderFlowTest` (۴۰ کیس): جریان‌های سفارش در هر دو حالت قدیمی و HPOS.
- نتیجه: تست ۱۵۸۱/۴۰ · لینت ۲۶۹/۰ · موجودی به‌روز · صفحات ادمین با HPOS روشن سالم.
- ثبت صادقانه: تصویربرداری مرورگری در این سندباکس اجرا نشد (دانلود مرورگر مسدود)؛ وارسی زندهٔ
  DOM جایگزین شد. مهاجرت/راستی‌آزمایی داده وظیفهٔ فاز ۲۲ است.
- **نقطهٔ شروع بعدی:** فاز ۲۲ (مهاجرت و راستی‌آزمایی HPOS) — نیازمند برنامه و تأیید تازهٔ کارفرما.
  کار باز قبلی: شناسهٔ مدیر ارشد؛ `vira/` خارج محدوده؛ اعلان GitHub نشد.

## ثبت ادامهٔ جلسهٔ ۱۴۰۵/۰۶/۰۶ — اجرای فاز ۲۲ (مهاجرت و راستی‌آزمایی HPOS) ✅ — دروازهٔ موج دو پاس شد

- `RUNBOOK-HPOS.md`: مسیر کامل مهاجرت (پیش‌نیاز → همگام‌سازی → راستی‌آزمایی → سوئیچ → پایش →
  بازگشت) با واقعیت‌های تأییدشدهٔ زندهٔ ووکامرس ۱۱ (معنای `'yes'`/`'no'` گزینه‌ها، نگهبان
  «صفر بودن صف همگام‌سازی»، مسیر دسته‌ای رسمی).
- ممیزی جریان‌ها: مرجوعی/تمدید/گزارش افزونه به ذخیره‌سازی سفارش وابسته نیستند؛ سناریوی مرجوعی
  به `HposOrderFlowTest` افزوده شد (۱۵۸۷/۴۰ سبز).
- دروازهٔ موج دو پاس شد: آزمون‌های مستقل و سبز برای cross-tenant، migration و HPOS.
- **نقطهٔ شروع بعدی:** فاز ۲۳ (هستهٔ Action Scheduler — آغاز موج سه).
  کار باز قبلی: شناسهٔ مدیر ارشد؛ `vira/` خارج محدوده؛ اعلان GitHub نشد؛ تصویربرداری مرورگری
  در این سندباکس ممکن نیست (دانلود مرورگر مسدود).

## ثبت ادامهٔ جلسهٔ ۱۴۰۵/۰۶/۰۶ — اجرای فاز ۲۳ (هستهٔ صف بادوام کار) ✅

- جدول `jobs` (دیتابیس ۲۴) + `Envelope` نسخه‌دار + `JobQueue` + `JobContext`: ادعای لیزدار با
  به‌روزرسانی شرطی، پشت‌بند نمایی سقف‌دار با لرزش، بن‌بست با دلیل، لغو فقط برای شروع‌نشده‌ها،
  کلید همسانی پایدار در همهٔ تلاش‌ها و آزادشده پس از پایان، مستأجر/ردیابی/گروه تا هندلر.
- **باگ نهفته از فاز ۱۹ کشف و اصلاح شد:** نخستین ارتقای واقعی دیتابیس روی فروشگاه زنده سقوط کرد —
  ارجاع متد خصوصی در نردبان مهاجرت برای `Migrator` قابل‌اجرا نبود. گام‌ها عمومی شدند و آزمون
  بازگشتی نردبان واقعی اضافه شد. مهاجرت واقعی ۲۳→۲۴ زنده تأیید شد (جدول ساخته شد، قفل آزاد شد).
- `JobQueueTest` با ۱۱ سناریو. نتیجه: تست ۱۶۷۱/۴۱ · لینت ۲۷۳/۰ · سایت زنده سالم.
- **نقطهٔ شروع بعدی:** فاز ۲۴ (انتقال کارهای پنج‌دقیقه‌ای به صف).
  کار باز قبلی: شناسهٔ مدیر ارشد؛ `vira/` خارج محدوده؛ اعلان GitHub نشد؛ تصویربرداری مرورگری
  در این سندباکس ممکن نیست.

## ثبت ادامهٔ جلسهٔ ۱۴۰۵/۰۶/۰۶ — اجرای فاز ۲۴ (انتقال کارهای پنج‌دقیقه‌ای) ✅

- پنج کار پنج‌دقیقه‌ای (محتوا، ثبت، انتشار/انقضای وی‌آی‌پی، مارکت‌پلیس) به صف بادوام منتقل شدند:
  ضربان فقط با کلید همسانیِ بازه‌ای صف‌بندی می‌کند؛ `QueueRunner` با بودجهٔ کار/زمان تخلیه می‌کند.
- باگ واقعی آزمون زنده: هوک وردپرس به شنونده‌ها آرگومان می‌دهد؛ پوستهٔ `on_beat` + آزمون بازگشتی.
- آزمون زندهٔ ضربان: هر ۵ کار اجرا و `done` شدند؛ ضربان تکراری جذب شد.
- `CronJobsTest` با ۷ سناریو. نتیجه: تست ۱۷۰۵/۴۲ · لینت ۲۷۵/۰ · سایت زنده سالم.
- **نقطهٔ شروع بعدی:** فاز ۲۵ (انتقال کارهای ساعتی). کار باز قبلی: شناسهٔ مدیر ارشد؛ `vira/`
  خارج محدوده؛ اعلان GitHub نشد؛ تصویربرداری مرورگری در این سندباکس ممکن نیست.

## ثبت ادامهٔ جلسهٔ ۱۴۰۵/۰۶/۰۶ — اجرای فاز ۲۵ (انتقال کارهای ساعتی با عدالت مستأجر) ✅

- جاروب‌های ساعتی (`bnpl.overdue`، `bnpl.reminders`، `carts.sweep`) به پخش‌بینی «یک کار به‌ازای
  هر مستأجر فعال» منتقل شدند: کلید همسانی `بازه:مستأجر`، `group_key` مستأجر، ادعای نوبتی
  (راند-رابین) بین گروه‌ها در `claim`، و قرارداد ادامه — بچهٔ پُر دور بعد را با کلید مشتق صف
  می‌کند، سقف ۱۰ دور در ساعت. سرویس‌ها پارامتر اسکوپ `tenant_id` گرفتند (صفر = رفتار قدیمی).
- کارهای کنترل‌پلن `ig.funnels.retry` (با ادامه)، `ig.insights.reconcile` و `hub.tick` (با گیت
  فاصله‌ای در زمان اجرا) به صف منتقل شدند؛ دونده در ضربان ساعتی هم تخلیه می‌کند.
- `HourlyJobsTest` با ۱۰ سناریو + پشتیبانی `get_col` در دوقلوی صف.
- آزمون زنده: ۳ کار کنترل‌پلن و ۶ کار پخش‌بینی (۲ مستأجر × ۳ جاروب) همه `done`؛ ضربان تکراری
  جذب شد. نتیجه: تست ۱۷۴۶/۴۳ · لینت ۲۷۶/۰ · سایت زنده سالم.
- **نقطهٔ شروع بعدی:** فاز ۲۶ (انتقال کارهای روزانه و cleanup — batchهای محدود و ادامه‌پذیر).
  کار باز قبلی: شناسهٔ مدیر ارشد؛ `vira/` خارج محدوده؛ اعلان GitHub نشد؛ تصویربرداری مرورگری
  در این سندباکس ممکن نیست.

## ثبت ادامهٔ جلسهٔ ۱۴۰۵/۰۶/۰۶ — اجرای فاز ۲۶ (انتقال کارهای روزانه و cleanup) ✅

- هر ده کار روزانه به جاب‌های مستقل با کلید همسانیِ بازهٔ روزانه منتقل شدند: تمدید پلن‌ها،
  پورسانت‌ها، فکش مارکت‌پلیس، آزادسازی سپرده، گردآوری اینسایت، پاکسازی توکن/دستگاه، قبض‌سازی و
  تسویهٔ ارزی، شارژ کارت رمپ و نگهداشت کرون.
- قرارداد ادامه به `JobQueue::continue_round()` رسمی شد (یک نقطهٔ واحد؛ فاز ۲۵ هم بازنشانی شد)؛
  تسویهٔ ارزی به `bill_accounts()`/`settle_due(limit)` تفکیک شد؛ گیت‌ها به زمان اجرا منتقل شدند.
- `QueueRunner` در ضربان روزانه هم تخلیه می‌کند. `DailyJobsTest` با ۹ سناریو.
- آزمون زندهٔ ضربان روزانه: هر ۱۰ کار در همان ضربان `done`؛ ضربان تکراری جذب شد.
  نتیجه: تست ۱۷۸۳/۴۴ · لینت ۲۷۷/۰ · سایت زنده سالم.
- **نقطهٔ شروع بعدی:** فاز ۲۷ (مشاهده‌پذیری و آزمون صف — backlog/age/replay/DLQ).
  کار باز قبلی: شناسهٔ مدیر ارشد؛ `vira/` خارج محدوده؛ اعلان GitHub نشد؛ تصویربرداری مرورگری
  در این سندباکس ممکن نیست.

## ثبت ادامهٔ جلسهٔ ۱۴۰۵/۰۶/۰۶ — اجرای فاز ۲۷ (مشاهده‌پذیری و آزمون صف) ✅

- `JobQueue::stats()` (شمارش وضعیت‌ها + سن قدیمی‌ترین کار منتظر)، `dead_letters()` و `replay()`
  (بازصف‌بندی کنترل‌شده با صفر شدن تلاش‌ها و حفظ کلید همسانی) افزوده شد.
- داشبورد مدیریتی `JobsPage` (پشت‌لاگ/سن + جدول بن‌بست‌ها با دکمهٔ بازپخش نانس‌دار) و دستورهای
  `wp igbz jobs stats|dead|replay|drain` (ثبت فقط در حضور `WP_CLI`).
- `QueueLoadTest` با ۶ سناریو: عدالت زیر بار، ادعای جدای دو کارگر، قطع بالادست → پشت‌بند → بن‌بست،
  بازپخش زنده، صحت آمار و ترتیب بن‌بست‌ها.
- صفحهٔ داشبورد زنده با ۲۰۰ رندر شد. نتیجه: تست ۱۸۱۴/۴۵ · لینت ۲۸۰/۰ · سایت زنده سالم.
  **دروازهٔ موج سه پاس شد:** هیچ کار طولانی/رشدپذیری بر کرون تجمیعی بدون صف بادوام نمانده است.
- **نقطهٔ شروع بعدی:** فاز ۲۸ (لجر کیف‌پول). کار باز قبلی: شناسهٔ مدیر ارشد؛ `vira/` خارج محدوده؛
  اعلان GitHub نشد؛ تصویربرداری مرورگری در این سندباکس ممکن نیست.

## ثبت ادامهٔ جلسهٔ ۱۴۰۵/۰۶/۰۶ — اجرای فاز ۲۸ (دفتر کل کیف پول) ✅

- چرخهٔ کامل رزرو در لجر: `reserve()`، `release_reserve()` (دقیقاً یک‌بار)، `settle_reserve()`
  (نهاد صفر-مبلغ `mark` — مصرف دائمی بدون جابه‌جایی اضافه) و `refund()` با قرارداد مرجع
  `refund:{اصل}:{بازپرداخت}` و گارد مجموع که بازپرداختِ بیش از بدهی اصلی را قبل از نوشتن رد می‌کند.
- همسانی بر کلید مرجع در همهٔ عملیات‌ها (تکرار → `duplicate`)؛ گاردهای `already_settled` /
  `already_released` / `unknown_reservation` / `over_refund` / `unknown_original`.
- `check_invariant()` + `reconcile_all()` (پیمایش keyset محدود و تعمیر انحراف) — به‌صورت کار
  روزانهٔ `wallet.reconcile` به صف بادوام وصل شد.
- `WalletLedgerTest` با ۷ سناریو (شامل رقابت دو برداشت که هرگز منفی نمی‌شود و تعمیر انحراف).
- آزمون زنده: کار تطبیق در ضربان روزانهٔ واقعی `done` شد؛ انحراف صفر.
  نتیجه: تست ۱۸۶۵/۴۶ · لینت ۲۸۱/۰ · سایت زنده سالم.
- **نقطهٔ شروع بعدی:** فاز ۲۹ (هستهٔ تراکنش و webhook inbox). کار باز قبلی: شناسهٔ مدیر ارشد؛
  `vira/` خارج محدوده؛ اعلان GitHub نشد؛ تصویربرداری مرورگری در این سندباکس ممکن نیست.

## ثبت ادامهٔ جلسهٔ ۱۴۰۵/۰۶/۰۶ — اجرای فاز ۲۹ (هستهٔ تراکنش و وب‌هوک اینباکس) ✅

- جدول `webhook_events` (اسکیمای v25، نسخهٔ دیتابیس ۲۵): دریافت سریع و هم‌زمان فقط درج می‌کند؛
  تکرارزدایی با کلید یکتای `(source, event_key)` در سطح دیتابیس → پاسخ `duplicate` با شناسهٔ اصلی.
- امضا: `WebhookInbox::verify_signature()` — HMAC-SHA256 روی بدنهٔ خام با سکرت هر سورس (سکرت مشترک
  به‌عنوان fallback) و `hash_equals`؛ تحویل نامعتبر هرگز دیسپچ نمی‌شود و با دلیل مرده ثبت می‌گردد.
- پردازش ناهم‌گام: `claim_due()` با UPDATE شرطی اتمی، `process_batch()` با بک‌آف توانی (۳۰ ثانیه تا
  سقف یک ساعت)؛ وضعیت «نامعلوم» یا خطا به استخر برمی‌گردد و پس از اتمام تلاش‌ها با دلیل مرده می‌شود.
- `PaymentStateMachine`: گذارهای مجاز فقط (`paid/failed/cancelled` پایانی؛ `unknown` درجه‌یک) با
  نوشتن شرطی چسبیده به وضعیت فعلی — بازندهٔ رقابت `lost_race` می‌گیرد، نه خراب‌کاری.
  `PaymentService::apply_notification()` مسیر اعلان ناهم‌گام؛ تسویه از همان کد مسیر بازگشت مرورگر.
- سطوح: `POST /igbz/v1/webhook/{source}` (بدون لاگین؛ اعتبار با امضا؛ در نبود شناسه، هش بدنه) و
  کار پنج‌دقیقه‌ای `webhooks.drain` با ادامه‌دهی دسته‌ای؛ سورس `psp` به ماشین وضعیت متصل.
- `WebhookInboxTest` با ۸ سناریو؛ آزمون زنده: مهاجرت واقعی به v25، دریافت→تکرار→درین→آمار هم‌خوان و
  پاسخ صحیح اندپوینت REST روی پلی‌گراند. نتیجه: تست ۱۹۲۸/۴۷ · لینت ۲۸۵/۰ · سایت زنده سالم.
- **نقطهٔ شروع بعدی:** فاز ۳۰ (سخت‌سازی آداپتورهای درگاه ریالی). کار باز قبلی: شناسهٔ مدیر ارشد؛
  `vira/` خارج محدوده؛ اعلان GitHub نشد؛ تصویربرداری مرورگری در این سندباکس ممکن نیست.

## ثبت ادامهٔ جلسهٔ ۱۴۰۵/۰۶/۰۶ — اجرای فاز ۳۰ (سخت‌سازی درگاه‌های ریالی) ✅

- تایم‌اوت واحد برای همهٔ ۱۵ آداپتور: `PspHttp::timeout()` (تنظیم `payments.timeout`، پیش‌فرض ۳۰،
  کلمپ ۵–۶۰)؛ ۲۰ تایم‌اوت هاردکد ۲۵/۳۰ در ۱۱ فایل حذف شد.
- طبقه‌بندی خطای مشترک `GatewayErrors::classify()`: ترنسپورت → `network_timeout` (گذرا)، بدنهٔ خالی
  → `invalid_response`، کد/پیام خود درگاه دست‌نخورده؛ اعمال روی زرین‌پال و HttpPsp.
- ریفاند به‌صورت قابلیت: `RefundableGatewayInterface` + `PaymentRefundResult`؛ زرین‌پال پیاده نشد
  (تحقیق: سرویسش حسب دستور بانک مرکزی موقتاً غیرفعال است — چیزی جعل نشد)؛ `HttpPspGateway` با
  `refund_url` و کلید اجباری `Idempotency-Key` پیاده شد.
- همسانیِ ایجاد: `start()` ردیف `created` هم‌کلید را بازیابی می‌کند؛ هر درخواست کلید `pay-{id}`
  (یا کلید تماس‌گیرنده) را حمل می‌کند؛ ایجاد هرگز خودکار تکرار نمی‌شود (خطر شارژ مضاعف).
- `refund_payment()`: فقط `paid`، گارد مجموع ≤ مبلغ، کلید `refund:{id}:{ترتیب}`، ثبت در متا.
- `GatewayHardeningTest` با ۷ سناریو؛ آزمون زنده: ۱۵ آداپتور بی‌خطا، تشخیص درست قابلیت ریفاند،
  رد سریع بدون تماس با درگاه. نتیجه: تست ۱۹۶۲/۴۸ · لینت ۲۹۰/۰ · سایت زنده سالم.
- **نقطهٔ شروع بعدی:** فاز ۳۱ (درگاه مرکزی، refund و reconciliation). کار باز قبلی: شناسهٔ مدیر ارشد؛
  `vira/` خارج محدوده؛ اعلان GitHub نشد؛ تصویربرداری مرورگری در این سندباکس ممکن نیست.

## ثبت ادامهٔ جلسهٔ ۱۴۰۵/۰۶/۰۶ — اجرای فاز ۳۱ (درگاه مرکزی، درگاه مرکزی و تطبیق) ✅

- اسکیمای v26: ستون `refunded_amount` روی `ig_master_payments` و `idempotency_key VARCHAR NULL`
  با ایندکس یکتای `(tenant_id, idempotency_key)` روی `ig_master_withdrawals`.
- `refund()` بازنویسی شد: قفل خوش‌بینانه (`lost_race`)، گارد `over_refund` پیش از نوشتن، ریفاند
  کامل/جزئی، گذار به `refunded`؛ رد روی `released` (تحویل مضاعف) و `already_refunded`.
- `release()` فلیپ شرطی `held→released` گرفت — دو سوییپ هم‌زمان یک ردیف را دو بار تسویه نمی‌کنند؛
  تسویه فقط باقیماندهٔ ریفاندنشده را اعتبار می‌دهد. `resolve_dispute()` با حکم release/refund.
- `reconcile()`: کردیت گم‌شدهٔ `master:{id}` را شناسایی و با کردیت همسان تعمیر می‌کند + اختلاف‌های
  باز کهنه را می‌شمارد — کار روزانهٔ `master.reconcile` (ششمین کار روزانه).
- برداشت با کلید همسانی: تکرار → `duplicate` با شناسهٔ اصلی؛ مرجع بدهی کیف پول نیز کلیددار.
- `MasterPaymentServiceTest` با ۹ سناریو؛ آزمون زنده: مهاجرت واقعی به v26، هولد→تکرار→ریفاند جزئی→
  رد بیش‌برداشت→تطبیق بدون انحراف. نتیجه: تست ۲۰۰۹/۴۹ · لینت ۲۹۱/۰ · سایت زنده سالم.
- **نقطهٔ شروع بعدی:** فاز ۳۲ (پلن و اشتراک). کار باز قبلی: شناسهٔ مدیر ارشد؛ `vira/` خارج محدوده؛
  اعلان GitHub نشد؛ تصویربرداری مرورگری در این سندباکس ممکن نیست.

## ثبت ادامهٔ جلسهٔ ۱۴۰۵/۰۶/۰۶ — اجرای فاز ۳۲ (پلن و اشتراک) ✅

- تعلیق دستی `suspend()` با فلیپ شرطی روی وضعیت‌های زنده + تعلیق هم‌زمان مستأجر؛ تکرار و
  وضعیت‌های مرده رد می‌شوند.
- بازفعال‌سازی `reactivate()` با حفظ زمان باقی‌مانده؛ دورهٔ گذشته در تعلیق به `past_due` برمی‌گردد
  تا سوئیپ تمدید آن را بردارد (بدون پول‌گرفتن دستی). اشتراک معلق در `active_subscription()` نامرئی است.
- مهلت: `plans.grace_days` (بذر ۳، پیش‌فرض کد ۷، ≥۰)؛ سوئیپ `expire_past_grace()` انقضا فقط پس از
  «پایان دوره + مهلت» با فلیپ شرطی و تعلیق مستأجر.
- هر دو سوئیپ تمدید و مهلت روی کار روزانهٔ `plans.renewals` با ادامه‌دهی دسته‌ای (مجموع هر دو مبنای
  ادامه است).
- `PlansLifecycleTest` با ۶ سناریو؛ آزمون زنده: مقدار مهلت زنده و متدهای جدید تأیید شدند.
  نتیجه: تست ۲۰۲۹/۵۰ · لینت ۲۹۲/۰ · سایت زنده سالم.
- **نقطهٔ شروع بعدی:** فاز ۳۳ (BNPL). کار باز قبلی: شناسهٔ مدیر ارشد؛ `vira/` خارج محدوده؛ اعلان
  GitHub نشد؛ تصویربرداری مرورگری در این سندباکس ممکن نیست.

## ثبت ادامهٔ جلسهٔ ۱۴۰۵/۰۶/۰۶ — اجرای فاز ۳۳ (بی‌ان‌پی‌ال) ✅

- اسکیمای v27: ستون `collection_attempts` روی `bnpl_installments`.
- فلیپ شرطی `mark_installment_paid()` — کال‌بک درگاه و وصول خودکار که روی یک قسط مسابقه دهند هرگز
  دو بار تسویه/هوک/گزارش نمی‌فرستند؛ تکرار بی‌اثر است.
- سورس `bnpl` به اینباکس وب‌هوک (فاز ۲۹) وصل شد: `apply_provider_notification()` با دو شکل شناسه،
  حکم «نامعلوم» صادقانه به اینباکس برمی‌گردد، پیام ناقص بدون تکرار بی‌پایان تصدیق می‌شود.
- تلاش مجدد محدود: سقف `bnpl.max_collect_attempts` (پیش‌فرض ۵) — پس از سقف، کیف پول دیگر لمس
  نمی‌شود و مسیر قصردادن ادامه می‌یابد.
- `reconcile()`: تسویهٔ قراردادهای تمام‌پرداختی جامانده + گزارش انحراف مبالغ بدون اصلاح بی‌صدا —
  کار روزانهٔ `bnpl.reconcile` (هفتمین کار روزانه).
- `BnplHardeningTest` با ۵ سناریو؛ آزمون زنده: مهاجرت واقعی به v27، اجرای تطبیق و تصدیق کال‌بک.
  نتیجه: تست ۲۰۵۲/۵۱ · لینت ۲۹۳/۰ · سایت زنده سالم.
- **نقطهٔ شروع بعدی:** فاز ۳۴ (قرارداد، او‌تی‌پی پرداخت و شاهکار). کار باز قبلی: شناسهٔ مدیر ارشد؛
  `vira/` خارج محدوده؛ اعلان GitHub نشد؛ تصویربرداری مرورگری در این سندباکس ممکن نیست.

## ثبت ادامهٔ جلسهٔ ۱۴۰۵/۰۶/۰۶ — اجرای فاز ۳۴ (قرارداد، او‌تی‌پی پرداخت و شاهکار) ✅

- سه نتیجهٔ شاهکار از هم جدا شد: `matched` / `mismatch` / `error` — شکست ترنسپورت، بدنهٔ خالی و
  پاسخ بی‌حکم دیگر «عدم تطابق» ثبت نمی‌شوند (تهمت به کاربر و بن‌بست تلاش مجدد حذف شد). طبقه‌بندی در
  `classify_response()` جدا از وضعیت کاربر و تست‌پذیر است.
- اعتبارسنجی پیش از رجیستری: چک‌سام رسمی کد ملی ده‌رقمی + فرمت موبایل؛ ورودی نامعتبر تماسی با
  رجیستری نمی‌گیرد و ردی برجا نمی‌گذارد.
- سقف تلاش روزانهٔ هر کاربر `legal.shahkar_max_attempts_per_day` (پیش‌فرض ۵).
- انضباط پی‌آی‌آی مستند شد: فقط هش کد ملی ذخیره می‌شود؛ موبایل ذخیره و کد ملی لاگ نمی‌شود.
- `NationalIdVerifierTest` با ۴ سناریو. نتیجه: تست ۲۰۸۲/۵۲ · لینت ۲۹۴/۰.
- ⚠ توکن گیت‌هاب سندباکس منقضی شده؛ پوش فازهای ۳۳ و ۳۴ منتظر اتصال مجدد گیت‌هاب توسط کارفرماست
  (کامیت‌ها محلی‌اند: فاز ۳۳ `c716dfc`).
- **نقطهٔ شروع بعدی:** فاز ۳۵ (نرخ و نقل‌قول ارزی). کار باز قبلی: شناسهٔ مدیر ارشد؛ `vira/` خارج
  محدوده؛ اعلان GitHub نشد؛ تصویربرداری مرورگری در این سندباکس ممکن نیست.

## ثبت ادامهٔ جلسهٔ ۱۴۰۵/۰۶/۰۶ — فازهای ۳۳ تا ۳۵ + بازیابی پس از بازسازی سندباکس ✅

- ⚠ سندباکس میانهٔ کار بازسازی شد: تاریخ گیت محلی به پایه ریست شد. فازهای ۲۹ تا ۳۲ روی ریموت امن
  بودند (پوش شده)؛ کامیت‌های محلی فاز ۳۳/۳۴ از بین رفتند اما محتوای فایل‌ها در اسنپ‌شات ماند و با
  `git fetch` + `git reset --mixed FETCH_HEAD` تاریخ از ریموت بازیابی و همهٔ تغییرات دوباره کامیت شد.
  درس: بلافاصله پس از هر کامیت، پوش کن.
- فاز ۳۳ (بی‌ان‌پی‌ال، اسکیمای v27): فلیپ شرطی پرداخت قسط (تکرار بی‌اثر؛ مسابقهٔ کال‌بک/وصول هرگز
  دو بار تسویه نمی‌شود)، سورس `bnpl` به اینباکس وب‌هوک وصل شد (حکم نامعلوم → تلاش مجدد)، سقف تلاش
  وصول `bnpl.max_collect_attempts`، و `reconcile()` گزارشگر + کار روزانهٔ `bnpl.reconcile`.
- فاز ۳۴ (شاهکار): تفکیک سه نتیجهٔ `matched`/`mismatch`/`error` (شکست ترنسپورت دیگر «عدم تطابق»
  نیست)، چک‌سام رسمی کد ملی و فرمت موبایل پیش از تماس با رجیستری، سقف تلاش روزانه، انضباط پی‌آی‌آی
  مستند شد.
- فاز ۳۵ (نقل‌قول ارزی، اسکیمای v28): قفل مدرک‌دار (نرخ خام/اسپرد/نرخ اعمال‌شده/انقضا)، رد صریح در
  نبود نرخ، حذف رقابت نقل‌قول/قفل با ترتیب قفل-اول در `FxTopupService`، و `quote_is_valid()`.
- ابزارها نیز بازسازی شدند: `setup.sh` محیط تست و لینتر `_devenv/.work/lint.php` از نو ساخته شد.
- نتیجه: تست ۲۱۰۲/۵۳ · لینت ۲۹۵/۰ · اسموک زندهٔ مهاجرت به v28 و سرویس‌ها پاس · سایت زنده سالم.
- **نقطهٔ شروع بعدی:** فاز ۳۶ (پرداخت و تسویهٔ ارزی). کار باز قبلی: شناسهٔ مدیر ارشد؛ `vira/` خارج
  محدوده؛ اعلان GitHub نشد؛ تصویربرداری مرورگری در این سندباکس ممکن نیست.

## ثبت ادامهٔ جلسهٔ ۱۴۰۵/۰۶/۰۶ — فاز ۳۶ ✅

- تسویهٔ ارزی سخت شد (بدون تغییر اسکیمای v28): وضعیت نامعلوم (`pending` — برداشت نگه داشته
  می‌شود، بازپرداخت در تردید یعنی پرداخت دوباره)، سقف ریسک روزانهٔ `fx.payout_daily_cap_usd`،
  آستانهٔ تأیید انسانی `fx.payout_approval_threshold_usd` (قبض بزرگ فقط دستی)، و
  `reconcile()` روزانه با `query()` اختیاری ادپترها؛ همهٔ احکام از تک‌نقطهٔ `resolve_payout()`
  می‌گذرند پس بازپخش وب‌هوک بی‌اثر است. کار روزانهٔ هشتم: `fx.billing.reconcile`.
- تست `FxBillingHardenTest` با ۸ سناریو؛ نتیجه: تست ۲۱۳۷/۵۴ · لینت ۲۹۶/۰ · اسموک زنده پاس.
- **وضعیت:** موج یک و دو و فازهای ۲۳–۳۶ تمام. ادامهٔ فهرست فازها (موج پنجم، فازهای ۳۷ به بعد)
  به تصمیم فاز ۰۲ گره خورده که هنوز باز است — برای ادامه باید کارفرما تصمیم ظرفیت/دیتابیس/ترجمه
  را بگیرد، یا فاز مستقل دیگری ابلاغ شود.

## ثبت ادامهٔ جلسهٔ ۱۴۰۵/۰۶/۰۶ — فاز ۳۷ ✅

- ماژول دامنه با قرارداد ادپتر (جستجو/نقل‌قول/ثبت) و اسکیمای v29 ساخته شد: جدول
  `ig_domain_quotes` (قیمت با مهلت، کلمپ TTL، رد قیمت غیرمثبت) و `ig_domain_orders.idempotency_key`
  با کلید یکتا؛ رزرو فقط ردیف محلی است و `register()` هرگز پیش از پرداخت صدا نمی‌خورد؛
  `confirm_paid()` یک‌بار رزرو را `paid` می‌کند. تست `DomainOrderTest` با ۵ سناریو.
- نتیجه: تست ۲۱۷۵/۵۵ · لینت ۳۰۰/۰ · اسموک زندهٔ مهاجرت به v29 پاس · سایت زنده سالم.
- **فاز بعدی:** ۳۸ (ثبت فقط پس از پرداخت قطعی، کال‌بک امضاشده، پایش پشتیبان و جبران).

## ثبت ادامهٔ جلسهٔ ۱۴۰۵/۰۶/۰۶ — فاز ۳۸ ✅

- ثبت دامنه فقط روی سفارش `paid`؛ کرش میانهٔ فراخوانی وضعیتی مرئی (`registering`)؛ شکست
  پرووایدر ⇒ `failed` + بازپرداخت ایدمپوتنت ارزی + ژورنال (`ig_domain_journal`، اسکیمای v30).
  کال‌بک‌ها با HMAC-SHA256 راستی‌آزمایی می‌شوند؛ پایش پشتیبان `poll_stuck()` احکام را از مسیر
  یکتای `apply_provider_result()` اعمال می‌کند و حکم نامعلوم حدس زده نمی‌شود.
- تست `DomainRegistrationTest` با ۶ سناریو؛ نتیجه: تست ۲۲۱۲/۵۶ · لینت ۳۰۱/۰ · اسموک زنده پاس.
- **فاز بعدی:** ۳۹ (تمدید، دی‌ان‌اس و تطبیق دامنه).

## ثبت ادامهٔ جلسهٔ ۱۴۰۵/۰۶/۰۶ — فاز ۳۹ ✅

- چرخهٔ عمر دامنه کامل شد (اسکیمای v31): تمدید محدود به مستأجر با اولویت رجیستری (شکست
  پرووایدر هرگز امتداد محلی نمی‌سازد)، اعلان ایدمپوتنت به‌تفکیک تاریخ انقضا، نردبان انقضای
  تک‌پله‌ای (فعال → مهلت → بازخرید → آزاد) با غیرفعال‌سازی نگاشت مستأجر در آزادسازی، و تطبیق
  گزارشگر که اختلاف انقضا با رجیستری را فقط گزارش و ژورنال می‌کند.
- تست `DomainLifecycleTest` با ۵ سناریو؛ نتیجه: تست ۲۲۴۱/۵۷ · لینت ۳۰۲/۰ · اسموک زنده پاس.
- **فاز بعدی:** ۴۰ (همکاری در فروش).

## ثبت ادامهٔ جلسهٔ ۱۴۰۵/۰۶/۰۶ — فاز ۴۰ ✅

- همکاری در فروش سخت شد: خودارجاعی در هر سه دروازه (والد، ثبت کمیسیون، چسباندن کد)، ابطال
  بازپرداخت روی `pending` و `approved` (پرداخت‌شده دست نمی‌خورد و در گزارش آشکار می‌شود)، و
  `fraud_report()` فقط‌گزارشی با چهار سیگنال (خودارجاعی، خوشهٔ آی‌پی مشترک، نرخ ابطال بالای
  نصف، کمیسیون پرداخت‌شده روی سفارش مستردشده).
- تست `AffiliateHardenTest` با ۳ سناریو؛ نتیجه: تست ۲۲۵۱/۵۸ · لینت ۳۰۳/۰ · اسموک زنده پاس.
- **فاز بعدی:** ۴۱ (گیمیفیکیشن).

## ثبت ادامهٔ جلسهٔ ۱۴۰۵/۰۶/۰۶ — فاز ۴۱ ✅

- گیمیفیکیشن با دفتر امتیاز افزودنی ساخته شد (اسکیمای v32): ایدمپوتنتی رویدادها، سقف روزانهٔ
  سوءاستفاده، انقضای ردیفی، خرج‌کرد به‌صورت ردیف منفی (یک `SUM` همه را تطبیق می‌دهد)، خرید
  جایزهٔ ایدمپوتنت، مسابقهٔ فقط‌درآمدی و `reconcile()` گزارشگر.
- تست `GamificationTest` با ۵ سناریو؛ نتیجه: تست ۲۲۹۱/۵۹ · لینت ۳۰۴/۰ · اسموک زنده پاس.
- **فاز بعدی:** ۴۲ (آموزش و ویدیوی امن).

## ثبت ادامهٔ جلسهٔ ۱۴۰۵/۰۶/۰۶ — فاز ۴۲ ✅

- ویدیوی امن کامل شد: `verify_video_signature()` پس از تأیید امضا و مهلت، `can_watch()` را نیز
  می‌خواند — پیش‌نمایش آزاد باز، بقیهٔ درس‌ها نیازمند ثبت‌نام زنده؛ بازپرداخت یا پایان پنجرهٔ
  دسترسی لینک معتبر را همان لحظه می‌کشد. بقیهٔ اسکوپ (ثبت‌نام، پیشرفت، آزمون، گواهی، بازپرداخت)
  از قبل موجود بود و در تست قفل شد.
- تست `LmsVideoAccessTest` با ۵ سناریو؛ نتیجه: تست ۲۲۹۹/۶۰ · لینت ۳۰۵/۰ · اسموک زنده پاس.
- ⚠ احراز هویت GitHub در میانهٔ کار منقضی شد؛ کامیت‌های فاز ۴۱ و ۴۲ محلی‌اند و به محض اتصال باید
  پوش شوند.
- **فاز بعدی:** ۴۳ (هستهٔ لجستیک).

## ثبت ادامهٔ جلسهٔ ۱۴۰۵/۰۶/۰۶ — فاز ۴۳ ✅

- ماشین وضعیت تحویل قانون شد: جدول گذار صریح در `LogisticsService::TRANSITIONS`؛ تحویل فقط از
  «رسیده به مقصد»، تحویل به حامل فقط از پیش‌نویس، رسیدن فقط از پیک‌خورده/در مسیر، و وضعیت‌های
  پایانی بی‌گذار. هر چهار نقطهٔ تغییر وضعیت (حامل، مدیریت، پیک) از `can_transition()` عبور
  می‌کنند؛ مجوز نقش‌ها نیز ممیزی و مستند شد.
- تست `LogisticsStateMachineTest` با ۴ سناریو؛ نتیجه: تست ۲۳۳۴/۶۱ · لینت ۳۰۶/۰ · اسموک زنده پاس.
- ⚠ احراز هویت GitHub همچنان منقضی است؛ کامیت‌های فاز ۴۱، ۴۲ و ۴۳ محلی‌اند و به محض اتصال باید
  پوش شوند.

## ثبت ادامهٔ جلسهٔ ۱۴۰۵/۰۶/۰۶ — فاز ۴۴ ✅ و رفع مشکل پوش

- مدرک تحویل (`pod_ref`/`pod_at`) در اسکیمای v33 افزوده شد و چرخهٔ مالی پرداخت در محل با ماشین
  وضعیت هماهنگ شد: تسویهٔ نقدی و درون‌اپ فقط از وضعیت مجاز می‌گذرد، زودهنگام `bad_state` می‌گیرد
  و ردیف پرداخت نمی‌سازد؛ تسویهٔ موفق ردیف `ig_cod_payments` و مدرک تحویل ثبت می‌کند.
- تست `CourierCodTest` با ۴ سناریو؛ نتیجه: تست ۲۳۵۲/۶۲ · لینت ۳۰۷/۰ · اسموک زنده پاس.
- ✅ احراز هویت گیت‌هاب برگشت؛ کامیت‌های معوقٔ فازهای ۴۱ تا ۴۳ پوش شدند.
- **فاز بعدی:** ۴۵ (آداپتورهای لجستیک).

## ثبت ادامهٔ جلسهٔ ۱۴۰۵/۰۶/۰۶ — فاز ۴۵ ✅

- لایهٔ همگام‌سازی لجستیک ساخته شد: نگاشت واژگان پرووایدر به ماشین وضعیت (با آموزش فروشگاه)،
  کال‌بک امضاشدهٔ حامل، و `sync_tracking()` که خودش مکانیزم تلاش مجدد است؛ هدف وضعیت از
  کوتاه‌ترین مسیر قانونی پیمایش می‌شود و وضعیت پایانی عقب نمی‌رود.
- تست `ShippingSyncTest` با ۴ سناریو؛ نتیجه: تست ۲۳۷۶/۶۳ · لینت ۳۰۹/۰ · اسموک زنده پاس.
- **فاز بعدی:** ۴۶ (مارکت‌پلیس‌ها).

## ثبت ادامهٔ جلسهٔ ۱۴۰۵/۰۶/۰۶ — فاز ۴۶ ✅

- مارکت‌پلیس‌ها (اسکیمای ۳۴): انتشار ایدمپوتنت با هش کانونیکال، توقف پایانی روی تضاد ۴۰۹،
  تعویق با `Retry-After`/بک‌آف برای ۴۲۹ و خرابی‌ها، سقف پوش در هر تیک، و ثبت کامل دفترچهٔ
  لینک (`remote_id`/`payload_hash`/`remote_rev`).
- تعیین رسمی بودن: باسام دارای پلتفرم رسمی توسعه‌دهنده؛ دیجی‌کالا و دیوار بدون درگاه رسمی ثبت
  محصول → کانال‌ها فقط با واسطهٔ مورد اعتماد، بدون اسکرپ.
- تست `MarketplaceSyncTest` با ۷ سناریو؛ نتیجه: تست ۲۴۰۱/۶۴ · لینت ۳۱۰/۰ · اسموک زنده پاس.
- **فاز بعدی:** ۴۷ (سئو و تبلیغات).

## ثبت ادامهٔ جلسهٔ ۱۴۰۵/۰۶/۰۶ — فاز ۴۷ ✅

- سئو و تبلیغات (اسکیمای ۳۵، ۸۱ جدول): دادهٔ ساختاریافتهٔ محصول با قاعدهٔ برابری محتوا،
  تگ هد/کانونیکال یکتا، نقشهٔ سایت کران‌دار محصولات قابل‌فروش، چرخهٔ تأیید یک‌طرفهٔ کمپین‌ها،
  کنترل هزینهٔ پیش‌از‌خرج و سد روزانهٔ محتوای انبوه.
- تست `SeoAdsTest` با ۶ سناریو؛ نتیجه: تست ۲۴۴۹/۶۵ · لینت ۳۱۷/۰ · اسموک زنده پاس.
- **فاز بعدی:** ۴۸ (ترجمهٔ دامنه‌ای و تجارت بین‌المللی).

## ثبت ادامهٔ جلسهٔ ۱۴۰۵/۰۶/۰۶ — فاز ۴۸ ✅

- ترجمه و تجارت بین‌المللی (اسکیمای ۳۶، ۸۴ جدول): حافظهٔ ترجمهٔ tenant-scoped با تطبیق دقیق،
  واژه‌نامهٔ قفل‌شده با جای‌دار، اعتبارسنجی ارز/منطقهٔ زمانی، دفترچهٔ رضایت تاریخ‌دار و ثبت‌کنندهٔ
  قابلیت درگاه‌های بین‌المللی (بدون اختراع پرووایدر؛ تأیید زنده در `PV-INTL-*`).
- تست `IntlCommerceTest` با ۶ سناریو؛ نتیجه: تست ۲۴۸۸/۶۶ · لینت ۳۲۲/۰ · اسموک زنده پاس.
- **فاز بعدی:** فازهای `PV-*` (تأیید زندهٔ پرووایدرها) — نیازمند اعتبارنامهٔ واقعی از کارفرما.

## ثبت ادامهٔ جلسهٔ ۱۴۰۵/۰۶/۰۶ — فاز ۴۹ ✅

- اتصال Zernio و نگاشت پروفایل (اسکیمای ۳۷، ۸۵ جدول، طبق بند ۵ سند `ADR-0004`): یک پروفایل
  برای هر فروشگاه، کلید مرکزی در سکوی رازها، کلیدهای فروشگاه رمزنگاری‌شده و چرخش/لغو سالم،
  نگاشت تصمیم‌گیر بک‌اند، هویت وب‌هوک زمان‌دار با راز اختصاصی و حذف کامل داده.
- تب «Zernio» با فیلد کلید مرکزی به فرم تنظیمات افزوده شد (درفت‌گارد رازها قفل‌شده با فرم).
- تست `ZernioConnectTest` با ۵ سناریو؛ نتیجه: تست ۲۵۲۹/۶۷ · لینت ۳۲۶/۰ · اسموک زنده پاس.
- **فاز بعدی:** ۵۰ (مهاجرت به تنها provider اجتماعی و حذف کد قدیم).
