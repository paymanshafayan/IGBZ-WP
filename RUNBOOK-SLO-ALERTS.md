# Runbook — SLO، هشدار و بهره‌برداری worker/cron (فاز ۷۱)

این سند برای بهره‌بردار فروشگاه است: چه چیزی پایش می‌شود، آستانه‌ها کدام‌اند،
وقتی قرمز شد چه کنید. همهٔ داده‌ها از جداول خود محصول می‌آیند
(`wp_igbz_jobs` و `wp_igbz_logs`) — هیچ سرویس بیرونی لازم نیست.

## ۱) نمای کلی

| بخش | کجاست | چه می‌دهد |
|---|---|---|
| SLO پنل | پیشخوان ← IGBZ ← وضعیت، بالای صفحه | سبز/قرمز + جدول سنجه‌های ۲۴ ساعت گذشته |
| سلامت ترافیک | `/?igbz_health=1` | 200/503 + پرچم degraded (فاز ۷۰) |
| لاگ ساختاریافته | جدول `igbz_logs` + صفحهٔ وضعیت، بخش «logs» | هر خط دارای `request_id` (فاز ۷۱) |
| صف کارها | `wp igbz jobs stats` | عمق صف، مرده‌ها، تلاش‌ها |

## ۲) سنجه‌ها و آستانه‌ها

آستانه‌ها تنظیمات‌اند (`slo.*`) و بدون استقرارِ دوباره قابل تنظیم‌اند؛
مقادیر پیش‌فرض در `src/Support/Observability/Slo.php` (`Slo::DEFAULTS`).

| سنجه | تعریف | پیش‌فرض | کلید تنظیم |
|---|---|---|---|
| نرخ موفقیت کارها | سهم `done` از کارهای تمام‌شده در ۲۴ ساعت | ≥ ۹۸٪ | `slo.job_success_rate` |
| عمق صف | کارهای `pending` سرِرسیده | ≤ ۵۰ | `slo.max_pending` |
| تأخیر صف | عمر قدیمی‌ترین کار سرِرسیده | ≤ ۱۵ دقیقه | `slo.max_wait_minutes` |
| خطاها | سطرهای `error` در ۲۴ ساعت | ≤ ۲۵ | `slo.max_errors_24h` |

تنظیم نمونه (WP-CLI):

```bash
wp option patch update igbz_settings slo.max_errors_24h 50 --format=json
```

نکتهٔ مهم: کارهای «آینده» (delay دار) در تأخیر صف حساب نمی‌شوند — صف فقط
وقتی عقب است که کارِ سرِرسیده بی‌راننده مانده باشد.

## ۳) اقدام در هر نقض

هر نقض در پنل، نام اقدام (`action`) را هم می‌آورد:

- **`jobs-failures`** (نرخ موفقیت < آستانه):
  1. `wp igbz jobs dead --format=json` — ببین کدام `job_type` مُرده.
  2. آخرین خطا در ستون `last_error` همان ردیف.
  3. لاگ همان کار: `wp db query "SELECT * FROM wp_igbz_logs WHERE context LIKE '%<trace_id>%' ORDER BY id DESC LIMIT 20"` — `request_id` درون `context` است.
  4. رفع علت (معمولاً یک ادغام بیرونی قطع است) سپس `wp igbz jobs replay <id>`.
- **`queue-backlog`** (عمق صف > آستانه):
  1. `wp igbz jobs stats` — صف کدام نوع پر است.
  2. یک ران دستی: `wp igbz jobs drain` — اگر پایش سرور (بخش ۴) روشن است
     و باز انباشته می‌شود، workerِ سرور نمرده بلکه کند است؛
     بودجهٔ زمانی `QueueRunner::DEFAULT_TIME_BUDGET` (۲۴۰ث) را در
     `_devenv` بالا ببرید و علت کندی (معمولاً ادغام خارجی با timeout) را بگیرید.
- **`queue-stalled`** (تأخیر > آستانه): ضربان پنج‌دقیقه‌ای اجرا نمی‌شود.
  1. `wp cron event list` — رویداد `igbz_cron_five_minutes` سرِرسیده اما نرانده؟
  2. اگر `DISABLE_WP_CRON` فعال است، cron سرور را چک کنید (بخش ۴)؛
     در ریل‌وی: لاگ دیپلوی، خط `igbz: cron beat failed`.
  3. ران اضطراری: `wp cron event run igbz_cron_five_minutes`.
- **`error-storm`** (خطای ۲۴ ساعت > آستانه):
  1. صفحهٔ وضعیت، بخش logs — گروه‌بندی بر اساس `channel`.
  2. یک کانال غالب = یک زیرسیستم مشکل دارد؛ با `request_id` همان ردیف‌ها
     زنجیرهٔ درخواست/کار را دنبال کنید (بخش ۵).

## ۴) worker/cron سرور (استقرار ابری)

در `railway/entrypoint.sh`، با متغیر `IGBZ_SERVER_CRON=1` یک حلقهٔ پس‌زمینه
هر ۶۰ ثانیه `wp cron event run --due` و `wp igbz jobs drain` را می‌راند.

- `DISABLE_WP_CRON` عمداً **تنها با تصمیم صریح بهره‌بردار** فعال شود
  (پیش‌فرض: خاموش؛ دو راننده موازی برای رویدادهای idempotent بی‌ضرر است و
  حذف رانندهٔ وب بدون اطمینان از رانندهٔ سرور، سایت را ساکت می‌کشد).
  فعال‌سازی: `wp config set DISABLE_WP_CRON true --raw`
- صفحهٔ وضعیت اگر `DISABLE_WP_CRON` ببیند و رانندهٔ سرور روشن نباشد،
  هشدار زرد می‌دهد (این رفتار از قبل بود؛ با بخش ۴ هم‌خوان است).

## ۵) ردیابی درخواست (trace)

از فاز ۷۱ هر سطر لاگ `request_id` دارد (۳۲ نویسهٔ hex، ساخت `Crypto::token`).
کارهای صف هنگام enqueue همان trace درخواستِ ایجادکننده را در پاکت
(envelope) خود می‌برند و worker کار را زیر همان trace اجرا می‌کند — یعنی
«سفارش فلان ساعت ۱۴:۰۳ شکست خورد» یکی-دو grep حل می‌شود:

```bash
wp db query "SELECT id,level,channel,message FROM wp_igbz_logs \
  WHERE context LIKE '%<request_id>%' ORDER BY id"
```

## ۶) هشدار خودکار (اختیاری، پیشنهادی)

پنل SLO pull-based است؛ برای push، توصیهٔ ثبت‌شده: یک رویداد ساعته در
`Cron::HOOK_HOURLY` که `Slo::report()` را صدا بزند و در نقض، از `wp_mail`
به ایمیل بهره‌بردار خبر دهد (نرخ هشدار: حداکثر یک ایمیل در ساعت، ارسال فقط
در تغییر وضعیت سبز→قرمز). این قابلیت هنوز پیاده نشده — تا درخواست صریح
کارفرما کد نمی‌شود (قاعدهٔ §۰.۷).
