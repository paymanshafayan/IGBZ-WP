# فاز ۹ — مارکت‌پلیس‌ها و انتشار خودکار محتوا: طراحی

**وضعیت:** طراحی تأییدشده توسط کارفرما. پلتفرم‌ها: ترب، دیجی‌کالا، دیوار (کنار دیوار) و
باسلام (سلام API) — با نقش محوری «پادو» در ثبت، مدیریت و ارتقای جایگاه.

> **قانون حاکم:** بدون Graph API اینستاگرام/متا. «انتشار خودکار به باسلام» یعنی خروجی محتوای
> تولیدشده (توسط Manus/پادو) به‌جای یک مقصد، به چند مقصد می‌رود.

---

## ۱. پلتفرم‌ها و روش اتصال

| پلتفرم | روش | وضعیت در کد |
|---|---|---|
| **ترب** | خروجی JSON داینامیک (فید) — ربات ترب خودش می‌خواند | ✅ موجود (`MarketplaceFeeds`) |
| **دیجی‌کالا** | Open API (توکن فروشنده) — POST/PUT محصول، همگام قیمت/موجودی | 🟡 آداپتور HTTP ساخته شد؛ اتصال واقعی با کلید |
| **دیوار** | **کنار دیوار (Kenar API)** — OAuth + API آگهی/فروشگاه | 🟡 آداپتور HTTP ساخته شد؛ تنظیم OAuth |
| **باسلام** | **سلام API** (`developers.basalam.com`) — محصول، پرداخت، اشتراک، تبلیغ، **محتوا (استوری/پست/ویدیو)** | 🟡 آداپتور جدید (پایین) |

---

## ۲. انتشار خودکار محتوا به باسلام (هستهٔ جدید)

### ۲.۱ چرا
باسلام امکانات شبیه اینستاگرام دارد (استوری، پست، عکس/فیلم محصول). پس:

> **هر محتوایی که برای اینستاگرام ساخته می‌شود (پست/استوری/ریلز)، اگر «انتشار به باسلام» فعال
> باشد، به‌صورت خودکار برای باسلام هم ارسال می‌شود** (از طریق سلام API).

### ۲.۲ جریان
```
محتوای تولیدشده (Manus/پادو: عکس، کپشن، ویدیو، استوری)
  → مقصد اینستاگرام (مسیر موجود)
  → اگر basalam.enabled باشد: همان خروجی → سلام API → پست/استوری در غرفهٔ باسلام
  → ثبت وضعیت انتشار در ig_content (ستون/متا: basalam_status)
```

### ۲.۳ آداپتور باسلام
- `BasalamAdapter` (پیاده‌سازی `MarketplaceAdapterInterface` یا `ContentPublisherInterface`):
  - محصول: ساخت/به‌روزرسانی (نام، قیمت، موجودی، دسته، تصاویر)
  - محتوا: انتشار پست/استوری/ویدیو (از خروجی آماده)
  - تأیید: کال‌بک/وضعیت از سلام API
- کلیدها: `basalam.api_key`، `basalam.base_url` (https://api.basalam.com)، `basalam.gharhe_id`
- تنظیم: `basalam.enabled` (پیش‌فرض خاموش — فقط وقتی ادمین وصل کند)

---

## ۳. نقش پادو (در تمام مراحل)

### ۳.۱ در ثبت (Onboarding)
- **راهنمایی گام‌به‌گام** برای هر پلتفرم (ثبت‌نام، گرفتن توکن/کلید، OAuth دیوار).
- **اتوماسیون:** پر کردن فرم‌ها، نگاشت دسته (`ig_category_mapping` موجود)، آپلود محصولات،
  پیگیری وضعیت تأیید.
- چک‌لیست «آمادگی پلتفرم» برای هر فروشگاه.

### ۳.۲ مدیریت مستمر (پس از ثبت)
| اقدام | شرح |
|---|---|
| همگام‌سازی خودکار | قیمت/موجودی/محصول جدید → همهٔ پلتفرم‌ها (صف `ig_marketplace_sync` موجود) |
| به‌روزرسانی لیستینگ | عنوان/توضیح/تصویر بهینه برای هر پلتفرم |
| پاسخ به نظرات | رصد نظرات مشتریان در هر پلتفرم + پاسخ هوشمند (از طریق API هر پلتفرم) |
| تحلیل و گزارش | گزارش وضعیت هر پلتفرم (تعداد محصول، فروش، رتبه) |

### ۳.۳ ارتقای جایگاه (اقدام مستمر)
| اقدام | شرح |
|---|---|
| **قیمت‌گذاری رقابتی** | مقایسهٔ قیمت با رقبا (از دادهٔ پلتفرم) + پیشنهاد قیمت در محدودهٔ مجاز |
| **سئوی محصول** | بهینه‌سازی عنوان/کلمات کلیدی برای جستجوی هر پلتفرم |
| **مدیریت موجودی** | جلوگیری از «ناموجود» (اولویت با پلتفرم‌های پرفروش) |
| **برنامهٔ محتوا** | انتشار منظم استوری/پست در باسلام برای دیده‌شدن بیشتر |
| **رصد رتبه** | پیگیری جایگاه محصول در جستجوی هر پلتفرم + پیشنهاد اقدام بعدی |

> پادو این‌ها را به‌صورت **دوره‌ای (کرون) + در صورت نیاز** انجام می‌دهد، و هر اقدام را
> برای تأیید/گزارش به ادمین می‌دهد (الگوی «پلن ← تأیید ← اجرا ← گزارش»).

---

## ۴. جدول‌ها/تنظیمات
| مورد | محتوا |
|---|---|
| `basalam.enabled/api_key/base_url/gharhe_id` | تنظیمات باسلام |
| `ig_content` | + متا/ستون `basalam_status` (در صورت نیاز مهاجرت) |
| `ig_marketplace_sync.marketplace` | + مقدار `basalam` |
| `ig_category_mapping.marketplace` | + مقدار `basalam` و `divar` (کنار) |

---

## ۵. اندپوینت‌ها (افزوده)
```
GET/POST /igbz/v1/admin/marketplaces/basalam/connect   (اتصال/وضعیت)
POST     /igbz/v1/admin/marketplaces/basalam/sync      (همگام‌سازی دستی)
POST     /igbz/v1/admin/marketplaces/basalam/content   (انتشار محتوا به باسلام)
GET      /igbz/v1/admin/marketplaces/basalam/status    (وضعیت/رتبه)
```

---

## ۶. فازبندی پیاده‌سازی (پس از تأیید)
| گام | محتوا |
|---|---|
| ۱ | آداپتور باسلام (محصول + محتوا) + تنظیمات |
| ۲ | انتشار خودکار اینستاگرام → باسلام (سوییچ `basalam.enabled`) |
| ۳ | دیوار از طریق کنار (OAuth + آگهی/فروشگاه) |
| ۴ | پادو: راهنمای ثبت + مدیریت مستمر + ارتقای جایگاه (کرون + پیشنهادها) |
| ۵ | تست‌ها + تأیید زنده |
