# نقشهٔ تکمیل اپ‌های فلاتر — طراحی

**وضعیت:** طراحی تأییدشده توسط کارفرما. مبنای کار: اپ‌های فلاتر موجود در
`IGBZ-project-full.zip` (ادمین + مشتری) — **کامل می‌شوند، نه بازنویسی** (ثبت در
PROJECT-STATE). اپ پیک و وباپ بر اساس همین ساختار ساخته می‌شوند.

> ## ⏸️ معلق تا تکمیل بک‌اند و فرانت‌اند — تصمیم کارفرما (۱۴۰۵/۰۵/۳۱)
>
> > «طراحی اپلیکیشن‌های موبایل باید بمونه برای **بعد از طراحی کامل بک‌اند و فرانت‌اند**،
> > تا از دوباره‌کاری جلوگیری کنه. در زمانش هم فایل کامل اپلیکیشن رو ارسال می‌کنم.»
>
> **دلیل:** اپ مصرف‌کنندهٔ قرارداد سرور است. اگر پیش از تثبیت بک‌اند طراحی شود، هر تغییر
> در سرور یک بازنویسی در اپ می‌سازد.
>
> **پیامدها:**
>
> ۱. **این سند دست‌نخورده می‌ماند** و مرجع باقی است، ولی رویش کار نمی‌شود.
> ۲. **فایل اپ‌ها را کارفرما در زمان خودش می‌فرستد** — پس نیازی به دسترسی ریپوی جداگانه
>    نیست و آن سؤال منتفی است.
> ۳. ⚠️ **قید مهم:** الزام امنیتی `DESIGN-LEGAL-AUTH.md §۷.۶` (امضای بیومتریک + اطلاعات
>    دستگاه + کد یکتای اپ برای خروجی گروهی) **سمت اپ** پیاده می‌شود. چون طراحی اپ معلق
>    است، سند امنیت باید **قرارداد سمت سرور** را کامل و صریح تعریف کند — شکل امضا،
>    هدرها، و آنچه سرور انتظار دارد — تا وقتی نوبت اپ رسید، اپ فقط پیروی کند. سرور
>    نباید منتظر اپ بماند.
> ۴. ترتیب کلی کار در `PROJECT-STATE.md §۵.۵` ثبت است: اول امنیت و مدیریت درخواست‌ها،
>    بعد بقیه، و اپ‌ها **بعد از تکمیل بک‌اند و فرانت‌اند**.

> **قانون:** هر اپ باید نسخهٔ وب‌اپ (PWA) هم داشته باشد (Flutter Web — یک کدبیس، دو خروجی).

---

## ۱. اپ ادمین (`03-instagram_store_admin_flutter`) — اضافه‌ها

### ۱.۱ AI Studio (زیرمجموعهٔ پادو)
- صفحهٔ «AI Studio» در اپ ادمین: تولید محتوا (تصویر استودیویی، حذف پس‌زمینه، ویدیوی کوتاه،
  TTS فارسی، عکس مدل) — متصل به REST `/igbz/v1/...` (و سمت سرور `AiStudioService` موجود).
- خروجی به مدیالایبری/محصول.

### ۱.۲ گام‌های ۱–۶ ثبت محصول
- UI گام‌به‌گام در اپ ادمین، متصل به REST موجود (`/intake/*` — ۱۴ مسیر):
  1. عکاسی/انتخاب عکس
  2. ارسال عکس (ارزیابی)
  3. نمایش نتیجهٔ ارزیابی (رد با دلایل / قبول)
  4. آماده‌سازی تصویر (نمایش پیش‌نمایش)
  5. ویرایش اختیاری
  6. توضیح محصول (متن/صدا) + قیمت/موجودی/دسته

---

## ۲. اپ مشتری (`04-nopcommerce_flutter_app_customers`) — اضافه‌ها

### ۲.۱ بخش آموزش کامل (LMS — فاز ۱۳)
| بخش | محتوا |
|---|---|
| دوره‌ها | فهرست + جستجو + صفحهٔ دوره (سرفصل‌ها) + ثبت‌نام/خرید |
| درس/ویدیو | پخش با **لینک امضاشدهٔ VOD** (HMAC + انقضا + IP — `LmsVodService`) |
| امنیت ویدیو | **FLAG_SECURE** (اندروید) + **isCaptured** (iOS) + **واترمارک پویا** (موبایل ماسک‌شده) + ضد شبیه‌ساز |
| پیشرفت | نمایش پیشرفت + علامت‌گذاری درس‌ها |
| آزمون | شرکت در آزمون + نتیجه |
| گواهی | مشاهده/دریافت گواهی |
| ضمیمه | دانلود PDF/فایل (در صورت مجاز) |

> توجه: پلیرهای ویدیو/صدا/کتابِ موجود مربوط به «محصولات دیجیتال» فروشگاه‌اند؛ بخش آموزش
> **جداست** و کامل پیاده می‌شود (هرچند از همان کامپوننت‌های پلیر می‌تواند استفاده کند).

### ۲.۲ فاز ۷ — رصد و ارتباط
- مشاهدهٔ وضعیت مرسوله
- **رصد زندهٔ موقعیت روی نقشه** (بعد از «تحویل به پیک»)
- **چت با پیک** + (COD) پرداخت در لحظه با اپ

### ۲.۳ کانال VIP — آیکون ذخیره و نسخهٔ آفلاین (طبق `DESIGN-VIP-EXPIRY.md`)

پست VIP هفت روز پس از انتشار **از سرور حذف می‌شود**، و چیزی که به مشتری وعده داده‌ایم این است
که می‌تواند نسخهٔ خودش را نگه دارد. سمت سرور آماده است؛ این کارها در اپ مانده:

| بخش | کار |
|---|---|
| آیکون ذخیره | روی هر پست فید و صفحهٔ پست → `POST /igbz/v1/vip/posts/{id}/save` |
| نسخهٔ آفلاین | بلافاصله پس از ذخیره → `GET /igbz/v1/vip/posts/{id}/offline` و دانلود فایل‌ها |
| محل نگهداری | **حافظهٔ خصوصی اپ** (نه گالری، نه فایل‌منیجر) و **بدون دکمهٔ اشتراک‌گذاری** |
| فهرست ذخیره‌ها | `GET /igbz/v1/vip/saved` — پستی که پاک شده هم برمی‌گردد (`locked`, `media_count: 0`) تا اپ بگوید «فقط نسخهٔ خودت مانده» |
| شمارش معکوس | `expires_at`/`retention_days`/`expiry_notice` از همان پاسخ می‌آید — متن را در اپ بازنویسی نکنید |

> **مرز امنیتی:** این مسیر آفلاین فقط برای VIP است (امنیت سبک). بخش آموزش (LMS) نسخهٔ آفلاین
> ندارد و نباید پیدا کند: آنجا `FLAG_SECURE`، `isCaptured`، واترمارک و لینک مقید به دستگاه است.

---

## ۳. اپ پیک (جدید — بر اساس ساختار موجود)
- مسیریابی ترتیبی + «به مقصد رسیدم»
- اسکن بارکد (شناسایی بسته)
- PIN مشتری + تأیید تحویل
- COD کامل (اپ مشتری / نقدی / درگاه / کارت‌به‌کارت)
- تماس + چت با مشتری
- ارسال موقعیت زنده (برای رصد مشتری)

---

## ۴. وب‌اپ (PWA) برای هر سه
- هر اپ (ادمین، مشتری، پیک) با **Flutter Web** به‌صورت وب‌اپ منتشر می‌شود (یک کدبیس).

---

## ۵. ترتیب پیشنهادی پیاده‌سازی
| گام | محتوا |
|---|---|
| ۱ | بخش آموزش (LMS) در اپ مشتری + امنیت ویدیو |
| ۲ | فاز ۷ در اپ مشتری (رصد/چت) + اپ پیک |
| ۳ | AI Studio + گام‌های ۱–۶ ثبت محصول در اپ ادمین |
| ۴ | وب‌اپ (PWA) هر سه اپ |
| ۵ | تست + تأیید زنده |
