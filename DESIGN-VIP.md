# کانال VIP — طراحی

نسخهٔ ۲. بازنویسی‌شده پس از تصمیم‌های شما: امنیت سبک، حفظ شکل پست اینستاگرام،
داشبورد مدیر، انقضای پست، و صفحهٔ فرود برای دکمهٔ شیر.

> **تطبیق ۱۴۰۵/۰۶/۰۶:** تولید تیزر با پادو و انتشار آن فقط از Zernio انجام می‌شود. هر
> اشارهٔ تاریخی به Manus در این سند با ADR-0004 جانشین شده است؛ WooCommerce/IGBZ مرجع
> entitlement و Zernio صرفاً درگاه اجتماعی است.

> **وضعیت: پیاده‌سازی شده و تحویل داده شده** — کامیت `e9ed315` (نسخهٔ ۱۰ دیتابیس، نه جدول
> `vip_*`، هشت سرویس، صفحهٔ مدیر با پنج تب، ۲۹ مسیر REST، ۲۱ سناریوی تست). این سند طراحی
> است و به‌عنوان مرجع «چرا این‌طور ساخته شد» نگه داشته می‌شود. برای وضعیت جاری پروژه
> [`PROJECT-STATE.md`](PROJECT-STATE.md) را ببینید.
>
> از تصمیم‌های باز بخش ۱۲، **تنها سیاست انقضا هنوز رأی شما را می‌خواهد** (موارد ۱ و ۲).
> مورد ۳ با آداپتور و شروع از سرور محلی، و مورد ۴ با انتشار فوری به‌همراه امکان مخفی‌سازی
> پیاده شد.

---

## ۱. ایده در یک جمله

**یک اینستاگرام کوچک و خصوصی داخل اپ خودمان** که جای Close Friends را می‌گیرد —
با این تفاوت که عضویتش خودکار و پولی است.

Close Friends واقعی سه مشکل دارد که این طرح هر سه را حل می‌کند:

| مشکل Close Friends | راه‌حل ما |
|---|---|
| فهرست دستی است، API ندارد | هرکس اشتراک بخرد خودکار عضو می‌شود |
| پولی‌سازی متا در ایران نیست | درگاه‌های ایرانی موجود |
| رابطهٔ مشتری در اختیار متاست | مشتری در پایگاه دادهٔ ماست |

مسیرهایی که بررسی و **رد** شدند: خواندن پست Close Friends از اینستاگرام (دکمهٔ شیر
روی آن محتوا مسدود است)، امبد کردنشان (oEmbed فقط عمومی، و از آوریل ۲۰۲۵ فقط با
Graph API)، و اشتراک پولی خود اینستاگرام (در ایران نیست + وابستگی به متا).

---

## ۲. تصحیح مهم: دو سطح امنیت متفاوت

در نسخهٔ اول همه‌جا امنیت سنگین گذاشته بودم. اشتباه بود.

| بخش | امنیت | چرا |
|---|---|---|
| **دوره‌های آموزشی** (LMS) | سنگین: `FLAG_SECURE`، `isCaptured`، ضدشبیه‌ساز، لینک گره‌خورده به دستگاه، واترمارک | محصول گران، سرقتش ضرر مستقیم دارد |
| **کانال VIP** | سبک: در حد Close Friends | محتوای جاری و کوتاه‌عمر |

امنیت کانال VIP یعنی دقیقاً همان چیزی که اینستاگرام می‌دهد:

- ✅ فقط مشترک محتوا را می‌بیند (کنترل دسترسی سمت سرور)
- ✅ لینک محتوا موقتی است و لو نمی‌رود
- ✅ دکمهٔ اشتراک‌گذاری و دانلود در اپ وجود ندارد
- ❌ جلوی اسکرین‌شات گرفته **نمی‌شود** — مثل خود اینستاگرام

این تصمیم درستی است: `FLAG_SECURE` روی فید، تجربهٔ کاربر را خراب می‌کند (تمام صفحه
در سوییچر اپ‌ها سیاه می‌شود) و برای محتوایی که یک هفته بعد پاک می‌شود، هزینه‌اش
بیشتر از فایده است.

---

## ۳. حفظ شکل پست اینستاگرام

مشتری باید حس کند در حال دیدن اینستاگرام است. یعنی هر پست باید این‌ها را داشته باشد:

- کاروسل چندتصویری با اسلاید افقی
- ویدیو با پخش خودکار و بی‌صدا (تپ برای صدا)
- کپشن با «بیشتر»، هشتگ‌ها، منشن
- **لایک** با انیمیشن دوبار-تپ
- **کامنت** با پاسخ به کامنت
- **ذخیره** (بوکمارک)
- شمارندهٔ بازدید
- **ارسال دایرکت به مدیر** از داخل پست

سه مورد آخرِ فهرست بالا در طرح نسخهٔ اول اصلاً نبود. این لایهٔ اجتماعی، بخش
سنگین کار است و سه جدول جدا می‌خواهد.

---

## ۴. جدول‌ها (DB v10)

هفت جدول جدید طراحی شد؛ پیاده‌سازی نهایی با **نه جدول** انجام شد (`vip_threads` و
`vip_messages` که پایین همین بخش آمده‌اند هم اضافه شدند). با پیشوند `vip_` تا از جدول‌های
`ig_*` (اینستاگرام واقعی) جدا بماند.

### `vip_plans` — طرح‌های اشتراک
```
id, tenant_id, slug, name, description,
price DECIMAL(18,4), currency VARCHAR(8),
duration_days INT,
is_active, sort_order, created_at, updated_at
UNIQUE KEY tenant_slug (tenant_id, slug)
```

### `vip_memberships` — اشتراک فعال مشتری
```
id, tenant_id, user_id, plan_id,
status VARCHAR(20),        -- active | expired | cancelled | refunded
starts_at, ends_at, payment_id, auto_renew,
created_at, updated_at
KEY user_active (user_id, status, ends_at)
```

⚠ عمداً از جدول‌های `plans`/`subscriptions` موجود استفاده **نشد**: آن‌ها به
`tenant_id` وصل‌اند و اشتراکِ *صاحب فروشگاه* در سامانهٔ ما را مدل می‌کنند. استفادهٔ
دوگانه، «فروشگاه اشتراکش تمام شد» را با «مشتری اشتراکش تمام شد» قاطی می‌کرد.

### `vip_posts` — خودِ پست
```
id, tenant_id, account_id, author_id,
kind VARCHAR(20),          -- image | carousel | video | text
caption LONGTEXT, media LONGTEXT,   -- JSON: [{type,url,thumb,w,h,duration}]
teaser_content_id BIGINT,  -- ارجاع به ig_content (تیزر عمومی)
product_id BIGINT,
access VARCHAR(20),        -- members | purchase | free
price DECIMAL(18,4),
status VARCHAR(20),        -- draft | scheduled | published | expired | deleted
publish_at, published_at,
expires_at DATETIME NULL,  -- NULL = بدون انقضا
expiry_action VARCHAR(20), -- hide | delete
likes_count, comments_count, views_count,
created_at, updated_at
KEY feed (tenant_id, status, published_at)
KEY expiry (status, expires_at)
```

### `vip_post_likes`
```
id, post_id, user_id, created_at
UNIQUE KEY post_user (post_id, user_id)
```

### `vip_post_comments`
```
id, tenant_id, post_id, user_id,
parent_id BIGINT DEFAULT 0,   -- پاسخ به کامنت
body TEXT,
status VARCHAR(20),           -- visible | hidden | deleted
is_pinned, likes_count,
created_at, updated_at
KEY post_visible (post_id, status, id)
```

### `vip_post_views`
```
id, post_id, user_id, viewed_at, seconds_watched
UNIQUE KEY post_user (post_id, user_id)
```

### `vip_entitlements` — خرید تکی
```
id, tenant_id, user_id, post_id,
source VARCHAR(20),        -- purchase | membership | gift
payment_id, expires_at, revoked_at,
created_at, updated_at
UNIQUE KEY user_post (user_id, post_id)
```

### `vip_threads` و `vip_messages` — دایرکت داخل اپ
```
vip_threads:  id, tenant_id, user_id, subject, last_message_at,
              unread_admin, unread_user, status, created_at, updated_at
vip_messages: id, thread_id, sender_type (user|admin), sender_id,
              body TEXT, attachment, post_id, read_at, created_at
```

دایرکت داخل اپ خودمان است، نه دایرکت اینستاگرام. یعنی **پنجرهٔ ۲۴ ساعتهٔ متا اینجا
وجود ندارد** — مدیر هر وقت بخواهد می‌تواند به مشتری پیام بدهد. این یکی از
بزرگ‌ترین مزیت‌های عملی کل طرح است.

---

## ۵. انقضای پست — تصمیم لازم

شما گفتید پست‌ها باید منقضی شوند. دو ستون گذاشته‌ام: `expires_at` و `expiry_action`.

**پیشنهاد من: `hide` به‌جای `delete` به‌عنوان پیش‌فرض.**

دلیل: اگر رکورد پاک شود، کامنت‌ها و آمار هم می‌روند و مدیر دیگر نمی‌فهمد آن پست چه
عملکردی داشت. با `hide` پست از فید مشتری ناپدید می‌شود ولی در داشبورد مدیر می‌ماند.
فایل مدیا را می‌توان جدا پاک کرد تا فضا آزاد شود.

گزینه‌های مدت: ۱ روز (استوری‌وار) · ۷ روز · ۳۰ روز · بدون انقضا. پیش‌فرض قابل تنظیم
در تنظیمات، و قابل تغییر برای هر پست.

---

## ۶. صفحهٔ فرود دکمهٔ شیر

وقتی کاربر تیزر عمومی را share می‌کند و اپ ما را انتخاب می‌کند (یا لینک را در مرورگر
باز می‌کند)، به صفحه‌ای می‌رسد با:

1. پیش‌نمایش تیزر
2. **دو دکمه:** «خرید اشتراک» یا «خرید همین پست»
3. **لینک دانلود اپ** (کافه‌بازار، مایکت، دانلود مستقیم APK، اپ‌استور)
4. توضیح اینکه محتوا کجاست و چطور دیده می‌شود
5. اگر پست عمومی باشد: **دکمهٔ حمایت مالی**

این صفحه در وردپرس رندر می‌شود (نه اپ)، چون باید در مرورگر و بدون نصب اپ باز شود.
آدرس: `/vip/p/{shortcode}`.

اگر اپ نصب باشد، deep link مستقیم به همان پست در اپ می‌رود؛ اگر نه، همین صفحهٔ وب.

---

## ۷. حمایت مالی روی پست عمومی

یک مسیر ساده و جدا از VIP: کاربر روی پست عمومی share می‌زند → صفحهٔ فرود → «حمایت
مالی» → مبلغ دلخواه یا مبالغ پیشنهادی → درگاه.

از `payments` موجود با `purpose = vip_tip` استفاده می‌شود. رکورد در
`vip_entitlements` ساخته نمی‌شود چون دسترسی‌ای نمی‌خرد.

---

## ۸. داشبورد مدیر در اپ

مدیر باید صفحهٔ VIP خود را مثل پیج اینستاگرام بگرداند:

- **فید:** فهرست پست‌ها با آمار (لایک، کامنت، بازدید، درآمد)
- **ساخت پست:** آپلود مدیا، کپشن، زمان‌بندی، انقضا، قیمت
- **کامنت‌ها:** پاسخ، پین، مخفی، حذف
- **دایرکت:** صندوق پیام با شمارندهٔ خوانده‌نشده
- **اعضا:** فهرست مشترکان، تاریخ انقضا، درآمد
- **آمار:** رشد اعضا، درآمد ماهانه، پست‌های برتر

تمام این‌ها از REST می‌آید تا اپ فلاتر مصرفشان کند؛ پنل وردپرس هم همان سرویس‌ها را
صدا می‌زند.

---

## ۹. اندپوینت‌ها (`igbz/v1`)

**مشتری:**
```
GET  /vip/feed                      فید پست‌ها (قفل‌شده‌ها بدون مدیا)
GET  /vip/posts/{id}                یک پست
POST /vip/posts/{id}/like           لایک / برداشتن لایک
GET  /vip/posts/{id}/comments       کامنت‌ها
POST /vip/posts/{id}/comments       کامنت جدید
POST /vip/posts/{id}/view           ثبت بازدید
GET  /vip/posts/{id}/media          لینک موقت مدیا
GET  /vip/plans                     طرح‌ها
POST /vip/subscribe                 خرید اشتراک
POST /vip/posts/{id}/purchase       خرید تکی
GET  /vip/membership                وضعیت اشتراک من
GET  /vip/threads                   دایرکت‌های من
POST /vip/threads/{id}/messages     ارسال پیام
POST /vip/tip                       حمایت مالی
```

**مدیر:**
```
GET|POST   /vip/admin/posts         فهرست / ساخت
PUT|DELETE /vip/admin/posts/{id}    ویرایش / حذف
GET        /vip/admin/comments      همهٔ کامنت‌ها
POST       /vip/admin/comments/{id}/{reply,pin,hide,delete}
GET        /vip/admin/threads       صندوق دایرکت
GET        /vip/admin/members       اعضا
GET        /vip/admin/stats         آمار
GET|POST   /vip/admin/plans         مدیریت طرح‌ها
```

---

## ۱۰. جریان کامل

```
مدیر در اپ: پست VIP می‌سازد + تیزر برای اینستاگرام
        ↓
پادو تیزر را تولید می‌کند؛ بک‌اند پس از تأیید آن را از Zernio منتشر می‌کند
        ↓
کاربر تیزر را می‌بیند → share → اپ ما (یا لینک در بیو)
        ↓
صفحهٔ فرود: اشتراک / خرید تکی / دانلود اپ
        ↓
پرداخت (درگاه یا کیف پول)
        ↓
عضویت ثبت می‌شود → فید VIP باز می‌شود
        ↓
مشتری پست را می‌بیند، لایک و کامنت می‌کند، دایرکت می‌دهد
        ↓
پس از expires_at پست از فید محو می‌شود
```

---

## ۱۱. ترتیب ساخت

| گام | محتوا | وضعیت |
|---|---|---|
| ۱ | اسکیما v10: نُه جدول `vip_*` | ✅ |
| ۲ | `VipAccessService` — یک نقطهٔ تصمیم دسترسی | ✅ |
| ۳ | `VipPostService` — CRUD، انتشار، انقضا | ✅ |
| ۴ | `VipSocialService` — لایک، کامنت، بازدید | ✅ |
| ۵ | `VipMessageService` — دایرکت | ✅ |
| ۶ | `VipBillingService` — اشتراک، خرید تکی، حمایت | ✅ |
| ۷ | `VipController` + `VipAdminController` | ✅ |
| ۸ | صفحهٔ فرود وب + deep link + `assets/css/vip-share.css` | ✅ |
| ۹ | پنل وردپرس (`Admin/VipPage.php`، پنج تب) + تب تنظیمات + bindingها | ✅ |
| ۱۰ | cron انتشار و انقضا (روی `igbz_cron_five_minutes`) | ✅ |
| ۱۱ | تست‌ها (`tests/VipChannelTest.php` — ۲۱ سناریو) | ✅ |

هر یازده گام پیاده و روی سایت واقعی (WordPress 7.0.4 + WooCommerce 11.0.1)
آزمایش شد: ساخت پست از پنل، انتشار، لینک اشتراک، پرداخت‌نشدن تیپ زیر حداقل،
انقضا با cron، و ۴۰۳ گرفتن لینک مدیای جعلی.

### چیزهایی که آزمایش زنده پیدا کرد و اصلاح شد

| مشکل | چرا مهم بود |
|---|---|
| `wp_kses_post()` روی فرم پنل | همهٔ `select`/`input`ها را حذف می‌کرد — فرم ساخت پست عملاً خالی بود |
| `esc_url()` روی deep link | اسکیم سفارشی را دور می‌ریخت؛ دکمهٔ «باز کردن در اپ» `href=""` داشت |
| `$m['thumb'] ?? $m['blur']` | کلید `thumb` همیشه هست (رشتهٔ خالی)، پس هرگز به blur نمی‌رسید؛ کاور پست عمومی سفید بود |
| `width="0" height="0"` | مرورگر تصویر را جمع می‌کرد |
| بلوک خالی «دو راه برای باز کردن» | وقتی هیچ طرحی قیمت‌گذاری نشده بود، عنوان بالای هیچ نمایش داده می‌شد |
| طرح نمونه فقط در migration | نصب تازه هیچ طرحی نداشت؛ حالا `activate()` هم آن را می‌نویسد |
| `purchase_post()` برای عضو فعال | عضو می‌توانست بابت پستی که همین حالا باز است دوباره پول بدهد |
| `vip_messages.tenant_id` پر نمی‌شد | پیام در صندوق یک فروشگاه دیده نمی‌شد |

---

## ۱۲. تصمیم‌های باز

1. **انقضا:** ✅ **بسته شد (۱۴۰۵/۰۵/۲۶)** — مدت را مدیر ارشد IGBZ تعیین می‌کند (پیش‌فرض ۷ روز)
   و پست پس از آن **از سرور حذف** می‌شود. شرح کامل: [`DESIGN-VIP-EXPIRY.md`](DESIGN-VIP-EXPIRY.md).
2. **خرید تکی پس از انقضای پست:** ✅ **بسته شد** — دسترسی نمی‌ماند، ولی مشتری **قبل از پرداخت**
   تاریخ حذف را روی صفحهٔ فرود می‌بیند و می‌تواند با آیکون ذخیره، نسخهٔ آفلاین را در اپ نگه دارد.
3. **ذخیرهٔ مدیا:** سرور وردپرس یا ابری؟ (پیشنهاد: آداپتور، شروع از سرور محلی)
4. **کامنت مشتری:** بلافاصله منتشر شود یا تأیید مدیر بخواهد؟ (پیشنهاد: بلافاصله،
   با امکان مخفی‌سازی)

> **توجه به بخش ۲ همین سند:** قید «بدون دانلود در اپ» برای VIP نرم شد — با تصمیم کارفرما، عضو
> می‌تواند نسخهٔ آفلاین یک پست را **داخل حافظهٔ خصوصی اپ** نگه دارد (نه در گالری، بدون دکمهٔ
> اشتراک‌گذاری). دلیلش این است که پست پس از یک هفته پاک می‌شود و بدون این راه، خریدِ تک‌پست
> چیزی است که ناپدید می‌شود. **این استثنا فقط برای VIP است؛ LMS همچنان دانلود ندارد.**
