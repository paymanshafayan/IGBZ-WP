# امنیت و مراقبت

**ایجاد:** ۱۴۰۵/۰۵/۳۱ به دستور کارفرما.
**آخرین ممیزی:** ۱۴۰۵/۰۶/۰۵، فاز ۰۱.

> «اول یه پوشه روی ریپو درست کن به اسم امنیت و مراقبت. بعد سندهایی که بهت گفتم، مقالات و
> اسنادی که گفتم برو تحقیق کن، اونایی که ازشون استفاده کردی رو در اون پوشه بذار که من
> بتونم بعداً بررسی کنم و چک کنم با کاری که انجام شده.»

در ۱۴۰۵/۰۶/۰۵ کارفرما همین فایل را صریحاً به‌عنوان **مبنای امنیتی اجرای برنامه** انتخاب
کرد. بنابراین در همهٔ فازها، تصمیم امنیتی باید به منبع معتبر، محل استفاده، کد و آزمونش در
`REQUIREMENTS-TRACEABILITY.md` وصل شود. ثبت منبع بدون استفاده یا ادعای امنیت بدون تطبیق
پیاده‌سازی با منبع، اشکال ممیزی است.

---

## هدف این پوشه

هر منبعی که در طراحی امنیت و مدیریت درخواست‌ها **واقعاً استفاده شده** اینجا خلاصه و ثبت
می‌شود، تا کارفرما بتواند **کار انجام‌شده را با منبعش مقایسه کند** — نه اینکه به ادعای
عامل اعتماد کند.

**قاعده:** هیچ منبعی اینجا نمی‌آید مگر در تصمیمی از تصمیم‌های پروژه به کار رفته باشد.
هر فایل بگوید **کجای پروژه** از آن استفاده شده.

---

## فهرست منابع

### `منابع/OWASP/`

| فایل | چه ریسکی | کجای پروژه استفاده شد |
|---|---|---|
| `API3-2023-Broken-Object-Property-Level-Authorization.md` | افشا و دستکاری ویژگی‌های شیء | `DESIGN-LEGAL-AUTH.md §۷.۶` و `§۷.۷`؛ `REQUIREMENTS-TRACEABILITY.md` نیاز SEC-002 |
| `API4-2023-Unrestricted-Resource-Consumption.md` | مصرف بی‌حد منابع و **هزینهٔ پیامک** | قاعدهٔ ۵ در `PROJECT-STATE.md §۵.۵`؛ نیازهای SEC-004 و SEC-005؛ اصلاح phone+IP در OTP |
| `API6-2023-Sensitive-Business-Flows.md` | سوءاستفادهٔ خودکار از جریان کاری سالم | قاعدهٔ ۵.۱ در `§۵.۵`؛ `DESIGN-LEGAL-AUTH.md §۷.۶`؛ نیازهای SEC-004 و SEC-009 |
| `Agentic-Top-10-2026.md` | ابزار، هویت، ارتباط، زنجیرهٔ تأمین و حافظهٔ عامل | مرز اعتماد پادوی بیرونی در ADR-0002؛ نیازهای PAD-001 تا PAD-006 |

### `منابع/WordPress/`

| فایل | موضوع | کجای پروژه استفاده شد |
|---|---|---|
| `WP-Cron-System-Task-Scheduler.md` | مستندات رسمی WP-Cron و کرون سیستمی | قاعدهٔ ۶ در `PROJECT-STATE.md §۵.۵`؛ نیازهای QUE-001 تا QUE-004 |
| `WooCommerce-MCP-Abilities.md` | MCP و Abilities API موجود در baseline ووکامرس ۱۱.۰.۱ | ورودی تصمیم فاز ۰۲ برای مرز پادو؛ نیازهای PAD-001 و SEC-003؛ هنوز فعال‌سازی تأیید نشده |

### `منابع/AI-Commerce/`

| فایل | موضوع | کجای پروژه استفاده شد |
|---|---|---|
| `e-commerce-agents-MIT.md` | پروژهٔ MIT با ماژول‌های `hitl.py`، `rate_limit.py`، `guardrails/` | الگوی چرخهٔ تأیید پادو و چک‌لیست گاردریل؛ نیازهای PAD-002، PAD-003 و PAD-006 |

### `منابع/IETF/`

| فایل | موضوع | کجای پروژه استفاده شد |
|---|---|---|
| `RateLimit-Headers-and-429.md` | کد ۴۲۹، هدر `Retry-After`، وضعیت پیش‌نویس استاندارد | جدول پاسخ استاندارد در قاعدهٔ ۵؛ نیاز SEC-005 |

---

## سه یافته‌ای که با تحقیق پیدا شد و بدون آن نوشته نمی‌شد

**۱. سناریوی #۱ سند `API4` منشأ کشف شکاف تاریخی OTP بود.**
یک شبکهٔ اجتماعی، جریان «فراموشی رمز» با پیامک، ارائه‌دهنده‌ای که هر پیامک ۰.۰۵ دلار
می‌گیرد، و مهاجمی که ده‌ها هزار بار صدایش می‌زند. OWASP صریح می‌گوید محدودکردن «چند بار
یک کاربر می‌تواند **درخواست بازیابی رمز** بدهد» جزو همین ریسک است. در کد فعلی محدودیت
شماره+IP و ایندکس مربوط افزوده شده است؛ اما اتمی‌بودن شمارنده، سهمیهٔ هزینهٔ provider و
پاسخ HTTP استاندارد هنوز باید جداگانه تأیید شوند.

**۲. `API6` یک دستهٔ کامل بود که جا انداخته بودم.**
در نسخهٔ ۲۰۱۹ وجود نداشت. نکته‌اش این است که «لزوماً از باگ پیاده‌سازی نمی‌آید» — جریان
سالم است، ولی خودکارسازی‌اش به کسب‌وکار آسیب می‌زند. محدودیت نرخِ صرف جلویش را نمی‌گیرد.

**۳. سه خواستهٔ مستقل کارفرما دقیقاً بر توصیه‌های استاندارد منطبق‌اند.**
بدون اینکه از روی این اسناد نوشته شده باشند:

| خواستهٔ کارفرما | توصیهٔ OWASP `API6` |
|---|---|
| امضای **بیومتریک** برای خروجی گروهی | «تشخیص انسان: کپچا یا راهکارهای بیومتریک» |
| **اطلاعات دستگاه** ثبت‌شده در اپ | «اثر انگشت دستگاه» |
| گزارش رفتار مشکوک به **ویرا** | «تحلیل جریان کاربر برای کشف الگوهای غیرانسانی» |

---

## آنچه این پوشه **نیست**

- **کپی کامل اسناد اصلی نیست.** خلاصهٔ فارسی با نقل‌قول‌های کلیدی و لینک منبع است، تا
  حجم مخزن بی‌جهت بزرگ نشود. لینک هر فایل بالای خودش هست.
- **سند طراحی نیست.** طراحی در `DESIGN-GAPS-FIX.md` و اسناد `DESIGN-*` است؛ اینجا فقط
  **پشتوانه** است.

---

## چطور از این پوشه برای بازبینی استفاده کنید

هر فایل منبع یک بخش **«کجای پروژه استفاده شد»** دارد. برای هر تصمیم امنیتی می‌توانید:

۱. سند طراحی را باز کنید (`DESIGN-GAPS-FIX.md` یا `DESIGN-LEGAL-AUTH.md`)
۲. منبع متناظرش را از همین پوشه بخوانید
۳. بسنجید که آیا کاری که انجام شده واقعاً با توصیهٔ استاندارد می‌خواند یا نه

اگر جایی نخواند، **آن یک اشکال است و باید گزارش شود** — چه از سوی شما، چه از سوی بازبین.

برای وضعیت اجرای هر کنترل، `REQUIREMENTS-TRACEABILITY.md` مرجع است. متن «وضعیت پروژه» در
خلاصه‌های منبع، snapshot تاریخ بازیابی است و باید هنگام هر ممیزی با کد فعلی تطبیق داده
شود؛ خلاصهٔ منبع به‌تنهایی اثبات پیاده‌سازی یا امنیت نیست.
