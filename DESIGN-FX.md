# سیستم واسط پرداخت ارزی (FX Gateway) — طراحی

**وضعیت:** زیرساخت FX موجود است، اما کاربرد تاریخی آن برای Manus/ManyChat با ADR-0004
منسوخ شد. در معماری جاری، DeepInfra را خود فروشگاه مستقیم می‌پردازد و هزینهٔ profile
Zernio در اشتراک IGBZ همان فروشگاه منظور می‌شود. جدول‌ها و جریان‌های قدیمی پایین فقط
موجودی کد و نیازمند migration هستند؛ مرجع جاری بخش ۱۲ همین سند است.

---

## ۱. مسئله و ایده در یک جمله

ادمین فروشگاه ایرانی باید هزینهٔ سرویس‌های ارزی (Manus ~۲۰ دلار/ماه و ManyChat ~۱۷–۳۹ دلار/ماه)
را بدهد ولی کارت ارزی ندارد. **راه‌حل: ادمین با ریال (درگاه ایرانی) حسابش را شارژ کند؛ پرداخت
ارزی واقعی را سیستم واسطِ پروژه، خودکار انجام دهد.**

## ۲. تصمیم‌های کلیدی (تأییدشده)

| موضوع | تصمیم | دلیل |
|---|---|---|
| **مدل مالکیت** | **آژانسی:** اپراتور یک هویت واحد (پروفایل/کارت/مرکز هزینه)؛ هر مستأجر **اکانت خارجی خودش** را دارد (ManyChat الزاماً، Manus برای مقیاس) | کلید ManyChat به یک پیج مقید است؛ یک اکانت مشترک Manus فقط ۲۰ تسک همزمان دارد و در مقیاس صدها مستأجر گلوگاه/نقطهٔ شکست می‌شود. مدل آژانسی ManyChat رسماً پشتیبانی می‌شود (۲۰۰+ اکانت). کلیدهای per-account از قبل در افزونه ساخته شده‌اند (`credential_mode = own`). |
| **صف تسک** | **هیچ صفی بین مستأجرها وجود ندارد.** FX فقط در لحظهٔ ارسال، اعتبارِ خودِ همان مستأجر را چک می‌کند: کافی → ارسال فوری؛ ناکافی → رد همان لحظه با پیام «شارژ کنید». | قید صریح کارفرما: سیستم نباید باعث صف تسک منوس شود. (صفِ درونیِ خودِ هر اکانت، جدا از این است.) |
| **پرداخت ارزی** | **خودکار از ابتدا** — آداپتور پرداخت (مستقل از فروشنده) + کارت مجازیِ تأمین‌شده با USDT. | قید کارفرما: نه دستی. |
| **قیمت‌گذاری** | **ترکیبی:** اشتراک ماهانهٔ پایه به‌ازای هر اکانت + مصرف اضافه با نرخ تسکی. | انتخاب کارفرما. |
| **نرخ ارز** | **خودکار** از یک منبع، با fallback دستی؛ نرخ در لحظهٔ شارژ قفل و ثبت می‌شود. | انتخاب کارفرما. |
| **جایگاه** | ماژول جدید **`fx`** (قابل خاموش‌وروشن، پیش‌فرض خاموش) | استقلال از `multitenant`/`instagram`؛ مرز تمیز با پرداخت داخلی. |

## ۳. جریان‌های اصلی

### ۳.۱ شارژ ریالی
```
ادمین → پنل/اپ: «شارژ حساب ارزی» (مبلغ ریالی، با نرخ روز از fx_rates)
     → PaymentService::start(amount, purpose='fx_topup', …) → درگاه ایرانی
     → کال‌بک → igbz_payment_verified → ثبت در fx_ledger (idempotent)
     → تبدیل ریال→دلار با نرخ قفل‌شده → افزایش fx_wallets
```

### ۳.۲ تسویهٔ خودکار قبض ارزی (هستهٔ واسط)
```
کرون روزانه: قبض‌های سررسید (اشتراک ماهانهٔ هر اکانت) را می‌خواند
     → برای هر قبض: اعتبارِ آن مستأجر کافی است؟
         → بله: FxPayoutAdapter::pay(bill) → کارت مجازی/USDT → ثبت + لجر
         → نه: وضعیت قبض = unpaid + اعلان به ادمین (بدون صف، بدون قرض)
تطبیق (reconciliation): از صورت‌وضعیت/وبوک ارائه‌دهندهٔ کارت، نه API منوس/منیچت
     (این دو API عمومی «قبض» ندارند).
```

### ۳.۳ مصرف اضافه (بالاتر از اشتراک)
هر dispatch منوس / تحویل منیچت: `FxMeter::claim(tenant, price_key)` — الگوی
claim-before-call مثل `claim_trial_task()`: کافی → کسر + اجرا؛ ناکافی → رد همان لحظه.
خطای سرویس → بازگشت اعتبار (`release` مثل trial).

## ۴. جدول‌ها (DB v14 — با پیشوند `fx_`)

| جدول | ستون‌های کلیدی | نکته |
|---|---|---|
| `fx_wallets` | tenant_id (UNIQUE)، balance_usd DECIMAL(18,4)، balance_irt، updated_at | قفل/upsert مثل `wallet_balances` |
| `fx_ledger` | tenant_id، reason (`topup`/`subscription`/`task`/`refund`/`payout`)، amount_usd، amount_irt، rate_id، reference، idempotency_key | **UNIQUE (tenant, reason, reference)** ضد دوباره‌کسر/دوباره‌شارژ |
| `fx_rates` | rate_irt_per_usd، source (`auto`/`manual`)، captured_at | هر شارژ به یک rate_id قفل می‌شود |
| `fx_prices` | service (`manus_sub_monthly`/`manus_task`/`manychat_sub_monthly`/…)، price_usd، is_active | جدول قیمت اپراتور؛ بدون margin مخفی — margin در همین قیمت‌ها |
| `fx_accounts` | tenant_id، provider (`manus`/`manychat`)، provider_account_id، status، billing_day | اکانت خارجیِ هر مستأجر (کلیدها همچنان در `ig_accounts` با `own`) |
| `fx_bills` | fx_account_id، period_start/end، amount_usd، status (`due`/`paid`/`unpaid`/`cancelled`)، paid_at، payout_ref | قبض‌های ماهانه و تسویه‌ها |

جداول بدون `tenant_id` عمداً: `fx_rates` (سراسری) و `fx_prices` (قیمت‌ها را اپراتور می‌سازد،
نه هر مستأجر). بقیه با tenant_id.

## ۵. نقاط اتصال در کد موجود (بدون تغییر رفتار تا فعال‌شدن ماژول)

- `Modules::FX = 'fx'` + ثبت در `igbz_enabled_modules`.
- `PaymentService::PURPOSE_FX_TOPUP` + شاخهٔ settle — یا بهتر: مثل VIP، **تسویه روی
  `igbz_payment_verified`** (بدون دست زدن به `settle()`).
- `AccountCredentials` → حالت جدید؟ خیر — کلیدها `own` می‌مانند؛ گیت اعتبار در یک
  `FxMeter` که `ManusService::dispatch()` (از طریق filter/هوک) و `FunnelService::settle()`
  آن را صدا می‌زنند.
- `ContentScheduler::fair_share` دست‌نخورده می‌ماند (مربوط به اکانت‌های trial اشتراکی است).
- `IGBZ_DB_VERSION` 13 → 14 + `Activator::migrate_to_v14()`.

## ۶. آداپتور پرداخت خودکار (مستقل از فروشنده)

```
FxPayoutAdapterInterface:
  pay(Bill $bill): PayoutResult          // پرداخت قبض
  card_balance(): Money                  // موجودی کارت
  top_up_card(usd): PayoutResult         // تأمین کارت از USDT
  webhook(payload): void                 // رویدادهای کارت (موفقیت/شکست)
```
- پیاده‌سازی نخست: **کارت مجازی با تأمین مالی USDT** (گزینه‌ها: Moon، Bitsika، Cardn3،
  SolCard و… — تصمیم با اپراتور؛ آداپتور فروشنده‌محور نیست).
- زنجیرهٔ کامل خودکار: ریال → صرافی (فاز ۳) → USDT → کارت → قبض. در فاز ۱، کارت به‌صورت
  دستی توسط اپراتور شارژ USDT می‌شود ولی **پرداخت قبض‌ها خودکار** است.
- درگاه داخلی: `igbz_register_fx_payout_providers` (مثل `igbz_register_payment_gateways`).

## ۷. نرخ ارز

- منبع خودکار: یک API قیمت (مثلاً قیمت لحظه‌ای USDT/ریال از صرافی معتبر) با fallback
  مقدار دستی اپراتور وقتی منبع در دسترس نیست.
- نرخ در لحظهٔ شارژ قفل می‌شود (`fx_rates.id`) و برای کل مبلغ همان شارژ ثابت می‌ماند.

## ۸. قیمت‌گذاری ترکیبی (پیشنهاد اولیه — قابل تغییر توسط اپراتور)

| سرویس | نوع | پیشنهاد |
|---|---|---|
| Manus هر اکانت | ماهانه | ~۲۵ دلار (شامل حاشیهٔ سود) |
| ManyChat هر اکانت | ماهانه | ~۲۵–۴۵ دلار بسته به پلن/مخاطب |
| تسک اضافهٔ Manus | تسکی | ~۰٫۵ دلار |
| تحویل اضافهٔ ManyChat | — | طبق جدول قیمت |

## ۹. پنل مدیر و REST

- صفحهٔ «پرداخت ارزی» (`igbz-fx`): موجودی، شارژ (درگاه)، قیمت‌ها، قبض‌ها، تاریخچه،
  هشدار کمبود اعتبار؛ برای اپراتور: مدیریت نرخ/قیمت/اکانت‌ها، گزارش تسویه.
- REST (برای اپ): `GET /fx/balance`، `POST /fx/topup`، `GET /fx/ledger`، `GET /fx/prices`،
  `GET /fx/bills`.

## ۱۰. فازبندی

- **فاز ۱ — هسته:** ماژول `fx` + جدول‌ها (v14) + شارژ ریالی + آداپتور پرداخت (کارت
  مجازی/USDT) + گیت اعتبار لحظه‌ای منوس + پنل پایه.
- **فاز ۲:** قبض‌های ماهانهٔ ManyChat + اشتراک/مصرف اضافه + نرخ خودکار + REST اپ.
- **فاز ۳ (تمام):** آداپتورهای PST.NET (اصلی) و RedotPay (پایلوت) + تسویهٔ دستی + وبوک. باقی‌مانده: آداپتور صرافی ایرانی برای خرید خودکار USDT و گزارش‌های مالی اپراتور.

## ۱۱. ریسک‌ها و تصمیم‌های باز

- **ریسک:** نوسان نرخ → قفل نرخ در لحظهٔ شارژ.
- **ریسک:** دوباره‌کسر/دوباره‌شارژ → idempotency در `fx_ledger` + claim-before-call.
- **ریسک:** رد پرداخت توسط فروشندهٔ کارت → وبوک + وضعیت `failed` + تلاش مجدد با محدودیت.
- **باز:** انتخاب ارائه‌دهندهٔ کارت مجازی (آداپتور آماده است)؛ منبع نرخ خودکار؛ حداقل
  شارژ؛ سیاست بازگشت اعتبار استفاده‌نشده؛ سرنوشت اعتبار هنگام خروج مستأجر.

---

## ۱۲. تطبیق با معماری پادو و Zernio — ۱۴۰۵/۰۶/۰۶

طبق `ADR/ADR-0004-PADO-ZERNIO-SOCIAL-ARCHITECTURE.md`:

- حساب، کلید، اعتبار و هزینهٔ DeepInfra متعلق به همان فروشگاه است و IGBZ آن را شارژ،
  نگهداری، proxy یا از کیف FX مشترک تسویه نمی‌کند؛
- Zernio از حساب مرکزی IGBZ پرداخت می‌شود، اما profile و هزینهٔ قابل‌انتساب هر فروشگاه
  جداست و مبلغ آن در پلن/اشتراک IGBZ همان فروشگاه محاسبه می‌شود؛
- `manus_*` و `manychat_*` در `fx_prices`، `fx_accounts`، قبض‌ها، گزارش و متن رابط legacy
  هستند و باید در فاز migration با حفظ audit مالی آرشیو/حذف شوند؛
- هیچ مانده، بدهی یا پرداخت قبلی نباید بدون reconciliation، خروجی حساب و تأیید مالی حذف
  یا به هزینهٔ Zernio تبدیل شود؛
- آداپتورهای payout ممکن است برای سایر هزینه‌های ارزی IGBZ مفید بمانند، ولی دیگر جزء
  مسیر inference مستقل فروشگاه نیستند.

ثبت این تصمیم به‌معنای migration دیتابیس یا تغییر کد نیست. تا فاز مصوب، قبض تازه برای
Manus/ManyChat ساخته نشود و کلید واقعی جدیدی برای آن‌ها وارد نشود.
