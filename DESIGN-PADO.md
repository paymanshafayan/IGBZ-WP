# پادو — هستهٔ هوشمند فروشگاه (طرح اولیه)

> **ریشه نام (دهخدا):** پادو (اِ مرکب): پاکار، خدمتکارگونه و فرمانبر، وردست شاطر نانوایی، شاگرد حجره تجار؛ کسی که برای انجام دادن کارهای سرپایی و دوندگی فرستاده می‌شود.

**وضعیت:** v0.2 — بخش «ذهنیت» هنوز پیش‌نویس و برای تکمیل مشترک است، اما **فاز ۰ (کلون
کامل) ساخته شد**: ابزار با نام **ویرا** در پوشهٔ `vira/` مخزن، اجراشده و تست‌شده.
گزارش اجرا در بخش ۱۳.
**تاریخ:** ۱۴۰۵/۰۵/۲۶

---

## ۱. خواسته‌ها — عیناً از گفته‌های کارفرما

این بخش «قانون» است؛ بقیهٔ سند فقط تفسیر و اجرای همین‌هاست.

| # | خواسته | نقل از کارفرما |
|---|---|---|
| ۱ | **دامنه: کل فروشگاه، نه یک بخش** | «تقریباً از هر لحاظ سایت را مدیریت کند… یک هستهٔ هوشمند برای کنترل فروشگاه» |
| ۲ | **در حد قابلیت‌های Claude Code** | «تقریباً قابلیت‌های کلاد کد را داشته باشد» |
| ۳ | **بومی و مستقل از مدل** | «یک ابزار بومی که بتونه از هر مدلی استفاده کنه» |
| ۴ | **بدون وابستگی به منوس** | «بدون وابستگی به ابزارهایی مثل منوس و بدون محدودیت‌های آن‌ها» |
| ۵ | **نصب اسکیل‌های آماده** (نه ساختن اسکیل) | «نمی‌خوام اسکیل بسازیم، بلکه می‌خوام بتونه اسکیل‌های آماده را نصب کنه» |
| ۶ | **اتصال به MCP server ها** | «ارتباط با MCP server های مورد نیاز» |
| ۷ | **تخصصی، نه همه‌فن‌حریف** | «ابزار ما یک ابزار همه‌فن‌حریف نیست، بلکه یک ابزار تخصصی است» |
| ۸ | **روند کاری از پیش تعریف‌شده = مزیت رقابتی** | «ابزارهای دیگر پلتفرم باز دارند… نیاز به بازتعریف و اشکال‌زدایی دوباره و دوباره دارد؛ اما ما یک روند خاص و کارهای مشخص داریم که می‌توان دقیقاً در خود ابزار مشخص کرد و از هر نوع اشتباهی جلوگیری کرد» |
| ۹ | **مسیر کامل: معرفی محصول تا پشتیبانی** | «ادمین… محصولاتش را معرفی می‌کند و بعد با خیال راحت همه‌چیز را به ابزار می‌سپارد و تنها نظارت می‌کند؛ ابزار باید کلیهٔ مراحل بازاریابی، ارتباط با مشتری، فروش، ارسال و پیگیری و در نهایت پشتیبانی را بر عهده بگیرد» |
| ۱۰ | **معرفی محصول هم باید آسان باشد** | «در این مورد هم ابزار باید کار را برای او آسان کند» |
| ۱۱ | **رابطهٔ ادمین با ابزار = رابطهٔ کارفرما با دستیار** | «ابزار پلن می‌دهد (و یا گزارش)، ادمین تأیید می‌کند و ابزار اجرا می‌کند» |
| ۱۲ | **با یک تفاوت کلیدی** | «من اینجا باید بگم چه می‌خوام، اما آنجا خواسته‌ها از قبل تعریف شده» |
| ۱۳ | **دانش تجارت، نه فقط اجرای فنی** | «مثل یک کارشناس فروش و استاد تجارت… اصول فروش، مدیریت مشتری، قیمت‌گذاری، مذاکره، وفاداری» |
| ۱۴ | **نتیجه باید چشمگیر باشد** | «و نتیجه هم در انتها چشمگیر باشد» |
| ۱۵ | **عجله ممنوع** | «برای شروع کار اصلاً عجله نکن… مهم‌تر از هر چیز این است که ذهنیت من کاملاً به تو منتقل شود» |
| ۱۶ | **اول یک نسخهٔ عیناً مثل Claude Code — بدون حذف هیچ چیز** | «در ابتدا نمی‌خوام هیچ چیزی تغییر کنه یا حذف بشه… بعد به‌مرور تغییرش بدیم و کاملش کنیم» |
| ۱۷ | **فهرست کارها هنوز کامل نیست** | «تو هنوز نمی‌دونی که من چه کارهایی می‌خوام این ابزار انجام بده. مثلاً یکی از آن‌ها طراحی قالب سایت فروشگاه است و بسیاری موارد دیگر» |

---

## ۲. پاسخ کوتاه به سؤال Windsor و Sandcastles

**Windsor** — قبلاً بررسی و **رد** شد و در `PROJECT-STATE.md` ثبت است: عامل مرورگر است و
اتوماسیون مرورگر روی اینستاگرام طبق ToS متا ممنوع است. تصمیم عوض نمی‌شود.

**Sandcastle** (که در هیچ سند پروژه نبود) — یک **کتابخانهٔ متن‌باز TypeScript** است
(mattpocock/sandcastle) برای **اجرای عامل‌های کدنویس داخل سندباکس ایزوله**: هر اجرا یک
worktree گیت جدا و یک کانتینر (Docker/Podman/Vercel) می‌گیرد، عامل داخل آن کار می‌کند و
کامیت‌هایش بعداً merge می‌شود. ربطی به اینستاگرام ندارد و رقیب پادو نیست؛ اما **یک الگوی
معماری** دارد که مستقیماً به کار ما می‌آید:

> **کار عامل، مستقیم روی واقعیت اعمال نمی‌شود؛ اول در یک فضای ایزوله انجام می‌شود و سپس
> به‌شکل یک «تغییر قابل بازبینی» ارائه می‌گردد.**

معادل تجاری‌اش در پادو: هر اقدام اول به‌صورت **پیش‌نمایش/شبیه‌سازی** ساخته می‌شود
(«این ۲۳ قیمت این‌طور تغییر می‌کنند»، «این پیام برای این ۴۱۰ نفر می‌رود»)، ادمین تأیید
می‌کند، بعد اعمال می‌شود — و امکان برگشت (rollback) دارد. این دقیقاً همان بند ۱۱ کارفراست.

نکتهٔ مرتبط دیگر: Anthropic هم «سندباکس خودمیزبان» و «MCP tunnel» را عرضه کرده — یعنی
اجرای ابزارها داخل زیرساخت خودِ سازمان. برای ما این تأیید می‌کند که مسیر «هستهٔ بومی +
ابزارهای داخل زیرساخت خودمان» مسیر درستی است.

---

## ۲.۵ ویرا مثل مغز است — تصحیح بنیادی کارفرما (۱۴۰۵/۰۵/۳۱)

> «ویرا مثل مغز انسان می‌مونه که ابزارهای مختلف داره، مثل چشم‌ها، گوش‌ها، عضلات. ویرا هم
> ابزارهای مختلفی داره که در قالب ماژول‌ها طراحی شدن. یکی مثلاً از این ماژول‌ها رابط
> کاربریه. وقتی می‌گم ویرا بالا اومد، منظورم ماژول رابط کاربریش برای مدیره، نه اینکه
> خودِ ویرا خاموش بوده حالا روشن می‌شه.»

این تعریف با بند ۱ خواسته‌ها («هستهٔ هوشمند برای کنترل فروشگاه») یکی است و آن را دقیق‌تر
می‌کند: **هسته مغز است و همیشه بیدار؛ ماژول‌ها اندام‌های اویند و می‌آیند و می‌روند.**

| | نقش | چرخهٔ حیات |
|---|---|---|
| **هسته** (مغز) | مدیریت، نظارت، تصمیم | **همیشه بالا** |
| **ماژول رابط کاربری** (چشم و زبان) | تعامل با مدیر | با نیاز |
| **سایر ماژول‌ها** (حواس و عضلات) | ابزار، گیت، تونل، هاب، امنیت | بسته به کار |

**چرا این ثبت شد:** سند استقرار یک قید داشت — «پروسه‌ها بعد از ۳۰ دقیقه بیکاری خاموش
می‌شوند» — که **پیشنهادی تأییدنشده** بود و کارفرما آن را رد کرد. هسته‌ای که همه‌چیز را
مدیریت می‌کند نمی‌تواند خاموش شود؛ با مسئولیت امنیتی (`DESIGN-DEPLOY-VIRA.md §۱۴`)
خاموشی یعنی کور شدن. جزئیات و پیامدها: `DESIGN-DEPLOY-VIRA.md §۵.۰`.

---

## ۳. یک تعریف، تا سوءتفاهم نشود

> **پادو یک چت‌بات نیست، و یک «Claude Code برای فروشگاه» هم نیست. پادو یک
> «کارشناس فروش و مدیر عملیات دیجیتال» است که ابزارهای دقیق و محدودی در اختیار دارد و
> روی ریل یک روند تجاری از پیش تعریف‌شده کار می‌کند.**

از Claude Code این‌ها را وام می‌گیریم (بند ۲):

| از Claude Code می‌گیریم | چون |
|---|---|
| حلقهٔ عامل (plan → act → observe → repeat) | کار چندمرحله‌ای بدون دخالت انسان |
| **Skills** (نصب اسکیل آماده) | بند ۵ |
| **MCP client** | بند ۶ |
| Hooks / Permission gates | بند ۱۱ (دروازهٔ تأیید) |
| Sub-agent ها | کار موازی (محتوا، قیمت، پشتیبانی) |
| Audit trail و قابلیت بازگشت | مسئولیت مالی |
| اجرای ایزوله (الگوی Sandcastle) | پیش‌نمایش قبل از اعمال |
| **ابزارهای عمومی: فایل، ویرایش، شل، جستجو، وب** | **بند ۱۶ — بدون این‌ها کارهایی مثل طراحی قالب فروشگاه اصلاً ممکن نیست** |

### ⚠️ تصحیح مهم (۱۴۰۵/۰۵/۲۶ — تصمیم کارفرما)

نسخهٔ اول این سند پیشنهاد داده بود ابزارهای عمومی (شل/فایل) **حذف** شوند و جایشان فقط
ابزار دامنه‌ای بنشیند. **کارفرما این را رد کرد و درست هم می‌گوید:**

> «در ابتدا نمی‌خوام هیچ چیزی تغییر کنه یا حذف بشه؛ می‌خوام یک نسخه عیناً شبیه به Claude Code
> داشته باشیم، بعد با توجه به نیازهامون به‌مرور تغییرش بدیم و کاملش کنیم. و تو هنوز نمی‌دونی
> که من چه کارهایی می‌خوام این ابزار انجام بده. مثلاً یکی از آن‌ها **طراحی قالب سایت فروشگاه**
> است و بسیاری موارد دیگر.»

دلیل فنی‌اش هم روشن است: **طراحی قالب یعنی نوشتن PHP/CSS/JS و ساخت فایل و اجرای بیلد.** اگر
ابزار فایل و شل را برداریم، ابزار از همان روز اول از انجام یکی از خواسته‌های اصلی ناتوان است.
و چون فهرست کارها هنوز کامل نیست، **هر حذفی امروز، یک قابلیتِ فرداست که نداریم.**

**قاعدهٔ جایگزین — تخصصی‌سازی با سیاست، نه با حذف:**

> هستهٔ ما **همهٔ توانایی‌های Claude Code را دارد**؛ آنچه محدود می‌شود **دسترسی است، نه
> توانایی**. تخصصی‌بودن از راه **افزودن** (ابزار دامنه‌ای، اسکیل، Playbook) و **سیاست‌گذاری
> نقش‌محور** (پروفایل مجوز) به دست می‌آید، نه از راه قطع‌کردن دست ابزار.

سه پروفایل مجوز، روی **یک** هستهٔ کامل:

| پروفایل | کاربر | دسترسی |
|---|---|---|
| **توسعه‌دهنده** | ما / مدیر ارشد | کامل — فایل، شل، بیلد، قالب، دیپلوی (مثل Claude Code) |
| **اپراتور فروشگاه** | ادمین فروشگاه | ابزار دامنه‌ای + قالب در محیط ایزوله با پیش‌نمایش؛ شل خام بسته |
| **خودکار (کرون)** | خود سیستم | فقط ابزارهای کم‌ریسکِ فهرست‌سفید |

پروفایل یک **فایل تنظیمات** است، نه یک شاخهٔ کد. یعنی توانایی هرگز از بین نمی‌رود؛ فقط برای
یک نقش خاموش است و هر وقت لازم شد، روشن می‌شود.

و این‌ها را **نمی‌گیریم** (چون به کار ما نمی‌آید، نه چون خطرناک است): یکپارچگی با IDE،
GitHub App، نشست ریموت/تله‌پورت، ورودی صوتی.

---

## ۴. مزیت اصلی ما، به زبان مهندسی (بند ۷ و ۸)

کارفرما درست می‌گوید: ابزار عمومی، فضای عمل بی‌نهایت دارد؛ مدل هر بار راه را از نو کشف
می‌کند، خطا می‌کند، و کاربر گرفتار بازتعریف و اشکال‌زدایی مکرر می‌شود.

اما راه‌حلش **کم‌کردن توانایی ابزار نیست** (بند ۱۶)؛ راه‌حلش این است که **مسیر درست از قبل
در دسترس باشد** تا مدل مجبور نباشد کشفش کند:

| مسئله | راه‌حل ما |
|---|---|
| مدل نمی‌داند «روش درست» چیست | **Playbook** — رویهٔ تجاری کدشده و آماده |
| مدل باید کار را از پایه بسازد | **ابزار دامنه‌ای** — یک فراخوانی به‌جای ده گام دستی |
| مدل ممکن است اشتباه پرهزینه کند | **مجوز + پیش‌نمایش + سقف ایمنی + بازگشت** |
| مدل قواعد این کسب‌وکار را نمی‌داند | **پروفایل فروشگاه و خطوط قرمز** در حافظه |
| مدل حساب‌وکتاب را خراب می‌کند | **فرمول‌ها با کد**، نه با متنِ مدل |

پس ترتیب درست این است: **اول توانایی کامل، بعد ریل.** ابزار عمومی زیرِ همه‌چیز باقی می‌ماند
(برای وقتی که کاری خارج از Playbook ها لازم شد — مثل طراحی قالب)، و روی آن، مسیرهای آماده
می‌نشیند تا کار روزمره از مسیر امن و آزموده برود.

> قاعدهٔ طلایی: **مدل تصمیم‌های قضاوتی می‌گیرد؛ کد تصمیم‌های محاسباتی. پول همیشه با کد
> جابه‌جا می‌شود، هرگز با متنِ مدل.**

---

## ۵. معماری پیشنهادی — هفت لایه

```
┌─────────────────────────────────────────────────────────────┐
│ ۷. رابط‌ها: پنل وردپرس · اپ ادمین (فلاتر/PWA) · گزارش/اعلان │
├─────────────────────────────────────────────────────────────┤
│ ۶. حاکمیت: پلن → تأیید → اجرا → گزارش · سطح خودمختاری ·      │
│    پیش‌نمایش و بازگشت · لاگ کامل · سقف‌های ایمنی             │
├─────────────────────────────────────────────────────────────┤
│ ۵. مغز تجاری: Playbook ها + KPI ها + فرمول‌های تجاری          │
│    (قیمت‌گذاری، RFM، CLV، سبد رهاشده، مذاکره، وفاداری)        │
├─────────────────────────────────────────────────────────────┤
│ ۴. حلقهٔ عامل: برنامه‌ریز · مجری · ناظر · حافظه               │
├─────────────────────────────────────────────────────────────┤
│ ۳. توسعه‌پذیری: Skill Registry (نصب اسکیل آماده) · MCP Client │
├─────────────────────────────────────────────────────────────┤
│ ۲. ابزارها (Tools): محصول، قیمت، سفارش، مرسوله، مشتری،        │
│    محتوا، کمپین، تیکت، گزارش — هرکدام با مجوز و ریسک مشخص     │
├─────────────────────────────────────────────────────────────┤
│ ۱. مدل: Model Router — هر مدلی (ایرانی/خارجی/محلی)            │
└─────────────────────────────────────────────────────────────┘
```

### لایهٔ ۱ — Model Router (بند ۳: «از هر مدلی»)

- یک اینترفیس (`LlmProviderInterface`) و آداپتورهای HTTP تنظیم‌پذیر — **دقیقاً همان الگویی
  که در این پروژه جواب داده** (`AiProviderInterface`, `FxPayoutAdapterInterface`,
  `GatewayInterface`).
- سه ردهٔ سرویس‌دهنده، هم‌زمان و قابل‌سوئیچ:
  - **ایرانی/ریالی:** دیپ‌فا، آتنا، ویرا، رخشای، هوش‌نو (مطابق سند خود کارفرما و اصل
    «پرداخت ریالی، بدون کارت ارزی»)
  - **بین‌المللی:** از طریق واسط پرداخت ارزی موجود (ماژول `fx`)
  - **محلی/خودمیزبان:** برای دادهٔ حساس
- **مسیریابی بر اساس کار، نه سلیقه:** کار ارزان (دسته‌بندی، برچسب) → مدل کوچک؛ کار
  قضاوتی (استراتژی قیمت، پاسخ شکایت) → مدل بزرگ. سقف هزینه به‌ازای هر مستأجر از همان
  لجر `ig_ai_credit_ledger` و ماژول `fx` که امروز داریم.

### لایهٔ ۲ — ابزارها: عمومی + دامنه‌ای، روی هم

ابزارها **دو دسته‌اند و هر دو می‌مانند**:

**الف) ابزارهای عمومی (میراث Claude Code، دست‌نخورده):** خواندن/نوشتن/ویرایش فایل، شل،
جستجو (glob/grep)، fetch و جستجوی وب، اجرای زیرعامل، فهرست کار. این‌ها همان چیزی‌اند که
کارهای «باز» را ممکن می‌کنند: **طراحی و ویرایش قالب فروشگاه**، ساخت افزونهٔ کوچک، اصلاح
CSS، مهاجرت داده، اسکریپت یک‌بارمصرف، اشکال‌زدایی.

**ب) ابزارهای دامنه‌ای (افزودهٔ ما):** هر کدام یک تابع با اسکیمای ورودی، **سطح ریسک**،
**مجوز لازم** و **قابلیت پیش‌نمایش/بازگشت**.

| گروه | نمونه ابزار | ریسک |
|---|---|---|
| کاتالوگ | ساخت/ویرایش محصول، دسته، برچسب، ترجمه، متای سئو | کم |
| قیمت | تغییر قیمت، تخفیف، کوپن، قیمت‌گذاری پلکانی | **بالا (پول)** |
| محتوا | تولید پست/ریلز/کپشن، زمان‌بندی، انتشار | متوسط (آبرو) |
| مشتری | سگمنت‌بندی، ارسال پیام/کمپین، پاسخ دایرکت | **بالا (اسپم/آبرو)** |
| سفارش | تأیید، لغو، بازگشت وجه، فاکتور | **بالا (پول)** |
| ارسال | ساخت مرسوله، انتخاب مسیر، برچسب، پیگیری | متوسط |
| پشتیبانی | تیکت، مرجوعی، گارانتی، نظرسنجی | متوسط |
| قالب و ظاهر | ویرایش قالب، بلوک، رنگ‌بندی، صفحه‌ساز، پیش‌نمایش | **بالا (سایت زنده)** |
| تحلیل | گزارش، پیش‌بینی، هشدار موجودی، سودآوری | کم (فقط خواندن) |

**نکتهٔ کلیدی:** بخش بزرگی از دستهٔ (ب) **همین امروز به‌شکل سرویس در افزونه وجود دارد**
(`payments`, `logistics`, `marketplace`, `seo`, `lms`, `vip.*`, `ig.funnels`, `wallet`…).
پادو چیز جدیدی از صفر نمی‌سازد؛ روی همان‌ها یک لایهٔ ابزار می‌کشد. این هم کار را کوتاه
می‌کند هم ریسک را: منطق مالی همان منطق آزمودهٔ فعلی می‌ماند.

**دستهٔ (ب) جانشین (الف) نیست، مکمل آن است.** جایی که ابزار دامنه‌ای هست، مسیر امن‌تر و
ارزان‌تر است؛ جایی که نیست، ابزار عمومی کار را انجام می‌دهد.

### لایهٔ ۳ — Skills و MCP (بند ۵ و ۶)

- **Skill = دانش رویه‌ای، MCP = دسترسی.** این تفکیک استاندارد صنعت است و ما هم همان را
  می‌پذیریم.
- **نصب اسکیل آماده:** فرمت `SKILL.md` (فولدر + دستورالعمل + اسکریپت اختیاری) که حالا
  عملاً استاندارد مشترک چند ابزار است. یعنی اسکیل‌های آمادهٔ بازار **بدون بازنویسی** قابل
  نصب‌اند. صفحهٔ «اسکیل‌ها» در پنل: نصب از مخزن/فایل، فعال/غیرفعال، **محدودسازی دسترسی هر
  اسکیل به مجموعه‌ای از ابزارها**، و نسخه‌بندی.
- دو اسکیل از قبل آمادهٔ ماست: `PROMPTS-SEO-PADO.md` و `PROMPT-IG-GROWTH-PADO.md`.
- **کلاینت MCP:** اتصال به سرورهای بیرونی (ChatPlace MCP، مارکت‌پلیس‌ها، حسابداری/مؤدیان،
  انبار). هر سرور با اجازهٔ صریح مدیر ارشد و با دامنهٔ محدود.
- **دروازهٔ امنیت (لازم، نه تزئینی):** اسکیل و MCP یعنی «کد و دستورالعملِ غریبه». پس:
  فهرست سفید، تأیید مدیر ارشد، اجرای ایزوله، و **ممنوعیت مطلق دسترسی اسکیل به ابزارهای
  ردهٔ «پول» مگر با تأیید موردی**.

### لایهٔ ۴ — حلقهٔ عامل و حافظه

- **برنامه‌ریز** هدف را به گام‌های ابزاری می‌شکند · **مجری** اجرا می‌کند · **ناظر** نتیجه
  را با KPI می‌سنجد و در صورت انحراف، اصلاح می‌کند.
- **حافظه در سه لایه (تصمیم نهایی ۱۴۰۶/۰۶/۰۱):**
  ۱. **دانش (حافظه معنایی):** بخش‌بندی‌شده در ۷ حوزه متناظر با کارهای پادو:
     طراحی قالب · اقتصادی/کسب‌وکار · فنی/عملیات · بازاریابی/محتوا · مشتری/پشتیبانی ·
     حقوقی/قوانین · امنیت. منابع پایه (معرفی‌شده توسط تیم ما، مثل ۷ مرجع طراحی قالب)
     تغییرناپذیرند. اگر پاسخ در دانش موجود پیدا نشد، پادو تحقیق می‌کند، منبع معتبر
     می‌یابد، اعتبارسنجی می‌کند، و یافته را با ثبت منبع به بخش مربوطه می‌افزاید —
     دانش به مرور انباشت می‌شود.
  ۲. **تجربه (حافظه الگویی):** الگوهای تعمیم‌یافته از چهار منبع: رفتار/ترجیحات ادمین،
     واکنش واقعی مشتری (نتیجه کمپین، قیمت، طراحی، نرخ تبدیل، فروش)، عملکرد خود پادو
     (کدام روش‌ها/کدها موفق یا ناموفق بود)، و **رفتار و نتایج رقبا** (چه کاری نتیجه
     مثبت/منفی داده تا از تکرار شکست پرهیز شود و موفقیت‌ها شبیه‌سازی/بهبود داده شوند).
     هدف مطلق این لایه: **حذف آزمون و خطای مجدد**. قبل از هر کار جدید، الگوهای
     موفق مشابه تکرار و الگوهای ناموفق پرهیز می‌شوند.
  ۳. **خاطره (حافظه بلندمدت دائمی):** از لحظه افتتاح فروشگاه تا زمان تعطیلی آن تمام
     رخدادها را ذخیره می‌کند: تمام رفتارها و درخواست‌های ادمین، تمام رفتارها و تعاملات
     مشتریان، و کل کارهای روزمره پادو. به‌دلیل وجود اطلاعات بسیار حساس (اطلاعات شخصی
     مشتریان، داده‌های مالی، اطلاعات محرمانه فروشگاه)، این لایه مجهز به **لایه امنیتی
     فوق‌العاده** است (رمزنگاری قوی در حالت استراحت و انتقال، دسترسی حداقلی، ایزوله
     کامل per-tenant، ردیابی کامل دسترسی، ماسک‌سازی داده‌های حساس حتی در بازیابی).

  **مکمل‌های ضروری (لایه مستقل نیستند):**
  - **حافظه کاری (کوتاه‌مدت):** زمینه لحظه‌ای کارهای فعال (مثل جلسه ویرایش قالب یا
    کمپین در حال اجرا) را به صورت موقت نگه می‌دارد. پس از پایان هر کار، فقط داده‌های
    ارزشمند به لایه‌های دانش/تجربه/خاطره منتقل و بقیه اطلاعات موقت پاک می‌شوند تا
    حافظه حجیم و آشفته نشود.
  - **لایه نگهداشت و خودبازبینی دوره‌ای:** فرایند پس‌زمینه خودکار هفتگی/ماهانه که:
    محتوای منقضی شده دانش را به‌روزرسانی/حذف می‌کند، الگوهای تجربه را با داده‌های جدید
    راستی‌آزمایی و الگوهای نادرست را پاک می‌کند، داده‌های خاطره را پس از اتمام دوره
    نگهداری قانونی حذف/ناشناس‌سازی می‌کند، و هرگونه دسترسی غیرمجاز یا ناهنجاری را
    فوراً گزارش می‌دهد.
  همه **کاملاً ایزوله per-tenant** برای داده‌های فروشگاه؛ الگوهای تجربه سراسری (که
  در هیچ فروشگاه خاصی هویت مشتری/ادمین را فاش نمی‌کنند) به صورت ناشناس در لایه تجربه
  مشترک قابل استفاده‌اند.
- **یادگیری از نتیجه:** هر اقدام یک KPI دارد؛ نتیجه ثبت می‌شود، به الگو تبدیل می‌گردد
  و در تصمیم بعدی وزن می‌گیرد. این همان چیزی است که «نتیجهٔ چشمگیر» (بند ۱۴) را
  می‌سازد، نه هوشمندی مدل.

### لایهٔ ۵ — مغز تجاری (بند ۱۳)

خواستهٔ «استاد تجارت» با پرامپت حل نمی‌شود. سه تکه دارد:

1. **Playbook ها** — رویه‌های تجاری کدشده و قابل‌ویرایش: راه‌اندازی محصول، بازیابی سبد
   رهاشده، بازگرداندن مشتری خفته، نردبان آپ‌سل، رسیدگی به شکایت، حراج فصلی، تسویهٔ موجودی
   راکد… هر Playbook: شرط شروع، گام‌ها، KPI موفقیت، خط قرمز.
2. **فرمول‌ها (کد، نه مدل)** — حاشیهٔ سود، نقطهٔ سربه‌سر، کف قیمت، RFM، CLV، نرخ تبدیل،
   کشش قیمت، سطح موجودی/نقطهٔ سفارش.
3. **قضاوت مدل** — لحن، خلاقیت محتوا، اولویت‌بندی، مذاکره در چارچوب کف قیمتِ محاسبه‌شده.

### لایهٔ ۶ — حاکمیت: قرارداد ادمین و پادو (بند ۱۱ و ۱۲)

**چرخه:** مشاهده → **پلن** → تأیید ادمین → اجرا → **گزارش** → سنجش نتیجه.

تفاوتی که خود کارفرما گفت (بند ۱۲) این‌طور پیاده می‌شود: ادمین لازم نیست بگوید «چه
می‌خواهم»؛ **خواسته‌ها از پیش در Playbook ها تعریف شده‌اند** و پادو خودش تشخیص می‌دهد
الان کدام Playbook باید اجرا شود. ادمین فقط **تأیید/رد/اصلاح** می‌کند.

**چهار سطح خودمختاری، قابل تنظیم به‌ازای هر حوزه** (نه یک کلید سراسری):

| سطح | رفتار | پیشنهاد پیش‌فرض |
|---|---|---|
| ۰ — مشاهده | فقط گزارش و پیشنهاد | گزارش‌ها |
| ۱ — تأیید موردی | هر اقدام جدا تأیید می‌شود | قیمت، بازگشت وجه، پیام انبوه |
| ۲ — تأیید دسته‌ای | یک پلن، تأیید یک‌جا، اجرای گام‌به‌گام | محتوا، سئو، کمپین |
| ۳ — خودکار در ریل | بدون تأیید، داخل سقف‌های تعریف‌شده، با گزارش پسین | پاسخ اولیهٔ مشتری، برچسب‌گذاری، پیگیری ارسال |

**سقف‌های ایمنی (Guardrails) که هرگز شکسته نمی‌شوند:** حداکثر درصد تغییر قیمت، کف حاشیهٔ
سود، سقف تعداد پیام روزانه، ممنوعیت تماس در ساعات خاص، سقف مبلغ بازگشت وجه بدون انسان،
و «هر کاری که پول جابه‌جا می‌کند idempotent است و لاگ دارد».

**اعتمادسازی تدریجی:** فروشگاه از سطح ۱ شروع می‌کند؛ هر حوزه که پادو در آن N تصمیم
تأییدشدهٔ پیاپی با KPI مثبت داشت، **پیشنهاد ارتقا به سطح بالاتر** می‌دهد. تصمیم با ادمین.

---

## ۶. فازبندی بر اساس مسیر تجاری (خواستهٔ کارفرما در بند ۹)

مسیر تجاری استاندارد: **جذب → تبدیل → تحویل → نگهداشت**. فازها روی همین سوار می‌شوند و
هر فاز **به‌تنهایی ارزش قابل‌لمس** دارد (اصل «هر فاز باید قابل تحویل باشد»).

| فاز | نام | محتوا | خروجی محسوس برای ادمین |
|---|---|---|---|
| **۰** | **کلونِ کامل (بند ۱۶)** | یک نسخهٔ **عیناً معادل Claude Code**، بدون حذف هیچ قابلیتی، با **Model Router** (OpenRouter / OpenAI-compatible / ایرانی / محلی)، Skills، Plugins، MCP، Hooks، Permissions، Sub-agents، Sandbox، headless | «همان Claude Code را دارم، با هر مدلی که بخواهم» |
| **۰.۵** | **اتصال به دنیای ما** | پروفایل‌های مجوز، لاگ/گزارش، پیش‌نمایش و بازگشت، اتصال به سایت و پنل، اجرای امن روی نصب فروشگاه | «روی فروشگاه من کار می‌کند و کارش برگشت‌پذیر است» |
| **۱** | **راه‌اندازی و کاتالوگ** | ورود آسان محصول (عکس/صدا/دسته‌ای)، غنی‌سازی، دسته‌بندی، قیمت اولیه، سئو، ترجمه | «محصولاتم را دادم، بقیه‌اش را خودش کرد» (بند ۱۰) |
| **۲** | **قالب و ظاهر فروشگاه** | طراحی/شخصی‌سازی قالب، بلوک‌ها، صفحهٔ فرود، سرعت و موبایل — روی محیط ایزوله با پیش‌نمایش | «سایتم را خودش ساخت و من فقط تأیید کردم» |
| **۳** | **مشتری و فروش** | CRM، سگمنت‌بندی RFM، پیشنهاد شخصی، سبد رهاشده، مذاکره/تخفیف در چارچوب کف قیمت، آپ‌سل | «فروش بالا رفت و می‌دانم چرا» |
| **۴** | **بازاریابی و محتوا** | تقویم محتوا، تولید و انتشار پست/ریلز/استوری، قیف کامنت→دایرکت، سئو، تبلیغات، مارکت‌پلیس‌ها | «پیجم بدون من فعال است» |
| **۵** | **سفارش، ارسال، پیگیری** | پردازش سفارش، مسیر و پیک، برچسب، COD، اطلاع‌رسانی، رصد تأخیر | «سفارش‌ها خودشان راه می‌افتند» |
| **۶** | **پشتیبانی پس از فروش** | تیکت، مرجوعی، گارانتی، شکایت، نظرسنجی | «مشتری معطل نمی‌ماند» |
| **۷** | **وفاداری و رشد** | باشگاه مشتریان، بازگشت مشتری خفته، VIP، گیمیفیکیشن، آموزش | «مشتری برمی‌گردد» |
| **۸** | **مالی و مدیریتی** | داشبورد مدیریتی، سودآوری، پیش‌بینی، موجودی، حسابداری/مؤدیان | «می‌دانم کجای کارم» |
| **۹** | **خودمختاری** | ارتقای تدریجی سطح‌ها بر پایهٔ KPI | «دیگر فقط نظارت می‌کنم» (بند ۹) |

**ترتیب:** ۰ و ۰.۵ اجباراً اول. بعد ۱ → ۲ → ۳ → ۴ → ۵ → ۶ → ۷ → ۸ → ۹.
(۳ قبل از ۴ عمدی است: اول باید بلد باشیم بفروشیم و اندازه بگیریم، بعد ترافیک بیاوریم؛
بازاریابی روی قیف نابلد فقط هزینه است.)

> **این فازبندی موقت است.** تا «کارنامهٔ کارها» (بخش ۷) کامل نشود، ترتیب و محتوای فازها
> نهایی نیست — چون هنوز همهٔ کارهایی که کارفرما از ابزار می‌خواهد روی میز نیست (بند ۱۷).

> ### 🔄 تصحیح کارفرما (۱۴۰۵/۰۶/۳۱) — پیش از هر استفاده از این جدول
>
> روش کار پادو تعیین شد: «مراحل را یک‌به‌یک بررسی و جمع‌بندی می‌کنیم؛ در آخر یک پلن طراحی
> می‌دهیم؛ بعد از تأییدِ من شروع می‌کنیم به اجرا.» و تصحیح صریح: **اولین کاری که پادو باید
> بعد از نصب فروشگاه انجام دهد، «طراحی قالب» است — نه ثبت محصول/کاتالوگ.** یعنی جای فاز ۱ و ۲
> این جدول در ترتیب واقعی برعکس است. این جدول تا پایان بررسی مرحله‌به‌مرحله بازبینی و بازنویسی
> می‌شود؛ جمع‌بندی پیشنهادیِ بررسیِ مرحلهٔ «طراحی قالب» در `HANDOFF-PROMPT.md §۴-د` ثبت شده
> و **هنوز تأیید کارفرما را ندارد**.
>
> **به‌روزرسانی (۱۴۰۶/۰۵/۳۱ ≈ ۲۲ اوت ۲۰۲۶):** در ابتدای جلسه کارفرما دستور داد فروشگاه
> نمونه با المنتور ساخته شود و این به‌عنوان «پیش‌فرض المنتور» ثبت شد، اما در پایان همین
> جلسه تصمیم **بازبینی و نهایی گردید**: به‌دلیل حجم بالای المنتور (~۱۰۸ مگابایت)،
> **المنتور و Hello Elementor کلاً از نصب پیش‌فرض حذف شدند** (تصمیم نهایی §۸). طراحی
> قالب مرحلهٔ ۱ پادو یک **قالب فرزند FSE** روی قالب هسته وردپرس تولید می‌کند و به
> صفحه‌ساز ثالث وابسته نیست.

---

## ۷. کارنامهٔ کارها — فهرستی که باید کارفرما کاملش کند

بند ۱۷ می‌گوید هنوز نمی‌دانم ابزار قرار است چه کارهایی بکند. این بخش جای ثبت آن‌هاست.
چیزهایی که تا امروز می‌دانم را نوشته‌ام تا کارفرما فقط **اصلاح و اضافه** کند، نه اینکه از
صفر بنویسد. علامت 🆕 یعنی کارفرما تازه مطرح کرده.

| # | کار | ابزار لازم | وضعیت فهرست |
|---|---|---|---|
| ۱ | 🆕 **طراحی و شخصی‌سازی قالب سایت فروشگاه** | فایل + شل + پیش‌نمایش | تأییدشده |
| ۲ | معرفی و ثبت انبوه محصولات (با عکس/صدا) | دامنه‌ای + AI | از اسناد |
| ۳ | تولید محتوای اینستاگرام و انتشار | دامنه‌ای + AI | از اسناد |
| ۴ | قیف کامنت→دایرکت و پاسخ‌گویی | دامنه‌ای | از اسناد |
| ۵ | سئو و ثبت در گوگل/بینگ/ترب/ایمالز | دامنه‌ای | از اسناد |
| ۶ | مارکت‌پلیس‌ها (دیجی‌کالا/دیوار/باسلام) | دامنه‌ای | از اسناد |
| ۷ | راهنمایی و اتوماسیون اینماد | دامنه‌ای + وب | از اسناد |
| ۸ | دامنه و DNS | دامنه‌ای | از اسناد |
| ۹ | قیمت‌گذاری و تخفیف | دامنه‌ای + فرمول | از اسناد |
| ۱۰ | سفارش، ارسال، پیک، COD | دامنه‌ای | از اسناد |
| ۱۱ | پشتیبانی و مرجوعی | دامنه‌ای | از اسناد |
| ۱۲ | ترجمه و چندزبانه | دامنه‌ای | از اسناد |
| ۱۳ | گزارش و داشبورد مدیریتی | فقط خواندن | از اسناد |
| ۱۴ | … | | **جای شما** |

**سؤال‌های راهنما برای کامل‌کردنش:** کارهایی که امروز خودتان یا ادمین‌ها **دستی** انجام
می‌دهید و وقت می‌برد چیست؟ کدام کارها **تخصص** می‌خواهد و ادمین بلد نیست؟ کدام‌ها **تکراری
و روزمره** است؟ و کدام کار است که اگر ابزار انجامش دهد، مشتری می‌گوید «این چیز دیگری است»؟

---

## ۸. آنچه «طراحی قالب» به معماری تحمیل می‌کند

> **پیش‌نیاز قطعی (تصمیم نهایی کارفرما — ۱۴۰۶/۰۵/۳۱ ≈ ۲۲ اوت ۲۰۲۶):** هر فروشگاهٔ تازه‌تأسیس
> در IGBZ **بدون صفحه‌ساز ثالث و فقط با قالب پیش‌فرض هسته وردپرس** (مثلاً Twenty Twenty-Five)
> بالا می‌آید. المنتور و Hello Elementor به‌دلیل حجم بالا (~۱۰۸ مگابایت) از نصب پیش‌فرض
> **حذف شدند** و مدیر فروشگاه می‌تواند بعداً در صورت تمایل هر صفحه‌سازی نصب کند. مرحلهٔ ۱
> پادو (طراحی قالب) یک **قالب فرزند FSE** (روی قالب هسته) تولید می‌کند که شامل
> `theme.json` + تمپلیت‌های HTML + CSS + محتوا/تصاویر است و برای نصب یا ویرایش درگ‌اند‌دراپ
> به هیچ صفحه‌ساز ثالثی (از جمله Elementor) وابسته نیست. خروجی به‌صورت یک بستهٔ `*.zip`
> از قالب فرزند (به‌اضافهٔ اختیاری تمپلیت‌های JSON بلوکی وردپرس) از دروازه امنیتی بک‌اند
> عبور می‌کند و در preview نصب می‌شود. نصب پیش‌فرض هسته وردپرس/ووکامرس در بوت‌استرپ
> `n`امین فروشگاه انجام می‌شود و جزو مراحل «نصب فروشگاه» است، نه بخشی از کاری که
> پادو هر بار باید انجام دهد.

> **منابع الهام مرحلهٔ ۱ طراحی قالب (تصمیم کارفرما — ۱۴۰۶/۰۵/۳۱):**
> ۱. ✅ **اسکیل `nextlevelbuilder/ui-ux-pro-max-skill`** (کامیت `bc826e2`) — هفت اسکیل با
>    فرمت `SKILL.md` + مراجع جدا (قواعد UX: کنتراست 4.5:1، alt، ناوبری کیبورد، لمس
>    حداقل 44×44px، WebP/lazy/CLS<0.1، و الگوی اسکیل `brand`). این اسکیل‌ها
>    **به‌صورت پیش‌فرض در ویرا/پادو نصب هستند** (پوشهٔ `.vira/skills/` همین مخزن:
>    `banner-design`, `brand`, `design`, `design-system`, `slides`, `ui-styling`,
>    `ui-ux-pro-max`) و به‌عنوان الگوی ساختار اسکیل و قواعد UX پایه برای
>    `igbz-store-theme` استفاده می‌شوند (نه مستقیم برای خروجی قالب فارسی — فونت و RTL
>    خودمان را دارند).
> ۲. ✅ **`21st.dev`** — وب‌سایت مرجع الهام بصری (کامپوننت‌های React/Tailwind مدرن: WebGL
>    hero، shader، liquid/metal، wave، gradient bar، کارت شیشه‌ای، hoverهای زنده). فقط
>    **مرجع بصری** برای الگوی «عمق/سه‌بعدی دو پله» در CSS است؛ اسکیل فایلی نیست و چیزی
>    در ویرا نصب نمی‌شود.
> ۳. ❌ **MCP/Abilities داخلی ووکامرس** — کارفرما دستور داد: «با ووکامرس MCP کاری نداریم».
>    دلیل: احراز هویت با کلید REST مسیر موازی است که `CoreSurfaceGuard` را دور می‌زند و
>    تا حل این شکاف امنیتی در سند امنیت، نباید در فروشگاه‌های تولیدی فعال شود (جزئیات در
>    `HANDOFF-PROMPT.md §۵ یافته ۱` و
>    `امنیت و مراقبت/منابع/WordPress/WooCommerce-MCP-Abilities.md`).

این کار (بند ۱۷) یک خواستهٔ ساده به‌نظر می‌رسد ولی سه چیز را قطعی می‌کند:

1. **هسته باید به فایل‌سیستم سایت دسترسی داشته باشد** — یعنی روی همان سروری اجرا شود که
   وردپرس هست (یا از راه یک کانال امن به آن وصل شود). این به سؤال «سرویس جدا یا داخل
   وردپرس؟» جهت می‌دهد: **سرویس جدا، ولی هم‌مکان با سایت.**
2. **بدون محیط ایزوله و پیش‌نمایش، این کار خطرناک است.** ویرایش قالب سایت زنده = ریسک
   از کار افتادن فروشگاه. پس الگوی Sandcastle اینجا اختیاری نیست: کپی/نسخهٔ کاری → تغییر →
   **پیش‌نمایش** → تأیید ادمین → اعمال → امکان **بازگشت با یک کلیک**.
3. **نسخه‌بندی لازم است.** تغییرات قالب باید تاریخچه داشته باشد (گیت یا معادلش)، وگرنه
   «برگرداندن» شعار است.

---

## ۹. چه چیزی از امروز داریم و چه چیزی نداریم

**داریم (پایهٔ محکم — پادو روی این‌ها سوار می‌شود):** ۷۰ جدول، ۲۸ صفحهٔ ادمین،
۱۴۵ مسیر REST، سرویس‌های واقعی پرداخت/لجستیک/مارکت‌پلیس/سئو/ترجمه/LMS/VIP/قیف،
`AiProviderInterface` و `AiStudioService`، `AiCreditsService` (لجر اعتبار)، ماژول `fx`
(پرداخت ارزی ابزارها)، `ChatPlaceClient` (و مسیر MCP در آینده)، دو اسکیل نوشته‌شده
(`PROMPTS-SEO-PADO.md`, `PROMPT-IG-GROWTH-PADO.md`)، و اسکیل مرجع
`ui-ux-pro-max-skill` (هفت اسکیل UX، کامیت `bc826e2`) که به‌صورت پیش‌فرض در
`.vira/skills/` نصب است (git-ignored).

**نداریم (کار فاز ۰):** حلقهٔ عامل، رجیستری ابزار و مجوز، Model Router، کلاینت MCP،
رجیستری اسکیل، حافظه/پروفایل فروشگاه، موتور Playbook، دفترچهٔ KPI، صفحهٔ حاکمیت و تأیید.

---

## ۱۰. سؤالات باز — این‌ها را باید با هم ببندیم

1. **محل اجرا:** پادو داخل خود افزونهٔ وردپرس اجرا شود، یا یک **سرویس جدا** (مثلاً
   Node/Python) که افزونه از طریق REST به آن وصل می‌شود؟ (توصیهٔ اولیهٔ من: سرویس جدا؛
   حلقهٔ عامل کار طولانی است و PHP روی وب‌سرور جای اجرای آن نیست.)
2. **مدل پیش‌فرض** برای شروع کدام باشد؟ (ایرانی ریالی برای همه‌چیز، یا ترکیبی؟)
3. **مالکیت هزینه:** هزینهٔ توکن با اپراتور است یا ادمین فروشگاه؟ (لجر و متراژ آماده است.)
4. **سطح خودمختاری پیش‌فرض** برای فروشگاه تازه: پیشنهاد من سطح ۱ در همه‌جا.
5. **مرز داده:** آیا دادهٔ فروشگاه اجازهٔ خروج به مدل خارجی را دارد؟ (اگر نه، فقط ردهٔ
   ایرانی/محلی — که روی معماری اثر مستقیم دارد.)
6. **اسکیل‌ها:** فقط اسکیل‌های تأییدشدهٔ مدیر ارشد نصب شوند یا ادمین فروشگاه هم بتواند؟
7. **زبان و لحن:** فارسی پیش‌فرض؛ لحن برند از کجا تعریف شود؟
8. **«نتیجهٔ چشمگیر» را با چه عددی می‌سنجیم؟** (نرخ تبدیل؟ فروش؟ زمان صرف‌شدهٔ ادمین؟)
   بدون این عدد، فاز ۸ معنا ندارد.

---

## ۱۱. امکان‌سنجی: «دقیقاً مثل Claude Code، ولی با هر پرووایدری»

پاسخ کوتاه: **بله، و لازم نیست از صفر ساخته شود.** امروز چند هارنس متن‌باز بالغ وجود دارد
که همان سطح قابلیت را دارند و **از ابتدا provider-agnostic** اند (OpenAI-compatible،
OpenRouter، مدل محلی). دو استاندارد کلیدی هم دیگر انحصاری نیستند: **MCP** پروتکل باز است و
**فرمت `SKILL.md`** را چند ابزار دیگر هم پذیرفته‌اند — یعنی «نصب اسکیل آماده» شدنی است.

### ۱۱.۱ جدول قابلیت‌ها

| قابلیت Claude Code | برای پادو | یادداشت |
|---|---|---|
| حلقهٔ عامل + Plan mode | ✅ | در همهٔ هارنس‌های متن‌باز هست |
| ابزارهای داخلی (Read/Write/Bash/Grep/WebFetch/…) | ✅ **کامل و دست‌نخورده** | لازمهٔ کارهایی مثل طراحی قالب (بند ۱۶) |
| Sub-agent | ✅ | محتوا / قیمت / پشتیبانی به‌موازات |
| Hooks (۳۰+ رویداد) | ✅ | ما ۸–۱۰ رویداد اول را لازم داریم |
| Permissions (allow / ask / deny) | ✅ **حیاتی** | ستون فقرات «تأیید ادمین» |
| Skills — نصب اسکیل آماده | ✅ | فرمت `SKILL.md` |
| Plugins + Marketplace | ✅ | بستهٔ اسکیل + MCP + هوک |
| MCP client (stdio/HTTP) | ✅ | استاندارد باز |
| Slash commands | ✅ | برای ما: دستورهای تجاری (`/کمپین`, `/گزارش`) |
| Memory / فایل دستورالعمل پروژه | ✅ | «پروفایل فروشگاه» |
| فشرده‌سازی کانتکست | ✅ | لازم برای کار طولانی |
| Checkpoint / rewind | ✅ و **مهم‌تر از کد** | بازگشت عملیات تجاری |
| Sandbox / اجرای ایزوله | ✅ | الگوی Sandcastle |
| حالت headless و خروجی JSON | ✅ **حیاتی** | کرون و اجرای بی‌ناظر |
| SDK برای تعبیه | ✅ | پنل و اپ از همین می‌خورند |
| سوئیچ مدل وسط کار | ✅ | ارزان برای کار ساده، گران برای قضاوت |
| شمارش هزینه و سقف | ✅ | لجر `ig_ai_credit_ledger` موجود |
| IDE / GitHub App / teleport / voice | ⛔ بی‌ربط | حذف می‌شوند |

### ۱۱.۲ سه چیزی که «دقیقاً همان» نمی‌شود — صادقانه

1. **کیفیت خروجی به مدل بند است، نه به هارنس.** هارنس را می‌شود مو‌به‌مو ساخت؛ اما دقت
   ابزارفراخوانی مدل‌ها یکسان نیست. علاجش مهندسی است نه آرزو: اسکیمای سخت‌گیرانه، اعتبارسنجی
   خروجی، تلاش مجدد، و یک «بازبین» که کار را قبل از اعمال چک کند.
2. **کش پرامپت و اقتصاد توکن** بین پرووایدرها فرق دارد؛ همان کار با مدل ارزان‌تر ممکن است
   گران‌تر تمام شود. مسیریابی بر اساس نوع کار، پاسخ این است.
3. **قابلیت‌های گره‌خورده به زیرساخت ابری آنتروپیک** (نشست ابری، ریموت، تیم) نه شدنی است
   نه لازم.

### ۱۱.۳ سه مسیر پیاده‌سازی

| مسیر | شرح | زمان | ریسک |
|---|---|---|---|
| **الف) فورک/تعبیهٔ هارنس متن‌باز** | هسته آماده (حلقه، ابزار، مجوز، MCP، اسکیل، هوک) + لایهٔ IGBZ | **هفته‌ها** | هم‌گام ماندن با upstream |
| ب) ساخت روی یک فریم‌ورک عامل | کنترل بیشتر، کار بیشتر | ماه‌ها | بازنویسی چیزهای حل‌شده |
| ج) از صفر | کنترل کامل | چند ماه | بازاختراع hooks/permissions/MCP |

**توصیهٔ من: مسیر الف.** هارنس امروز عملاً کالای رایگان است؛ **۹۰٪ ارزش پادو در
ابزارهای دامنه‌ای، Playbook ها و ریل‌های ماست** — همان چیزی که هیچ‌کس دیگر ندارد.

### ۱۱.۴ نکتهٔ ایران

- پرووایدرهای ایرانی معمولاً **OpenAI-compatible** اند؛ پس یک آداپتور، هم آن‌ها را پوشش
  می‌دهد هم OpenRouter و هم مدل محلی.
- پرداخت OpenRouter از ایران با **ماژول `fx`** که همین حالا داریم حل می‌شود.
- برای دادهٔ حساس، مدل خودمیزبان روی سرور خودمان گزینهٔ سوم است.

---

## ۱۲. پیشنهاد گام بعدی

این سند «ذهنیت» است، نه نقشهٔ ساخت. طبق خواستهٔ کارفرما (بند ۱۵) عجله نمی‌کنیم. پیشنهادم:

1. ~~انتخاب هارنس و ساخت فاز ۰~~ — **انجام شد** (بخش ۱۳).
2. **کارنامهٔ کارها (بخش ۷) را کارفرما کامل کند** — تا این کامل نشود، فازبندی نهایی نیست.
3. سؤالات بخش ۱۰ را ببندیم (مهم‌ترینشان: خروج داده به مدل خارجی، مالکیت هزینه، و عددی که
   «نتیجهٔ چشمگیر» را می‌سنجد).
4. فاز ۰.۵: اتصال ویرا به دنیای فروشگاه (پروفایل‌های مجوز، ابزارهای دامنه‌ای، پیش‌نمایش و
   بازگشت) — با سند طراحی جدا و تأیید کارفرما.

> یادآوری قانون کار: تا اینجا هیچ کدی نوشته نشده و هیچ چیزی کامیت نشده. این سند فقط
> «ذهنیت مشترک» است.

---

## ۱۳. فاز ۰ — ساخته شد (ویرا نسخهٔ ۰.۲)

کارفرما تصمیم گرفت: **اول یک کلون کامل، بدون حذف هیچ چیز؛ تخصصی‌سازی به‌مرور.** نام ابزار
**ویرا** و نسخهٔ اول **دسکتاپ با نصب آسان**. این فاز تمام شد.

### چه چیزی ساخته شد

| بخش | وضعیت |
|---|---|
| حلقهٔ عامل (plan → tool → observe → repeat) با سقف گام | ✅ |
| ۹ ابزار داخلی: read/write/edit/list/glob/grep/bash/web_fetch/todo | ✅ |
| لایهٔ پرووایدر به سبک Cline: ۱۵ پرووایدر + سازگار با OpenAI/Anthropic + واکشی فهرست مدل | ✅ |
| سه حالت مجوز (plan/default/auto) + قواعد پیشوندی + محدودهٔ پوشهٔ کاری | ✅ |
| **MCP** با SDK رسمی (stdio و HTTP) | ✅ |
| **اسکیل** با فرمت استاندارد `SKILL.md` — نصب اسکیل آماده | ✅ |
| **پلاگین و مارکت‌پلیس** (گیت یا مسیر محلی؛ اسکیل + دستور + MCP + هوک) | ✅ |
| **زیرعامل** (`task`) با محدودیت عمق | ✅ |
| **هوک** با قابلیت توقف ابزار و تزریق کانتکست | ✅ |
| **دستورهای اسلش** داخلی و سفارشی با تکمیل خودکار | ✅ |
| فشرده‌سازی کانتکست + `VIRA.md` + نشست‌ها + شمارش توکن | ✅ |
| رابط فارسی (RTL) با استریم و پنل افزونه‌ها | ✅ |
| پوستهٔ Electron | نوشته شده، **در سندباکس اجرا نشده** (باینری Electron مسدود است) |

### تصمیم فنی که شرایط تحمیل کرد

سندباکس توسعه نه Electron می‌تواند دانلود کند، نه bun دارد نه rust. یعنی فورک OpenCode یا
Codex CLI کدی می‌داد که **حتی یک بار هم اجرا نمی‌شد**. پس هسته با **Node خالص** نوشته شد؛
تنها وابستگی‌اش `@modelcontextprotocol/sdk` است. سود جانبی‌اش: نصب برای کاربر یک دستور است.

### آنچه تأیید زنده پیدا کرد (و در تست قفل شد)

- **زیرعامل می‌توانست بی‌نهایت زیرعامل بسازد.** حالا زیرعامل ابزار `task` نمی‌گیرد.
- **رویداد `idle` زیرعامل بیرون درز می‌کرد** و رابط کاربری فکر می‌کرد کل کار تمام شده.
- **تایمرهای پاک‌نشدهٔ MCP** پروسه را ۱۲۰ ثانیه بعد از پایان کار زنده نگه می‌داشتند
  (سوئیت تست از ۱۲۱ ثانیه به ۰٫۷ ثانیه رسید).
- مرز کلمهٔ `\b` در جاوااسکریپت روی حروف فارسی کار نمی‌کند.

### تست

**۴۰ تست بدون وابستگی** (`node vira/test/run.mjs`) — شامل اتصال به یک **سرور MCP واقعی**
و هر دو پروتکل پرووایدر با سرور جعلی. چهار جهش عمدی در کد زده شد و هر چهار بار تست درست
قرمز شد.

---

---

## ۱۴. فاز ۰ (ادامه) — رابط کامل Claude Code (ویرا نسخهٔ ۰.۳)

کارفرما گفت: «یک UI کلاد کد **کامل** با تمام امکانات کلاد کد» — نه یک پنل اضافه. نسخهٔ ۰.۲
چیدمان را داشت ولی امکانات را نه؛ نسخهٔ ۰.۳ فاصله را بست.

| افزوده | وضعیت |
|---|---|
| **تنظیمات ۱۳ تبی** (Customize) — هر چیزی که در JSON بود، حالا فرم دارد | ✅ |
| **افزودن/آزمودن/حذف کانکتور MCP از داخل برنامه** (stdio و HTTP، سراسری و پروژه‌ای) | ✅ |
| ابزارهای تازه: `multi_edit`، `bash --background`، `bash_output`، `kill_shell`، `web_search`، `exit_plan_mode`، `ask_user_question` | ✅ |
| **چک‌پوینت و بازگشت** (فایل‌ها + بریدن گفتگو) | ✅ |
| **کارت تأیید نقشه** و **کارت پرسش چندگزینه‌ای** | ✅ |
| دیف واقعی برای `write_file`/`edit_file`/`multi_edit` + پیش‌نمایش دیف در دروازهٔ تأیید | ✅ |
| ریل کناری: کارهای جاری، شل‌های پس‌زمینه، چک‌پوینت‌ها، تغییرات گیت | ✅ |
| اشارهٔ فایل با `@` (جستجوی فازی)، پالت فرمان Ctrl+K، ۱۲ میان‌بر | ✅ |
| زیرعامل تعریف‌شدهٔ کاربر (`~/.vira/agents/*.md`) با ابزار و مدل خودش | ✅ |
| هزینه و دفتر مصرف روزانه، خروجی گفتگو، تشخیص خرابی (`/doctor`) | ✅ |
| صف پیام هنگام مشغولی، تغییر نام و حذف نشست، عنوان قابل‌ویرایش | ✅ |

**تست:** ۶۴ تست واحد + **۱۹ آزمون زنده** روی یک سرور واقعی با SSE (`node vira/test/live.mjs`).
شش جهش عمدی زده شد و هر شش بار تست درست قرمز شد.

**سه باگ واقعی که آزمون زنده پیدا کرد:** ساختار حلقه‌ای در نوار گفتگو (رویداد `rewound`
خودش را حمل می‌کرد)، گم‌شدن جواب کارت پرسش، و آبشار خطا وقتی نوبت قبلی تمام نشده بود.

جدول کامل برابری با Claude Code و فهرست آنچه عمداً ساخته نشد: `vira/DESIGN-UI-PARITY.md`.

### دور دوم فاز ۰ — ویرا ۰.۴

| افزوده | وضعیت |
|---|---|
| **حالت بدون‌رابط** `vira -p` با خروجی text/json/stream-json و کد خروج معنادار | ✅ |
| **SDK**: `query()` و `createVira()` برای تعبیه در برنامهٔ دیگر (پیش‌نیاز پادو) | ✅ |
| **ورودی تصویر** در هر دو پروتکل + نمایش در گفتگو | ✅ |
| **بلوک فکر** برای مدل‌های استدلالی | ✅ |
| **اجرای موازی ابزارهای خواندنی** با حفظ ترتیب نتیجه‌ها | ✅ |
| **پرامپت و منبع MCP** (`/mcp__سرور__پرامپت` و `read_mcp_resource`) | ✅ |
| انتخاب سریع مدل از کامپوزر | ✅ |

**تست:** ۷۶ تست واحد + ۲۳ آزمون زنده؛ چهار جهش تازه، چهار قرمزشدن درست.

نکتهٔ مهم برای پادو: **SDK همان دری است که فاز ۰.۵ از آن رد می‌شود.** پنل فروشگاه لازم
نیست سرور ویرا را بالا بیاورد؛ می‌تواند `createVira()` را با پروفایل مجوز دامنه‌ای صدا بزند
و رویدادها را در پنل خودش نشان بدهد.

### دور سوم فاز ۰ — ویرا ۰.۵ (دو تصمیم کارفرما)

کارفرما پرسید چرا چهار قابلیت را «عمداً نساختم». جواب: دوتایش بهانه بود و در ۰.۴ ساخته شد؛
دوتای دیگر را خودش تعیین تکلیف کرد.

| تصمیم کارفرما | نتیجه |
|---|---|
| **`NotebookEdit` ساخته شود** | ابزار `notebook_edit` با سه حالت replace/insert/delete؛ `read_file` هم `.ipynb` را خوانا نشان می‌دهد |
| **سندباکس: گزینهٔ «ج» (کانتینر)** | `bash` و شل‌های پس‌زمینه داخل Docker/Podman؛ شبکهٔ بسته، سقف منابع، `--cap-drop ALL`؛ پیش‌فرض خاموش؛ **شکستِ بسته** |

**تست:** ۹۲ تست واحد + ۲۵ آزمون زنده؛ پنج جهش تازه، پنج قرمزشدن درست. چون این سندباکس نه
داکر دارد نه پادمن، مسیر کانتینر با یک **موتور جعلی در PATH** آزموده شد.

**نکتهٔ حاکمیتی برای پادو:** سندباکسِ پروسه، جلوی خطای تجاری را نمی‌گیرد. خطر واقعی
پادو «قیمت غلط برای پنج هزار مشتری» است، نه `rm -rf`. حفاظ‌های دامنه‌ای — اجرای آزمایشی،
سقف گیرنده، آستانهٔ تأیید انسانی، بازگشت‌پذیری — باید در سند فاز ۰.۵ طراحی شوند.

---

## ۱۵. تصمیم‌های تأییدشدهٔ ۱۴۰۵/۰۵/۲۶ — استقرار و هاب پرووایدر

این‌ها در گفتگوی همان تاریخ تأیید شدند و مبنای دو سند طراحی بعدی‌اند. **هنوز پیاده نشده‌اند.**

### تصحیح مهم کارفرما دربارهٔ «تخصصی بودن»

> «وقتی من گفتم مزیت ویرا این هست که همه‌فن‌حریف نیست و یک سری کارهای مشخص برایش تعریف
> شده، این مربوط به زمانی هست که ویرا به‌صورت خودکار دارد کارهای روزمرهٔ فروشگاه را انجام
> می‌دهد. یعنی این محدودیت برای حالت خودکار هست. اما چیزی که ما داریم الان درست می‌کنیم
> نسخه‌ای هست که در پنل مدیریت مرکزی می‌نشیند و مدیران با آن کار می‌کنند… پس ویرا باید با
> تمام امکانات در اختیار مدیر باشد.»

پس: **تخصصی‌سازی با کم‌کردن توانایی به‌دست نمی‌آید، با تعریف کار و بستن مجوز به‌دست می‌آید.**
نسخهٔ پنل باید همه‌کاره باشد — رفع باگ، عیب‌یابی سرور، تغییر طراحی، پیداکردن و وصل‌شدن به
یک API بیرونی.

### استقرار

| # | موضوع | تصمیم |
|---|---|---|
| ۱ | معماری | **پروسهٔ جدا روی لوکال‌هاست**؛ پنل به آن پراکسی می‌کند |
| ۲ | دسترسی از بیرون | **کاملاً ممنوع.** هیچ مسیری از بیرون به ویرا یا سرور باز نیست؛ تنها راه، پنل است |
| ۳ | رسیدن به سرورهای دیگر | **SSH از سرور ویرا** برای ماشین‌هایی که در اختیار ماست، **و API** برای سرویس‌هایی که شل ندارند |
| ۴ | ~~عامل‌های فروشگاه~~ | ❌ **باطل شد (۱۴۰۵/۰۵/۳۱).** پادو یک **سرویس ابری بیرونی** است که ادمینِ هر فروشگاه خودش تهیه می‌کند؛ هیچ پروسه‌ای روی سرور ما ندارد. ما فقط **اسکیل** می‌نویسیم. `DESIGN-DEPLOY-VIRA.md §۱۱` |
| ۵ | «پلن کاری» | **موضوع پادو است، نه ویرا.** در زمان خودش |
| ۶ | دادهٔ فروشگاه به مدل بیرونی | **موضوع پادو است.** تا آن موقع، پیش‌فرض موقت: ماسک‌کردن نام، تلفن، آدرس، ایمیل و کد ملی |
| | جای اجرا | روی **سرور**؛ رابطش در پنل مدیریت مرکزی |
| | ابزار `bash` | **می‌ماند** — ممنوعیت مربوط به دسترسی از بیرون است، نه اجرای فرمان محلی با تأیید مدیر |
| | محیط‌ها | توسعه/آزمایشی/زنده — بسته به **مجوز همان مدیر** |
| | مدیران | چند نفر، تعداد متغیر، **اختیارات نابرابر**. ادمین فروشگاه **هیچ رابطی با ویرا ندارد** |
| | تأیید | در بخش مدیریت **هیچ تغییری بدون تأیید انسانی مدیر** انجام نمی‌شود |
| | نسبت این نسخه با فروشگاه‌ها | **نظارت، و فقط دو کار** (تصحیح کارفرما ۱۴۰۵/۰۵/۳۱): ۱) **به‌روزرسانی اسکیل‌ها** بنا به نیاز · ۲) **گزارش ماهانه** از فعالیت فروشگاه‌ها تا معلوم شود پادو کارش را درست انجام می‌دهد. کنترل زنده، دیدن گفتگوها، و دخالت در کار روزمره **جزو نظارت نیست** |

### بند ۳ — دو مسیر، نه یکی

> **یادداشت تصحیح (همان روز):** در نسخهٔ اول این سند، جواب اولیهٔ کارفرما («فقط API») ثبت
> شده بود. کارفرما بعداً تصریح کرد که پیشنهاد خودِ عامل مبنا باشد. مبنای درست این است:

**SSH مسیر اصلی است** برای ماشین‌هایی که در اختیار ماست — چون «سرور دان شده» را با API
نمی‌شود دید. اگر وب‌سرور بالا نیاید، API ای هم بالا نیست.

**API مسیر مکمل است** برای سرویس‌هایی که اصلاً شل ندارند: پنل میزبانی، CDN، سرویس پایش،
DNS، وردپرسِ سالمِ یک فروشگاه دیگر.

قیدهایی که SSH با خودش می‌آورد و باید رعایت شوند:

- کلید در **مخزن کلید پنل**، با همان الگوی `keyRef` — مقدارش هرگز به مدل نمی‌رسد.
- **فهرست سفید میزبان‌ها.** هر میزبان دیگری رد می‌شود، حتی اگر مدل اصرار کند.
- **شل تعاملی وجود ندارد.** هر فرمان یک فراخوانی ابزار جدا است که از دروازهٔ تأیید رد
  می‌شود — نه یک نشست باز که مدل داخلش هرچه خواست بزند.
- **هر فرمان و خروجی ثبت می‌شود**، با نام مدیری که تأییدش کرده و میزبانی که رویش اجرا شده.
- خروجی قبل از رسیدن به مدل **پاک‌سازی** می‌شود: هر چیزی که شبیه راز باشد ماسک می‌خورد.

قیدهای مسیر API همان‌هایی است که قبلاً نوشته شد: ابزار HTTP احرازشده، کلید از پنل،
فهرست سفید، و یک آداپتور یا کانکتور MCP برای هر سرویس.

### آنچه بند ۲ تحمیل می‌کند

«دسترسی از بیرون کاملاً ممنوع» یعنی:

- سرویس فقط روی `127.0.0.1` گوش می‌دهد. گزینهٔ `--host 0.0.0.0` که امروز در `cli.js` هست،
  در استقرار سرور باید **غیرفعال** باشد، نه فقط پیش‌فرض‌نبودن.
- هویت از پنل می‌آید. ویرا خودش صفحهٔ ورود ندارد و نباید داشته باشد.
- پراکسی پنل باید هر درخواست را با نشست وردپرس و **دسترسی همان مدیر** بسنجد.
- SSE هم از همان پراکسی رد می‌شود؛ اتصال مستقیم مرورگر به پورت ویرا وجود نخواهد داشت.

### هاب پرووایدر — ✅ ساخته شد (ویرا ۰.۷)

جمع‌بندی کامل در **`vira/DESIGN-PROVIDER-HUB.md`**. خلاصه: چند اتصال هم‌زمان (استاندارد و
سازگار، با صفحهٔ تنظیمات جدا)، رجیستری مدل، ترکیب و راهبرد مسیریابی، تشخیص خودکار جنس
درخواست، یادگیری از نتیجه، خروجی سازگار با OpenAI برای بقیهٔ ماژول‌ها، و یک **مدل عیب‌یاب
جدا از هاب** که خطاهای اتصال را دنبال و رفع می‌کند و راه‌حل را در دفتری آزموده ذخیره می‌کند.

کد در `vira/src/hub/` است: نُه راهبرد مسیریابی، امتیازدهی نُه‌عاملی حالت خودکار، مدارشکن
و صدک تأخیر، سه سقف هزینه، کش، نردبان چهارپلهٔ عیب‌یابی با فهرست بستهٔ وصله، و
`GET /v1/models` + `POST /v1/chat/completions` روی لوکال‌هاست.

**نکتهٔ مهم برای پادو:** همان خروجی سازگار با OpenAI، نقطهٔ اتصال بقیهٔ ماژول‌هاست.
مترجم، خلاصه‌ساز، پرامپت‌های سئو و رشد اینستاگرام لازم نیست هیچ‌کدام کلید جدا داشته باشند؛
به `http://127.0.0.1:<پورت ویرا>/v1` وصل می‌شوند و مدل را `auto` می‌گذارند. یک جای کلید،
یک جای هزینه، یک جای سلامت — همان چیزی که تصمیم ۸ می‌خواست.

### سند استقرار — نوشته شد

جمع‌بندی کامل در **`DESIGN-DEPLOY-VIRA.md`** (ریشهٔ مخزن). آن سند همین تصمیم‌ها را
مبنا می‌گیرد و به هشت موردِ «جور نیست»ِ زیر یک‌به‌یک جواب می‌دهد: دروازهٔ پنل و مسئلهٔ
SSE، پروسه به‌ازای هر مدیر (چون هستهٔ امروز تک‌کاربره است)، محدوده‌های فایل‌سیستم به‌جای
یک پوشهٔ کاری، ابزار SSH با فهرست سفید، کارگزار کلید، جدول `igbz_audit` با زنجیرهٔ درهم،
صف واقعی به‌جای `wp_cron`، و بستن در پشتیِ `manage_options`.

**وضعیت: پیشنهاد، منتظر تأیید.** هفت تصمیم هم در بند ۱۵ آن سند منتظر کارفراست.

### آنچه در معماری امروز با این تصمیم‌ها جور نیست

۱. همه‌چیز به یک «پوشهٔ کاری» محدود است — عیب‌یابی سرور ممکن نمی‌شود.
۲. سندباکس شبکه را پیش‌فرض می‌بندد — «برو API صرافی پیدا کن» اجرا نمی‌شود.
۲.۵ `--host 0.0.0.0` وجود دارد و در استقرار سرور باید بسته شود، نه اینکه فقط پیش‌فرض نباشد.
۳. جایی برای کلید سرویس ثالث در خود ویرا نیست (و نباید هم باشد — جایش پنل است).
۴. کار بلندمدت و ناهم‌زمان نداریم؛ هر چیزی در همان نوبت گفتگو تمام می‌شود.
۵. مدل مجوز تک‌کاربره است.
۶. `wp_cron` برای کار بلندمدت کافی نیست — صف واقعی لازم است.
۷. `Logger.php` هست ولی دفتر رویدادِ **منتسب و غیرقابل‌ویرایش** برای تأییدها نیست.
۸. `Capabilities::current_user_can()` هر چیزی را برای `manage_options` باز می‌گذارد؛ برای
   محیط زنده کافی نیست.

---

## ۱۶. اتصال به مخزن گیت — خواستهٔ ۱۴۰۵/۰۵/۲۶

کارفرما تصویری از رابط Arena فرستاد و روی یک چیز انگشت گذاشت: **نوار پایینی کامپوزر**.

```
⌗ IGBZ-WP    ⑂ arena/01a00c31-igbz-wp    +12403 −898  ›    [ Merge PR ]
```

> «چیزی که در این رابط کاربری برای من مهم هست کادر پایینی چت باکس است، یعنی اتصال به ریپوی
> گیت. این را حتماً در نظر بگیر.»

### چرا این مهم است

برای مدیری که وسط یک خرابی نشسته، سه سؤال همیشه باید بی‌کلیک جواب داشته باشند: **روی کدام
مخزن کار می‌کنم؟ روی کدام شاخه؟ تا حالا چقدر عوض شده؟** و یک راه برای بستن کار.

ویرا امروز هیچ‌کدام را ندارد. فقط یک «پوشهٔ کاری» می‌شناسد و گیت برایش یک تب فرعی در ریل
کناری است.

### آنچه لازم است

**اتصال مخزن — نه فقط پوشه.** آدرس مخزن + اعتبارنامه از مخزن کلید پنل (توکن هرگز به مدل
نمی‌رسد) + کلون در فضای کاری مدیریت‌شده. چند مخزن هم‌زمان: افزونهٔ IGBZ، مخزن اپ‌ها، هر چیز
دیگر.

**نوار پایینی کامپوزر** — همیشه دیده می‌شود:

| بخش | کار |
|---|---|
| نشان مخزن | نام مخزن؛ کلیک = عوض‌کردن یا وصل‌کردن مخزن تازه |
| نشان شاخه | شاخهٔ جاری؛ کلیک = تعویض یا ساخت شاخه |
| شمار تغییر | `+n −m` از شروع کار؛ کلیک = دیدن دیف کامل |
| دکمهٔ اقدام | کامیت، پوش، ساخت PR، ادغام |

**سیاست شاخه.** ویرا هیچ‌وقت مستقیم روی شاخهٔ اصلی کار نمی‌کند. هر کار روی شاخهٔ خودش،
و ورودش به اصلی فقط از راه PR با تأیید مدیر — که با قاعدهٔ «هیچ تغییری بدون تأیید انسانی»
هم‌خوان است.

**پنل تغییرات.** فهرست فایل‌های تغییرکرده با شمار خط و دیف هرکدام — همان چیزی که در تصویر
سمت راست دیده می‌شود.

**تأیید در سه نقطه:** کامیت · پوش · ادغام. هیچ‌کدام خودکار نیست.

### آنچه در کد امروز نیست

- مفهوم «مخزن» وجود ندارد؛ فقط `workspace` که یک مسیر است.
- کلون، شاخه، کامیت، پوش و PR ابزار ندارند — فقط از راه `bash` و بدون هیچ حفاظی.
- توکن گیت جایی برای نشستن ندارد.
- دیف تجمعی نشست محاسبه نمی‌شود؛ فقط دیف هر ویرایش جداگانه.


---

## ۱۷. تصمیم جدید — انتخاب مدل/سرویس پادو با ماست (۱۴۰۶/۰۵/۳۱)

کارفرما به‌صراحت تصحیح کرد:
- **انتخاب مدل و سرویس پادو با ما (تیم توسعه) است**، نه با ادمین فروشگاه.
- ادمین فروشگاه **فقط کلید API** مربوط به سرویس/مدلی را که ما اعلام می‌کنیم
  تهیه می‌کند و در پنل مدیریت فروشگاه ثبت می‌کند.
- انتخاب مدل باید **بر اساس بهترین عملکرد در اتوماسیون** صورت گیرد، نه ترجیح
  شخصی یا بازاریابی. معیارها (به‌ترتیب وزن تقریبی):
  1. قابلیت اطمینان در **ابزارفراخوانی (tool/function calling)** چندمرحله‌ای
     (مهم‌ترین ویژگی برای اسکیل‌های اتوماسیون).
  2. پشتیبانی از **Structured Output** و رعایت دقیق قرارداد خروجی
     (JSON معتبر، ساختار ZIP مشخص).
  3. کیفیت تولید/ویرایش کد (HTML/CSS/JSON/`theme.json` و تمپلیت‌های بلوکی FSE) برای مرحلهٔ طراحی قالب.
  4. پشتیبانی مناسب از **زبان فارسی و جهت RTL** در تولید کد و متن.
  5. پایداری، نرخ خطا، و زمان پاسخ قابل پیش‌بینی.
  6. هزینهٔ به‌ازای توکن و سازگاری با بودجهٔ عملیاتی پلتفرم.
  7. پشتیبانی از استاندارد MCP و/یا فرمت `SKILL.md` (یا قابلیت معادل).
- **API Gateway واحد در ویرا**: همهٔ درخواست‌های مدل از هر نقطه (افزونه، n8n، پنل،
  پادو) باید از دروازهٔ یکپارچه ویرا عبور کنند. این دروازه:
  * اعتبار کلید ادمین را چک می‌کند.
  * بودجه/سقف هزینه را اعمال می‌کند (از لجر `ig_ai_credit_ledger`).
  * محدودیت نرخ و سقف درخواست را اعمال می‌کند (بک‌اند).
  * محتوای ممنوعه/خطرناک را پالایش می‌کند.
  * تمام تراکنش‌ها را در Audit Log ثبت می‌کند.
  * خروجی مدل را قبل از تحویل به گیرنده (اسکیل/وردفلو) اعتبارسنجی می‌کند.
- ادمین فروشگاه **حق انتخاب مدل** ندارد و به‌هیچ‌وجه کلید خودش را مستقیماً به
  هیچ بخشی جز «تنظیمات پادو» در پنل ما وارد نمی‌کند.
- در آینده می‌توان مدل پشتیبان/ثانویه هم اضافه کرد (برای کاهش هزینه یا Fallback)
  ولی تصمیم‌گیری با ماست.

**اثر این تصمیم بر اسناد/طراحی قبلی:**
- تمام ارجاعات به «انتخاب مدل توسط ادمین» یا «تهیهٔ سرویس بیرونی توسط ادمین» باید
  به‌روز شوند؛ الان داریم آن‌ها را در HANDOFF و DESIGN-AUTOMATION اصلاح می‌کنیم.
- معماری لایه ۳ در §۳ و §۸ و §۱۵ به‌گونه‌ای باید باشد که ادمین با یک پنجره
  (مرکز پادو) روبرو است که فقط از او کلید را می‌گیرد.
- این به نفع ادمین است: پیچیدگی انتخاب مدل/تنظیمات از دوش او برداشته می‌شود و
  ما تضمین کیفیت خروجی را یکپارچه می‌کنیم.


---

## ۱۸. جمع‌بندی پاسخ‌های پلن مرحله ۱ (پادو) — ۱۴۰۶/۰۵/۳۱

در این بخش پاسخ‌های کارفرما به سؤالات هفت‌گانهٔ پلن طراحی پادو (مرحله ۱) به‌ترتیب ثبت
می‌شوند. تا پایان همهٔ سؤالات، هیچ کدی برای پادو نوشته نمی‌شود.

**۷-الف) خروجی نهایی قالب:** ✅ **گزینه «د» — هر سه به انتخاب ادمین**.
پادو بسته به انتخاب ادمین در پرسش‌نامه باید بتواند هر سه نوع خروجی زیر را تولید کند:
  1. قالب فرزند FSE (روی قالب هسته وردپرس / قالب مادر خودمان در آینده)
  2. قالب کلاسیک PHP
  3. تمپلیت‌های قابل ایمپرت برای صفحه‌سازهای محبوب (به‌صورت پلاگین‌الحاقی، نه پیش‌فرض)

⚠️ پیامد طراحی: اسکیل `igbz-store-theme` باید سه قرارداد خروجی مجزا داشته باشد و
دروازه اعتبارسنجی بک‌اند باید هر سه نوع را بشناسد. جزئیات در زمان پلن اجرایی بسته می‌شود.

**۷-ب) نقطه تحویل خروجی:** ✅ **گزینه «ج» — هر دو**.
  - مسیر پیش‌فرض: دریافت مستقیم از API Gateway ویرا ← اعتبارسنجی بک‌اند ← نصب
    خودکار در محیط پیش‌نمایش ← تأیید/بازگشت با یک کلیک.
  - مسیر جایگزین: دکمهٔ «دریافت فایل ZIP» همیشه در دسترس است (برای پشتیبان‌گیری،
    انتقال به سایت دیگر، یا نصب دستی توسط ادمین‌های حرفه‌ای).

**۷-ج) سطح خودکارسازی قیمت/Refund:** ✅ **گزینه «ج» — تأیید دسته‌ای**.
  - پادو در بازه‌های معین (پیش‌فرض: ساعتی/روزانه، بسته به نوع عملیات) یک «پلن
    پیشنهادی» شامل تمام اقدامات قیمت‌گذاری، تخفیف، و مرجوعی تولید می‌کند.
  - ادمین در مرکز پادو کل لیست را می‌بیند و می‌تواند هر مورد را تأیید/رد/ویرایش
    کند؛ با یک دکمه «تأیید همه» موارد تیک‌خورده به‌صورت تراکنشی و بادوام (از طریق
    Action Scheduler) اجرا می‌شوند.
  - هیچ عملیات مالی (تغییر قیمت، Refund، تغییر انبوه) بدون این چرخه اجرا نمی‌شود.
  - سقف‌های اضطراری (مثلاً Refund بیش از X تومان) همچنان نیاز به تأیید تک‌به‌تک دارند.

**۷-د) فونت پیش‌فرض فارسی:** ✅ **گزینه «الف» — وزیرمتن (Vazirmatn)**.
  - فونت پیش‌فرض در همهٔ قالب‌های تولیدشده توسط پادو، وزیرمتن (با مجوز OFL) است.
  - فونت به‌صورت محلی درون بسته قالب (در پوشهٔ `assets/fonts/`) تعبیه می‌شود —
    **هیچ‌گاه از Google Fonts یا CDN خارجی** بارگذاری نمی‌شود (قاعده دائمی ایران در اسناد).
  - ادمین می‌تواند بعداً از طریق پنل پادو فونت را تعویض کند (یا فونت سفارشی آپلود)
    ولی پیش‌فرض همیشه وزیرمتن است.

**۷-ه) «مرکز پادو» در پنل مدیریت:** ✅ **گزینه «الف» — در همین مرحله S0**.
  - یک صفحهٔ مدیریتی با عنوان «پادو» (زیرمنوی WooCommerce یا منوی اصلی IGBZ)
    در پیشخوان وردپرس اضافه می‌شود که از S0 وجود دارد.
  - اجزای پایه در S0:
    * تب «تنظیمات»: ورود کلید API مدل منتخب ما (فقط این)، نمایش وضعیت اتصال.
    * تب «طراحی قالب»: پرسش‌نامه + دکمه «شروع طراحی» + وضعیت در حال اجرا +
      دکمه‌های «مشاهده پیش‌نمایش» / «انتشار» / «بازگشت» / «دریافت ZIP».
    * تب «درخواست‌های مجوز» (در S0 با معماری کامل ولی تعداد محدودی از انواع
      درخواست فعال است): لیست یکپارچهٔ درخواست‌های مجوز روزانه/ساعتی برای
      **همهٔ** عملیات حساس — تغییر قیمت، Refund، انتشار پست اینستاگرام (هر چهار
      نوع)، ارسال کمپین، تغییر انبوه، و غیره. ادمین با کلیک روی هر درخواست،
      **شرح دلایل و جزئیات پیشنهاد پادو** را می‌بیند و می‌تواند تأیید/رد/
      ویرایش کند. این زیرساخت در S0 برای «اعمال/بازگشت قالب» فعال است و
      در فازهای بعدی برای سایر عملیات بدون تغییر معماری استفاده می‌شود.
    * تب «تاریخچه»: لاگ تمام اقدامات پادو + قابلیت بازگشت (در S0 فقط
      تاریخچه طراحی قالب).

**۷-و) انتشار اینستاگرام (S6):** ⏸ **معوق** — کارفرما درخواست کرد پس از پایان
  سایر سؤالات به این مورد برگشته و درباره‌اش بحث/جمع‌بندی کنیم.

**۷-ز) مکان فیزیکی اسکیل‌ها:** ✅ **گزینه «ب» — `skills/` در ریشهٔ ریپو**.
  - ساختار قطعی:
    * `skills/igbz-*/`  — اسکیل‌های خودمان (مثل `igbz-store-theme/`، `igbz-seo/`،
      `igbz-crm/`، …) با فرمت `SKILL.md` + `references/`. این‌ها **کامیت می‌شوند**
      و تغییراتشان در گیت ثبت می‌گردد.
    * `skills/_thirdparty/`  — اسکیل‌های دانلودشده از منابع بیرونی (مثل
      `ui-ux-pro-max-skill`). این پوشه git-ignored است و از URL مرجع
      (مخزن گیت‌هاب + کامیت پین‌شده) در هر محیط قابل بازیابی است.
    * `skills/README.md`  — قوانین بالا به‌صورت سند داخل پوشه.
  - هستهٔ ویرا (`vira/`) این پوشه را (همراه با `~/.vira/skills/` در استقرار
    سراسری) به‌عنوان یک رجیستری محلی می‌خواند.
  - مکان قدیمی `.vira/skills/` از `.gitignore` حذف نشده (برای سازگاری
    backward با نسخه ۰.۹.۹ ویرا) ولی دیگر مکان رسمی نیست.

**۷-و) انتشار اینستاگرام (S6) — جمع‌بندی نهایی:** ✅ **الگوی یکپارچه «درخواست مجوز روزانه»
  برای همهٔ عملیات حساس، از جمله چهار نوع پست اینستاگرام**.
  - در مرکز پادو (تب «درخواست‌های مجوز» به‌جای «تأییدها» که قبلاً در ۷-ه ثبت شد)
    هر روز (یا هر بازه‌ای) پادو **فهرستی از درخواست مجوز** را با ذکر نوع،
    دلیل/شرح، تعداد، و هدف به ادمین ارائه می‌دهد.
  - ادمین با کلیک روی هر درخواست، **شرح دلایل و جزئیات پیشنهاد پادو** را می‌بیند:
    * برای پست‌های وایرال/رشد: موضوع، فرمت، مخاطب، انتظار رشد، دفعات انتشار
    * برای پست‌های تخصصی/اعتماد: محور محتوا، ارزش افزوده برای مخاطب
    * برای پست‌های شخصی/وفاداری: هدف انسانی‌کردن برند
    * برای پست‌های فروش/کمپین: محصول/دسته هدف، تخفیف/پیشنهاد، بودجه، هدف تبدیل،
      زمان‌بندی، قیف (کامنت→دایرکت یا لینک در بیو)
  - ادمین می‌تواند هر درخواست را **تأیید / رد / ویرایش** کند (مثلاً تعداد پست‌ها را
    کم کند، زمان را عوض کند، هشتگ‌ها را تغییر دهد، یا کلاً کمپین آن روز را رد کند).
  - پس از تأیید دسته‌ای، پادو **به‌صورت خودکار** پرامپت‌های لازم (کپشن،
    تصویر/ریلز پیشنهادی، زمان‌بندی، CTA) را تولید و به **Manus (انتشار/محتوا) یا
    ChatPlace (قیف کامنت→دایرکت)** ارسال می‌کند — تماس مستقیم با Graph API همچنان
    ممنوع است.
  - هر انتشار در Audit Log ثبت می‌شود (نوع پست، شناسهٔ محتوا در `ig_content`،
    زمان، و کدام درخواست مجوز منجر به آن شد).
  - ⚠️ پیامد مهم: این بخش **در S0 اسکلتش ایجاد می‌شود** (تب «درخواست‌های مجوز» در
    مرکز پادو با معماری یکپارچه)، و در همان مرحله دسته‌های موجود در S0 (مثلاً
    «اعمال قالب» و بعدها «تغییر قیمت» و «Refund») از آن عبور می‌کنند. اینستاگرام
    وقتی در فاز ۴ روشن می‌شود، **از همین زیرساخت مجوز موجود** استفاده می‌کند
    (نه یک سیستم جداگانه).
  - سطوح مجوز برای چهار نوع پست:
    * وایرال/رشد (۱) و تخصصی/اعتماد (۲): تأیید دسته‌ای در صف روزانه کافی است.
    * شخصی/وفاداری (۳): تأیید دسته‌ای کافی است.
    * فروش/کمپین (۴): درخواست جداگانه با جزئیات کامل (قیمت، تخفیف، کد، بودمه)،
      نیاز به تأیید صریح ادمین دارد (چون اثر مالی دارد).
