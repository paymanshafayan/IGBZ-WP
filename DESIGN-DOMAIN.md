# سرویس دامنه (Domain Service) — طراحی

**وضعیت:** طراحی تأییدشده توسط کارفرما. این سرویس بین‌ماژولی است: در Onboarding (بعد از
خرید پنل)، فاز ۶ (پیش‌شرط درگاه بانکی) و فاز ۱۰ (ثبت خودکار گوگل/بینگ) استفاده می‌شود.

> **ایده:** ادمین نیازی به مراجعه به سایت خارجی ندارد — جستجو، ثبت، انتقال و مدیریت دامنه
> همه داخل پنل خودمان، از طریق API یک ارائه‌دهندهٔ مرجع.

---

## ۱. ارائه‌دهنده‌های مرجع (API)

| نوع دامنه | مرجع | روش |
|---|---|---|
| دامنه‌های ایرانی (.ir و زیرمجموعه‌ها) | **ایرنیک (NIC.ir)** — از طریق نمایندگان (Reseller) | API نماینده (کلون‌کلود، برتینا، افیکس‌هاست و…) |
| دامنه‌های بین‌المللی (.com و…) | **ICANN** — از طریق ثبت‌کننده‌های بزرگ | API استاندارد (ResellerClub و مشابه) |

- **جستجوی دامنه:** API لحظه‌ای (آزاد بودن/قیمت/پسوندها).
- **ثبت دامنه:** ثبت/تمدید/تنظیم DNS به‌صورت خودکار.
- یک **آداپتور** (`DomainProviderInterface`) + پیاده‌سازی HTTP تنظیم‌پذیر (مثل بقیهٔ پروژه).

## ۲. جریان (Onboarding)

```
ادمین پنل را خرید → فروشگاه ساخته شد → هدایت به «بخش دامنه»
  → دو انتخاب:
     ۱) زیردامنهٔ سایت مادر (پیش‌فرض، رایگان): <store>.igbz.ir
     ۲) دامنهٔ مستقل:
         - دامنه دارد → راهنمای «انتقال دامنه» (تنظیم DNS، Nameserver)
         - ندارد → دکمهٔ «همین حالا دامنت را ثبت کن» → جستجو/ثبت
  → مزایای دامنهٔ مستقل به ادمین اعلام می‌شود:
      اعتماد برند، سئو (ثبت خودکار گوگل/بینگ)، حرفه‌ای‌بودن، مالکیت دامنه
```

## ۳. جدول‌ها

| جدول | محتوا |
|---|---|
| `tenant_domains` | نگاشت دامنهٔ قابل‌مسیریابی به مستأجر: `tenant_id`، `domain`، `is_primary`، `verified_at`، `verification_token`، `created_at` |
| `ig_domains` | دفتر سفارش/ثبت دامنه: `tenant_id`، `name`، `type` (`subdomain`/`registered`/`transferred`)، `status` (`pending`/`active`/`failed`)، `provider_ref`، `dns_verified`، `expires_at`، `created_at` |
| `ig_domain_orders` | سفارش‌های خرید/تمدید: `tenant_id`، `domain_id`، `action` (`register`/`renew`/`transfer`)، `amount`، `status`، `provider_ref` |

`tenant_domains` منبع حقیقت مسیریابی است و `ig_domains` رکورد ارائه‌دهنده و چرخهٔ ثبت را نگه می‌دارد؛ هنگام ساخت زیر‌دامنه یا ثبت/انتقال، این دو نگاشت همگام می‌شوند.

## ۴. اندپوینت‌ها

```
GET  /igbz/v1/domains/search?q=&tld=ir|com|...     (جستجوی لحظه‌ای + قیمت)
POST /igbz/v1/domains/register                     { name, tld, years } → پرداخت → ثبت
POST /igbz/v1/domains/transfer                     { name, auth_code? } → راهنما
GET  /igbz/v1/domains                              (دامنه‌های من)
POST /igbz/v1/domains/{id}/verify-dns              (تأیید اتصال DNS)
```

## ۵. قواعد

- ثبت دامنهٔ پولی → از طریق `payments` (درگاه/کیف پول) — یک «سفارش دامنه».
- `dns_verified` شرط «دامنهٔ مستقل فعال» است (و سپس درگاه بانکی/گوگل فعال می‌شود).
- پادو در انتخاب نام، انتقال و تنظیم DNS راهنمایی می‌کند.

## ۶. فازبندی پیاده‌سازی (پس از تأیید)
| گام | محتوا |
|---|---|
| ۱ | `DomainProviderInterface` + آداپتور HTTP (جستجو/ثبت/تمدید) |
| ۲ | جدول‌ها + REST (جستجو/ثبت/فهرست/تأیید DNS) |
| ۳ | صفحهٔ «دامنه» در پنل (زیردامنه/انتقال/ثبت + مزایا) |
| ۴ | اتصال: فعال‌شدن درگاه بانکی و گوگل/بینگ پس از `dns_verified` |
| ۵ | تست‌ها + تأیید زنده |

---

## وضعیت اجرایی جلسهٔ ۱۴۰۵/۰۶/۰۵

نگاشت `tenant_domains` اکنون منبع حقیقت مسیریابی است و هنگام ایجاد مستأجر و ثبت دامنه با دفتر `ig_domains` همگام می‌شود. تأیید دامنه از طریق بررسی DNS انجام می‌شود و دامنهٔ تأییدنشده برای مسیریابی یا فعال‌سازی درگاه بانکی معتبر نیست. اتصال ثبت دامنهٔ پولی به پرداخت و ارائه‌دهندهٔ واقعی هنوز نیازمند کلید و آزمون زنده است.

### ممیزی پس از ادغام PR شمارهٔ ۱۰ — ۱۴۰۵/۰۶/۰۵

بررسی کد، وضعیت دقیق‌تری از جملهٔ بالا نشان داد:

| بخش | وضعیت واقعی |
|---|---|
| زیردامنهٔ رایگان و همگام‌سازی نگاشت | ✅ پیاده است |
| جستجوی ارائه‌دهنده | 🟡 یک قرارداد HTTP ثابت در `DomainService` دارد؛ `DomainProviderInterface` و رجیستری آداپتورها ساخته نشده‌اند |
| ثبت دامنه | 🔴 `register()` پیش از هر پرداخت، مستقیماً درخواست ثبت را به ارائه‌دهنده می‌فرستد و سپس سفارش pending می‌سازد؛ ترتیب طراحی‌شدهٔ «سفارش → پرداخت تأییدشده → ثبت» برقرار نیست |
| تمدید | ❌ متد، مسیر REST، زمان‌بند و اجرای ارائه‌دهنده وجود ندارد |
| callback/webhook ارائه‌دهنده | ❌ مسیر امضاشده و تطبیق وضعیت سفارش/انقضا وجود ندارد |
| تأیید DNS | 🟡 TXT/CNAME واقعی و همگام‌سازی دو جدول پیاده است |
| مجوز شیء در تأیید DNS | 🔴 کنترلر اجازهٔ مدیریت مستأجر جاری را می‌سنجد، اما `verify_dns(id)` مالکیت همان `id` به مستأجر جاری را بررسی نمی‌کند؛ باید پیش از هر lookup یا update، `tenant_id` در شرط باشد |
| فهرست دامنه‌ها | 🟡 tenant-scoped است، ولی صفحه‌بندی/سقف ندارد |
| آزمون واقعی | ❌ بدون sandbox و کلید واقعی ارائه‌دهنده، پرداخت و callback آزموده نشده‌اند |

بنابراین «اتصال به ارائه‌دهندهٔ واقعی» فقط به معنی ثبت کلید نیست. معیار تکمیل، آداپتور
نسخه‌دار، quote دارای انقضا، سفارش و پرداخت idempotent، ثبت فقط پس از پرداخت قطعی، callback
امضاشده، تمدید و retry/dead-letter، مجوز شیء tenant-scoped، و آزمون مثبت/منفی روی sandbox
ارائه‌دهنده است. تا آن زمان ثبت پولی نباید در محیط زنده فعال شود.
