# Runbook — انتشار، canary و rollback (فاز ۷۳)

چهار چیز باید بتوان برگرداند: نسخهٔ افزونه، اسکیمای دیتابیس، قالب پادو، و
یک انتشارِ خراب. این سند اهرم‌ها و ترتیبشان است.

## §auto — گیت انتشار و بازگشت خودکار

- **گیت سلامت پلتفرم (لایهٔ اول):** `railway.json` با
  `healthcheckPath: /?igbz_health=1` — اگر نسخهٔ جدید ۲۰۰ ندهد، پلتفرم
  دیپلوی را ناسالم علامت می‌زند و ترافیک به نسخهٔ قبلی برمی‌گردد
  (قابلیت auto-rollback ریل‌وی باید در تنظیمات سرویس روشن باشد).
- **گیت محصول (لایهٔ دوم — دست کارفرما):** پس از هر دیپلوی:

  ```bash
  wp igbz release verify --tries=6 --sleep=5
  ```

  حکم‌ها: **green** = اعلام انتشار؛ **degraded** = سرویس می‌دهد اما دریفت
  اسکیما دیده می‌شود (§drift)؛ **red** = اعلام نکن، برگردان (§plugin).
  خروج غیرصفر برای خط لوله/کرون canary.

## §plugin — rollback افزونه

1. در ریل‌وی: دیپلوی قبلی را از لیست deployments دوباره فعال کنید
   (هر دیپلوی = کامیت گیت؛ immutable).
2. اگر افزونه از ZIP نصب شده: `wp plugin install <prev.zip> --force`.
3. پس از برگشت: `wp igbz release verify` — اگر دیتابیس جلوتر از کد رفته
   باشد §db را ببین.

## §db — rollback دیتابیس

مهاجرت‌ها forward-only هستند (checkpoint/resume در `Migrator`)؛ برگشتِ
اسکیما یعنی بازیابی از باندل فاز ۷۲:

1. `Migrator::previous_version()` نسخهٔ مبدأ ارتقا را می‌گوید — نقطهٔ هدف.
2. باندل پیش از ارتقا را برگردانید (`RUNBOOK-BACKUP-RESTORE.md` §۴).
3. باندل فقط جداول محصول را دارد؛ `igbz_db_version` پس از restore با
   گزینهٔ درون باندل برمی‌گردد.
4. اگر ترافیک بین ارتقا و برگشت رخ داده، سفارش‌های آن پنجره را از لاگ‌ها
   و درگاه بازسازی کنید — این هزینهٔ واقعی RPO است و باید در پست‌مورتِم بیاید.

## §theme — rollback قالب پادو

آرتیفکت قالب امضاشده و immutable است (`ThemeReleaseService` — sha256+HMAC
هنگام ingest؛ تغییر دیسک بعدی در verify لو می‌رود):

1. در پنل پادو نسخهٔ فعال قبلی را دوباره فعال کنید (نسخه‌ها نگه داشته
   می‌شوند؛ سقف نگهداشت `MAX_VERSIONS_KEPT`).
2. اگر verify شکست خورد (آرتیفکت دستکاری شده): نسخه را رد کنید، دوباره
   ZIP امضاشدهٔ اصلی را بگذارید — «تعمیرِ امضا» ممنوع؛ کلید
   `igbz_theme_signing_key` را فقط با رویهٔ چرخش عوض کنید.

## §drift — دریفت اسکیما (degraded)

1. `/?igbz_health=1` → فیلدهای `igbz_tables` و `schema_expected_dbv` را
   ببینید؛ صفحهٔ وضعیت هم تفصیلش را می‌دهد.
2. علت رایج: کد قدیم روی دیتابیس ارتقایافته (یا برعکس) — یعنی §plugin یا
   یک مهاجرت نیمه‌کاره؛ `wp option get igbz_migration_checkpoint` اگر
   غیرصفر بود، مهاجرت resume می‌خواهد نه rollback.
3. drift فقط هشدار است؛ تا وقتی 200 می‌دهید فروشگاه روشن بماند و درست
   تصمیم بگیرید.

## §pause — اهرم توقف صف در حادثه

```bash
wp option patch update igbz_settings flags.queue_paused true --format=json   # توقف
wp option patch update igbz_settings flags.queue_paused false --format=json  # آزاد
```

- کارهای صف انباشته می‌شوند، از دست نمی‌روند؛ drain با آزادسازی ادامه
  می‌یابد (اولویت بر اساس available_at).
- هنگام توقف، SLO تأخیر صف صادقانه قرمز می‌شود — عمدی است: هزینهٔ
  نگه‌داشتن صف دیده شود.
- برای جبران انباشت پس از آزادسازی: `wp igbz jobs drain --budget=500`.

## چک‌لیست انتشار (هر بار)

1. باندل پیش از انتشار: `wp igbz backup create` + `verify`.
2. دیپلوی؛ پلتفرم تا ۲۰۰ِ health ترافیک نمی‌دهد (فاز ۷۰).
3. `wp igbz release verify` → green.
4. یک ساعت نخست: پنل SLO (نرخ خطای کارها/صف) زیر نظر.
5. قرمز؟ §plugin → §db → §theme به ترتیبِ چیزی که عوض شده.
