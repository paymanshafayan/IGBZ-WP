# احراز هویت قانونی (Legal Authentication) — طراحی

**وضعیت:** طراحی تأییدشده توسط کارفرما. این یک **الزام حقوقی فراگیر** است (نه فقط یک فاز):
برای پرداخت بانکی، «ثبت»های سایت و اپ مشتری.

> **چرا مهم است:** عدم رعایت ثبت‌نام/احراز قانونی می‌تواند منجر به پروندهٔ قضایی و جبران
> خسارت‌های سنگین شود. این سند جریان صحیح را الزامی می‌کند.

---

## ۱. ثبت‌نام قانونی (برای پرداخت بانکی)

```
۱) دریافت شماره موبایل + کد ملی
۲) تطبیق توسط سامانهٔ رسمی (شاهکار) — اگر مطابقت داشت:
۳) ارسال کد یکبارمصرف (OTP حداکثر ۶ رقمی)
۴) ساخت اکانت پس از واردکردن کد
۵) (بعداً) کاربر از داشبورد خود یک پسورد تعریف می‌کند
   → از آن پس می‌تواند به‌جای رمز یکبارمصرف با پسورد وارد شود
```

## ۲. قاعدهٔ کلی «هر ثبت»

برای **هر ثبت کاری در سایت** (مثل ثبت در سبد خرید و هر اقدام مشابه):
- شمارهٔ همراه کاربر گرفته شود
- یک **رمز یکبارمصرف حداکثر ۶ رقمی** برای وی ارسال شود
- (OTP زیرساخت موجود: `OtpService` با کاوه‌نگار/اس‌ام‌اس.آی‌آر)

## ۳. مرحلهٔ کد ملی/تطابق — فقط زمان پرداخت (به انتخاب ادمین)

| حالت | رفتار |
|---|---|
| ادمین «تطابق کد ملی هنگام پرداخت» را **فعال** کند | قبل از پرداخت، شماره موبایل + کد ملی گرفته و با **سامانهٔ شاهکار** تطبیق داده می‌شود |
| ادمین **غیرفعال** کند | ادمین باید **قرارداد دیجیتال محکم حقوقی** را تأیید کند که «تمام عواقب آن تنها متوجه شخص اوست» |

### شرط فعال‌سازی (مهم)
**بخش تطابق کد ملی فقط وقتی فعال می‌شود که «مدیر ارشد» کلید شاهکار را در تنظیمات ثبت کرده باشد.**
تا آن زمان، این بخش خاموش است (و اگر ادمین بخواهد پرداخت بانکی بدون تطابق انجام دهد، مسیر
قرارداد دیجیتال الزامی است).

## ۴. شماره موبایل در اپ مشتری

ثبت شماره موبایل در اپ مشتری علاوه بر ورود، برای **تشخیص فروشگاه توسط اپ** هم کاربرد دارد
(اپ بر اساس موبایل/اکانت، فروشگاه مرتبط را شناسایی می‌کند).

---

## ۵. جدول‌ها/تنظیمات

| مورد | محتوا |
|---|---|
| `legal.national_id_check` | فعال/غیرفعال (به انتخاب ادمین) — فقط وقتی `legal.shahkar_api_key` توسط مدیر ارشد ثبت شده |
| `legal.shahkar_api_key` | کلید سامانهٔ شاهکار (فقط مدیر ارشد می‌بیند/ثبت می‌کند) |
| `legal.shahkar_base_url` | آدرس API شاهکار |
| `ig_legal_agreements` | قراردادهای دیجیتال حقوقی تأییدشده: `tenant_id`، `type` (`payment_without_nid`)، `version`، `accepted_by`، `accepted_at`، `ip`، `hash` |
| `ig_nid_verifications` | سوابق تطبیق کد ملی: `user_id`، `national_id` (ماسک‌شده)، `status`، `ref`، `at` |

## ۶. اندپوینت‌ها

```
POST /igbz/v1/auth/register-legal      { phone, national_id } → تطبیق شاهکار → OTP
POST /igbz/v1/auth/otp/verify         (موجود) → ساخت اکانت
POST /igbz/v1/auth/set-password       (پس از ورود اول) → تعریف پسورد
POST /igbz/v1/checkout/require-otp    (هر «ثبت» مثل سبد خرید) → OTP
(پنل) POST /igbz/v1/admin/legal/nid-check-toggle   → فعال/غیرفعال (با شرط کلید شاهکار)
(پنل) POST /igbz/v1/admin/legal/agreement          → تأیید قرارداد دیجیتال
```

## ۷. قواعد
- **بدون کلید شاهکار (ثبت‌شده توسط مدیر ارشد) → تطابق کد ملی فعال نمی‌شود** (قفل).
- هر «ثبت» → OTP الزامی (حداکثر ۶ رقمی).
- پرداخت بانکی بدون تطابق → فقط با قرارداد دیجیتال حقوقیِ تأییدشده.
- کد ملی هرگز به‌صورت کامل ذخیره نمی‌شود (ماسک/هش).

## ۷.۵ وضعیت پیاده‌سازی — به‌روزرسانی ۱۴۰۶/۰۶/۰۴

جریان قرارداد حقوقی، گیت پیش از پرداخت و کول‌داون OTP اکنون در کد پیاده‌سازی شده و مسیرهای پنل و REST برای آن‌ها وجود دارد. تنها بخش وابسته به زیرساخت بیرونی، ثبت کلید واقعی شاهکار و آزمون زندهٔ آن است.

### ۷.۵.۱ آنچه ساخته شده ✅

`OtpService` (ارسال/تأیید/ساخت کاربر/ورود) · محدودیت تلاش (`otp.max_attempts`، پیش‌فرض ۵) ·
سهمیهٔ ساعتی (`otp.max_per_hour`، پیش‌فرض ۵) · ۹ مسیر REST احراز هویت · ورود با پسورد ·
شورت‌کد `[igbz_otp_login]` · جدول‌های `ig_nid_verifications` و `ig_legal_agreements` در
اسکیما · `NationalIdVerifier` (کد اتصال به شاهکار نوشته شده).

### ۷.۵.۲ ✅ قرارداد دیجیتال و گیت پرداخت

`LegalWaiverService` متن قرارداد را با نسخه و هش ثبت می‌کند، پذیرش را به مستأجر و کاربر
منتسب می‌سازد و مسیرهای پنل و REST آن را ارائه می‌کنند. `PaymentService` پیش از ساخت هر
تلاش پرداخت بانکی، دامنهٔ تأییدشده، اینماد و یکی از دو شرط قانونی را بررسی می‌کند:
تطابق کد ملیِ فعال با کلید و نشانی شاهکار، یا قرارداد معتبر همان مستأجر. درگاه‌های
پیکربندی‌شده نیز پیش از نمایش از همین گیت عبور می‌کنند.

### ۷.۵.۳ 🟠 شاهکار عملاً خاموش است

`NationalIdVerifier::is_available()` فقط وقتی `true` است که `legal.shahkar_api_key` ثبت
شده باشد، و طبق طراحی فقط **مدیر ارشد** آن را ثبت می‌کند. این کلید هنوز وجود ندارد (جزو
تصمیم باز «کلیدهای واقعی سرویس‌ها» در `PROJECT-STATE.md`).

**یعنی امروز مرحلهٔ ۱ و ۲ جریان بند ۱ اجرا نمی‌شود؛ ثبت‌نام عملاً فقط موبایل + OTP است.**
کد آماده است و با ثبت کلید فعال می‌شود — ولی تا آن روز این شکاف باید **آگاهانه** پذیرفته
شود، نه اینکه فراموش شود.

### ۷.۵.۴ ✅ تولید کد یکبارمصرف با ماژول کریپتو — الزام کارفرما

> «ساخت کد یکبارمصرف توسط ماژول کریپتو انجام بشه حتماً.»

**وضعیت: از قبل رعایت شده.** `OtpService` از `Crypto::numeric_code()` استفاده می‌کند که
روی `random_int()` بنا شده — یعنی مولد امن رمزنگاری، نه `rand()` یا `mt_rand()`.

این الزام اینجا ثبت می‌شود تا **قاعدهٔ دائمی** باشد: هیچ کد یکبارمصرفی در هیچ ماژولی
نباید با مولد غیرامن ساخته شود. هر مسیر تازه‌ای که کد تولید می‌کند باید از `Crypto`
بگذرد.

### ۷.۵.۵ 🟠 کول‌داون ارسال مجدد: **موبایل و IP با هم** (AND) — الزام کارفرما

> «برای بررسی انقضای مهلت ارسال کد مجدد، بر اساس شماره موبایل **و** آی‌پی بررسی کنه. اگه
> زمان لازم منقضی نشده جلوی درخواست رو بگیره و پیام لازم رو ارسال کنه که هنوز مهلت ارسال
> مجدد منقضی نشده.»
>
> **تصریح کارفرما:** «من گفتم آی‌پی **و** شماره موبایل، یعنی اگه **هر دو یکی باشن**.»

#### شرط دقیق — AND است، نه OR

کول‌داون وقتی اعمال می‌شود که **هر دو** با درخواست قبلی یکی باشند:

```
اگر ( همان شماره موبایل ) و ( همان IP ) و ( هنوز resend_seconds نگذشته )
        → رد کن، با پیام «هنوز مهلت ارسال مجدد منقضی نشده» و retry_after
در غیر این صورت → اجازه بده
```

یعنی کوئری باید روی **هر دو ستون** فیلتر کند:

```sql
SELECT created_at FROM {otp_codes}
 WHERE phone = %s AND ip_hash = %s AND purpose = %s
 ORDER BY id DESC LIMIT 1
```

**چرا این تفسیر مهم است:** برداشت اولیهٔ عامل «هرکدام که زودتر پر شد» (OR) بود، که
سخت‌گیرانه‌تر است و کاربران پشت NAT را قفل می‌کند. با AND آن نگرانی **خودبه‌خود حل
می‌شود**: دو کاربر واقعی پشت یک IP شمارهٔ متفاوت دارند، پس شرط برایشان صادق نیست و
مزاحمتی ایجاد نمی‌شود.

> **قید بازمانده:** AND جلوی «یک نفر، یک شماره، اسپم رفرش» را می‌گیرد — که خواستهٔ صریح
> کارفرماست. ولی جلوی «یک IP، شماره‌های چرخان» را نمی‌گیرد. سهمیهٔ ساعتی موجود
> (`otp.max_per_hour`) هم فقط روی شماره است. اگر روزی این الگو دیده شد، **جای درستش
> لایه‌های امنیتی و گزارش به ویراست** (`DESIGN-DEPLOY-VIRA.md §۱۴`)، نه سخت‌گیرتر کردن
> این کول‌داون.

#### 🔴 بررسی باید در بک‌اند باشد، نه فرانت‌اند — الزام صریح کارفرما

> «این بررسی باید حتماً در **بک‌اند** انجام بشه، نه در فرانت‌اند که با یه رفرش صفحه دور
> زده نشه.»

این قاعده **قابل مذاکره نیست**. شمارندهٔ فرانت‌اند (تایمر «۱۲۰ ثانیه تا ارسال مجدد») فقط
یک **نمایش** است؛ با رفرش صفحه، بستن تب، یا یک درخواست مستقیم به API کاملاً دور زده
می‌شود.

| لایه | نقش | معتبر؟ |
|---|---|---|
| تایمر فرانت‌اند | تجربهٔ کاربری — کاربر بداند چقدر مانده | ❌ **هیچ ارزش امنیتی ندارد** |
| بررسی بک‌اند در `OtpService::send()` | **تنها** نقطهٔ تصمیم واقعی | ✅ |

**وضعیت امروز:** کول‌داونِ موبایل و IP **درست** در بک‌اند است (`OtpService::send()`): شرط
فقط وقتی فعال می‌شود که هر دو مقدار شماره و نشانی IP یکسان باشند. این کنترل در خود سرویس
قرار دارد و در کنترلر، شورت‌کد یا جاوااسکریپت تکرار نشده است.

**دلیل معماری:** `OtpService::send()` تنها مسیری است که همهٔ ورودی‌ها از آن می‌گذرند —
شورت‌کد سایت، مسیر REST اپ، و هر مصرف‌کنندهٔ آینده. هر بررسی‌ای که بالاتر از این تابع
گذاشته شود، با اولین مسیر تازه دور زده می‌شود.

**قاعدهٔ دائمی:** هر محدودیت نرخ، کول‌داون، یا سقف تلاش در این پروژه باید در **سرویس
بک‌اند** اعمال شود. فرانت‌اند فقط بازتاب می‌دهد، تصمیم نمی‌گیرد.

---

## ۷.۶ 🔐 خروج گروهی اطلاعات کاربران — امضای بیومتریک + دستگاه

> **الزام کارفرما (۱۴۰۵/۰۵/۳۱).** کارفرما تصریح کرد این مورد **متعلق به بخش امنیت** است
> و اینجا فقط ثبت می‌شود تا فراموش نشود؛ طراحی کاملش در سند امنیت انجام می‌گیرد.

### ۷.۶.۱ گفتهٔ کارفرما

> «هرگونه درخواست اطلاعات کاربران فروشگاه **به صورت گروهی** فقط از طریق **اپ مدیریت**
> قابل انجام است، و باید توسط یک **پارامتر بیومتریک** و همچنین **اطلاعات دستگاه** که از
> قبل در اپلیکیشن مدیر ثبت شده **امضا** بشه و برای سرور ارسال بشه. و **بررسی صحت اون
> توسط سرور انجام می‌شه، نه اپ مدیریت.** همچنین هر اپ دارای یک **کد شناسایی یکتا**ست که
> به صورت **رمزگذاری‌شده** ارسال می‌شه. تمام این موارد در زمان **ثبت‌نام مدیر در اپ
> مدیریت** در اپ ذخیره شده و به صورت **خودکار** روی درخواست‌های گروهی ارسال می‌شه.»

### ۷.۶.۲ چهار جزء الزامی

| # | جزء | قید |
|---|---|---|
| ۱ | **فقط از اپ مدیریت** | خروجی گروهی از پنل وب یا هر مسیر دیگر ممکن نیست |
| ۲ | **پارامتر بیومتریک** | ثبت‌شده هنگام ثبت‌نام مدیر در اپ |
| ۳ | **اطلاعات دستگاه** | همان دستگاهی که در ثبت‌نام ثبت شده |
| ۴ | **کد یکتای اپ** | **رمزگذاری‌شده** ارسال می‌شود |

هر سه مورد آخر در **امضای درخواست** جمع می‌شوند و **خودکار** ضمیمه می‌گردند — مدیر کار
دستی اضافه‌ای نمی‌کند.

### ۷.۶.۳ 🔴 قاعدهٔ مرکزی: اعتبارسنجی در سرور، نه در اپ

> **«بررسی صحت اون توسط سرور انجام می‌شه، نه اپ مدیریت.»**

این همان قاعده‌ای است که در `§۷.۵.۵` برای کول‌داون OTP تثبیت شد، و اینجا **دوباره و
مستقل** توسط کارفرما تکرار شده — یعنی یک اصل پایدار پروژه است، نه یک تصمیم موردی.

**چرا حیاتی است:** اپ روی دستگاه کاربر اجرا می‌شود و قابل مهندسی معکوس، دیباگ و دستکاری
است. اگر اپ خودش بگوید «بیومتریک تأیید شد»، مهاجم همان پیام را جعل می‌کند. سرور باید
**امضا را مستقل تأیید کند** و هرگز به ادعای اپ اعتماد نکند.

### ۷.۶.۴ نسبت با استانداردها

این کنترل مستقیماً به دو ریسک OWASP API Security Top 10 (2023) پاسخ می‌دهد:

- **`API3:2023` — Broken Object Property Level Authorization:** همان «افشای بیش از حد
  داده» که خروجی گروهی مصداق بارز آن است.
- **`API6:2023` — Unrestricted Access to Sensitive Business Flows:** استخراج انبوه
  اطلاعات مشتریان، نمونهٔ کلاسیک «جریان کاری سالمی که خودکارسازی‌شده سوءاستفاده می‌شود».

با مقیاس پروژه (صدها فروشگاه × هزاران کاربر) این کنترل **حیاتی** است: یک خروجی گروهی
بی‌محافظت یعنی نشت اطلاعات همهٔ مشتریان یک فروشگاه در یک درخواست.

### ۷.۶.۵ نقطهٔ تماس با کد موجود

`StoreAdminController` امروز مسیر `GET /admin/customers` را دارد (جستجوی مشتریان) که
**دقیقاً مصداق همین قاعده** است. فهرست کامل مسیرهای «گروهی» باید در سند امنیت استخراج و
همگی مشمول این کنترل شوند — نه فقط این یکی.

### ۷.۶.۶ آنچه در سند امنیت باید طراحی شود

- **تعریف دقیق «گروهی»:** از چند رکورد به بالا؟ آیا صفحه‌بندی معمولی هم مشمول است یا فقط
  خروجی/صادرات؟
- **الگوریتم امضا** و اینکه چه چیزی امضا می‌شود (بدنه، مسیر، مهر زمانی، nonce).
- **جلوگیری از بازپخش (replay):** امضای بدون مهر زمانی و nonce قابل ضبط و ارسال مجدد است.
- **چرخهٔ حیات:** تعویض دستگاه، از دست رفتن گوشی، ابطال کد یکتا، و ثبت‌نام مجدد مدیر.
- **رمزگذاری کد یکتا:** چه الگوریتمی، و کلید کجا نگهداری می‌شود.
- **ثبت در دفتر رویداد:** هر خروجی گروهی — موفق یا ردشده — باید در `igbz_audit` بنشیند
  (`DESIGN-DEPLOY-VIRA.md §۸`)، و رد شدنش رویداد امنیتی برای ویراست (`§۱۴`).

---

## ۷.۷ 🔴 مسیرهای هستهٔ وردپرس هم باید بسته شوند — الزام کارفرما

> «سیستم باید درخواست‌های این‌چنینی از سوی **هستهٔ وردپرس** را هم بند کنه.»

### ۷.۷.۱ چرا این از بند ۷.۶ هم مهم‌تر است

بند ۷.۶ مسیرهای **خودمان** را محافظت می‌کند. ولی وردپرس و ووکامرس مسیرهای **خودشان** را
دارند که همان داده را می‌دهند و **هیچ‌کدام از کنترل‌های ما از آنها رد نمی‌شود**.

بستن درِ جلو در حالی که درِ پشتی باز است، امنیت نیست. مهاجم سراغ ضعیف‌ترین در می‌رود.

### ۷.۷.۲ درهای پشتی شناخته‌شده

| مسیر | چه می‌دهد |
|---|---|
| `GET /wp-json/wp/v2/users` | فهرست کاربران — نام کاربری و شناسه |
| `GET /wp-json/wc/v3/customers` | **مشتریان ووکامرس با ایمیل، تلفن و آدرس** |
| `GET /wp-json/wc/v3/orders` | سفارش‌ها با اطلاعات کامل خریدار |
| `xmlrpc.php` | رابط قدیمی؛ بردار کلاسیک brute-force و تقویت حمله |
| `?author=N` و فید نویسنده | شمارش کاربران بدون نیاز به REST |
| صادرات وردپرس (`Tools → Export`) | خروجی انبوه از پنل |
| `admin-ajax.php` | مسیرهای دلخواه افزونه‌ها |

### ۷.۷.۳ 🔴 وضعیت امروز — شکاف تأییدشده

بررسی کد نشان داد `Authenticator::rest_authentication_errors()` (خط ۱۳۷) **فقط وقتی وارد
عمل می‌شود که یک توکن Bearer در درخواست باشد**:

```php
$token = self::bearer_token();
if ( '' === $token ) {
    return $errors;   // ← بدون توکن، هیچ کنترلی اعمال نمی‌شود
}
```

یعنی محدودیت نرخِ موجود **فقط روی کاربران اپ** اعمال می‌شود. درخواستی که با **کوکی نشست
وردپرس** یا **Application Password** بیاید، از این فیلتر رد می‌شود و به مسیرهای هستهٔ
وردپرس و ووکامرس می‌رسد — **بدون سقف، بدون امضای بیومتریک، بدون ثبت در دفتر رویداد.**

با مقیاس پروژه (صدها فروشگاه × هزاران کاربر) این یعنی: یک حساب ادمین فروشگاه که لو برود،
می‌تواند کل پایگاه مشتریان را از `wc/v3/customers` بیرون بکشد و هیچ‌کدام از کنترل‌های
بند ۷.۶ جلویش را نمی‌گیرد.

### ۷.۷.۴ قاعده

> هر مسیری که **داده‌های کاربران را گروهی** برمی‌گرداند — چه مالِ ما، چه هستهٔ وردپرس،
> چه ووکامرس، چه هر افزونهٔ دیگر — مشمول **همان** کنترل بند ۷.۶ است.

**رویکرد درست: فهرست سفید، نه فهرست سیاه.** شمردن درهای پشتی و بستن یکی‌یکی محکوم به
شکست است، چون افزونهٔ بعدی یک در تازه باز می‌کند. باید **پیش‌فرض بسته** باشد و مسیرهای
مجاز صریحاً اجازه بگیرند.

### ۷.۷.۵ آنچه در سند امنیت باید طراحی شود

- **گیت مرکزی روی `rest_pre_dispatch`** (نه `rest_authentication_errors` که فقط با توکن
  کار می‌کند) تا همهٔ مسیرها — از جمله هستهٔ وردپرس و ووکامرس — از آن رد شوند.
- **`xmlrpc.php`:** خاموش‌کردن کامل یا مسدودسازی در nginx.
- **شمارش کاربران:** بستن `?author=N`، فیدهای نویسنده، و `oEmbed`.
- **صادرات پنل وردپرس:** محدود به مدیر ارشد یا کاملاً بسته.
- **`admin-ajax.php`:** بازبینی مسیرهای ثبت‌شده.
- **Application Passwords:** خاموش‌کردن، چون یک مسیر احراز هویت موازی است که کنترل‌های ما
  را دور می‌زند.
- **رابطه با nginx:** بعضی از اینها بهتر است در لایهٔ وب‌سرور بسته شوند تا اصلاً به PHP
  نرسند (هم‌راستا با «۱۳ لایه» و قاعدهٔ «هیچ کار سنگینی در مسیر درخواست»).

### ۷.۷.۶ وضعیت اجرا — ۱۴۰۵/۰۶/۰۶ (فاز ۰۶ ✅)

همهٔ اقلام بالا در `CoreSurfaceGuard` پیاده و آزمونه شدند: گیت `rest_pre_dispatch` (از قبل)،
خاموشی XML-RPC و Application Passwords با تنظیم پیش‌فرض روشن، بستن `?author=N` برای بازدید
ناشناس، حذف کامل مسیر/پیوندهای oEmbed (`security.disable_oembed` + لایهٔ دوم حذف نام نویسنده)،
نگهبانی صادرات پنل، و بازبینی admin-ajax (تنها اکشن ناشناس `igbz_otp` با nonce و کول‌داون OTP؛
سطح ناشناس با پین در `DriftGuardTest` قفل شد). **مسیر اضطراری مدیر ارشد** افزوده شد:
`security.senior_admin_id` که فقط super admin می‌تواند بنویسدش و هر استفادهٔ آن از گیت‌ها با
ثبت «security» در دفتر رویداد همراه است (`DoorsTest`). تأیید زنده در سندباکس: oEmbed ‏۰۴ و
Application Passwords ‏۴۰۳؛ پروب ناشناس `?author=N` در این محیط به‌دلیل ورود خودکار
Playground معنا ندارد و رفتار ناشناس با تست واحد پوشش داده شد (محدودیت محیط، ثبت شد).

### ۷.۷.۷ وضعیت اجرا — ۱۴۰۵/۰۶/۰۶ (فاز ۰۷ ✅)

مجوز سطح شیء برای مسیرهای تجاری چندمستأجری (مطابق الگوی مالکیت سمت سرور برای
هر خواندن/نوشتن با شناسه). قاعدهٔ اجرا: هر خواندن «شیء با شناسه» در ماژول‌های تجاری باید
`AND tenant_id = %d` با شناسهٔ مستأجر جاری بخورد؛ شناسهٔ شیء به‌تنهایی هرگز از مرز
مستأجر عبور نمی‌کند.

- **اجراشدگان:** `LmsService` (دوره، دوره با شناسهٔ محصول، درس، آزمون، ثبت‌نام و سه
  خواندن خام ثبت‌نام در مسیر پیشرفت/گواهی)، `PlanService::renew` (اشتراک)،
  `BnplService` (قرارداد، دو خواندن قسط با شناسه، فهرست اقساط قرارداد)،
  `AffiliateService::find`، `PaymentService` (پرداخت با شناسه و فهرست پرداخت‌های کاربر)،
  و پنل ادمین `LmsPage` (حذف آزمون فقط برای آزمونِ خود مستأجر — حذف اول مالکیت را
  اثبات می‌کند، بعد تلاش‌ها پاک می‌شوند؛ و لغو ثبت‌نام با شرط مستأجر).
- **استثناهای تعمدی (ثبت‌شده و بازبینی‌شده):**
  - `PaymentService::payment_by_authority` بدون شرط مستأجر: authority یک راز
    «دارنده‌کلید» است که کال‌بک‌های ناهمگام درگاه با آن برمی‌گردند؛ در آن لحظه ممکن
    است مستأجر جاری هنوز resolve نشده باشد. امنیت با خودِ راز تأمین می‌شود.
  - `AffiliateService::find_by_code` بدون شرط مستأجر: کد معرف هویت عمومی ارجاع در
    لحظهٔ ورود بازدیدکننده است، پیش از آنکه مستأجری وجود داشته باشد.
  - `TenantRepository::find` سراسری: خود شیء مستأجر است و در مسیرهای پنل ادمینِ
    بالاتر از مستأجر (فهرست مستأجرها، داشبورد مالک) خوانده می‌شود.
  - `PlanService::plan` سراسری: جدول طرح‌ها کاتالوگ جهانی است و ستون `tenant_id` ندارد.
- **آزمون منفی (الگوی منفی دسترسی):** `TenantScopeTest` — برای هر دامنه یک ردیف در
  مستأجر ۱ کاشته می‌شود؛ خواندن در مستأجر ۲ باید «پیدا نشد» بدهد و در مستأجر ۱ پیدا
  شود (دوره، درس، آزمون، ثبت‌نام، دوره با محصول، پرداخت، فهرست پرداخت کاربر،
  همکار، قرارداد اقساط). کفپوش دابل `LmsDb` شرط مستأجر را در جست‌وجوی شناسه رعایت
  می‌کند. نتیجه: ۱۳۸۵ اظهارنظر در ۲۸ کیس سبز، لینت ۲۴۹ فایل صفر خطا.

### ۷.۷.۸ وضعیت اجرا — ۱۴۰۵/۰۶/۰۶ (فاز ۰۸ ✅)

ادامهٔ الگوی مالکیت سمت سرور برای مسیرهای جانبی: لجستیک، مارکت‌پلیس، گیمیفیکیشن، دامنه
و پرداخت اصلی. سئو/ترجمه/کاتالوگ‌ها از قبل مستأجر-محور بودند و ممیزی شدند.

- **لجستیک:** ثبت درگاه/تحویل/خواندن مرسوله در `LogisticsService` شرط مستأجر گرفتند؛
  مسیر «پرداخت درون‌اپ تحویل» (`cod_app_paid`) هم همین‌طور. چت پیک فقط روی مرسولهٔ
  منتسب به همان پیک باز می‌شود (`chat`/`send_chat` با گیت مالکیت)؛ `chat_get` هم گیت
  پیک بودن گرفت. مسیرهای مشتری «وضعیت/مسیر زنده» با اثبات دارنده‌کلید = کد رهگیری
  (`hash_equals`) و شرط مستأجر محافظت شدند؛ بارکد در پرداخت درون‌اپ نیز مستأجر-محور شد.
  چاپ لیبل (`group_shipments`) با پیوند گروه لیبل به مستأجر بسته شد.
- **مارکت‌پلیس:** پیوند محصول↔کانال (`link`) کلید مستأجر گرفت تا شناسهٔ محصول جهانی
  نتواند پیوند مستأجر دیگر را بخواند یا بازنویسی کند؛ شمارنده‌های پنل هم مستأجر جاری را
  می‌خوانند.
- **گیمیفیکیشن:** اعتبار هوش مصنوعی (`balance`/ایدمپوتنسی دفتر کل) و سبد رهاشده
  (`watch`) شرط مستأجر (و کاربر) گرفتند.
- **پرداخت اصلی:** چک ایدمپوتنسی `hold` و عملیات‌های `open_dispute`/`refund` مستأجر-محور
  شدند؛ اختلاف فقط روی پرداخت متعلق به همان مستأجر می‌نشیند.
- **دامنه (نقص شناخته‌شدهٔ تأیید):** `verify_dns` شناسهٔ مستأجر را می‌گیرد و رکورد دامنه
  فقط برای مستأجر خودش فعال می‌شود؛ `sync_tenant_domain` دیگر نمی‌تواند `verified_at`
  نگاشت دامنهٔ متعلق به مستأجر دیگر را جابه‌جا کند. توکن تأیید با `Crypto::token` در هر
  نگاشت تازه ساخته می‌شود. ثبت فرآیندی: پیش از حذف دامنه باید رکورد سمت کاربر پاک شود
  (کنترل عملیاتی، مطابق توصیهٔ منابع چندمستأجری برای جلوگیری از تصاحب زیردامنه).
  صفحات کنترل‌پلین (مانند `TenantsPage` با `MANAGE_TENANTS`) به‌طرح ذاتی سراسری‌اند.
- **آزمون:** چهار سناریوی منفی تازه در `TenantScopeTest` (مرسوله، پیوند مارکت‌پلیس،
  ایدمپوتنسی پرداخت اصلی میان دو مستأجر، چت پیک روی مرسولهٔ بیگانه). نتیجه: تست
  ۱۳۹۳/۲۸ سبز، لینت ۲۴۹/۰.

### ۷.۷.۹ وضعیت اجرا — ۱۴۰۵/۰۶/۰۶ (فاز ۰۹ ✅)

ادامهٔ الگوی مالکیت برای ماژول‌های اینستاگرام، هاب، اف‌ایکس، پادو و موبایل‌ای‌پی‌آی.

- **اینستاگرام:** محتوای مانوس، محصول ورودی (خواندن و حذف)، قیف (گتر شناسه)، قرعه‌کشی
  (قرعه‌کشی روی قیف بیگانه «پیدا نشد» می‌دهد) و دو خواندن کامنت در `VipPage` مستأجر-محور
  شدند.
- **موبایل‌ای‌پی‌آی:** `VipAdminController` (کامنت و پست) شرط مستأجر گرفت.
- **اف‌ایکس:** تسویهٔ دستی صورت‌حساب در پنل شرط مستأجر گرفت؛ حساب‌های ارائه‌دهنده
  (`FxAccountsService`) در خواندن/نوشتن مستأجر-محور شدند.
- **پادو:** تصمیم روی درخواست بازبینی (`decide`) اسکوپ مستأجر می‌گیرد (پنل اسکوپ را از
  `MANAGE_TENANTS`/مستأجر جاری مشتق می‌کند).
- **استثناهای تعمدی (ثبت‌شده):** `ManusService::account(id)` چون زمان‌بند کرون بدون
  مستأجر آن را می‌خواند؛ قلاب ناهمگام `on_payment_verified` اف‌ایکس (شناسه از سامانهٔ
  پرداخت می‌آید نه کاربر، و اعتبار به `tenant_id` خود ردیف واریز می‌شود)؛ خواندن قسط در
  صف اعلان کرون؛ و مسیرهای کنترل‌پلین (دامنه‌وریفایر هاب، صفحهٔ دستگاه‌های موبایل‌ای‌پی‌آی،
  فهرست مستأجرها) که ذاتاً با نقش‌های بالاتر از مستأجر کار می‌کنند.
- **آزمون:** سناریوهای منفی تازه در `TenantScopeTest` (حساب اف‌ایکس و قرعه‌کشی). نتیجه:
  تست ۱۳۹۷/۲۸ سبز، لینت ۲۴۹/۰.

### ۷.۷.۱۰ وضعیت اجرا — ۱۴۰۵/۰۶/۰۶ (فاز ۱۰ ✅)

درخواست‌های بیرونی و مهار درخواست ساختگی سمت سرور (مطابق الگوی دروازهٔ متمرکز، نه
رشته‌سنجی پراکنده):

- **`UrlGuard` (Support):** اسکیم فقط http/https؛ میزبان خالی رد می‌شود؛ آی‌پی‌های خصوصی،
  حلقه‌ای، پیوند-محلی (شامل بازهٔ متادیتای ابری)، نشانی نامشخص و CGNAT و معادل‌های IPv6
  رد می‌شوند؛ میزبان‌های دارای کولون به‌عنوان IPv6 سنجیده و بدفرم‌ها رد می‌شوند. رزولور در
  دسترس باشد، همهٔ نشانی‌های برگشته باید پاک باشند (پاسخ به ریبایندینگ).
- **مرز اجرا:** لفاف `Http::request` پیش از هر درخواست از دروازه می‌گذرد و آدرس ردشده هرگز
  به حمل‌ونقل نمی‌رسد و در کانال «امنیت» ثبت می‌شود. دنبال‌کردن تغییرمسیر خاموش شد (هر هاپ
  از دروازه فرار می‌کرد)؛ سقف اندازهٔ پاسخ پیش‌فرض ۲۰ مگابایت با کلید `max_bytes`.
- **نقاط دریافت خارج از لفاف:** تبدیل گفتار (مالتی‌پارت) و دو نقطهٔ دانلود رسانه
  (استودیو هوش مصنوعی و محصول ورودی = نشانی‌های کاربرساخت) جداگانه از دروازه عبور کردند.
- **سیاست ارسال حامل:** دانلود زیپ پادو دیگر کلید را به هر میزبانی نمی‌فرستد؛ بریر فقط
  وقتی همراه می‌رود که میزبان مقصد همان میزبان پیکربندی‌شدهٔ سرویس باشد.
- **محدودیت ثبت‌شده:** رزولور سندباکس تست برای هر نامی نشانی خصوصی برمی‌گرداند؛ آزمون‌ها
  با آی‌پی‌های قطعی و نام‌های رزرو `.test` اجرا می‌شوند و رفتار رزولوشده فقط در تولید فعال است.
- **آزمون:** `UrlGuardTest` (اسکیم‌ها، بازه‌ها، میزبان‌های براکت‌دار، و اثبات اینکه آدرس ردشده
  به حمل‌ونقل نمی‌رسد). تست ۱۴۳۶/۲۹ سبز، لینت ۲۵۱/۰.

### ۷.۷.۱۱ وضعیت اجرا — ۱۴۰۵/۰۶/۰۶ (فاز ۱۱ ✅)

بارگذاری، آرشیو و مصنوعات (مطابق الگوی «قضاوت ورودی‌به‌ورودی پیش از نشستن چیزی روی دیسک»):

- **`ArchiveGuard` (Support):** دروازهٔ مشترک آرشیو — سقف تعداد ورودی (۲۰۴۸)، سقف حجم
  بازشدهٔ اعلام‌شده در سرصفحه (۱۰ مگابایت، هم‌تراز `ThemeValidator`)، رد نام‌های دارای
  `..` یا مسیر مطلق یا بک‌اسلش، و بررسی امضای «بایت جادویی» پیش از بازکردن.
- **اجرا:** مسیر دریافت قالب پادو (`ingest_zip`) از منطق پراکندهٔ قبلی به دروازه منتقل شد
  و مسیر پیش‌نمایش/فعال‌سازی (`install_preview`) که پیش‌تر بدون بازبینی استخراج می‌کرد هم
  از همان دروازه می‌گذرد (دفاع در عمق: آرشیو در هر دو نقطهٔ ورود و استخراج قضاوت می‌شود).
- **میم و اندازه در بارگذاری‌ها:** بارگذاری محصول از `wp_handle_upload` با فهرست میم و سقف
  `wp_max_upload_size` استفاده می‌کند (وردپرس خودش اندازه را اعمال می‌کند)؛ بارگذاری پنل
  فروشگاه به زیرشاخهٔ محافظ منتقل می‌شود.
- **کد اجرایی در خروجی مدل:** `ThemeValidator` پیش از این هر فایل قالب را برای الگوهای ممنوع
  (`eval`/`base64_decode`/فراخوانی شبکه و…) پویش می‌کرد و این جلسه تغییری در آن لازم نبود؛
  آرشیو حاوی چنین فایلی در همان قرنطینهٔ اعتبارسنجی رد و فایل موقت پاک می‌شود.
- **آزمون:** `ArchiveGuardTest` با آرشیوهای واقعی (سالم، زیپ‌اسلیپ، سقف تعداد، پی‌اچ‌پی با
  نام zip). تست ۱۴۴۳/۳۰ سبز، لینت ۲۵۳/۰.

### ۷.۷.۱۲ وضعیت اجرا — ۱۴۰۵/۰۶/۰۶ (فاز ۱۲ ✅)

هویت، چرخهٔ توکن و قرارداد امضای بیومتریک:

- **چرخهٔ اتمیک توکن:** `TokenService::refresh` دیگر جداگانه «بخوان/باطل کن/صادر کن» نیست؛
  یک `UPDATE ... WHERE revoked_at IS NULL` توکن را اتمیک تصاحب می‌کند و بازندهٔ رقابت همان
  حکم «استفادهٔ مجدد» را می‌گیرد: کل دستگاه باطل و رویداد امنیتی ثبت می‌شود. نقطهٔ
  `/auth/refresh` هم سقف تلاش در ۵ دقیقه گرفت (کلید: نشانی کلاینت + هش توکن).
- **قرارداد سمت سرور امضای بیومتریک (§۷.۶):** سرویس `BiometricGate` — قرارداد
  `HMAC-SHA256(device_id|user_id|nonce|ts|payload_hash)` با کلید دستگاه؛ سه دروازهٔ مستقل:
  پنجرهٔ زمانی، نانس یک‌بارمصرف (ضد بازپخش) و بررسی امضا در زمان ثابت. کلید دستگاه در
  ثبت‌نام دستگاه با `Crypto::encrypt` ذخیره می‌شود و هرگز بازنمی‌گردد (ستون `signing_key`
  در مهاجرت دیتابیس v22). این دروازه نقطهٔ اجباری هر خروج گروهی اطلاعات کاربر است؛ خودِ
  بیومتریک سمت اپ با کلید امن دستگاه باز می‌شود و سرور فقط امضا را راستی‌آزمایی می‌کند.
- **وضعیت موجود پیش از این فاز (تأیید شد):** چرخهٔ جفت توکن با `jti` پایگاه‌داده‌ای و باطل‌سازی
  فوری، هش ذخیره‌شدهٔ توکن نوسازی، رمزنگاری عددی با `Crypto`، کول‌داون شماره+نشانی در
  ارسال و سقف تلاش در تأیید.
- **آزمون:** `BiometricGateTest` (پذیرش، بازپخش نانس، برچسب زمانی کهنه، امضای جعلی، قرض
  گرفتن ردیف دستگاه). تست ۱۴۵۰/۳۱ سبز، لینت ۲۵۵/۰، دیتابیس 22.

### ۷.۷.۱۳ وضعیت اجرا — ۱۴۰۵/۰۶/۰۶ (فاز ۱۳ ✅)

حریم خصوصی داده، دفتر رویداد و خروج از سرویس:

- **ماسک در لحظهٔ ورود (نه نمایش):** `Logger::redact` علاوه بر کلیدهای راز، کلیدهای دادهٔ
  شخصی را هم می‌پوشاند: تلفن فقط چهار رقم آخر، ایمیل حرف اول + دامنه، نشانی/کد ملی/کارت و
  مانند آن‌ها به‌طور کامل جایگزین می‌شوند — و همین رفتار برای آرایه‌های تودرتو هم اجرا
  می‌شود. دادهٔ شخصی اگر به جدول برسد دیگر «نشت احتمالی» نیست، پس پیش از نوشتن پوشانده
  می‌شود.
- **ماندگاری:** از قبل فعال بود و ممیزی شد — کرون روزانه جدول رویداد را با تنظیم
  `log.retention_days` (پیش‌فرض ۳۰ روز) می‌تراشد؛ توکن‌ها و دستگاه‌های مرده هم
  `prune` جدا دارند.
- **خروج مستأجر:** `TenantOffboarding` با قلاب `igbz_tenant_deleted` همهٔ ۶۲ جدول
  مستأجردار را در یک جاروی ثبت‌شده در کانال امنیتی پاک می‌کند؛ جدول‌های بدون ستون مستأجر
  (پیشرفت درس، اقلام گروه لیبل) به ردیف‌های پاک‌شده آویخته‌اند و در سند به‌عنوان باقی‌مانده
  ثبت شدند. حذف خود مستأجر همچنان فقط از کنترل‌پلین با نقش بالاتر از مستأجر ممکن است.
- **حذف افزونه:** `uninstall.php` از قبل وجود دارد و داده فقط با انتخاب صریح اپراتور
  («حذف همهٔ داده‌ها هنگام حذف افزونه») می‌افتد؛ تغییری لازم نداشت.
- **آزمون:** `OffboardingTest` (پوشش جارو روی همهٔ جدول‌ها، شرط مستأجر در هر حذف، ثبت
  امنیتی، و ماسک PII در لاگ). تست ۱۴۶۰/۳۲ سبز، لینت ۲۵۷/۰.

### ۷.۷.۱۴ وضعیت اجرا — ۱۴۰۵/۰۶/۰۶ (فاز ۱۴ ✅)

تعیین مستأجر از هویت معتبر، نه از پارامتر کلاینت (OWASP API1:2023 — BOLA):

- **مسیر واحد `TenantScope::page_tenant_id(?int $requested)`:** شناسهٔ مستأجر در شش نقطهٔ
  خواندن (هر دو فعل واکشی و ذخیرهٔ فرم) سه صفحهٔ ادمین — کیف‌پول، افیلیت و BNPL — دیگر از
  `$_GET/$_POST['tenant_id']` با اعتماد خام نمی‌آید؛ مستأجر از هویت معتبر (منطقهٔ مستأجر
  حل‌شده از درخواست) گرفته می‌شود و فقط دارندهٔ `MANAGE_TENANTS` می‌تواند صفحه را به
  مستأجر دیگری نشانه برود. نقش `ROLE_TENANT_OWNER` دقیقاً `MANAGE_WALLET/BNPL/AFFILIATE`
  را دارد، پس پیش از این مالک می‌توانست کیف‌پول/قرارداد اقساط/افیلیت هر مستأجر دیگری را
  ببیند یا تعدیل کند.
- **الگوی همیشگی:** `BaseController::scoped_tenant_id` از قبل همین قاعده را داشت (مالک قفل
  به مستأجر خود؛ فقط کنترل‌پلین آزاد) و ممیزی شد. صفحات هاب و فهرست مستأجرها ذاتاً با
  `MANAGE_TENANTS` سراسری‌اند و استثنا ثبت شد.
- **پیام‌رسانی وی‌آی‌پی:** مستأجرِ رشتهٔ گفت‌وگوی مشتری با ادمین به‌جای پارامتر کلاینت از
  خود پست (`VipController::message_admin` → خواندن ردیف پست) مشتق می‌شود؛ پست‌ها خود
  مستأجر-محورند، پس مشتری دیگر نمی‌تواند با ادمین فروشگاه بی‌ربط رشته باز کند. خواندن
  رشته‌ها و پیام‌ها همچنان با شرط `user_id` قفل است.
- **آزمون:** `TenantResolutionTest` سناریوی منفی — مالک درخواست `?tenant_id=99` می‌دهد ولی
  پاسخ مستأجر خودش است؛ ادمین پلتفرم با `MANAGE_TENANTS` می‌تواند هدف بگیرد. تست ۱۴۶۴/۳۳
  سبز، لینت ۲۵۹/۰.

### ۷.۷.۱۵ وضعیت اجرا — ۱۴۰۵/۰۶/۰۶ (فاز ۱۵ ✅)

جداسازی منابع مشترک (کش/ترنزینت، فایل، نرخ) بر اساس مستأجر (مطابق الگوی «هر کلید و هر
مسیر ذخیره باید پیشوند مستأجر داشته باشد»):

- **مسیر واحد `TenantScope::cache_key`:** کلیدهای کش و ترنزینت که دادهٔ مستأجر نگه
  می‌دارند با مستأجر حل‌شده نام‌فضا می‌گیرند؛ شش نقطهٔ دادهٔ مستأجر به این مسیر منتقل
  شدند — فلش سئو، نتیجهٔ استودیو هوش مصنوعی، نتیجهٔ ارسال پوش، فلش حساب کاربری،
  نتیجهٔ آزمون، و لینک وی‌آی‌پی هاب. کد بدون مستأجر در فضای صریح `t0` می‌نشیند و هرگز با
  مستأجر یک برخورد نمی‌کند.
- **جریان‌های منی‌چت:** کلید کش به‌جای نام سراسری با هش کلید حساب ساخته می‌شود؛ چون کلاینت
  به‌ازای حساب متصل می‌شود، فهرست جریان یک فروشگاه دیگر به فروشگاه دیگر سرو نمی‌شود.
- **سقف نرخ مستأجر (noisy neighbour):** افزون بر سقف هر توکن، یک سقف تجمیعی دقیقه‌ای برای
  کل مستأجر (`api.tenant_rate_limit_per_minute`، پیش‌فرض ۶۰۰) در `Authenticator` اعمال
  می‌شود تا یک فروشگاه با دستگاه‌های بسیار هم نتواند سهم بقیه را ببلعد. کلید هر دو سطل،
  مستأجر و دقیقه را در خود دارد و میان فروشگاه‌ها و زمان برخورد نمی‌کند.
- **فایل قالب پادو:** `storage_dir_for( tenant )` زیرشاخهٔ جدا می‌سازد تا بارگذاری هم‌نام
  دو فروشگاه روی هم ننویسد.
- **استثناهای سراسری (ثبت‌شده):** کاتالوگ مستأجر (`TenantRepository`)، توکن سرویس گوگل
  (`GoogleAuth`)، دایرکتوری عمومی هاب (`DirectoryService`)، و آنتی‌بروت‌فورس ورود
  (`AuthController`) ذاتاً فراتر از یک مستأجرند و ممیزی شدند.
- **آزمون:** `TenantIsolationTest` (برخورد کلید میان دو مستأجر، فضای `t0`، و هم‌زمانی:
  مستأجر پرمصرف قفل می‌شود ولی مستأجر دیگر سهم کامل خود را دارد). تست ۱۴۷۵/۳۴ سبز، لینت
  ۲۶۰/۰.

### ۷.۷.۱۶ وضعیت اجرا — ۱۴۰۵/۰۶/۰۶ (فاز ۱۶ ✅)

تمامیت جریان ساخت فروشگاه (provisioning) — مطابق الگوی «هر گام باید idempotent باشد و
خرابی، منابع نیمه‌ساخته را پاک کند»:

- **اجرای مجدد امن (idempotency):** ارسال دوبارهٔ ثبت‌نام با همان ایمیل و همان نشانی، اگر
  فروشگاه از قبل برای همان مالک ساخته شده باشد، همان فروشگاه را برمی‌گرداند — نه خطا، نه
  ردیف تکراری. این رفتار با توصیهٔ منابع چندمستأجری هم‌راستاست: «اجرای چندبارهٔ یک درخواست
  ساخت باید همان نتیجهٔ یک‌بار اجرا را بدهد».
- **Rollback جزئی:** هر خطا میان ساخت مستأجر و تلاش پرداخت — عضویت، زیردامنه، نقش مالک،
  اشتراک — مستأجر تازه‌ساخت را با عضویتش حذف می‌کند (حذف مستأجر، جاروی خروج فاز ۱۳ را هم
  فعال می‌کند) و مشتری پیام شکست می‌گیرد. فروشگاه نیمه‌ساخته — مالک بدون عضویت یا مستأجر
  بدون اشتراک — از شکست تمیز بدتر است.
- **تنزل به‌جای شکست:** شکست گام زیردامنه به‌تنهایی فروشگاه را بی‌نشانی نمی‌کند (نشانی
  مسیری مادر باقی است)؛ فقط در کانال هاب ثبت می‌شود و ساخت ادامه می‌یابد.
- **محدودهٔ دست‌نخورده (از قبل درست):** حل کاربر موجود، ساخت کاربر تازه با ایمیل/تلفن،
  انتخاب طرح و چرخهٔ صورتحساب، شروع پرداخت برای طرح‌های پولی بدون دورهٔ آزمایشی.
- **آزمون:** `SignupTest` با دابل ماندگار جدول‌های مستأجر/عضویت/اشتراک/طرح — ساخت کامل،
  اجرای مجدد بدون تکرار، رد نشانی متعلق به مالک دیگر، و خرابی شبیه‌سازی‌شدهٔ اشتراک که باید
  اثر صفر بگذارد. تست ۱۴۹۲/۳۵ سبز، لینت ۲۶۱/۰.

### ۷.۷.۱۷ وضعیت اجرا — ۱۴۰۵/۰۶/۰۶ (فاز ۱۷ ✅)

مسیریابی دامنهٔ مستأجر و جلوگیری از تزریق میزبان/مستأجر:

- **فقط مستأجر قابل‌مسیریابی سرویس می‌گیرد:** دو شاخهٔ رزولور که به بازدیدکننده فروشگاه
  تحویل می‌دهند (دامنه و مسیر) اکنون هر دو با `is_active()` غربال می‌شوند — فروشگاه معلق،
  بسته، در انتظار، یا آزمایشیِ منقضی‌شده به‌محض تغییر وضعیت دیگر روی هیچ نشانی‌ای پاسخ
  نمی‌دهد. پیش از این شاخهٔ مسیر وضعیت را نمی‌سنجید و شاخهٔ دامنه انقضای دورهٔ آزمایشی را
  نادیده می‌گرفت. فال‌بک‌های کاربر/پیش‌فرض عمداً بی‌قید ماندند: آن‌ها بافت پنل مدیریت‌اند و
  مالک باید فروشگاه در انتظار خود را ببیند.
- **نگاشت یک‌به‌یک دامنه↔مستأجر:** `add_domain` دامنه‌ای را که مستأجر دیگری نگه داشته رد
  می‌کند؛ پیش از این `LIMIT 1` رزولور میان دو مدعی انتخاب غیرقطعی داشت و می‌توانست فروشگاه
  اشتباه را سرویس دهد. افزودن مجدد برای همان مستأجر idempotent است.
- **اعتبارسنجی ورودی دامنه:** فقط بخش میزبانِ کوچک‌نوشته با شکل معتبر ذخیره می‌شود؛ اسکیم،
  مسیر و نویسه‌های نامعتبر حذف یا رد می‌شوند تا رشتهٔ ساختگی نتواند وارد جدول مسیریابی شود.
- **ممیزی زیرساخت موجود (از قبل درست):** `DomainVerifier` وضعیت را فقط با رکورد واقعی
  `_igbz-verify` TXT یا CNAME به میزبان پلتفرم یا A به آی‌پی آن تأیید می‌کند؛ رزولور به
  نگاشت بدون `verified_at` اعتنا نمی‌کند؛ میزبان ورودی کوچک‌نویسی، `sanitize` و حذف پورت
  می‌شود و با تطبیق دقیق در جدول سرو می‌شود — جعل هدر میزبان بدون کنترل رکورد بی‌اثر است؛
  درهای ادمین هاب (فهرست/تأیید دامنه) با `MANAGE_TENANTS` محافظت شده‌اند و کرون ساعتی
  دامنه‌های در انتظار را بازبینی می‌کند.
- **آزمون:** `DomainRoutingTest` (توقف سرویس‌دهی مسیر برای معلق، توقف دامنه برای آزمایشی
  منقضی، بی‌اثری دامنهٔ تأییدنشده، یکتایی نگاشت، و نرمال‌سازی/رد ورودی). تست ۱۵۰۴/۳۶ سبز،
  لینت ۲۶۲/۰.

### ۷.۷.۱۸ وضعیت اجرا — ۱۴۰۵/۰۶/۰۶ (فاز ۱۸ ✅)

معماری قالب tenant-scoped (مطابق ممیزی `DESIGN-PADO.md §۱۹.۱` که `switch_theme()` سراسری را
شکاف بحرانی شناخته بود):

- **فعال‌سازی بدون سوئیچ سراسری:** `activate_live` دیگر گزینه‌های `template`/`stylesheet`
  سایت را تغییر نمی‌دهد؛ اسلاگ قالب در ستون `theme` همان مستأجر نوشته می‌شود، قالب‌های
  زندهٔ قبلیِ همان مستأجر بایگانی می‌شوند و نشانهٔ بازگشت ثبت می‌شود. در نتیجه فعال‌سازی
  یک فروشگاه، سایت مادر و بقیهٔ فروشگاه‌ها را دست نمی‌زند.
- **رندر در زمان درخواست:** `TenantThemeRouter` با فیلترهای `template`/`stylesheet` فقط در
  فرانت‌اند، قالب ستون مستأجر را اعمال می‌کند؛ اگر قالب نصب نباشد، پوستهٔ فعلی حفظ
  می‌شود. در معماری تک‌نصبی فعلی این همان «فعال‌سازی زیرسایت» است و با مهاجرت به
  چندسایتی (ADR-0001) ستون `theme` منبع حقیقت باقی می‌ماند.
- **بازگشت مستأجر-محور:** `rollback` قالب قبلی را به ستون همان مستأجر بازمی‌گرداند و فقط
  قالب زندهٔ همان مستأجر را بایگانی می‌کند؛ پیش از این `UPDATE` بدون شرط `tenant_id`
  می‌توانست وضعیت قالب زندهٔ سایر فروشگاه‌ها را بایگانی کند. بازگشت موفق نشانهٔ قبلی را
  پاک می‌کند.
- **پیش‌نمایش با گیت مالکیت:** پارامتر `igbz_theme_preview` فقط وقتی رندر می‌شود که قالب
  وجود داشته باشد، وضعیتش پیش‌نمایش/زنده باشد، متعلق به مستأجرِ در حال سرویس باشد و
  بازدیدکننده عضو آن مستأجر یا ادمین پلتفرم باشد — اسلاگ به‌تنهایی هرگز از مرز مستأجر
  عبور نمی‌کند (هم‌راستا با قاعدهٔ فاز ۰۷).
- **باقی‌ماندهٔ ثبت‌شده:** فایل‌های فیزیکی قالب نصب‌شده پس از حذف مستأجر روی دیسک
  می‌مانند؛ جاروی خروج فاز ۱۳ جدول‌ها را پاک می‌کند ولی پاک‌سازی دیسک در بک‌لاگ است.
- **آزمون:** `ThemeRoutingTest` (فعال‌سازی بدون دست‌زدن به پوستهٔ سایت، بازگشت محدود به
  مستأجر خود، رندر قالب خود فروشگاه و نه قالب نصب‌نشده، و سه سناریوی گیت پیش‌نمایش). تست
  ۱۵۲۰/۳۷ سبز، لینت ۲۶۴/۰.

### ۷.۷.۱۹ وضعیت اجرا — ۱۴۰۵/۰۶/۰۶ (فاز ۱۹ ✅)

چارچوب مهاجرت پایگاه‌داده — تمامیت داده در ارتقاها:

- **قفل تک‌اجراکننده:** یک ارتقا در هر لحظه؛ قفل تازه درخواست دوم را عقب می‌نشاند و قفل
  کهنهٔ فرآیند مرده (بیش از ۱۰ دقیقه) تصاحب می‌شود تا ارتقا پشت یک اجرای مرده نماند.
  تصاحب با بازخوانی توکن انجام می‌شود تا رقابت صادقانه حل شود.
- **نقطهٔ بررسی و ادامه:** پس از هر گام موفق، نسخهٔ انجام‌شده ثبت می‌شود؛ اجرای بعدی از
  همان‌جا ادامه می‌دهد. شکست یک گام، نسخهٔ سراسری را بالا نمی‌برد و قفل را آزاد می‌کند —
  هیچ ارتقایی «نیمه‌اعمال‌شدهٔ بی‌ردپا» نمی‌ماند.
- **ایدِمپوتنس به‌عنوان قرارداد:** امنیت ادامه‌دادن از نقطهٔ بررسی به باز‌اجرایی‌پذیری گام‌ها
  وابسته است؛ همهٔ گام‌های `Activator` از قبل با همین قاعده نوشته شده بودند و اکنون این
  قاعده در مستند چارچوب ثبت شده است.
- **گزارش پیشرفت:** رکورد خواندنی از/به/انجام‌شده/باقی‌مانده برای صفحه‌های مدیریت؛ پس از
  پایان موفق پاک می‌شود.
- **راه بازگشت صادقانه:** گام‌های اسکیمی این پروژه رو‌به‌جلو هستند؛ چارچوب نسخهٔ پیش از
  ارتقا را نگه می‌دارد و بازیابی رسمی = بازگرداندن پشتیبان پایگاه‌داده + نوشتن همان نسخه،
  نه عقب‌گرد خاموش وضعیت اسکیم.
- **سازگاری:** رفتار تاریخی سایت بدون نسخهٔ ثبت‌شده (مهر خوردن بدون اجرای پلکان) حفظ شد.
- **آزمون:** `MigratorTest` (ترتیب و پاک‌سازی پایانی، شکست و ادامه از نقطهٔ بررسی بدون
  اجرای دوبارهٔ گام‌ها، قفل تازه، تصاحب قفل کهنه، و ارتقای مستقیم از نسخهٔ قدیمی با رد
  پله‌های انجام‌شده). تست ۱۵۴۴/۳۸ سبز، لینت ۲۶۶/۰.

### ۷.۷.۲۰ وضعیت اجرا — ۱۴۰۵/۰۶/۰۶ (فاز ۲۰ ✅)

کوئری، ایندکس و صفحه‌بندی — دسترس‌پذیری و رشدپذیری (مطابق الگوی «حذف انبوه باید دسته‌بندی
شود و فهرست‌ها سقف داشته باشند»):

- **مسیر واحد حذف دسته‌ای `Db::delete_batches`:** دسته‌های مرتب بر `id` با سقف تکرار؛
  باقی‌مانده به دور بعدی خانه‌تکانی می‌رود. همهٔ حذف‌های انبوه به آن منتقل شدند: هرس
  روزانهٔ کدهای یک‌بارمصرف و توکن‌ها (`Cron`)، پالایش دستگاه‌های خاموش، توکن‌های
  منقضی/باطل (`TokenService`)، ماندگاری دفتر رویداد (`Logger::prune`)، پاک‌سازی دستی ادمین
  و جاروی خروج مستأجر (`TenantOffboarding`) روی همهٔ جدول‌های مستأجردار.
- **فهرست‌های بی‌کران:** حساب‌های فعال اینستاگرام در کرون به پیمایش کی‌ست صفحه‌بندی‌شده
  تبدیل شدند (همهٔ حساب‌ها بازدید می‌شوند؛ واکشی یک‌جای سراسری حذف شد). فهرست دستگاه‌های
  کاربر به ۱۰۰ تازه‌ترین، پیوندهای مارکت‌پلیس به ۵۰۰۰، قیف‌ها به ۵۰۰، کاتالوگ نرخ اف‌ایکس به
  ۵۰۰ و طرح‌های وی‌آی‌پی به ۱۰۰ ردیف سقف گرفتند — فهرست‌های مدیریت تک‌فروشگاه ذاتاً کوچک‌اند
  و سقف، قرارداد مستند آن‌هاست نه برش پنهان.
- **ایندکس‌های مرکب (دیتابیس ۲۳):** `api_tokens` بر `expires_at`، `refresh_expires_at` و
  `revoked_at` برای هرس و پویش نشست؛ `devices` بر `last_seen_at` برای پالایش دستگاه خاموش.
  بقیهٔ الگوهای داغ (کل‌خورد/توکن/اشتراک/دامنه/دفتر رویداد) از قبل ایندکس مرکب داشتند و
  ممیزی شدند.
- **ثبت صادقانه:** سندباکس پایگاه‌دادهٔ واقعی ندارد؛ ایندکس‌ها از الگوهای کوئری کد مشتق
  شدند و اعتبارسنجی `EXPLAIN` روی دادهٔ حجیم به‌عنوان وظیفهٔ عملیاتی تولید ثبت می‌شود.
- **آزمون:** `BatchTest` (جمع دسته‌ها، توقف در دستهٔ ناقص، سقف اطمینان، و شکل مرتب/محدود
  هر کوئری). تست ۱۵۵۴/۳۹ سبز، لینت ۲۶۷/۰، دیتابیس 23.

### ۷.۷.۲۱ وضعیت اجرا — ۱۴۰۵/۰۶/۰۶ (فاز ۲۱ ✅)

سازگاری با ذخیره‌سازی سفارش ووکامرس (HPOS):

- **انضباط دسترسی:** ممیزی سراسری تأیید کرد هیچ مسیری سفارش را از `wp_posts`/متادیتای پست
  نمی‌خواند؛ همه از لایهٔ CRUD (`wc_get_order` / `wc_get_orders` / متادیتای شیء) عبور می‌کنند
  و بنابراین با هر دو ذخیره‌سازی کار می‌کنند.
- **سطح واحد سازگاری `WooCommerceCompat`:** اعلام رسمی `custom_order_tables` و
  `cart_checkout_blocks` روی `before_woocommerce_init`؛ لینک ویرایش سفارش از
  `OrderUtil::get_order_admin_edit_url` (همان مسیر بازنویسی هسته)؛ نام جدول سفارش‌ها از
  `OrdersTableDataStore` با پیشوند سفارشی. بدون این کلاس، ووکامرس افزونه را در صفحهٔ تنظیمات
  HPOS «ناسازگار» اعلام می‌کند حتی اگر کد سالم باشد.
- **یافتهٔ راستی‌آزمایی زنده:** `OrderUtil::get_order_edit_link` در ووکامرس ۱۱ وجود ندارد؛
  در اجرا روی فروشگاه سندباکس کشف و با `get_order_admin_edit_url` جایگزین شد.
- **آزمون:** `HposOrderFlowTest` با دوقلوی ووکامرس با حالت ذخیره‌سازی قابل تعویض — مُهر مستأجر،
  جریان سفارش تکمیل‌شده (کمیسیون)، آمار درآمد و لینک ویرایش در هر دو حالت؛ نوشتن مستقیم به
  متادیتای پست برای سفارش‌ها ممنوع و اثبات می‌شود. تست ۱۵۸۱/۴۰ سبز.
- **ثبت صادقانه:** تصویربرداری مرورگری (قانون ۱۲) در این سندباکس اجرا نشد — دانلود مرورگر
  مسدود است؛ وارسی زندهٔ DOM صفحات متأثر جایگزین شد. مهاجرت/راستی‌آزمایی داده وظیفهٔ فاز ۲۲ است.

### ۷.۷.۲۲ وضعیت اجرا — ۱۴۰۵/۰۶/۰۶ (فاز ۲۲ ✅)

مهاجرت و راستی‌آزمایی ذخیره‌سازی سفارش:

- **ران‌بوک عملیاتی `RUNBOOK-HPOS.md`:** پیش‌نیازها، روشن‌کردن همگام‌سازی (معنای `'yes'`/`'no'`
  گزینه‌ها — تأییدشده که مقدار `'1'` فعال نمی‌کند)، همگام‌سازی دسته‌ای رسمی
  (`get_next_batch_to_process` + `process_batch` تا صفر شدن `has_orders_pending_sync`)،
  راستی‌آزمایی داده، سوئیچ منبع حقیقت، دورهٔ پایش و بازگشت کنترل‌شده با همان نگهبان
  «صفر بودن صف همگام‌سازی».
- **ممیزی جریان‌ها:** مرجوعی/لغو (`on_order_reversed`) فقط با شناسهٔ سفارش روی جدول‌های خودی
  (`affiliate_commissions`، `enrollments`) کار می‌کند و شیء سفارش را باز نمی‌کند؛ تمدیدهای
  اشتراک و گزارش‌ها نیز روی جدول‌های افزونه‌اند — پس مهاجرت ذخیره‌سازی هیچ دادهٔ جانبی افزونه را
  جابه‌جا نمی‌کند و ریسک آن به خود سفارش‌ها محدود است.
- **آزمون:** سناریوی مرجوعی در `HposOrderFlowTest` به هر دو حالت افزوده شد (۱۵۸۷/۴۰ سبز).
- **ثبت صادقانه:** مهاجرت روی دادهٔ واقعی تولید/استیجینگ در سندباکس اجرا نشد (دیتابیس ماندگار
  ندارد)؛ سند برای اجرای عملیاتی است و نگهبان‌هایش در فروشگاه زندهٔ سندباکس آزموده شدند.

### ۷.۷.۲۳ وضعیت اجرا — ۱۴۰۵/۰۶/۰۶ (فاز ۲۳ ✅)

هستهٔ صف بادوام کار (پیش‌نیاز موج سه):

- **جدول `jobs` (دیتابیس ۲۴):** وضعیت/تلاش‌ها/مهلت ادعا/کلید همسانی/پاکت؛ ایندکس مرکب
  `(status, available_at)` برای پیمایش ادعا و یکتای `(queue, idempotency_key)` برای جلوگیری از
  صفوف تکراری در رقابت.
- **قرارداد تحویل:** حداقل‌یک‌بار — ادعا هرگز سطر را حذف نمی‌کند، لیز می‌دهد؛ کارگر مرده با
  انقضای اجاره برمی‌گردد (یا اگر تلاش‌ها تمام شده باشد به بن‌بست می‌رود). هندلرها باید همسان
  باشند؛ کلید همسانیِ عمل در همهٔ تلاش‌ها ثابت می‌ماند و هرگز بازتولید نمی‌شود.
- **پاکت نسخه‌دار:** نسخهٔ بالاتر از فهم کارگر = بن‌بست با دلیل، نه حدس. شناسهٔ ردیابی و مستأجر
  از صف تا لاگ‌ها همراه کار می‌مانند.
- **تلاش مجدد:** پشت‌بند نمایی سقف‌دار + لرزش (جلوگیری از هجوم هم‌زمان پس از یک قطعی بالادست)؛
  خطای هر تلاش ثبت می‌شود؛ کار سمی پس از آخرین تلاش به بن‌بست می‌رود و هرگز دوباره ادعا نمی‌شود.
- **آزمون:** `JobQueueTest` — ۱۱ سناریو شامل رقابت ادعا، انقضای اجاره، لرزش/سقف پشت‌بند،
  ایزوله‌سازی سه نوع کار غیرقابل‌اجرا و لغو. تست ۱۶۷۱/۴۱ سبز.
- **باگ نهفتهٔ کشف‌شده در این فاز:** نخستین ارتقای واقعی دیتابیس (۲۳→۲۴) روی فروشگاه زنده با
  خطای نوع سقوط کرد — ارجاع `[ کلاس, 'متد خصوصی' ]` در نردبان مهاجرت از دید کلاس `Migrator`
  callable نبود (از فاز ۱۹ نهفته بود؛ محیط سندباکس همیشه از مسیر «نسخهٔ صفر = مهر اولیه» می‌گذشت
  و هرگز نردبان را اجرا نکرده بود). همهٔ گام‌ها عمومی شدند و آزمون بازگشتی
  `real_activation_ladder_is_constructible` اکنون نردبان واقعی را دقیقاً مثل تولید می‌سازد.

### ۷.۷.۲۴ وضعیت اجرا — ۱۴۰۵/۰۶/۰۶ (فاز ۲۴ ✅)

انتقال کارهای پنج‌دقیقه‌ای به صف بادوام:

- **الگو:** ضربان کرون فقط صف‌بندی می‌کند (با کلید همسانیِ بازهٔ پنج‌دقیقه‌ای — ضربان تکراری
  وردپرس‌کرون جذب می‌شود)؛ `QueueRunner` در اولویت ۵۰ همان ضربان با بودجهٔ کار/زمان تخلیه می‌کند
  و هرچه نرسد برای ضربان بعد می‌ماند — صف منبع حقیقت است، نه اجرای کرون.
- **کارهای منتقل‌شده:** `ig.content.tick`، `ig.intake.tick`، `ig.vip.publish_due`،
  `ig.vip.expire_due` (ماژول اینستاگرام) و `marketplace.sync` (چندمستأجری). سوئیچ مارکت‌پلیس در
  زمان اجرا داخل هندلر بررسی می‌شود تا تغییر دیرهنگام تنظیمات هم محترم بماند.
- **رفتار شکست:** عقب‌نشینی آینده‌دار رعایت می‌شود (اجرای زودرس ممنوع)، آخرین شکست به بن‌بست با
  دلیل می‌رود؛ `CronJobsTest` شامل ۷ سناریوی انتها-به-انتها با جاسوس‌های سرویس است.
- **باگ واقعی آزمون زنده:** هوک وردپرس به هر شنونده دست‌کم یک آرگومان می‌دهد؛ اتصال مستقیم
  `run()` با امضای تایپی در نخستین ضربان زنده سقوط کرد — پوستهٔ بدون‌آرگومان `on_beat` جایگزین شد
  و آزمون بازگشتی دارد. آزمون زندهٔ ضربان: هر ۵ کار `done` شدند.

### ۷.۷.۲۵ وضعیت اجرا — ۱۴۰۵/۰۶/۰۶ (فاز ۲۵ ✅)

انتقال کارهای ساعتی با عدالت مستأجر، بسته‌بندی و بازصف‌بندی:

- **پخش‌بینی مستأجر:** جاروب‌های ساعتی چندمستأجری (`bnpl.overdue`، `bnpl.reminders`،
  `carts.sweep`) دیگر یک اسکن سراسری با LIMIT نیستند؛ برای هر مستأجر فعال (شامل دورهٔ آزمایشیِ
  نگذشته) یک کار صف می‌شود (`JobQueue::fan_out_tenants`). کلید همسانی `بازهٔ ساعتی:مستأجر`
  ضربان تکراری وردپرس‌کرون را در هر مستأجر جذب می‌کند.
- **عدالت ادعا:** `claim` ردیف‌های سررسید را نوبتی بین `group_key`ها (مستأجر) برمی‌دارد — یک دور
  یک کار از هر گروه، تا بودجه تمام شود یا صف خشک شود. هیچ مستأجری با پس‌لاگ بزرگ نمی‌تواند
  بقیه را گرسنه نگذارد؛ کارهای بدون گروه (قدیمی/کنترل‌پلن) یک گروه واحد هستند و رفتار قبلی حفظ شده.
- **قرارداد ادامه:** هر هندلر با بچهٔ سقف‌دار کار می‌کند (۲۰۰ برای BNPL، ۵۰ برای سبد، ۲۰ برای قیف).
  بچهٔ پُر یعنی احتمالاً ردیف مانده — دور بعد با کلید مشتق (`کلید:rN`) بلافاصله صف می‌شود؛ سقف
  ۱۰ دور در ساعت بدترین حالت را کران می‌دهد. کلید مشتق از دور قبل می‌گیرد، پس بازپخش یک دور
  هرگز شاخهٔ تکراری نمی‌سازد.
- **اسکوپ مستأجر در سرویس‌ها:** `process_overdue/send_reminders/sweep/retry_failed` پارامتر
  `tenant_id` گرفتند؛ صفر یعنی اسکن سراسری قدیمی (سازگاری کامل). همهٔ گیت‌های تنظیمات
  (`abandoned_cart.enabled`، `manus.collect_insights`، `hub.enabled` و فاصلهٔ همگام‌سازی هاب)
  به زمان اجرا داخل هندلر منتقل شدند — الگوی فاز ۲۴.
- **کارهای کنترل‌پلن:** `ig.funnels.retry` (با قرارداد ادامه)، `ig.insights.reconcile` و
  `hub.tick` تک‌کار ماندند؛ `QueueRunner` در ضربان ساعتی هم تخلیه می‌کند تا کار همان درخواست
  شروع شود نه پنج دقیقه بعد.
- **پوشش:** `HourlyJobsTest` با ۱۰ سناریو + `get_col` برای دوقلوی صف. آزمون زندهٔ ضربان ساعتی:
  ۳ کار کنترل‌پلن و ۶ کار پخش‌بینی (۲ مستأجر فعال × ۳ جاروب) همه `done` در همان ضربان؛ ضربان
  تکراری جذب شد (۱۴ ردیف ثابت). تست ۱۷۴۶/۴۳ · لینت ۲۷۶/۰.

### ۷.۷.۲۶ وضعیت اجرا — ۱۴۰۵/۰۶/۰۶ (فاز ۲۶ ✅)

انتقال کارهای روزانه و پاکسازی‌ها به صف بادوام:

- **ده کار روزانه:** `plans.renewals`، `affiliate.commissions`، `marketplace.flush`،
  `master.release` (چندمستأجری)، `ig.insights.collect` (اینستاگرام)، `api.prune` (REST)،
  `fx.billing.bills`، `fx.billing.settle`، `fx.ramp.fund` (ارزی) و `cron.housekeeping`
  (نگهداشت: هرس لاگ + پاکسازی دسته‌ای `otp_codes`/`api_tokens`). هر کدام با کلید همسانیِ
  بازهٔ روزانه (`JobQueue::slot(DAY_IN_SECONDS)` — نیمه‌شب UTC)؛ ضربان تکراری جذب می‌شود.
- **قرارداد ادامهٔ رسمی:** منطق «بچهٔ پُر → دور بعد با کلید مشتق، با سقف دور» از کپی در ماژول‌ها
  به `JobQueue::continue_round()` ارتقا یافت — یک نقطهٔ واحد برای همهٔ جاروب‌ها (فاز ۲۵ هم به آن
  بازنشانی شد). کارهای سقف‌دار روزانه: تمدید ۱۰۰، پورسانت ۲۰۰، آزادسازی سپرده ۱۰۰، تسویهٔ
  ارزی ۵۰ — همه با سقف ۱۰ دور.
- **تفکیک تسویهٔ ارزی:** `FxBillingService::run_daily()` به `bill_accounts()` (ایجاد صورتحساب
  ماهانه — ذاتاً همسان، یک قبض به‌ازای هر حساب در هر دوره) و `settle_due(limit)` (بچهٔ محدود با
  شمارش برگشتی برای ادامه) تفکیک شد؛ `run_daily()` برای سازگاری باقی ماند.
- **الگوی همیشگی:** گیت‌های تنظیمات (`master_payment.enabled`، `manus.collect_insights`) در زمان
  اجرا داخل هندلر بررسی می‌شوند؛ `QueueRunner` در ضربان روزانه هم تخلیه می‌کند تا کار همان
  درخواست انجام شود.
- **پوشش و آزمون زنده:** `DailyJobsTest` با ۹ سناریو (جذب تکرار، ادامه تا بچهٔ ناقص، سقف دور،
  گیت‌های زمان اجرا، کف ۳۰ روز نگهداری دستگاه‌ها، قرارداد رسمی ادامه). آزمون زندهٔ ضربان روزانه:
  هر ۱۰ کار در همان ضربان `done` شدند و ضربان تکراری جذب شد. تست ۱۷۸۳/۴۴ · لینت ۲۷۷/۰.

### ۷.۷.۲۷ وضعیت اجرا — ۱۴۰۵/۰۶/۰۶ (فاز ۲۷ ✅)

مشاهده‌پذیری و آزمون صف:

- **آمار:** `JobQueue::stats()` شمارش هر وضعیت + سن قدیمی‌ترین کار منتظر (نخستین نشانهٔ دونده‌ای
  که عقب افتاده) را می‌دهد؛ `dead_letters()` پشت‌لاگ بن‌بست‌ها را تازه‌ترین-اول برمی‌گرداند.
- **بازپخش کنترل‌شده:** `replay()` فقط کار بن‌بست‌خورده را به صف برمی‌گرداند، تلاش‌ها صفر می‌شود
  ولی کلید همسانی حفظ می‌شود — بازپخش همان عملیات منطقی است و محافظت «تحویل تکراری نشود» باید
  از آن جان سالم به در ببرد.
- **سطوح عملیاتی:** داشبورد `JobsPage` (فهرست وضعیت‌ها، سن انتظار، جدول بن‌بست‌ها با دکمهٔ
  بازپخش نانس‌دار) و دستور `wp igbz jobs stats|dead|replay|drain` — ثبت فقط وقتی `WP_CLI`
  تعریف شده باشد؛ در وب هیچ اثری ندارد.
- **آزمون فشار (`QueueLoadTest`):** (۱) ۳۰+۵+۵ کار برای سه مستأجر با بودجهٔ ادعای ۱۵ — هر سه
  دقیقاً ۵ سهم می‌گیرند و در دورهای بعد همهٔ ۴۰ کار دقیقاً یک‌بار ادعا می‌شود؛ (۲) دو کارگر روی
  یک استخر مجموعه‌های جدا می‌برند و کار سوم خشک می‌بیند؛ (۳) قطع بالادست: تلاش اول با پشت‌بند
  آینده زمان‌بندی می‌شود (دلیل روی ردیف می‌ماند)، تلاش دوم به بن‌بست با دلیل و شمارش تلاش می‌رود؛
  (۴) بازپخش زنده اجرا و `done` می‌شود و بار دوم رد می‌شود؛ (۵/۶) آمار و ترتیب بن‌بست‌ها.
- صفحهٔ زندهٔ داشبورد با ۲۰۰ رندر شد. تست ۱۸۱۴/۴۵ · لینت ۲۸۰/۰.

### ۷.۷.۲۸ وضعیت اجرا — ۱۴۰۵/۰۶/۰۶ (فاز ۲۸ ✅)

دفتر کل کیف پول — چرخهٔ رزرو و تطبیق:

- **رزرو:** `reserve()` یک بدهی با دلیل `reserve` است؛ مبلغ از موجودیِ در دسترس خارج می‌شود تا
  جریان‌های پرداخت هم‌زمان نتوانند دوبار خرجش کنند. همسان بر کلید مرجع.
- **آزادسازی:** `release_reserve()` مبلغ رزروشده را دقیقاً یک‌بار برمی‌گرداند؛ کلید لجر تکرار را
  جذب می‌کند و رزرو تسویه‌شده با خطای `already_settled` رد می‌شود.
- **تسویه:** `settle_reserve()` مصرف رزرو را با یک نهاد صفر-مبلغ با جهت `mark` دائمی ثبت می‌کند —
  چون بدهیِ رزرو قبلاً پول را خارج کرده، تسویه پولی جابه‌جا نمی‌کند؛ آزادسازیِ پس از تسویه رد
  می‌شود (`already_released` در جهت عکس). تنها مسیر مجاز برای نهاد صفر.
- **بازپرداخت:** `refund()` با قرارداد مرجع `refund:{اصل}:{بازپرداخت}`؛ مجموع بازپرداخت‌های یک
  بدهی اصلی هرگز از مبلغ آن فراتر نمی‌رود (`over_refund` قبل از هر نوشتن بررسی می‌شود)؛ تکرارِ یک
  بازپرداخت `duplicate` برمی‌گرداند — همسانی مقدم بر گاردهاست.
- **تطبیق و وارسی:** `check_invariant()` برابری موجودی کش‌شده با مجموع لجر برای یک کیف پول؛
  `reconcile_all()` پیمایش keyset محدود روی همهٔ موجودی‌ها و تعمیر انحراف (لجر منبع حقیقت است).
  به‌صورت کار روزانهٔ `wallet.reconcile` به صف بادوام وصل شد.
- **پوشش:** `WalletLedgerTest` با ۷ سناریو — از جمله رقابت دو برداشت ۸۰ از موجودی ۱۰۰ که دقیقاً
  یکی موفق می‌شود (قفل مشورتی + تراکنش)، و تعمیر انحرافِ عمداً واردشده. آزمون زنده: کار تطبیق در
  ضربان روزانهٔ واقعی `done` شد و انحراف صفر گزارش شد. تست ۱۸۶۵/۴۶ · لینت ۲۸۱/۰.

### ۷.۷.۲۹ وضعیت اجرا — ۱۴۰۵/۰۶/۰۶ (فاز ۲۹ ✅)

هستهٔ تراکنش و صندوق ورودی وب‌هوک:

- **صندوق بادوام (`webhook_events`، اسکیمای v25):** هر اعلان ورودی اول درج می‌شود — مسیر درخواست
  فقط ذخیره می‌کند و ۲۰۰ برمی‌گرداند؛ پردازش بعداً و غیرهم‌زمان است. کلید یکتای `(source, event_key)`
  تکرار را در سطح دیتابیس می‌گیرد و پاسخ `duplicate` با شناسهٔ نسخهٔ اصلی برمی‌گردد.
- **امضا:** `verify_signature()` — HMAC-SHA256 روی بدنهٔ خام با سکرت هر سورس
  (`webhooks.{source}.secret`، با سکرت مشترک `webhooks.hmac_secret` به‌عنوان fallback) و مقایسهٔ
  `hash_equals`. نتیجه روی رویداد ثبت می‌شود (`valid`/`invalid`) و تحویل نامعتبر هرگز به هندلر
  نمی‌رسد؛ در درین با دلیل «امضای نامعتبر» مرده ثبت می‌شود.
- **درین با ادعای اتمی:** `claim_due()` رویدادهای سررسید را با UPDATE شرطیِ چسبیده به وضعیت به
  `processing` می‌برد — دو درین هم‌زمان هرگز یک رویداد را نمی‌گیرند. `process_batch()` نتیجه را
  شمارش می‌کند (انجام/نامعلوم/شکست/مرده).
- **وضعیت نامعلوم:** هندلر می‌تواند `unknown` برگرداند — رویداد با بک‌آف توانی (پایهٔ ۳۰ ثانیه،
  سقف یک ساعت) به استخر برمی‌گردد و تا روشن‌شدن یا اتمام تلاش‌ها تکرار می‌شود؛ آن‌گاه با دلیل،
  مرده ثبت می‌شود. شکست هندلر (استثنا) همین چرخه را دارد. هیچ رویدادی بی‌صدا حذف نمی‌شود.
- **ماشین وضعیت پرداخت (`PaymentStateMachine`):** وضعیت‌ها `created → pending → paid|failed|cancelled`
  به‌علاوهٔ وضعیت درجه‌یکِ `unknown` برای پاسخ مبهم درگاه؛ `paid`/`failed`/`cancelled` پایانی‌اند.
  هر نوشتن، UPDATE شرطی روی وضعیتِ خوانده‌شده است: بازندهٔ رقابت به‌جای خراب‌کاری `lost_race`
  گزارش می‌کند. `PaymentService::apply_notification()` مسیر اعلان ناهم‌گام درگاه است و تسویهٔ
  پرداخت موفق را دقیقاً از همان کد مسیر بازگشت مرورگر می‌گذراند.
- **سطوح:** اندپوینت `POST /igbz/v1/webhook/{source}` (بدون لاگین؛ اعتبار فقط با امضا) دریافت می‌کند؛
  در نبود هدر شناسه، هش بدنه کلید تکرار می‌شود. کار پنج‌دقیقه‌ای `webhooks.drain` با ادامه‌دهیِ
  دسته‌ای (دستهٔ ۲۰) اینباکس را تخلیه می‌کند؛ سورس `psp` به `apply_notification` متصل شد.
- **پوشش:** `WebhookInboxTest` با ۸ سناریو — دریافت/تکرار، صحت امضا و پنج حالت رد، درین و تکمیل،
  مرده‌شدن تحویل نامعتبر، چرخهٔ کامل تلاش‌های نامعلوم تا مرده‌شدن با بک‌آف، هندلر خطادهنده،
  ماشین وضعیت (گذار مجاز/غیرمجاز/یافت‌نشدن/رقابت)، و ادامهٔ کارِ درین روی دستهٔ پر. آزمون زنده:
  مهاجرت واقعی به v25 روی پلی‌گراند، دریافت→تکرار→درین→آمار هم‌خوان، و پاسخ صحیح اندپوینت REST.
  تست ۱۹۲۸/۴۷ · لینت ۲۸۵/۰.

### ۷.۷.۳۰ وضعیت اجرا — ۱۴۰۵/۰۶/۰۶ (فاز ۳۰ ✅)

سخت‌سازی آداپتورهای درگاه ریالی (بدون آزمون سندباکس درگاه‌ها — طبق خود فاز):

- **تایم‌اوت واحد:** ۲۰ تایم‌اوت هاردکدشدهٔ ۲۵/۳۰ در ۱۱ فایل به یک مقدار مشترک تبدیل شد —
  `PspHttp::timeout()`، تنظیم `payments.timeout`، پیش‌فرض ۳۰ ثانیه، کلمپ ۵ تا ۶۰.
- **طبقه‌بندی خطای مشترک (`GatewayErrors::classify()`):** شکست دورِ HTTP → `network_timeout`
  (گذرا — تکرارِ تأیید/ریفاند امن است)؛ بدنهٔ خالی → `invalid_response`؛ کد و پیام خود درگاه
  هرگز بازنویسی یا حذف نمی‌شود؛ در نبود آن‌ها کد جایگزین آداپتور. روی زرین‌پال و HttpPsp اعمال
  شد — پیش از این، تایم‌اوت و ردِ دائمی درگاه هر دو یکسان به چشم می‌آمدند.
- **ریفاند به‌صورت قابلیت:** `RefundableGatewayInterface` + `PaymentRefundResult`. فقط درگاه‌هایی
  که سرویس مستند دارند پیاده می‌کنند؛ برای زرین‌پال چیزی ساخته نشد (تحقیق ۱۴۰۵/۰۶: این سرویس حسب
  دستور بانک مرکزی موقتاً غیرفعال است). `HttpPspGateway` با تنظیم `payments.httppsp.refund_url`
  فعال می‌شود و کلید `Idempotency-Key` برایش اجباری است.
- **همسانیِ ایجاد:** `PaymentService::start()` ردیف `created` با همان (مستأجر/سفارش/هدف/درگاه/مبلغ)
  را بازیابی می‌کند به‌جای درج تکراری؛ هر درخواست با کلید `pay-{id}` (یا کلید تماس‌گیرنده، که هرگز
  بازنویسی نمی‌شود) به درگاه می‌رود. ایجاد هیچ‌جا خودکار تکرار نمی‌شود: تایم‌اوت پس از پذیرش درگاه،
  با تکرار خودکار به دومین توکن و شارژ مضاعف می‌رسید — بدترین خروجی ممکن یک لایهٔ درگاه.
- **لولهٔ ریفاند:** `refund_payment()` — فقط پرداختِ `paid`؛ مجموع بازپرداخت‌های ثبت‌شده در متا
  هرگز از مبلغ پرداخت نمی‌گذرد (`over_refund` پیش از تماس با درگاه)؛ کلید همسانی
  `refund:{id}:{ترتیب}`؛ نتیجهٔ موفق روی متای پرداخت ثبت و هوک `igbz_payment_refunded` فراخوانی
  می‌شود. تطبیق و اختلاف کامل در فاز ۳۱ است.
- **پوشش:** `GatewayHardeningTest` با ۷ سناریو — تایم‌اوت و کلمپ، قرارداد طبقه‌بندی، بازیابی ردیف
  `created` و درج تازه، کلید همسانی در هر درخواست، گاردهای ریفاند و ترتیب‌بندی، و رد ریفاند بدون
  تنظیمات. آزمون زنده: ۱۵ آداپتور بدون خطا، تشخیص درست قابلیت ریفاند، و رد سریع `start()` بدون
  تماس با PSP. تست ۱۹۶۲/۴۸ · لینت ۲۹۰/۰.

### ۷.۷.۳۱ وضعیت اجرا — ۱۴۰۵/۰۶/۰۶ (فاز ۳۱ ✅)

درگاه مرکزی، درگاه مرکزی و تطبیق (اسکیمای v26):

- **درگاه مرکزی با قفل خوش‌بینانه:** `refund()` مجموع جاری `refunded_amount` را فقط با
  UPDATE شرطی روی مقدار خوانده‌شده جابه‌جا می‌کند — دو درگاه مرکزی هم‌زمان هر دو برنده نمی‌شوند
  و بازنده `lost_race` گزارش می‌کند. مبلغ تهی = باقیماندهٔ کامل. مجموع هرگز از مبلغ اسکرو فراتر
  نمی‌رود (`over_refund` پیش از هر نوشتن)؛ پرداختِ کامل-درگاه مرکزی `refunded` می‌شود.
- **تحویل مضاعف، دو طرف بسته:** درگاه مرکزی روی پرداخت `released` رد می‌شود (پول قبلاً به صاحب
  رسیده)؛ طرف دیگر، `release()` دیگر نوشتن بی‌قید نیست — فلیپ شرطی `held→released` است و دو سوییپ
  هم‌زمان نمی‌توانند یک ردیف را دو بار تسویه کنند. تسویه فقط باقیماندهٔ درگاه مرکزی‌نشده را به کیف
  پول صاحب اعتبار می‌دهد.
- **حل اختلاف:** `resolve_dispute()` با حکم `release` پرداخت را به مسیر انتشار برمی‌گرداند (تنها
  نویسندهٔ پول همان سوییپ باقی می‌ماند — یک مسیر برای ممیزی) و با حکم `refund` کل باقیمانده را
  برمی‌گرداند؛ اختلاف دوبار بسته نمی‌شود.
- **گزارش اختلاف و جبران (`reconcile()`):** هر پرداخت `released` باید کردیت کیف‌پول با مرجع
  `master:{id}` داشته باشد؛ نبودش شناسایی، با همان کردیتِ همسان (که تکرارش بی‌اثر است) تعمیر و در
  گزارش شمرده می‌شود. اختلاف‌های بازِ کهنه‌تر از آستانهٔ تنظیم‌شده نیز گزارش می‌شوند. به‌صورت کار
  روزانهٔ `master.reconcile` به صف بادوام وصل شد (ششمین کار روزانه).
- **همسانی برداشت:** ستون جدید `idempotency_key VARCHAR(191) NULL` با ایندکس یکتای
  `(tenant_id, idempotency_key)`؛ درخواست تکراری همان برداشت قبلی را برمی‌گرداند (`duplicate`) و
  مرجع بدهی کیف پول نیز `withdraw:{مستأجر}:{کلید}` است — هیچ راهی برای برداشت دوبار با یک درخواست
  نمانده. کلید تهی (بدون همسانی) همچنان مجاز است و ردیف خودش را می‌گیرد.
- **پوشش:** `MasterPaymentServiceTest` با ۹ سناریو — تکرارزدایی هولد، انتشار مشروط (سررسید/اختلاف/
  آینده)، درگاه مرکزی جزئی تا گارد و باخت رقابت، رد تحویل مضاعف و اسکوپ مستأجر، اعتباردهی دقیق
  باقیمانده، حل اختلاف با هر دو حکم، باخت رقابت در انتشار، تعمیر کردیت گم‌شده در تطبیق، و همسانی
  برداشت. آزمون زنده: مهاجرت واقعی به v26 و هر دو ستون جدید، هولد→تکرار→درگاه مرکزی جزئی→رد
  بیش‌برداشت→تطبیق بدون انحراف روی پلی‌گراند. تست ۲۰۰۹/۴۹ · لینت ۲۹۱/۰.

### ۷.۷.۳۲ وضعیت اجرا — ۱۴۰۵/۰۶/۰۶ (فاز ۳۲ ✅)

پلن و اشتراک — تکمیل چرخهٔ حیات:

- **تعلیق دستی (`suspend()`):** فلیپ شرطی فقط از وضعیت‌های زنده (`active/trialing/past_due`) —
  دو تعلیق هم‌زمان هر دو اثر نمی‌گذارند و وضعیت‌های مرده هرگز تعلیق نمی‌شوند. مستأجر در همان
  لحظه معلق می‌شود تا هیچ مسیر سرویس‌دهی باز نماند.
- **بازفعال‌سازی (`reactivate()`):** فقط از `suspended`؛ زمان باقی‌مانده حفظ می‌شود. اگر دوره در
  تعلیق گذشته باشد اشتراک به `past_due` برمی‌گردد و سوئیپ تمدید آن را برمی‌دارد — پول گرفتن دستی
  در این مسیر وجود ندارد. اشتراک معلق در `active_subscription()` نامرئی است.
- **مهلت (گریس):** پنجرهٔ `plans.grace_days` (بذر ۳ روز؛ پیش‌فرض کد ۷؛ کلمپ ≥۰). سوئیپ
  `expire_past_grace()` اشتراک‌های `past_due` را فقط پس از «پایان دوره + مهلت» منقضی و مستأجر را
  معلق می‌کند؛ داخل مهلت سرویس‌دهی ادامه دارد. فلیپ انقضا نیز شرطی است (`past_due→expired`).
- **اجرای دسته‌ای ادامه‌پذیر:** سوئیپ مهلت روی همان کار روزانهٔ `plans.renewals` سوار شد و شمار
  پردازش‌شدهٔ هر دو سوئیپ با هم مبنای ادامه‌دهی است — دستهٔ پر در هر طرف، دور بعد را صف می‌کند و
  سقف دورها همان قرارداد عمومی صف است.
- **پوشش:** `PlansLifecycleTest` با ۶ سناریو — تعلیق و بازفعال‌سازی با اسکوپ مستأجر، شرطی‌بودن
  تعلیق (تکرار و وضعیت مرده رد می‌شوند)، بازگشت دورهٔ گذشته به `past_due`، نامرئی‌بودن اشتراک
  معلق، انقضای فقط بیرون از مهلت، و پنجرهٔ مهلت قابل تنظیم و کلمپ‌شده. آزمون زنده: مقدار مهلت از
  تنظیمات زنده خوانده شد و هر سه متد جدید در دسترس بودند. تست ۲۰۲۹/۵۰ · لینت ۲۹۲/۰.

---

### ۷.۷.۳۳ وضعیت اجرا — ۱۴۰۵/۰۶/۰۶ (فاز ۳۳ ✅)

بی‌ان‌پی‌ال — سخت‌سازی پول‌رسانی (اسکیمای v27):

- **فلیپ شرطی پرداخت قسط:** `mark_installment_paid()` با شرط وضعیتِ خوانده‌شده می‌نویسد؛ قسطِ
  پرداخت‌شده در برابر تکرار بی‌اثر است. نتیجه: کال‌بک درگاه و وصول خودکار کیف پول که روی یک قسط
  مسابقه دهند هرگز دو بار تسویه/هوک/گزارش درگاه نمی‌فرستند — حتی اگر هر دو از سد خواندن بگذرند،
  فقط یکی نوشتن را می‌برد.
- **سطح کال‌بک درگاه:** سورس `bnpl` به اینباکس وب‌هوک (فاز ۲۹) وصل شد؛ `apply_provider_notification()`
  قسط را با `installment_id` یا `(contract_id, sequence)` می‌یابد، حکم `paid` را از همان مسیر شرطی
  عبور می‌دهد، حکم «نامعلوم»/ناموجود را برای تلاش مجدد به اینباکس برمی‌گرداند و پیام ناقص را بدون
  تکرار بی‌پایان تصدیق می‌کند.
- **تلاش مجدد محدود:** ستون `collection_attempts` و سقف `bnpl.max_collect_attempts` (پیش‌فرض ۵، حداقل
  ۱). سوئیپ پیگیری، قسط عقب‌افتاده را تا سقف از کیف پول وصول می‌کند؛ پس از سقف، تلاش متوقف می‌شود و
  مسیر قصردادنِ موجود ادامه می‌یابد — کوبیدن بی‌نهایت روی کیف پول خالی حذف شد.
- **هماهنگ‌سازی:** `reconcile()` قراردادهای `active`/`settled` را می‌خواند؛ قرارداد تمام‌پرداختی که
  وضعیتش برنگشته از همان مسیر عادی `maybe_settle_contract` تسویه می‌شود و هر قرارداد که
  «پرداخت‌شده + باقی‌مانده»اش با کل قابل پرداخت (±۰٫۰۱) نخواند در گزارش شمرده می‌شود — هیچ اصلاح
  بی‌صدایی وجود ندارد. به‌صورت کار روزانهٔ `bnpl.reconcile` به صف بادوام وصل شد (هفتمین کار روزانه).
- **پوشش:** `BnplHardeningTest` با ۵ سناریو — شرطی‌بودن فلیپ و بی‌اثری تکرار، اعمال کال‌بک با هر دو
  شکل شناسه، صداقت حکم نامعلوم، سقف تلاش‌های وصول (بدون لمس کیف پول پس از سقف)، و تطبیق: تسویهٔ
  جامانده + گزارش انحراف بدون اصلاح. آزمون زنده: مهاجرت واقعی به v27، اجرای تطبیق و تصدیق کال‌بک.
  تست ۲۰۵۲/۵۱ · لینت ۲۹۳/۰.

---

### ۷.۷.۳۴ وضعیت اجرا — ۱۴۰۵/۰۶/۰۶ (فاز ۳۴ ✅)

قرارداد، او‌تی‌پی پرداخت و شاهکار:

- **نسخهٔ قرارداد و مدرک پذیرش:** `LegalWaiverService` از فازهای پیش موجود است و دست نخورد —
  پذیرش به نسخه + هش دقیق متن گره دارد و تغییر متن، پذیرش‌های قبلی را باطل می‌کند؛ گیت پرداخت
  بانکی (`payment_allowed`) اول تطابق کد ملی را می‌سنجد و انصراف‌نامهٔ دیجیتال را به‌عنوان شاخهٔ
  قانونی جایگزین می‌پذیرد.
- **سه نتیجهٔ شاهکار، هرگز درهم نمی‌شوند:** `matched` / `mismatch` / `error`. پیش از این هر شکستی
  «عدم تطابق» ثبت می‌شد — هم تهمت به کاربر و هم بن‌بست بدون تلاش مجدد. اکنون شکست ترنسپورت، بدنهٔ
  خالی و پاسخ بی‌حکم `error` است و فقط پاسخ صریح رجیستری می‌تواند «نامنطبق» بسازد؛ طبقه‌بندی در
  `classify_response()` جدا از وضعیت کاربر و کاملاً تست‌پذیر است.
- **اعتبارسنجی پیش از رجیستری:** چک‌سام رسمی کد ملی ده‌رقمی (رد ارقام تکراری) و فرمت موبایل —
  ورودی نامعتبر تماسی با رجیستری نمی‌گیرد و ردی برجا نمی‌گذارد.
- **سقف تلاش روزانه:** `legal.shahkar_max_attempts_per_day` (پیش‌فرض ۵، حداقل ۱) — هزینهٔ هر تماس
  رجیستری دارد و مسیر، بدون سقف، ابزار سوءاستفاده می‌شد.
- **انضباط پی‌آی‌آی:** فقط هش کد ملی در `ig_nid_verifications` ذخیره می‌شود؛ شمارهٔ موبایل در
  درخواست می‌رود و هرگز ذخیره نمی‌شود؛ کد ملی خام هرگز لاگ نمی‌شود.
- **پوشش:** `NationalIdVerifierTest` با ۴ سناریو — چک‌سام کد ملی (معتبر/نامعتبر/مرزی)، طبقه‌بندی
  پاسخ بدون حدس‌زدن در همهٔ شکل‌های ممکن، گیت‌های پیش از تماس رجیستری، و سقف تلاش روزانه بدون لمس
  رجیستری. تست ۲۰۸۲/۵۲ · لینت ۲۹۴/۰.

---

### ۷.۷.۳۵ وضعیت اجرا — ۱۴۰۵/۰۶/۰۶ (فاز ۳۵ ✅)

نرخ و نقل‌قول ارزی (اسکیمای v28):

- **قفل مدرک‌دار:** `lock_rate()` در `fx_rates` ثبت می‌کند: نرخ خام، اسپرد، `rate_applied`
  (خام + اسپرد؛ تنها عددی که نقل‌قول با آن قیمت می‌شود) و `expires_at`. هیچ نقل‌قولی بدون ردیف
  قفل وجود ندارد و هر ردیف، شواهد خودش را دارد.
- **اسپرد و مهلت:** `fx.spread_percent` (کلمپ ۰ تا ۵۰) و `fx.quote_ttl_minutes` (پیش‌فرض ۱۵،
  حداقل ۱). `quote_is_valid()` ردیف ناموجود یا گذشته از مهلت را مرده می‌خواند — وعدهٔ گذشته از
  مهلت با نرخِ بازارِ رفته قیمت نمی‌گیرد.
- **رفتار نبود نرخ:** شکست منبع خودکار + نبود نرخ دستی ⇒ `lock_rate()` صفر برمی‌گرداند و شارژ
  ارزی صریح رد می‌شود؛ قیمت‌گذاری با نرخ صفر (راه گم‌شدن پول) بسته شد.
- **حذف رقابت نقل‌قول/قفل:** `FxTopupService::start()` اول قفل می‌کند، بعد از همان ردیف قفل‌شده
  قیمت می‌گیرد — ترتیب قبلی (نقل از نرخ زنده، قفل بعدی) پنجره‌ای داشت که بازار در آن حرکت کند و
  خریدار قیمتی بپردازد که هیچ ردیف قفلی توجیهش نمی‌کرد. اکنون قیمت نمایشی و ردیف قفل ذاتاً یکی‌اند.
- **پوشش:** `FxQuoteTest` با ۴ سناریو — کلمپ اسپرد و مهلت، رد قفل در نبود نرخ (بدون ردیف جعلی)،
  ثبت کامل شواهد قفل و بی‌اثری کش پس از `refresh()`، و مرده‌خوانی نقل‌قول منقضی/ناموجود. دابل
  `FxTest` برای جریان قفل-اول به‌روز شد. آزمون زنده: مهاجرت واقعی به v28 و سه ستون جدید.
  تست ۲۱۰۲/۵۳ · لینت ۲۹۵/۰.

---

### ۷.۷.۳۶ وضعیت اجرا — ۱۴۰۵/۰۶/۰۶ (فاز ۳۶ ✅)

پرداخت و تسویهٔ ارزی (بدون تغییر اسکیمای v28 — وضعیت `pending` در ستون موجود `status`):

- **وضعیت نامعلوم:** `pay()` ادپتر می‌تواند با `state => 'pending'` بگوید «فرستادم، جواب را
  نمی‌دانم»؛ هر استثنا از ادپتر نیز همین معنا را دارد. قبض `pending` می‌شود و برداشت کیف پول
  حفظ می‌شود — بازپرداخت در لحظهٔ تردید، وقتی پی‌اس‌پی واقعاً شارژ کرده، پرداخت دوباره است.
- **یک حکم، یک اثر:** `resolve_payout()` تنها نقطهٔ اعمال نتیجه برای قبض معلق است (وب‌هوک و
  تطبیق هر دو از آن می‌گذرند): تأیید ⇒ `paid` با مرجع پی‌اس‌پی؛ شکست ⇒ بازپرداخت کیف پول و
  بازگشت به `due`. قبض غیرمعلق دست نمی‌خورد، پس بازپخش وب‌هوک بی‌اثر است.
- **سقف ریسک:** `fx.payout_daily_cap_usd` مجموع متعهدشدهٔ امروز (پرداخت‌شدهٔ امروز + معلق)
  به‌علاوهٔ این قبض را می‌سنجد؛ عبور از سقف قبض را برای فردا نگه می‌دارد (صفر = بدون سقف).
- **تأیید انسانی:** `fx.payout_approval_threshold_usd` قبض بزرگ‌تر از آستانه را از مسیر خودکار
  خارج می‌کند؛ تسویهٔ آن فقط با `settle_bill_manually()` اپراتور ممکن است (صفر = خاموش).
- **تطبیق:** `reconcile()` روزانه (کار `fx.billing.reconcile`) قبض‌های معلق را از ادپتر می‌پرسد
  — ادپترها ممکن است `query()` با `settled|failed|unknown` پیاده کنند. حکم نامعلوم هیچ چیز را
  تغییر نمی‌دهد و فقط شمرده می‌شود؛ تردید حدس زده نمی‌شود.
- **پوشش:** `FxBillingHardenTest` با ۸ سناریو — آستانهٔ تأیید، سقف روزانه، پارک معلق در نتیجهٔ
  نامعلوم و در استثنا، یک‌بار بودن حکم (بازپخش بی‌اثر)، بازپرداخت در شکست، اعمال احکام تطبیق و
  بی‌تغییری حکم نامعلوم. آزمون زنده: تطبیق و سقف‌ها روی پلی‌گراند. تست ۲۱۳۷/۵۴ · لینت ۲۹۶/۰.

---

### ۷.۷.۳۷ وضعیت اجرا — ۱۴۰۵/۰۶/۰۶ (فاز ۳۷ ✅)

جستجو، نقل‌قول و سفارش دامنه (اسکیمای v29):

- **قرارداد ادپتر:** `DomainAdapterInterface` (جستجو/نقل‌قول/ثبت) و `DomainAdapterRegistry` با
  انتخاب `domain.provider` — همان الگوی درگاه‌ها و ادپتر پرداخت ارزی؛ بدون پرووایدر هر عملیات
  صادقانه خطا می‌دهد.
- **نقل‌قول = قیمت با مهلت:** جدول `ig_domain_quotes` قیمت، ارز نرمال‌شده و `expires_at` نگه
  می‌دارد. مهلت = کوچک‌ترین مقدار بین `domain.quote_ttl_minutes` (پیش‌فرض ۱۵، حداقل ۱) و مهلت
  خود پرووایدر. قیمت غیرمثبت رد می‌شود — چیزی با صفر قابل سفارش نیست.
- **سفارش ایدمپوتنت:** `ig_domain_orders.idempotency_key` با `UNIQUE(tenant_id,idempotency_key)`؛
  درخواست تکراری همان رزرو اول را برمی‌گرداند (`created => false`) و ردیف دوم نمی‌سازد. سفارش
  بدون کلید رد می‌شود و مبلغش همان قیمت نقل‌قول معتبر است.
- **رزرو بدون ثبت:** رزرو ردیف محلی با وضعیت `reserved` است؛ `register()` ادپتر هرگز پیش از
  پرداخت فراخوانی نمی‌شود. `confirm_paid()` دقیقاً یک بار رزرو را `paid` می‌کند (بازپخش
  بی‌اثر، وضعیت‌های دیگر رد می‌شوند) — این تنها دروازهٔ ثبت واقعی است و خود ثبت با کال‌بک
  امضاشده و جبران در فاز ۳۸ می‌آید. نقل‌قول منقضی سفارش نمی‌سازد.
- **پوشش:** `DomainOrderTest` با ۵ سناریو — اعتبارسنجی جستجو و نبود پرووایدر، مهلت و کلمپ
  نقل‌قول و رد قیمت صفر، مرده‌خوانی نقل‌قول منقضی، ایدمپوتنتی سفارش و رد کلید خالی، و
  «رزرو هرگز ثبت نیست» تا تأیید پرداخت یک‌باره. آزمون زنده: مهاجرت واقعی به v29 (جدول جدید و
  ستون کلید) روی پلی‌گراند. تست ۲۱۷۵/۵۵ · لینت ۳۰۰/۰.

---

### ۷.۷.۳۸ وضعیت اجرا — ۱۴۰۵/۰۶/۰۶ (فاز ۳۸ ✅)

ثبت، کال‌بک و جبران دامنه (اسکیمای v30):

- **ثبت فقط پس از پرداخت:** `register_paid()` هر وضعیتی جز `paid` را رد می‌کند (رزرو ثبت نیست).
  سفارش پیش از تماس با پرووایدر `registering` می‌شود — کرش میانهٔ فراخوانی وضعیتی مرئی است، نه
  سکوت. استثنا از ادپتر مانند شکست رفتار می‌شود.
- **جبران:** شکست پرووایدر ⇒ `failed` + بازپرداخت مبلغ سفارش به کیف پول ارزی با مرجع
  `domain_refund:<id>`؛ ایدمپوتنتی `(reason, reference)` کیف پول تضمین می‌کند تکرار جبران پول
  دوم نمی‌سازد.
- **ژورنال:** جدول `ig_domain_journal` رویدادهای آغاز ثبت، ثبت موفق، شکست و بازپرداخت را برای
  هر سفارش ثبت می‌کند — هر شکست پرووایدر پس از واقعه قابل توضیح است.
- **کال‌بک امضاشده:** `verify_callback()` با `hash_hmac('sha256', body, domain.webhook_secret)` و
  مقایسهٔ زمان-ثابت؛ امضای جعلی، امضای خالی یا نبود رمز ⇒ کال‌بک هرگز دریافت‌شده حساب نمی‌شود.
- **مسیر یکتای احکام:** کال‌بک و پایش پشتیبان هر دو از `apply_provider_result()` می‌گذرند؛ سفارش
  ثبت‌شده/بازپرداخت‌شده در برابر حکم متناقض بعدی بی‌تغییر است.
- **پایش پشتیبان:** `poll_stuck()` سفارش‌های `registering` پیرتر از آستانهٔ ساعتی را از ادپتر
  می‌پرسد (رابط جدید `query()` با `registered|failed|unknown`)؛ حکم نامعلوم فقط شمرده می‌شود —
  تردید حدس زده نمی‌شود.
- **پوشش:** `DomainRegistrationTest` با ۶ سناریو — دروازهٔ پرداخت، شواهد ژورنالِ ثبت موفق، جبران
  ایدمپوتنت با ژورنال، راستی‌آزمایی امضای کال‌بک، تک‌مسیره بودن احکام و پایش پشتیبان (پیر/جوان).
  آزمون زنده: مهاجرت واقعی به v30 و راستی‌آزمایی امضا روی پلی‌گراند. تست ۲۲۱۲/۵۶ · لینت ۳۰۱/۰.

---

### ۷.۷.۳۹ وضعیت اجرا — ۱۴۰۵/۰۶/۰۶ (فاز ۳۹ ✅)

تمدید، دی‌ان‌اس و تطبیق دامنه (اسکیمای v31):

- **تمدید امن:** `renew()` ردیف را با `WHERE id AND tenant_id` می‌خواند — دامنهٔ مستأجر دیگر
  «پیدا نمی‌شود» (نگهبان BOLA). امتداد از `max(اکنون, انقضای فعلی)` محاسبه می‌شود؛ دامنهٔ
  `released` تمدیدپذیر نیست. با پرووایدر پیکربندی‌شده تمدید اول در رجیستری ثبت می‌شود و شکست آن
  هیچ امتداد محلی‌ای نمی‌سازد — ثبتی که در رجیستری تکان نخورده، محلی هم تکان نمی‌خورد. پرچم
  تمدید خودکار در ستون جدید `auto_renew` ذخیره می‌شود.
- **اعلان ایدمپوتنت:** `notify_renewals()` برای دامنه‌های فعال در پنجرهٔ هشدار
  (`domain.renewal_warning_days`، پیش‌فرض ۱۴) یک بار به‌تفکیک تاریخ انقضا ژورنال می‌زند؛ اجرای
  دوم همان چرخه چیزی نمی‌افزاید و پس از تمدید، چرخهٔ تازه پاک شروع می‌شود. دامنهٔ گذشته از
  انقضا متعلق به چرخهٔ انقضا است، نه اعلان.
- **نردبان انقضا:** `run_expiry_sweep()` در هر اجرا فقط یک پله: منقضی فعال → `grace`
  (پس از `domain.grace_days`) → `redemption` (پس از `domain.redemption_days` اضافی) → `released`.
  هر پله ژورنال می‌شود؛ آزادسازی `verified_at` نگاشت مستأجر را نیز برمی‌دارد تا دامنهٔ مرده
  دیگر دروازهٔ هیچ چیز نباشد. گذارها به وضعیت فعلی شرط‌اند، پس اجرای مجدد بی‌اثر است.
- **تطبیق گزارشگر:** `reconcile()` انقضای محلی دامنه‌های دارای مرجع پرووایدر را با رجیستری
  می‌سنجد (تحمل اختلاف یک روز)؛ ناهم‌خوانی گزارش و ژورنال می‌شود و محلی هرگز بی‌صدا اصلاح
  نمی‌گردد — تصمیم با اپراتور است.
- **دی‌ان‌اس:** تأیید و نگاشت دامنه از فاز ۱۷ محدود به `tenant_id` بود؛ این فاز آن رفتار را در
  تست قفل کرد (تلاش برای تأیید/تمدید دامنهٔ مستأجر دیگر بی‌نتیجه است).
- **پوشش:** `DomainLifecycleTest` با ۵ سناریو — محدودهٔ مستأجر و نقطهٔ شروع تمدید، شکست
  پرووایدر بدون امتداد، اعلان یک‌بار به‌تفکیک چرخه، نردبان تک‌پله‌ای تا آزادسازی با
  غیرفعال‌سازی نگاشت، و تطبیق گزارشگر بدون اصلاح خودکار. آزمون زنده: مهاجرت واقعی به v31 روی
  پلی‌گراند. تست ۲۲۴۱/۵۷ · لینت ۳۰۲/۰.

---

### ۷.۷.۴۰ وضعیت اجرا — ۱۴۰۵/۰۶/۰۶ (فاز ۴۰ ✅)

همکاری در فروش — سخت‌سازی (بدون تغییر اسکیمای ۳۱):

- **خودارجاعی در هر سه دروازه:** والدِ خودِ کاربر در `enroll()` حذف می‌شود (سطح ۱)، ارجاع به
  خود هنگام ثبت کمیسیون رد می‌شود و کد خودِ کاربر به خودش نمی‌چسبد — هر سه در تست قفل شدند.
- **اثر بازپرداخت:** `void_order_commission()` هم `pending` و هم `approved` پرداخت‌نشده را باطل
  می‌کند. کمیسیون `paid` عمداً دست نمی‌خورد: پول جابه‌جا شده و «باطل‌شدن»ش دروغ است؛ به‌جایش
  گزارش تقلب آن را آشکار می‌کند.
- **گزارش تقلب (فقط گزارش، بدون تنبیه خودکار):** `fraud_report()` چهار سیگنال می‌دهد —
  کمیسیون‌های خودارجاعی؛ خوشه‌های تبدیل با هش آی‌پی مشترک (دو کاربر متمایز یا بیشتر)؛ همکاران
  با حداقل `affiliate.fraud_min_commissions` (پیش‌فرض ۳) کمیسیون و سهم ابطال بالای نصف؛ و
  کمیسیون‌های `paid` روی سفارش‌هایی که فروشگاه بعداً مسترد/لغو کرده — بدهی‌ای که مسیر ابطال
  نمی‌تواند برگرداند. تصمیم با اپراتور است.
- **پوشش:** `AffiliateHardenTest` با ۳ سناریو — حذف والدِ خود، ابطال دوسطحی بدون دست‌زدن به
  پرداخت‌شده، و آشکارسازی هر چهار سیگنال تقلب بدون تغییر هیچ ردیف. آزمون زنده: اجرای گزارش روی
  پلی‌گراند. تست ۲۲۵۱/۵۸ · لینت ۳۰۳/۰.

---

### ۷.۷.۴۱ وضعیت اجرا — ۱۴۰۵/۰۶/۰۶ (فاز ۴۱ ✅)

گیمیفیکیشن (اسکیمای v32):

- **یک دفتر برای همه‌چیز:** `ig_points_ledger` افزودنی و ایدمپوتنت است
  (`UNIQUE(user_id,reason,reference)`) — رویداد تکراری امتیاز تازه نمی‌سازد و همان ردیف قبلی
  برمی‌گردد. خرج‌کرد، ردیف منفی بدون انقضا در همان دفتر است؛ پس یک `SUM` درآمد، خرج و انقضا
  را با هم تطبیق می‌دهد.
- **سوءاستفاده:** سقف روزانهٔ `gamification.daily_cap` مجموع امتیاز مثبت امروز کاربر را
  می‌سنجد (صفر = خاموش)؛ اعتبار نامعتبر (صفر/منفی، بدون دلیل یا مرجع) رد می‌شود.
- **انقضا:** هر اعتبار می‌تواند مهلت داشته باشد (`gamification.point_ttl_days` یا مهلت صریح
  هر رویداد)؛ ردیف منقضی حذف نمی‌شود — فقط از جمع تراز می‌افتد و دفتر قابل حسابرسی می‌ماند.
- **جایزه:** `redeem()` موجودی کافی می‌خواهد، کلید ایدمپوتنت اجباری است، خرج‌کرد در دفتر ثبت
  می‌شود و ردیف `issued` در `ig_reward_redemptions` می‌نشیند؛ پخش دوبارهٔ همان کلید همان خرید
  اول را برمی‌گرداند.
- **مسابقه:** `race()` رتبه‌بندی امتیازهای مثبت در بازهٔ روزانه است — خرج‌کردن امتیاز مسابقه را
  کم نمی‌کند (رقابت بر سر فعالیت است، نه خرید).
- **تطبیق:** `reconcile()` سه عدد می‌دهد — ردیف‌های منقضی‌شده، ترازهای منفی (سیگنال باگ یا
  دستکاری؛ هرگز نباید رخ دهد) و کلیدهای خرید تکراری.
- **پوشش:** `GamificationTest` با ۵ سناریو — ایدمپوتنتی و سقف، انقضا، خرید تک‌ردیفی ایدمپوتنت،
  مسابقهٔ فقط‌درآمدی و گزارش تطبیق. آزمون زنده: مهاجرت واقعی به v32 و اعتبار ایدمپوتنت روی
  پلی‌گراند. تست ۲۲۹۱/۵۹ · لینت ۳۰۴/۰.

---

### ۷.۷.۴۲ وضعیت اجرا — ۱۴۰۵/۰۶/۰۶ (فاز ۴۲ ✅)

آموزش و ویدیوی امن (بدون تغییر اسکیمای ۳۲):

- **وضعیت موجود پیش از این فاز:** ثبت‌نام با پنجرهٔ دسترسی (`access_days`)، ثبت پیشرفت، آزمون
  با سقف تلاشِ مهارشده (سایت سقف است نه پیش‌فرض)، گواهی با کد، و ابطال دسترسی از سفارش
  مستردشده — همه موجود بودند.
- **حلقهٔ بسته‌شده:** لینک امضاشدهٔ ویدیو شرط لازم بود نه کافی؛ اثبات می‌کرد لینک خودی و
  نامنقضی است، نه اینکه بیننده هنوز حق تماشا دارد. اکنون `verify_video_signature()` پس از
  تأیید امضا و مهلت، `can_watch()` را نیز می‌خواند: پیش‌نمایش آزاد برای همه باز است؛ هر درس
  دیگر ثبت‌نام زندهٔ دوره‌اش را می‌خواهد.
- **اثر بازپرداخت:** `revoke_from_order()` ردیف ثبت‌نام را حذف می‌کند، پس لینک معتبر باقی‌مانده
  همان لحظه از کار می‌افتد — نه بعد از تمام‌شدن عمر لینک. پایان پنجرهٔ دسترسی نیز همان مسیر
  `is_enrolled()` را می‌بندد.
- **پوشش:** `LmsVideoAccessTest` با ۵ سناریو — لینک معتبر با ثبت‌نام زنده، مرگ فوری لینک پس از
  ابطال سفارش، بسته‌شدن پنجرهٔ دسترسی، بازماندن پیش‌نمایش آزاد و بسته‌ماندن کلید ناشناس، و رد
  لینک منقضی و امضای جعلی. آزمون زنده: راستی‌آزمایی امضا و دسترسی روی پلی‌گراند. تست ۲۲۹۹/۶۰ ·
  لینت ۳۰۵/۰.

---

### ۷.۷.۴۳ وضعیت اجرا — ۱۴۰۵/۰۶/۰۶ (فاز ۴۳ ✅)

هستهٔ لجستیک — ماشین وضعیت تحویل (بدون تغییر اسکیمای ۳۲):

- **وضعیت موجود پیش از این فاز:** نقل‌قول مسیر تنظیم‌پذیر، ایجاد مرسوله با پین رمزنگاشته، گروه
  برچسب با بارکد یکتا (`IGBZ-<id>-<hex>`)، پایش موقعیت پیک، و مسیرهای پیک محدود به مرسوله‌های
  خود پیک — همه موجود بودند.
- **ماشین وضعیت (افزودهٔ این فاز):** جدول گذار صریح — پیش‌نویس ⇒ پیک‌خورده/ثبت‌شده نزد حامل/شکست؛
  پیک‌خورده ⇒ در مسیر/رسیده/شکست؛ ثبت‌شده ⇒ در مسیر/شکست؛ در مسیر ⇒ رسیده/شکست؛ رسیده ⇒
  تحویل/شکست؛ تحویل و شکست پایانی‌اند. `can_transition()` استاتیک و خالص است.
- **نقاط اجرا:** تحویل به حامل فقط از پیش‌نویس؛ `mark_delivered` و `deliver` پیک فقط از
  «رسیده به مقصد»؛ `arrived` فقط از پیک‌خورده/در مسیر؛ `assign` فقط از پیش‌نویس. گذار غیرمجاز
  رد و لاگ می‌شود — هیچ فراخوانی نمی‌تواند وضعیت را دور بزند.
- **مجوز نقش‌ها:** صفحهٔ لجستیک با `Capabilities::MANAGE_SUITE` بالا می‌آید؛ کنترلر پیک فقط
  کاربران دارای ردیف پیک فعال را می‌پذیرد و فقط مرسوله‌های همان پیک را نشان می‌دهد؛ تحویل بدون
  پین مشتری (که هرگز به پیک نمایش داده نمی‌شود) ممکن نیست.
- **پوشش:** `LogisticsStateMachineTest` با ۴ سناریو — جدول گذار قانون است، تحویل پیش از رسیدن
  رد می‌شود، جریان پیک ماشین را قدم‌به‌قدم می‌پیماید و وضعیت‌های پایانی هیچ گذاری نمی‌پذیرند؛
  تست قدیمی `PhasesTest` نیز برای پیمایش ماشین به‌روز شد. آزمون زنده روی پلی‌گراند. تست
  ۲۳۳۴/۶۱ · لینت ۳۰۶/۰.

---

### ۷.۷.۴۴ وضعیت اجرا — ۱۴۰۵/۰۶/۰۶ (فاز ۴۴ ✅)

پیک و پرداخت در محل — مدرک تحویل و انضباط مالی (اسکیمای v33):

- **وضعیت موجود پیش از این فاز:** قرارداد ای‌پی‌آی پیک (مرسوله‌های خود پیک، ترتیب مسیر، جستجوی
  بارکد، رسیدن، تحویل با پین، مسیرهای پرداخت در محل، پایش موقعیت و گفتگو) کامل بود.
- **مدرک تحویل (افزودهٔ این فاز):** `ig_shipments.pod_ref` + `pod_at` — هر تحویل مدرکش را روی
  ردیف نگه می‌دارد: مرجع عکس یا امضای ثبت‌شده در اپ پیک برای تحویل با پین، و نشان تسویه برای
  مسیرهای پرداخت در محل. اختلاف مالی از خود ردیف پاسخ می‌گیرد.
- **انضباط ماشین در چرخهٔ مالی:** تسویهٔ نقدی (`cod()` با method=cash) و پرداخت درون‌اپ
  (`cod_app_paid()`) تنها از وضعیتی می‌گذرند که `can_transition(…, delivered)` تأیید کند؛ تلاش
  زودهنگام `bad_state` می‌گیرد و ردیف پرداخت نمی‌سازد. تسویهٔ موفق: وضعیت `delivered` + ردیف
  `ig_cod_payments` با مبلغ، وضعیت `paid`، روش و مرجع.
- **پوشش:** `CourierCodTest` با ۴ سناریو — ثبت مدرک در تحویل با پین، رد تسویهٔ نقدی زودهنگام و
  صحت ردیف پرداخت پس از رسیدن، رفتار مشابه برای پرداخت درون‌اپ، و رد روش‌های ناشناخته و
  مرسوله‌های غیر پرداخت‌درمحل. آزمون زنده: مهاجرت واقعی به v33 روی پلی‌گراند. تست ۲۳۵۲/۶۲ ·
  لینت ۳۰۷/۰.

---

### ۷.۷.۴۵ وضعیت اجرا — ۱۴۰۵/۰۶/۰۶ (فاز ۴۵ ✅)

آداپتورهای لجستیک — نگاشت، کال‌بک و تطبیق (بدون تغییر اسکیمای ۳۳):

- **وضعیت موجود پیش از این فاز:** قرارداد مشترک پرووایدر (`register`/`track`) و آداپتر
  تنظیم‌محور `HttpShippingAdapter` (مسیرها، اسکیمای احراز هویت و مسیرهای پاسخ همگی از تنظیمات)
  موجود بود.
- **نگاشت وضعیت:** `ShippingSyncService::STATUS_MAP` واژگان پرووایدرها را به وضعیت‌های ماشین
  می‌برد؛ فروشگاه می‌تواند با `logistics.status_map_<raw>` واژهٔ خاص خودش را آموزش دهد؛ کلمهٔ
  ناشناخته «نمی‌دانم» می‌ماند و چیزی را تغییر نمی‌دهد.
- **کال‌بک امضاشده:** `apply_carrier_callback()` با `Crypto::hmac` روی بدنهٔ خام و
  `logistics.callback_secret`؛ امضای جعلی، امضای خالی یا نبود رمز ⇒ کال‌بک هرگز دریافت‌شده
  حساب نمی‌شود. حکم تأییدشده همان مسیر پیمایش را می‌رود.
- **پیمایش ماشین، نه پرش:** هدف وضعیت با جست‌وجوی سطح‌اول روی جدول گذار از کوتاه‌ترین مسیر
  قانونی به دست می‌آید؛ پرووایدری که مستقیم «تحویل شد» بفرستد باز هم از «رسیده به مقصد»
  می‌گذرد و وضعیت پایانی هرگز عقب نمی‌رود — پریدن از پله‌ها همان راهی است که تحویل‌های شبح از
  آن می‌آیند.
- **تلاش مجدد = خود پیمایش:** `sync_tracking()` وضعیت زندهٔ مرسوله‌های قابل رهگیری را می‌پرسد؛
  خطای گذرای ادپتر فقط شمرده می‌شود و پیمایش بعدی دوباره می‌پرسد — هیچ تنبیهی برای خطای شبکه
  نیست.
- **پوشش:** `ShippingSyncTest` با ۴ سناریو — نگاشت واژگان و آموزش فروشگاه، پیمایش پله‌به‌پله تا
  تحویل و بی‌حرکتی وضعیت پایانی، پیمایش به‌مثابهٔ تلاش مجدد و بی‌تغییری وضعیت‌های نامعلوم، و
  دروازهٔ امضای کال‌بک با رد احکام نگاشت‌نشده. آزمون زنده روی پلی‌گراند. تست ۲۳۷۶/۶۳ · لینت
  ۳۰۹/۰. آزمون زندهٔ هر پرووایدر واقعی به فازهای `PV-LOGISTICS-*` موکول است.

---

### ۷.۷.۴۶ وضعیت اجرا — ۱۴۰۵/۰۶/۰۶ (فاز ۴۶ ✅)

مارکت‌پلیس‌ها — انتشار ایدمپوتنت، تضاد، محدودیت نرخ (اسکیمای ۳۴:
`marketplace_links.payload_hash`/`remote_rev` و `ig_marketplace_sync.not_before`):

- **انتشار ایدمپوتنت:** هر بارگذاری هش کانونیکال (مرتب‌شده، بدون حساسیت به ترتیب کلید) می‌گیرد؛
  محصولی که همین هش را قبلاً «سنکرون‌شده» منتشر کرده بدون تماس با پرووایدر done می‌شود — کرش
  بعد از ایجاد و قبل از ثبت نیز با `external_id` سمت پرووایدر هم‌معنا می‌ماند.
- **تضاد (۴۰۹):** پاسخ تضاد، سطر را پایانی با خطای `conflict` متوقف می‌کند و دفترچهٔ لینک
  وضعیت `conflict` می‌گیرد؛ بازنویسی بی‌صدا هرگز اتفاق نمی‌افتد.
- **محدودیت نرخ و بک‌آف:** ۴۲۹ با `Retry-After` خودش (کران‌دار تا یک ساعت) یا بک‌آف نمایی
  `پایه * 2^(تلاش-1)` به `not_before` می‌رود و در این فاصله برای درین نامرئی است؛ «صبر کن»
  پرووایدر تلاشِ شکست حساب نمی‌شود. خرابی ۵۰۰/خطای شبکه همان بک‌آف را دارد و پس از
  `marketplace.sync_retries` پایانی شکست می‌خورد؛ سایر ۴۰۰ها خطای بارگذاری‌اند و تکرارشان
  سود ندارد.
- **سقف هر تیک:** هر کانال در هر درین حداکثر `marketplace.<id>_per_tick` پوش می‌گیرد؛ بقیه
  دست‌نخورده به تیک بعد می‌مانند.
- **تعیین قابلیت‌های بدون درگاه رسمی (یافتهٔ تحقیق ۱۴۰۵/۰۶/۰۶):** باسلام پلتفرم رسمی
  توسعه‌دهنده دارد (مستندات، کلاینت/سکرت یا توکن شخصی، اسکوپ و SDK)؛ دیجی‌کالا و دیوار هیچ درگاه
  رسمی عمومی برای «ثبت محصول» ندارند — دسترسی‌های موجود، خواندنی و غیررسمی یا واسطه‌ای‌اند.
  نتیجهٔ اجرایی: کانال‌های غیررسمی فقط با آداپتر تنظیم‌محور و واسطهٔ مورد اعتماد خود فروشگاه
  فعال می‌شوند؛ اسکرپ یا جعل جلسه هرگز پیاده‌سازی نمی‌شود.
- **پوشش:** `MarketplaceSyncTest` با ۷ سناریو — پایداری هش نسبت به ترتیب کلید، به‌خاطرسپاری
  بارگذاری منتشرشده، دسته‌بندی صادقانهٔ نتیجه‌ها (done/تضاد/خرابی بارگذاری)، تعویق بدون
  سوزاندن تلاش برای ۴۲۹، بک‌آف تا سقف برای خرابی پرووایدر، ثبت کامل دفترچهٔ لینک و
  ایدمپوتنس خود صف. آزمون زنده روی پلی‌گراند. تست ۲۴۰۱/۶۴ · لینت ۳۱۰/۰. آزمون زندهٔ هر
  پرووایدر واقعی به فازهای `PV-MARKETPLACE-*` موکول است.

---

## ۸. فازبندی پیاده‌سازی (پس از تأیید)
| گام | محتوا |
|---|---|
| ۱ | تنظیمات `legal.*` + جدول‌ها (`ig_nid_verifications`, `ig_legal_agreements`) |
| ۲ | آداپتور شاهکار (`NationalIdVerifier`) — فقط با کلید ثبت‌شده فعال |
| ۳ | جریان ثبت‌نام قانونی (تطبیق → OTP → اکانت → پسورد) |
| ۴ | OTP سراسری برای «هر ثبت» (سبد خرید و…) |
| ۵ | قرارداد دیجیتال حقوقی (پرداخت بدون تطابق) |
| ۶ | شماره موبایل برای تشخیص فروشگاه در اپ |
| ۷ | تست‌ها + تأیید زنده |

---

## وضعیت اجرایی جلسهٔ ۱۴۰۵/۰۶/۰۵

قرارداد حقوقی اکنون از طریق سرویس، پنل پرداخت و مسیرهای REST قابل مشاهده و پذیرش است. گیت پرداخت بانکی در زمان شروع تراکنش نیز دوباره بررسی می‌شود؛ فعال‌بودن گزینهٔ تطابق کد ملی بدون کلید و نشانی شاهکار مجوز پرداخت ایجاد نمی‌کند.

### تصحیح امنیتی ممیزی ۱۴۰۵/۰۶/۰۵

اگرچه جریان قرارداد و گیت پرداخت در کد حاضر است، **آزمون شاهکار نباید فقط با افزودن کلید
شروع شود**. `legal.shahkar_api_key` یکی از ۲۲ فیلد password است که در نسخهٔ فعلی عضو
`Settings::SECRETS` نیست؛ در نتیجه plaintext ذخیره و در HTML فرم بازنمایش می‌شود. نخست
باید رجیستری رازها و مهاجرت امن طبق `API-KEYS.md §۵` تکمیل شود، سپس کلید sandbox با
کمترین دسترسی وارد و آزمون‌های مثبت، عدم‌تطابق، timeout، پاسخ خراب، retry و ثبت ممیزی
بدون PII/راز اجرا شوند. این مورد اکنون پیش‌نیاز آزمون زندهٔ شاهکار است، نه کار اختیاری.
