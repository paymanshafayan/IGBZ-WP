# ممیزی پیش‌تولید — یافته‌ها، حکم‌ها و کارهای باز

- **وضعیت:** ثبت‌شده — مبنای کار، نه تغییر کد
- **تاریخ ممیزی:** ۱۴۰۵/۰۶/۰۹
- **روش:** بررسی مستقیم کد و اسناد پروژه (نه حدس) برای ۷ موردی که کارفرما ارائه کرد
- **نتیجهٔ کلی:** از ۷ مورد، ۱ مورد نادرست، ۱ مورد نیمه‌قدیمی، ۳ مورد از قبل در کد/اسناد مهارشده، و ۲ مورد بهبود واقعی و کوچک. هیچ‌کدام مسدودکنندهٔ کار معماری ارائه‌دهندهٔ AI نیست.

---

## جدول جمع‌بندی

| # | مورد | حکم | اقدام لازم |
| --- | --- | --- | --- |
| ۱ | SEC-001: ۲۲ کلید خارج از رجیستری | نیمه‌قدیمی (رفعِ کد از قبل هست) | ممیزی عملیاتی، نه کد |
| ۲ | فقدان Adapter واقعی Zernio | نادرست | تأیید زندهٔ OAuth (PV-ZERNIO-*) |
| ۳ | ریسک SQLite / دابل‌اسپند | از قبل مهارشده | تأیید استقرار MySQL/InnoDB |
| ۴ | وابستگی به Cron واقعی | از قبل مستند و عمدی | راهنمای عملیاتی موجود |
| ۵ | SSRF: غیرفعال‌شدن گارد با فیلتر | درست، ولی دریچهٔ مستند | بهبود کوچک: سخت‌گیری در تولید |
| ۶ | نبود Rate-Limit استاندارد (RFC 6585) | درست به‌عنوان بهبود | بهبود کوچک: هدر `Retry-After` |
| ۷ | تناقض نسخهٔ دیتابیس در اسناد | درست | اصلاح سند (قاعدهٔ ۹) |

---

## ۱) SEC-001: ۲۲ کلید عبور خارج از رجیستری — نیمه‌قدیمی

**واقعیت:**
- عدد ۲۲ درست است، ولی رفع آن از قبل در کد هست. `Settings::SECRETS` (در
  `igbz-suite/src/Support/Settings.php`، خطوط ~۲۰ تا ~۶۰) همهٔ ۲۲ کلید را دارد، به‌علاوهٔ
  توکنِ تولیدشدهٔ `vip.media_hmac_secret` و `zernio.central_api_key`.
- `encrypt_legacy_secrets()` در `Settings.php` (خط ۱۶۷) یک‌باره مقادیر متنیِ قدیمی را
  رمز می‌کند و در `Activator::migrate_to_v20()` اجرا می‌شود (`Activator.php` ~۴۲۱–۴۲۷).
- از آن پس `set()` و `set_many()` هر کلیدِ ثبت‌شده را خودکار رمز می‌کنند و `masked()` برای
  فرم‌های ادمین جای‌نگهدار برمی‌گرداند.

**پیشنهاد:**
- کارِ باقی‌مانده فقط **ممیزی عملیاتی** است: روی هر سایت مستقر، بررسی شود که گزینهٔ
  `igbz_settings` مقدار سادهٔ متنی برای کلیدهای فهرست‌شده نداشته باشد (و نسخهٔ DB از ۲۰
  عبور کرده باشد). شایسته است یک چک در `StatusPage`/`/?igbz_health=1` اضافه شود تا این
  وضعیت خودکار گزارش شود. اولویت: پایین.

## ۲) فقدان Adapter واقعی Zernio — نادرست

**واقعیت:**
- `ZernioClient` (`igbz-suite/src/Modules/Instagram/Gateways/ZernioClient.php`) وجود دارد،
  `ZernioAdapterInterface` را پیاده می‌کند و `create_profile`/`issue_profile_key` دارد.
- در کانتینر ثبت است: `ig.zernio_client`، `ig.zernio` و `ig.zernio_social`
  (`InstagramModule.php` خطوط ~۱۰۸–۱۲۰).
- `create_profile` شناسه را از `$body['profile']['_id']` اول می‌خواند — با پاسخ زندهٔ
  واقعی (`POST /profiles → HTTP 201`) هم‌خوان.

**پیشنهاد:**
- چیزی که واقعاً مانده، **تأیید زندهٔ چرخهٔ کامل OAuth و هواپیمای اجتماعی** است
  (اقلام `PV-ZERNIO-*`)، نه پیاده‌سازی کلاینت. هیچ کار کدی برای این مورد نیست.

## ۳) ریسک SQLite در Production / دابل‌اسپند — از قبل مهارشده

**واقعیت:**
- `Db.php` یک پوشش تراکنش واقعی دارد و صریح مستند کرده که `SELECT … FOR UPDATE` به
  InnoDB نیاز دارد؛ `is_sqlite()` برای تشخیص است.
- `WalletService` روی `is_sqlite()` شاخه می‌زند (حذف `FOR UPDATE` در SQLite) و قفل نامی
  (`GET_LOCK`) دارد؛ کیف پول با کلید `(reason, reference)` idempotent است.
- ران‌بوک استقرار دیتابیس `MySQL` را الزامی کرده (`RUNBOOK-RAILWAY-DEPLOYMENT.md`) و
  نسخهٔ مصوب کارفرما **MySQL 8.4 LTS** است.

**پیشنهاد:**
- توصیهٔ «فقط MySQL/MariaDB با InnoDB» همین حالا در اسناد استقرار هست؛ این یک
  **تأیید/کنترل استقرار** است، نه نقص کد. در استقرار تولید فقط مطمئن شویم دیتابیس
  SQLite نباشد (ران‌بوک از قبل این را می‌گوید).

## ۴) وابستگی به Cron واقعی — از قبل مستند و عمدی

**واقعیت:**
- `Cron.php` زمان‌بندها (۵دقیقه/ساعتی/روزانه) را ثبت می‌کند و کارها به صف بادوام
  (`JobQueue`) می‌روند.
- `StatusPage` بخش cron و اجرای دستی دارد.
- `README.md` (خط ۱۱۸) و `RUNBOOK-SLO-ALERTS.md` صریح نوشته‌اند `DISABLE_WP_CRON`
  عمداً پیش‌فرض خاموش است و فقط با تصمیم بهره‌بردار + رانندهٔ سرور فعال می‌شود؛
  صفحهٔ وضعیت اگر ببیند `DISABLE_WP_CRON` روشن است ولی رانندهٔ سرور روشن نیست هشدار
  می‌دهد. ران‌بوک متغیر `IGBZ_SERVER_CRON=1` را برای ضربان ۶۰ثانیه‌ای معرفی کرده.

**پیشنهاد:**
- این مورد راهنمای عملیاتیِ موجود است، نه نقص کد. هنگام استقرار طبق ران‌بوک، رانندهٔ
  cron سرور (یا `IGBZ_SERVER_CRON`) باید فعال شود.

## ۵) SSRF در Sandbox: گارد با فیلتر غیرفعال می‌شود — درست، ولی دریچهٔ مستند

**واقعیت:**
- `UrlGuard::is_safe()` دو فیلتر عمدی دارد:
  - `igbz_url_guard_allow` — اگر چیزی `true` برگرداند، گارد به‌کلی می‌پرد (پیش‌فرض `null`).
  - `igbz_url_guard_resolve` — اگر چیزی `false` برگرداند، مرحلهٔ رفع‌نام DNS رد می‌شود و
    تابع `true` برمی‌گرداند (پیش‌فرض `true`).
- هیچ‌کدام در کد تولید به‌صورت پیش‌فرض بسته نمی‌شوند؛ فقط `tests/bootstrap.php` فیلتر
  `igbz_url_guard_resolve` را (برای sandbox) می‌بندد.

**پیشنهاد (بهبود کوچک):**
- سخت‌گیری: در محیط تولید، بستن `igbz_url_guard_allow` به `true` یا `igbz_url_guard_resolve`
  به `false` باید یا ممنوع باشد یا با هشدار/لاگ صریح همراه شود (fail-closed). یک چک محیطی
  ساده در گارد کافی است. اولویت: متوسط.

## ۶) نبود Rate-Limit استاندارد (RFC 6585) — درست به‌عنوان بهبود

**واقعیت:**
- rate-limit هست: `HubController::throttle()` پاسخ ۴۲۹ می‌دهد و `InboxService` سقف
  ساعتی مستأجر/فرستنده دارد.
- ولی پاسخ ۴۲۹ **هدر `Retry-After` ندارد**؛ RFC 6585 این هدر را برای ۴۲۹ الزامی می‌داند
  تا WAF/کلاینت بتوانند درست عقب‌نشینی کنند.

**پیشنهاد (بهبود کوچک):**
- افزودن هدر `Retry-After` (و ترجیحاً `X-RateLimit-Limit/Remaining/Reset`) به پاسخ ۴۲۹
  در `HubController`. تغییر کوچک و محلی است. اولویت: متوسط.

## ۷) تناقض نسخهٔ دیتابیس در اسناد — درست

**واقعیت:**
- کد: `IGBZ_DB_VERSION = 48` (`igbz-suite/igbz-suite.php` خط ۲۲).
- اسناد ناهمخوان:
  - `PROJECT-STATE.md` خط ۶۴۸: عدد ۱۷ را «وضعیت فعلی» معرفی کرده (ناهمخوانی زنده).
  - `HANDOFF-PROMPT.md` خط ۳۵۳: عدد ۳۷.
  - `REQUIREMENTS-TRACEABILITY.md` خط ۳۰۸: عدد ۲۲.
  - ران‌بوک‌ها (`RUNBOOK-RAILWAY-DEPLOYMENT.md` خط ۱۴۰ و `RUNBOOK-RAILWAY-LTS.md`) درست‌اند: ۴۸.

**پیشنهاد (اصلاح سند — قاعدهٔ ۹):**
- اصلاح سه عدد بالا به ۴۸. بخشی از آن‌ها سند تاریخی فازها هستند، ولی خط ۶۴۸
  `PROJECT-STATE.md` یک تناقض زنده است و باید اصلاح شود.

---

## فهرست کارهای باز (برای پلن/تأیید آینده)

۱. بهبود کوچک — هدر `Retry-After` (و `X-RateLimit-*`) در پاسخ ۴۲۹ِ `HubController`.
۲. بهبود کوچک — سخت‌گیری فیلترهای `UrlGuard` در محیط تولید (fail-closed).
۳. اصلاح سند — نسخهٔ DB در `PROJECT-STATE.md`، `HANDOFF-PROMPT.md` و
   `REQUIREMENTS-TRACEABILITY.md` به ۴۸.
۴. ممیزی عملیاتی (نه کد) — بررسی گزینهٔ `igbz_settings` برای کلیدهای متنیِ قدیمی در
   سایت‌های مستقر + چک گزارش در `StatusPage`/health.
۵. تأیید استقرار — دیتابیس تولید MySQL/InnoDB (ران‌بوک موجود) + رانندهٔ cron سرور.

هر تغییر کدِ این فهرست همچنان نیازمند پلن ← تأیید ← «شروع کن» است (قاعدهٔ §۰.۱).
