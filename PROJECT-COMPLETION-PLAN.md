# برنامهٔ جامع تطبیق کد، اسناد و تکمیل پروژهٔ IGBZ-WP

> تاریخ تدوین: ۱۴۰۵/۰۶/۰۵ — ۲۷ اوت ۲۰۲۶ · آخرین تطبیق: ۱۴۰۵/۰۶/۰۶ — ۲۸ اوت ۲۰۲۶
>
> وضعیت: فاز ۰۱ تمام؛ معماری پادو/اینستاگرام در ADR-0004 ثبت شده؛ هنوز مجوز اجرای کد فازهای بعدی صادر نشده است.
>
> تصمیم جاری: DeepInfra مستقل هر فروشگاه برای inference و Zernio مرکزی با profile جدا برای
> هر فروشگاه به‌عنوان تنها provider اجتماعی. Manus، ChatPlace، ManyChat و Ayrshare حذف‌اند.
>
> دامنه: افزونهٔ IGBZ، محیط وردپرس/ووکامرس، پنل و فروشگاه، API، استقرار، آزمون و
> اسناد همین مخزن. پوشهٔ `vira/` طبق دستور کارفرما خارج از محدوده است. اپ‌های فلاتر نیز
> تا دریافت فایل کامل و شروع در مخزن جدا، فقط از نظر قرارداد API در این برنامه دیده می‌شوند.

---

## ۱. تعریف صادقانهٔ «تکمیل کامل»

تکمیل کامل به معنی زیادبودن کلاس‌ها، سبزبودن تست پایه یا نبودن عبارت TODO نیست. پروژه فقط
وقتی کامل محسوب می‌شود که برای **هر نیاز پذیرفته‌شده** زنجیرهٔ زیر قابل اثبات باشد:

```text
منبع نیاز ← تصمیم معماری ← کد واقعی ← آزمون فنی ← آزمون زنده/ویژوال ← سند بهره‌برداری
```

هر نیاز باید یکی از این وضعیت‌های صریح را داشته باشد:

- `پیشنهادی`: هنوز تصمیم کارفرما نیست و مبنای کدنویسی قرار نمی‌گیرد؛
- `پذیرفته‌شده`: تصمیم قطعی است ولی ممکن است هنوز کد نداشته باشد؛
- `در حال اجرا`: بستهٔ آن با مجوز صریح شروع شده است؛
- `پیاده‌شده`: کد دارد، ولی هنوز همهٔ شواهد پذیرش کامل نیست؛
- `تأییدشده`: کد، آزمون، اجرای زنده و سند همگی کامل‌اند؛
- `به‌تعویق‌افتاده`: با علت، مالک تصمیم و شرط بازگشت؛
- `ردشده`: عمداً از دامنه حذف شده و نباید در سند دیگری «لازم» معرفی شود.

شرط نهایی این برنامه این است که هیچ نیاز پذیرفته‌شده‌ای در وضعیت مبهم، بدون کد، بدون آزمون
یا با ادعای خلاف واقع باقی نماند. کد بدون نیاز مستند نیز باید یا به نیاز معتبر متصل شود یا
با تأیید کارفرما حذف گردد.

---

## ۲. نتیجهٔ تحقیق به‌روز و اثر آن بر برنامه

۱. برای جلوگیری از واگرایی دوباره، یک **ماتریس زنده و دوطرفهٔ ردیابی نیازمندی** لازم است؛
   هر نیاز باید به طراحی، کد، آزمون، نقص و مدرک انتشار متصل باشد و مسیر معکوس «کد به نیاز»
   نیز وجود داشته باشد.
۲. اعلام سازگاری ووکامرس کافی نیست. خود ووکامرس آزمون دستی با سبد/تسویهٔ بلوکی، HPOS،
   ویرایشگر محصول، ویرایشگر سایت، قالب‌ها و افزونه‌های رایج را لازم می‌داند. برای فروشگاه
   بزرگ، مهاجرت HPOS باید ابتدا محلی، سپس روی staging با CLI و راستی‌آزمایی داده و در آخر
   مرحله‌ای در تولید انجام شود.
۳. در سامانهٔ چندمستأجری، شناسهٔ مستأجر باید در query، cache، فایل، صف، rate limit و audit
   حضور داشته باشد و مجوز مالکیت شیء در لایهٔ دسترسی داده اعمال شود؛ کنترل صرف در کنترلر
   برای جلوگیری از BOLA کافی نیست.
۴. webhook و callback ذاتاً تکراری، نامرتب و گاهی گم‌شدنی‌اند. الگوی قابل اتکا عبارت است از:
   دریافت سریع، امضای معتبر، inbox بادوام، deduplication، پردازش غیرهم‌زمان، retry محدود،
   dead-letter، replay و reconciliation دوره‌ای با منبع بیرونی.
۵. خروجی عامل هوشمند و محتوای بیرونی قابل اعتماد نیست. رخدادهای واقعی ۲۰۲۵ و ۲۰۲۶ نشان
   داده‌اند ترکیب ورودی غیرقابل‌اعتماد با ابزار دارای اختیار و رازهای CI می‌تواند به اجرای
   فرمان و نشت کلید منجر شود. پادو باید کمترین اختیار، خروجی ساختاریافته، تأیید انسانی،
   جداسازی داده/دستور و ممنوعیت اجرای کد تولیدی داشته باشد.
۶. راهنمای جاری میزبانی وردپرس، MySQL 8.4 LTS یا MariaDBهای LTS پشتیبانی‌شده را توصیه
   می‌کند. راهنمای فعلی Railway این پروژه هنوز `mysql:8.0` را پیشنهاد می‌دهد؛ پشتیبانی آن
   طبق منبع رسمی در آوریل ۲۰۲۶ پایان یافته و باید پس از آزمون سازگاری به نسخهٔ LTS منتقل شود.

منابع اصلی:

```text
https://developer.woocommerce.com/docs/extensions/best-practices-extensions/compatibility/
https://developer.woocommerce.com/docs/features/orders/high-performance-order-storage/guide-large-store/
https://developer.woocommerce.com/docs/features/orders/high-performance-order-storage/recipe-book/
https://actionscheduler.org/
https://actionscheduler.org/perf/
https://actionscheduler.org/wp-cli/
https://cheatsheetseries.owasp.org/cheatsheets/Multi_Tenant_Security_Cheat_Sheet.html
https://cheatsheetseries.owasp.org/cheatsheets/Secrets_Management_Cheat_Sheet.html
https://cheatsheetseries.owasp.org/cheatsheets/Server_Side_Request_Forgery_Prevention_Cheat_Sheet.html
https://owasp.org/www-project-top-10-for-large-language-model-applications/
https://make.wordpress.org/hosting/handbook/server-environment/
```

بازخوردهای عملی بررسی‌شده نیز بر سه مشکل پرتکرار تأکید داشتند: گیرکردن مهاجرت HPOS در
فروشگاه بزرگ، تکرار/گم‌شدن webhook بدون reconciliation، و prompt injection در عامل‌های
دارای دسترسی CI. بنابراین آزمون migration، replay/reconciliation و مرز ابزار پادو در این
برنامه دروازهٔ انتشارند، نه کارهای اختیاری.

---

## ۳. مرحلهٔ صفر — ایجاد منبع حقیقت و بستن دامنه

این مرحله قبل از هر توسعهٔ تازه انجام می‌شود.

### ۳.۱ ماتریس ردیابی

فایل مرجع زیر ساخته می‌شود:

```text
REQUIREMENTS-TRACEABILITY.md
```

هر ردیف حداقل این ستون‌ها را دارد:

- شناسهٔ یکتای نیاز؛
- متن دقیق نیاز و منبع آن؛
- قطعی یا پیشنهادی بودن؛
- اولویت و ریسک؛
- وابستگی‌ها و تصمیم معماری؛
- فایل‌ها/کلاس‌ها/جدول‌ها/مسیرهای پیاده‌ساز؛
- تست‌های واحد، یکپارچه، قراردادی، مرورگر و بار؛
- مدرک زنده یا اسکرین‌شات؛
- وضعیت واقعی و دلیل؛
- آخرین نسخه‌ای که تأیید شده است.

خانوادهٔ شناسه‌ها:

```text
GOV  حاکمیت و ردیابی          SEC  امنیت و رازها
TEN  چندمستأجری              DAT  داده و مهاجرت
QUE  صف و کار پس‌زمینه       COM  تجارت، پرداخت و کیف پول
DOM  دامنه                    EXT  سرویس‌های بیرونی
IG   اینستاگرام و VIP         PAD  پادو
API  API و موبایل             UX   تجربه، ترجمه و دسترس‌پذیری
OPS  عملیات و استقرار         DOC  اسناد و انتشار
```

### ۳.۲ موجودی ماشینی و مسیر معکوس

اسکریپت ممیزی فقط‌خواندنی باید فهرست‌های زیر را از کد تولید کند تا شمارش دستی سندها منبع
حقیقت نباشد:

- ماژول‌ها، سرویس‌های کانتینر و capabilityها؛
- همهٔ مسیرهای REST همراه permission callback و schema؛
- جدول‌ها، نسخه‌های migration و ایندکس‌ها؛
- تنظیمات و طبقه‌بندی راز/PII؛
- cronها، jobها، batchها و تماس‌های بیرونی؛
- shortcodeها، صفحات مدیریت و endpointهای حساب کاربری؛
- providerها، webhookها و callbackها؛
- فایل‌های ترجمه و رشته‌های بدون ترجمه؛
- تست‌ها و نیازهایی که پوشش می‌دهند.

سپس بررسی معکوس انجام می‌شود: هر مسیر، جدول، تنظیم، job و کلاس دامنه‌ای باید حداقل به یک
نیاز معتبر وصل باشد. موارد یتیم برای حذف یا مستندسازی به کارفرما ارائه می‌شوند.

### ۳.۳ پاک‌سازی تناقض‌های فعلی اسناد

موارد تاریخی حذف نمی‌شوند؛ در بخش «تاریخچه» نگه داشته می‌شوند، ولی وضعیت جاری فقط از یک
بخش نسخه‌دار خوانده می‌شود. تناقض‌های شناخته‌شده که باید در همین مرحله بسته شوند:

- نسخهٔ دیتابیس ۱۸/۱۹ و شمارش‌های تاریخی ۶۹/۷۰/۷۲؛
- ۱۴۳ فراخوانی ثبت مسیر در کد در برابر شمارش ۱۴۵ بعضی اسناد؛
- ادعای «تمام‌شدن فازهای ۶ تا ۱۴» در برابر نبود آزمون واقعی providerها؛
- ادعای رمزشدن همهٔ کلیدها در برابر ۲۲ فیلد جاافتاده؛
- «پادو سرویس بیرونی» در برابر نقش‌های نوشته‌شده برای ویرا و n8n — با ADR-0004 بسته شد؛
- خروجی فقط FSE در برابر سه نوع خروجی در بخش‌های دیگر؛
- چند provider اجتماعی و مسیر مستقیم/غیررسمی اینستاگرام — با انتخاب انحصاری Zernio بسته شد؛
- وضعیت واقعی دامنه، HPOS، ترجمه، اپ‌ها و استقرار؛
- شمارش و مسیرهای قدیمی در README، مرجع ماژول‌ها و هندآف.

### ۳.۴ دروازهٔ تصمیم‌های معماری

پیش از کدنویسی بسته‌های وابسته، این تصمیم‌ها جداگانه با کارفرما نهایی و در ADR ثبت می‌شوند:

۱. ✅ معماری نهایی مستأجر: **وردپرس چندسایتی + هستهٔ مدیریتی مشترک**؛ ثبت‌شده در
   `ADR/ADR-0001-MULTISITE-TENANCY.md`؛
۲. ✅ معماری پادو و اینستاگرام: **پادوی Playbookمحور مستقل + DeepInfra مستقل هر
   فروشگاه + Zernio مرکزی با profile جدا**؛ ثبت‌شده در
   `ADR/ADR-0004-PADO-ZERNIO-SOCIAL-ARCHITECTURE.md` که جانشین ADR-0002 است؛
۳. ✅ خروجی قالب: **هر سه نوع به انتخاب ادمین**؛ بلوکی، کلاسیک PHP و تمپلیت صفحه‌ساز
   الحاقی با گیت امنیتی متفاوت؛ ثبت‌شده در `ADR/ADR-0003-THREE-THEME-OUTPUTS.md`؛
۴. قرارداد و محیط آزمون واقعی providerهای منتخب؛ از جمله Zernio، DeepInfra و providerهای
   غیرمرتبط با این تصمیم مانند پرداخت/پیامک/لجستیک؛
۵. ظرفیت هدف، SLO، تعداد مستأجر و سقف سفارش/وبهوک؛
۶. پایگاه‌دادهٔ تولید و برنامهٔ مهاجرت از MySQL 8.0؛
۷. دامنهٔ ترجمه و پیام‌هایی که طبق تصمیم کارفرما عمداً انگلیسی می‌مانند؛
۸. تکلیف قابلیت‌های پیشنهادی که هنوز تصمیم قطعی نیستند.

**دروازهٔ خروج مرحلهٔ صفر:** همهٔ نیازها شناسه و وضعیت دارند؛ تناقض فعال اسناد صفر است؛
تصمیم‌های معماری لازم ثبت شده‌اند؛ و backlog فقط از نیازهای پذیرفته‌شده ساخته شده است.

---

## ۴. بستهٔ یک — امنیت و سدهای فوری انتشار

این بسته قبل از ورود هر کلید واقعی یا استفادهٔ تولیدی اجرا می‌شود.

### ۴.۱ رازها و تنظیمات

- یک رجیستری واحد و قابل آزمون برای تمام secretها؛
- مهاجرت idempotent و نسخه‌دار ۲۲ مقدار plaintext به ciphertext؛
- عدم بازنمایش مقدار قبلی در HTML و حفظ مقدار هنگام ارسال فیلد خالی/ماسک‌شده؛
- کلید رمزنگاری مستقل، نسخه‌دار، قابل پشتیبان‌گیری و rotation؛
- جداسازی رازهای محیط توسعه، staging و تولید؛
- اسکن تاریخچه، optionها، backupها، URLها و logها برای نشت؛
- برنامهٔ ابطال و چرخش تمام کلیدهایی که قبلاً در فیلد ناامن وارد شده‌اند؛
- تستی که افزودن هر فیلد password خارج رجیستری را در CI رد کند.

### ۴.۲ مجوز و جداسازی امنیتی

- فهرست همهٔ مسیرهای دارای شناسهٔ شیء و آزمون BOLA/BFLA؛
- اعمال `(tenant_id, resource_id)` در repository، نه فقط کنترلر؛
- ماتریس نقش‌های مدیر ارشد، مالک، کارمند، پیک، مشتری و سرویس؛
- بازبینی مسیرهای هسته، export، XML-RPC، Application Password، oEmbed و AJAX؛
- ابطال نشست، refresh token، replay، تغییر نقش و offboarding؛
- ثبت audit برای عملیات حساس بدون راز و PII خام.

### ۴.۳ ورودی بیرونی و فایل

- SSRF-safe fetch با allowlist، کنترل هر redirect، DNS/IP خصوصی، timeout و سقف حجم؛
- عدم ارسال Bearer به میزبان artefact؛
- stream دانلود، hash/امضا، quarantine و پاک‌سازی فایل موقت؛
- جلوگیری از traversal، symlink، zip bomb، MIME جعلی و PHP اجرایی؛
- schema و اندازهٔ قطعی برای JSON، webhook، تصویر و ZIP.

### ۴.۴ احراز و سوءاستفاده

- OTP و PIN فقط با مولد رمزنگاری امن؛
- rate limit اتمیک و tenant/user/IP-scoped؛
- پیام خطای غیرقابل‌شمارش، قفل کوتاه‌مدت و audit ضد سوءاستفاده؛
- امضای بیومتریک سمت سرور، nonce، timestamp و چرخهٔ دستگاه؛
- بررسی CSRF، XSS، SQL injection، mass assignment و open redirect.

**دروازهٔ خروج:** صفر راز plaintext شناخته‌شده؛ صفر نقص بحرانی/بالای باز؛ آزمون منفی
نقش و مستأجر برای همهٔ مسیرهای حساس؛ rotation و بازیابی کلید موفق؛ و گزارش امنیتی امضاشده.

---

## ۵. بستهٔ دو — معماری واقعی چندمستأجری

- resolver واحد مستأجر از هویت معتبر، دامنه و عضویت؛ بدون اعتماد به شناسهٔ ارسالی کاربر؛
- قرارداد repository اجباری برای همهٔ خواندن/نوشتن‌های tenant-owned؛
- tenant scope در cache key، transient، object cache، فایل، artefact، queue و rate limit؛
- quota و fairness برای جلوگیری از noisy neighbor؛
- provision و offboard کامل با امکان پاک‌سازی/ناشناس‌سازی؛
- break-glass مدیر ارشد با علت، زمان انقضا و audit؛
- حذف `switch_theme()` سراسری از جریان قالب مستأجر و اجرای معماری مصوب مرحلهٔ صفر؛
- تست خودکار دو یا چند مستأجر متخاصم روی تمام خانواده‌های منابع.

**دروازهٔ خروج:** مجموعه‌آزمون cross-tenant هیچ خواندن، نوشتن، cache collision، فایل مشترک
یا اجرای job برای مستأجر دیگر پیدا نکند؛ قالب و rollback نیز واقعاً tenant-scoped باشند.

---

## ۶. بستهٔ سه — داده، migration و سازگاری ووکامرس

- تثبیت نسخهٔ schema و زنجیرهٔ migration بدون پرش؛
- lock، checkpoint، resume و rollback برای migrationهای دیرپا؛
- migration روی دیتاست حجیم و نسخه‌های ارتقای مستقیم پشتیبانی‌شده؛
- بررسی uniqueness، کلیدهای خارجی منطقی، timestamp، retention و حذف امن؛
- `EXPLAIN` برای queryهای پرتکرار و sweepها و اصلاح ایندکس‌های مرکب؛
- ممنوعیت دسترسی مستقیم به جدول سفارش و استفاده از CRUD ووکامرس؛
- آزمون کامل CPT و HPOS در چهار حالت sync روشن/خاموش و منبع حقیقت قدیم/جدید؛
- checkout، refund، subscription، report، export و REST در HPOS؛
- مهاجرت CLI، راستی‌آزمایی داده، توقف/ادامه و بازگشت؛
- آزمون سازگاری MySQL 8.4 LTS و MariaDB LTS قبل از تغییر تولید.

**دروازهٔ خروج:** migration از نسخه‌های پشتیبانی‌شده تکرارپذیر و قابل بازگشت باشد؛ دادهٔ
HPOS با ابزار رسمی verify شود؛ هیچ query مستقیم ناسازگار با HPOS باقی نماند.

---

## ۷. بستهٔ چهار — صف، cron و مقیاس

- تعریف job envelope نسخه‌دار شامل tenant، نوع، payload hash، idempotency key و trace id؛
- انتقال تماس‌های بیرونی و کارهای طولانی از cron تجمیعی به Action Scheduler؛
- گروه‌بندی بر اساس مستأجر/زیرسیستم، claim lease و محدودیت هم‌زمانی؛
- retry نمایی با jitter، timeout، circuit breaker، cancellation و dead-letter؛
- replay کنترل‌شده و idempotency برای همهٔ side effectها؛
- batch کوچک و requeue برای cleanup، expiry، renewal و reconciliation؛
- cursor pagination و سقف اجباری برای فهرست‌های رشدپذیر؛
- server cron/CLI به‌جای اتکای صرف به ترافیک سایت؛
- object cache مشترک و شمارندهٔ اتمیک برای چند نمونه؛
- داشبورد backlog، سن job، شکست، retry، dead-letter و مصرف هر مستأجر؛
- آزمون بار، soak، race، قطع provider و بازگشت پس از backlog.

**دروازهٔ خروج:** اجرای تکراری اثر مضاعف نسازد؛ مستأجر پرترافیک بقیه را گرسنه نکند؛ پس
از قطعی، backlog بدون مداخلهٔ خطرناک تخلیه شود؛ p95/p99 و سقف ظرفیت ثبت شده باشند.

---

## ۸. بستهٔ پنج — هستهٔ تجارت، پول و احراز قانونی

این بسته به‌صورت برش‌های عمودی مستقل اجرا می‌شود:

### ۸.۱ کیف پول و دفتر کل

- دفتر کل append-only، موجودی مشتق‌شده و invariant جمع؛
- رزرو، آزادسازی، برگشت، تسویه و اصلاح با شناسهٔ یکتا؛
- race و double-spend، مبلغ/ارز/گردکردن و audit؛
- reconciliation روزانه و گزارش اختلاف.

### ۸.۲ درگاه‌ها و پرداخت مرکزی

- interface واحد provider و state machine تراکنش؛
- idempotency برای create/capture/refund؛
- webhook inbox با امضا، replay protection و پردازش غیرهم‌زمان؛
- callback مرورگر فقط برای تجربهٔ کاربر، نه منبع حقیقت پرداخت؛
- reconciliation با API بانک/provider؛
- refund کامل/جزئی، chargeback، timeout و وضعیت نامعلوم؛
- جلوگیری از double fulfillment و double refund.

### ۸.۳ اشتراک، پلن، BNPL و تسویه

- چرخهٔ کامل ایجاد، تمدید، grace، تعلیق، لغو و بازفعال‌سازی؛
- قانون قیمت اقساطی و نمایش شفاف؛
- callbackهای تکراری/نامرتب و جبران شکست؛
- تسویهٔ فروشنده، برداشت، escrow و گزارش حسابداری؛
- محدودیت مالی و تأیید انسانی برای عملیات پرریسک.

### ۸.۴ OTP، قرارداد و شاهکار

- قرارداد نسخه‌دار و مدرک پذیرش غیرقابل‌انکار؛
- اجرای شاهکار فقط پس از بستهٔ رازها و روی sandbox؛
- سناریوهای تطابق، عدم‌تطابق، timeout، پاسخ خراب و retry؛
- کمینه‌سازی PII، retention و ماسک audit.

**دروازهٔ خروج:** invariant مالی در race و retry حفظ شود؛ تمام callbackها idempotent باشند؛
تطبیق مالی صفر اختلاف حل‌نشده داشته باشد؛ provider واقعی sandbox تأیید شده باشد.

---

## ۹. بستهٔ شش — چرخهٔ کامل دامنه

- `DomainProviderInterface` و آداپتورهای نسخه‌دار؛
- جستجو و quote دارای قیمت، ارز، مالیات و زمان انقضا؛
- سفارش idempotent و رزرو نام بدون ثبت پیش از پرداخت؛
- ثبت فقط پس از تأیید قطعی پرداخت؛
- جبران و refund در شکست ثبت؛
- callback امضاشده، polling پشتیبان و reconciliation؛
- انتقال، تمدید دستی/خودکار، grace/redemption و اعلان انقضا؛
- DNS، مالکیت شیء مستأجر، CNAME/TXT و خطاهای انتشار؛
- audit و پنهان‌سازی اطلاعات تماس ثبت‌کننده.

**دروازهٔ خروج:** ثبت، شکست پرداخت، شکست provider، تکرار callback، تمدید، انقضا، جبران و
reconciliation روی sandbox واقعی موفق باشند.

---

## ۱۰. بستهٔ هفت — تکمیل تمام زیرسیستم‌های کسب‌وکار

هر زیرسیستم یک برش عمودی جدا با نیاز، کد، provider، آزمون و مدرک زنده خواهد داشت:

### ۱۰.۱ همکاری در فروش و گیمیفیکیشن

- نسبت‌دادن conversion، جلوگیری از self-referral و تقلب؛
- کمیسیون، برگشت پس از refund و تسویه؛
- امتیاز، جایزه، انقضا، سقف و race؛
- گزارش مالی قابل تطبیق.

### ۱۰.۲ آموزش

- دوره، ثبت‌نام، آزمون، گواهی و refund؛
- دسترسی ویدیو با لینک امضاشده، expiry و عدم نشت secret؛
- پیشرفت idempotent و آزمون نقش/مستأجر.

### ۱۰.۳ لجستیک و پیک

- quote، shipment، label، barcode، tracking و تحویل؛
- COD با state machine مالی و proof of delivery؛
- callback، retry و reconciliation ارائه‌دهنده؛
- حریم موقعیت مکانی، retention و مجوز پیک/مشتری؛
- قرارداد API اپ پیک، بدون شروع کد اپ در این مخزن.

### ۱۰.۴ مارکت‌پلیس‌ها

- آداپتور رسمی هر پلتفرم، mapping محصول/دسته/قیمت/موجودی؛
- انتشار و به‌روزرسانی idempotent؛
- rate limit، conflict resolution و تطبیق خطا؛
- تعیین تکلیف صریح قابلیت‌هایی که API رسمی ندارند.

### ۱۰.۵ سئو و تبلیغات

- متای معتبر، sitemap/feed، canonical و دادهٔ ساختاریافته؛
- اتصال فقط به API رسمی و با رضایت؛
- کنترل هزینه و تأیید انسانی کمپین؛
- پرهیز از محتوای انبوه کم‌ارزش و ثبت منشأ محتوای تولیدی.

### ۱۰.۶ ترجمه و بین‌المللی‌سازی

- حافظهٔ ترجمه tenant-scoped، glossary و نسخهٔ زبان؛
- بازبینی انسانی برای محتوای حساس؛
- ارز، timezone، عدد و مالیات مستقل از ترجمهٔ متن؛
- Stripe/PayPal/NOWPayments فقط پس از قرارداد واقعی، راز امن و sandbox.

### ۱۰.۷ واسط پرداخت ارزی

- نرخ دارای منبع، timestamp و expiry؛
- quote قفل‌شده، spread شفاف و جلوگیری از race؛
- payout adapter، وضعیت نامعلوم، جبران و reconciliation؛
- سقف ریسک، تأیید انسانی و audit مالی.

**دروازهٔ خروج:** هیچ زیرسیستم فقط به‌خاطر وجود جدول/کلاس «کامل» ثبت نشود؛ هر provider
فعال باید تست قرارداد و sandbox داشته باشد و هر provider غیرفعال صریحاً غیرفعال بماند.

---

## ۱۱. بستهٔ هشت — اینستاگرام، ثبت محصول و VIP

- lifecycle اتصال Zernio، profile mapping، revoke، rotation کلید مرکزی و tenant scope؛
- حذف/migration کامل Manus، ChatPlace، ManyChat و هر fallback اجتماعی دیگر؛ عدم ورود کانال
  Instagram مبتنی بر session در Agent Reach؛
- ممنوعیت Graph API مستقیم، scraper، cookie و session مرورگر Instagram؛
- ثبت محصول ۱۳مرحله‌ای با checkpoint، resume، idempotency و بررسی انسانی؛
- ورودی دادهٔ WooCommerce و Insight رسمی Zernio به Playbookهای پادو؛
- تولید، زمان‌بندی، انتشار، webhook و verify واقعی نتیجه از مسیر Zernio؛
- قیف comment-to-DM با deduplication، opt-out، rate limit، policy و audit بک‌اند؛
- صدای Instagram فقط از endpoint کاتالوگ Zernio و با ذخیرهٔ شناسه/provenance؛
- Business Discovery و Hashtag Search فقط پس از ارائهٔ endpoint رسمی Zernio؛
- Giveaway مقاوم به تقلب و انتخاب قابل ممیزی؛
- VIP: پلن، عضویت، خرید تکی، save، like، comment، message و expiry؛
- حذف امن فایل منقضی، entitlement آفلاین و قرارداد API اپ؛
- آزمون Zernio در برابر timeout، ۴۲۹، ۵xx، webhook تکراری/نامعتبر و قطع شبکه.

**دروازهٔ خروج:** دو profile مستقل Zernio باید بدون نشت متقاطع مسیر کامل اتصال، انتشار،
Insight و comment-to-DM را در sandbox/حساب آزمون طی کنند؛ هیچ mock یا قابلیت فاقد endpoint
به‌عنوان تکمیل شمرده نشود؛ حق دسترسی VIP در همهٔ نقش‌ها درست باشد.

---

## ۱۲. بستهٔ نه — تکمیل پادو و مرکز مجوز

### ۱۲.۱ قرارداد و مرز اعتماد

- نقش نهایی پادو، n8n، سرویس مدل و ویرا طبق ADR؛
- schema نسخه‌دار ورودی/خروجی و رد پاسخ خارج قرارداد؛
- جداسازی متن غیرقابل‌اعتماد از دستور سیستم؛
- ابزارهای allowlist با کمترین اختیار و credential مستقل؛
- بودجهٔ زمان/توکن/هزینه و قطع‌کن اضطراری؛
- هیچ اجرای مستقیم shell، PHP یا کد تولیدشده توسط مدل.

### ۱۲.۲ صف مجوز

- claim اتمیک `pending → executing`؛
- hash و نسخهٔ payload؛ تغییر payload تأیید را باطل کند؛
- مالک، tenant، تصمیم‌گیر، capability، سقف مالی و expiry؛
- اجرای دقیقاً یک‌بار از دید اثر تجاری با idempotency؛
- timeout، escalation، cancellation، جبران و audit؛
- اتصال واقعی قیمت، refund، حذف انبوه، کمپین، سیاست و انواع انتشار.

### ۱۲.۳ طراحی قالب

- نهایی‌کردن اسکیل `igbz-store-theme` و هفت مرجع؛
- فقط artefact غیرقابل‌اجرا در صورت انتخاب FSE؛
- ZIP امن، امضا، hash، schema و golden fixtures؛
- preview واقعاً جدا و tenant-scoped؛
- ویرایش نسخه‌دار، مقایسهٔ بصری، تأیید و rollback واقعی؛
- عدم تغییر قالب سراسری سایت مشترک.

### ۱۲.۴ حافظه، Playbook و KPI

- دادهٔ دانش، تجربه، خاطره و حافظهٔ کاری با tenant scope؛
- provenance، نسخه، retention، حذف و حق دسترسی؛
- دفاع در برابر memory poisoning و محتوای آلوده؛
- Playbook نسخه‌دار و امکان rollback؛
- KPI قابل اندازه‌گیری و عدم نسبت‌دادن نتیجهٔ جعلی به پادو؛
- بازبینی دوره‌ای و audit دسترسی.

### ۱۲.۵ آزمون خصمانه

- direct/indirect prompt injection؛
- tool poisoning و خروجی مخرب provider؛
- درخواست افشای secret/PII؛
- جعل tenant یا capability؛
- artefact بزرگ/خراب/اجرایی؛
- تأیید تکراری، هم‌زمان و منقضی؛
- قطع مدل، تغییر schema و افزایش هزینه.

**دروازهٔ خروج:** هیچ عملیات حساس خارج مرکز مجوز اجرا نشود؛ خروجی مدل بدون اعتبارسنجی به
ابزار نرسد؛ preview و live جدا باشند؛ تمام عملیات هدف اثر قابل‌اثبات و rollback آزموده داشته باشند.

---

## ۱۳. بستهٔ ده — API موبایل و قرارداد اپ‌ها

- انتشار قرارداد OpenAPI نسخه‌دار برای همهٔ endpointهای اپ؛
- JWT access/refresh با rotation، replay detection و ابطال دستگاه؛
- permission و tenant ownership روی تک‌تک منابع؛
- cursor pagination، سقف پاسخ، فیلتر و sort معتبر؛
- idempotency برای درخواست‌های نوشتنی موبایل؛
- upload امن و resumable در صورت نیاز؛
- FCM token lifecycle، رضایت و حذف دستگاه؛
- سازگاری نسخهٔ قدیم اپ و sunset policy؛
- contract test تولیدشده از schema؛
- سناریوهای offline/retry/conflict برای قرارداد، بدون تغییر اپ تا دریافت مخزن آن.

**دروازهٔ خروج:** قرارداد API پایدار و قابل تولید کلاینت باشد؛ آزمون نقش/شیء/tenant کامل؛
نسخهٔ قبلی طبق سیاست شکسته نشود؛ سپس کار اپ‌ها در مخزن جدا آغاز می‌شود.

---

## ۱۴. بستهٔ یازده — تجربهٔ کاربری، فارسی و دسترس‌پذیری

- تولید و نگهداشت `fa_IR.po/mo` از POT جاری؛
- ترجمهٔ همهٔ رشته‌های پذیرفته‌شده و تعیین صریح استثناها؛
- بسته‌بندی ترجمه‌های جاوااسکریپت ووکامرس؛
- RTL واقعی، اعداد، ارز، تاریخ، timezone و فونت محلی؛
- رفع مربع glyph و جایگزینی emoji کنشی با Dashicons/SVG محلی؛
- رفع fixture کشور تسویه برای مهمان و کاربر واردشده؛
- طراحی responsive برای دسکتاپ، تبلت و موبایل؛
- دسترس‌پذیری صفحه‌کلید، focus، label، contrast و screen reader؛
- آزمون نقش‌های مدیر ارشد، مالک، کارمند، پیک و مشتری؛
- baseline تصویری نسخه‌دار و بازبینی چشمی تمام‌صفحه.

**دروازهٔ خروج:** سناریوهای هدف بدون متن ناخواستهٔ انگلیسی و glyph شکسته باشند؛ کشور و
واحد درست؛ آزمون دسترس‌پذیری هدف و ویژوال در اندازه‌ها/نقش‌های مصوب سبز باشد.

---

## ۱۵. بستهٔ دوازده — عملیات، استقرار و آمادگی تولید

- staging همسان تولید با نسخهٔ LTS پایگاه‌داده؛
- مدیریت راز خارج تصویر/مخزن و rotation بدون downtime؛
- health/readiness جدا از liveness؛
- اجرای server cron و worker قابل مشاهده؛
- log ساختاریافته با trace id و tenant id، بدون راز/PII؛
- شاخص‌های latency، خطا، queue lag، provider، هزینه و conversion؛
- SLO و هشدار قابل اقدام با owner و escalation؛
- backup رمز‌شدهٔ DB، uploads، artefact و config؛
- restore drill واقعی و سنجش RPO/RTO؛
- rollback افزونه، migration، قالب و استقرار؛
- dependency، licence، secret و vulnerability scan؛
- تست load، soak، race، chaos و قطعی provider؛
- canary/مرحله‌ای، smoke مرورگر روی URL عمومی و توقف خودکار انتشار؛
- Runbook رخداد، کلید، صف، پرداخت، دامنه، HPOS و بازیابی.

**دروازهٔ خروج:** restore و rollback واقعاً اجرا شده باشند؛ SLO قابل اندازه‌گیری باشد؛
پایگاه‌دادهٔ منقضی استفاده نشود؛ staging و تولید از نظر نسخه/تنظیمات کنترل‌شده همسان باشند.

---

## ۱۶. بستهٔ سیزده — تطبیق نهایی کد و سند و انتشار

### ۱۶.۱ آزمون نهایی

- تست واحد و mutation برای invariantهای امنیتی/مالی؛
- تست یکپارچه روی وردپرس و ووکامرس واقعی؛
- تست قرارداد تمام providerهای فعال؛
- تست مرورگر کامل و اسکرین‌شات تمام‌صفحه با بازبینی چشمی؛
- ماتریس WordPress/PHP/WooCommerce/DB/HPOS؛
- تست migration، backup، restore و rollback؛
- تست بار و chaos با نتایج ثبت‌شده؛
- ممیزی امنیتی مستقل و رفع همهٔ بحرانی/بالا.

### ۱۶.۲ تطبیق نهایی اسناد

- ماتریس ردیابی برای همهٔ نیازهای پذیرفته‌شده `تأییدشده` یا تعویق/رد مصوب؛
- هر مسیر، جدول، setting، job، capability و provider به سند متصل؛
- شمارش‌ها از کد تولید شوند و CI در اختلاف شکست بخورد؛
- بخش‌های تاریخی به‌وضوح تاریخی و وضعیت جاری یکتا باشد؛
- README نصب، API، امنیت، migration، عملیات و عیب‌یابی به‌روز؛
- فهرست کلیدها بدون نمایش secret و با راهنمای rotation؛
- changelog، نسخهٔ schema، نسخهٔ افزونه و release note هماهنگ؛
- قابلیت فاقد کد هرگز با برچسب «کامل» باقی نماند.

### ۱۶.۳ پذیرش کارفرما

یک گزارش نهایی ارائه می‌شود که برای هر خانوادهٔ نیاز، لینک سند، کد، تست و مدرک زنده را
نشان می‌دهد. موارد تعویق‌یافته جدا و با اثرشان بر محصول اعلام می‌شوند. سپس کارفرما بر اساس
همین مدرک، انتشار یا ادامه را تأیید می‌کند.

**دروازهٔ خروج نهایی:** هیچ نیاز پذیرفته‌شدهٔ بدون شاهد، هیچ کد دامنه‌ای یتیم، هیچ تناقض
فعال میان اسناد و کد، هیچ نقص بحرانی/بالا، و هیچ آزمون پذیرش قرمز باقی نماند.

---

## ۱۷. راهبرد آزمون در تمام بسته‌ها

هر بسته متناسب با ریسک خود این ترتیب را دارد:

۱. تست واحد برای منطق خالص و invariant؛
۲. تست یکپارچه با DB و API واقعی وردپرس/ووکامرس؛
۳. تست contract با sandbox provider؛
۴. تست race/idempotency برای پول، صف، webhook و مجوز؛
۵. تست امنیتی مثبت و منفی برای نقش/tenant/ورودی؛
۶. اجرای زنده با مرورگر واقعی؛
۷. اسکرین‌شات تمام‌صفحه و بازبینی چشمی؛
۸. تست بار یا migration برای مسیرهای رشدپذیر؛
۹. به‌روزرسانی سند و ماتریس ردیابی؛
۱۰. commit مستقل روی شاخهٔ همان جلسه.

mock فقط برای خطاهای کنترل‌شده و سرعت تست واحد است. وجود mock هیچ provider را «تأییدشده»
نمی‌کند. `curl` یا تست واحد به‌تنهایی نیز تأیید نهایی رابط نیست.

---

## ۱۸. ترتیب وابستگی و امکان کار موازی

ترتیب بحرانی:

```text
مرحلهٔ صفر
  ← امنیت رازها و مجوز
  ← معماری مستأجر و داده
  ← صف و مقیاس
  ← پرداخت/دامنه/سرویس‌های بیرونی
  ← اینستاگرام و پادو
  ← API پایدار
  ← فارسی و تجربهٔ کاربری
  ← عملیات و تطبیق نهایی
```

پس از تثبیت مرحلهٔ صفر، این کارها می‌توانند تا حدی موازی شوند:

- امنیت مسیرها با migration داده؛
- تست HPOS با طراحی صف؛
- زیرسیستم‌های غیرمالی با قرارداد API موبایل؛
- ترجمه با کار providerها، به‌شرط ثابت‌شدن رشته‌ها؛
- مشاهده‌پذیری از ابتدای هر بسته، نه فقط پایان پروژه.

پرداخت دامنه نباید قبل از هستهٔ پرداخت کامل شود؛ provider واقعی نباید قبل از اصلاح رازها
کلید بگیرد؛ پادو نباید قبل از صف اتمیک و معماری tenant قالب، عملیات حساس اجرا کند؛ اپ‌ها
نباید پیش از تثبیت قرارداد API شروع شوند.

---

## ۱۹. روش اجرای هر بسته طبق قوانین کارفرما

برای هر بسته دقیقاً این چرخه اجرا می‌شود:

```text
تحقیق تازهٔ وب
← گزارش وضعیت همان بخش از روی کد
← برنامهٔ فایل‌به‌فایل و تست‌به‌تست
← تأیید کارفرما
← عبارت صریح «شروع کن»
← پیاده‌سازی
← آزمون هدفمند + زنده + ویژوال
← به‌روزرسانی ماتریس و اسناد
← گزارش نهایی همان بسته
```

- «خوبه»، سکوت یا تأیید کلی، مجوز شروع کد نیست.
- سؤال‌ها در صورت نیاز تک‌به‌تک با گزینه و پاسخ آزاد مطرح می‌شوند.
- برای تغییر کوچک، تست سبک و متناسب گرفته می‌شود.
- هیچ اعلان GitHub بدون دستور تازه ارسال نمی‌شود.
- پوشهٔ `vira/` بدون دستور صریح بررسی یا تغییر نمی‌کند.

---

## ۲۰. معیارهای عددی پایان پروژه

- ۱۰۰٪ نیازهای پذیرفته‌شده دارای تصمیم، کد، آزمون و مدرک یا تعویق/رد مصوب؛
- ۱۰۰٪ مسیرهای REST دارای schema، مجوز، tenant test و سقف ورودی/خروجی متناسب؛
- ۱۰۰٪ فیلدهای حساس طبقه‌بندی‌شده و صفر secret plaintext شناخته‌شده؛
- صفر دسترسی cross-tenant در ماتریس آزمون؛
- صفر side effect مالی/اجرایی تکراری در آزمون race/replay؛
- صفر query مستقیم ناسازگار با HPOS؛
- صفر فهرست یا cleanup رشدپذیر بدون limit/batch مصوب؛
- صفر provider فعال بدون contract test و sandbox evidence؛
- صفر نقص امنیتی بحرانی یا بالا؛
- صفر آزمون پذیرش قرمز و صفر خطای ویژوال پذیرفته‌نشده؛
- restore، rollback و HPOS migration موفق و زمان‌گیری‌شده؛
- صفر تناقض فعال میان ماتریس، اسناد جاری و کد.

این معیارها ممکن است پس از تعیین SLO و ظرفیت هدف سخت‌گیرانه‌تر شوند، اما بدون تأیید صریح
کارفرما ضعیف‌تر نمی‌شوند.

---

## ۲۱. برآورد و محدودیت‌های فعلی

در حال حاضر ارائهٔ زمان تقویمی دقیق صادقانه نیست، چون موارد زیر هنوز مشخص نیستند:

- تصمیم معماری مستأجر و پادو؛
- ظرفیت و SLO هدف؛
- دسترسی sandbox و قرارداد providerها؛
- تعداد نفرات و امکان کار موازی؛
- فایل کامل اپ‌ها؛
- اجازهٔ ورود به محدودهٔ ویرا.

پس از مرحلهٔ صفر، backlog دارای اندازه و وابستگی می‌شود و می‌توان برآورد مبتنی بر شواهد
ارائه کرد. تا آن زمان هر تاریخ قطعی، حدس خواهد بود.

---

## ۲۲. اولین گام پیشنهادی

پس از تأیید این برنامه، نخستین برنامهٔ اجرایی باید فقط برای این برش نوشته شود:

```text
SEC-001 — رجیستری واحد رازها، مهاجرت امن ۲۲ فیلد و جلوگیری از بازنمایش HTML
```

این برش پیش‌نیاز آزمون واقعی شاهکار، دامنه، FX، درگاه‌ها، لجستیک، مارکت‌پلیس، ترجمه و
تبلیغات است. هنوز اجرای آن آغاز نشده و به تأیید و عبارت صریح «شروع کن» نیاز دارد.

---

## ۲۳. فازبندی اجرایی تک‌نوبتی — بازتنظیم بر اساس ظرفیت واقعی

بسته‌های بخش‌های ۴ تا ۱۶، **نقشهٔ پوشش کلان** هستند و نباید یک‌جا اجرا شوند. برای اینکه
هر واحد کار در یک نوبت، بدون توقف برنامه‌ریزی‌شده و بدون گزارش میانی قابل پایان باشد، آن‌ها
به فازهای زیر شکسته شدند. این بخش، واحدبندی اجرایی نهایی برنامه است.

### ۲۳.۱ قرارداد اندازهٔ هر فاز

هر فاز فقط وقتی شروع می‌شود که همهٔ شرط‌های ورودش آماده باشند:

- تصمیم معماری و معیار پذیرش آن از قبل قطعی باشد؛
- هیچ سؤال باز لازم برای اجرای همان فاز باقی نمانده باشد؛
- فازهای پیش‌نیاز تمام شده باشند؛
- credential، sandbox، فایل یا دسترسی بیرونی لازم پیش از شروع آماده باشد؛
- وضعیت شاخه و خط پایهٔ تست معلوم باشد؛
- دامنهٔ تغییر پیش از عبارت «شروع کن» بسته شده باشد.

حد معمول یک فاز:

- یک رفتار دامنه‌ای کامل یا یک نگرانی سراسری محدود؛
- معمولاً ۳ تا ۱۲ فایل کد محصول؛ اگر ممیزی قبل از شروع دامنهٔ بزرگ‌تری نشان دهد، فاز پیش
  از اجرا خرد می‌شود؛
- حداکثر یک migration اصلی؛
- حداکثر یک provider واقعی؛
- حداکثر پنج صفحهٔ رابط متأثر؛
- تست هدفمند، اجرای زنده، اسکرین‌شات تمام‌صفحه، بازبینی چشمی و سند در همان فاز.

گزارش میانی داده نمی‌شود. خطاهای عادی کد و تست در همان نوبت بررسی و رفع می‌شوند و در پایان
فقط یک گزارش نهایی **دو یا سه خطی** ارائه می‌شود؛ جزئیات در اسناد همان فاز می‌ماند. تنها
استثنا، مانع خارج از اختیار مانند قطع احراز GitHub،
قطع sandbox ارائه‌دهنده، نبود credential وعده‌داده‌شده یا نیاز به تصمیم تازهٔ کارفرماست.
در چنین وضعی وعدهٔ «ادامه به هر قیمت» صادقانه نیست؛ فاز با یک گزارش نهاییِ مانع متوقف می‌شود.

این خردسازی با اصل تحویل در batch کوچک DORA، برش عمودی قابل آزمون و بازبینی تغییرات کوچک
هم‌راستاست. GitHub نیز برای تغییرات بزرگ، لایه‌های کوچک و مستقل را به‌منظور بازبینی دقیق‌تر
توصیه می‌کند. منابع:

```text
https://dora.dev/capabilities/trunk-based-development/
https://github.blog/changelog/2026-07-30-stacked-pull-requests-are-now-in-public-preview/
https://github.blog/engineering/turn-one-giant-ai-generated-pull-request-to-a-reviewable-stack/
https://www.agilekaizen.com/library/product-ownership/user-stories/user-stories-making-the-vertical-slice
https://sre.google/sre-book/reliable-product-launches/
```

### ۲۳.۲ موج صفر — منبع حقیقت و تصمیم‌ها

#### فاز ۰۱ — موجودی و ماتریس ردیابی — ✅ تمام‌شده در ۱۴۰۵/۰۶/۰۵

استخراج ماشینی اسناد، نیازها، ماژول‌ها، مسیرها، جدول‌ها، تنظیمات، capabilityها، cronها،
providerها و تست‌ها؛ ساخت ماتریس اولیه و ثبت همهٔ موارد بدون کد یا بدون سند.

**خروج محقق‌شده:** `REQUIREMENTS-TRACEABILITY.md` با ۶۷ خانوادهٔ نیاز و
`PHASE-01-INVENTORY.json` با استخراج‌گر قابل بازتولید؛ موجودی ۲۱۵ فایل منبع/۱۴۳ ثبت
REST/۷۲ جدول/۳۰۴ تنظیم، ده تناقض و نه تصمیم لازم؛ بدون تغییر رفتار محصول.
`امنیت و مراقبت/README.md` نیز به‌عنوان مبنای امنیت منتخب کارفرما و محل ردیابی منبع به
تصمیم روزآمد شد.

#### فاز ۰۲ — ثبت تصمیم‌های معماری — در جریان

پاسخ‌ها تک‌به‌تک گرفته و به ADRهای نسخه‌دار تبدیل می‌شوند. چهار ADR ثبت شده‌اند: معماری
چندسایتی با control plane مشترک؛ پادوی Playbookمحور و DeepInfra مستقل هر فروشگاه همراه
Zernio مرکزی/چندپروفایلی به‌عنوان تنها provider اجتماعی؛ و سه خروجی قالب به انتخاب ادمین.
ADR-0004 جانشین ADR-0002 است. ظرفیت، دیتابیس، ترجمه و دامنهٔ سایر پیشنهادها هنوز بازند.

**خروج:** صفر تصمیم معماری لازمِ نامشخص برای فازهای بعدی.

#### فاز ۰۳ — یکسان‌سازی اسناد جاری — ✅ تمام‌شده در ۱۴۰۵/۰۶/۰۶

تاریخچه از وضعیت جاری جدا می‌شود؛ نسخهٔ دیتابیس، تعداد مسیرها/جدول‌ها، وضعیت فازهای ۶ تا
۱۴، پادو، دامنه، ترجمه، استقرار و اپ‌ها در همهٔ READMEها و DESIGNها هماهنگ می‌شوند.

**خروج:** یک وضعیت جاری یکتا و بدون ادعای متناقض؛ بخش‌های قدیمی با برچسب تاریخچه.

**نتیجهٔ محقق‌شده:** شمارش‌های کهنهٔ «وضعیت جاری» در `HANDOFF-PROMPT.md` (نمونهٔ سلامت و
دام شمارش جدول‌ها) و `DESIGN-DEPLOY-VIRA.md` با کد هم‌راستا شد (۷۲ جدول)؛ بخش‌های دارای
تاریخ (مانند `DESIGN-VIP-EXPIRY.md`) به‌عمد دست‌نخورده ماندند چون توصیف همان لحظه‌اند.

#### فاز ۰۴ — نگهبان خودکار واگرایی — ✅ تمام‌شده در ۱۴۰۵/۰۶/۰۶

ساخت بررسی‌های خودکار برای شمارش و ردیابی مسیرها، جدول‌ها، تنظیمات، رازها، ترجمه و پیوند
نیازها به آزمون؛ اختلاف کد و artefact تولیدشده باید تست را قرمز کند.

**خروج:** واگرایی قابل‌تشخیص خودکار، نه وابسته به ممیزی دستی آخر پروژه.

**نتیجهٔ محقق‌شده:** `igbz-suite/tests/DriftGuardTest.php` شمارش جدول‌ها، نسخهٔ دیتابیس و
جفت‌شدگی «فیلد password ↔ رجیستری رازها» را می‌سنجد (بلافاصله ۲۶ واگرایی واقعی را قرمز کرد)
و گام «drift guard» در `_devenv/test.sh` با `phase01_inventory.py --check` کهنه‌بودن
`PHASE-01-INVENTORY.json` را قرمز می‌کند.

### ۲۳.۳ موج یک — امنیت و مرز اعتماد

#### فاز ۰۵ — رازها و مهاجرت — ✅ تمام‌شده در ۱۴۰۵/۰۶/۰۶

رجیستری واحد، رمزنگاری ۲۲ فیلد جاافتاده، migration سازگار، ماسک HTML، نگهداشت مقدار خالی،
rotation، اسکن نشت و تست جلوگیری از افزودن password خارج رجیستری.

**نتیجهٔ محقق‌شده:** ۲۲ فیلد جاافتاده + توکن امضای `vip.media_hmac_secret` به
`Settings::SECRETS` افزوده شد (رجیستری ۴۰)؛ مهاجرت v20 (`encrypt_legacy_secrets`) مقدارهای
plaintext موجود را idempotent رمز می‌کند؛ `rotate_secret()` چرخش امن می‌دهد؛ تست
`SecretsTest` (رمز در سکون/مهاجرت/ماسک/نگهداشت خالی/چرخش) و `DriftGuardTest` (ممنوعیت
password خارج رجیستری) سبز؛ تست ۱۳۵۴/۲۶؛ اسکرین‌شات و بازبینی چشمی ۱۶ صفحه در
`visual-testing/phase-05/` با ۰ نشت plaintext. تحقیق OWASP پیش از اجرا ثبت شد.

#### فاز ۰۶ — نقش‌ها و درهای هسته — ✅ تمام‌شده در ۱۴۰۵/۰۶/۰۶

تثبیت ماتریس capability، مسیرهای هستهٔ وردپرس/ووکامرس، export، XML-RPC، oEmbed، Application
Password و مسیر اضطراری مدیر ارشد با audit.

**نتیجهٔ محقق‌شده:** ماتریس ۱۱ capability و سطح ajax ناشناس در `DriftGuardTest` پین شد؛
oEmbed کامل خاموش (`security.disable_oembed` + مهاجرت v21 برای هم‌راستایی فرم و رفتار)،
مسیر اضطراری مدیر ارشد (`security.senior_admin_id`) فقط‌نوشتنی توسط super admin و با ثبت
audit؛ `DoorsTest` (۶ سناریو) سبز؛ تأیید زندهٔ oEmbed=۴۰۴ و Application Passwords=۴۰؛
تست ۱۳۷۱/۲۷. جزئیات: `DESIGN-LEGAL-AUTH.md §۷.۷.۶` و `visual-testing/PHASE-06-REPORT.md`.

#### فاز ۰۷ — مجوز مسیرهای تجاری چندمستأجری — ✅ تمام‌شده در ۱۴۰۵/۰۶/۰۶

آزمون و اصلاح مالکیت شیء برای tenant، plan، subscription، wallet، payment، BNPL، affiliate
و LMS؛ شرط tenant در لایهٔ repository.

**نتیجهٔ محقق‌شده:** هر خواندن «شیء با شناسه» در مسیرهای تجاری شرط `tenant_id` مستأجر
جاری را گرفت (LMS شامل پیشرفت/گواهی، اشتراک در تمدید، قرارداد و اقساط اعتباری، همکار،
پرداخت، و دو عملیات نوشتاری پنل ادمین LMS)؛ کیف‌پول از قبل کاربر+مستأجر بود. چهار
استثنای تعمدی ثبت شد (کال‌بک درگاه با authority، کد معرف عمومی، خود شیء مستأجر، کاتالوگ
طرح‌ها). `TenantScopeTest` با الگوی منفی دسترسی (مستأجر ۲ به دادهٔ مستأجر ۱ نمی‌رسد)
سبز شد؛ تست ۱۳۸۵/۲۸. جزئیات و استثناها: `DESIGN-LEGAL-AUTH.md §۷.۷.۷`.

#### فاز ۰۸ — مجوز مسیرهای جانبی چندمستأجری — ✅ تمام‌شده در ۱۴۰۵/۰۶/۰۶

آزمون و اصلاح مالکیت شیء برای logistics، marketplace، SEO، translation، gamification، legal و domain، از
جمله نقص شناخته‌شدهٔ تأیید DNS.

**نتیجهٔ محقق‌شده:** مالکیت شیء در لجستیک (مرسوله/چت پیک با گیت مالکیت/مسیرهای مشتری با
کد رهگیری به‌عنوان اثبات دارنده‌کلید/چاپ لیبل)، مارکت‌پلیس (پیوند محصول↔کانال)، گیمیفیکیشن
(اعتبار هوش مصنوعی و سبد رهاشده)، پرداخت اصلی (ایدمپوتنسی و اختلاف) و دامنه اعمال شد؛
نقص تأیید با مستأجر-محورکردن `verify_dns` و `sync_tenant_domain` بسته شد. سئو/ترجمه از قبل
مستأجر-محور بودند و ممیزی شدند. چهار سناریوی منفی تازه در `TenantScopeTest`. تست ۱۳۹۳/۲۸.
جزئیات: `DESIGN-LEGAL-AUTH.md §۷.۷.۸`.

#### فاز ۰۹ — مجوز سایر ماژول‌ها — ✅ تمام‌شده در ۱۴۰۵/۰۶/۰۶

آزمون و اصلاح مسیرهای Instagram، Hub، FX، Pado و Mobile API؛ کنترل جداگانهٔ نقش سرویس،
مدیر، مالک، کارمند، پیک و مشتری.

**نتیجهٔ محقق‌شده:** خواندن‌های «شیء با شناسه» در اینستاگرام (محتوا/محصول ورودی/قیف/
قرعه‌کشی/کامنت‌ها)، موبایل‌ای‌پی‌آی (کامنت و پست)، اف‌ایکس (تسویهٔ دستی و حساب‌های ارائه‌دهنده)
و پادو (تصمیم بازبینی) مستأجر-محور شدند. تفکیک نقش‌ها با همان گیت‌های مالکیت تأمین شد:
پیک با `courier_id`، مشتری با مالکیت سفارش/کد رهگیری، نقش‌های پنل با قابلیت‌های پین‌شدهٔ
فاز ۰۶ و کنترل‌پلین با نقش‌های بالاتر از مستأجر. استثناهای تعمدی (کرون/قلاب‌های ناهمگام/
کنترل‌پلین) ثبت شدند. تست ۱۳۹۷/۲۸. جزئیات: `DESIGN-LEGAL-AUTH.md §۷.۷.۹`.

#### فاز ۱۰ — درخواست‌های بیرونی و SSRF — ✅ تمام‌شده در ۱۴۰۵/۰۶/۰۶

ساخت client امن مشترک با allowlist، کنترل redirect و IP، timeout، سقف پاسخ، stream و سیاست
ارسال credential؛ انتقال دانلود ZIP پادو به این مرز.

**نتیجهٔ محقق‌شده:** دروازهٔ متمرکز `UrlGuard` جلوی هر درخواست خروجی (لفاف `Http`، تبدیل
گفتار، دانلود رسانهٔ استودیو هوش مصنوعی و محصول ورودی) نشست؛ اسکیم محدود، رد بازه‌های
خصوصی/متادیتا (IPv4+IPv6)، خاموشی تغییرمسیر، سقف اندازهٔ پاسخ، و سیاست حامل: کلید پادو فقط
به میزبان پیکربندی‌شدهٔ خودش می‌رود. `UrlGuardTest` با آی‌پی‌های قطعی سبز شد؛ تست ۱۴۳۶/۲۹.
جزئیات: `DESIGN-LEGAL-AUTH.md §۷.۷.۱۰`.

#### فاز ۱۱ — بارگذاری، آرشیو و artefact — ✅ تمام‌شده در ۱۴۰۵/۰۶/۰۶

کنترل MIME، اندازه، traversal، symlink، zip bomb، hash/امضا، quarantine، پاک‌سازی فایل موقت
و ممنوعیت PHP/کد اجرایی در خروجی مدل.

**نتیجهٔ محقق‌شده:** دروازهٔ مشترک `ArchiveGuard` (سقف تعداد/حجم بازشده، رد نام ناامن،
امضای بایت جادویی) جایگزین منطق پراکندهٔ دریافت قالب شد و مسیر پیش‌نمایش/فعال‌سازی هم که
پیش‌تر بی‌بازبینی استخراج می‌کرد از همان دروازه گذشت؛ میم و اندازهٔ بارگذاری‌های دیگر با
`wp_handle_upload` و سقف‌های وردپرس تأمین بود؛ پویش کد ممنوع `ThemeValidator` بدون تغییر.
`ArchiveGuardTest` با آرشیوهای واقعی سبز شد؛ تست ۱۴۴۳/۳۰. جزئیات: `DESIGN-LEGAL-AUTH.md §۷.۷.۱۱`.

#### فاز ۱۲ — هویت، OTP، JWT و دستگاه — ✅ تمام‌شده در ۱۴۰۵/۰۶/۰۶

چرخهٔ access/refresh، ابطال، replay، rate limit اتمیک، OTP شماره+IP، lifecycle دستگاه و
قرارداد سمت سرور امضای بیومتریک.

**نتیجهٔ محقق‌شده:** نوسازی توکن با تصاحب اتمیک (رقابت دو درخواست = حکم بازپخش = ابطال کل
دستگاه) و سقف تلاش نقطهٔ refresh؛ قرارداد سمت سرور امضای بیومتریک `BiometricGate` با سه
دروازهٔ پنجرهٔ زمانی/نانس یک‌بارمصرف/زمان ثابت و کلید رمزگذاری‌شدهٔ دستگاه (مهاجرت دیتابیس
v22)؛ چرخهٔ جفت توکن، کول‌داون شماره+نشانی و سقف تلاش تأیید از قبل برقرار بود و ممیزی شد.
`BiometricGateTest` سبز شد؛ تست ۱۴۵۰/۳۱. جزئیات: `DESIGN-LEGAL-AUTH.md §۷.۷.۱۲`.

#### فاز ۱۳ — PII، audit، حذف و offboarding — ✅ تمام‌شده در ۱۴۰۵/۰۶/۰۶

طبقه‌بندی دادهٔ حساس، ماسک لاگ، retention، export کنترل‌شده، حذف/ناشناس‌سازی مستأجر، uninstall
امن و آزمون عدم باقی‌ماندن راز یا دادهٔ بین‌مستأجری.

**نتیجهٔ محقق‌شده:** ماسک دادهٔ شخصی در لحظهٔ ورود به لاگ (تلفن/ایمیل/نشانی/کد ملی/کارت،
شامل آرایه‌های تودرتو)؛ ماندگاری رویدادها و توکن‌ها از قبل فعال بود و ممیزی شد؛
`TenantOffboarding` با قلاب حذف مستأجر همهٔ جداول مستأجردار را با ثبت امنیتی جارو می‌کند؛
`uninstall.php` از قبل با انتخاب صریح اپراتور کار می‌کرد. خروج گروهی داده تنها از دروازهٔ
امضای بیومتریک فاز ۱۲ عبور می‌کند. `OffboardingTest` سبز شد؛ تست ۱۴۶۰/۳۲. جزئیات:
`DESIGN-LEGAL-AUTH.md §۷.۷.۱۳`.

**دروازهٔ موج یک:** همهٔ فازهای ۰۵ تا ۱۳ باید سبز باشند و هیچ نقص بحرانی/بالای باز نماند.

### ۲۳.۴ موج دو — چندمستأجری، داده و قالب

#### فاز ۱۴ — resolver و repository مستأجر — ✅ تمام‌شده در ۱۴۰۵/۰۶/۰۶

ساخت مسیر واحد تعیین مستأجر از هویت معتبر و قرارداد دسترسی داده؛ حذف اعتماد به tenant id
ارسالی و قفل‌کردن queryهای مالکیت در repository.

**نتیجهٔ محقق‌شده:** مسیر واحد `TenantScope::page_tenant_id` جایگزین اعتماد مستقیم به
`$_GET/$_POST['tenant_id']` در شش نقطهٔ خواندن سه صفحهٔ ادمین (کیف‌پول، افیلیت، BNPL) شد؛
مستأجر فقط از هویت معتبر (منطقهٔ مستأجر حل‌شده از درخواست) می‌آید و فقط دارندهٔ
`MANAGE_TENANTS` می‌تواند صفحه را به مستأجر دیگری نشانه برود — مالکان فروشگاه که
`MANAGE_WALLET/BNPL/AFFILIATE` دارند دیگر نمی‌توانند کیف‌پول/BNPL/افیلیت مستأجر دیگری را
ببینند یا تغییر دهند (بستن BOLA). مسیر `BaseController::scoped_tenant_id` از قبل همین
الگو را داشت و ممیزی شد. در پیام‌رسانی وی‌آی‌پی هم مستأجر به‌جای پارامتر کلاینت از خود
پست مشتق می‌شود تا مشتری نتواند با ادمین فروشگاه بی‌ربط رشتهٔ گفت‌وگو باز کند.
`TenantResolutionTest` سناریوی منفی را می‌سنجد. تست ۱۴۶۴/۳۳. جزئیات:
`DESIGN-LEGAL-AUTH.md §۷.۷.۱۴`.

#### فاز ۱۵ — cache، فایل، نرخ و quota — ✅ تمام‌شده در ۱۴۰۵/۰۶/۰۶

افزودن tenant scope به cache key، transient، فایل، artefact و rate limit؛ quota و جلوگیری
از noisy neighbor با آزمون برخورد کلید و مصرف هم‌زمان.

**نتیجهٔ محقق‌شده:** مسیر واحد `TenantScope::cache_key` کلیدهای کش/ترنزینت حاوی دادهٔ
مستأجر را با مستأجر حل‌شده نام‌فضا می‌دهد (شش نقطه: فلش سئو، استودیو هوش مصنوعی، نتیجهٔ
ارسال پوش، فلش حساب کاربری، نتیجهٔ آزمون، لینک وی‌آی‌پی هاب)؛ کد بدون مستأجر در فضای
صریح `t0` می‌نشیند نه در مستأجر یک. جریان‌های منی‌چت به‌جای کلید سراسری با هش کلید حساب
کش می‌شوند. سقف تجمیعی دقیقه‌ای هر مستأجر (`api.tenant_rate_limit_per_minute`، پیش‌فرض ۶۰۰)
جدا از سقف هر توکن اعمال می‌شود — یک فروشگاه با دستگاه‌های زیاد هم نمی‌تواند دیگران را
تشنه کند. فایل‌های قالب پادو در زیرشاخهٔ مستأجر ذخیره می‌شوند. استثناهای سراسری (کاتالوگ
مستأجر، توکن سرویس گوگل، دایرکتوری عمومی هاب، آنتی‌بروت‌فورس ورود) ممیزی و ثبت شدند.
`TenantIsolationTest` برخورد کلید و هم‌زمانی را می‌سنجد. تست ۱۴۷۵/۳۴. جزئیات:
`DESIGN-LEGAL-AUTH.md §۷.۷.۱۵`.

#### فاز ۱۶ — ثبت‌نام و provision فروشگاه — ✅ تمام‌شده در ۱۴۰۵/۰۶/۰۶

تکمیل جریان Hub از ثبت‌نام تا ساخت tenant، مالک، دامنهٔ اولیه، تنظیمات فروشگاه و rollback
جزئی؛ idempotency و اجرای مجدد امن.

**نتیجهٔ محقق‌شده:** جریان ثبت‌نام هاب از قبل کاربر + مستأجر + عضویت مالک + نقش + زیردامنه +
اشتراک + پرداخت را یک‌جا می‌ساخت؛ دو شکاف بسته شد. **اجرای مجدد امن:** ارسال دوبارهٔ همان
ثبت‌نام (همان ایمیل و نشانی) به‌جای خطای «نشانی گرفته‌شده»، همان فروشگاه موجود را برمی‌گرداند
و هیچ ردیف تکراری نمی‌سازد. **Rollback جزئی:** هر خطا میان ساخت مستأجر و تلاش پرداخت
(عضویت، زیردامنه، نقش، اشتراک) مستأجر تازه‌ساخت را همراه عضویتش پاک می‌کند — جاروی خروج
فاز ۱۳ هم روی آن اجرا می‌شود — و مشتری پیام شکست تمیز می‌گیرد؛ شکست زیردامنه به‌تنهایی
«تنزل» است نه شکست (نشانی مسیری باقی می‌ماند) و فقط ثبت می‌شود. `SignupTest` چهار سناریو
را با دابل ماندگار می‌سنجد (ساخت کامل، اجرای مجدد، نشانی متعلق به دیگری، و خرابی شبیه‌سازی‌شدهٔ
اشتراک با اثر صفر). تست ۱۴۹۲/۳۵. جزئیات: `DESIGN-LEGAL-AUTH.md §۷.۷.۱۶`.

#### فاز ۱۷ — مسیریابی دامنهٔ مستأجر — ✅ تمام‌شده در ۱۴۰۵/۰۶/۰۶

تثبیت منبع حقیقت نگاشت دامنه، resolve در درخواست، CNAME/TXT، دامنهٔ تأییدنشده و جلوگیری از
host-header/tenant injection.

**نتیجهٔ محقق‌شده:** دو شاخهٔ سرویس‌دهی رزولور (دامنه و مسیر) اکنون فقط مستأجر
«قابل‌مسیریابی» را حل می‌کنند — فعال، یا آزمایشی که منقضی نشده باشد (`is_active`)؛ فروشگاه
معلق/بسته/در انتظار به‌محض تغییر وضعیت دیگر روی نشانی‌اش پاسخ نمی‌دهد، در هر دو شاخه.
پیش از این شاخهٔ مسیر بی‌قید بود و شاخهٔ دامنه انقضای آزمایشی را نمی‌سنجید. جدول نگاشت دامنه
یک‌به‌یک شد: دامنهٔ متعلق به یک مستأجر به مستأجر دوم داده نمی‌شود (رفع ابهام `LIMIT 1` و
بستن مسیر سرویس‌دهی به فروشگاه اشتباه) و ورودی دامنه فقط به شکل میزبانِ کوچک‌نوشتهٔ معتبر
ذخیره می‌شود — اسکیم، مسیر و نویسه‌های نامعتبر حذف یا رد می‌شوند تا رشتهٔ ساختگی نتواند وارد
جدول مسیریابی شود. زیرساخت‌های از قبل درست ممیزی شدند: تأیید واقعی با رکورد
`_igbz-verify` TXT یا CNAME یا A در `DomainVerifier`، الزام `verified_at` در resolve،
بهداشت میزبان (کوچک‌نویسی، حذف پورت، `sanitize`)، و درهای ادمین هاب با `MANAGE_TENANTS`.
`DomainRoutingTest` پنج سناریو را می‌سنجد. تست ۱۵۰۴/۳۶. جزئیات: `DESIGN-LEGAL-AUTH.md §۷.۷.۱۷`.

#### فاز ۱۸ — معماری قالب tenant-scoped — ✅ تمام‌شده در ۱۴۰۵/۰۶/۰۶

اجرای گزینهٔ مصوب فاز ۰۲، حذف اتکای چندفروشگاهی به `switch_theme()` سراسری و ساخت مرز صحیح
قالب/تنظیمات/الگوهای هر مستأجر.

**نتیجهٔ محقق‌شده:** حذف کامل `switch_theme()` سراسری — فعال‌سازی زندهٔ قالب اکنون وضعیت
ستون `theme` مستأجر است و `TenantThemeRouter` آن را در زمان درخواست فقط برای فروشگاه خود
اعمال می‌کند (فیلترهای `template`/`stylesheet`، فقط فرانت‌اند، و قالب نصب‌نشده هرگز سرویس
نمی‌شود)؛ فعال‌سازی یک فروشگاه، سایت مادر و بقیهٔ فروشگاه‌ها را رنگ نمی‌کند. بازگشت قالب هم
مستأجر-محور شد: فقط قالب زندهٔ همان مستأجر بایگانی می‌شود (پیش از این `UPDATE` بی‌شرط
می‌توانست قالب زندهٔ سایر فروشگاه‌ها را بایگانی کند) و بازگشت موفق، نشانهٔ قالب قبلی را پاک
می‌کند. پیش‌نمایش با پارامتر `igbz_theme_preview` فقط برای عضو همان مستأجر یا ادمین پلتفرم
و فقط روی فروشگاه خودش رندر می‌شود — اسلاگ به‌تنهایی از مرز مستأجر عبور نمی‌کند.
باقی‌ماندهٔ ثبت‌شده: فایل‌های نصب‌شدهٔ قالب پس از حذف مستأجر روی دیسک می‌مانند (پاک‌سازی
دیسک در بک‌لاگ). `ThemeRoutingTest` چهار سناریو. تست ۱۵۲۰/۳۷. جزئیات:
`DESIGN-LEGAL-AUTH.md §۷.۷.۱۸`.

#### فاز ۱۹ — چارچوب migration — ✅ تمام‌شده در ۱۴۰۵/۰۶/۰۶

lock، checkpoint، resume، idempotency، گزارش پیشرفت و راه بازگشت برای migrationها؛ آزمون
ارتقای مستقیم از نسخه‌های پشتیبانی‌شده.

**نتیجهٔ محقق‌شده:** چارچوب `Migrator` جایگزین اجرای یک‌نفسهٔ ارتقا شد: قفل تک‌اجراکننده
با تصاحب قفل کهنهٔ فرآیند مرده، نقطهٔ بررسی پس از هر گام (اجرای مجدد از همان‌جا ادامه
می‌دهد نه از صفر)، رکورد پیشرفت خواندنی (از/به/انجام‌شده/باقی‌مانده)، و قرارداد
ایدِمپوتنس برای همهٔ گام‌ها که همان چیزی است که ادامه‌دادن پس از قطع را امن می‌کند.
`Activator::maybe_upgrade` پلکان گام‌های موجود را به چارچوب سپرد؛ شکست یک گام نسخه را بالا
نمی‌برد و درخواست بعدی از نقطهٔ بررسی ادامه می‌دهد؛ درخواست هم‌زمان با قفل تازه عقب
می‌نشیند. راه بازگشت صادقانه ثبت شد: گام‌های اسکیمی رو‌به‌جلو هستند؛ نشانگر نسخهٔ پیش از
ارتقا نگهداری می‌شود و بازیابی = بازگرداندن پشتیبان پایگاه‌داده + نوشتن همان نسخه — نه
عقب‌گرد خاموش اسکیم. رفتار تاریخی نسخهٔ صفر (مهر خوردن بدون مهاجرت) حفظ شد. `MigratorTest`
پنج سناریو از جمله ارتقای مستقیم از نسخهٔ قدیمی. تست ۱۵۴۴/۳۸. جزئیات:
`DESIGN-LEGAL-AUTH.md §۷.۷.۱۹`.

#### فاز ۲۰ — query، ایندکس و صفحه‌بندی — ✅ تمام‌شده در ۱۴۰۵/۰۶/۰۶

فهرست‌های نامحدود، DELETEهای بدون batch و sweepها اصلاح می‌شوند؛ `EXPLAIN` روی دادهٔ حجیم و
ایندکس‌های مرکب لازم ثبت می‌شوند.

**نتیجهٔ محقق‌شده:** حذف‌های انبوه همه به مسیر واحد `Db::delete_batches` رفتند — دسته‌های
مرتب بر `id` با سقف تکرار که باقی‌مانده را به دور بعدی خانه‌تکانی می‌سپارد: هرس روزانهٔ
کدهای یک‌بارمصرف و توکن‌ها، پالایش دستگاه‌های خاموش، توکن‌های منقضی/باطل، ماندگاری دفتر
رویداد، پاک‌سازی دستی ادمین و جاروی خروج مستأجر روی همهٔ جدول‌ها. فهرست‌های بی‌کران اصلاح
شدند: حساب‌های فعال اینستاگرام در کرون به پیمایش کی‌ست صفحه‌بندی‌شده تبدیل شدند (همهٔ حساب‌ها
بازدید می‌شوند بدون واکشی یک‌جای سراسری)؛ فهرست دستگاه‌های کاربر (۱۰۰ تازه‌ترین)، پیوندهای
مارکت‌پلیس (۵۰۰۰)، قیف‌ها (۵۰۰)، کاتالوگ نرخ اف‌ایکس (۵۰۰) و طرح‌های وی‌آی‌پی (۱۰۰) سقف
گرفتند. ایندکس‌های مرکب تازه بر اساس الگوهای کوئری کد به اسکیما افزوده شد (ستون‌های انقضای
توکن برای هرس و پویش نشست، `last_seen_at` دستگاه‌ها) و دیتابیس به نسخهٔ ۲۳ رسید. ثبت صادقانه:
سندباکس پایگاه‌دادهٔ واقعی ندارد، پس اعتبارسنجی `EXPLAIN` روی دادهٔ حجیم به‌عنوان وظیفهٔ
عملیاتی تولید ثبت شد. `BatchTest` رفتار دسته‌ها را می‌سنجد. تست ۱۵۵۴/۳۹. جزئیات:
`DESIGN-LEGAL-AUTH.md §۷.۷.۲۰`.

#### فاز ۲۱ — سازگاری کد با HPOS — ✅ تمام‌شده در ۱۴۰۵/۰۶/۰۶

ممیزی دسترسی سفارش، استفاده از CRUD ووکامرس و آزمون تمام جریان‌های سفارش در حالت قدیمی و
HPOS، بدون اجرای مهاجرت تولیدی.

**نتیجهٔ محقق‌شده:** ممیزی سراسری کد نشان داد همهٔ مسیرهای سفارش (مُهر مستأجر در چک‌اوت،
کمیسیون همکاری در فروش، بازگشت وجه، اعتبار هدیه، ثبت‌نام دوره، درگاه‌های پرداخت و کیف پول)
از قبل فقط از لایهٔ CRUD ووکامرس (`wc_get_order` / `wc_get_orders` / متادیتای شیء سفارش)
استفاده می‌کنند. دو نقطهٔ وابسته به ذخیره‌سازی به کلاس واحد جدید `WooCommerceCompat` منتقل
شدند: لینک ویرایش سفارش در پیشخوان (صفحهٔ `wc-orders` در مقابل `post.php`) و نام جدول
درآمد در حالت فعال. اعلام رسمی سازگاری `custom_order_tables` و `cart_checkout_blocks` روی
قلاب `before_woocommerce_init` ثبت شد. `HposOrderFlowTest` جریان‌های مُهر مستأجر، لینک
ویرایش، آمار درآمد و جریان کامل سفارش تکمیل‌شده را در هر دو حالت قدیمی و HPOS می‌سنجد و
بی‌بُردن به متادیتای پست را اثبات می‌کند. راستی‌آزمایی زنده روی فروشگاه سندباکس با فعال‌سازی
واقعی جدول‌ها انجام شد و یک باگ واقعی را یافت و اصلاح کرد (`OrderUtil::get_order_edit_link`
در ووکامرس ۱۱ وجود ندارد؛ API پایدار `get_order_admin_edit_url` جایگزین شد). ثبت صادقانه:
تصویربرداری مرورگری در این سندباکس ممکن نبود (دانلود مرورگر مسدود است) و با وارسی زندهٔ DOM
صفحات جایگزین شد؛ مهاجرت و راستی‌آزمایی داده کار فاز ۲۲ است. تست ۱۵۸۱/۴۰. جزئیات:
`DESIGN-LEGAL-AUTH.md §۷.۷.۲۱`.

#### فاز ۲۲ — مهاجرت و راستی‌آزمایی HPOS — ✅ تمام‌شده در ۱۴۰۵/۰۶/۰۶

همگام‌سازی محلی و استیجینگ با مسیر رسمی هسته، راستی‌آزمایی داده، روشن/خاموش همگام‌سازی،
منبع حقیقت قدیم/جدید، جریان‌های مرجوعی/تمدید/گزارش و بازگشت کنترل‌شده — همه در
`RUNBOOK-HPOS.md` با واقعیت‌های تأییدشدهٔ زنده روی ووکامرس ۱۱ سندباکس ثبت شد.

**نتیجهٔ محقق‌شده:** مسیر کامل مهاجرت (پیش‌نیاز → همگام‌سازی → راستی‌آزمایی → سوئیچ → پایش →
بازگشت) با دستورات و نگهبان‌های واقعی هسته مستند شد؛ جریان مرجوعی (`on_order_reversed` → باطل‌کردن
کمیسیون از روی شناسهٔ سفارش) در هر دو حالت ذخیره‌سازی آزموده شد — این جریان‌ها اصلاً شیء سفارش را
باز نمی‌کنند و بنابراین ذاتاً مستقل از ذخیره‌سازی‌اند؛ تمدیدهای اشتراک و گزارش‌های داخلی نیز روی
جدول‌های خودی افزونه‌اند. ثبت صادقانه: اجرای مهاجرت روی دادهٔ واقعی تولید/استیجینگ در این
سندباکس ممکن نیست (دیتابیس ماندگار ندارد) و ران‌بوک برای اجرای عملیاتی نوشته شده است؛ آزمون
رفتاری دو حالته ۱۵۸۷/۴۰ سبز. جزئیات: `DESIGN-LEGAL-AUTH.md §۷.۷.۲۲`.

**دروازهٔ موج دو:** آزمون cross-tenant، migration و HPOS همگی مستقل و سبز باشند.
✅ پاس شد (۱۴۰۵/۰۶/۰۶): cross-tenant در `TenantScopeTest`/`TenantIsolationTest`/`TenantResolutionTest`، migration در `MigratorTest`، HPOS در `HposOrderFlowTest` — هر سه مستقل و سبز.

### ۲۳.۵ موج سه — صف و مقیاس

#### فاز ۲۳ — هستهٔ صف بادوام کار — ✅ تمام‌شده در ۱۴۰۵/۰۶/۰۶

job envelope نسخه‌دار، مستأجر، شناسهٔ ردیابی، همسانی، گروه، اجارهٔ ادعا، مهلت، تلاش مجدد،
لرزش، بن‌بست، لغو و قرارداد اجرای کار.

**نتیجهٔ محقق‌شده:** هستهٔ صف بادوام روی جدول جدید `jobs` (دیتابیس ۲۴) ساخته شد: پاکت نسخه‌دار
`Envelope` (نسخهٔ ناشناخته هرگز حدس زده نمی‌شود — به بن‌بست می‌رود)، ادعای لیزدار با به‌روزرسانی
شرطی که کارگر مرده را پس از انقضای اجاره برمی‌گرداند، تلاش مجدد با پشت‌بند نمایی سقف‌دار به‌همراه
لرزش برای جلوگیری از هجوم هم‌زمان، بن‌بست با دلیل برای کارهای سمی، لغو فقط برای کارهای شروع‌نشده،
و `JobContext` که مستأجر/ردیابی/کلید همسانی/شمارهٔ تلاش/گروه را به هندلر می‌رساند. کلید همسانی در
همهٔ تلاش‌های یک عمل پایدار است و پس از پایان آزاد می‌شود. تحویل حداقل‌یک‌بار است و قرارداد،
همسان بودن هندلرها را الزام می‌کند. `JobQueueTest` با ۱۱ سناریو. جابه‌جایی کارهای واقعی در
فازهای ۲۴ تا ۲۶ انجام می‌شود. تست ۱۶۷۱/۴۱. جزئیات: `DESIGN-LEGAL-AUTH.md §۷.۷.۲۳`.

#### فاز ۲۴ — انتقال کارهای پنج‌دقیقه‌ای — ✅ تمام‌شده در ۱۴۰۵/۰۶/۰۶

فقط کارهای رویداد پنج‌دقیقه‌ای به jobهای مستقل منتقل و رفتار failure/retry آن‌ها آزموده می‌شود.

**نتیجهٔ محقق‌شده:** هر پنج کار پنج‌دقیقه‌ای (زمان‌بند محتوا، کارگر ثبت، انتشار و انقضای وی‌آی‌پی،
همگام‌سازی مارکت‌پلیس) از اجرای خطی در کرون مشترک به صف بادوام منتقل شدند: ضربان فقط با کلید
همسانیِ بازه‌ای (جذب ضربان‌های تکراری کرونگ) صف‌بندی می‌کند و `QueueRunner` با بودجهٔ کار و زمان
در همان ضربان تخلیه می‌کند. سوئیچ مارکت‌پلیس در زمان اجرا بررسی می‌شود نه صف‌بندی. رفتار
شکست/تلاش مجدد با `CronJobsTest` پوشیده شد: عقب‌نشینی رعایت می‌شود، شکست نهایی به بن‌بست می‌رود
و هیچ کاری گم یا دوبار اجرا نمی‌شود. یک باگ واقعی در آزمون زنده یافت و اصلاح شد (هوک وردپرس
به هر شنونده آرگومان می‌دهد و امضای تایپی مستقیم سقوط کرد). آزمون زندهٔ ضربان: هر ۵ کار اجرا و
`done` شدند. تست ۱۷۰۵/۴۲. جزئیات: `DESIGN-LEGAL-AUTH.md §۷.۷.۲۴`.

#### فاز ۲۵ — انتقال کارهای ساعتی — ✅ تمام‌شده در ۱۴۰۵/۰۶/۰۶

فقط sweepها و تماس‌های بیرونی ساعتی با fairness مستأجر، batch و requeue منتقل می‌شوند.

**نتیجهٔ محقق‌شده:** جاروب‌های ساعتی چندمستأجری (`bnpl.overdue`، `bnpl.reminders`، `carts.sweep`)
از اسکن سراسری با LIMIT به پخش‌بینی «یک کار به‌ازای هر مستأجر فعال» منتقل شدند: کلید همسانیِ
`بازه:مستأجر` ضربان تکراری را در هر مستأجر جذب می‌کند، `group_key` مستأجر است و `claim` صف
بین گروه‌ها نوبتی (راند-رابین) می‌شود تا مستأجر پرهیاهو بقیه را گرسنه نگذارد. هر هندلر اسکوپ‌شده
به مستأجر کار می‌کند و قرارداد ادامه دارد: بچهٔ پُر یعنی ممکن است ردیف مانده باشد، پس دور بعد با
کلید مشتق از همان کلید صف می‌شود — با سقف دور در ساعت. جاروب‌ها (`process_overdue`،
`send_reminders`، `sweep`، `retry_failed`) پارامتر اسکوپ مستأجر گرفتند (صفر = رفتار قدیمی).
کارهای تک‌نمونهٔ کنترل‌پلن (`ig.funnels.retry` با ادامه، `ig.insights.reconcile`، `hub.tick` با
گیت فاصله‌ای در زمان اجرا) هم به صف منتقل شدند و دونده در ضربان ساعتی هم تخلیه می‌کند.
`HourlyJobsTest` با ۱۰ سناریو (عدالت ادعا، پخش‌بینی و جذب تکرار، ادامه تا بچهٔ ناقص، سقف دور،
گیت‌های زمان اجرا، اسکوپ SQL). آزمون زنده: ۳ کار کنترل‌پلن و ۶ کار پخش‌بینی (۲ مستأجر × ۳)
همه `done`؛ ضربان تکراری جذب شد. تست ۱۷۴۶/۴۳ · لینت ۲۷۶/۰. جزئیات: `DESIGN-LEGAL-AUTH.md §۷.۷.۲۵`.

#### فاز ۲۶ — انتقال کارهای روزانه و cleanup — ✅ تمام‌شده در ۱۴۰۵/۰۶/۰۶

renewal، expiry، reconciliation و پاکسازی‌ها به batchهای محدود و ادامه‌پذیر تبدیل می‌شوند.

**نتیجهٔ محقق‌شده:** هر ده کار روزانه به جاب‌های مستقل با کلید همسانیِ بازهٔ روزانه منتقل شدند:
`plans.renewals`، `affiliate.commissions`، `marketplace.flush`، `master.release` (چندمستأجری)،
`ig.insights.collect` (اینستاگرام)، `api.prune` (REST)، `fx.billing.bills`، `fx.billing.settle`،
`fx.ramp.fund` (ارزی) و `cron.housekeeping` (نگهداشت کرون). قرارداد ادامه به‌صورت رسمی به
`JobQueue::continue_round` ارتقا یافت و همهٔ کارهای سقف‌دار (تمدید ۱۰۰، پورسانت ۲۰۰، آزادسازی ۱۰۰،
تسویهٔ ارزی ۵۰) از آن استفاده می‌کنند؛ نیمهٔ تسویهٔ `FxBillingService` به `settle_due(limit)`
قابل‌ادامه تفکیک شد. همهٔ گیت‌ها (`master_payment.enabled`، `manus.collect_insights`) به زمان اجرا
منتقل شدند. دونده در ضربان روزانه هم تخلیه می‌کند. `DailyJobsTest` با ۹ سناریو. آزمون زندهٔ
ضربان روزانه: هر ۱۰ کار در همان ضربان `done` شدند و ضربان تکراری جذب شد (۲۴ ردیف ثابت).
تست ۱۷۸۳/۴۴ · لینت ۲۷۷/۰. جزئیات: `DESIGN-LEGAL-AUTH.md §۷.۷.۲۶`.

#### فاز ۲۷ — مشاهده‌پذیری و آزمون صف — ✅ تمام‌شده در ۱۴۰۵/۰۶/۰۶

داشبورد backlog/age/failure، ابزار CLI، replay کنترل‌شده و آزمون load، race، قطع provider و
عدم starvation.

**نتیجهٔ محقق‌شده:** `JobQueue::stats()` (شمارش وضعیت‌ها + سن قدیمی‌ترین کار منتظر)،
`dead_letters()` و `replay()` (بازصف‌بندی کنترل‌شدهٔ بن‌بست‌ها با صفر شدن تلاش‌ها و حفظ کلید
همسانی) افزوده شد. داشبورد مدیریتی «صف کارها» (`JobsPage`) پشت‌لاگ/سن و جدول بن‌بست‌ها با دکمهٔ
بازپخش را نشان می‌دهد؛ ابزار عملیاتی `wp igbz jobs stats|dead|replay|drain` فقط در حضور
`WP-CLI` ثبت می‌شود. `QueueLoadTest` با ۶ سناریو: عدالت زیر بار (مستأجر پرهیاهو سهم برابر می‌گیرد
و هیچ کاری جا نمی‌ماند)، دو کارگر هرگز یک ردیف را دوبار نمی‌برند، قطع بالادست با پشت‌بند تلاش
مجدد و سپس بن‌بست می‌شود، بازپخش زنده اجرا می‌شود، آمار و فهرست بن‌بست‌ها درست گزارش می‌کنند.
صفحهٔ زنده با ۲۰۰ رندر شد. تست ۱۸۱۴/۴۵ · لینت ۲۸۰/۰. جزئیات: `DESIGN-LEGAL-AUTH.md §۷.۷.۲۷`.

**دروازهٔ موج سه:** هیچ کار طولانی یا رشدپذیر بر cron تجمیعی و بدون صف بادوام باقی نماند.

### ۲۳.۶ موج چهار — پول، اشتراک و احراز قانونی

#### فاز ۲۸ — دفتر کل کیف پول — ✅ تمام‌شده در ۱۴۰۵/۰۶/۰۶

invariant موجودی، رزرو، آزادسازی، برگشت، تسویه، شناسهٔ یکتا، race و reconciliation.

**نتیجهٔ محقق‌شده:** چرخهٔ کامل رزرو به لجر افزوده شد: `reserve()` (بدهی با دلیل `reserve`)،
`release_reserve()` (برگشت دقیقاً یک‌بار؛ رزرو تسویه‌شده رد می‌شود)، `settle_reserve()` (مصرف با
نهادِ صفر-مبلغ `mark` — پول قبلاً خارج شده و تسویه فقط مصرف را دائمی ثبت می‌کند؛ آزادسازیِ پس از
آن رد می‌شود) و `refund()` با قرارداد مرجع `refund:{اصل}:{بازپرداخت}` و گارد مجموع — بازپرداخت
هرگز از بدهی اصلی بیشتر نمی‌شود. همسانی (idempotency) در همهٔ عملیات‌ها بر کلید مرجع است و تکرار،
`duplicate` برمی‌گرداند. `check_invariant()` برابری موجودی کش با مجموع لجر را می‌سنجد و
`reconcile_all()` با پیمایش keyset محدود انحراف را تعمیر می‌کند — به‌صورت کار روزانهٔ
`wallet.reconcile` به صف وصل شد. `WalletLedgerTest` با ۷ سناریو (رزرو/آزادسازی/تسویه، گارد
بازپرداخت، رقابت دو برداشت که هرگز موجودی را منفی نمی‌کند، تعمیر انحراف، چرخهٔ کامل). آزمون زنده:
کار روزانهٔ تطبیق در ضربان واقعی `done` شد و انحراف صفر. تست ۱۸۶۵/۴۶ · لینت ۲۸۱/۰.
جزئیات: `DESIGN-LEGAL-AUTH.md §۷.۷.۲۸`.

#### فاز ۲۹ — هستهٔ تراکنش و webhook inbox — ✅ تمام‌شده در ۱۴۰۵/۰۶/۰۶

state machine مشترک پرداخت، دریافت سریع، امضا، deduplication، inbox بادوام، پردازش
غیرهم‌زمان و وضعیت نامعلوم.

**نتیجهٔ محقق‌شده:** صندوق ورودی وب‌هوکِ بادوام (جدول `webhook_events` در اسکیمای v25):
`WebhookInbox::receive()` در مسیر درخواست فقط درج می‌کند و سریع برمی‌گردد؛ کلید یکتای
`(source, event_key)` تکرار را در سطح دیتابیس `duplicate` می‌کند و هرگز دو بار پردازش
نمی‌شود. امضای HMAC-SHA256 روی بدنهٔ خام با سکرت هر سورس (سکرت مشترک به‌عنوان fallback)
و مقایسهٔ `hash_equals` سنجیده و وضعیتش روی رویداد ثبت می‌شود — تحویل نامعتبر هرگز
دیسپچ نمی‌شود و در درین به‌عنوان مرده ثبت می‌گردد. `process_batch()` با ادعای اتمیِ
شرطی روی وضعیت، رویدادهای سررسید را به هندلر سورس می‌سپارد؛ پاسخ «نامعلوم» یا خطا با
بک‌آف توانی (۳۰ ثانیه تا سقف یک ساعت) دوباره به استخر برمی‌گردد و پس از اتمام تلاش‌ها
با دلیل، مرده ثبت می‌شود — هیچ‌چیز بی‌صدا گم نمی‌شود. `PaymentStateMachine` تنها گذارهای
مجاز را اعمال می‌کند (`paid/failed/cancelled` پایانی؛ `pending→unknown` برای پاسخ‌های
مبهم درگاه) و نوشتن آن UPDATE شرطیِ چسبیده به وضعیت فعلی است — دو کال‌بکِ هم‌زمان هرگز
هر دو برنده نمی‌شوند و بازنده `lost_race` گزارش می‌کند. `PaymentService::apply_notification()`
مسیر اعلان‌های ناهم‌گام درگاه است و تسویه را دقیقاً از همان کد مسیر بازگشت مرورگر می‌گذراند.
اندپوینت `POST /igbz/v1/webhook/{source}` بدنه را می‌گیرد و ۲۰۰ سریع برمی‌گرداند؛ کارِ
پنج‌دقیقه‌ای `webhooks.drain` اینباکس را با ادامه‌دهیِ دسته‌ای تخلیه می‌کند.
`WebhookInboxTest` با ۸ سناریو (دریافت/تکرار، امضا، درین و تکمیل، رد تحویل نامعتبر، چرخهٔ
تلاشِ نامعلوم تا مرده‌شدن، هندلر خطادهنده، ماشین وضعیت قانونی/غیرقانونی/رقابت، ادامهٔ کارِ
درین روی دستهٔ پر). آزمون زنده: مهاجرت واقعی به v25، دریافت→تکرار→درین→آمار هم‌خوان، و
اندپوینت REST روی پلی‌گراند پاسخ صحیح داد. تست ۱۹۲۸/۴۷ · لینت ۲۸۵/۰.
جزئیات: `DESIGN-LEGAL-AUTH.md §۷.۷.۲۹`.

#### فاز ۳۰ — سخت‌سازی آداپتورهای درگاه ریالی — ✅ تمام‌شده در ۱۴۰۵/۰۶/۰۶

یکسان‌سازی قرارداد create/verify/refund، idempotency، timeout و mapping خطا برای آداپتورهای
مصوب؛ این فاز جای آزمون sandbox هر provider نیست.

**نتیجهٔ محقق‌شده:** تایم‌اوت واحد برای همهٔ ۱۵ آداپتور: `PspHttp::timeout()` با تنظیم
`payments.timeout` (پیش‌فرض ۳۰، کلمپ ۵ تا ۶۰) — ۲۰ جای هاردکد ۲۵/۳۰ حذف شد. طبقه‌بندی خطای
مشترک `GatewayErrors::classify()`: شکست ترنسپورت → `network_timeout` (گذرا)، بدنهٔ خالی →
`invalid_response`، کد/پیام خود PSP دست‌نخورده عبور می‌کند — روی زرین‌پال و HttpPsp اعمال شد
(قبلاً تایم‌اوت با ردِ دائمی درگاه یکسان دیده می‌شد). قرارداد بازپرداخت به‌صورت قابلیت:
`RefundableGatewayInterface` + `PaymentRefundResult`؛ فقط آداپتوری که PSP واقعاً endpoint
مستند دارد پیاده می‌کند (زرین‌پال طبق تحقیق به دستور بانک مرکزی موقتاً ندارد و چیزی جعل
نشد)؛ `HttpPspGateway` با تنظیم `refund_url` و کلید اجباری `Idempotency-Key` پیاده شد.
همسانیِ ایجاد: `start()` ردیف `created` هم‌کلید را به‌جای درج تکراری بازیابی می‌کند و هر
درخواست با کلید `pay-{id}` (یا کلید تماس‌گیرنده) به درگاه می‌رود؛ ایجاد هرگز خودکار
تکرار نمی‌شود (جلوگیری از authority مضاعف = جلوگیری از شارژ مضاعف).
`PaymentService::refund_payment()` با گاردهای فقط-پرداخت‌شده، مجموع بازپرداخت ≤ مبلغ، کلید
`refund:{id}:{ترتیب}` و ثبت روی متای پرداخت. `GatewayHardeningTest` با ۷ سناریو. آزمون زنده:
۱۵ آداپتور بدون خطا بارگذاری شدند، `supports_refund` درست پاسخ داد و `start()` با درگاه
غیرفعال بدون تماس با PSP سریع رد شد. تست ۱۹۶۲/۴۸ · لینت ۲۹۰/۰.
جزئیات: `DESIGN-LEGAL-AUTH.md §۷.۷.۳۰`.

#### فاز ۳۱ — درگاه مرکزی، درگاه مرکزی و هماهنگ‌سازی — ✅ تمام‌شده در ۱۴۰۵/۰۶/۰۶

اسکرو، تسویه، برداشت، استرداد کامل/جزئی، تحویل مضاعف، گزارش اختلاف و جبران.

**نتیجهٔ محقق‌شده (اسکیمای v26):** بازپرداخت اسکرو بازنویسی شد — `refund()` اکنون کلید
خوش‌بینانه دارد (دو ریفاند هم‌زمان هر دو برنده نمی‌شوند؛ بازنده `lost_race`)، مجموع
`refunded_amount` هرگز از مبلغ نگهداری‌شده فراتر نمی‌رود (`over_refund` پیش از هر نوشتن)،
ریفاند با مبلغ تهی باقیماندهٔ کامل را برمی‌گرداند، و پرداخت کامل-ریفاندشده به `refunded`
می‌رود. گارد تحویل مضاعف دوطرفه: ریفاند روی پرداخت `released` (پول خارج‌شده) رد می‌شود و
`release()` اکنون یک فلیپ شرطی `held→released` است — دو سوییپ هم‌زمان نمی‌توانند یک ردیف را
دو بار تسویه کنند. تسویه فقط «باقیماندهٔ ریفاندنشده» را به کیف پول صاحب اعتبار می‌دهد.
`resolve_dispute()` اختلاف را با حکم `release` (بازگشت به مسیر انتشار برای یکنواختیِ تنها
نویسندهٔ پول) یا `refund` می‌بندد. `reconcile()` گزارش اختلاف و جبران است: هر پرداخت
`released` باید کردیت کیف‌پولِ `master:{id}` داشته باشد — نبودش شناسایی، با کردیت
همسانِ مجدد تعمیر و گزارش می‌شود؛ اختلاف‌های بازِ کهنه شمرده می‌شوند. برداشت‌ها کلید
همسانی گرفتند (ایندکس یکتای `(tenant_id, idempotency_key)` ستون جدید `VARCHAR NULL`) و
مرجع بدهی کیف پول نیز با همان کلید است — تکرار درخواست برداشت هرگز دوبار برداشت نمی‌کند.
کار روزانهٔ `master.reconcile` به صف بادوام وصل شد (ششمین کار روزانه).
`MasterPaymentServiceTest` با ۹ سناریو (تکرارزدایی هولد، انتشار مشروط، ریفاند جزئی تا گارد،
رد تحویل مضاعف، اعتباردهی باقیمانده، حل اختلاف، باخت رقابت، تعمیر تطبیق، همسانی برداشت).
آزمون زنده: مهاجرت واقعی v26 (هر دو ستون جدید)، هولد→تکرار→ریفاند جزئی→رد بیش‌برداشت→تطبیق
بدون انحراف روی پلی‌گراند. تست ۲۰۰۹/۴۹ · لینت ۲۹۱/۰.
جزئیات: `DESIGN-LEGAL-AUTH.md §۷.۷.۳۱`.

#### فاز ۳۲ — پلن و اشتراک — ✅ تمام‌شده در ۱۴۰۵/۰۶/۰۶

ایجاد، تمدید، مهلت (گریس)، تعلیق، لغو، بازفعال‌سازی، امتیاز و اجرای دسته‌ای ادامه‌پذیر.

**نتیجهٔ محقق‌شده:** چرخهٔ حیات اشتراک کامل شد. ایجاد و تمدید از قبل موجود بود (تمدید همسان بر
مرجع لجر `sub:{id}:{ماه}` با سه‌تلاشی تا انقضا)؛ این فاز افزود: **تعلیق دستی** `suspend()` با
فلیپ شرطی روی وضعیت‌های زنده (دو تعلیق هم‌زمان هر دو اثر نمی‌گذارند) که مستأجر را نیز در همان
لحظه معلق می‌کند؛ **بازفعال‌سازی** `reactivate()` که زمان باقی‌مانده را حفظ می‌کند — اگر دوره در
تعلیق گذشته باشد اشتراک به `past_due` برمی‌گردد تا سوئیپ تمدید آن را بردارد، نه اینکه دستی پول
گرفته شود؛ اشتراک معلق در `active_subscription()` دیده نمی‌شود. **مهلت:** پنجرهٔ `plans.grace_days`
(بذر ۳ روز، پیش‌فرض کد ۷، هرگز منفی نیست) — سوئیپ جدید `expire_past_grace()` اشتراک‌های
`past_due` را فقط پس از «پایان دوره + مهلت» منقضی و مستأجر را معلق می‌کند؛ داخل مهلت سرویس‌دهی
ادامه دارد. **اجرای دسته‌ای ادامه‌پذیر:** هر دو سوئیپ (تمدید و مهلت) روی همان کار روزانهٔ
`plans.renewals` با ادامه‌دهی دسته‌ای صف بادوام سوارند — دستهٔ پر در هر طرف، دور بعد را صف می‌کند.
`PlansLifecycleTest` با ۶ سناریو (تعلیق/بازفعال‌سازی و اسکوپ، شرطی‌بودن تعلیق، بازگشت دورهٔ گذشته
به `past_due`، نامرئی‌بودن معلق، انقضای فقط بیرون مهلت، پنجرهٔ قابل تنظیم). آزمون زنده: مقدار مهلت
زنده خوانده شد و هر سه متد جدید در دسترس بودند. تست ۲۰۲۹/۵۰ · لینت ۲۹۲/۰.
جزئیات: `DESIGN-LEGAL-AUTH.md §۷.۷.۳۲`.

#### فاز ۳۳ — بی‌ان‌پی‌ال (خرید اقساطی) — ✅ تمام‌شده در ۱۴۰۵/۰۶/۰۶

قرارداد مشترک، نقل‌قول، نمایش قانونی قیمت، فراخوانی (کال‌بک)، تلاش مجدد، لغو و هماهنگ‌سازی؛ ارائه‌دهنده واقعی در فاز جداگانه‌ای تست می‌شود.

**نتیجهٔ محقق‌شده (اسکیمای v27):** قرارداد، نقل‌قول و لغو از قبل موجود بود؛ این فاز پول‌رسانی را
سخت کرد. **فلیپ شرطی پرداخت قسط:** `mark_installment_paid()` اکنون با شرط وضعیتِ خوانده‌شده
می‌نویسد و تکرار را بی‌اثر می‌کند — کال‌بک درگاه و وصول خودکار کیف پول که هم‌زمان روی یک قسط
مسابقه دهند هرگز دو بار تسویه، هوک و گزارش درگاه نمی‌فرستند. **سطح کال‌بک درگاه:** سورس `bnpl`
به اینباکس وب‌هوک فاز ۲۹ وصل شد — `apply_provider_notification()` با شناسهٔ قسط یا
(قرارداد+ترتیب) حل می‌شود، حکم `paid` را از همان مسیر شرطی عبور می‌دهد و حکم «نامعلوم» را صادقانه
برای تلاش مجدد به اینباکس برمی‌گرداند؛ پیام ناقص بدون تکرار بی‌پایان تصدیق می‌شود. **تلاش مجدد
محدود:** ستون جدید `collection_attempts` و سقف `bnpl.max_collect_attempts` (پیش‌فرض ۵) — سوئیپ
پیگیری، کیف پول خالی را تا سقف دوباره امتحان می‌کند و بعد به مسیر قصردادن می‌سپارد؛ هرگز بی‌نهایت
بر یک قسط نمی‌کوبد. **هماهنگ‌سازی:** `reconcile()` قراردادهای زنده را می‌خواند: قرارداد تمام‌پرداختی
که وضعیتش هرگز برنگشته از همان مسیر عادی تسویه می‌شود و هر قرارداد که «پرداخت‌شده + باقی‌مانده»اش
با کل قابل پرداخت نخواند شمرده و گزارش می‌شود — چیزی بی‌صدا اصلاح نمی‌شود. کار روزانهٔ
`bnpl.reconcile` به صف وصل شد (هفتمین کار روزانه). `BnplHardeningTest` با ۵ سناریو. آزمون زنده:
مهاجرت واقعی به v27 با ستون جدید، اجرای تطبیق و تصدیق کال‌بک روی پلی‌گراند. تست ۲۰۵۲/۵۱ ·
لینت ۲۹۳/۰. جزئیات: `DESIGN-LEGAL-AUTH.md §۷.۷.۳۳`.

#### فاز ۳۴ — قرارداد، او‌تی‌پی پرداخت و شاهکار — ✅ تمام‌شده در ۱۴۰۵/۰۶/۰۶

نسخهٔ قرارداد، مدرک پذیرش، گیت پرداخت، تطابق/عدم‌تطابق، پی‌آی‌آی و سناریوهای خطای شاهکار.

**نتیجهٔ محقق‌شده:** نسخهٔ قرارداد و مدرک پذیرش (`LegalWaiverService`: نسخه + هش محتوا، گیت
پرداخت بانکی) از فازهای پیش موجود بود؛ این فاز شاهکار را سخت کرد. سه نتیجهٔ شاهکار از هم جدا
شدند: `matched` / `mismatch` / `error` — پیش از این هر شکستی «عدم تطابق» ثبت می‌شد که هم تهمت به
کاربر بود و هم مانع تلاش مجدد؛ اکنون شکست ترنسپورت، بدنهٔ خالی یا پاسخ بی‌حکم `error` است و کاربر
هرگز بی‌دلیل «نامنطبق» نمی‌شود. اعتبارسنجی پیش از تماس با رجیستری: الگوریتم رسمی چک‌سام کد ملی
(ده‌رقمی، رد ارقام تکراری) و فرمت موبایل — ورودی نامعتبر اصلاً به رجیستری نمی‌رود و ردی هم برجا
نمی‌گذارد. محدودیت تلاش روزانهٔ هر کاربر (`legal.shahkar_max_attempts_per_day`، پیش‌فرض ۵) هزینهٔ
رجیستری و سوءاستفاده را می‌بندد. انضباط پی‌آی‌آی حفظ و مستند شد: فقط هش کد ملی ذخیره می‌شود، شماره
موبایل هرگز ذخیره و کد ملی هرگز لاگ نمی‌شود. `NationalIdVerifierTest` با ۴ سناریو (چک‌سام،
طبقه‌بندی بدون حدس‌زدن، گیت‌های پیش از تماس، سقف تلاش). تست ۲۰۸۲/۵۲ · لینت ۲۹۴/۰.
جزئیات: `DESIGN-LEGAL-AUTH.md §۷.۷.۳۴`.

#### فاز ۳۵ — نرخ و نقل‌قول ارزی — ✅ تمام‌شده در ۱۴۰۵/۰۶/۰۶

منبع نرخ، مهر زمانی، انقضا، اسپرد، نقل‌قول قفل‌شده، رقابت و رفتار نبود نرخ.

**نتیجهٔ محقق‌شده (اسکیمای v28):** نقل‌قول ارزی از «خواندن نرخ و امیدوار بودن» به «قفل مدرک‌دار»
ارتقا یافت. `lock_rate()` اکنون در `fx_rates` مدرک کامل ثبت می‌کند: نرخ خام، اسپرد
(`fx.spread_percent`، کلمپ ۰ تا ۵۰)، نرخ اعمال‌شده (`rate_applied` = خام + اسپرد، همان عددی که
نقل‌قول با آن قیمت می‌شود) و مهلت انقضا (`fx.quote_ttl_minutes`، پیش‌فرض ۱۵ دقیقه، حداقل ۱).
**رفتار نبود نرخ:** وقتی منبع خودکار شکست بخورد و نرخ دستی هم تنظیم نباشد، `lock_rate()` صفر
برمی‌گرداند و شارژ ارزی به‌جای قیمت‌گذاری با صفر، صریح رد می‌شود. **حذف رقابت نقل‌قول/قفل:**
`FxTopupService::start()` قبلاً از نرخ زنده نقل می‌گرفت و بعد قفل می‌کرد — بین دو خواندن بازار
می‌توانست حرکت کند؛ اکنون اول قفل می‌شود و نقل‌قول از همان ردیف قفل‌شده قیمت می‌گیرد: قیمت
نمایشی و عدد قفل‌شده ذاتاً یکی‌اند. `quote_is_valid()` نقل‌قول گذشته از مهلت یا ناموجود را مرده
می‌خواند — قول گذشته از مهلت، قول نیست. `FxQuoteTest` با ۴ سناریو + اصلاح دابل `FxTest` برای
جریان جدید. آزمون زنده: مهاجرت واقعی به v28 با سه ستون جدید، طبقه‌بندی شاهکار و تطبیق اقساط روی
پلی‌گراند. تست ۲۱۰۲/۵۳ · لینت ۲۹۵/۰. جزئیات: `DESIGN-LEGAL-AUTH.md §۷.۷.۳۵`.

#### فاز ۳۶ — پرداخت و تسویهٔ ارزی — ✅ تمام‌شده در ۱۴۰۵/۰۶/۰۶

ادپتر مستقل، چرخهٔ قبض/پرداخت، وضعیت نامعلوم، سقف ریسک، تأیید انسانی و تطبیق.

**نتیجهٔ محقق‌شده (بدون تغییر اسکیمای v28):** چرخهٔ قبض/پرداخت از «موفق/شکست» به سه
نتیجه ارتقا یافت. **وضعیت نامعلوم:** وقتی ادپتر پس از ارسال تراکنش جواب نمی‌دهد (تایم‌اوت یا
استثنا)، قبض به وضعیت `pending` می‌رود و برداشت کیف پول **نگه داشته می‌شود** — بازپرداخت در آن
لحظه یعنی پرداخت دوباره وقتی پی‌اس‌پی واقعاً پول برده. **سقف ریسک:** `fx.payout_daily_cap_usd`
مجموع پرداختی امروز (پرداخت‌شده + معلق) را می‌سنجد و قبض‌ها را پشت سقف نگه می‌دارد. **تأیید
انسانی:** `fx.payout_approval_threshold_usd` قبض‌های بزرگ را از مسیر خودکار خارج می‌کند؛ تنها
راه تسویه‌شان مسیر دستی اپراتور است. **تطبیق:** `reconcile()` معلق‌ها را از ادپتر می‌پرسد
(`query()` اختیاری ادپترها: `settled`/`failed`/`unknown`) و از `resolve_payout()` عبور می‌دهد —
حکم تأیید قبض را `paid` و حکم شکست را با بازپرداخت به `due` برمی‌گرداند؛ حکم نامعلوم هیچ چیز را
تغییر نمی‌دهد، فقط شمرده می‌شود. وب‌هوک‌ها و تطبیق هر دو از همان `resolve_payout()` می‌گذرند، پس
یک حکم بیش از یک بار اعمال نمی‌شود. کار روزانهٔ هشتم: `fx.billing.reconcile`. تست
`FxBillingHardenTest` با ۸ سناریو. تست ۲۱۳۷/۵۴ · لینت ۲۹۶/۰. جزئیات:
`DESIGN-LEGAL-AUTH.md §۷.۷.۳۶`.
### ۲۳.۷ فاز تکرارشوندهٔ provider واقعی

برای **هر provider فعال** یک فاز مستقل ساخته می‌شود؛ چند provider در یک فاز جمع نمی‌شوند.
تعداد این فازها پس از تصمیم فاز ۰۲ قطعی می‌شود، بنابراین اعلام یک عدد ثابت از حالا صادقانه
نیست. الگوی نام‌گذاری:

```text
PV-PSP-<provider>
PV-BNPL-<provider>
PV-SMS-<provider>
PV-DOMAIN-<provider>
PV-FX-<provider>
PV-LOGISTICS-<provider>
PV-MARKETPLACE-<provider>
PV-AI-<provider>
PV-INTERNATIONAL-<provider>
```

هر فاز provider شامل credential امن، sandbox، قرارداد پاسخ، webhook/callback، replay،
timeout، ۴۲۹، ۵xx، پاسخ خراب، reconciliation، rotation و Runbook است. provider بدون sandbox
یا مدرک قراردادی «تأییدشده» نمی‌شود و غیرفعال باقی می‌ماند.

**اقلام نیازمند کارفرما (ثبت ۱۴۰۶/۰۶/۰۹):** اعتبارنامه/حساب واقعی Zernio برای
`PV-ZERNIO-*` (تأمین توسط کارفرما؛ تا رسیدن آن trending-audio/Business Discovery/
Hashtag Search راستی‌آزمایی نمی‌شوند) و تأمین حساب مستقل DeepInfra فروشگاه‌ها +
تأیید benchmark فارسی به‌عنوان کلید فعال‌سازی adapter (فاز ۵۶).

### ۲۳.۸ موج پنج — دامنه و زیرسیستم‌های کسب‌وکار

#### فاز ۳۷ — جستجو، نقل‌قول و سفارش دامنه — ✅ تمام‌شده در ۱۴۰۵/۰۶/۰۶

جستجو، قیمت/انقضا، سفارش ایدمپوتنت و رزرو نام بدون ثبت پیش از پرداخت.

**نتیجهٔ محقق‌شده (اسکیمای v29):** ماژول دامنه با همان قرارداد ادپتر-محور درگاه‌ها ساخته شد:
`DomainAdapterInterface` (جستجو/نقل‌قول/ثبت) + رجیستری با انتخاب `domain.provider`.
**نقل‌قول، قیمت با مهلت است:** جدول جدید `ig_domain_quotes` قیمت و `expires_at` نگه
می‌دارد؛ مهلت از `domain.quote_ttl_minutes` (پیش‌فرض ۱۵، حداقل ۱) و مهلت خود پرووایدر کوچک‌تر
گرفته می‌شود؛ قیمت غیرمثبت رد می‌شود — دامنه با صفر قابل سفارش نیست. **سفارش ایدمپوتنت:**
`ig_domain_orders.idempotency_key` با کلید یکتای `(tenant, key)`؛ درخواست تکراری همان سفارش
اول را برمی‌گرداند و رزرو دوم نمی‌سازد. **رزرو بدون ثبت:** رزرو فقط ردیف محلی است؛ `register()`
ادپتر هرگز پیش از پرداخت فراخوانی نمی‌شود و سفارش برای ثبت بدون نقل‌قول معتبر (منقضی‌نشده) رد
می‌شود. `confirm_paid()` رزرو را دقیقاً یک بار `paid` می‌کند — دروازهٔ ثبت در فاز ۳۸. تست
`DomainOrderTest` با ۵ سناریو. تست ۲۱۷۵/۵۵ · لینت ۳۰۰/۰. جزئیات:
`DESIGN-LEGAL-AUTH.md §۷.۷.۳۷`.
#### فاز ۳۸ — ثبت، کال‌بک و جبران دامنه — ✅ تمام‌شده در ۱۴۰۵/۰۶/۰۶

ثبت فقط پس از پرداخت قطعی، کال‌بک امضاشده، پایش پشتیبان، شکست پرووایدر و بازپرداخت/ژورنال.

**نتیجهٔ محقق‌شده (اسکیمای v30):** `register_paid()` فقط روی سفارش `paid` اجرا می‌شود؛ سفارش پیش
از فراخوانی پرووایدر به `registering` می‌رود تا کرش میانهٔ کار مرئی بماند، نه ساکت. نتیجهٔ
موفق `registered` با مرجع پرووایدر؛ شکست پرووایدر ⇒ وضعیت `failed` + **بازپرداخت ایدمپوتنت** به
کیف پول ارزی (مرجع `domain_refund:<id>` با ایدمپوتنتی ذاتی کیف پول) — تکرار جبران، پول دوم
نمی‌سازد. **ژورنال:** جدول جدید `ig_domain_journal` هر رویداد (آغاز ثبت، ثبت، شکست، بازپرداخت)
را ثبت می‌کند تا شکست پرووایدر همیشه قابل توضیح باشد. **کال‌بک امضاشده:** `verify_callback()`
با HMAC-SHA256 روی بدنهٔ خام و `domain.webhook_secret`؛ بدون امضای معتبر کال‌بک هرگز دریافت‌شده
حساب نمی‌شود. **پایش پشتیبان:** `poll_stuck()` سفارش‌های مانده در `registering` (پیرتر از آستانهٔ
ساعتی) را از ادپتر می‌پرسد (`query()`: registered/failed/unknown) و حکم را از همان مسیر یکتای
`apply_provider_result()` اعمال می‌کند؛ حکم نامعلوم شمرده می‌شود و چیزی حدس زده نمی‌شود. تست
`DomainRegistrationTest` با ۶ سناریو. تست ۲۲۱۲/۵۶ · لینت ۳۰۱/۰. جزئیات:
`DESIGN-LEGAL-AUTH.md §۷.۷.۳۸`.
#### فاز ۳۹ — تمدید، دی‌ان‌اس و تطبیق دامنه — ✅ تمام‌شده در ۱۴۰۵/۰۶/۰۶

تمدید دستی/خودکار، اعلان، grace/redemption، دی‌ان‌اس محدود به مستأجر و تطبیق پرووایدر.

**نتیجهٔ محقق‌شده (اسکیمای v31):** چرخهٔ عمر دامنه در `MultiTenant/Domain/DomainService` کامل
شد. **تمدید:** `renew()` محدود به مستأجر (نگهبان BOLA)؛ تمدید از تاریخ انقضای فعلی امتداد
می‌یابد نه از امروز؛ دامنهٔ آزادشده تمدیدپذیر نیست؛ با پرووایدرِ پیکربندی‌شده تمدید اول در
رجیستری انجام می‌شود و شکست پرووایدر هرگز به امتداد محلی تبدیل نمی‌شود؛ پرچم تمدید خودکار در
ستون جدید `auto_renew` ثبت می‌شود. **اعلان:** `notify_renewals()` به دامنه‌های فعالِ در آستانهٔ
انقضا (پنجرهٔ `domain.renewal_warning_days`) هشدار می‌دهد — ایدمپوتنت به‌تفکیک تاریخ انقضا.
**نردبان انقضا:** `run_expiry_sweep()` در هر اجرا فقط یک پله جلو می‌رود: منقضی فعال → `grace` →
`redemption` → `released`؛ آزادسازی، تأیید نگاشت مستأجر را نیز برمی‌دارد. **دی‌ان‌اس:** مسیرهای
تأیید و نگاشت از قبل محدود به `tenant_id` بودند و در تست قفل شدند. **تطبیق:** `reconcile()`
انقضای محلی را با رجیستری می‌سنجد و اختلاف را گزارش و ژورنال می‌کند — هرگز بی‌صدا اصلاح
نمی‌شود. تست `DomainLifecycleTest` با ۵ سناریو. تست ۲۲۴۱/۵۷ · لینت ۳۰۲/۰. جزئیات:
`DESIGN-LEGAL-AUTH.md §۷.۷.۳۹`.
#### فاز ۴۰ — همکاری در فروش — ✅ تمام‌شده در ۱۴۰۵/۰۶/۰۶

انتساب، جلوگیری از خودارجاعی، کمیسیون، اثر بازپرداخت، تسویه و گزارش تقلب.

**نتیجهٔ محقق‌شده (بدون تغییر اسکیمای ۳۱):** لایهٔ همکاری در فروش سخت شد. **خودارجاعی:**
`enroll()` والدِ خودِ کاربر را حذف می‌کند (سطح به ۱ برمی‌گردد)؛ ارجاع به خود در ثبت سفارش و
چسباندن کد به کاربر نیز از قبل بسته بود و اکنون هر سه دروازه در تست قفل شدند. **اثر
بازپرداخت:** `void_order_commission()` حالا هم `pending` و هم `approved` پرداخت‌نشده را باطل
می‌کند؛ کمیسیون `paid` دست نمی‌خورد — پول جابه‌جا شده و گزارش تقلب آن را به‌جای پنهان‌کاری
نمایش می‌دهد. **گزارش تقلب:** `fraud_report()` فقط گزارش می‌دهد و هرگز خودکار تنبیه نمی‌کند —
چهار سیگنال: کمیسیون‌های خودارجاعی، خوشه‌های تبدیل با آی‌پی مشترک (۲+ کاربر متمایز)،
همکاران با نرخ ابطال بالای نصف (حداقل `affiliate.fraud_min_commissions` کمیسیون) و کمیسیون‌های
پرداخت‌شده روی سفارش‌های بعداً مستردشده. تست `AffiliateHardenTest` با ۳ سناریو. تست ۲۲۵۱/۵۸ ·
لینت ۳۰۳/۰. جزئیات: `DESIGN-LEGAL-AUTH.md §۷.۷.۴۰`.
#### فاز ۴۱ — گیمیفیکیشن — ✅ تمام‌شده در ۱۴۰۵/۰۶/۰۶

امتیاز، جایزه، انقضا، سقف، مسابقه، سوءاستفاده و دفتر رویداد قابل تطبیق.

**نتیجهٔ محقق‌شده (اسکیمای v32):** گیمیفیکیشن روی یک دفتر کل افزودنی ساخته شد:
`ig_points_ledger` (ایدمپوتنت به‌تفکیک `(user, reason, reference)` با کلید یکتا)،
`ig_point_rewards` (فهرست جوایز) و `ig_reward_redemptions` (ایدمپوتنت به‌تفکیک کاربر+کلید).
**امتیاز و سقف:** `credit()` تکراری ردیف تازه نمی‌سازد؛ سقف روزانهٔ
`gamification.daily_cap` جلوی انباشت سوءاستفاده‌آمیز را می‌گیرد. **انقضا:** هر اعتبار می‌تواند
`expires_at` داشته باشد (`gamification.point_ttl_days`) و ردیف منقضی فقط از جمع می‌افتد.
**جایزه:** خرج‌کرد یک ردیف منفی بدون انقضا در همان دفتر است، پس یک `SUM` کل چرخه را تطبیق
می‌دهد؛ کمبود موجودی یا کلید خالی رد می‌شود. **مسابقه:** `race()` فقط امتیازهای مثبتِ بازه را
می‌شمارد — خرج‌کردن رتبه را کم نمی‌کند. **تطبیق:** `reconcile()` ردیف‌های منقضی، ترازهای منفی
(که هرگز نباید رخ دهند) و کلیدهای تکراری را گزارش می‌دهد. تست `GamificationTest` با ۵ سناریو.
تست ۲۲۹۱/۵۹ · لینت ۳۰۴/۰. جزئیات: `DESIGN-LEGAL-AUTH.md §۷.۷.۴۱`.
#### فاز ۴۲ — آموزش و ویدیوی امن — ✅ تمام‌شده در ۱۴۰۵/۰۶/۰۶

ثبت‌نام، پیشرفت، آزمون، گواهی، بازپرداخت، لینک امضاشده و انقضای دسترسی ویدیو.

**نتیجهٔ محقق‌شده (بدون تغییر اسکیمای ۳۲):** زیرساخت آموزش (ثبت‌نام با پنجرهٔ دسترسی، پیشرفت،
آزمون با سقف تلاش، گواهی، و ابطال دسترسی از سفارش مستردشده) از قبل وجود داشت؛ حلقهٔ گمشده
**انقضای دسترسی ویدیو در لحظهٔ تماشا** بود. لینک امضاشده فقط ثابت می‌کرد «لینک مال ماست و هنوز
منقضی نشده» — کاربری که دسترسی‌اش پس گرفته می‌شد تا پایان عمر لینک می‌توانست تماشا کند. اکنون
`verify_video_signature()` پس از راستی‌آزمایی امضا، `can_watch()` را هم می‌پرسد: درسِ پیش‌نمایش
آزاد برای همه باز است و هر درس دیگر ثبت‌نام زندهٔ همان دوره را می‌خواهد (که خود مهلت دسترسی و
حذفِ ابطالِ بازپرداخت را رعایت می‌کند). بازپرداخت ⇒ لینک معتبر همان لحظه می‌میرد. تست
`LmsVideoAccessTest` با ۵ سناریو. تست ۲۲۹۹/۶۰ · لینت ۳۰۵/۰. جزئیات:
`DESIGN-LEGAL-AUTH.md §۷.۷.۴۲`.
#### فاز ۴۳ — هستهٔ لجستیک — ✅ تمام‌شده در ۱۴۰۵/۰۶/۰۶

نقل‌قول، مرسوله، برچسب، بارکد، رهگیری، ماشین وضعیت تحویل و مجوز نقش‌ها.

**نتیجهٔ محقق‌شده (بدون تغییر اسکیمای ۳۲):** نقل‌قول مسیر (`categorize_route`)، مرسوله، گروه
برچسب با بارکد یکتا و پایش موقعیت از قبل موجود بودند؛ حلقهٔ گمشده **ماشین وضعیت تحویل** بود —
هر سرویسی هر وضعیتی را بدون پرسش می‌پذیرفت. اکنون جدول گذار `TRANSITIONS` قانون است: پیش‌نویس
فقط به پیک/حامل/شکست می‌رود، تحویل فقط از «رسیده به مقصد» قانونی است، و `delivered`/`failed`
پایانی‌اند؛ `can_transition()` استاتیک و خالص است و هر چهار نقطهٔ تغییر وضعیت
(`register_with_carrier`، `mark_delivered`، `assign/arrived/deliver` پیک) از آن عبور می‌کنند —
گذار غیرمجاز با لاگ رد می‌شود. **مجوز نقش‌ها:** صفحهٔ لجستیک با `Capabilities::MANAGE_SUITE`
محافظت می‌شود، سرویس پیک فقط مرسوله‌های خود پیک را نشان می‌دهد و تحویل بدون پین مشتری ممکن
نیست؛ عملیات مدیریت (برچسب/ثبت/واگذاری) از مسیر صفحهٔ مدیریتِ دارای مجوز می‌گذرد. تست
`LogisticsStateMachineTest` با ۴ سناریو + اصلاح تست قدیمی برای پیمایش ماشین. تست ۲۳۳۴/۶۱ ·
لینت ۳۰۶/۰. جزئیات: `DESIGN-LEGAL-AUTH.md §۷.۷.۴۳`.
#### فاز ۴۴ — پیک و پرداخت در محل — ✅ تمام‌شده در ۱۴۰۵/۰۶/۰۶

قرارداد ای‌پی‌آی پیک، مکان، ترتیب مسیر، مدرک تحویل و چرخهٔ مالی پرداخت در محل.

**نتیجهٔ محقق‌شده (اسکیمای v33):** قرارداد ای‌پی‌آی پیک (مرسوله‌ها، ترتیب مسیر، بارکد، پایش
موقعیت، گفتگو) از قبل موجود بود؛ دو حلقهٔ باقی‌مانده بسته شد. **مدرک تحویل:** ستون‌های جدید
`pod_ref` و `pod_at` در `ig_shipments` — هر تحویل مدرکش (مرجع عکس/امضا یا هر آنچه اپ پیک
ثبت کرده) را روی خود ردیف نگه می‌دارد تا اختلاف پرداخت در محل از همان ردیف پاسخ بگیرد.
**چرخهٔ مالی پرداخت در محل با انضباط ماشین:** تسویهٔ نقدی و پرداخت درون‌اپ فقط از وضعیتی
مجاز است که ماشین وضعیت تحویل اجازهٔ تحویل می‌دهد — تلاش برای تسویهٔ مرسولهٔ در راه با خطای
`bad_state` رد می‌شود و هیچ ردیف پرداختی نمی‌سازد؛ تسویهٔ موفق، ردیف `ig_cod_payments` با مبلغ
و وضعیت `paid` و مدرک تحویل ثبت می‌کند. تست `CourierCodTest` با ۴ سناریو. تست ۲۳۵۲/۶۲ · لینت
۳۰۷/۰. جزئیات: `DESIGN-LEGAL-AUTH.md §۷.۷.۴۴`.
#### فاز ۴۵ — آداپتورهای لجستیک — ✅ تمام‌شده در ۱۴۰۵/۰۶/۰۶

قرارداد مشترک پرووایدر، نگاشت وضعیت، کال‌بک، تلاش مجدد و تطبیق؛ آزمون زندهٔ هر پرووایدر در فاز
`PV` جداست.

**نتیجهٔ محقق‌شده (بدون تغییر اسکیمای ۳۳):** قرارداد مشترک (`register`/`track`) و آداپتر
تنظیم‌محور از قبل موجود بودند؛ لایهٔ جدید `ShippingSyncService` سه حلقه را بست. **نگاشت
وضعیت:** واژگان متفاوت پرووایدرها با `STATUS_MAP` (و آموزش فروشگاه از طریق
`logistics.status_map_<raw>`) به وضعیت‌های ماشین می‌روند؛ کلمهٔ ناشناخته هیچ چیز را تغییر
نمی‌دهد. **کال‌بک امضاشده:** کال‌بک حامل با HMAC-SHA256 روی بدنهٔ خام و
`logistics.callback_secret` راستی‌آزمایی می‌شود؛ امضای نامعتبر هرگز دریافت‌شده حساب نمی‌شود.
**تلاش مجدد و تطبیق:** پیمایش `sync_tracking()` خودش مکانیزم تلاش مجدد است — خطای گذرای ادپتر
شمرده می‌شود و پیمایش بعدی دوباره می‌پرسد. **پیمایش ماشین:** هدف وضعیت از کوتاه‌ترین مسیر
قانونی (`path()` با جست‌وجوی سطح‌اول روی جدول گذار) به دست می‌آید — پرووایدری که یک‌سره
«تحویل شد» بفرستد هم از پلهٔ «رسیده به مقصد» می‌گذرد؛ وضعیت پایانی هرگز عقب نمی‌رود. تست
`ShippingSyncTest` با ۴ سناریو. تست ۲۳۷۶/۶۳ · لینت ۳۰۹/۰. جزئیات:
`DESIGN-LEGAL-AUTH.md §۷.۷.۴۵`.
#### فاز ۴۶ — مارکت‌پلیس‌ها — ✅ تمام‌شده در ۱۴۰۵/۰۶/۰۶

نگاشت، انتشار ایدمپوتنت، تضاد، محدودیت نرخ و تعیین وضعیت رسمی پرووایدرها (اسکیمای ۳۴).

**نتیجهٔ محقق‌شده:** نگاشت محصول/دسته/قیمت/موجودی از قبل موجود بود؛ این فاز حلقه‌های باقی را
بست. **انتشار ایدمپوتنت:** هر بارگذاری هش کانونیکال می‌گیرد (`payload_hash`) و محصولی که همین
هش را قبلاً منتشر کرده هرگز دوباره پوش نمی‌شود؛ آداپترها `external_id` ما را همراه بار می‌فرستند
و `remote_id`/`remote_rev` در دفترچهٔ لینک‌ها ثبت می‌شود. **تضاد:** پاسخ ۴۰۹ یعنی کسی لیستینگ
را از پشت صحنه تغییر داده — سطر متوقف و `conflict` علامت می‌خورد، هرگز بی‌صدا بازنویسی نمی‌شود.
**محدودیت نرخ و تلاش مجدد:** ۴۲۹ با `Retry-After` (یا بک‌آف) به `not_before` می‌رود و تلاش
نمی‌سوزاند؛ خرابی ۵۰۰/شبکه بک‌آف نمایی می‌گیرد و پس از سقف `marketplace.sync_retries` شکست
می‌خورد؛ هر مارکت‌پلیس در هر تیک حداکثر `marketplace.<id>_per_tick` پوش می‌گیرد. **تعیین
رسمیت (یافتهٔ تحقیق، نه حدس):** باسلام پلتفرم توسعه‌دهندهٔ رسمی دارد (developers.basalam.com،
Client ID/Secret یا PAT و اسکوپ)؛ دیجی‌کالا و دیوار هیچ درگاه رسمی عمومی برای ثبت محصول ندارند —
کانال‌های آن‌ها فقط از مسیر آداپتر تنظیم‌محور با واسطهٔ مورد اعتماد کار می‌کنند و اسکرپ در کار
نیست. تست `MarketplaceSyncTest` با ۷ سناریو. تست ۲۴۰۱/۶۴ · لینت ۳۱۰/۰. جزئیات:
`DESIGN-LEGAL-AUTH.md §۷.۷.۴۶`.
#### فاز ۴۷ — سئو و تبلیغات — ✅ تمام‌شده در ۱۴۰۵/۰۶/۰۶

تگ‌های هد، دادهٔ ساختاریافته، نقشهٔ سایت، تأیید کمپین، کنترل هزینه و سد محتوای کم‌ارزش
انبوه (اسکیمای ۳۵: `ig_ad_campaigns` و `ig_seo_activity`، جمعاً ۸۱ جدول).

**نتیجهٔ محقق‌شده:** **دادهٔ ساختاریافته** فقط با قاعدهٔ «برابری محتوا»: نام/تصویر/توضیح
واقعی محصول و `Offer` با قیمت رشتهٔ عددی ساده، ارز `IRR` و دسترس‌پذیری با آدرس کامل
schema.org؛ بدون امتیاز ساختگی، و قیمت صفر هرگز ژست فهرست فروشگاهی نمی‌گیرد. **تگ هد:** یک
کانونیکال واقعی برای هر محصول؛ صفحهٔ غیرقابل‌فروش `noindex` می‌شود به‌جای تظاهر. **نقشهٔ
سایت:** کران‌دار (۱۰۰۰) و فقط محصولات قابل‌فروش. **کمپین‌ها:** چرخهٔ تأیید
(`pending_approval` ← `approved`/`rejected`، یک‌طرفه)؛ خرج فقط پس از تأیید و فقط تا سقف
بودجه (`over_budget` قبل از تماس با شبکه ثبت می‌شود)؛ خرج فقط بعد از تأیید پرووایدر در
`spent_irt` می‌نشیند. **سد محتوای انبوه:** شمارندهٔ روزانهٔ راستین (`ig_seo_activity`) با سقف
`seo.daily_content_cap`. تست `SeoAdsTest` با ۶ سناریو. تست ۲۴۴۹/۶۵ · لینت ۳۱۷/۰. جزئیات:
`DESIGN-LEGAL-AUTH.md §۷.۷.۴۷`.
#### فاز ۴۸ — ترجمهٔ دامنه‌ای و تجارت بین‌المللی — ✅ تمام‌شده در ۱۴۰۵/۰۶/۰۶

حافظهٔ ترجمهٔ tenant-scoped، واژه‌نامهٔ قفل‌شده، ارز/منطقهٔ زمانی، رضایت و هستهٔ درگاه‌های
بین‌المللی (اسکیمای ۳۶: `ig_translation_memory`، `ig_glossary_terms` و `ig_intl_consents`،
جمعاً ۸۴ جدول).

**نتیجهٔ محقق‌شده:** **حافظهٔ ترجمه:** تطبیق دقیق به تفکیک مستأجر/زبان/هش — برخورد حافظه هرگز
به پرووایدر نمی‌رود و هر پاسخ تازهٔ پرووایدر حافظهٔ تکرار بعدی می‌شود. **واژه‌نامه:** اصطلاحات
قفل‌شده پشت جای‌دار می‌روند و با ترجمهٔ مصوب برمی‌گردند؛ هیچ جای‌داری به صفحه نشت نمی‌کند.
**ارز/منطقهٔ زمانی:** فقط کدهای ایزو ۴۲۱۷ و منطقه‌های IANA واقعی پذیرفته می‌شوند؛ مقدار ساختگی
به پیش‌فرض برمی‌گردد. **رضایت:** دفترچهٔ تاریخ‌دار — سکوت رضایت نیست، لغو هم تاریخ می‌خورد و
پردازش برون‌مرزی بدون رضایت فعال ممکن نیست. **هستهٔ درگاه‌ها:** ثبت‌کنندهٔ قابلیت تنظیم‌محور
(مانند آداپترهای لجستیک)؛ هیچ پرووایدر واقعی اختراع نشده و تأیید هر کدام به فازهای
`PV-INTL-*` موکول است. تست `IntlCommerceTest` با ۶ سناریو. تست ۲۴۸۸/۶۶ · لینت ۳۲۲/۰.
جزئیات: `DESIGN-LEGAL-AUTH.md §۷.۷.۴۸`.
#### فاز ۴۹ — اتصال Zernio و profile mapping — ✅ تمام‌شده در ۱۴۰۵/۰۶/۰۶

اتصال رسمی حساب، پروفایل جدا، کلید مرکزی در secret store، revoke/rotation، هویت وب‌هوک،
دامنهٔ مستأجر و حذف داده (اسکیمای ۳۷: `ig_zernio_profiles`، جمعاً ۸۵ جدول).

**نتیجهٔ محقق‌شده (طبق ADR-0004 §۵):** **یک پروفایل برای هر فروشگاه:** جدول با کلید یکتای
مستأجر؛ تلاش دوم `already_provisioned` می‌گیرد. **کلید مرکزی:** در `Settings::SECRETS` ثبت و
رمزنگاری‌شده نگهداری می‌شود (فیلد واقعی در تب جدید «Zernio» فرم تنظیمات)؛ فروشگاه فقط کلید
profile-scoped رمزنگاری‌شده دریافت می‌کند و کلید مرکزی هرگز به بافت فروشگاه نمی‌رود.
**چرخهٔ کلید:** چرخش نسخه را بالا می‌برد و کلید پیشین را می‌اندازد؛ لغو، کلید را پاک و مهر
زمان‌دار می‌زند؛ اتصال مجددِ فروشگاه لغوشده نسخه را هرگز به عقب برنمی‌گرداند. **نگاشت:**
زنجیرهٔ مستأجر↔پروفایل↔حساب↔اینستاگرام در بک‌اند است و `resolve()` ادعای کلاینت خام را با
نگاشت می‌سنجد — پروفایل به‌تنهایی مجوز نیست. **هویت وب‌هوک:** HMAC روی payload+timestamp با
راز اختصاصی پروفایل و پنجرهٔ ۳۰۰ ثانیه‌ای ضدبازپخش. **حذف داده:** سطر مستأجر کامل پاک می‌شود و
در فهرست `TenantOffboarding` نیز ثبت شد. آداپتر واقعی پشت قرارداد `ZernioAdapterInterface`
است و تأیید زندهٔ اندپوینت‌ها به فاز `PV-ZERNIO-*` موکول است. تست `ZernioConnectTest` با ۵
سناریو. تست ۲۵۲۹/۶۷ · لینت ۳۲۶/۰. جزئیات: `DESIGN-LEGAL-AUTH.md §۷.۷.۴۹`.
#### فاز ۵۰ — مهاجرت به تنها provider اجتماعی — ✅ تمام‌شده در ۱۴۰۶/۰۶/۰۹

ساخت adapter رسمی Zernio و migration کنترل‌شدهٔ داده؛ حذف Manus، ChatPlace، ManyChat و
fallbackهای اجتماعی از container، تنظیمات، endpoint، UI و Runbook؛ افزودن گارد معماری و
آزمون برای جلوگیری از ورود کانال Instagram مبتنی بر session در Agent Reach.

**نتیجهٔ محقق‌شده:** `ZernioClient` (همهٔ مسیرها از طریق تنظیمات `zernio.*` قابل جابجایی
هستند؛ دو صفحهٔ کلید — profile با کلید مرکزی، social با کلید scoped خودش + `Idempotency-Key`)
پشت `ZernioAdapterInterface` قرار گرفت و `ZernioSocialService` روی قرارداد
`SocialProvider` (تنها پیاده‌سازی مجاز: `zernio`، گارد `SocialProviders` کلیدهای ممنوعه را
`social_provider_not_allowed` رد می‌کند) resolve می‌شود. `SocialMigrationService` هر ساعت
(`ig.social.migrate`، حد ۲۰) دو مرحلهٔ روزن‌ماننده را جلو می‌برد — ensure profile با کلید
مرکزی، سپس stamping `legacy_deprecated_at` بر حساب‌های قدیمی؛ تکرار مرحلهٔ تمام‌شده
`already_done` می‌گیرد و مستأجر به‌محض تمام‌شدن هر دو مرحله از فهرست due خارج می‌شود
(نیمه‌کاره‌ها — حتی با کلید مرکزی خالی — در فهرست می‌مانند تا round بعد). UI صفحهٔ حساب
دکمهٔ «مهاجرت حساب‌های قدیمی» + فهرست روزن نشان می‌دهد و Runbook مرحلهٔ v38 را دارد. حذف:
۲۴ فایل کلاینت/سرویس قدیمی، ۶ فایل تست، کلیدهای secret و تنظیمات و مسیرهای وب‌هوک قدیمی
و دکمه‌های UI و سطرهای Runbook. گارد معماری (`SocialArchitectureGuardTest`) شناسه‌های
معماریِ حذف‌شده — نام‌های کلاس، bindingهای container، پیشوند کلیدهای secret، نشانه‌های
کانال session — را در کل `src/` نگه می‌دارد تا بازگشت هر بخشِ قدیمی تست را می‌شکند.
تست ۱۳۰۷۳/۶۴ · لینت ۲۹۶/۰. جزئیات: `DESIGN-LEGAL-AUTH.md §۷.۷.۵۰`.

#### فاز ۵۱ — Inbox و comment-to-DM با Zernio — ✅ تمام‌شده در ۱۴۰۶/۰۶/۰۹

webhook امضاشده، deduplication، rule بک‌اند، opt-out، rate limit، approval، delivery state،
idempotency و audit؛ هیچ تصمیم تجاری به automation بیرونی واگذار نمی‌شود.

**نتیجهٔ محقق‌شده:** اینباکس (`POST /igbz/v1/zernio/inbox`) خوداحرازش با HMAC
رویداد — رویداد روی account بیگانه قبل از هر ذخیره‌ای رد می‌شود و امضا با راز
خودِ پروفایل و در پنجرهٔ ۳۰۰ ثانیه‌ای چک می‌شود. خط لولهٔ تصمیم کاملاً در بک‌اند
(`InboxService`): capture با dedupe روی `(profile, event)` ← opt-out (ماندگار و
خودتشخیص از پیام کاربر) ← rule بک‌اند با priority ← rate limit ساعتی (per-sender
و per-tenant) ← approval انسانی (پیش‌فرض خاموش) ← delivery با کلید idempotency
لنگر‌داده‌شده به رویداد (`inbox:<event_id>`) — ارسال با کلید scoped فروشگاه و
بدون کلید مرکزی. شکست‌ها در دفترچۀ `ig_inbox_actions` با همان کلید retry می‌شوند.
سطح ادمین (`/igbz/v1/ig/inbox*`) رویدادها، دفترچۀ delivery و approve/reject/retry،
optout و قواعد را به صاحب فروشگاه می‌دهد (scoped). اسکیمای ۳۹ با ۹۰ جدول.
آزمون زندهٔ ارسال (پنجرهٔ ۲۴ ساعتی اینستاگرام و شکل‌های واقعی پاسخ) با
`PV-ZERNIO-*`؛ تا آن زمان پیش‌فرض approval انسانی است و چیزی بدون تأیید بیرون
نمی‌رود. تست ۱۳۲۵۳/۶۵ · لینت صفر خطا. جزئیات: `DESIGN-LEGAL-AUTH.md §۷.۷.۵۱`.

#### فاز ۵۲ — ثبت محصول ۱۳مرحله‌ای — ✅ تمام‌شده در ۱۴۰۶/۰۶/۰۹

checkpoint، resume، idempotency، validation، تأیید انسانی، محصول ووکامرس و شکست/جبران.

**نتیجهٔ محقق‌شده:** ماشین حالت سطر-محور با ۱۳ checkpoint روی جدول
`ig_product_registrations` (اسکیمای ۴۰، ۹۱ جدول): هر فراخوانی REST یا webhook فقط
یک checkpoint جلو می‌رود، هر گام `failed` می‌شود و با `retry` از همان
`failed_from` ادامه می‌یابد. idempotency دو لایه: شروع روی `(tenant, client_token)`
و ساخت محصول روی `product_id` ذخیره‌شده (کرش‌محور، تست‌شده). درز agent صادقانه:
مراحل هوش مصنوعی فقط از `IntakeAgentInterface` و بدون agent با
`agent_not_configured` رد می‌شوند — هیچ نتیجهٔ ساختگی‌ای ثبت نمی‌شود و مسیرهای
`manual_*` جبران انسانی‌اند (اتصال agent واقعی با ۵۳/۵۵). مرحلۀ تجاری
(`WooCommerceDraftFactory`) **فقط draft** می‌سازد با metaهای
`_igbz_registration_id`/`_igbz_public_code` (کد عمومی = id محصول). `approve` فقط
از `awaiting_approval` و با ثبت `approved_by/at` یک سطر **draft** از `ig_content`
(provider `zernio`) می‌سازد — ورودیِ publisherِ فاز ۵۳؛ هیچ چیز با approve
منتشر نمی‌شود. `compensate` محصول draft را حذف می‌کند (غیر-draft هرگز لمس
نمی‌شود؛ در صورتِ باقی‌ماندن مرجع روی سطر می‌ماند). ۲۴ مسیر REST scoped زیر
`/igbz/v1/ig/product-registrations`. تست ۱۳۵۱۲/۶۶ · لینت ۳۰۳/۰ · DriftGuard ۹۱/۴۰.
جزئیات: `DESIGN-LEGAL-AUTH.md §۷.۷.۵۲`.

#### فاز ۵۳ — انتشار، صدا و راستی‌آزمایی Zernio — ✅ تمام‌شده در ۱۴۰۶/۰۶/۰۹

انتخاب زمان، دریافت صدای کاتالوگ‌شده، job انتشار، نتیجهٔ واقعی webhook/polling، duplicate
prevention، failure state و گزارش؛ عدم دسترسی به audio گیت production است.

**نتیجهٔ محقق‌شده:** `ContentPublishService` (بایند `ig.content_publish`) ماشینِ
انتشار را روی خودِ `ig_content` پیاده کرد: `draft ← scheduled ← publishing ←
published` + `failed`. انتخاب زمان = `schedule` (۶۰ ثانیه تا ۹۰ روز)؛ اجرای
لحظه‌ای = `publish_now`. duplicate prevention ریشه‌ای با کلید idempotency ثابتِ
سطر `content:<id>` روی هر `POST /posts` (دستی/sweep/retry) — یک سطر هرگز دو
پست نمی‌سازد؛ sweep فقط `scheduled` بدون `provider_task_id` را اجرا می‌کند.
retry بدون کور بودن: اول reconcile با `GET /posts/{id}`، سپس `POST /posts/{id}/
retry` provider، در صورتِ رد re-publish با همان کلید؛ سقف ۳. نتیجهٔ واقعی از دو
در (webhook خوداحرازی `POST /zernio/posts` با همان مدلِ امزای فاز ۵۱ + pollingِ
احتیاطی هر ۵ دقیقه) به یک قیف (`apply_provider_state`) ختم می‌شود که فقط
پیشرویِ رو به جلو می‌پذیرد؛ `partial`/`cancelled` به `failed` با دلیلِ نام‌دار.
ledgerِ جدید `ig_publish_events` (اسکیمای v41، جدول ۹۲) با
`UNIQUE(profile_id,event_id)` = dedupe + گزارش. صدا گیت است نه تزئین:
`igbz.publisher_audio` (پیش‌فرض خاموش) فقط وقتی `voice_url` واقعی روی
registration هست mp3 را به `media[]` اضافه می‌کند — هیچ صدای ساختگی. ۷ مسیر REST
scoped + ۱ webhook. تست **۱۳۷۲۲/۶۷** · لینت ۳۰۶/۰ · DriftGuard ۹۲/۴۱.
جزئیات: `DESIGN-LEGAL-AUTH.md §۷.۷.۵۳` و `PROJECT-STATE.md §۱۱.۹`.

#### فاز ۵۴ — کانال VIP — ✅ تمام‌شده در ۱۴۰۶/۰۶/۰۹

پلن، عضویت، خرید تکی، entitlement، save، like، comment، message، expiry و حذف امن فایل.

**نتیجهٔ محقق‌شده:** سخت‌سازی سرتاسری کانال با الگوی فازهای ۲۳–۵۳. **پول:**
ایجازِ ساخت سطر پرداخت به `user_id` گره خورد (باگ واقعی: دو خریدارِ همان قیمت می‌توانستند
یک سطر پرداخت مشترک بگیرند) + `dedupe_key` جدید روی ستون `payments.idempotency_key`
(یونیک tenant+purpose+key) — دابل‌تپِ عضویت/خرید تکی همان سطر را بازیابی می‌کند؛ سطر
pending عضویت بازیابی می‌شود و طرح رایگانِ زنده دورهٔ مجدد نمی‌سازد. **فلیپ‌های شرطی:**
فعال‌سازی عضویت (pending→active، یک هوک)، انقضای عضویت (تمدیدِ لحظهٔ جاروب منقضی
نمی‌شود)، publish (ماشین وضعیت — منقضی/حذف‌شده زنده نمی‌شود) و expire_due. **Entitlement:**
grantِ بازندهٔ مسابقهٔ UNIQUE سطر را زنده می‌کند — پول پرداخته هرگز بی‌entitlement
نمی‌ماند. **حذف امن فایل:** shred (رونویسی با بایت تصادفی) پیش از unlink؛ media JSON =
دفترچهٔ purge که تا `purge_complete` (راستی‌آزمایی file-system) پاک نمی‌شود؛ ستون
`media_purged_at` مهرِ تکمیل؛ job روزانهٔ `ig.vip.reconcile` ناتمام‌ها را بازتلاش و
شمارنده‌ها را تطبیق می‌دهد. **رفع باگ فاز ۵۳:** نقطهٔ اضافه در انتهای CREATE جدول
`ig_publish_events` ساختش را روی هر دیتابیسی شکست می‌داد (نصب تازه ۹۱/۹۲) — حذف شد؛
الان ۹۲/۹۲. اسکیمای v42 (۹۲ جدول). تست `VipChannelTest` با ۱۰ سناریو: **۱۳۷۰۴/۶۸** ·
لینت ۳۰۶/۰ · DriftGuard ۹۲/۴۲ · دود زندهٔ ۱۳گامی سبز + تست ویژوال کرومیوم.
جزئیات: `DESIGN-LEGAL-AUTH.md §۷.۷.۵۴` و `PROJECT-STATE.md §۱۱.۱۰`.

#### فاز ۵۵ — قرعه‌کشی، insights و ورودی رقبا — ✅ تمام‌شده در ۱۴۰۶/۰۶/۰۹

انتخاب قابل ممیزی، مقاومت به تقلب، provenance آمار، retention، ingestion رسمی Insight و
ورودی دستی رقبا. Business Discovery و Hashtag Search تا endpoint رسمی Zernio backlog هستند.

**نتیجهٔ محقق‌شده:** **قرعه‌کشی** با الگوی commit–reveal (استاندارد صنعت قرعه‌کشی‌های
قابل‌ممیزی، تحقیق فاز): هنگام ساخت، seed تصادفی (ماژول کریپتو) تولید و فقط تعهد SHA-256
آن منتشر می‌شود؛ seed رمزنگاری‌شده در DB می‌ماند؛ هنگام قرعه، استخر ورودی‌ها فریز و هش
می‌شود، seed فاش می‌شود و شمارهٔ برنده با تابع مستند بازتولیدپذیر (HMAC-SHA256(seed,
pool_hash) + پنجره‌های ۵۲بیتی + نمونه‌گیری پس‌زدن) مشتق می‌شود؛ بستهٔ ممیزی کامل (seed،
تعهد، هش استخر، تعداد، شمارهٔ برنده، متن الگوریتم) روی ردیف می‌ماند و مسیر
`POST …/audit/verify` آن را از استخر فریزشده بازمشتق می‌کند. **مقاومت به تقلب:** یونیک
یک-نفر-به-ازای-قرعه‌کشی (کلید UNIQUE + بازبازیابی)، پنجرهٔ ورود در بک‌اند، ورود بعد از
بسته‌شدن ممنوع، قرعه‌کشی دوم صادقانه `already_drawn` (فلیپ شرطی)، کانسلیفقط در open.
**Insights:** ستون‌های provenance (source=manual/zernio + provider_ref)، ingestion رسمی از
analytics پروفایل متصل از طریق همان لولهٔ Zernio (فروشگاه نا متصل = `not_connected`
صادقانه، هرگز عددی ساخته نمی‌شود)، اصلاح روزانهٔ همان کلید، retention روزانه
(`ig.insights_retention_days`، پیش‌فرض ۷۳۰، کف ۹۰) با job `ig.insights.prune`.
**رقبا:** جدول‌های `ig_competitors` + `ig_competitor_snapshots` — هندل عمومی مدیر،
اسنپ‌شات زمان‌دار روزانه با لینک evidence و یادداشت؛ سنجه‌های غیرقابل‌دانست (reach/save/
فروش رقیب) اصلاً فیلد نیستند؛ دادهٔ عمومی هرگز با insight حساب متصل قاطی نمی‌شود
(جدول/سرویس جدا). اسکیمای v43 (۹۵ جدول) + backfill `ig_publish_events` برای سایت‌های
مستقرِ گرفتار باگ فاز ۵۴. تست `GrowthIntelTest` (۱۲ سناریو) + سناریوی CronJobsTest:
**۱۳۹۸۴/۶۹** · لینت ۳۱۱/۰ · DriftGuard ۹۵/۴۳ · دود زندهٔ ۷گامی + REST زنده ۲۰۰ +
تست ویژوال کرومیوم. جزئیات: `PROJECT-STATE.md §۱۱.۱۱` و `DESIGN-LEGAL-AUTH.md §۷.۷.۵۵`.

### ۲۳.۱۰ موج هفت — پادو

#### فاز ۵۶ — adapter مستقل DeepInfra، schema و ابزار — ✅ تمام‌شده در ۱۴۰۶/۰۶/۰۹

قرارداد نسخه‌دار `AiProviderInterface`/`DeepInfraAdapter`، اجرای درخواست از حساب مستقل
فروشگاه بدون ذخیرهٔ کلید در IGBZ، تفکیک داده از دستور، allowlist ابزار، بودجه، timeout،
ثبت هزینه و ممنوعیت اجرای کد تولیدی. فعال‌سازی مشروط به benchmark فارسی و صلاحیت جغرافیایی.

**نتیجهٔ محقق‌شده:** قرارداد نسخه‌دار (`CONTRACT_VERSION=1`) + `AiRequest`/`AiResult`
(فلگ `executed` همیشه false) + `AiToolbox` (allowlist چهار ابزارِ فقط‌خواندنی/درافتیِ v1 با
JSON Schema و اعتبارسنج آرگومان در بک‌اند) + `DeepInfraAdapter` روی endpoint رسمی
سازگار با OpenAI (تحقیق فاز: `api.deepinfra.com/v1/openai/chat/completions`، پاسخ
`usage.estimated_cost` دارد). **کلید فقط زمان اجرا:** از حساب مستقل فروشگاه در درخواست
می‌آید، دقیقاً یک فراخوانی HTTPS، هرگز در گزینه/لاگ/دفتر ذخیره نمی‌شود (تست‌شده).
**سه گیت فعال‌سازی** (فعال‌سازی مدیر + قبولی benchmark فارسی + صلاحیت جغرافیایی) همگی
پیش‌فرض خاموش؛ بدون هر سه = `provider_disabled` صادقانه. **تفکیک داده/دستور:** system فقط
از Playbook؛ پیام داده فقط user/assistant — قاچاق نقش system پیش از هر ترافیک رد می‌شود.
**ابزار:** فقط نام‌های مجازِ Playbook ∩ allowlist به مدل عرضه می‌شود؛ tool call برگشتی با
نامِ خارج از allowlist یا آرگومانِ خارج از schema حذف می‌شود. **بودجه:** سقف
`max_tokens` (≤۴۰۹۶) + سقف توکن روزانهٔ tenant (`pado.deepinfra.daily_token_budget`) از
دفتر `ig_ai_credit_ledger` (ردیف‌های `ai_usage` با delta=0 و meta مدل/توکن/هزینه؛ dedupe
روی reference)؛ timeout مشبک ۵–۱۲۰. endpoint فقط HTTPS؛ لیست مدل پین‌شده (پیش‌فرض سه
مدل ADR-0004 §4، قابل تصویب عملیاتی). تست `DeepInfraAdapterTest` با ۱۳ سناریو: مجموعه
**۱۴۲۷۰/۷۰** · لینت ۳۱۷/۰ · دود زندهٔ ۷گامی + تست ویژوال کرومیوم
(`visual-testing/phase-56/`). فعال‌سازی واقعی منتظر تأمین حساب DeepInfra و تأیید
benchmark از کارفرما است (بک‌لاگ ثبت‌شده).

#### فاز ۵۷ — صف مجوز اتمیک — ✅ تمام‌شده در ۱۴۰۶/۰۶/۰۹

claim، نسخه/hash payload، expiry، capability، تصمیم‌گیر، idempotency، cancellation و audit.

**نتیجهٔ محقق‌شده:** جدول `igbz_approval_requests` (v44، بدون جدول جدید) به صف مجوز
اتمیک ارتقا یافت — ستون‌های payload_version/payload_hash (هش کانونیِ مرتب‌سازی‌شده؛
ترتیب کلیدها هرگز هش را عوض نمی‌کند)، idempotency_key + UNIQUE (tenant,kind,key)،
capability (تصمیم‌گیر باید اثباتش را پاس کند)، expires_at، claimed_by/claimed_at/
attempts/execution_error و audit (JSONL الحاقی). **هر گذار یک UPDATE شرطی است:**
تصمیم pending→(approved/rejected/cancelled) — بازندهٔ مسابقه false می‌گیرد؛ claim
approved→claimed فقط برای worker مثبت و فقط یکی؛ complete claimed→executed/failed فقط
به‌دست خودِ claimer با نگهداشت دلیل خطا؛ انقضای pending→expired با جاروب روزانه
(idempotent). مسیر executor قدیمی هم از دل claim می‌گذرد (یک‌بار‌اجرا حتی در legacy).
cancel فقط توسط خودِ درخواست‌گر در حالت pending. `verify_payload_integrity` هش مهرشده
را بازمحاسبه می‌کند (ردیف دستکاری‌شده فریاد می‌زند) و `audit_trail` کل زندگی ردیف را
با بازیگران برمی‌گرداند. تست `PermissionQueueTest` با ۱۲ سناریو: مجموعه **۱۴۳۲۵/۷۱** ·
لینت ۳۱۸/۰ · دود زندهٔ ۶گامی (شامل مهاجرت واقعی v44 روی جدول مستقر) + تست ویژوال
کرومیوم تب مجوزها (`visual-testing/phase-57/`).

#### فاز ۵۸ — عملیات حساس تجاری — ✅ تمام‌شده در ۱۴۰۶/۰۶/۰۹

اتصال واقعی قیمت، refund و حذف انبوه به صف مجوز و اجرای قابل جبران.

**نتیجهٔ محقق‌شده:** سرویس `SensitiveOperationsService` (بایند `pado.ops`) سه نوع حساس
را به صف فاز ۵۷ می‌بَرد و اجرای قابل‌جبران می‌کند: **تغییر قیمت** (`price_change`، سطح
متوسط؛ old_price در payload؛ نوشتن regular+فعال، راستی‌آزمایی بازخوانیِ قیمتِ «معمول»،
جبران به قیمتِ ثبت‌شده هنگام نچسبیدن نوشتار، تلاش دوبارهٔ فعال‌سازی قیمت فعال)،
**بازپرداخت** (`payment_refund`، سطح بالا؛ واگذاری کامل به نگهبان‌های
`PaymentService::refund_payment` — پرداختِ ادانشده/مبالغ نامعتبر هیچ حرکتی نمی‌کنند؛
کلید idempotency PSP می‌رود روی درخواست) و **حذف انبوه** (`bulk_product_delete`، سطح
بحرانی؛ فقط زباله‌دان نه نابودی، سقف ۵۰۰، بازیابیِ عناصر قبلی هنگام شکست میان‌دسته).
`run()` پیش از هر اجرا هش payload را بازمحاسبه می‌کند (ردیف دستکاری‌شده هرگز اجرا
نمی‌شود)؛ تصمیم‌گیر باید `manage_tenant` را اثبات کرده باشد. PadoPage در `handle_decide`
انواع حساس را به `pado.ops->run()` می‌سپارد. **یافته‌های دود زنده (اصلاح‌شده):** قیمتِ
مالکِ عمل قیمتِ «معمول» است — ووکامرنس روی محصولِ دارای حراج، قیمت فعال را از حراج
می‌سازد و بازخوانی `get_price` اجرای صادقانه را فریب می‌داد؛ یک‌بار هم sync قیمت فعال
جا ماند و سرویس اکنون یک‌بار فعال‌سازی را دوباره می‌کند و در ناکامی جبران می‌زند.
تست `SensitiveOpsTest` با ۱۰ سناریو (اجرای پس از تأیید + یک‌بارمصرف، تکرار=یک ردیف،
جبران نوشتار دروغگو، payload دستکاری‌شده، حراج، تأخیر sync فعال، رد بازپرداختِ
ادانشده، زباله‌دان نه نابودی، بازیابی شکست میان‌دسته، سقف/اعتبارسنجی): مجموعه
**۱۴۴۲۲/۷۲** · لینت ۵۰۰۴/۰ · دود زندهٔ ۱۹گامی همه‌سبز + تست ویژوال کرومیوم تب مجوزها
(`visual-testing/phase-58/`). تحقیق مبنا: الگوی جبران ساگا، قرارداد «اول زباله‌دان»
اکوسیستم ووکامرنس و بازپرداختِ صف‌شده با تأیید دستی (`DESIGN-LEGAL-AUTH §۷.۷.۵۸`).

#### فاز ۵۹ — انتشار، کمپین و سیاست — ✅ تمام‌شده در ۱۴۰۶/۰۶/۰۹

اتصال انواع انتشار، کمپین و تغییر سیاست به صف، سقف اختیار و نتیجهٔ قابل اثبات.

**نتیجهٔ محقق‌شده:** سرویس `ContentOperationsService` (بایند `pado.content_ops`) با
**سقف اختیار** مطابق ADR-0004 §7 و ادبیات HITL: سه دستهٔ محتوایی
(وایرال/رشد، تخصصی/اعتماد، شخصی/وفاداری) با یک تأیید دسته‌ای حداکثر ۵۰ ردیف
(سطح متوسط)؛ پست فروش/کمپین فقط تک‌ردیفی با دلیل الزامی (سطح بالا)؛ ارسال کمپین
مشتری‌محور با سقف ۲۰۰ گیرنده (سطح بالا) که فهرست گیرندگان در payload نمی‌نشیند
(صف «قصد» را نگه می‌دارد نه «دفتر عضویت» را)؛ تغییر سیاست فقط از لیست بستهٔ
کلیدهای AI پادو با نوع هر کلید (سطح بحرانی). اجراها «نتیجهٔ قابل اثبات» دارند:
`record_outcome` شمارش/خطا/از-به را در metadata ردیف می‌نویسد؛ انتشار در اولین رد
می‌ایستد (آنچه رفته برگشت‌ناپذیر است — صادقانه در داک) و پیامِ رسیده قابل حذف
نیست ولی ادامهٔ ارسال متوقف می‌شود؛ تغییر سیاست بازخوانی و در نچسبیدن، به مقدار
قدیمی جبران می‌شود. `run()` پیش از اجرا هش payload را بازمحاسبه می‌کند؛ گیت
`manage_tenant`. PadoPage شش kind جدید را به `pado.content_ops->run()` می‌سپارد.
تست `ContentOpsTest` با ۱۲ سناریو: مجموعه **۱۴۵۲۷/۷۳** · لینت ۵۰۰۴/۰ · دود زندهٔ
۱۶گامی همه‌سبز + تست ویژوال کرومیوم (`visual-testing/phase-59/`). تحقیق مبنا:
سطح‌بندی تأیید بر اساس برگشت‌ناپذیری/بردِ پرتو (تأیید دسته‌ای فقط برای
برگشت‌پذیرها ۱۰–۵۰تایی، یک‌به‌یک برای پول/تعهد عمومی) و «اعمال فقط همان payloadِ
تصویب‌شده با ثبت آنچه واقعاً اجرا شد» (`DESIGN-LEGAL-AUTH §۷.۷.۵۹`).

#### فاز ۶۰ — اسکیل قالب و اعتبارسنج — ✅ تمام‌شده در ۱۴۰۶/۰۶/۰۹

تکمیل اسکیل `igbz-store-theme`، قرارداد FSE مصوب، مراجع، fixture طلایی و اعتبارسنجی فاقد PHP.

**نتیجهٔ محقق‌شده:** **اسکلت مرجع** `igbz-suite/themes/igbz-store-theme/` — قالب
فرزند بلوکیِ twentytwentyfive، کاملاً فاقد PHP/JS/شبکه/CDN: هدر قرارداد در
`style.css`، `theme.json` نسخهٔ ۳ (پالت فارسی، تایپوگرافی سیال، وزیرمتن از پشتهٔ
سیستم)، شش قالب (index/page/single/404 + قرارداد ووکامرس: archive-product/
single-product با بلوک‌های هسته)، دو بخش سربرگ/پابرگ ثبت‌شده در templateParts، و
`rtl.css`؛ **مراجع** قرارداد در `REFERENCE.md`. **قرارداد FSE مصوب** به‌صورت کلاس
بک‌اند `ThemeContract`: لیست قالب مادر مجاز، نسخه‌های اسکیما، قالب‌ها/بخش‌های
اجباری، پسوندهای ممنوع (php/js/phar/…) و ممنوعیت نشانی شبکه‌ای (تنها استثنا
`$schema` اسکیمای wp.org)؛ `validate_php_free()` همان قرارداد را بایت‌به‌بایت پاس
می‌کند و **فیچر طلایی همان اسکلتِ shippy است** (`ThemeContract::golden_dir()`) —
تست و محصول هرگز از هم جدا نمی‌افتند. **اتصال به درگاه:** `ThemeService::ingest_zip`
هر بسته‌ای که خود را فرزندِ قالب مادرِ مصوب معرفی کند از درگاه سخت‌گیر عبور
می‌کند (PHP در این مسیر دیگر «هشدار» نیست، «رد» است)؛ قالب کلاسیک بی‌تغییر.
**دو یافتهٔ دود زنده (اصلاح‌شده):** باگ قدیمی `record()` که `insert_id` ناموجود
روی پوشش Db می‌خواند (هر ثبت واقعی قالب «ناموفق» می‌شد)؛ و هارنسِ بازگردانِ
قالب دمو که فرزندِ قالب مادر را هم له می‌کرد. تست `ThemeContractTest` با ۱۲
سناریو: مجموعه **۱۴۵۹۹/۷۴** · لینت **۵۰۰۶/۰** · دود زندهٔ چرخهٔ کامل (بسته‌بندی ←
درگاه ← نصب پیش‌نمایش ← تشخیص بلوکی ← فعال‌سازی ← رندر ← ردِ دوقلوی دستکاری‌شده)
همه‌سبز + تست ویژوال کرومیوم (`visual-testing/phase-60/`). تحقیق مبنا: حداقلِ قالب
بلوکی (style.css + templates/index.html)، فرزندِ قالب بلوکی و وراثت، theme.json
نسخهٔ ۳ (`DESIGN-PADO`، `REFERENCE.md`).

#### فاز ۶۱ — preview، live و rollback قالب — ✅ تمام‌شده در ۱۴۰۶/۰۶/۰۹

artefact امضاشده، preview جدا، اعمال tenant-scoped، مقایسهٔ بصری و rollback واقعی.

**نتیجهٔ محقق‌شده:** سرویس `ThemeReleaseService` (بایند `pado.theme_releases`) —
**artefact امضاشده:** بستهٔ ZIP لحظهٔ ذخیره در `ingest_zip` امضا می‌شود (SHA-256
فایل + HMAC-SHA256 با کلید سایت، فقط ماژول Crypto — کلید با token(32) ساخته و در
گزینهٔ اختصاصی نگهداری می‌شود) و امضا/هش در metadata ردیف می‌نشیند؛
`verify()` هش فایلِ روی دیسک را با امضای مهرشده مقایسه می‌کند (hash_equals) و در
**هر دو مرزِ زمانی** — نصب پیش‌نمایش و فعال‌سازی زنده — دوباره صدا زده می‌شود:
بسته‌ای که بعد از ثبت روی دیسک ویرایش شده باشد، هرگز به پیش‌نمایش یا تولید
نمی‌رسد، هرچه انسانی تأیید کرده باشد (خطاها صریح: `unsigned_artifact`،
`signature_mismatch`، `artifact_missing`؛ چرخش کلید = بی‌اعتباری امضاهای قدیمی —
تست‌شده). **اعمال tenant-scoped و rollback واقعی** از فازهای ۱۵/۱۸ حالت زیر
امضا اجرا و در دود زنده اثبات شد. **مقایسهٔ بصری ساختاری:**
`snapshot_diff` دو رندر را واکشی می‌کند و امضاهای مرتبِ بلوک‌ها/سرتیترها را
مقایسه می‌کند (حذف اسکریپت/استایل/ادمین‌بار؛ خروجی added/removed/common) — به
انسان می‌گوید «در چیدمان چه تغییر کرد»، نه هش پیکسلی که با هر cache-buster
می‌شکند. تست `ThemeReleaseTest` با ۹ سناریو (+ سازگارسازی ThemeRoutingTest با
artefact امضاشده): مجموعه **۱۴۶۶۹/۷۵** · لینت **۵۰۰۸/۰** · دود زندهٔ ۱۰گامی
همه‌سبز (امضا در ثبت ← راستی‌آزمایی ← پیش‌نمایش ← فعال‌سازی مستأجر ← rollback ←
دستکاری بسته ← رد در هر دو مرز) + مقایسهٔ ساختاری روی رندرهای واقعی (۱۹ بلوک
مشترک) + ویژوال کرومیوم دو حالتی (`visual-testing/phase-61/`).

#### فاز ۶۲ — حافظهٔ پادو — ✅ تمام‌شده در ۱۴۰۶/۰۶/۰۹

دانش، تجربه، خاطره و حافظهٔ کاری با provenance، tenant scope، retention و دفاع از poisoning.

**نتیجهٔ محقق‌شده:** سرویس `PadoMemoryService` (بایند `pado.memory`) روی دو جدول
v45 — `pado_memory` و `pado_memory_access` (ممیزی کامل چرخهٔ حیات) — با چهار لایه:
دانش (۷ حوزهٔ بسته)، تجربه (۴ منبع بسته)، خاطره و حافظهٔ کاری (TTL ۲۴ ساعته).
**Provenance روی هر ردیف:** کلاس منبع (team/human/outcome/competitor/tool/ai) با
اعتماد متناظر (۱۰۰…۳۰)، بازیگر، مرجع، زمان؛ پایهٔ تیمی تغییرناپذیر؛ ارتقای
حافظهٔ کاری به لایهٔ ماندگار = گذار ممتازِ ممیزی‌شده که زنجیرهٔ provenance را
حفظ و اعتماد را شسته‌نمی‌کند. **Tenant scope در لایهٔ ذخیره:** هر خواندن/نوشتن
tenant_id را در خود SQL حمل می‌کند؛ تنها ردیف‌های بین‌مستأجری، الگوهای تجربهٔ
جهانیِ صراحتاً ناشناس‌اند (tenant صفر) و ادعای ناشناس بودن برای ردیف مستأجری
رد می‌شود. **Retention:** جاروب روزانه در کرون نگهداشت — حافظهٔ کاریِ منقضی حذف،
خاطره‌ها پس از پنجرهٔ نگهداشت «پاک‌شده» می‌شوند (stone قبر می‌ماند، محتوا نه)،
دانش کهنه منقضی؛ `erase` حق فراموشی با حفظ ممیزی. **دفاع لایه‌ای در برابر
poisoning:** حافظه «داده» است نه فرمان — محتوای دستوری («ignore previous»،
نقش‌ها، تگ PHP/اسکریپت) در نوشتن رد می‌شود؛ رازها هرگز وارد نمی‌شوند؛ سقف
۲۰۰ نوشتن ماشینی/روز/مستأجر؛ تکراری‌ها شمارنده می‌خورند نه ردیف تازه؛ خاطره
در سکون رمزنگاری‌شده (`igbz1:` AES-256-GCM)، در بازیابی ماسک‌شده (ایمیل/تلفن/
کارت) و هر خواندنش ممیزی می‌شود؛ بازیابی اعتمادآگاه (ترتیب بر اساس اعتماد).
تست `PadoMemoryTest` با ۱۲ سناریو (+ به‌روزرسانی DriftGuard و PHASE-01-INVENTORY
به ۹۷ جدول/v45): مجموعه **۱۴۷۸۶/۷۶** · لینت **۵۰۱۰/۰** · دود زندهٔ ۱۹/۱۹ سبز
(نوشتن واقعی، ایزولاسیون دو مستأجر واقعی، الگوی جهانی ناشناس، رد سم و راز،
رمزنگاری در سکون + ماسک + ممیزی خواندن، ارتقا با زنجیره، جاروب، bump تکراری)
· ویژوال کرومیوم سه‌صفحه‌ای (`visual-testing/phase-62/`) با صفر خطای JS.

#### فاز ۶۳ — چهار Playbook رشد، KPI و خودبازبینی — ✅ تمام‌شده در ۱۴۰۶/۰۶/۰۹

پیاده‌سازی گردآوری، تحلیل، راهبرد و تولید مطابق `PROMPT-IG-GROWTH-PADO.md`؛ نسخه‌بندی
Playbook، schema/provenance، سنجه‌های فروش/Insight واقعی، نگهداشت دوره‌ای، هزینه، حذف و
rollback دانش/قاعده.

**نتیجهٔ محقق‌شده:** سرویس `GrowthPlaybookService` (بایند `pado.playbooks`) روی دو جدول
v46 — `pado_playbooks` (نسخه‌های **تغییرناپذیر** Playbook با هش محتوا، تبار
parent_id، changelog و وضعیت draft/active/retired) و `pado_playbook_runs` (روزنامهٔ هر
اجرا: نسخهٔ دقیق + مدل + snapshot ورودی + خروجی + حکم بک‌اند + مصرف/هزینه). چهار kind
بسته (gather/analyze/strategy/produce). **نسخه‌بندی و rollback:** هر تغییر = نسخهٔ جدید؛
فعال‌سازی چرخش اشاره‌گر است که هرگز دو فعال نمی‌سازد؛ rollback تک‌فراخوانی به جدیدترین
نسخهٔ بازنشسته؛ نسخهٔ بازنشستهٔ با rollback دوباره فعال نمی‌شود (مسیر درست = نسخهٔ
تازه). **قرارداد داده (§۱):** هر fact باید {type از شش نوع، confidence، captured_at}
داشته باشد، PII هرگز وارد اجرا نمی‌شود و دادهٔ حساب متصل با دادهٔ عمومی رقیب در تحلیل
ترکیب نمی‌شود. **قرارداد خروجی سخت‌گیر (§۸):** تفکیک reality/analysis/suggestion و
ارجاع هر پیشنهاد به factهای موجود — ادعای بی‌منبع = رد صریح (نه ترمیم). **توقف امن:**
بدون شواهد کافی (کمینهٔ ۳ fact) یا بدون provider، اجرا «متوقف» می‌شود نه تحلیلِ قطعیِ
هیچ. **KPI واقعی:** `kpi_snapshot` از جدول‌های واقعی (`ig_insights` + تبدیل قیف؛ درآمد
ووکامرس فقط وقتی ووو در دسترس است، وگرنه «ناموجود» — هرگز حدس)؛ `learn()` پیش‌بینی را
با نتیجهٔ اندازه‌گیری‌شده مقایسه و تجربهٔ پالایش‌شده را در حافظهٔ مستأجر (فاز ۶۲) با
اعتماد outcome می‌نویسد. **هزینه:** `cost_summary` هزینه به‌ازای خروجی پذیرفته‌شده.
**نگهداشت دوره‌ای:** `prune()` در کرون روزانه — اجراهای کهنه حذف، سقف ۲۰ نسخه در هر
kind (فعال + هدف rollback هرگز حذف نمی‌شوند). تست `GrowthPlaybookTest` با ۱۵ سناریو:
مجموعه **۱۴۹۲۰/۷۷** · لینت **۵۰۱۲/۰** · دود زندهٔ ۲۱/۲۱ سبز (نسخه‌گذاری/چرخش/rollback/
بدون زامبی، رد نوع fact و PII، توقف امن، اجرای معتبر با پین‌شدن نسخه+مدل، رد ادعای
بی‌منبع، حلقهٔ یادگیری روی جدول‌های واقعی حافظه، هزینه به‌ازای پذیرفته، KPI واقعی،
نگهداشت) + ویژوال سه‌صفحه‌ای (`visual-testing/phase-63/`). یافتهٔ زنده: ستون `usage`
در MySQL واژهٔ رزرو‌شده است و dbDelta را می‌شکند — نام ستون `usage_json` شد.

#### فاز ۶۴ — آزمون خصمانهٔ پادو — ✅ تمام‌شده در ۱۴۰۶/۰۶/۰۹

prompt injection مستقیم/غیرمستقیم، tool poisoning، memory poisoning، جعل tenant، نشت secret،
artefact مخرب، race مجوز و قطع مدل.

**نتیجهٔ محقق‌شده:** مجموعهٔ خصمانهٔ `AdversarialPadoTest` (۹ سناریوی حمله، هر حمله باید
شکست بخورد) + **چهار سخت‌سازی که خود این مجموعه پیش از انتشارشان شکافشان را نشان
داد**: (۱) گیت تزریق مستقیم/غیرمستقیم روی مقدار و منبع fact در اجرای Playbook
(`fact_is_instructions_not_data`)؛ (۲) گیت بازتاب راز از خروجی مدل
(`output_contains_secret`) **با پاک‌نویسی** — روزنامهٔ اجرا حتی در رد، بایت‌های راز را
نگه نمی‌دارد (یافتهٔ دود زنده)؛ (۳) قطع مدل = اجرای «failed» روزنامه‌نگاری‌شده با خطای
provider، نه کرش یا اجرای نیمه‌کاره؛ (۴) گیت عنوان در حافظه (عنوان هم به prompt
برمی‌گردد — هم الگوهای دستوری هم الگوهای راز، با ارزیابی عنوان به‌مثابه رشتهٔ مستقل
برای لنگرهای ^). حملات پوشش‌داده‌شده: تزریق مستقیم (۴ payload) و غیرمستقیم از مسیر
source (شبیه خروجی ابزار مسموم)؛ tool poisoning به دیوار allowlist/اسکیمای آرگومان
(نام قربانی‌ما، کلید اضافی، نوع غلط، پارامتر غایب)؛ مسمومیت حافظه (محتوا/عنوان/راز/
سیل ماشینی)؛ جعل مستأجر (خواندن، نوشتن tenant صفر، دزدی تبار Playbook، یادگیری از
اجرای دیگری، ممیزی)؛ artefact مخرب (تزریق فایل PHP پس از امضا → signature_mismatch؛
متادیتای جعلی)؛ race مجوز (claim و decide رقابتی — دقیقاً یک‌بار)؛ قطع مدل. تست
**۱۴۹۷۴/۷۸** · لینت **۵۰۱۳/۰** · دود زندهٔ خصمانهٔ ۱۷/۱۷ سبز روی پلی‌گراند واقعی
(تزریق×۲، مسمومیت×۴، راز از مدل + اثبات نبود راز در روزنامه، جعل مستأجر×۲، artefact
امضاشده → تزریق PHP → رد، race واقعی روی جدول approvals، قطع provider) + ویژوال
سه‌صفحه‌ای (`visual-testing/phase-64/`).

### ۲۳.۱۱ موج هشت — API و تجربهٔ کاربری

#### فاز ۶۵ — قرارداد OpenAPI و نسخه‌بندی — ✅ تمام‌شده در ۱۴۰۶/۰۶/۰۹

تولید قرارداد همهٔ مسیرهای اپ، schema، کد خطا، سازگاری عقب‌رو و سیاست sunset.

**نتیجهٔ محقق‌شده:** سرویس `ApiContractService` (بایند `api.contract`) + کنترلر
`GET /igbz/v1/contract` — سند OpenAPI 3.1 **تولیدشده از مسیرهای واقعاً ثبت‌شدهٔ**
سرور REST (۱۷۰ مسیر؛ خود قرارداد هم خودش را مستند می‌کند): پارامترها از args
اعلام‌شده (نوع/enum/توضیح/اجباری) و از شکل URL (گروه‌های regex → پارامتر مسیرِ
اجباری)؛ سطح مجوز هر عملیات صریح (x-auth + security)؛ تاکسونومی خطای مشترک
هفت‌گانه با نگاشت کد→HTTP و پاکت‌های پاسخ؛ **دو پاکت خطا صادقانه مستند شده**
(پاکت کنترلر {ok,code,error} + پاکت WP دروازهٔ مجوز — یافتهٔ دود زنده). **سیاست
سازگاری:** نسخهٔ اصلی فقط در URL؛ تغییرات افزودنی آزاد (مسیر جدید، پارامتر
اختیاری جدید، رشد enum، شل‌شدن مجوز)؛ هر شکست سازگاری (حذف مسیر/متد/پارامتر،
اجباری‌شدن پارامتر، تغییر نوع، کوچک‌شدن enum، سخت‌شدن مجوز، عقب‌رفتن نسخه،
برداشتن deprecation) یعنی گشودن igbz/v2. **گیت عقب‌رو:** `breaking_changes()`
تک‌تک این نقض‌ها را با نام و نشانی می‌گوید و در تست خودکار می‌نشیند؛ خط پایهٔ
منتشرشده در `igbz-suite/contracts/api-v1.json` پین شده و دود زنده تطابق سندِ
سرو‌شده با آن را اثبات می‌کند. **سیاست sunset در زمان اجرا:** رجیستری deprecation
(اکنون خالی) + سرصفحه‌های Deprecation/Sunset (RFC 8594) و
`Link: rel="successor-version"` از طریق فیلتر `rest_pre_serve_request`. تست
`ApiContractTest` ۹ سناریو (تولید، پارامترها، مجوزها، تاکسونومی، سرصفحه‌ها،
گیت با ۸ نوع شکست + تغییرات افزودنیِ مجاز، خط پایهٔ پین): مجموعه
**۱۵۲۹۰/۷۹** · لینت **۵۰۱۶/۰** · دود زندهٔ ۸/۸ سبز + ویژوال
(`visual-testing/phase-65/`).

#### فاز ۶۶ — احراز API و دستگاه — ✅ تمام‌شده در ۱۴۰۶/۰۶/۱۰

JWT، refresh rotation، replay، revoke، device lifecycle، capability و tenant ownership.

**نتیجهٔ محقق‌شده:** ممیزی موجودی (JWT با الگوریتم سنجاق‌شده/iss/leeway، چرخش تک‌مصرف
اتمیک، بازپخش→کشت دستگاه، ابطال jti/دستگاه/کاربر، چرخهٔ حیات کامل دستگاه با کلید
بیومتریک رمزنگاری‌شده، scopes و tenant ownership) + **پنجرهٔ مهلت چرخش** به‌عنوان تنها
شکاف واقعی بر پایهٔ پژوهش (RFC 9700 §4.14.2 و مشکل خروج ناخواستهٔ کلاینت موبایل روی
شبکهٔ ناپایدار): ستون `rotated_at` (v47) چرخش را از ابطال صریح جدا می‌کند — بازپخشِ
توکنِ «چرخیده» در پنجرهٔ کوتاه (پیش‌فرض ۳۰ ثانیه، گزینهٔ `api.refresh_grace_seconds`،
بازهٔ مجاز ۰–۳۰۰) دقیقاً یک بار جفت تازه می‌گیرد و دستگاه زنده می‌ماند؛ مصرف مارکر
اتمیک است (بار سوم = دزدی)، بازپخش بیرون از پنجره = ابطال کل دستگاه، و توکنِ
صراحتاً باطل‌شده (خروج) هرگز مهلت نمی‌گیرد. بازندهٔ مسابقهٔ همزمان چرخش (claim صفر
اما مهر برنده حاضر) با بازخوانی همان مسیر پاسخ می‌گیرد نه مجازات. هویت معتبر از
ردیف دیتابیس می‌آید نه claims. تست `ApiAuthDeviceTest` با ۸ سناریو (و دابل مسطح با
ارزیابی صادقانهٔ SET/WHERE و NULL): مجموعه **۱۵۳۳۱/۸۰** · لینت صفر · دود زندهٔ ۱۳/۱۳
روی جدول‌های واقعی (شامل strict/مهلت/دزدی/خروج/ابطال فوری) + ویژوال
(`visual-testing/phase-66/`).

#### فاز ۶۷ — رفتار API موبایل — ✅ تمام‌شده در ۱۴۰۶/۰۶/۱۰

cursor pagination، limit، idempotency نوشتن، upload، push، retry/offline conflict و contract test.

**نتیجهٔ محقق‌شده:** کرسر کیست مات (tuple پایان‌یافته در ستون یکتا) روی چهار فید
موبایل — سفارش‌ها، کیف پول، دفاتر FX و محصولات — با کرسر مات `CursorCodec`
(base64url سخت‌گیر؛ هر چیز غیرکانونیک = ۴۰۰، نه برش غلط)، پاکسنجی limit (۱–۱۰۰)،
پاکت یکدست `items/has_more/next_cursor` از همان صفحهٔ اول (`?limit=N`)، و حل دقیق
تساوی همان‌ثانیه با کوئری بازه‌ای WC؛ پایداری زیر درج میانی به‌صورت زنده اثبات شد.
نوشتن idempotent به سبک Stripe/Adyen: جدول `api_idempotency` (v48) با ادعای اتمیک
روی کلید یکتا `(user_id, idem_key)`، بازپخش پاسخ اول واژه‌به‌واژه (خطاها هم!) با
سرصفحهٔ `Idempotency-Replayed`، ۴۰۹ برای درگیری/بازاستفادهٔ کلید، لیس ۱۲۰ث برای
ادعای مرده، انقضای ۴۸س و هرس روزانه — فعال روی topup کیف پول/FX، پرداخت قسط،
پیشرفت درس، ارسال آزمون، تولید AI و تحویل/COD پیک. تعارض آفلاین: `revision` نمایه +
`expected_revision` → ۴۰۹ با revision جاری. ممیزی upload/push موجود (آپلود محافظت‌شده
از فازهای قبل، پوش FCM با پاسخگیری per-token از ۶۶) کافی تشخیص شد. **باگ واقعی
فاز ۶۵ در این فاز پیدا و رفع شد:** کلید متدهای آرایهٔ انجمنی وردپرس ('GET'=>true)
به «۱» فرومی‌افتاد و عملیات‌های هم‌مسیر را جایگزین می‌کرد — قرارداد اکنون ۱۸۷
عملیات واقعی دارد و پین `contracts/api-v1.json` به 1.1.0 بازتولید شد. یونیت
`ApiMobileBehaviorTest` (۱۱ سناریو): مجموعه **۱۵۴۶۸/۸۱** · لینت صفر · دود زندهٔ
۱۶/۱۶ + ویژوال (`visual-testing/phase-67/`).

#### فاز ۶۸ — بستهٔ فارسی و checkout — ✅ تمام‌شده در ۱۴۰۶/۰۶/۱۰

`fa_IR.po/mo`، ترجمهٔ اسکریپت، اعداد/ارز/تاریخ، رفع glyph و کشور مهمان/کاربر تسویه.

**نتیجهٔ محقق‌شده:** بستهٔ زبان `languages/igbz-suite-fa_IR.po/.mo` (کامپایلر MO
اختصاصی `_devenv/build_fa_ir.py` — msgfmt در سندباکس نیست) با ۳۱۵ رشتهٔ ترجمه‌شدهٔ
تمام سطح موبایل (تاکسونومی خطای REST، پیام‌های احراز/کیف پول/پیک/پوش) و ویترین
(OTP، حساب، فرود VIP، طرح‌ها) — رشته‌های ترجمه‌نشده صادقانه خالی می‌مانند (fallback
انگلیسی) و ترجمهٔ یتیم حذف می‌شود. لایهٔ محصول `FaStorefront` جای هک‌های هارنس را
گرفت: واحد پول IRT (تومان) با نماد درست (IRR = ریال، بدون بازبرچسب‌گذاری خاموش)،
پیش‌فرض `base` برای آدرس مشتری فروشگاه تازه + فیلتر کشور تسویه فقط برای حالت خالی
(مقدار ذخیره‌شده هرگز بازنویسی نمی‌شود؛ مهمان بی‌کوکی → `IR` از Store API اثبات شد)،
و ارقام فارسی ویترین با همان محافظ‌های جنگ‌آزموده (اسکریپت/استایل/تگ/موجودیت
دست‌نخورده؛ REST هرگز تبدیل نمی‌شود — JSON برنامهٔ موبایل). کمکی‌های خالص `FaLocale`:
ارقام، بهداشت گلیف (ي/ك/ة/ارقام عربی → فارسی)، نیم‌فاصلهٔ محافظه‌کار (فهرست بستهٔ
افعال؛ «می میوه» هرگز چسبیده نمی‌شود)، تومان (جداکنندهٔ ¨U+066C) و تقویم جلالی
دقیق (الگوریتم jalaali؛ لنگرهای نوروز ۱۴۰۳/۰۴/۰۵ آزمون). ترجمهٔ اسکریپت از مسیر
`wp_localize_script` با رشته‌های PHP پوشش داده شد. seed دمو به IRT رفت و هارنس
fa-demo لاغر شد (نقشهٔ رشته‌های ووکامرس باقی ماند). یونیت `FaLocaleTest` (۸ سناریو،
خوانندهٔ MO داخلی): مجموعه **۱۵۶۰۷/۸۲** · لینت صفر · دود زندهٔ ۱۰/۱۰ + مهمان
Store-API + ویژوال (`visual-testing/phase-68/`).

#### فاز ۶۹ — RTL، دسترس‌پذیری و ماتریس نقش — ✅ تمام‌شده در ۱۴۰۶/۰۶/۱۰

دسکتاپ/تبلت/موبایل، صفحه‌کلید، focus، label، contrast، screen reader، همهٔ نقش‌ها و تصاویر
تمام‌صفحهٔ تأییدشده.

**نتیجهٔ محقق‌شده:** ممیزی axe-core (WCAG 2.x A/AA) با کرومیوم واقعی روی ۸ صفحه × ۳
ویوپورت (۱۴۴۰/۷۶۸/۳۹۰) + گذر مهمان بی‌کوکی — ۲۷ اسکرین‌شات تمام‌صفحه. ماتریس نقش:
مهمان/مشتری/صاحب فروشگاه روی ویترین-محصول-سبد-تسویه-حساب-OTP-پادو-تنظیمات؛ سطح API
(پیک/سکو) به ماتریس مجوز یونیت فازهای قبل ارجاع. تنها نقضِ کد خودمان (بحرانی:
file-input بی‌برچسب در آپلود ZIP قالب پادو) با label/id/aria-describedby اصلاح و
اسکن مجدد **صفر** شد؛ کنتراست همه‌جا سبز؛ کیبورد در ۴۰ عنصر اول هر صفحه بدون
focus نامرئی/تله؛ RTL و overflowX=۰ در هر سه ویوپورت. چهار خانوادهٔ نقض باقی‌مانده
بالادستی‌اند (بلوک سبد/تسویهٔ WC 11.0.1 و ناوبری WP 7.0.4) و با انتساب دقیق
selector/بسته به‌عنوان وظیفهٔ پیگیری ارتقا ثبت شدند. ابزار ماندگار:
`visual-testing/phase-69/a11y-audit-69.cjs` + `a11y-guest-69.cjs` (axe از npm).
ممیزی انسانی NVDA/VoiceOver وظیفهٔ تولید (ابزار DOM‌محور ~۳۰–۵۰٪ را می‌گیرد).
یونیت **۱۵۶۰۷/۸۲** · لینت صفر · دود زندهٔ فازهای قبل پابرا.

### ۲۳.۱۲ موج نه — عملیات و پذیرش نهایی

#### فاز ۷۰ — Railway و پایگاه‌دادهٔ LTS — 🔶 مصنوعات محلی کامل؛ اجرای ابری در انتظار کارفرما (۱۴۰۶/۰۶/۱۰)

staging همسان، مهاجرت آزموده از MySQL 8.0 به نسخهٔ LTS مصوب، TLS، volume و health/readiness.

**وضعیت:** سندباکس به railway.app دسترسی ندارد (فقط npm باز است؛ آزموده) و توکن
ریل‌وی سیکرت مخزن است — پس بخش ابری با ران‌بوک کپی-پیست `RUNBOOK-RAILWAY-LTS.md`
به کارفرما سپرده شد (کارفرما پیشنهاد داد توکن را در چت بگذارد؛ به‌دلیل
بی‌راهی شبکه رد شد). **انجام‌شده در کد:** نقطهٔ سلامت/آمادگی محصول
`HealthEndpoint` (`/?igbz_health=1`: 200/503 + پرچم degraded = دریفت بدون قرمزکردن
فروشگاه؛ بدون سیکرت؛ یونیت ۳ سناریو)؛ ناظر مستقل mu-plugin (اگر خود افزونه
نباشد پاسخ صادقانهٔ 503، نه سکوت)؛ `IGBZ_DB_TLS` → MYSQLI_CLIENT_SSL؛ حلقهٔ
`igbz: ready (health 200)` در entrypoint؛ `healthcheckPath` در railway.json؛
ران‌بوک مهاجرت 8.0→8.4 (دیتابیس موازی + تله‌های 8.4 + rollback با بازگرداندن
متغیر + چک‌لیست پذیرش). **مانده:** اجرای کارفرما (استقرار staging + تمرین
مهاجرت) و تأیید نسخهٔ LTS مقصد.

#### فاز ۷۱ — worker، cron و مشاهده‌پذیری — ✅ تمام‌شده ۱۴۰۶/۰۶/۱۰

server cron، worker، log ساختاریافته، trace/tenant id، داشبورد، SLO و هشدار دارای Runbook.

**خلاصهٔ تحقق:** trace درخواست/کار (`Trace` + مهر `request_id` در Logger + اجرای
کار زیر trace پاکت با `fork`)؛ سنجه‌های SLO برآیندی (`Observability/Slo`:
نرخ موفقیت ۹۸٪/عمق ۵۰/تأخیر ۱۵د/خطا ۲۵ — تنظیم‌پذیر `slo.*`) با پنل سبز/قرمز
در صفحهٔ وضعیت؛ worker/cron سرور با `IGBZ_SERVER_CRON=1` در entrypoint (هر
۶۰ث: wp-cron due + drain صف؛ DISABLE_WP_CRON فقط با تصمیم بهره‌بردار)؛
`RUNBOOK-SLO-ALERTS.md` (آستانه‌ها، اقدام هر نقض، جست‌وجوی trace، هشدار push
پیشنهادیِ در-انتظار-دستور)؛ اصلاح کنتراست status_pill؛ یونیت ۱۵۷۷۷/۸۵، لینت
۳۴۷/۰، ویژوال+crawl زنده سبز. جزئیات: STATE §۱۱.۲۷.

#### فاز ۷۲ — پشتیبان و بازیابی — ✅ تمام‌شده ۱۴۰۶/۰۶/۱۰

پشتیبان رمز‌شدهٔ DB/uploads/artefact/config، retention و restore drill واقعی با RPO/RTO.

**خلاصهٔ تحقق:** باندل رمز‌شدهٔ AES-256-GCM (`Backup/Bundle`+`BackupService`):
دامپ منطقی ۱۰۰ جدول محصول + سند تنظیمات + uploads تا سقف فایل، مانیفست
sha256 هر عضو؛ کار روزانهٔ `cron.backup` (slot idempotent) با مهر
`igbz_last_backup` که پنل SLO آن را به نقض RPO (پیش‌فرض ۲۶ ساعت) تبدیل
می‌کند؛ retention ۷؛ دستورهای `wp igbz backup create/list/verify/restore
[--apply]`؛ restore فایل‌ها اول و SQL فقط برای بازبینی مگر `--apply`؛
`RUNBOOK-BACKUP-RESTORE.md` (RPO≤24h/RTO≤4h، تلهٔ salts، تمرین فصلی)؛
یونیت BackupTest (ساخت/رمز/گردش-کامل/دستکاری/retention/RPO) → ۱۵۹۴۱/۸۶؛
زنده: باندل واقعی ساخته شد و «Last backup: 0 min» در پنل. جزئیات: STATE §۱۱.۲۸.

#### فاز ۷۳ — rollback و خط لولهٔ انتشار

rollback افزونه/DB/قالب، artefact immutable، feature flag، canary، توقف و بازگشت خودکار.

#### فاز ۷۴ — بار، دوام و chaos

بار مسیرهای بحرانی، soak، burst، queue backlog، قطع DB/cache/provider و رفتار degraded.

#### فاز ۷۵ — ممیزی امنیت نهایی

اسکن dependency/secret، تست دسترسی، SSRF، فایل، webhook، پادو و رفع همهٔ بحرانی/بالا.

#### فاز ۷۶ — تطبیق نهایی و پذیرش انتشار

صفر تناقض کد/سند، ماتریس کامل، شمارش تولیدشده، همهٔ آزمون‌ها و ویژوال سبز، smoke روی URL
عمومی، Runbookها و گزارش پذیرش کارفرما.

### ۲۳.۱۳ تعداد واقعی و شیوهٔ اجرا

- تعداد پایه: **۷۶ فاز تک‌نوبتی**؛
- به‌علاوه: **یک فاز مستقل برای هر provider فعال**؛
- عدد نهایی پس از فاز ۰۲ و تعیین providerهای واقعاً موردنیاز مشخص می‌شود؛
- فازها به ترتیب وابستگی اجرا می‌شوند، اما فازهای مستقل یک موج می‌توانند با تأیید کارفرما
  جابه‌جا شوند؛
- هر فاز یک برنامهٔ کوتاه فایل‌به‌فایل دارد، سپس تأیید و عبارت «شروع کن» می‌گیرد؛
- از شروع تا پایان همان فاز گزارش میانی داده نمی‌شود؛
- در پایان فقط گزارش نهایی دو یا سه خطی همان فاز و وضعیت فاز بعد ارائه می‌شود؛ شواهد کامل
  در اسناد ثبت می‌ماند.

فاز اجرایی `۰۱` تمام شده و فاز `۰۲` در جریان است؛ سه تصمیم نخست آن در ADR-0001 تا
ADR-0003 ثبت شدند. نخستین فاز دارای تغییر کد امنیتی، `فاز ۰۵` است.
