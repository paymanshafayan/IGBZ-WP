# گزارش تست ویژوال — فاز ۵۹ (انتشار، کمپین و سیاست روی صف)

تاریخ: ۱۴۰۶/۰۶/۰۹ (۲۹ اوت ۲۰۲۶) · ابزار: Chromium واقعی (Playwright 1.62.1 + @sparticuz/chromium 149)

## سطح بازبینی‌شده

تب «درخواست‌های مجوز» مرکز پادو با سه ردیف واقعیِ فاز ۵۹ در انتظار (پست کمپین
فروش، ارسال کمپین مشتریان، تغییر سیاست پادو) به‌علاوهٔ ردیف‌های pending/باقی از
فاز ۵۸ و نمای وضعیت «اجرا شده» با ۱۰ ردیف.

## نتیجه

| بررسی | نتیجه |
|---|---|
| رندر ردیف‌های سه نوع جدید (کمپین فروش / ارسال کمپین / تغییر سیاست) | ✅ هر سه در DOM |
| سرریز افقی | ۰ پیکسل ✅ |
| خطای JS | ۰ ✅ |
| نمای executed | ✅ ۱۰ ردیف |
| تصویر خراب | ۵ — همهٔ بیرونی (۱ gravatar + ۴ ایموجی twemoji از CDN)؛ سندباکس شبکهٔ بیرونی ندارد — مورد شناخته‌شدهٔ محیط، خارج از کد افزونه |

## محدودیت صادقانه

بینایی مدل در این جلسه خاموش بود؛ بازبینی «چشمی» مستقیم ممکن نشد. شواهد: DOM
خوانده‌شده با کرومیوم واقعی + دو اسکرین‌شات تمام‌صفحه برای بازبینی کارفرما:
`pado-approvals-pending-59.png` و `pado-approvals-history-59.png`.

## تأیید زندهٔ همراه

دود ۱۶گامی `/?igbz_content_smoke=1` همه‌سبز (کاوشگر موقت، پس از اجرا حذف شد):
انتشار زمان‌بندی‌شدهٔ واقعی (ردیف ig_content واقعی → scheduled) با outcome در
metadata؛ انتشار فوری بدون اتصال Zernio صادقانه رد (ردیف failed + خطای
not_connected در outcome + محتوا دست‌نخورده)؛ ارسال کمپین واقعی به دو عضو فعال
VIP (دو پیام admin) با outcome شمارش؛ تغییر سیاست واقعی (بودجهٔ روزانه) با
old/new در payload و outcome. اجرای مجدد دود روی idempotency خورد و تصمیم را
درست رد کرد (رفتار طراحی‌شده). جزئیات در PROJECT-STATE §۱۱.۱۵.
