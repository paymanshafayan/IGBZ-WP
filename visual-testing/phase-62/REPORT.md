# فاز ۶۲ — تست ویژوال حافظهٔ پادو

تاریخ: ۱۴۰۶/۰۶/۰۹ (بازبینی ۲۰۲۶-۰۸-۳۰) · ابزار: کرومیوم v149 واقعی + Playwright · اسکریپت: `/home/user/browser-test/visual-check-release-62.cjs`

حافظهٔ پادو در این فاز یک لایهٔ پشتیبان است (بدون رابط کاربری جدید)؛ شاهد ویژوال = سالم‌ماندن سطوح میزبان پس از اتصال سرویس به ماژول و کرون، به‌علاوهٔ شواهد DOM:

| صفحه | شواهد | نتیجه |
|---|---|---|
| مرکز پادو `admin.php?page=igbz-pado` | عنوان «Pado (AI Center)…»، چهار زبانه (🎨 طراحی قالب / ✅ درخواست‌های مجوز / 📜 تاریخچه / ⚙️ تنظیمات)، overflowX=۰، صفر خطای JS | ✅ |
| سلامت وردپرس `site-health.php` | بدون خطای مهلک پس از مهاجرت v45 | ✅ |
| ویترین `/` | ۱۶ کارت محصول، overflowX=۰، صفر خطای JS | ✅ |

- خطاهای کنسول: **صفر** در هر سه صفحه.
- `brokenImgs: 1` در دو صفحه = همان تصویر جانگهدار شناخته‌شدهٔ قبلی (پیش از فاز ۶۲ هم بود) — رگرسیون جدید نیست.
- بازبینی پیکسلی چشمی در این نشست ممکن نبود (بینایی خاموش)؛ مطابق رویهٔ ثبت‌شده، شواهد DOM + اسکرین‌شات تمام‌صفحه ملاک است.
- خروجی‌ها: `pado-center.png`، `site-health.png`، `storefront.png`.
- الگوی پایدار: بدون `FONTCONFIG_PATH`، بارگذاری `domcontentloaded` + انتظار، retry سه‌باره با صفحهٔ تازه.
