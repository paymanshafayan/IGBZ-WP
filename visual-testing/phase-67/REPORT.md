# فاز ۶۷ — تست ویژوال رفتار API موبایل

تاریخ: ۱۴۰۶/۰۶/۱۰ · ابزار: کرومیوم v149 واقعی + Playwright · اسکریپت: `visual-testing/phase-67/visual-check-release-67.cjs`

فاز ۶۷ لایهٔ API است (کرسر/idempotency؛ بدون رابط کاربری جدید)؛ شاهد ویژوال = سالم‌ماندن سطوح میزبان پس از مهاجرت v47→v48 و تغییرات کنترلرها:

| صفحه | شواهد | نتیجه |
|---|---|---|
| مرکز پادو `admin.php?page=igbz-pado` | چهار زبانه، overflowX=۰، صفر خطای JS | ✅ |
| سلامت وردپرس `site-health.php` | بدون خطای مهلک پس از v48 (۱۰۰/۱۰۰ جدول) | ✅ |
| ویترین `/` | ۱۶ کارت محصول، overflowX=۰، صفر خطای JS | ✅ |

- خطاهای کنسول: **صفر**؛ `brokenImgs: 1` = همان جانگهدار شناخته‌شده.
- شواهد اختصاصی فاز (بدون مرورگر، با هارنس زندهٔ ۱۶ بررسی): راه‌رفتن exact کرسر روی سفارش‌ها/کیف پول/دفتر FX/محصولات، پایداری کرسر زیر درج میانی، بستن ۴۰۰ کرسر خراب/غریب/مرتب‌سازی ناسازگار، تعارض revision نمایه، بازپخش idempotent با `Idempotency-Replayed: true` و بدون ردیف دوم پرداخت.
- بازبینی پیکسلی ممکن نبود (بینایی خاموش) — شواهد DOM + اسکرین‌شات ملاک.
