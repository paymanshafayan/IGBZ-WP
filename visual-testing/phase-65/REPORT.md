# فاز ۶۵ — تست ویژوال قرارداد API

تاریخ: ۱۴۰۶/۰۶/۰۹ · ابزار: کرومیوم v149 واقعی + Playwright · اسکریپت: `/home/user/browser-test/visual-check-release-65.cjs`

فاز ۶۵ قرارداد API است (سند JSON + سرصفحه‌ها؛ بدون رابط کاربری جدید)؛ شاهد ویژوال = سالم‌ماندن سطوح میزبان پس از افزودن کنترلر قرارداد و فیلتر سرصفحه‌ها:

| صفحه | شواهد | نتیجه |
|---|---|---|
| مرکز پادو `admin.php?page=igbz-pado` | چهار زبانه، overflowX=۰، صفر خطای JS | ✅ |
| سلامت وردپرس `site-health.php` | بدون خطای مهلک | ✅ |
| ویترین `/` | ۱۶ کارت محصول، overflowX=۰، صفر خطای JS | ✅ |

- شواهد اختصاصی فاز (بدون مرورگر، با curl زنده): نقطهٔ پایانی `/igbz/v1/contract` با سرصفحه‌های `X-Contract-Version: 1.0.0` و `Cache-Control` پاسخ می‌دهد؛ پیکربندی دقیق سند = خط پایهٔ پین‌شده؛ گیت ۴۰۱ روی مسیر محافظت‌شده با پاکت مستندشده.
- خطاهای کنسول: **صفر**؛ `brokenImgs: 1` = همان جانگهدار شناخته‌شده.
- بازبینی پیکسلی ممکن نبود (بینایی خاموش) — شواهد DOM + اسکرین‌شات ملاک.
- خروجی‌ها: `pado-center.png`، `site-health.png`، `storefront.png`.
