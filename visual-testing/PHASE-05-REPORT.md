# گزارش تست ویژوال — فاز ۰۵ (رازها) — ۱۴۰۵/۰۶/۰۶

ابزار: `/home/user/browser-test/visual-phase05.cjs` (کرومیوم ۱۴۹، پلی‌رایت) روی سرور زندهٔ `_devenv`.

- ۱۶ اسکرین‌شات تمام‌صفحه در همین پوشه: ۱۴ تب تنظیمات + مرکز پادو + صفحهٔ فروشگاه.
- ۲۸ ورودی راز شناسایی و پایش شد؛ **۰ نشت plaintext** (هر مقدار یا خالی است یا ماسک).
- بازبینی چشمی: ماسک نقطه‌ای در فیلد «JWT signing secret» تأیید شد؛ چیدمان/RTL سالم.
- ۲۰ خطای کنسول همگی `ERR_CONNECTION_CLOSED` درخواست‌های بیرونی در سندباکس آفلاین است؛ ربطی به تغییر فاز ندارد (رفتار شناخته‌شدهٔ محیط).
- خروجی ماشینی: `_report.json` همین پوشه.
