# ران‌بوک مهاجرت و راستی‌آزمایی ذخیره‌سازی سفارش ووکامرس (HPOS) — فاز ۲۲

تأییدشده در برابر ووکامرس ۱۱ روی فروشگاه سندباکس (۱۴۰۵/۰۶/۰۶). موتور مهاجرت متعلق به هستهٔ
ووکامرس است (`DataSynchronizer` / `CustomOrdersTableController`); نقش افزونه سازگاری و
راستی‌آزمایی است. مهاجرت روی تولید هرگز بدون اجرای کامل همین ران‌بوک روی استیجینگ انجام نمی‌شود.

## ۱. پیش‌نیازها

- پشتیبان کامل دیتابیس پیش از هر قدم.
- ووکامرس ≥ ۸٫۲ (جدول‌های سفارش: `wc_orders`، `wc_order_addresses`، `wc_order_operational_data`،
  `wc_orders_meta` با پیشوند سایت).
- افزونهٔ ما سازگاری رسمی اعلام کرده است (`custom_order_tables` + `cart_checkout_blocks` در
  `WooCommerceCompat::declare_compatibility`). در صفحهٔ ووکامرس → وضعیت → ویژگی‌ها نباید هیچ
  افزونهٔ «ناسازگار» فهرست شود؛ در غیر این صورت ابتدا همان افزونه اصلاح شود.

## ۲. روشن‌کردن همگام‌سازی (حالت سازگاری)

- مسیر: ووکامرس → پیکربندی → پیشرفته → ویژگی‌ها → «ذخیره‌سازی سفارش با کارایی بالا» +
  تیک «همگام‌سازی داده‌های سفارش».
- معنای ذخیره‌شدهٔ گزینه‌ها مهم است — هر دو مقدار `'yes'`/`'no'` می‌گیرند، نه `1`/`0`
  (راستی‌آزمایی‌شده: مقدار `'1'` حالت را فعال نمی‌کند چون خوانش با `=== 'yes'` است).
- در این حالت هر سفارش در هر دو ذخیره‌سازی نوشته می‌شود؛ سربار دوگانه‌نویسی طبیعی و موقتی است.

## ۳. همگام‌سازی سفارش‌های موجود

- دکمهٔ «اکنون همگام‌سازی کن» در همان صفحه، یا از مسیر برنامه‌ای:
  `BatchProcessingController::enqueue_processor( DataSynchronizer::class )`.
- پیمایش دسته‌ای رسمی: `DataSynchronizer::get_next_batch_to_process( n )` و سپس
  `process_batch( $ids )` تا زمانی که `has_orders_pending_sync()` نادرست شود.
- پیش‌شرط تغییر منبع حقیقت: صفر بودن سفارش‌های در انتظار همگام‌سازی؛ در غیر این صورت هسته
  استثنا می‌اندازد (راستی‌آزمایی‌شده):
  «The authoritative table for orders storage can't be changed while there are orders out of sync».

## ۴. راستی‌آزمایی داده (پیش از سوئیچ)

- شمارش: تعداد ردیف‌های `wc_orders` در برابر پست‌های `shop_order`.
- نمونه‌گیری: مجموع `total_amount` در `wc_orders` در برابر مجموع متادیتای `_order_total`.
- نقطه‌های خاص افزونهٔ ما (هر دو از روی شناسهٔ سفارش‌اند و به ذخیره‌سازی وابسته نیستند، پس
  نیازی به جابه‌جایی ندارند): مُهر `_igbz_tenant_id` و `_igbz_ref_code` روی سفارش، جدول‌های
  `affiliate_commissions`، `enrollments`، `master_payments`، تراکنش‌های کیف پول.

## ۵. سوئیچ منبع حقیقت به جدول‌های سفارش

- همان فرم ویژگی‌ها را ذخیره کنید؛ اگر همگام‌سازی کامل باشد سوئیچ می‌پذیرد.
- آزمون دود پس از سوئیچ: ساخت یک سفارش آزمایشی باید در `wc_orders` بنشیند و از طریق
  `wc_get_order` با متادیتا خوانده شود؛ فهرست «سفارش‌ها» در پیشخوان صفحهٔ `wc-orders` است.

## ۶. دورهٔ پایش و بازگشت کنترل‌شده

- حداقل یک چرخهٔ کامل کسب‌وکار (پرداخت، مرجوعی، تمدید اشتراک، گزارش) را روی ذخیره‌سازی جدید
  ببینید؛ جریان‌های افزونهٔ ما همگی از لایهٔ CRUD عبور می‌کنند و آزمون آن‌ها در هر دو حالت
  در `HposOrderFlowTest` سبز است.
- **بازگشت:** همان گزینهٔ منبع حقیقت را خاموش کنید؛ باز هم مشروط به صفر بودن صف همگام‌سازی
  است (ابتدا همگام‌سازی معکوس). همگام‌سازی را تا اطمینان کامل روشن نگه دارید؛ خاموش‌کردن زودهنگام
  یعنی از دست رفتن تور ایمنی بازگشت.

## ۷. یافته‌های زندهٔ این پروژه (برای مجریان بعدی)

- `OrderUtil::get_order_edit_link` در ووکامرس ۱۱ وجود ندارد؛ لینک ویرایش سفارش باید از
  `OrderUtil::get_order_admin_edit_url` گرفته شود (باگ واقعی در همین فاز کشف و در
  `WooCommerceCompat::order_edit_url` اصلاح شد).
- «اکنون همگام‌سازی کن» فقط از صفحهٔ `section=features` با نانس مخصوص خودش کار می‌کند؛ برای
  اسکریپت، مسیر دسته‌ای بند ۳ را استفاده کنید.
- جدول‌ها با پیشوند سایت ساخته می‌شوند؛ هیچ کوئری دستی نباید پیشوند را حدس بزند
  (`WooCommerceCompat::orders_table_name` از `OrdersTableDataStore` می‌گیرد).
