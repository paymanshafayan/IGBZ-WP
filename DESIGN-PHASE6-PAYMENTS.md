# فاز ۶ — درگاه‌ها، BNPL و درگاه پرداخت مرکزی: طراحی

**وضعیت:** طراحی تأییدشده توسط کارفرما. مبنای آن: نقشهٔ راه nop (فاز ۶)، بازبینی Parbad و
معادل PHP آن (Shetabit/Payment)، و تعاریف تکمیلی کارفرما (بله‌پی، درگاه مرکزی، اینماد،
قرارداد دیجیتال).

> **الگوی حاکم:** هر درگاه = یک آداپتور جدا (الگوی Parbad/Shetabit). منطق هر بانک از
> مستندات رسمی همان بانک (و الگوی کتابخانه‌های باز مانند Shetabit) گرفته می‌شود؛ چون
> کتابخانه‌های موجود (Parbad برای C# و Shetabit برای Laravel) با وردپرس/PHP خام ما ناسازگارند،
> آداپتورها را خودمان با همان منطق پیاده می‌کنیم.

---

## ۱. درگاه‌های بانکی (ریالی)

### ۱.۱ فهرست (الگوی Parbad/Shetabit)
| درگاه | نوع | پروتکل/پیچیدگی |
|---|---|---|
| زرین‌پال، آیدی‌پی، نکست‌پی، پی.آی.آر | واسط | REST — ✅ موجود |
| **پارسیان** | مستقیم | REST (ساده) |
| **ایران‌کیش** | مستقیم | REST + رمزنگاری |
| **آسان‌پرداخت** | مستقیم | REST |
| **سداد (ملی)** | مستقیم | REST + امضا |
| **ملت (به‌پرداخت)** | مستقیم | SOAP/XML + DES |
| **سامان (SEP)** | مستقیم | REST + RSA سه‌کلید |
| **پاسارگاد** | مستقیم | REST + RSA |
| **سپهر (صادرات)** | مستقیم | REST |

هر درگاه: `GatewayInterface` موجود (request/verify) + `required_settings` + سندباکس در صورت
وجود. **اولویت پیاده‌سازی:** پارسیان ← ایران‌کیش ← آسان‌پرداخت ← سداد ← ملت ← سامان ←
پاسارگاد ← سپهر.

### ۱.۲ شرط فعال‌سازی درگاه بانکی
انتخاب/فعال‌سازی هر درگاه بانکی **فقط** وقتی ممکن است که:
1. فروشگاه **دامنهٔ مستقل** داشته باشد (ثبت‌شده و تأیید)، **و**
2. **اینماد فعال** داشته باشد.

تا این دو برقرار نشوند، گزینهٔ درگاه بانکی در تنظیمات **قفل** است.

### ۱.۳ پادو و فرآیند اینماد
«پادو» (دستیار هوشمند — همان هستهٔ AI که جداگانه طراحی می‌شود) ادمین را در **تمام مراحل
دریافت اینماد** راهنمایی و یاری می‌کند و «در صورت امکان» برخی کارها را خودکار انجام می‌دهد:

| مرحله | کمک پادو |
|---|---|
| ثبت‌نام در مرکز اینماد | راهنمای گام‌به‌گام + (در صورت امکان) پر کردن خودکار فرم‌ها |
| احراز هویت و مدارک | راهنمای مدارک + بررسی آمادگی |
| تأیید دامنه/سایت | راهنمای کد تأیید + بررسی صحت |
| بررسی محتوا و قوانین | چک‌لیست خودکار صفحات الزامی + تولید پیش‌نویس |
| نصب نماد | راهنمای نصب + (اختیاری) نصب خودکار در قالب |
| فعال‌شدن | اطلاع‌رسانی + باز شدن خودکار گزینهٔ درگاه |

### ۱.۴ صفحات الزامی — پیش‌فرض همهٔ فروشگاه‌ها
صفحات **«قوانین و مقررات»، «شرایط استفاده» و «حریم خصوصی»** به‌صورت **پیش‌فرض در قالب همهٔ
فروشگاه‌ها** ساخته و تحویل داده می‌شوند (در هنگام Provisioning). محتوا: قالب آماده با
جایگذاری خودکار (نام فروشگاه، دامنه، اطلاعات تماس)؛ ادمین می‌تواند ویرایش کند.

---

## ۲. بله‌پی (کیف پول بله)

- **مکانیزم:** پرداخت از طریق ربات/بازوی بله — `sendInvoice`/`createInvoiceLink` با
  `provider_token` (از `@botfather`)؛ تأیید با رویداد `SuccessfulPayment` و
  `provider_payment_charge_id` (به‌جای کال‌بک کلاسیک). واحد: ریال.
- **کاربردها:**
  1. درگاه پرداخت در checkout («پرداخت با کیف پول بله»).
  2. مسیر **پرداخت کارت‌به‌کارتِ بدون مالیات** برای ادمین (ثبت پرداخت از طریق بله).
- **آداپتور:** `BalePayGateway` (پیاده‌سازی `GatewayInterface`): ساخت Invoice ← ارسال در چت
  بله ← شنیدن SuccessfulPayment ← تأیید. کلیدها: `bale.provider_token`، `bale.bot_username`.

---

## ۳. BNPL — پرداخت اقساطی (اسنپ‌پی، دیجی‌پی، تارا)

### ۳.۱ آداپتورها
`HttpBnplProvider` موجود برای هر سه: اسنپ‌پی، **دیجی‌پی**، تارا — با **استعلام اعتبار
real-time** (underwrite). کلیدها و base URL به‌صورت تنظیم.

### ۳.۲ قاعدهٔ قیمت‌گذاری اقساطی (الزام قانونی)
| حالت | قیمت |
|---|---|
| پرداخت اقساطی (فعال توسط ادمین) | **قیمت اصلی** (همان قیمت پایه) |
| پرداخت نقدی | **قیمت اصلی − n٪ تخفیف نقدی** |

- «تخفیف پرداخت نقدی» (مثلاً ۳٪) به‌صورت خودکار در checkout برای روش‌های نقدی اعمال می‌شود.
- **ممنوع:** ارائهٔ «قیمت متفاوت برای اقساط» (خلاف قانون، جریمه سنگین).
- تنظیم: `bnpl.cash_discount_percent` (پیش‌فرض ۰ = غیرفعال تا ادمین فعال کند).

---

## ۴. درگاه پرداخت مرکزی (مادر) — Master Payment Gateway

### ۴.۱ ایده
به‌جای درگاه مستقل، ادمین می‌تواند از **درگاه پرداخت مرکزی ما** استفاده کند که هر دو فاز ۶
و ۱۲ را پوشش می‌دهد. درگاه مرکزی «ضامن اعتماد» است: وجه مشتری تا تسویه نزد شرکت می‌ماند.

### ۴.۲ محل نگهداری وجه
| بخش | حساب مقصد |
|---|---|
| ریالی | **حساب بانکی شرکت در ایران** |
| ارزی | **حساب شرکت در قبرس** (USDT) |

### ۴.۳ جریان ریالی (اسکرو)
```
مشتری checkout → «پرداخت با درگاه مرکزی» → درگاه بانکی رسمی (زیرین)
  → وجه به حساب شرکت در ایران
  → وضعیت: held
  → مرسوله تحویل شد (فاز ۷) → ۲۴ ساعت بدون نارضایتی
  → وجه به کیف پول داخلی ادمین (released)
  → ادمین برداشت به کارت/حساب درخواست می‌دهد
  → نارضایتی (اپ مشتری / پشتیبانی) → وجه بلوکه + بررسی → بازگشت/تسویه
```

### ۴.۴ جریان ارزی (فاز ۱۲)
```
مشتری خارجی checkout → درگاه ارزی مرکزی (NowPayments)
  → پرداخت با کریپتو (USDT/BTC) یا کارت بین‌المللی (Visa/Mastercard — تسویه USDT)
  → وجه به حساب شرکت در قبرس
  → تسویه به ادمین با کارمزد جزئی درگاه مرکزی
```
- ادمین برای بخش ارزی **فقط** می‌تواند از درگاه ارزی مرکزی استفاده کند (نه درگاه مستقل).

### ۴.۵ قرارداد دیجیتال (پیش‌شرط)
- فعال‌سازی درگاه مرکزی برای هر فروشگاه **فقط پس از تأیید «قرارداد دیجیتال»** توسط ادمین.
- قرارداد شامل: نگهداری وجه تا ۲۴ ساعت پس از تحویل، تسویه/برداشت، فرآیند نارضایتی، کارمزدها.
- رکورد `ig_master_agreements`: `tenant_id`، `version`، `accepted_at`، `accepted_by`، `ip`،
  `hash` (هش محتوای قرارداد برای اثبات).

### ۴.۶ جدول‌ها (v16)
| جدول | محتوا |
|---|---|
| `ig_master_payments` | تراکنش مرکزی: `phase` (`rial`/`fx`)، `amount`، `status` (`held`/`released`/`disputed`/`refunded`)، `hold_until`، `released_at`، `order_id`، `tenant_id`، `gateway_ref` |
| `ig_master_disputes` | نارضایتی: `payment_id`، `source` (`app`/`support`)، `reason`، `status` |
| `ig_master_withdrawals` | برداشت ادمین: `user_id`، `amount`، `method` (کارت/حساب)، `status` |
| `ig_master_agreements` | قرارداد دیجیتال پذیرفته‌شده (بالا) |

### ۴.۷ تسویه و برداشت
- تسویهٔ خودکار با کرون: بررسی «تحویل‌شده + ۲۴ ساعت گذشته + بدون dispute» → واریز به
  کیف پول داخلی ادمین → امکان درخواست برداشت به کارت/حساب (ثبت در `ig_master_withdrawals`).

### ۴.۸ NowPayments (فاز ۱۲ — درگاه ارزی مرکزی)
- `NowPaymentsGateway` موجود: کریپتو + کارت بین‌المللی؛ تسویهٔ USDT به حساب قبرس.
- کارمزد جزئی درگاه مرکزی روی تسویهٔ ارزی اعمال می‌شود.

---

## ۵. پنل ادمین
| صفحه | محتوا |
|---|---|
| Payments (موجود، تکمیل) | فهرست همهٔ درگاه‌ها + فعال/غیرفعال + کلیدها + **Test connection** + وضعیت اینماد/دامنه (قفل درگاه) |
| Master payments | وضعیت تراکنش‌های مرکزی، تسویه‌ها، برداشت‌ها، نارضایتی‌ها |
| اینماد (پادو) | راهنمای گام‌به‌گام اینماد + چک‌لیست + وضعیت |
| قرارداد دیجیتال | نمایش و تأیید قرارداد (پیش از فعال‌سازی درگاه مرکزی) |

---

## ۶. قواعد امنیتی/مهندسی
- هر درگاه: تأیید با فراخوانی واقعی + مقایسهٔ مبلغ + idempotency در `payments` ledger.
- درگاه مرکزی (حفظ وجه دیگران): ledger کامل + idempotency + وضعیت‌های صریح + لاگ.
- قرارداد دیجیتال: ذخیرهٔ `hash` محتوا برای اثبات نسخهٔ تأییدشده.
- بدون Graph API اینستاگرام/متا (قانون ثابت).

---

## ۷. فازبندی پیاده‌سازی (پس از تأیید)
| گام | محتوا |
|---|---|
| ۱ | آداپتورهای بانکی: پارسیان، ایران‌کیش، آسان‌پرداخت، سداد (REST) |
| ۲ | آداپتورهای بانکی: ملت، سامان، پاسارگاد، سپهر (XML/رمزنگاری) + Test connection |
| ۳ | بله‌پی (`BalePayGateway`) |
| ۴ | BNPL: دیجی‌پی + قاعدهٔ تخفیف نقدی |
| ۵ | درگاه مرکزی: جدول‌ها + اسکرو + تسویه ۲۴h + نارضایتی + قرارداد دیجیتال |
| ۶ | شرط دامنه/اینماد + پادؤ راهنما + صفحات پیش‌فرض قوانین/شرایط/حریم خصوصی |
| ۷ | تست‌ها + تأیید زنده |
