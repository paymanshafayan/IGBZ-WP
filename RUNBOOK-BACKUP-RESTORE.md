# Runbook — پشتیبان، بازیابی و تمرین بازیابی (فاز ۷۲)

هدف‌های سرویس (توافق‌شده با بهره‌بردار؛ مبنای تحقیق: راهنمای رسمی وردپرس
و رویه‌های رایج صنعت — قاعدهٔ 3-2-1):

- **RPO ≤ ۲۴ ساعت** — هر شب یک باندل رمز‌شدهٔ کامل از داده‌های محصول.
- **RTO ≤ ۴ ساعت** از «تصمیم به بازیابی» تا «فروشگاه سبز» (سلامت ۲۰۰).
- نگهداری: ۷ باندل (تنظیم `backup.retention`)؛ یک نسخهٔ خارج‌ازهاست
  (خارج از ریل‌وی) با ابزار خود کارفرما — باندل ما فقط داخل volume می‌نشیند.

## ۱) چه چیزی پشتیبان می‌شود و چه چیزی نه

| جزء | در باندل؟ | توضیح |
|---|---|---|
| جداول محصول (۱۰۰ جدول `igbz_*` از Schema) | ✅ | دامپ منطقی INSERT‌ها |
| سند تنظیمات (`igbz_settings`) | ✅ | رمز‌شده داخل باندل |
| فایل‌های uploads تا سقف `backup.max_file_mb` (پیش‌فرض ۵MB) | ✅ | فایل‌های بزرگ‌تر در «skipped» ثبت می‌شوند، بی‌سکوت |
| هستهٔ وردپرس، افزونه‌های دیگر، دیتابیس خام، سیکرت‌های wp-config | ❌ | وظیفهٔ snapshot/host و ازدیاد خارج‌ازهاست |

باندل: `wp-content/uploads/igbz-backups/igbz-backup-<Ymd-His>.igbzbk`
— یک پاکت AES-256-GCM (کلید از salts وردپرس + `IGBZ_ENCRYPTION_KEY`)
با مانیفست sha256 برای هر عضو.

## ۲) زمان‌بندی (RPO clock)

کار `cron.backup` هر روز با ضربان روزانه enqueue می‌شود (همان slot
idempotent خانه‌keeping) و worker صف آن را می‌راند. مهر زمان در گزینهٔ
`igbz_last_backup` فقط بعد از نوشتن موفق فایل زده می‌شود و پنل SLO آن را
به نقض `slo.max_backup_hours` (پیش‌فرض ۲۶ ساعت) تبدیل می‌کند — یعنی
پشتیبانی که بی‌سروصدا از کار افتاده باشد، خودش قرمز می‌شود.

## ۳) دستورهای بهره‌بردار (WP-CLI)

```bash
wp igbz backup create            # همین حالا یک باندل
wp igbz backup list              # باندل‌ها + آخرین مهر موفق
wp igbz backup verify <file>     # رمزگشایی + صحت‌سنجی checksumها
wp igbz backup restore <file>    # فایل‌ها برمی‌گردند؛ SQL فقط برای بازبینی نوشته می‌شود
wp igbz backup restore <file> --apply   # + اجرای SQL
```

## ۴) رویهٔ بازیابی (RTO)

1. `wp igbz backup list` → سالم‌ترین/تازه‌ترین باندل را انتخاب کن.
2. `wp igbz backup verify <file>` — سبز نبود؟ باندل قبلی.
3. `wp igbz backup restore <file>` — فایل‌ها اول (ترتیب توصیه‌شدهٔ وردپرس)،
   SQL و سند تنظیمات کنار باندل نوشته می‌شوند.
4. SQL را بازبینی کن؛ سپس `--apply` یا `wp db import <restore-*.sql>`.
5. سند تنظیمات فقط در صورت نیاز دستی وارد شود (عمداً خودکار نیست).
6. سلامت: `/?igbz_health=1` → 200 و پنل SLO سبز. ثبت زمان: RTO واقعی.

## ۵) نکات حیاتی

- **کلید باندل = salts وردپرس.** روی نصب تازه باید salts/`IGBZ_ENCRYPTION_KEY`
  نصب قبلی را در wp-config برگردانید وگرنه باندل «not decryptable» می‌شود
  (و این پاسخ صادقانه درست است — دیتای رمزشده بدون کلید همیشه گم است).
  این salts را جدا از خود باندل و امن نگه دارید.
- بازیابی روی محیط تمیز/استیجینگ تمرین شود، نه مستقیم روی تولید.
- فایل‌های skipped (over-cap) باید در گزارش create دیده شوند؛ اگر
  media library واقعی بزرگ شد، سقف را بالا ببرید یا از snapshot volume
  برای uploads استفاده کنید و باندل را برای داده‌های تراکنشی نگه دارید.

## ۶) تمرین بازیابی (هر فصل)

1. استیجینگ با همان salts بالا بیاورید.
2. `wp igbz backup create` روی تولید، فایل را به استیجینگ کپی کنید.
3. `verify` → `restore` (بدون `--apply`) → بازبینی SQL → `--apply`.
4. سلامت ۲۰۰ + یک سفارش آزمایشی + ثبت RPO/RTO واقعی در
   `PROJECT-STATE.md` (بخش فاز ۷۲).
