# IGBZ-WP — وضعیت پروژه

**آخرین به‌روزرسانی:** ۱۴۰۵/۰۶/۰۶ (۲۸ اوت ۲۰۲۶)
**وضعیت جاری:** فازهای ۰۱ تا ۲۶ تمام — موج یک و دو کامل (دروازهٔ موج دو پاس) + صف بادوام با هر سه ضربان (پنج‌دقیقه‌ای، ساعتی با پخش‌بینی مستأجر و ادعای نوبتی، روزانه با قرارداد ادامهٔ رسمی `continue_round`)؛ فاز ۰۲ برای تصمیم‌های ظرفیت/دیتابیس/ترجمه همچنان باز است. فاز بعدی: ۲۷ (مشاهده‌پذیری و آزمون صف).
**شاخهٔ جلسه:** `arena/01a04859-igbz-wp`
**نسخهٔ افزونه:** 1.0.0 · **نسخهٔ دیتابیس:** 24 · **تست‌ها:** ۱۷۸۳ اظهارنظر در ۴۴ کیس · **لینت:** ۲۷۷ فایل، صفر خطا

این سند منبع وضعیت جاری است؛ ردیابی نیازها در `REQUIREMENTS-TRACEABILITY.md` و برنامه در
`PROJECT-COMPLETION-PLAN.md` نگهداری می‌شود. در تعارض، کد و مدرک اجرای تازه مقدم‌اند و سند
باید اصلاح شود. پوشهٔ `vira/` در ممیزی و اجرای این فاز بررسی یا تغییر داده نشد.

## جلسهٔ ۱۴۰۵/۰۶/۰۶ — اجرای فازهای ۰۳ تا ۵ برنامه جامع

- **فاز ۰۳:** شمارش‌های کهنهٔ «وضعیت جاری» در `HANDOFF-PROMPT.md` و `DESIGN-DEPLOY-VIRA.md` با کد هم‌راستا شد (۷۲ جدول).
- **فاز ۰۴:** تست `DriftGuardTest` و گام drift در `_devenv/test.sh` ساخته شد؛ هر واگرایی شمارش/جفت‌شدگی، سوئیت یا موجودی را قرمز می‌کند.
- **فاز ۰۵:** رجیستری رازها ۴۰ عضوی شد (۲۲ فیلد جاافتاده + `vip.media_hmac_secret`)؛ مهاجرت v20 مقدارهای plaintext را رمز می‌کند؛ `rotate_secret()` و تست‌های `SecretsTest`/ ویژوال ۱۶ صفحه با ۰ نشت. جزئیات: `API-KEYS.md §۵` و `visual-testing/PHASE-05-REPORT.md`.
- تست: ۱۳۵۴ اظهارنظر در ۲۶ کیس · لینت ۲۴۷ فایل · سلامت زنده ۷۲/۷۲ جدول.

## تصمیم جاری پادو و اینستاگرام — ۱۴۰۵/۰۶/۰۶

مرجع قطعی:

```text
ADR/ADR-0004-PADO-ZERNIO-SOCIAL-ARCHITECTURE.md
DESIGN-INSTAGRAM-PADO-ZERNIO.md
```

- پادو دستیار کامل ولی Playbookمحور هر فروشگاه است؛ عامل عمومی آزاد نیست؛
- حساب و هزینهٔ پادو/DeepInfra مستقل و متعلق به همان مدیر فروشگاه است؛
- DeepInfra تنها provider هدف inference نسخهٔ اول، مشروط به benchmark فارسی و صلاحیت
  جغرافیایی است؛
- Zernio تنها provider اجتماعی است؛ یک حساب پرداخت مرکزی IGBZ و یک profile جدا برای هر
  فروشگاه دارد و هزینه‌اش در اشتراک فروشگاه منظور می‌شود؛
- ChatPlace، ManyChat، Manus و Ayrshare از معماری هدف حذف‌اند و fallback آنلاین نداریم؛
- بازاریابی، تحلیل، برنامه‌ریزی، تولید و یادگیری از فروش/کمپین کاملاً بر عهدهٔ پادوست؛
- trending audio از Zernio می‌آید؛ تحلیل رقبا در نسخهٔ نخست به دادهٔ رسمی محدود و ورودی
  مدیر مقید است؛ `Business Discovery` و `Hashtag Search` از Zernio درخواست می‌شوند؛
- scraper، cookie و نشست مرورگر Instagram مطلقاً وارد محصول نمی‌شوند؛
- WooCommerce منبع حقیقت و بک‌اند IGBZ مرجع هویت، مجوز، صف و اجرای قطعی می‌مانند.

متن‌های تاریخی پایین این سند دربارهٔ مسیر فعال Manus/ChatPlace/ManyChat یا مالکیت Meta بر
Manus فقط سابقه‌اند و مبنای معماری یا اجرای بعدی نیستند.

---

## ⚠️ قانون کار — بدون استثنا

سه قاعده‌ای که کارفرما در ۱۴۰۵/۰۵/۲۸ نهایی کرد. **تخطی از این‌ها به هیچ وجه مجاز نیست.**

### ۱. پلن، و بعد کلمهٔ «شروع کن»

**تا وقتی برای کار پلن ارائه نشده و کارفرما آن پلن را تأیید نکرده و دقیقاً کلمات
«شروع کن» را نگفته باشد، حق تغییر در کد پروژه وجود ندارد.**

- تأیید ضمنی، «خوبه»، «موافقم»، سکوت، یا برداشتِ خودِ عامل از خواستهٔ کارفرما **کافی نیست**.
  ملاک، همان دستور صریح **«شروع کن»** است.
- ترتیب همیشه این است: **پلن ← تأیید کارفرما ← «شروع کن» ← آن‌وقت تغییر**.
- کارهایی که چیزی را عوض نمی‌کنند (خواندن، بررسی، جستجو، تست، تحلیل، پاسخ به سؤال، و
  نوشتنِ خودِ پلن) آزادند و اجازه نمی‌خواهند.
- **کامیت، پوش و باز کردن PR بعد از تغییرِ تأییدشده آزاد است** و تأیید جداگانه نمی‌خواهد.

### ۲. سکوت تا پایان کار — فقط گزارش نهایی کوتاه

**تا فاز کاملاً تمام نشده، هیچ گزارش میانی به کارفرما داده نمی‌شود.** پس از اتمام و
به‌روزرسانی همان‌لحظهٔ اسناد مرتبط، فقط یک گزارش **دو یا سه خطی** ارائه می‌شود.

### ۲.۱ قرارداد پیام نقطه

از ۱۴۰۵/۰۶/۰۵، پیام کارفرما که فقط شامل `.` باشد، معادل «ادامه بده» است و کارِ مجاز و
در جریان را ادامه می‌دهد. این میان‌بر جای عبارت صریح «شروع کن» برای آغاز تغییر کد یا فاز
اجرایی تازه نیست، چون کارفرما آن را مشخصاً جایگزین «ادامه بده» تعریف کرده است.

### ۴. جمع‌آوری تصمیم‌ها: یک سؤال در هر نوبت، و صبر تا پایان همهٔ سؤال‌ها

کارفرما در ۱۴۰۵/۰۵/۳۱ دو تصحیح داد، بعد از اینکه عامل چند سؤال را با هم پرسید و
خلاصه‌نویسی کرد:

- **سؤال‌ها یکی‌یکی پرسیده شوند**، با **توضیح کامل** — نه فهرست فشرده و نه چند سؤال در
  یک نوبت. بعد از هر جواب، جمع‌بندی شود و **بعد** سؤال بعدی بیاید.
- **تا جواب همهٔ سؤال‌های باز داده نشده، هیچ تغییری در کد شروع نمی‌شود.** در این مرحله
  تنها کار مجاز، **ثبت جواب‌ها در اسناد** است.

### ۵. هر چیزی دربارهٔ ویرا، در سند خودش ثبت شود

کارفرما (۱۴۰۵/۰۵/۳۱): **«از این به بعد در بحث‌ها هر موردی دربارهٔ ویرا مطرح می‌شه که باید
به طراحی اون اضافه بشه، در سند مربوطهٔ خودش ثبت کن.»**

یعنی نتیجهٔ گفتگو نباید فقط در `PROJECT-STATE.md` یا در متن پاسخ بماند؛ اگر به طراحی ویرا
مربوط است باید در سند طراحی همان موضوع بنشیند (`vira/DESIGN-PROVIDER-UI.md`،
`DESIGN-PROVIDER-HUB.md`، `DESIGN-UI-PARITY.md`، `DESIGN-HUB-UI-FIX.md`،
`DESIGN-DEPLOY-VIRA.md` و…) تا موقع پیاده‌سازی گم نشود.

### ۳. تست سبک — مخصوصاً در ویرا

**برای هر تغییر کوچک در ویرا، تستِ متعدد و فراگیر گرفته نمی‌شود.** چند تست نرمال — فقط در
حدی که معلوم شود تغییر واقعاً اعمال شده — کافی است. **کارفرما خودش کامل تست می‌کند.**
وقت صرف سوئیت کامل و سناریوهای گسترده نکن.

> تاریخچه: نسخهٔ اول (۱۴۰۵/۰۵/۲۴) کامیت/پوش/PR را هم مشمول تأیید قبلی می‌دانست؛ کارفرما در
> ۱۴۰۵/۰۵/۲۷ تصحیح کرد که تأیید فقط برای **شروعِ تغییر** لازم است. در ۱۴۰۵/۰۵/۲۹ قاعدهٔ
> «تست سبک» اضافه شد، و در ۱۴۰۵/۰۵/۲۸ هر سه قاعده به شکل بالا نهایی و سخت‌گیرانه شد:
> کلیدواژهٔ صریح «شروع کن»، ممنوعیت گزارش میانی، و تست سبک.

---

## ⚠️ قانون اپ‌ها — وب‌اپلیکیشن برای همه

**تمام اپ‌های موبایل (که با فلاتر نوشته می‌شوند) باید نسخهٔ وب‌اپلیکیشن هم داشته باشند.**
بسیاری از کاربران تمایلی به نصب اپ ندارند؛ پس هر اپ (مشتری، ادمین، پیک و هر اپ آینده) باید
از مرورگر هم قابل استفاده باشد. چون فلاتر از Flutter Web/PWA پشتیبانی می‌کند، یک کدبیس
فلاتر با دو خروجی (موبایل + وب) منتشر می‌شود. این قانون به درخواست کارفرما ثبت شد.

---

## 📌 Agent Reach — دامنهٔ مجاز پس از ADR-0004

Agent Reach جزء اجباری معماری نیست. استفاده از آن برای وب، RSS و منابع عمومی مجاز می‌تواند
به‌عنوان ابزار تحقیق پادو بررسی شود، اما کانال مبتنی بر cookie/session اینستاگرام غیرفعال و
ورود هر scraper یا browser automation اینستاگرام ممنوع است. ترند صدای Instagram از Zernio
گرفته می‌شود.

## 📌 حذف ChatPlace و ManyChat؛ انتخاب Zernio

تصمیم تاریخی انتخاب ChatPlace و نگهداری ManyChat به‌عنوان fallback با ADR-0004 جانشین شد:

- Zernio تنها provider اجتماعی پروژه است؛
- ChatPlace و ManyChat از معماری هدف حذف می‌شوند؛
- Manus و Ayrshare نیز مسیر تولید، انتشار یا fallback نیستند؛
- حساب Zernio مرکزی است، ولی هر فروشگاه profile و account صریح و جدا دارد؛
- تولید، تحلیل و برنامه‌ریزی به پادو و اجرای اجتماعی به Zernio سپرده می‌شود.

## 📌 Graph API — مسیر رسمی از طریق Zernio

Graph API ممنوعیت ایدئولوژیک ندارد، اما IGBZ آن را مستقیم پیاده نمی‌کند. اتصال حرفه‌ای
Instagram با OAuth و عملیات انتشار/Inbox/analytics از Zernio عبور می‌کند. رمز، 2FA، cookie
یا session فروشگاه دریافت نمی‌شود. `Business Discovery` و `Hashtag Search` تا ارائهٔ endpoint
مستند Zernio در backlog می‌مانند.

## 📌 Windsor — بررسی و رد (ثبت کارفرما)

**ترکیب Windsor (عامل مرورگر Anthropic) با پادو برای تعامل با اینستاگرام بررسی و رد شد:**

- فنیاً Windsor می‌تواند با مرورگر واقعی با اینستاگرام کار کند (بدون Graph API).
- اما طبق ToS متا (۲۰۲۶): **اتوماسیون مرورگر/اکستنشن برای اینستاگرام ممنوع است** («fast-track
  to ban») — حتی سخت‌گیرانه‌تر از Graph API که حداقل مسیر رسمی است.
- ToS خود Anthropic هم استفاده از Claude برای اتوماسیون غیرانسانی (جز از طریق API) را ممنوع
  می‌کند.
- **نتیجه:** برای تعامل خودکار با اینستاگرام رد شد. مسیر جاری، پادو برای تحلیل/تولید و
  Zernio برای اتصال رسمی و عملیات اجتماعی است؛ هیچ browser automation یا session-based
  fallback وجود ندارد.

## 📌 اپ‌های فلاتر — مبنای کار (ثبت کارفرما)

**اپ‌های فلاتر موجود در پروژهٔ nopCommerce (`IGBZ-project-full.zip`) مبنای کار هستند و کامل
می‌شوند، نه بازنویسی:**

- `03-instagram_store_admin_flutter` — اپ ادمین فروشگاه (داشبورد، محصولات، سفارش‌ها، مشتریان،
  تخفیف‌ها، برندینگ سبک اینستاگرام)
- `04-nopcommerce_flutter_app_customers` — اپ مشتریان (کاتالوگ، سبد، تسویه، سفارش، محصولات
  دیجیتال، تشخیص خودکار فروشگاه با شماره موبایل)

این‌ها مبنای اپ‌های ما هستند؛ اپ پیک و نسخهٔ وب‌اپ (PWA — طبق قانون) هم بر اساس همین ساختار
ساخته می‌شوند. کد nop فقط مرجع منطق است؛ اما **UI/فلو/ساختار اپ‌های فلاتر مستقیم قابل
استفاده و تکمیل است**.

## 📌 جایگاه AI Studio (ثبت کارفرما)

**AI Studio زیرمجموعهٔ «ابزار هوشمند» (پادو) است و از آن جدا نیست.** استودیوی محتوا
یکی از قابلیت‌های پادو است (نه ماژول مستقل) و از همان زیرساخت (سرویس‌دهنده‌ها، کلیدها،
اعتبار) استفاده می‌کند. پیاده‌سازی موقت فعلی (`igbz-ai-studio`) فقط «پل موقت» است تا هستهٔ
پادو ساخته شود؛ سپس AI Studio به‌صورت یکی از Skill های آن درمی‌آید.

---

## ۱. پروژه در یک نگاه

پورت محصول IGBZ از nopCommerce به **وردپرس + ووکامرس**. چهار افزونهٔ nop به **یک افزونه با پنج
ماژول قابل‌خاموش‌وروشن** تبدیل شده است.

**معماری جاری اینستاگرام:** پادو تمام تحلیل، برنامه‌ریزی و تولید را انجام می‌دهد و Zernio
تنها درگاه اجتماعی برای OAuth، انتشار، Inbox، comment-to-DM و analytics است. حساب Zernio
مرکزی و هر فروشگاه profile جدا دارد؛ حساب و هزینهٔ inference پادو مستقل و متعلق به همان
فروشگاه است. Manus، ChatPlace، ManyChat و Ayrshare فقط در کد/تاریخچهٔ قدیمی دیده می‌شوند و
جزء معماری هدف نیستند. مرجع: `ADR/ADR-0004-PADO-ZERNIO-SOCIAL-ARCHITECTURE.md`.

**چندمستأجری:** تک‌سایتی با ستون `tenant_id`. از WordPress Multisite استفاده نمی‌شود. جریان ایجاد مستأجر، عضویت مدیر، نگاشت دامنهٔ قابل‌مسیریابی و داشبورد مالک در کد فعال است؛ آزمون دامنهٔ اختصاصی واقعی به تنظیم DNS و ارائه‌دهندهٔ دامنهٔ کارفرما وابسته است.

**پیش‌نیازهای نصب فروشگاه (تصمیم کارفرما — ۱۴۰۶/۰۵/۳۱ ≈ ۲۲ اوت ۲۰۲۶):** هر فروشگاهٔ تازه‌تأسیس در IGBZ
به‌صورت **پیش‌فرض** فقط با هستهٔ وردپرس + ووکامرس + افزونهٔ IGBZ-Suite و **قالب پیش‌فرض هسته**
بالا می‌آید. **هیچ صفحه‌ساز ثالثی (از جمله Elementor) و هیچ قالب باندل‌شده‌ای (از جمله Hello
Elementor) به‌صورت پیش‌فرض نصب نمی‌شود** (دلیل: حجم بالای المنتور ~۱۰۸ مگابایت در ریپو و بی‌نیازی
مرحلهٔ ۱ پادو به آن). مدیر فروشگاه می‌تواند بعداً هر قالب/صفحه‌سازی که خواست نصب کند، اما
مرحلهٔ ۱ پادو («طراحی قالب») یک **قالب فرزند FSE** روی قالب هسته تولید می‌کند و به نصب
صفحه‌ساز ثالث وابسته نیست.

**منابع الهام مرحلهٔ ۱ طراحی قالب (تصمیم کارفرما — ۱۴۰۶/۰۵/۳۱):**
- ✅ اسکیل `nextlevelbuilder/ui-ux-pro-max-skill` (کامیت `bc826e2`، هفت اسکیل UX با فرمت `SKILL.md`)
  به‌صورت پیش‌فرض در ویرا/پادو نصب است (پوشهٔ `.vira/skills/`، git-ignored؛ در استقرار سراسری
  در `~/.vira/skills/`). این اسکیل‌ها **الگو** هستند و مستقیم خروجی فارسی نمی‌دهند.
- ✅ `21st.dev` به‌عنوان **مرجع بصری** برای الگوی «عمق/سه‌بعدی» (heroهای WebGL، liquid metal، کارت
  شیشه‌ای، گرادیان بار، hover زنده). این یک وب‌سایت است، اسکیل فایلی نیست — چیزی در ویرا نصب نمی‌شود.
- ❌ **MCP/Abilities داخلی ووکامرس استفاده نمی‌شود** (دستور کارفرما). دلیل: هشدار امنیتی — احراز هویت
  با کلید REST (`X-MCP-API-Key`) مسیر موازی است که `CoreSurfaceGuard` را دور می‌زند. تا حل این
  شکاف در سند امنیت، پرچم `mcp_integration` خاموش می‌ماند.

**انتخاب مدل/سرویس پادو (تصمیم جاری — ۱۴۰۵/۰۶/۰۶):**
- DeepInfra تنها provider هدف inference نسخهٔ اول است؛
- هر فروشگاه حساب، credential، بودجه و هزینهٔ مستقل خودش را دارد؛
- مدل هر Playbook توسط تیم IGBZ و پس از benchmark انتخاب می‌شود، نه به‌صورت آزاد در هر اجرا؛
- اتصال از `AiProviderInterface` و `DeepInfraAdapter` می‌گذرد و ویرا proxy روزمرهٔ مدل نیست؛
- فعال‌سازی برای هر فروشگاه به benchmark فارسی و صلاحیت جغرافیایی/قراردادی وابسته است؛
- OpenRouter و fallback پولی خودکار در نسخهٔ اول استفاده نمی‌شوند.

**قاعده جدید — به‌روزرسانی خودکار اسناد (تصمیم کارفرما — ۱۴۰۶/۰۵/۳۱ ≈ ۲۲ اوت ۲۰۲۶):**
هرگونه تغییر/افزودن در **اسناد و کتب پروژه** (`HANDOFF-PROMPT.md`، `PROJECT-STATE.md`، فایل‌های `DESIGN-*.md`، `PROMPT-*.md`، READMEها، و به‌طور کلی هر فایل Markdown سندی) و همچنین **ثبت تصمیم‌هایی که کارفرما در جریان گفتگو می‌گیرد** در سند(های) مربوطه **به‌طور خودکار و بلافاصله** اعمال می‌شود و **نیازی به ارائه پلن، گرفتن تأیید، یا شنیدن کلمهٔ «شروع کن» ندارد.**
- ⚠️ این استثنا **فقط شامل اسناد** است. برای **هرگونه تغییر در کد** (فایل‌های `*.php`، `*.js`، `*.mjs`، اسکریپت‌های `_devenv/setup.sh` و `run.sh`، mu-pluginها، پلاگین اصلی در `igbz-suite/`، ویرا در `vira/`، و هر فایل اجرایی/قابل اجرای دیگر) کماکان قاعده ۱ `HANDOFF-PROMPT.md §۰` (پلن ← تأیید ← «شروع کن» ← تغییر) به‌قوت خود باقی است.
- به‌روزرسانی‌های خودکار اسناد در پایان هر کار به‌عنوان بخشی از **گزارش نهایی** فهرست می‌شوند، اما منتظر اجازه جداگانه نمی‌مانند.

### شش ماژول

شناسه‌ها در `src/Support/Modules.php`، ذخیره در آپشن `igbz_enabled_modules`:

| شناسه | محتوا |
|---|---|
| `multitenant` | مستأجرها، کیف پول، پرداخت، BNPL، همکاری در فروش، اشتراک، LMS |
| `instagram` | هدف: Zernio برای social plane و پادو برای تحلیل/تولید؛ کد legacy Manus/ManyChat/ChatPlace نیازمند migration |
| `hub` | دایرکتوری فروشگاه‌ها، ثبت‌نام، بلوک محتوا، تأیید دامنه |
| `rest_api` | API اپ موبایل (احراز هویت، حساب، کاتالوگ، دستگاه، پوش) |
| `fx` | زیرساخت پرداخت ارزی موجود؛ متراژ/قبض Manus و ManyChat legacy است. DeepInfra مستقیم با خود فروشگاه و Zernio در اشتراک IGBZ تسویه می‌شود |
| `pado` | مرکز مجوز و قالب فعلی؛ موتور Playbook و adapter مستقل DeepInfra هنوز پیاده نشده است |

پیش‌فرض `multitenant` و `pado` روشن هستند؛ پادو مرکز مجوز و طراحی قالب را فراهم می‌کند.

---

## ۲. کانال VIP — خواستهٔ اصلی کارفرما

جایگزین Close Friends اینستاگرام: یک فید خصوصی شبیه اینستاگرام **داخل اپ خودمان**.
پست عمومی اینستاگرام فقط **تیزر** است؛ محتوای واقعی روی استوریج خودمان و فقط برای کاربر
پولی داخل اپ قابل مشاهده است.

### چرا Close Friends جواب نمی‌داد (بسته‌شده، بازگشایی نکنید)

سه مانع، هر سه قطعی:
1. اینستاگرام **دکمهٔ اشتراک‌گذاری را روی محتوای Close Friends غیرفعال کرده**.
2. **oEmbed فقط محتوای عمومی** را می‌دهد؛ اندپوینت‌های قدیمی آوریل ۲۰۲۵ بازنشسته شدند.
3. عضویت در لیست Close Friends **هیچ API ندارد** و مشتریان ما در لیست شخصی مدیر نیستند.

### تصمیم امنیتی — دو سطح مجزا

این نکته یک بار اشتباه اعمال شده بود و کارفرما تصحیح کرد:

| بخش | سطح امنیت |
|---|---|
| **VIP** | **سبک** — حداکثر معادل یک پست Close Friends. بدون واترمارک، بدون اتصال به دستگاه، بدون مسدودسازی اسکرین‌شات |
| **LMS / آموزش** | **سنگین** — `FLAG_SECURE`، `isCaptured`، تشخیص شبیه‌ساز، لینک مقید به دستگاه، واترمارک |

این تفکیک در `VipMediaService.php:16` مستند شده تا دوباره اشتباه نشود.

### آنچه ساخته شد (کامیت `e9ed315`)

**سرویس‌ها** در `src/Modules/Instagram/Vip/`:

| فایل | مسئولیت |
|---|---|
| `VipAccessService` | تشخیص استحقاق: نویسنده / اشتراک / خرید تکی / رایگان |
| `VipPostService` | CRUD، زمان‌بندی، انقضا، شورت‌کد، ساخت فید با شکل‌دهی قفل/بازشده |
| `VipMediaService` | لینک مدیای امضاشده با HMAC و عمر کوتاه؛ بازبینی استحقاق در لحظهٔ سرو |
| `VipSocialService` | لایک، کامنت و ریپلای، سیو، شمارش بازدید، پین کامنت |
| `VipMessageService` | دایرکت داخل پست به مدیر (نخ گفتگو) |
| `VipBillingService` | اشتراک، خرید تک‌پست، حمایت مالی |
| `VipLandingPage` | صفحهٔ اشتراک‌گذاری `/vip/p/{shortcode}` |

**لایهٔ اجتماعی کامل اینستاگرام:** لایک، کامنت + ریپلای، سیو، شمارش بازدید، دایرکت داخل پست.

**داشبورد مدیر:** `Admin/VipPage.php` با پنج تب — Posts / Comments / Inbox / Members / Plans،
به‌همراه آمار درآمد و اعضا.

**صفحهٔ اشتراک‌گذاری** `/vip/p/{shortcode}`: پیش‌نمایش تیزر، دو دکمهٔ «خرید اشتراک» و
«خرید همین پست»، لینک دانلود اپ، توضیحات، و دکمهٔ حمایت مالی روی پست‌های عمومی.

**درآمدزایی:** هم اشتراک دوره‌ای، هم خرید تک‌پست، به‌علاوهٔ حمایت مالی (tip) روی پست عمومی.
مبالغ پیش‌فرض حمایت: ۵۰/۱۰۰/۲۰۰/۵۰۰ هزار (کلید `vip.tip_presets`).

**نه جدول** `vip_*` در نسخهٔ ۱۰ دیتابیس. **۲۹ مسیر** `/vip/*`. **۲۱ سناریوی تست**.

### انقضای پست VIP — بسته شد

سه سؤال باز این بخش با رأی کارفرما (۱۴۰۵/۰۵/۲۶) بسته و پیاده شدند؛ خلاصه در بالای همین بخش
و شرح کامل در [`DESIGN-VIP-EXPIRY.md`](DESIGN-VIP-EXPIRY.md). موتور انقضا همان کرون
پنج‌دقیقه‌ای است؛ آنچه عوض شد سیاست و مالکِ تنظیم است، نه مکانیزم.

---

### ✅ سیاست انقضای VIP — تصمیم گرفته شد (۱۴۰۵/۰۵/۲۶)

کارفرما رأی داد و پیاده شد (DB v17). سند کامل: [`DESIGN-VIP-EXPIRY.md`](DESIGN-VIP-EXPIRY.md).

- **مدت:** پیش‌فرض **۷ روز**، و **فقط مدیر ارشد IGBZ** آن را تعیین می‌کند
  (`vip.default_expiry_days` در IGBZ → Settings → VIP). ادمین فروشگاه فقط **مطلع** می‌شود؛
  کنترل انقضا از ویرایشگر پست برداشته شد.
- **بعد از انقضا:** پست **از سرور حذف می‌شود** (`delete` + پاک‌شدن فایل مدیا). ردیف پست با
  وضعیت `deleted` و رکوردهای مالی می‌مانند، چون سند خرید مشتری و آمار کانال به آن‌ها بند است.
- **خریدار تک‌پست:** بعد از انقضا دسترسی ندارد — ولی **قبل از پرداخت** روی صفحهٔ فرود با ذکر
  تاریخ به او گفته می‌شود، و راه نگه‌داشتن محتوا (آیکون ذخیره در اپ) هم همان‌جا آمده است.
- **ادمین فروشگاه** در صفحهٔ VIP تذکر می‌گیرد که با گذاشتن همان پست در **کلوز فرندز پیج
  اینستاگرام خودش** می‌تواند از محتوا نگهداری کند (یک جملهٔ راهنما به انسان — بدون هیچ تماسی
  با API اینستاگرام).
- **قابلیت «ذخیره» ساخته شد:** اسناد قبلی آن را «موجود» نوشته بودند ولی در کد نبود (نه جدول،
  نه متد، نه مسیر REST). حالا `vip_post_saves` + `POST /vip/posts/{id}/save` +
  `GET /vip/saved` + `GET /vip/posts/{id}/offline` (نسخهٔ آفلاین داخل اپ) وجود دارد.

---

## ۳. تاریخچهٔ نسخه‌های دیتابیس

نردبان مهاجرت در `src/Support/Activator.php:67+`. هر پله جداگانه و تکرارپذیر است.

| نسخه | محتوا |
|---|---|
| v6, v7 | پایه‌های اولیه |
| v8 | جدول `ig_intake` |
| v9 | ثبت محصول از طریق اپ |
| v10 | **نه جدول `vip_*`** — کانال VIP |
| v11 | **LMS** — رساندن آزمون، گواهی، لغو ثبت‌نام هنگام بازگشت وجه |
| v12 | ستون `ig_content.ig_shortcode` + بازبرچسب‌زنی پاداش قیف |
| v13 | **حذف** صف بلااستفادهٔ `jobs` |
| v14 | **شش جدول `fx_*`** — واسط پرداخت ارزی |
| v15 | **شش جدول فازهای ۶–۱۴** — ارسال، مارکت‌پلیس، سبد رهاشده، اعتبار AI، قرعه‌کشی |
| v16 | **۱۶ جدول جدید** — درگاه مرکزی، دامنه، وب‌پرزنس، پیک، برچسب، COD، رصد، چت، احراز قانونی |
| v17 | **`vip_post_saves`** + اعمال سیاست انقضای VIP (۷ روز، حذف واقعی) روی تنظیمات و پست‌های موجود |

مجموع: **۷۲ جدول**.

> شمارش را با `grep -c '$sql[] = "CREATE TABLE' src/Support/Schema.php` بگیرید، نه با
> `grep -c 'CREATE TABLE'` — عبارت دوم یک کامنت بالای فایل را هم می‌شمارد و یکی بیشتر می‌دهد.
> این اشتباه یک بار در همین اسناد رخ داد.

جداول بدون `tenant_id` (عمداً): `tenants`, `tenant_domains`, `tenant_members`, `plans`, `logs`,
`lesson_progress`, `vip_post_likes`, `vip_post_saves`, `vip_post_views`, `fx_rates`, `fx_prices`,
`ig_label_group_items`, `ig_courier_tracking`, `ig_courier_chat` (لیست مرجع: whitelist در
`tests/SchemaTest.php`).

---

## ۴. کارهای انجام‌شده به ترتیب زمانی

### بازبینی nopCommerce (تمام)
سند `REVIEW-IGBZ-NopCommerce.md`. مخزن مبدأ فقط خوانده شد؛ **هیچ تغییری در آن داده نشد**.

### کانال VIP (تمام — `e9ed315`, `649080b`)
شرح کامل در بخش ۲. سند طراحی: `DESIGN-VIP.md`.

### LMS (تمام — `52319df`)
سه موردی که کارفرما گفت «هر سه باید به‌ترتیب درست شوند»:
1. **رساندن آزمون به دانشجو** — `questions_for_client()` تنها راه مجاز ارسال آزمون به کلاینت است
   (پاسخ‌های درست را حذف می‌کند).
2. **واقعی‌کردن `lms.certificate_enabled`** — دو کلید باید موافق باشند: سراسری
   (`lms.certificate_enabled`) و هر دوره (`courses.certificate_enabled`).
3. **لغو ثبت‌نام هنگام بازگشت وجه** — تطبیق بر اساس `enrollments.order_id`.

جداول: `courses`, `lessons`, `enrollments`, `lesson_progress`, `quizzes`, `quiz_attempts`.

### شناسهٔ پست و برچسب پاداش (تمام — `92df5da`)
- **برچسب پاداش قیف:** قبلاً با دلیل `affiliate_commission` ثبت می‌شد و از پورسانت واقعی
  قابل‌تفکیک نبود. حالا دلیل اختصاصی `instagram_reward` دارد. مهاجرت v12 فقط ردیف‌هایی با
  ارجاع `ig_funnel:*` را بازبرچسب می‌زند و پورسانت‌های واقعی را دست نمی‌زند.
- **`PostIdentity`:** هر املای یک پست (شورت‌کد، `/p/`، `/reel/`، با یا بدون نام کاربری و
  پارامتر ردیابی) را به شورت‌کد تقلیل می‌دهد. فرم قیف حالا انتخابگر پست‌های منتشرشده است.
- **یک باگ نهفتهٔ از پیش‌موجود:** `ORDER BY (post_id <> '') ASC` ترتیب اولویت را وارونه می‌کرد —
  یک قیف «هر پست» روی همهٔ کمپین‌های اختصاصی سایه می‌انداخت. دیده نمی‌شد چون دابل تست هم
  فیلتر `IN` و هم `ORDER BY` را نادیده می‌گرفت. هر دو اصلاح شد.

### حذف کانوا (تمام — این جلسه)
به دستور کارفرما («اگر استفاده نمی‌شود کلاً حذف شود و هیچ ردپایی نماند»): کانوا کاملاً از کد و
اسناد حذف شد. یافتهٔ بررسی: کلید `canva.api_key` در هیچ‌جا خوانده نمی‌شد (مرده بود)؛ سوییچ
`manus.use_canva` هم حذف شد — حالا Manus همیشه خودش تصاویر نهایی را تولید و ضمیمه می‌کند
(همان شاخه‌ای که قبلاً در `PromptBuilder` وجود داشت). فایل‌های دست‌خورده: `ManusService.php` (۳
جا)، `PromptBuilder.php`، `Activator.php`، `SettingsPage.php`، `Settings.php`،
`InstagramModule.php`، `ContentBlockService.php`، `ContentGeneratorInterface.php`، `tests/` و
۵ ورودی `.pot` (۱۸۹۸ → ۱۸۹۵). دستیار هوش مصنوعی (Manus با کلید `manus.api_key`) بدون تغییر
ماند.

### واسط پرداخت ارزی — فاز ۱ (تمام — این جلسه)
طبق سند تأییدشدهٔ `DESIGN-FX.md` پیاده شد. ماژول جدید `fx` (پیش‌فرض خاموش) + نسخهٔ ۱۴
دیتابیس (شش جدول `fx_*`):

- **شارژ ریالی:** ادمین مبلغ دلاری می‌دهد؛ طبق قانون کارفرما **۱۰٪ کارمزد** (`fx.fee_percent`)
  روی مبلغ ارز اضافه می‌شود، با نرخ قفل‌شده به ریال تبدیل و با درگاه‌های ایرانی (`purpose =
  fx_topup`) پرداخت می‌شود؛ فقط مبلغ خالص در کیف پول دلاری می‌نشیند. تسویه روی
  `igbz_payment_verified` با idempotency است.
- **نرخ:** `fx.rate_source` = خودکار (URL + مسیر JSON) با fallback دستی؛ کش‌شده و در لحظهٔ
  شارژ در `fx_rates` قفل می‌شود.
- **متراژ منوس (بدون صف):** در `ManusService::dispatch()` برای اکانت‌های `own`، اعتبارِ همان
  مستأجر در لحظهٔ ارسال چک می‌شود: کافی → تسک فوراً می‌رود؛ ناکافی → همان لحظه رد می‌شود
  («شارژ کنید»)؛ تسکی که منوس نپذیرفت با همان مبلغ برمی‌گردد (`release`).
- **آداپتور پرداخت:** `FxPayoutAdapterInterface` + رجیستری (`igbz_register_fx_payout_providers`)
  + کلید `fx.payout_provider`. هنوز ارائه‌دهندهٔ واقعی وصل نشده (انتخاب با کارفرما).
- **قیمت‌ها:** `fx_prices` با پیش‌فرض‌های `manus_task=0.5$`، `manus_monthly=25$`،
  `manychat_monthly=30$` (قابل ویرایش در صفحهٔ igbz-fx).
- **پنل:** صفحهٔ «FX payments» (`igbz-fx`) با فرم شارژ، نرخ، قیمت‌ها و لجر؛ تب «FX payments»
  در تنظیمات؛ ردیف‌های health در Status.
- تست: کیس جدید `FxTest` (۱۱ سناریو) → مجموع ۹۳۱ اظهارنظر در ۲۰ کیس؛ ۱۶۸ فایل لینت.

### فازهای ۶ تا ۱۴ نقشهٔ راه nop (تمام — این جلسه)
طبق `DESIGN-PHASES-6-14.md` روی وردپرس پیاده شد (DB v15، شش جدول جدید؛ ۵۳ جدول کل):

- **فاز ۶ — درگاه‌ها و BNPL:** `HttpPspGateway` (درگاه تنظیم‌پذیر برای بانک‌های بیشتر) +
  `HttpBnplProvider` برای اسنپ‌پی/تارا (ثبت با `igbz_register_bnpl_providers`).
- **فاز ۷ — لجستیک:** جدول `ig_shipments`، `LogisticsService` (دسته‌بندی مسیر تنظیم‌پذیر،
  PIN تحویل با `random_int`، تأیید با `hash_equals`)، آداپتور تاپین/پستکس، صفحهٔ igbz-logistics.
- **فاز ۸ — استودیوی AI:** `AiProviderInterface` + `HttpAiStudioProvider` (تصویر/حذف
  پس‌زمینه/ویدیو/TTS/عکس مدل — Endpoint تنظیم‌پذیر) + `AiStudioService` (ذخیره در مدیالایبری)
  + صفحهٔ igbz-ai-studio.
- **فاز ۹ — مارکت‌پلیس‌ها:** جدول‌های `ig_marketplace_sync` (صف بادوام) و
  `ig_category_mapping` + آداپتور دیجی‌کالا/دیوار + کرون پنج‌دقیقه‌ای + صفحهٔ igbz-marketplaces.
- **فاز ۱۰ — سئو و تبلیغات:** `SeoService` (متای قطعی)، فید یکتانت/تپسل از کاتالوگ واقعی
  (`?igbz_feed=`)، آداپتور تریبون + صفحهٔ igbz-seo.
- **فاز ۱۱ — گیمیفیکیشن:** `GamificationService` (چرخ‌وفلک با سردی و RNG امن + کوپن واقعی
  ووکامرس)، `AbandonedCartService` (جدول `ig_abandoned_carts` + کرون ساعتی) + صفحه.
- **فاز ۱۲ — رمزارز و ترجمه:** `NowPaymentsGateway` (درگاه checkout برای مشتری خارجی — جدا
  از FX) + `TranslationService`/`HttpTranslationAdapter` + صفحهٔ igbz-translator.
- **فاز ۱۳ — VOD:** `LmsVodService` (لینک امضاشدهٔ HMAC با انقضا و Bind به IP — الگوی آروان).
- **فاز ۱۴ — اعتبار AI و قرعه‌کشی:** `AiCreditsService` (لجر `ig_ai_credit_ledger`، شارژ با
  درصد خرید + topup)، REST استودیوی مشتری (`/igbz/v1/ai/*`)، `GiveawayService` (قرعه‌کشی از
  کامنت‌های واقعی `ig_funnel_hits` با `random_int`) + صفحهٔ igbz-giveaways.
- تست: `PhasesTest` (۱۱ سناریو) → مجموع ۱۰۴۴ اظهارنظر در ۲۱ کیس؛ ۲۰۹ فایل لینت؛ تأیید زندهٔ
  ۷ صفحهٔ جدید + تب‌های تنظیمات.

### فازهای ۶ تا ۱۴ — تکمیل نهایی (تمام — این جلسه، DB v16)

طبق سندهای طراحی فازبه‌فاز (`DESIGN-PHASE6-PAYMENTS.md`، `DESIGN-PHASE7-COURIER.md`،
`DESIGN-PHASE9-MARKETPLACES.md`، `DESIGN-PHASE10-SEO.md`، `DESIGN-PHASE12-INTERNATIONAL.md`،
`DESIGN-DOMAIN.md`، `DESIGN-LEGAL-AUTH.md`) روی همان پایهٔ v15 ساخته شد و به **۶۹ جدول (v16)**
رسید:

- **فاز ۶ — تکمیل درگاه‌ها:** درگاه BalePay؛ **درگاه مرکزی escrow** (`ig_master_*` با کرون
  آزادسازی وجه، اختلافات و توافقنامهٔ دیجیتال)؛ ارائه‌دهندهٔ BNPL دیجی‌پی؛ تنظیم تخفیف نقدی
  (`bnpl.cash_discount_percent` — به‌صورت کارمزد سبد برای پرداخت غیراقساطی)؛ **احراز کد ملی
  شاهکار** (`NationalIdVerifier` — تا وقتی مدیر ارشد کلید را ثبت نکند، قفل است).
- **هشت آداپتور مستقیم بانک (IPG)** روی `GatewayInterface` با پایهٔ `AbstractIpgGateway`:
  سداد (REST + RSA-SHA1)، آسان‌پرداخت، پارسیان (SOAP)، ایران‌کیش (REST v3)، ملت (SOAP
  bpPay/bpVerify/bpSettle)، سامان (RSA سه‌کلید)، پاسارگاد (امضای RSA)، سپهر — هرکدام با
  تنظیمات و کلیدهای خودش، سازگار با سندباکس، و همگی پشت **قانون گیت دامنهٔ تأییدشده + انماد
  فعال** (`legal.enamad_active`)؛ دکمهٔ **Test connection** (پرداخت آزمایشی ۱ ریالی) در صفحهٔ
  Payments؛ **برداشت از درگاه مرکزی** (`request_withdrawal` + REST)؛ **انتقال دامنه**
  (`DomainService::transfer` + REST).
- **فاز ۷ — اپ پیک (کامل):** مسیریابی ترتیبی با دکمهٔ «arrived»، جستجوی بارکد، تحویل با
  PIN مشتری‌محور، **COD** به چهار شکل (نقد/درگاه/کارتخوان/اپ)، رصد زندهٔ موقعیت، چت پیک با
  مشتری؛ **چاپ برچسب استاندارد با بارکد** (جداول `ig_couriers`, `ig_label_groups`,
  `ig_cod_*`, `ig_courier_routes`, `ig_courier_tracking`, `ig_courier_chat`).
- **فاز ۹ — باسلام:** آداپتور باسلام + **انتشار خودکار محتوای تولیدی اینستاگرام در باسلام**
  (وقتی فعال باشد).
- **فاز ۱۰ — دامنه و پرزنس وب:** سرویس دامنه (جستجو/ثبت/زیردامنه/DNS) + ثبت خودکار
  وب‌پرزنس (گوگل/بینگ) برای دامنه‌های تأییدشده + صفحات قانونی پیش‌فرض هر فروشگاه + **ذخیرهٔ
  متای واقعی سئو روی محصولات** (انتخابگر محصول در `igbz-seo` — شکاف nop بسته شد).
- **فاز ۱۲ — چندزبانه:** سرویس i18n (زبان‌های انتخاب‌شده توسط ادمین) + تنظیمات Stripe/PayPal
  برای کارت قبرس شمالی.
- **فاز ۱۴ — چت‌پلیس:** `ChatPlaceClient` + سوئیچ `dm.provider = manychat|chatplace`؛
  منی‌چت غیرفعال به‌عنوان fallback می‌ماند (مدیر ارشد با ثبت کلید سوئیچ می‌کند).
- **REST:** کنترلرهای `CourierController` و `DomainController` (مجموع ۱۴۵ مسیر در دو
  فضای نام).
- تست: `Phases2Test` (سناریوهای escrow، تحویل پیک/COD، برچسب، مسیریابی) و `IpgAdaptersTest`
  (۶ سناریو: گیت تنظیمات، امضای RSA، payload های SOAP) → مجموع **۱۱۷۲ اظهارنظر در ۲۳ کیس؛
  ۲۳۴ فایل لینت**؛ تأیید زنده: ۶۹/۶۹ جدول، همهٔ صفحات ادمین ۲۰۰، دکمه‌های Test در صفحهٔ
  درگاه‌ها.
- کامیت‌ها: `a6b9523`, `acbc029`, `ac36835`, `67b9c8c`, `9b30a28`.

### باگ خواندن پاسخ در چهار درگاه بانکی (رفع شد — جلسهٔ ۱۴۰۵/۰۵/۳۰)

**علامت:** اجرای تست‌ها شش `Warning: Undefined array key` روی سداد، ملت و پارسیان می‌داد.

**ریشه:** `AbstractIpgGateway::post_json()` و `post_raw()` یک آرایهٔ **انجمنی** برمی‌گردانند
(`['ok'=>…, 'body'=>…, 'raw'=>…, 'error'=>…]`)، اما هشت فراخوانی در چهار درگاه آن را با
**لیست عددی** باز می‌کردند: `[ $ok, $body ] = $this->post_json(…)`. کلیدهای `0` و `1` وجود
ندارند، پس `$ok` و `$body` همیشه `null` می‌شدند — یعنی **هر پاسخ موفق بانک، شکست خوانده
می‌شد**. مشتری پول را پرداخت می‌کرد و سفارش تأیید نمی‌شد.

**دامنه از آنچه اول به‌نظر می‌رسید بزرگ‌تر بود:** علاوه بر سه درگاهی که Warning می‌دادند،
**آسان‌پرداخت** هم دقیقاً همین باگ را داشت ولی ساکت بود، چون هیچ تستی متدهایش را صدا
نمی‌زد. Warning نبودن دلیل سالم بودن نیست.

- **اصلاح:** هر هشت فراخوانی به خواندن با کلید تبدیل شد
  (`$r = $this->post_json(…); $ok = (bool) $r['ok']; $body = (array) $r['body'];`).
  فایل‌ها: `SadadGateway`، `AsanPardakhtGateway`، `MellatGateway`، `ParsianGateway`.
  چهار درگاه دیگر (ایران‌کیش، سامان، پاسارگاد، سپهر) مستقیم با `$response` کار می‌کردند و
  سالم بودند.
- **جلوگیری از تکرار:** قرارداد بازگشتی هر دو متد در `AbstractIpgGateway` با `@return
  array{ok:bool,body:array,raw:string,error:string}` و یک هشدار صریح مستند شد.
- **چرا تست‌های قبلی نگرفتند:** هر پنج سناریوی قبلی فقط `assert_false($result->success)`
  با مضمون «بدون شبکه ⟵ شکست» می‌سنجیدند — که با کد باگ‌دار **هم** سبز است. تست تازه
  (`test_gateways_read_a_successful_bank_response`) از قلاب HTTP هارنس یک **پاسخ موفق واقعی**
  بانک را در صف می‌گذارد و اصرار می‌کند که نتیجه موفق باشد و توکن درست برگردد. درس همان
  قاعدهٔ بخش ۶ است: تستی که فقط مسیر شکست را می‌سنجد، نبودِ قابلیت را از بودنش تشخیص نمی‌دهد.
- **جهش:** هر دو شاخهٔ اصلاح (سداد و ملت) عمداً به شکل باگ‌دار برگردانده شدند و تست قرمز شد
  (`expected 'SADAD-TOK', got ''` و `expected 'MELLAT-REF', got ''`) و Warningها برگشتند.
- تست: **۱۲۱۹ اظهارنظر در ۲۳ کیس** (۱۰ اظهارنظر جدید)، ۲۳۵ فایل لینت، **صفر Warning**.
- **هنوز با کلید واقعی آزموده نشده** — همان قید همیشگی: آداپتور آماده است، تأیید نهایی
  فقط با کلید واقعی بانک ممکن است.

### سیاست انقضای VIP و قابلیت ذخیره (تمام — جلسهٔ ۱۴۰۵/۰۵/۲۶، DB v17)

کارفرما سه سؤال باز VIP را بست: مدت اعتبار را **مدیر ارشد IGBZ** تعیین می‌کند (پیش‌فرض
**۷ روز**)، پس از آن پست **از سرور حذف می‌شود**، و به ادمین فروشگاه و مشتری **قبل** از
انتشار/پرداخت اطلاع داده می‌شود. سند طراحی و گزارش کامل اجرا:
[`DESIGN-VIP-EXPIRY.md`](DESIGN-VIP-EXPIRY.md).

- **مالکیت تنظیم عوض شد:** فیلدهای `expires_at` و `expiry_action` از ویرایشگر پستِ ادمین
  فروشگاه برداشته شدند (از `$_POST` هم خوانده نمی‌شوند) و جایشان یک سطر فقط‌خواندنی نشست.
  یک فروشگاه نباید بتواند بی‌صدا از سیاستی که به مشتری وعده داده شده خارج شود.
- **سه اطلاع‌رسانی:** بنر ۷ روزه + تذکر کلوز فرندز در صفحهٔ VIP؛ هشدار تاریخ‌دار **بالای**
  دکمه‌های خرید در صفحهٔ فرود؛ و `retention_days`/`expiry_notice` در خروجی REST تا اپ همان
  یک جمله را بگوید (یک متن، یک جا — `VipPostService::expiry_notice()`).
- **قابلیت «ذخیره» تازه ساخته شد.** اسناد چهار جا آن را «موجود» نوشته بودند و در کد نبود.
  حالا: جدول `vip_post_saves` (بدون `tenant_id`، مثل `vip_post_likes` — در whitelist
  `SchemaTest`)، `toggle_save`/`has_saved`/`mark_offline`/`saved_post_ids`/`saved_count`، و
  مسیرهای `POST /vip/posts/{id}/save`، `GET /vip/saved`.
- **نسخهٔ آفلاین:** `GET /vip/posts/{id}/offline` بایت‌ها را با لینک امضاشدهٔ بلندتر
  (`vip.offline_link_ttl`، پیش‌فرض ۳۶۰۰) به اپ می‌دهد تا کپی خصوصی داخل اپ بماند — فقط برای
  کاربر مستحق و فقط تا قبل از انقضا؛ دانلود، خودِ ذخیره را هم ثبت می‌کند (`offline_at`).
  **این فقط برای VIP است** (سطح امنیت سبک)؛ LMS دست‌نخورده ماند و مسیر آفلاین ندارد.
- **مهاجرت v17:** روز صفر → ۷ (ولی عدد مثبتی که مدیر ارشد انتخاب کرده دست‌نخورده می‌ماند)،
  `hide` → `delete`، و backfill تاریخ انقضا برای پست‌های منتشرشده — با خواندن و آپدیت
  سطر‌به‌سطر، چون UPDATE همبسته روی مترجم SQLite می‌شکند.
- تست: چهار سناریوی جدید در `VipChannelTest` → **۱۲۰۹ اظهارنظر در ۲۳ کیس**، ۲۳۵ فایل لینت.
  هر چهار سناریو با جهش‌دادن عمدی کد قرمز شدند (گیت دسترسیِ ذخیره، پیش‌فرض ۷ روز، متن هشدار).
- **دو چیز که فقط تأیید زنده پیدا کرد:** `TypeError` در `expiry_summary()` روی «پست جدید»
  (`$post` برابر `null` بود و فرم را از وسط می‌خواباند — امضا `?array` شد)، و یک متن متناقض
  در پیشنهاد خرید تک‌پست («این پست در کتابخانه‌تان می‌ماند») که دقیقاً خلاف سیاست جدید بود.



جلسه با «خواندن اسناد، بالا آوردن محیط، گزارش» شروع شد. تأیید زنده (بدون هیچ تغییری در کد)
نشان داد وضعیت ثبت‌شده درست است: **۱۱۷۲ اظهارنظر در ۲۳ کیس**، **۲۳۴ فایل لینت بدون خطا**،
**۶۹/۶۹ جدول** روی WP 7.0.4 / WC 11.0.1 / PHP 8.2.32، و **۲۸/۲۹ صفحهٔ ادمین با کد ۲۰۰ و صفر
Warning/Notice**. در `debug.log` هیچ خطای مربوط به افزونه و هیچ Fatal نبود (فقط سه خطای بوت
اولیهٔ خود ووکامرس روی SQLite).

سپس با تأیید کارفرما، پنج ناسازگاری سند/کد رفع شد. **هیچ منطق کدی تغییر نکرد**:

| مورد | قبل | بعد |
|---|---|---|
| وضعیت PR در همین سند | «PR شمارهٔ ۲ باز است» | PR شمارهٔ ۲ در ۱۵ اوت ادغام شد؛ کار جاری در PR شمارهٔ ۳ |
| `igbz-suite/README.md` (نصب) | «creates its **47** database tables» | ۶۹ جدول |
| `_devenv/AGENT-BRIEF.md` بخش ۴ | چهار ماژول (بدون `fx`)، فهرست ناقص سرویس‌ها، ۱۸ صفحهٔ ادمین | شش ماژول، فهرست کامل سرویس‌های هر ماژول، ۲۹ صفحه |
| `DESIGN-VIP.md` بخش ۴ | «هفت جدول جدید» | هفت جدول طراحی، **نه جدول** پیاده‌شده (`vip_threads`/`vip_messages`) |
| `languages/igbz-suite.pot` | ۱۸۸۷ رشته، استخراج ۱۴۰۵/۰۵/۲۴ | **۲۳۰۸ رشته** از ۲۰۶ فایل و ۲۸۵۶ ارجاع |

**ابزار جدید `_devenv/makepot.sh` + `_devenv/makepot.php`** (فقط ابزار توسعه، بخشی از محصول
نیست). `wp-cli i18n make-pot` در این سندباکس نصب‌شدنی نیست (نه Composer، نه دسترسی به
wordpress.org)، پس استخراج با tokenizer و همان php-wasm ای انجام می‌شود که تست‌ها را می‌برد:

- فقط `src/` را می‌خواند و همان پنج/هفت تابع gettext ای را می‌شناسد که واقعاً در کد هست
  (`__`, `_e`, `esc_html__`, `esc_html_e`, `esc_attr__`, `esc_attr_e`, `_n`).
- **فقط رشتهٔ لفظی** استخراج می‌شود؛ فراخوانی با متغیر یا الحاق، با هشدار روی stderr رد می‌شود —
  دقیقاً همان کاری که wp-cli می‌کند.
- قالب خروجی به فایل قبلی سنجاق شده (مرتب بر اساس msgid، هر ارجاع در یک خط، بدون wrap، بدون
  `msgctxt`) تا بازسازی، diff خوانا بدهد نه بازنویسی کل فایل.
- `--check` بدون نوشتن می‌گوید فایل کهنه است یا نه؛ `POT-Creation-Date` در مقایسه نادیده گرفته
  می‌شود تا فایل به‌روز، بی‌جهت مهر جدید نخورد.

**اعتبارسنجی ابزار (مهم):** استخراج‌کننده روی درخت کامیت `b917913` اجرا و با `.pot` همان کامیت
مقایسه شد. تنها اختلاف، سه رشتهٔ جدید و دو رشتهٔ تغییریافتهٔ `FunnelsPage` بود که از کامیت
`92df5da` در فایل ثبت‌شده جا مانده بودند — یعنی ابزار فایل قبلی را وفادارانه بازتولید می‌کند و
همان کهنگی مستندشده را هم لو می‌دهد. در بازسازی نهایی فقط دو msgid حذف شد
(`Post ID` و «Optional. Scope the funnel to a single post…») و بررسی شد که هر دو واقعاً از کد
رفته‌اند.



- قانون بنیادین کارفرما («هیچ تغییری بدون پلن مکتوب و تأیید صریح») در `b0cb0b0` ثبت شد.
- `b917913` — حذف کامل Canva (بالا).
- `6ca620c` — اصلاح شمارش جدول‌ها (۴۱)، تغییر نام مخزن به IGBZ-WP، رفع ارجاع‌های قدیمی شاخه.

### واسط پرداخت ارزی — فاز ۳b (تمام — این جلسه)
**آداپتور صرافی ایرانی (خرید خودکار USDT) + گزارش‌های مالی اپراتور:**

- **`FxRampInterface` + `HttpRampAdapter`:** قیمت لحظهٔ USDT/ریال، سفارش خرید، برداشت به
  آدرس کارت — همه‌چیز تنظیم‌پذیر (base URL و path ها و auth) با پیش‌فرض نوبیتکس.
- **`FxRampService`:** کرون روزانه (`ensure_card_funded`) وقتی موجودی کارت از
  `fx.ramp_min_card_balance` کمتر است، USDT می‌خرد (سقف `fx.ramp_max_irt_per_run`) و به
  `fx.ramp_usdt_deposit_address` برداشت می‌کند؛ همه در لجر اپراتور (tenant 0, reason `ramp`)
  ثبت می‌شود. دکمهٔ «Buy USDT now» در پنل (خرید دستی با `fx.ramp_manual_irt`). پیش‌فرض
  `fx.ramp_enabled = false` — عمداً، چون برداشت در صرافی‌های ایرانی معمولاً تأیید جدا می‌خواهد.
- **`FxReportsService` + بخش «Operator report» در پنل:** جمع شارژها (تعداد/ریال/دلار)،
  کارمزدها (از meta)، مصرف تسک/اشتراک، بازگشت‌ها، خریدهای ramp، قبض‌های پرداخت‌شده/نپرداخته در
  بازهٔ دلخواه.
- تست: ۳ سناریوی جدید → مجموع ۹۶۳ اظهارنظر در ۲۰ کیس؛ ۱۷۷ فایل لینت؛ تأیید زندهٔ پنل و تب
  تنظیمات.

### واسط پرداخت ارزی — فاز ۳ (تمام — این جلسه)
آداپتورهای پرداخت خودکار + تسویهٔ دستی:
- **PST.NET (اصلی):** `PstNetPayoutAdapter` — API رسمی صدور/شارژ/CVV/موجودی؛ کلیدهای
  `fx.pstnet_api_key`/`fx.pstnet_card_id`/`fx.pstnet_base_url`. کارت‌ها توسط **شرکت ثبت‌شدهٔ
  قبرس شمالی** نگهداری و پرداخت می‌شوند (لایهٔ قانونی حل است).
- **RedotPay (پایلوت):** `RedotPayPayoutAdapter` — کلیدهای
  `fx.redotpay_api_key`/`fx.redotpay_card_id`/`fx.redotpay_base_url`. به‌عنوان گزینهٔ دوم؛
  تعویض فقط با تغییر `fx.payout_provider`.
- **تسویهٔ دستی:** `settle_bill_manually()` + دکمه در صفحهٔ igbz-fx — ترمز ایمنی وقتی هر دو API
  خوابیدند (fallback همیشگی).
- **وبوک:** `POST /igbz/v1/fx/payout-webhook/{provider}` با توکن مشترک `fx.webhook_token`
  (قبول: `?token=` / `X-IGBZ-Token` / `Bearer`).
- تست: ۳ سناریوی جدید FxTest → مجموع ۹۶۳ اظهارنظر در ۲۰ کیس؛ ۱۷۷ فایل لینت؛ تأیید زندهٔ
  تب تنظیمات، صفحهٔ FX و گیت وبوک.

### واسط پرداخت ارزی — فاز ۲ (تمام — این جلسه)
روی فاز ۱: **قبض‌های ماهانه و تسویهٔ خودکار** (`FxBillingService`): برای هر `fx_account`
فعال، قبض ماهانه با قیمت سرویس (`manus_monthly`/`manychat_monthly`) ساخته می‌شود و کرون
روزانه قبض‌های due را با آداپتور payout تسویه می‌کند (بدهیِ کیف پول → وضعیت `unpaid` و پیام
شارژ؛ شکست payout → بازگشت وجه و ماندن `due`). **متراژ منی‌چت:** در `FunnelService::settle()`
هنگام تحویل موفق DM، از اعتبار همان مستأجر کسر می‌شود (idempotent به‌ازای هر hit). **REST
اپ:** مسیرهای `/igbz/v1/fx/balance|topup|ledger|prices|bills` (با احراز هویت فروشگاه). **پنل:**
بخش‌های Foreign accounts و Bills در صفحهٔ igbz-fx. تست: ۳ سناریوی جدید FxTest → مجموع ۹۴۴
اظهارنظر در ۲۰ کیس. نکتهٔ معماری: ترتیب ماژول‌ها طوری شد که `fx` قبل از `rest_api` بایند شود
تا کنترلر FX بتواند سرویس‌ها را پیدا کند.

باقی‌مانده برای فاز ۳: آداپتور واقعی پرداخت ارزی (کارت مجازی/USDT/صرافی) و انتخاب ارائه‌دهنده.

### حذف صف `jobs` (تمام — `2f9e05b`)
جدول `jobs` یک صف عمومی بود که **هیچ‌کس در آن کار نمی‌گذاشت** و هیچ اجراکننده‌ای نداشت؛ تنها کد
مرتبط، جاروی روزانه‌ای بود که ردیف‌های ناموجود را پاک می‌کرد. هر زیرسیستم از قبل صف بادوام خودش
را دارد (`ig_product_intake`, `ig_content`, `ig_funnel_hits`). طبق دستور «واقعی‌اش کن یا حذفش کن»
حذف شد. DDL در تاریخچهٔ گیت باقی است.

### سند اتوماسیون هوشمند + محیط تست مرورگری (جلسهٔ ۱۴۰۵/۰۶/۳۱ — `66a5924`)

- **`DESIGN-AUTOMATION.md`** — متن کامل (۴۱ بخش) به دستور کارفرما عیناً و بدون تغییر ثبت و پوش
  شد: معماری اتوماسیون هوشمند فروشگاه با هستهٔ **n8n** · ووکامرس منبع حقیقت دادهٔ تجاری ·
  پنج Agent (SEO/Marketing/CRM/Support/Instagram) · **Approval Center** برای سه عملیات حساس
  (تغییر قیمت، Refund، انتشار اینستاگرام) · PostgreSQL + pgvector · ۱۰ وردفلوی n8n
  (WF-001 تا WF-010) · ۸ فاز پیاده‌سازی · MVP بدون اینستاگرام. **هنوز روی `main` نیست**
  (شاخهٔ `arena/01a025f2-igbz-wp`).
- **محیط تست مرورگری** (`/home/user/browser-test`، خارج از ریپو) — سرور وردپرس همان `_devenv`
  است؛ Playwright 1.62.1 + Chromium 149 نصب و پیکربندی شد (باینری از کانال npm، چون CDNهای
  دانلود مرورگر و مخازن apt بلاک‌اند). **فقط محیط؛ هیچ تستی نوشته نشده** — به دستور صریح
  کارفرما. دستورات سریع: `HANDOFF-PROMPT.md §۷`.
- در همین جلسه بحث پادو روش «یک‌به‌یک بررسی ← جمع‌بندی ← پلن ← تأیید ← اجرا» را گرفت و
  تصحیح اولیه در `DESIGN-PADO.md §۶` ثبت شد: **مرحلهٔ اول پادو = طراحی قالب بعد از نصب
  فروشگاه.** جمع‌بندی پیشنهادی مرحلهٔ ۱ ارائه شد ولی هنوز تأیید نشده.


---

## ۵. ساختار کد

```
igbz-suite/
  igbz-suite.php            نقطهٔ ورود؛ IGBZ_DB_VERSION = 17 (خط ۲۲)
  src/Support/              Schema (۷۲ جدول), Activator, Db, Settings, Cron, Logger, Modules,
                            Http, Money, Admin (Menu, SettingsPage, StatusPage), Capabilities
  src/Modules/
    MultiTenant/            مستأجر، کیف پول، پرداخت (۸ درگاه بانکی + درگاه‌های پایه)، BNPL،
                            درگاه مرکزی escrow، همکاری فروش، LMS، لجستیک/اپ پیک، مارکت‌پلیس،
                            سئو، ترجمه/i18n، دامنه، گیمیفیکیشن، احراز هویت قانونی (شاهکار)
      Payments/             AbstractIpgGateway + Sadad/AsanPardakht/Parsian/IranKish/Mellat/
                            Saman/Pasargad/Sepehr + Zarinpal/IDPay/NextPay/Pay.ir/HttpPsp/
                            BalePay/NowPayments + PaymentService
      MasterPayment/        درگاه مرکزی escrow (آزادسازی، اختلاف، توافقنامهٔ دیجیتال، برداشت)
      Logistics/            CourierService، LabelPrintingService، ShippingAdapterInterface
      Domain/               DomainService، WebPresenceService
      Bnpl/                 ارائه‌دهنده‌های اقساطی (داخلی + اسنپ‌پی/تارا/دیجی‌پی)
      Otp/                  OTP + NationalIdVerifier (شاهکار)
      Seo/ Marketplace/ Translation/ Gamification/ Lms/ Affiliate/ Plans/ Wallet/ Frontend/
    Instagram/
      Services/             Manus, ManyChat, قیف، ثبت محصول، PostIdentity، متراژ منوس
      Gateways/             ManyChatClient + ChatPlaceClient (سوئیچ dm.provider)
      Vip/                  هشت سرویس کانال VIP (ذخیره + نسخهٔ آفلاین در VipSocialService)
      AiStudio/             استودیوی AI (پل موقت تا پادو)
      Admin/                IntakePage, ContentPage, FunnelsPage, VipPage, AiStudioPage,
                            GiveawayPage, ...
      Webhooks/             ManyChatWebhook
    Hub/                    دایرکتوری، ثبت‌نام، بلوک محتوا
    RestApi/                کنترلرهای اپ موبایل: Account, Auth, Catalog, Device, ProductIntake,
                            StoreAdmin, Vip, VipAdmin, Fx, AiStudio, Courier, Domain
    Fx/                     واسط پرداخت ارزی (شارژ، نرخ، قبض، payout، ramp، گزارش اپراتور)
  tests/                    ۲۳ کیس، اجرا با tests/run.php
  languages/igbz-suite.pot  ۲۳۱۸ رشته (بازسازی ۱۴۰۵/۰۵/۲۶ با `bash _devenv/makepot.sh`)
```

**۲۱۵ فایل PHP** در `src/`. **۲۹ صفحهٔ ادمین** زیر منوی یکپارچهٔ IGBZ. **۱۳۶ فراخوانی
`register_rest_route`** در دو فضای نام: `igbz/v1` و `igbz-hub/v1` (**۱۴۵ مسیر ثبت‌شده**:
۱۳۰ + ۱۵).

> شمار مسیرها را از سایت زنده بگیرید، نه با تخمین:
> `curl -s '…/?rest_route=/igbz/v1' | python3 -c "import sys,json;print(len(json.load(sys.stdin)['routes']))"`
> عدد فراخوانی (۱۳۶) با عدد مسیر (۱۴۵) یکی نیست: چند فراخوانی داخل حلقه‌اند و چند مسیر هم
> خودکار ثبت می‌شوند. عدد ۱۹۹ که تا ۱۴۰۵/۰۵/۳۰ در اسناد بود، از هیچ شمارشی درنمی‌آمد.

---

## ۵.۵ ⚖️ قانون کلی مدیریت درخواست — مقیاس چندمستأجری

> **تصمیم کارفرما (۱۴۰۵/۰۵/۳۱). این یک قاعدهٔ فراگیر است، نه یک فاز.**
>
> «ما یک سرویس‌دهندهٔ فروشگاهی هستیم که ممکنه صدها یا هزارها فروشگاه روی سرور ما قرار
> گرفته باشند، که هر کدومشون هم ممکنه صدها بلکه هزارها کاربر داشته باشه. برای همین مدیریت
> درخواست‌ها خیلی مهمه. باید براش یک قانون کلی طراحی بشه که **در تمام موارد طراحی پروژه
> رعایت بشه**.»

### مقیاس هدف

```
صدها تا هزاران فروشگاه  ×  صدها تا هزاران کاربر هر فروشگاه
        = ورودیِ هم‌زمانی که هیچ طراحیِ «تک‌فروشگاهی» تحملش را ندارد
```

**پیامد فوری:** هر جا در طراحی نوشته شده «برای هر فروشگاه یک …» یا «در هر درخواست …»،
باید در این مقیاس ضرب شود. چیزی که برای ۱۰ فروشگاه بی‌ضرر است، در ۱۰۰۰ فروشگاه سیستم را
می‌خواباند.

### 📚 مبنای استاندارد — تحقیق ۱۴۰۵/۰۵/۳۱

کارفرما: «به دانش خودت اکتفا نکن؛ برترین مقالات و قواعد استاندارد و مواردی که جدیداً نشر
پیدا کرده رو بررسی کن و یه طراحی اصولی و استاندارد انجام بده.»

منابع بررسی‌شده و آنچه از هرکدام گرفته می‌شود:

| منبع | نکتهٔ کلیدی برای ما |
|---|---|
| **OWASP API Security Top 10 (2023)** — `API4: Unrestricted Resource Consumption` | نسخهٔ ۲۰۱۹ اسمش «Lack of Rate Limiting» بود؛ در ۲۰۲۳ عمداً گسترده شد تا **هزینهٔ عملیاتی** را هم پوشش دهد. صریح می‌گوید منابعی مثل **پیامک و ایمیل که per-request پول می‌گیرند** جزو همین ریسک‌اند — یعنی همان شکاف OTP ما. |
| **OWASP `API6: Unrestricted Access to Sensitive Business Flows`** (تازه در ۲۰۲۳) | ریسکی که «باگ نیست»: جریان کاری سالم که **خودکارسازی‌شده** سوءاستفاده می‌شود. برای ما: ثبت‌نام، ارسال OTP، ثبت سفارش. |
| **IETF `draft-ietf-httpapi-ratelimit-headers-11`** | هنوز Internet-Draft است؛ دو فیلد `RateLimit` و `RateLimit-Policy` دارد و در حضور `Retry-After`، کلاینت باید دومی را مقدم بداند. |
| **RFC 6585 / RFC 9110** | **۴۲۹** برای «این کلاینت از سهمیه گذشت» · **۵۰۳** برای اشباع کل سرور · **۲۰۲** وقتی کار برای پردازش پذیرفته شده؛ انتخاب کد نهایی باید با معنای واقعی پاسخ تطبیق داده شود. |
| **الگوی Noisy Neighbor** (چند منبع ۲۰۲۵–۲۰۲۶) | در مدل «یک دیتابیس، فیلتر با `tenant_id`» — که مدل ماست — این مشکل **شدیدترین** حالت را دارد. |
| **Action Scheduler** (وابستگی رایج ووکامرس) | صف استاندارد اکوسیستم برای ذخیرهٔ بادوام، کنترل هم‌زمانی و پیگیری شکست؛ در پشتهٔ baseline در دسترس است، اما `igbz-suite/src` هیچ استفاده‌ای از آن ندارد. |
| **WP-Cron در مقیاس** (مستندات وردپرس + چند منبع ۲۰۲۶) | WP-Cron زمان‌بند واقعی نیست؛ روی بازدید صفحه اجرا می‌شود. در چندسایتی **باید** با کرون سیستمی جایگزین شود. `wp-cron.php` عمومی است و **بردار حملهٔ DDoS لایه ۷** است. |

**چهار چیزی که این تحقیق تغییر داد یا اضافه کرد:**

۱. **قاعدهٔ ۵ باید عدد و هدر استاندارد بدهد**، نه فقط «رد کن». پاسخ باید `429` +
   `Retry-After` باشد، و برای کارهای صف‌شونده `202`.
۲. **«جریان کاری حساس» یک دستهٔ جدا از «نرخ» است** (OWASP API6). محدودیت نرخِ صرف جلوی
   ربات آهسته‌ای که هزار ثبت‌نام جعلی می‌سازد را نمی‌گیرد.
۳. **Action Scheduler از قبل در دسترس است** — قاعدهٔ ۲ نباید صف تازه اختراع کند بدون
   اینکه اول این بررسی شود.
۴. **`DISABLE_WP_CRON` در README ما توصیه شده و `StatusPage` هم هشدارش را می‌دهد** —
   یعنی نیمی از راه رفته شده. آنچه نیست، **دسته‌ای و ادامه‌پذیر بودنِ** خودِ کارهاست.

### هفت قاعده‌ای که در هر طراحی باید رعایت شود

**۱. هیچ کار سنگینی در مسیر درخواست کاربر نیست.**
تماس شبکه‌ای به سرویس بیرونی، تولید فایل، پردازش تصویر، و فراخوانی مدل زبانی **هرگز**
نباید کاربر را منتظر نگه دارند. یا صف، یا پس‌زمینه.

**۲. هر کار پس‌زمینه، صفِ بادوام دارد — نه اجرای درجا.**
الگوی موجود پروژه همین است و باید ادامه پیدا کند: `ig_product_intake`, `ig_content`,
`ig_funnel_hits`. (جدول عمومی `jobs` قبلاً حذف شد چون بی‌مصرف بود؛ این قاعده به معنای
برگرداندنش نیست، بلکه یعنی هر زیرسیستم صف خودش را داشته باشد.)

⚠️ **قبل از ساختن هر صف تازه، `Action Scheduler` بررسی شود** — کتابخانهٔ صف اکوسیستم
ووکامرس که در پشتهٔ baseline در دسترس است و افزونه استفاده‌اش نمی‌کند. ذخیرهٔ بادوام،
کنترل هم‌زمانی، پیگیری شکست و رابط بازبینی را آماده دارد.

**۳. هیچ کوئری‌ای بدون مرز مستأجر و بدون سقف نتیجه اجرا نمی‌شود.**
هر `SELECT` روی جدول‌های چندمستأجری باید `tenant_id` و `LIMIT` داشته باشد. یک کوئری بدون
سقف که در یک فروشگاه بی‌ضرر است، با هزار فروشگاه دیتابیس را قفل می‌کند.

**۴. هر جدول پرترافیک، ایندکس متناسب با الگوی خواندنش دارد.**
مخصوصاً ستون‌هایی که در `WHERE` تکرار می‌شوند: `tenant_id`, `phone`, `ip_hash`,
`created_at`, `status`.

**۵. محدودیت نرخ در **بک‌اند**، در سطح **سرویس**، با پاسخ **استاندارد**.**
این همان قاعده‌ای است که در `DESIGN-LEGAL-AUTH.md §۷.۵.۵` تثبیت شد و اینجا **فراگیر**
می‌شود: فرانت‌اند فقط بازتاب می‌دهد، تصمیم نمی‌گیرد. محدودیت باید در تابع سرویس بنشیند
(مثل `OtpService::send()`) نه در کنترلر — چون کنترلرها تکثیر می‌شوند و صف‌ها و اپ موبایل
از آنها رد نمی‌شوند.

پاسخ هم باید استاندارد باشد، نه دلخواه:

| وضعیت | کد | هدر |
|---|---|---|
| این کلاینت از سهمیه گذشت | **۴۲۹** | `Retry-After` طبق قرارداد پروژه؛ فیلدهای draft اختیاری |
| کار واقعاً برای پردازش پذیرفته و صف شد | **۲۰۲** | نشانی وضعیت/رهگیری و در صورت نیاز راهنمای polling |
| کل سرور اشباع است | **۵۰۳** | `Retry-After` در صورت امکان |
| منع مجوز یا سیاست دائمی | **۴۰۳** | برای rate-limit موقت استفاده نشود |

**۵.۱ جریان‌های کاری حساس، دستهٔ جداگانه‌اند** (OWASP API6:2023). محدودیت نرخِ صرف کافی
نیست: رباتی که آهسته ولی پیوسته هزار ثبت‌نام جعلی می‌سازد از هیچ سقف نرخی رد نمی‌شود.
ثبت‌نام، ارسال OTP، ثبت سفارش و پرداخت باید **علاوه بر** سقف نرخ، محافظ جریان کاری داشته
باشند.

**۶. کرون‌ها نباید همهٔ فروشگاه‌ها را در یک اجرا پردازش کنند.**
با هزار فروشگاه، یک کرون «برای هر فروشگاه انجام بده» از مهلت PHP رد می‌شود و **بی‌صدا**
نیمه‌کاره می‌ماند. کار باید **دسته‌ای** و **ادامه‌پذیر** باشد: هر اجرا سهم خودش را بردارد
و نشانهٔ ادامه ذخیره کند.

دو نکتهٔ استاندارد که تحقیق اضافه کرد:
- **WP-Cron زمان‌بند واقعی نیست** — روی بازدید صفحه اجرا می‌شود. `DISABLE_WP_CRON` +
  کرون سیستمی **الزامی** است، نه توصیه. (README ما این را دارد و `StatusPage.php:236`
  هشدارش را می‌دهد؛ نیمی از راه رفته شده.)
- **`wp-cron.php` عمومی است و بردار حملهٔ DDoS لایهٔ ۷ است** — با `DISABLE_WP_CRON` باید
  دسترسی بیرونی به آن هم در nginx بسته شود.

**۷. سقف‌ها تنظیم‌شدنی‌اند، نه ثابت در کد.**
عددی که برای ۱۰ فروشگاه درست است برای ۱۰۰۰ تا غلط است. هر سقف باید از `Settings` بیاید با
پیش‌فرض معقول.

### نسبت با تصمیم‌های دیگر

- **ویرا (لایهٔ ۱۳):** ویرا ته قیف است و در مسیر همهٔ درخواست‌ها نمی‌نشیند — این دقیقاً
  به همین دلیل مقیاس است. (`DESIGN-DEPLOY-VIRA.md §۱۴.۵`)
- **nginx به‌جای پراکسی PHP:** تصمیم بند ۴.۴ سند استقرار، دلیل اصلی‌اش همین بود — نگه
  نداشتن پروسهٔ PHP برای پاسخ‌های طولانی.
- **پادو ابری است:** پس روی این سرور باری ندارد. (`DESIGN-DEPLOY-VIRA.md §۱۱`)
- **گزارش‌های امنیتی:** با این مقیاس، تجمیع و کف‌گیری واجب است وگرنه سیل هشدار می‌آید.

### ⚠️ وضعیت ممیزی جاری

کد در ممیزی ۱۴۰۵/۰۶/۰۵ با این قواعد سنجیده شد و چند نقض قطعی ثبت شد: صفر استفاده از
Action Scheduler، cleanup نامحدود، فهرست‌های رشدپذیر بدون pagination، batchهای بدون
requeue/fairness کامل، شمارندهٔ transient غیراتمیک و نبود آزمون ظرفیت. شرح در بخش ۱۰.۳ و
نیازهای DAT/QUE/OPS در `REQUIREMENTS-TRACEABILITY.md` است. تا بسته‌شدن آن‌ها پروژه با
قواعد این بخش هم‌خوان کامل نیست.

### 🔎 تحقیق پروژه‌های بیرونی — نتیجه (۱۴۰۵/۰۵/۳۱)

چهار مورد بررسی شد. خلاصهٔ کامل هرکدام در `امنیت و مراقبت/منابع/`.

| پروژه | مجوز | نتیجه |
|---|---|---|
| `Buaskoor-AI-Commerce-Platform` | ❌ کپی‌رایت انحصاری | ورک‌فلوی n8n تک‌فروشگاه. پرامپت‌نویسی‌اش خوب بود، کدش قابل استفاده نیست |
| `AI-E-Commerce-Automation-n8n` (Usman) | نامشخص | ضعیف. README به فایل‌های ناموجود ارجاع می‌دهد، ادعای Shopify دروغ است، **وب‌هوک بدون احراز هویت** و شناسهٔ سه base ایرتیبل نشت کرده |
| **`nitin27may/e-commerce-agents`** | ✅ **MIT** | ⭐ **مفیدترین.** ۳۳۱ فایل پایتون، ۸۴ فایل تست، `hitl.py` برای تأیید انسانی |
| **MCP + Abilities ووکامرس** | همراه افزونه | ⭐ **از قبل روی سرور ماست** و نمی‌دانستیم |

**دو یافتهٔ عملی:**

۱. ❌ **MCP و هشت قابلیت آمادهٔ ووکامرس** (`ProductsQuery`, `ProductCreate/Update/Delete`,
   `OrdersQuery`, `OrderUpdateStatus`, `OrderAddNote`, `AbstractDomainAbility`) در همان
   زیپ ووکامرس ۱۱.۰.۱ ما هستند — ولی **بنا به دستور کارفرما (۱۴۰۶/۰۵/۳۱) از آن استفاده نمی‌کنیم**:
   احراز هویتش با کلید REST ووکامرس است (`X-MCP-API-Key`) — یک مسیر موازی که
   `CoreSurfaceGuard` را دور می‌زند و تا حل این شکاف امنیتی در سند امنیت، پرچم
   `mcp_integration` خاموش می‌ماند. در طراحی پادو به‌عنوان منبع/ابزار ارجاع داده نشود.

۲. **چهار گاردریل که در سند پادؤ ما نیست** و در پروژهٔ MIT هست: دفاع در برابر تزریق
   پرامپت · سقف هزینهٔ توکن · پالایش خروجی · grounding (مهار توهم).

**برای بعد:** `nexscope-ai/eCommerce-Skills` بررسی نشده — اسکیل‌های آمادهٔ فرمت
`SKILL.md`، همان استانداردی که سند پادو پذیرفته.

### 📌 ترتیب کار — دستور کارفرما (۱۴۰۵/۰۵/۳۱)

> «بعد از اتمام این بخش و تعیین تکلیف کارهای باقیمانده، **اولین کاری که می‌کنی طراحی
> همین بخش باشه** — یعنی بخش امنیت و این بخش درخواست‌ها.»

پس ترتیب قطعی این است:

۱. **اول:** تعیین تکلیف کارهای باقی‌مانده (سؤال‌های باز فعلی). ✅ **انجام شد
   ۱۴۰۵/۰۵/۳۱.**
۲. **کار اول طراحی:** **سند امنیت + مدیریت درخواست‌ها** — شامل ۱۳ لایهٔ امنیتی،
   اسکیل‌ها و تنظیمات امنیتی ویرا، و پیاده‌سازی همین هفت قاعده.
   🟡 **بخشی انجام شد:** سه شکاف اجرایی `DESIGN-GAPS-FIX.md` در کامیت `06cbc0b` بسته شد
   (مسیرهای هسته · کول‌داون OTP · قرارداد حقوقی). **ولی خودِ سند طراحی امنیت — ۱۳ لایه
   و معماری کامل — هنوز نوشته نشده.** کارفرما گفته بود «۱۳ لایه رو در بخش امنیت توضیح
   می‌دم»؛ آن توضیح هنوز داده نشده و سند منتظر آن است.
۳. **کار دوم:** **پادو** — «می‌ریم سراغ پادو و انجامش می‌دیم و **تمومش می‌کنیم**»
   (تصمیم ۱۴۰۵/۰۵/۳۱). یکجا تا پایان، نه تکه‌تکه. بازبینی بدهی سند
   (`DESIGN-GAPS-FIX.md §۴`) ابتدای همین کار.
۴. **ترجمهٔ فارسی** — ۲۰۹۰ رشته، دامنه‌اش مشخص است.
۵. **بازبینی مقیاس‌پذیری کد** با هفت قاعدهٔ همین بند.
۶. **شناسهٔ کاربر وردپرسِ مدیر ارشد** — یک عدد از کارفرما.
۷. **پاک‌سازی ۱۰ اتصال نمونهٔ `sk-demo`** در ویرا — کار پنج‌دقیقه‌ای.
۸. **اپ‌های موبایل** آخر از همه — بعد از تکمیل بک‌اند و فرانت‌اند.

> **ترتیب بند ۴ تا ۷ را کارفرما در ۱۴۰۵/۰۵/۳۱ تأیید کرد** («موارد بند ب هم با همین
> اولویتی که نوشتی انجام می‌دیم»). **کلیدهای واقعی سرویس‌ها** در این ترتیب نیست چون دست
> کارفراست، نه عامل.

**روش الزامی برای آن سند** (دستور کارفرما): تکیه بر **مقالات برتر، قواعد استاندارد، و
منابع تازه‌منتشرشده** — نه دانش عمومی عامل. جدول منابع بالا نقطهٔ شروع است و باید هنگام
نگارش سند تکمیل شود.

---

## ۶. قواعد فنی که باید رعایت شوند

اینها از اشتباهات واقعی درآمده‌اند، نه از سلیقه:

- ⚖️ **قانون مدیریت درخواست (بند ۵.۵) بر همهٔ این قواعد مقدم است** و در هر طراحی تازه
  باید رعایت شود: بدون کار سنگین در مسیر درخواست · صف بادوام · `tenant_id` و `LIMIT` در
  هر کوئری · محدودیت نرخ در سرویس بک‌اند · کرون دسته‌ای و ادامه‌پذیر · سقف تنظیم‌شدنی.

- **SQL همیشه MySQL** بنویسید؛ فقط برای `FOR UPDATE` و `GET_LOCK` روی `Db::is_sqlite()` شاخه بزنید
  (نصب‌های Playground از SQLite استفاده می‌کنند).
- `Db::insert/update/delete` نام **کوتاه** جدول می‌گیرند؛ `row/results/query` نام **پیشونددار**.
- `Logger::info($channel, $message, $context)` — پارامتر دوم **رشته** است. آرایه دادن `TypeError`
  می‌دهد و کل درخواست را می‌خواباند.
- **هرگز `__()` قبل از `init`** صدا نزنید. فقط رشتهٔ لفظی داخل `__()`.
- `dbDelta` به `PRIMARY KEY  (` با **دو فاصله** نیاز دارد.
- `wp_get_referer()` وقتی referer با URL فعلی یکی باشد **`false` برمی‌گرداند** — دقیقاً حالت فرمی
  که به خودش POST می‌کند. مقصد را صریح پاس بدهید.
- `$data['key'] ?? $default` داخل شرطی که شاخهٔ درستش دوباره `$data['key']` را می‌خواند، هشدار
  می‌دهد. قبل از اعتماد، این الگو را grep کنید.
- **UPDATE با زیرکوئری همبسته** روی مترجم SQLite می‌شکند؛ برای مجموعه‌های کوچک، یک خواندن و
  سپس `Db::update()` سطر‌به‌سطر بنویسید.
- **آرایهٔ انجمنی را با کلید بخوانید، نه با list destructuring.** `[ $a, $b ] = $assoc`
  بی‌صدا دو `null` می‌دهد و همه‌چیز را «شکست» می‌خواند؛ چهار درگاه بانکی دقیقاً با همین
  اشتباه منتشر شده بودند (شرح در بخش ۴). اگر متدی آرایهٔ انجمنی برمی‌گرداند، در PHPDoc
  شکلش را با `@return array{…}` بنویسید.
- **جدول‌ها را با `$sql[] = "CREATE TABLE` بشمارید،** نه با `CREATE TABLE` خالی. عبارت خالی یک
  کامنت توضیحی بالای `Schema.php` را هم می‌شمارد و عدد را یکی بیشتر نشان می‌دهد. عدد درست را
  `Schema::tables()` و health check می‌دهند؛ اگر گزارش شما با آن‌ها نخواند، گزارش شما غلط است.

### دربارهٔ تست

- **سوئیتی که بار اول سبز شود، ثابت نکرده کار می‌کند.** کد را عمداً جهش بدهید و ببینید تست
  واقعاً قرمز می‌شود. این بار همین کار یک باگ نهفتهٔ چندماهه را لو داد.
- **دابل‌های درون‌حافظه‌ای خطرناک‌اند:** اگر دابل ترتیب «درست» را hardcode کند، با هر دو نسخهٔ
  درست و غلط موافق است. دابل باید کلید مرتب‌سازی را **از خود SQL** استخراج کند.
- **فقط مسیر شکست را نسنجید.** تستی با مضمون «بدون شبکه ⟵ شکست» روی کدی که هیچ‌وقت هیچ
  پاسخی را نمی‌تواند بخواند هم سبز می‌ماند. برای هر آداپتور، یک **پاسخ موفق واقعی** را از
  قلاب HTTP هارنس (`igbz_test_queue_http()`) عبور بدهید و اصرار کنید که نتیجه موفق باشد.
  باگ چهار درگاه بانکی (بخش ۴) ماه‌ها زیر پنج تستِ «شکست» زنده ماند.
- **نبودِ Warning دلیل سالم‌بودن نیست** — فقط یعنی آن مسیر اجرا نشده. از چهار درگاه معیوب،
  سه‌تا Warning می‌دادند و چهارمی ساکت بود چون هیچ تستی صدایش نمی‌زد.
- هارنس تست **بایندینگ‌های کانتینر ماژول‌ها را ثبت نمی‌کند**؛ گراف سرویس را دستی بسازید.
- کیس جدید را در آرایهٔ `$cases` در `tests/run.php` ثبت کنید، و متد تست را در `run()` همان کلاس
  صدا بزنید (کشف خودکار متد وجود ندارد).
- تست‌ها باید `igbz_test_reset_settings()` را صدا بزنند.

---

## ۷. محیط توسعهٔ آفلاین

پوشهٔ `_devenv/` — ابزار توسعه، بخشی از محصول نیست.

```bash
bash _devenv/setup.sh      # ~۲۳ ثانیه؛ فقط اگر .work پاک شده باشد
bash _devenv/run.sh        # http://127.0.0.1:9400، ورود خودکار مدیر؛ --port/--php
bash _devenv/test.sh       # تست واحد + لینت؛ --lint-only/--tests-only
bash _devenv/makepot.sh    # بازسازی languages/igbz-suite.pot؛ --check فقط گزارش کهنگی
```

- `_devenv/.work/` در گیت نیست و **بین جلسه‌ها پاک می‌شود**؛ اول `ls _devenv/.work/` را ببینید.
- `.work/mu/` باید **دقیقاً سه فایل** داشته باشد: `000-activate.php`, `010-enable-modules.php`,
  `020-healthcheck.php`. فایل موقتی که برای آزمایش می‌سازید را **قبل از کامیت پاک کنید**.
- بالا آمدن سایت ۳۰ تا ۹۰ ثانیه طول می‌کشد؛ یک ۵۰۲ قبل از اولین ۲۰۰ طبیعی است.
- **روی نصب تازه، ووکامرس صفحات مدیر را با ویزارد راه‌اندازی می‌دزدد.** یک بار این را بزنید:
  `admin.php?page=wc-admin&path=%2Fsetup-wizard&step=skip-guided-setup`
- لاگ خطا: جدیدترین `/tmp/node-playground-cli-site-*/wordpress/wp-content/debug.log`
- زیپ‌های هستهٔ تراکم‌شده: `wordpress-7.0.4.zip`, `woocommerce-11.0.1.zip` (wordpress.org مسدود است).

### تأیید زنده، نه فقط تست واحد

چند باگ فقط با کلیک واقعی پیدا شدند و هیچ تست واحدی نمی‌دیدشان: مسیر بازگشت `wp_get_referer()`،
`TypeError` در لاگر که کل مسیر بازگشت وجه را خواباند، و وارونگی اولویت قیف. **بعد از هر تغییر
معنادار، روی سایت زنده کلیک کنید.**

### محیط تست مرورگری (`/home/user/browser-test` — خارج از ریپو، ۱۴۰۵/۰۶/۳۱)

- **محدودیت شبکهٔ سندباکس:** فقط `registry.npmjs.org` باز است؛ `cdn.playwright.dev` ·
  `playwright.azureedge.net` · `registry.npmmirror.com` · `deb.debian.org` ·
  `storage.googleapis.com` · `wordpress.org` بلاک ⇒ `npx playwright install` و `apt install` ممکن نیست.
- **راه‌حل:** `playwright@1.62.1` + `@playwright/test@1.62.1` از npm، و باینری
  **Chromium 149.0.7827.0** از `@sparticuz/chromium@149.0.0` (باینری + libهای اشتراکی + فونت،
  همه داخل تاربال npm). libها از `/tmp/al2023/lib` با `LD_LIBRARY_PATH`.
- **اجرا:** `cd /home/user/browser-test && source ./env.sh` (استخراج idempotent به `/tmp`)،
  راستی‌آزمایی: `node verify-env.cjs`.
- `playwright.config.ts` آماده است: `workers=1` (سرور ۱ worker دارد) · `timeout=120s` ·
  `baseURL=$WP_BASE_URL` (پیش‌فرض `http://127.0.0.1:9400`) · `executablePath=/tmp/chromium`.
- **هیچ فایل `*.spec.ts` نوشته نشده** — به دستور کارفرما؛ تست‌ها فقط با دستور او اضافه می‌شوند.


---

## ۷.۵ ویرا — ابزار عاملِ خودمان (`vira/`)

**چیست:** یک دستیار عامل که روی دستگاه خود کارفرما اجرا می‌شود، ابزار واقعی دارد (فایل،
فرمان، جستجو، وب، گیت)، اسکیل و پلاگین و MCP می‌گیرد، و با **هر پرووایدری** کار می‌کند.
قرار است بعداً هستهٔ **پادو** (AI مرکزی فروشگاه) از دلش دربیاید.

**نسخه:** ۰.۹.۷ · **جا:** پوشهٔ `vira/` همین مخزن · **اجرا:** `.\vira.cmd` (ویندوز) یا
`./vira.sh` — از ریشهٔ مخزن، بدون `cd`.

| لایه | چه دارد |
|---|---|
| هسته | حلقهٔ عامل، ۱۵ ابزار + `skill`/`task`/MCP، سه حالت مجوز و دروازهٔ تأیید، چک‌پوینت، فشرده‌سازی کانتکست، نشست‌ها |
| پرووایدر | پروتکل OpenAI و Anthropic، واکشی فهرست مدل، پرووایدر سازگار (OpenRouter/Ollama/LM Studio) |
| **هاب** | چند اتصال هم‌زمان، رجیستری مدل، نُه راهبرد مسیریابی، تشخیص جنس درخواست، یادگیری از نتیجه، مدارشکن، سه سقف هزینه، کش، **عیب‌یاب چهارپله**، خروجی سازگار با OpenAI |
| رابط | بازسازی روی طرح تأییدشدهٔ کارفرما، **دوزبانه (فا/انگلیسی)**، آیکون‌های FA Pro، منوی راست‌کلیک گفتگوها، صفحه‌های تمام‌قد، نوزده تب تنظیمات |
| گیت | نوار همیشگی زیر کامپوزر؛ **مخزن و شاخه در ابتدای هر گفتگو انتخاب و با اولین پیام قفل می‌شوند**؛ فهرست مخزن‌ها از `gh repo list` (همان‌هایی که مجوز داده شده) |
| ایمنی | سندباکس کانتینری **پیش‌فرض روشن**، شکستِ بسته (بدون داکر، فرمان اجرا نمی‌شود)، توکن هرگز در خط فرمان |

**تست:** ۳۷۵ تست واحد + ۴۴ آزمون زنده روی سرور واقعی.

**۰.۹.۴ (۱۴۰۵/۰۵/۲۸):** شش تبِ تنظیمات هاب به یک **صفحهٔ تمام‌قد «هاب پرووایدر»** با
چهار نما رفت (توپولوژی/اتصال‌ها/ترکیب‌ها/سلامت)، کاتالوگ کارتی همیشه‌نمایان، ویزاردهای
چهارگامی اتصال و ترکیب، **ریست در سه سطح** (مدل/اتصال/کل) و رفع toast زیر مودال —
طرح و گزارش کامل: `vira/DESIGN-PROVIDER-UI.md`.

**۰.۹.۷ (۱۴۰۵/۰۵/۳۰):** «هاب کار نمی‌کند» حل شد و رابط به مرجع نزدیک شد. **ریشهٔ
اصلی:** `dispatcherFor` در `net.js` فقط آرگومان را می‌خواند و `effectiveProxy` عملاً
**کد مرده** بود، پس `HTTPS_PROXY` هرگز دیده نمی‌شد؛ حالت تک‌واحد با یک تماس شانس رد شدن
داشت ولی هاب که پیش از هر پاسخ چند تماس می‌زند، پشت تحریم می‌ماند — همان سرنخ کارفرما
(«تک‌واحد کار می‌کند»). به‌علاوه: `createProvider` تک‌واحد هم پراکسی گرفت؛ گیت بینایی
پیام صریح می‌دهد (قبلاً هر پیام تصویردار با «هیچ مدلی در دسترس نیست» رد می‌شد)؛ پیام
خطای نهایی می‌گوید چند مسیر آزموده شد و هرکدام چه داد؛ **تونل**: پارس `ss://` که همهٔ
کانفیگ‌های Shadowsocks را بی‌صدا دور می‌ریخت اصلاح شد، صبر ثابت ۳۵۰ms جای خود را به
پولینگ درگاه داد، مقصد آزمون چندتایی شد، و **تست دومرحله‌ای** آمد (مرحلهٔ ۲ فقط روی
بازماندگان، با سرویس واقعی؛ ۴۰۱ یعنی تونل سالم است) با وضعیت سه‌حالته و نوار پیشرفت و
دکمهٔ لغو؛ **رابط**: صفحهٔ هاب تمام‌قد شد (`.page-inner.wide`)، ستون ناوبری دوم به نوار
تب افقی تبدیل شد، یال‌های توپولوژی سه‌حالته و نقطه‌چین متحرک شدند و گره‌ها مستطیل کوچک؛
دکمهٔ «برو پایین» دیگر منتظر اسکرول نمی‌ماند؛ باکس استدلال واقعاً ۵ خط آخر را نشان
می‌دهد و در مسیر ابزار هم پاک می‌شود؛ توضیح فنی Node.js از رابط حذف شد.
سند: `vira/DESIGN-HUB-UI-FIX.md`.

**۰.۹.۶ (۱۴۰۵/۰۵/۲۹):** «موتور تونل داخلی» (درخواست کارفرما: یک v2ray داخل ویرا) —
دانلود هستهٔ رسمی xray، جمع‌آوری کانفیگ رایگان از شش مخزن پیشنهادی کارفرما، پارس
چهار پروتکل، تست تأخیر، انبارِ سالم‌ها با سنجاق، درگاه محلی پایدار با چرخش خودکار؛
صفحهٔ تنظیمات «پراکسی» عین صفحهٔ ویندوز (Snap15) با استثناها و حالت سه‌گانه؛ بخش
«لاگ‌ها» برای بررسی کامل خطاها (لاگر بافری + فایلی)؛ ظاهر هاب به سبک OmniRoute
(سایدبار داخلی + آواتار برند)؛ راه‌انداز npm ci خودکار. قاعدهٔ کارفرما: تست سبک،
فقط سلامت — نتیجه را خودش می‌آزماید.

**۰.۹.۵ (۱۴۰۵/۰۵/۲۹):** «پراکسی مطمئن» — ریشهٔ «همهٔ مسیرها شکست خوردند / ۴۲۹» پیدا
شد: Node.js پراکسی سیستم (Hiddify و مانندش) را نمی‌بیند و تماس‌ها مستقیم می‌روند.
حالا: لایهٔ شبکهٔ `src/net.js`، پراکسی سراسری هاب + مخصوص هر اتصال، پشتیبانی
http/socks5 و `HTTPS_PROXY`، مقصدهای محلی همیشه مستقیم، دکمهٔ «تست پراکسی» (مقایسهٔ
IP/تأخیر با و بدون)، و خطاهای ۴۲۹/۴۰۳ بدون پراکسی که سرنخ تحریم جغرافیایی می‌دهند.
دو وابستگی تازه: `undici` و `fetch-socks`. گزارش: `DESIGN-PROVIDER-UI.md` §۱۲. قاعدهٔ کار: هر تست با خراب‌کردن
عمدی کد قرمز شده است (جهش)، و «تأیید زنده» جدا از تست واحد انجام می‌شود.

**اسناد ویرا:** `vira/README.md` (مرجع کامل) · `vira/DESIGN-PROVIDER-HUB.md` (هاب و
عیب‌یاب، §۱۴ «آنچه در پیاده‌سازی فرق کرد») · `vira/DESIGN-UI-PARITY.md` (شانزده بخش:
تاریخچهٔ کامل رابط و هر باگی که پیدا شد) · `DESIGN-DEPLOY-VIRA.md` (استقرار روی سرور —
**پیاده نشده**، هفت سؤال باز) · `DESIGN-PADO.md` (طرح هستهٔ پادو که ویرا فاز ۰ آن
است — خواسته‌های کارفرما، §۱۵ تصمیم‌های استقرار، §۱۶ اتصال گیت).

**آنچه در ویرا باز است:** عدد بودجهٔ ماهانه · Fusion (تصمیم ۹) · elicitation در MCP ·
ترمینال داخل برنامه · استقرار سروری.

---

## ۸. اسناد پروژه

| سند | محتوا |
|---|---|
| `PROJECT-STATE.md` | همین سند — وضعیت کلی و مرجع اول |
| `MODULES-REFERENCE.md` | **مرجع ماژول‌ها** — شرح کارکرد و مستندات فنی همهٔ ماژول‌ها و زیرسیستم‌ها (سرویس‌ها، جداول، کرون‌ها، صفحات، REST، کلیدها) |
| `HANDOFF-PROMPT.md` | **پرامپت شروع چت جدید** — برای ادامهٔ کار در یک چت تازه کپی می‌شود |
| `API-KEYS.md` | فهرست کامل کلیدها و توکن‌های موردنیاز ادمین فروشگاه + جدول کلیدهای غیر ایرانی غیر رایگان |
| `DESIGN-FX.md` | طراحی سیستم واسط پرداخت ارزی: معماری آژانسی، شارژ ریالی، پرداخت خودکار، فازبندی |
| `DESIGN-PADO.md` | **طرح هستهٔ پادو (AI مرکزی فروشگاه)** — v0.2: پانزده خواستهٔ نقل‌شده از کارفرما، ذهنیت، اسکیل‌ها، تصمیم‌های استقرار (§۱۵) و اتصال گیت (§۱۶). بخش «ذهنیت» هنوز پیش‌نویس است؛ **فاز ۰ (کلون کامل) ساخته شد** و همان ویراست. مبنای `DESIGN-DEPLOY-VIRA.md` |
| `DESIGN-DEPLOY-VIRA.md` | **طراحی استقرار ویرا روی سرور**: دروازهٔ پنل، چند مدیر با اختیارات نابرابر، محدوده‌های فایل‌سیستم، SSH، کارگزار کلید، دفتر رویداد منتسب، صف — پیشنهاد، پیاده‌نشده، هفت سؤال باز (§۱۵) |
| `vira/DESIGN-PROVIDER-UI.md` | **بازطراحی رابط پرووایدرها و هاب (۰.۹.۴)** — چهار نما، کاتالوگ کارتی، ویزاردها، توپولوژی، ریست سه‌سطحی |
| `vira/DESIGN-PROVIDER-HUB.md` | هاب پرووایدر و عیب‌یاب — **ساخته شد** (ویرا ۰.۷) |
| `vira/README.md` | مرجع کامل ویرا: نصب، هاب، ابزارها، اسکیل، MCP، معماری، تست |
| `vira/DESIGN-UI-PARITY.md` | تاریخچهٔ رابط ویرا در شانزده بخش: طرح تأییدشده، کلاس واحد دکمه‌ها، دوزبانگی، آیکون‌ها، مخزن نشست — و هر باگی که سر راه پیدا شد |
| `DESIGN-PHASES-6-14.md` | طراحی و وضعیت فازهای ۶ تا ۱۴ نقشهٔ راه nop روی وردپرس (خلاصه) |
| `DESIGN-PHASE6-PAYMENTS.md` | طراحی فاز ۶: درگاه‌های مستقیم بانکی، BNPL خارجی، درگاه مرکزی escrow، تخفیف نقدی |
| `DESIGN-PHASE7-COURIER.md` | طراحی فاز ۷: اپ پیک، مسیریابی ترتیبی، COD، برچسب استاندارد، رصد و چت |
| `DESIGN-PHASE9-MARKETPLACES.md` | طراحی فاز ۹: دیجی‌کالا/دیوار/باسلام + انتشار خودکار محتوا |
| `DESIGN-PHASE10-SEO.md` | طراحی فاز ۱۰: سئو، فید یکتانت/تپسل، تریبون + پرامپت‌های پادو |
| `DESIGN-PHASE12-INTERNATIONAL.md` | طراحی فاز ۱۲: درگاه رمزارزی + ترجمهٔ خودکار + چندزبانه |
| `DESIGN-DOMAIN.md` | طراحی سرویس دامنه و ثبت وب‌پرزنس (فاز ۱۰) |
| `DESIGN-LEGAL-AUTH.md` | طراحی احراز هویت قانونی: کد ملی شاهکار، OTP همگانی، سلب مسئولیت دیجیتال |
| `DESIGN-APPS-ROADMAP.md` | نقشهٔ راه اپ‌های فلاتر (ادمین/مشتری/پیک) — در ریپوی اپ‌ها اجرا می‌شود |
| `DESIGN-AUTOMATION.md` | **سند جامع معماری اتوماسیون هوشمند فروشگاه** (۱۴۰۵/۰۶/۳۱، متن عیناً از کارفرما، کامیت `66a5924`): n8n + پنج Agent + Approval Center + ۱۰ وردفلو + ۸ فاز — **هنوز روی `main` نیست** |
| `PROMPTS-SEO-PADO.md` | پرامپت‌های سئو برای پادو (گوگل‌داک 1IaMi1zfrjsRKxO4iibVzLD8OWXd738qj8wZYh-K8AFA) |
| `PROMPT-IG-GROWTH-PADO.md` | پرامپت رشد اینستاگرام برای پادو (گوگل‌داک 1aBCGpY6UZ-LwC1CftDt1Q6ZjeBh7TMHEv_omORtArdM) |
| `DESIGN-VIP.md` | طراحی کانال VIP: جداول، اندپوینت‌ها، جریان کار |
| `DESIGN-VIP-EXPIRY.md` | سیاست انقضای پست VIP: مدت مرکزی، حذف از سرور، اطلاع‌رسانی سه‌گانه، ذخیره و نسخهٔ آفلاین |
| `REVIEW-IGBZ-NopCommerce.md` | بازبینی مخزن مبدأ (فقط خواندنی) |
| `README.md` | جریان ثبت‌نام ۱۳ مرحله‌ای |
| `igbz-suite/README.md` | مرجع فنی افزونه |
| `_devenv/AGENT-BRIEF.md` | جزئیات زیرسیستم‌ها و تله‌ها (v8 تا v16 + فازهای ۶–۱۴) |
| `_devenv/README.md` | راهنمای محیط توسعه (و ابزار `makepot`) |
| `ِDoc/` (پوشهٔ اسناد آپلودی) | فایل‌های راهنمای کارفرما: درگاه ارزی، درگاه چندمنظوره (Parbad)، ترب/دیجی‌کالا، پلاگین ارسال (تاپین/پستکس)، سرویس آموزش، سرویس‌دهنده‌های مترجم/AI، سئو و تبلیغات، صفحهٔ اصلی سایت مادر، `ARCHITECTURE_AND_ROADMAP.md`، `IGBZ-project-full.zip` |

---

## ۹. وضعیت فعلی و گام بعدی

**چهار PR اول همگی در `main` ادغام شده‌اند** — یعنی همهٔ کار محصول و همهٔ کار ویرا تا
۰.۹.۶ روی `main` است و هیچ کار معلقی از جلسه‌های قبل نمانده:

| PR | محتوا | وضعیت |
|---|---|---|
| ۱ | پورت اصلی nop → وردپرس | ادغام ۱۴ اوت (`43d89d9`) |
| ۲ | اصلاح مستندات، حذف کانوا، فازهای ۱–۳b ارزی، تکمیل فازهای ۶–۱۴، DB v16 | ادغام ۱۵ اوت (`c3f700a`) |
| ۳ | همگام‌سازی اسناد + بازسازی `.pot` + ابزار `makepot` + ساخت ویرا ۰.۷ تا ۰.۹.۲ | ادغام ۱۶ اوت |
| ۴ | ویرا ۰.۹.۶ — باکس خوش‌آمد، کادر چت، هاب، تونل، پراکسی، لاگ‌ها | ادغام ۱۸ اوت (`3570e35`) |

**جلسهٔ ۱۴۰۵/۰۵/۳۰:** محیط از صفر بالا آمد و وضعیت ثبت‌شده تأیید شد. سپس با تأیید کارفرما دو
کار انجام شد: **رفع باگ خواندن پاسخ در چهار درگاه بانکی** (پایین) و همگام‌سازی سه ناسازگاری
سند/کد (شمار مسیرهای REST، نسخه و تست‌های ویرا، ثبت `DESIGN-PADO.md` در فهرست اسناد).

**جلسهٔ ۱۴۰۵/۰۶/۳۱:** سند `DESIGN-AUTOMATION.md` (معماری اتوماسیون n8n، متن عیناً از کارفرما)
ثبت و پوش شد (`66a5924` روی `arena/01a025f2-igbz-wp` — **روی `main` نیست**؛ کار جلسهٔ قبل با
کامیت `8d661ac` همین شاخه است). سرور وردپرس از `_devenv` بالا آمد و **محیط تست مرورگری
Playwright + Chromium** در `/home/user/browser-test` آماده شد — بدون نوشتن هیچ تستی، به دستور
کارفرما. بحث پادو به روش «یک‌به‌یک بررسی مراحل» رفت؛ **مرحلهٔ اول: طراحی قالب** (تصحیح ثبت
شده در `DESIGN-PADO.md §۶`).

**جلسه‌های ۱۴۰۶/۰۶/۰۲ تا ۰۴ (شاخهٔ `arena/01a03227-igbz-wp` — PR #8، شرح کامل در
`HANDOFF-PROMPT.md §۴` و `§۷.۶`):**
- **پیش‌نمایش زنده:** دو راه‌حل ماندگار — لینک Playground رسمی با
  `_devenv/playground-blueprint.json` + دکمهٔ خودکار «Preview» روی هر PR
  (ورک‌فلوی نصب‌شده). رفع دو خطا: `refType=branch` و حافظهٔ ۵۱۲ مگابایتی.
- **تست ویژوال کامل (قانون جدید ۱۲):** ۳۵ اسکرین‌شات از ۲۹ منوی ادمین + ۵ صفحهٔ
  فروشگاه در `visual-testing/`؛ ۷ مشکل رفع شد از جمله دو باگ کد: دوباره‌رندر
  صفحهٔ Status (`StatusPage.php`) و نبود RTL کارت پادو (`PadoPage.php`).
  گزارش: `visual-testing/REPORT.md`.
- **اتوماسیون جولز:** صف `jules-tasks/` + ورک‌فلوی dispatcher + سیکرت
  `JULES_API_KEY`؛ تسک آزمایشی PR #9 را ساخت (دیفش درست بود؛ کارفرما به‌اشتباه
  بست — **کار باز: Reopen و ادغام**). جولز انتخاب قطعی است (پرداخت ارزی ممکن نیست).
- **ریل‌وی:** فایل‌های استقرار پیش‌نمایش دائمی (`railway/` + `railway.json` +
  نگهبان ولوم با سیکرت `RAILWAY_TOKEN`) آماده؛ استقرار منتظر ادغام PR #8 است
  (ریل‌وی از `main` می‌سازد که فایل‌ها را ندارد).
- تست‌ها: ۱۲۵۰ اظهارنظر (۲ شکست از-قبل-موجود ModulesTest که PR #9 رفعشان می‌کند) ·
  دیتابیس v19 با ۷۲ جدول.

> **قاعده:** شاخهٔ جلسه را همیشه **از سیستم** بگیرید (`git status`)، نه از متن اسناد؛ هر جلسه
> شاخهٔ `arena/…` خودش را دارد و شمارهٔ PR بعدی هم عوض می‌شود.

- تست افزونه: **۱۲۵۱ اظهارنظر / ۲۴ کیس** · لینت: **۲۴۵ فایل، صفر خطا** · دیتابیس: **۷۲/۷۲ جدول (v19)**
- تست ویرا: **۳۷۵ تست واحد + ۴۴ آزمون زنده** (`cd vira && node test/run.mjs && node test/live.mjs`)
  — روی ماشین بدون داکر، یک تست SDK محیطی رد می‌شود (۳۷۵/۳۷۶)، چون سندباکس پیش‌فرض روشن است
  و شکستِ بسته دارد. این رفتار درست است، نه باگ.
- تأیید زندهٔ سایت: همهٔ ۲۹ صفحهٔ ادمین ۲۰۰، درگاه‌ها با دکمهٔ Test، گیت وبوک، health check.

**منتظر تصمیم/دستور کارفرما:**
1. ~~سیاست انقضای پست VIP~~ — **بسته شد** (۱۴۰۵/۰۵/۲۶): ۷ روز، تعیین‌شده توسط مدیر ارشد،
   حذف از سرور، با اطلاع‌رسانی به ادمین و مشتری و قابلیت ذخیره در اپ.
2. **پادو و اینستاگرام** — ✅ **معماری در ۱۴۰۵/۰۶/۰۶ بسته شد:** پادو دستیار کامل ولی
   Playbookمحور هر فروشگاه است؛ DeepInfra حساب/هزینهٔ مستقل همان فروشگاه را دارد؛ Zernio
   تنها provider اجتماعی با حساب مرکزی و profile جداست. مرجع قطعی ADR-0004 و
   `DESIGN-INSTAGRAM-PADO-ZERNIO.md` است. adapterها، چهار Playbook، حلقهٔ یادگیری، profile
   mapping و migration کد legacy هنوز پیاده نشده‌اند و هر تغییر کد مجوز تازه می‌خواهد.
3. **اپ‌های فلاتر** (ادمین/مشتری/پیک + PWA) — ⏸️ **معلق تا تکمیل بک‌اند و فرانت‌اند
   (تصمیم ۱۴۰۵/۰۵/۳۱):** «تا از دوباره‌کاری جلوگیری کنه. در زمانش هم فایل کامل اپلیکیشن
   رو ارسال می‌کنم.» پس دسترسی به ریپوی اپ‌ها لازم نیست. ⚠️ ولی سند امنیت باید **قرارداد
   سمت سرور** برای امضای بیومتریک (`DESIGN-LEGAL-AUTH.md §۷.۶`) را کامل تعریف کند تا
   سرور منتظر اپ نماند.
4. **کلیدهای واقعی** سرویس‌ها — Zernio/DeepInfra و providerهای بانک، باسلام، گوگل، شاهکار
   و پیامک هنوز آزمون زنده ندارند. کلید DeepInfra در IGBZ ذخیره نمی‌شود؛ کلید مرکزی Zernio
   فقط در secret store است. برای Manus/ChatPlace/ManyChat کلید تازه وارد نشود.
5. **ترجمهٔ فارسی (`fa_IR.po`)** — ✅ **دامنه تصمیم گرفته شد (۱۴۰۵/۰۵/۳۱)**، خودِ ترجمه هنوز
   انجام نشده. جزئیات در بخش «✅ دامنهٔ ترجمهٔ فارسی» پایین همین بخش.

### 🔐 خروج گروهی اطلاعات کاربران — امضای بیومتریک (۱۴۰۵/۰۵/۳۱)

> **متعلق به بخش امنیت.** کارفرما گفت چون به ذهنش رسیده ثبت شود تا فراموش نشود؛ طراحی
> کاملش در سند امنیت انجام می‌شود. جزئیات: `DESIGN-LEGAL-AUTH.md §۷.۶`.

هر درخواست **گروهی** اطلاعات کاربران فروشگاه:

۱. **فقط از اپ مدیریت** — نه پنل وب، نه هیچ مسیر دیگر
۲. امضاشده با **پارامتر بیومتریک** + **اطلاعات دستگاهِ** ثبت‌شده در ثبت‌نام مدیر
۳. همراه **کد یکتای اپ**، به‌صورت **رمزگذاری‌شده**
۴. هر سه هنگام ثبت‌نام در اپ ذخیره و **خودکار** ضمیمه می‌شوند

> 🔴 **قاعدهٔ مرکزی: «بررسی صحت توسط سرور انجام می‌شه، نه اپ مدیریت.»** اپ روی دستگاه
> کاربر است و قابل دستکاری؛ اگر اپ خودش بگوید «بیومتریک تأیید شد»، مهاجم همان را جعل
> می‌کند. این **دومین بارِ مستقل** است که کارفرما اصل «تصمیم در سرور، نه در کلاینت» را
> تکرار می‌کند (بار اول: کول‌داون OTP در `§۷.۵.۵`) — یعنی اصل پایدار پروژه است.

**نسبت با استاندارد:** پاسخ مستقیم به `API3:2023` (افشای بیش از حد داده) و `API6:2023`
(سوءاستفادهٔ خودکار از جریان کاری حساس). با مقیاس پروژه حیاتی است: یک خروجی گروهی
بی‌محافظت = نشت اطلاعات همهٔ مشتریان یک فروشگاه در یک درخواست.

**نقطهٔ تماس موجود:** `StoreAdminController` امروز `GET /admin/customers` دارد که دقیقاً
مصداق این قاعده است. فهرست کامل مسیرهای گروهی باید در سند امنیت استخراج شود.

### 🔴 مسیرهای هستهٔ وردپرس هم باید بسته شوند (۱۴۰۵/۰۵/۳۱)

> «سیستم باید درخواست‌های این‌چنینی از سوی **هستهٔ وردپرس** را هم بند کنه.»
> جزئیات: `DESIGN-LEGAL-AUTH.md §۷.۷`.

کنترل بند ۷.۶ مسیرهای **خودمان** را می‌بندد، ولی وردپرس و ووکامرس مسیرهای **خودشان** را
دارند که همان داده را می‌دهند: `wp/v2/users` · **`wc/v3/customers`** (ایمیل، تلفن، آدرس) ·
`wc/v3/orders` · `xmlrpc.php` · `?author=N` · صادرات پنل · `admin-ajax.php`.

> ⚠️ **شکاف تأییدشده در کد:** `Authenticator::rest_authentication_errors()` (خط ۱۳۷) فقط
> وقتی عمل می‌کند که **توکن Bearer** در درخواست باشد؛ بدون توکن `return $errors` می‌زند.
> یعنی محدودیت نرخ فقط روی **کاربران اپ** است. درخواست با **کوکی نشست وردپرس** یا
> **Application Password** از این فیلتر رد می‌شود و به مسیرهای هسته و ووکامرس می‌رسد —
> بدون سقف، بدون امضای بیومتریک، بدون ثبت در دفتر رویداد.
>
> با مقیاس پروژه: یک حساب ادمین فروشگاه که لو برود می‌تواند کل پایگاه مشتریان را از
> `wc/v3/customers` بیرون بکشد و هیچ‌کدام از کنترل‌های بند ۷.۶ جلویش را نمی‌گیرد.

**قاعده:** هر مسیری که داده‌های کاربران را گروهی برمی‌گرداند — مالِ ما، هستهٔ وردپرس،
ووکامرس، یا هر افزونهٔ دیگر — مشمول همان کنترل بند ۷.۶ است. **رویکرد: فهرست سفید، نه
فهرست سیاه** — شمردن درهای پشتی محکوم به شکست است چون افزونهٔ بعدی یک در تازه باز می‌کند.

**نکتهٔ فنی برای طراحی:** گیت باید روی `rest_pre_dispatch` بنشیند، نه
`rest_authentication_errors` که فقط با توکن کار می‌کند.

### 📋 کارهای باقی‌ماندهٔ ثبت‌نام و احراز هویت (۱۴۰۵/۰۵/۳۱)

بازبینی کد نشان داد بخشی از `DESIGN-LEGAL-AUTH.md` طراحی شده ولی **کد ندارد**، و کارفرما
دو الزام تازه اضافه کرد. جزئیات کامل: `DESIGN-LEGAL-AUTH.md §۷.۵`.

| # | کار | جدیت | وضعیت |
|---|---|---|---|
| ۱ | **قرارداد دیجیتال حقوقی** — ثبت، هش نسخه، نمایش و پذیرش در سرویس و مسیر REST/پنل پیاده شد | ✅ | بسته |
| ۲ | **گیت پیش از پرداخت** — درگاه‌های بانکی فقط با دامنهٔ تأییدشده، اینماد فعال و تطابق کد ملیِ دارای کلید شاهکار یا قرارداد معتبر فعال می‌شوند | ✅ | بسته |
| ۳ | **کول‌داون OTP: موبایل و IP با هم (AND)** — شرط در سرویس بک‌اند و ایندکس متناظر پیاده شده است | ✅ | بسته |
| ۴ | **شاهکار** — کد آماده است، منتظر کلید مدیر ارشد | 🟠 | منتظر کلید |
| ۵ | **تولید کد با ماژول کریپتو** — الزام کارفرما | ✅ | از قبل رعایت شده |

**⚠️ ریسک حقوقی فعلی (بند ۱ و ۲):** الان حالتی ممکن است که پرداخت بانکی بدون تطابق کد ملی
انجام شود **و هیچ قرارداد امضاشده‌ای پشتش نباشد** — همان چیزی که ابتدای سند دربارهٔ
«پروندهٔ قضایی و جبران خسارت سنگین» هشدار داده.

**بند ۳ — شرط دقیق `AND` است، نه `OR`** (تصریح کارفرما): «من گفتم آی‌پی **و** شماره
موبایل، یعنی اگه **هر دو یکی باشن**.» یعنی کول‌داون فقط وقتی اعمال می‌شود که همان شماره
**و** همان IP باشد. برداشت اولیهٔ عامل (`OR`) غلط بود و کاربران پشت NAT را قفل می‌کرد؛
با `AND` آن نگرانی خودبه‌خود حل می‌شود چون دو کاربر واقعی پشت یک IP شمارهٔ متفاوت دارند.

> 🔴 **الزام صریح کارفرما — بررسی در بک‌اند، نه فرانت‌اند:** «این بررسی باید حتماً در
> بک‌اند انجام بشه، نه در فرانت‌اند که با یه رفرش صفحه دور زده نشه.» تایمر فرانت‌اند فقط
> **نمایش** است و هیچ ارزش امنیتی ندارد. شرط `ip_hash` باید داخل `OtpService::send()`
> بنشیند — نه در کنترلر REST، نه در شورت‌کد، نه در جاوااسکریپت — چون آن تابع تنها مسیری
> است که همهٔ ورودی‌ها (شورت‌کد سایت، REST اپ، مصرف‌کنندهٔ آینده) از آن می‌گذرند.
>
> **قاعدهٔ دائمی:** هر محدودیت نرخ، کول‌داون یا سقف تلاش در این پروژه در **سرویس بک‌اند**
> اعمال می‌شود. فرانت‌اند فقط بازتاب می‌دهد، تصمیم نمی‌گیرد.

**قاعدهٔ دائمی (الزام کارفرما):** هیچ کد یکبارمصرفی در هیچ ماژولی نباید با مولد غیرامن
(`rand`/`mt_rand`) ساخته شود؛ همیشه از `Crypto::numeric_code()` که روی `random_int()` بنا
شده. امروز رعایت می‌شود و باید در هر مسیر تازه هم رعایت شود.

### 🧠 ویرا مثل مغز است — تصحیح بنیادی کارفرما (۱۴۰۵/۰۵/۳۱)

> «ویرا مثل مغز انسان می‌مونه که ابزارهای مختلف داره، مثل چشم‌ها، گوش‌ها، عضلات. ویرا هم
> ابزارهای مختلفی داره که در قالب ماژول‌ها طراحی شدن. یکی مثلاً از این ماژول‌ها رابط
> کاربریه. وقتی می‌گم ویرا بالا اومد، منظورم ماژول رابط کاربریش برای مدیره، نه اینکه
> خودِ ویرا خاموش بوده حالا روشن می‌شه.»

| | نقش | چرخهٔ حیات |
|---|---|---|
| **هسته** (مغز) | مدیریت، نظارت امنیتی، بازبینی، تصمیم | **همیشه بالا** |
| **ماژول رابط کاربری** (چشم و زبان) | تعامل با مدیر | با نیاز می‌آید و می‌رود |
| **سایر ماژول‌ها** (حواس و عضلات) | ابزار، گیت، تونل، هاب، امنیت | بسته به کار |

**سه چیزی که با این تصحیح باطل شد:**

۱. **قید «خاموشی با بیکاری ۳۰ دقیقه»** در `DESIGN-DEPLOY-VIRA.md §۵.۳` — یک **پیشنهاد
   تأییدنشده** در نسخهٔ اول سند بود، نه تصمیم کارفرما.
۲. **بند ۲ بودجهٔ حافظه (§۵.۷)** که به آن قید تکیه کرده بود. نگرانی «پروسهٔ فراموش‌شده و
   هزینهٔ توکن» معتبر است ولی جوابش **محافظ ضدحلقه** است نه خاموش‌کردن هسته.
۳. **دلیل اولِ مرز امنیتی (§۱۴.۵)** — «ویرا خاموش می‌شود پس در مسیر بحرانی جا ندارد».
   آنچه معتبر می‌ماند فقط ملاحظهٔ **تأخیر** است، که با «خاموش است» فرق دارد.

> ⚠️ **درس روش‌کاری (اشتباه عامل):** خط «خاموشی با بیکاری» صریحاً **«پیش‌فرض پیشنهادی»**
> بود، ولی عامل آن را به‌عنوان واقعیت در استدلال به کار برد و نقش ویرا را بر پایه‌اش
> محدود کرد. **قاعده: هر چیزی که در اسناد «پیشنهادی» است، تا تأیید کارفرما نباید مبنای
> استدلال یا تصمیم بعدی شود** — و اگر ناچار به استفاده شد، صریح گفته شود که تأییدنشده است.

**باز مانده:** با باطل‌شدن دلیل اول، حدِ اختیار ویرا در **سد کردن** درخواست مشکوک باید
از کارفرما پرسیده شود — فقط تحلیل و هشدار، یا اجازهٔ مسدودکردن هم دارد؟ ضمناً مرز
جداسازی در §۵.۳ باید از «پروسه به‌ازای هر مدیر» به «هسته / رابط» بازطراحی شود.

### 🔐 ویرا مسئول امنیت پروژه — تصمیم کارفرما (۱۴۰۵/۰۵/۳۱)

> «ویرا مسئول امنیت پروژه است. تمام اجزای بک‌اند باید هر درخواست مشکوکی رو به ویرا گزارش
> کنند، مثل تعداد درخواست‌های خیلی زیاد و غیراستاندارد در یک محدودهٔ زمانی. برای ویرا هم
> باید تنظیمات و اسکیل‌های خیلی حرفه‌ای و قوی در مورد امنیت طراحی بشه.»

**چرخش نقش:** تا امروز اسناد ویرا را **موضوع** امنیت می‌دیدند (چیزی که باید قفل شود).
حالا نقش **فعال** هم دارد: ناظر امنیتی. هر دو هم‌زمان معتبرند.

**نکتهٔ کلیدی — تشخیص محلی، دید مرکزی:** امروز مکانیزم‌های پراکنده‌ای هست
(`otp.max_attempts`، ماسک راز در `Logger.php`) ولی **هیچ‌کدام به جایی گزارش نمی‌دهند**؛
محلی رد می‌کنند و تمام. ده تلاش ناموفق در ده ماژول، محلی بی‌معنی است و مرکزی یک الگو.

**جایگاه ویرا: لایهٔ سیزدهم (تصمیم کارفرما).**

> «ما ۱۳ لایهٔ امنیتی خواهیم داشت که در بخش امنیت توضیح می‌دم. ویرا لایهٔ آخر هست. فقط
> درخواست‌های خاص و فعالیت‌های مشکوک بهش ارجاع داده می‌شه، که برای هر کدوم تنظیمات و
> اسکیل‌های پیش‌بینی‌شده داره. پس **می‌تونه جلوی درخواست رو بگیره**.»

```
لایه‌های ۱ تا ۱۲ → دفاع سریع و خودکار
                      ↓ فقط موارد خاص و مشکوک ارجاع می‌شوند
لایهٔ ۱۳ = ویرا  → تحلیل + تصمیم + امکان سد کردن
```

⚠️ **نسخهٔ اول این بند باطل شد.** عامل نوشته بود «ویرا ناظر است نه دیوار آتش» و سد کردن
را از اختیارش بیرون گذاشته بود. هر دو دلیلش از بین رفت: دلیل اول (فرض خاموشی) قبلاً باطل
شده بود، و دلیل دوم (تأخیر) در معماری لایه‌ای **بی‌موضوع** است — ویرا ته قیف است و در
مسیر همهٔ درخواست‌ها نمی‌نشیند، پس حجم ورودی‌اش کم و هزینهٔ تحلیل قابل پرداخت است.

**قاعده‌ای که معتبر می‌ماند:** ویرا طبق **اسکیل و تنظیمات از پیش تعریف‌شده** عمل می‌کند،
نه قضاوت آزاد در لحظه — هم‌راستا با بند ۸ خواسته‌های `DESIGN-PADO.md`.

**ویرا «فکر نمی‌کند» — روند از پیش تعریف‌شده را اجرا می‌کند (تصحیح کارفرما):**

> «من می‌گم روند از پیش تعیین‌شده داره، تو می‌گید معطل بمونه. چطور می‌تونه معطل بمونه؟
> مثلاً براش تو اسکیل تعریف شده: اول منبع درخواست رو شناسایی کن، اگه شناسایی کردی اجازه
> بده، اگه نکردی به منبع پیام بفرست که از طریق مثلاً یک تأیید دوفاکتوری تأیید کنه…
> **ویرا فقط با سرعت چیزی که براش تعریف شده رو اجرا می‌کنه.**»

⚠️ عامل «تحلیل» را به معنای استدلال مدل زبانی گرفته بود (کند، نیازمند مهلت زمانی) و سه
نگرانی مطرح کرده بود: مهلت زمانی، fail-open/fail-closed، و معلق‌ماندن درخواست. **هر سه
باطل شد.** آنچه در لایهٔ ۱۳ اجرا می‌شود یک **درخت تصمیمِ از پیش نوشته‌شده** است، نه
استدلال آزاد — اجرایش میلی‌ثانیه است. «تردید» هم وجود ندارد چون هر شاخه جواب صریح خودش
را دارد. و انتظار برای تأیید دوفاکتوری کندیِ سیستم نیست؛ آنجا **کاربر** باید جواب بدهد.

**قاعده:** در لایهٔ امنیتی ویرا **مجری** است نه **متفکر**. هر رخدادِ ممکن باید در اسکیل
شاخهٔ خودش را داشته باشد؛ اگر موردی تعریف‌نشده پیش آمد، راه‌حل **افزودن شاخه به اسکیل**
است نه واگذاشتن تصمیم به قضاوت لحظه‌ایِ مدل. (هم‌راستا با بند ۸ خواسته‌های
`DESIGN-PADO.md`: «از هر نوع اشتباهی جلوگیری کرد».)

**دامنهٔ این ثبت:** فقط **جایگاه** ویرا. خودِ ۱۳ لایه، فهرست روندهای امنیتی، درخت تصمیم
هر رخداد، و مرز اقدام‌های ماندگار → **موضوع بخش امنیت و طراحی اسکیل‌هاست، نه اینجا**
(تأکید کارفرما ۱۴۰۵/۰۵/۳۱). جزئیات ثبت‌شده: `DESIGN-DEPLOY-VIRA.md §۱۴`.

### ✅ دفتر رویداد: هرگز پاک نشود + ماسک اجباری PII (۱۴۰۵/۰۵/۳۱)

**کارفرما: گزینهٔ الف — هرگز پاک نشود**، با بایگانی سالانه اگر حجم مسئله شد.

دلایل: حجمش ناچیز است (~۱۰۰MB در سال با هزار رویداد روزانه) · ارزشش با زمان **بیشتر**
می‌شود نه کمتر · حذف برگشت‌ناپذیر است ولی کوتاه‌کردنِ بعدی ضرری ندارد.

سه تصمیم دیگرِ همین تاریخ اهمیتش را بالا برده‌اند: ویرا **بازبین** است و گزارشش مبنای
merge می‌شود · آزمایشی و زنده روی **یک ماشین**‌اند · مسیر پولی **سقف ندارد**. در هر سه،
دفتر رویداد تنها راه فهمیدن «چه شد» است.

**ماسک اجباری PII (پیشنهاد عامل، تأیید کارفرما):** چون دفتر دائمی است، دادهٔ شخصی مشتری
که از `payload` یا متن خطا نشت کند **برای همیشه می‌ماند**. پس نام/تلفن/آدرس/ایمیل/کد ملی
پیش از نوشتن هر ردیف ماسک می‌شوند. سه قید: ماسک **پیش از** محاسبهٔ `hash` اعمال شود
وگرنه زنجیره می‌شکند · `Logger.php` امروز راز را ماسک می‌کند ولی **PII را نه** · شناسهٔ
**مدیر** ماسک نمی‌شود چون انتساب کل هدف این دفتر است. جزئیات:
`DESIGN-DEPLOY-VIRA.md §۸.۴` و `§۸.۵`.

### ✅ گردش کار توسعه و نقش ویرا — تصمیم گرفته شد (۱۴۰۵/۰۵/۳۱)

> «تمام کارها بر روی گیت‌هاب انجام می‌شه. توسعه‌دهنده‌ها بر روی سیستم‌های خودشون کار
> می‌کنند، بر روی برنچ خودشون پوش می‌کنند، مدیر ارشد بعد از بازبینی اجازهٔ مرج می‌ده.»
> «بررسی تمام تغییرات… توسط ویرا انجام می‌شه که گزارش کار رو به مدیر می‌ده تا مدیر تصمیم
> بگیره که مرج انجام بشه یا نه. در ضمن ویرا خودش یک محیط آزمایشی داره بر روی سرور اصلی.»

```
توسعه‌دهنده (سیستم خودش) → push به برنچ خودش → ویرا بررسی می‌کند
                          → گزارش به مدیر ارشد → مدیر: merge بله/خیر
```

**نقش ویرا: بازبین، نه توسعه‌دهنده.** بررسی تغییرات و گزارش‌دادن کارِ اوست؛ **تصمیم merge
با مدیر ارشد است و ویرا حق merge ندارد**.

**محیط‌ها:** توسعه روی **سیستم خودِ توسعه‌دهنده‌ها** (نه سرور) · آزمایشی و زنده هر دو روی
**سرور اصلی**. پس `/var/www/dev` روی سرور معنا ندارد و SSH به ماشین دیگر لازم نیست.

**پیامد مهم:** چون آزمایشی و زنده روی یک ماشین‌اند، مرز `stage` و `shop` در `scopes.json`
تنها چیزی است که جلوی خراب‌شدن سایت واقعی را می‌گیرد — باید سخت‌گیرانه اعمال شود.

**باز مانده:** قالب «گزارش بازبینی» · بررسی خودکار با هر push یا با درخواست مدیر؟ ·
آیا ویرا برنچ را روی محیط آزمایشی مستقر می‌کند یا فقط کد را می‌خواند؟ · احراز هویت
گیت‌هاب. جزئیات در `DESIGN-DEPLOY-VIRA.md §۱۳`.

### ⚠️ تصحیح مهم — پادو سرویس ابری است، «عامل فروشگاه» وجود ندارد (۱۴۰۵/۰۵/۳۱)

اسناد تا امروز فرض کرده بودند که به‌ازای هر فروشگاه یک **پروسهٔ محلی ویرا** روی سرور ما
اجرا می‌شود («عامل فروشگاه»، با محدودهٔ `/var/www/<store>`). **کارفرما این را رد کرد:**

> «من چیزی به عنوان عامل فروشگاه تعریف نکردم. ما یه پادو داریم که برای هر فروشگاه
> مستقله و یه سرویس ابریه… ویرا فقط برای اینا نظارت می‌کنه.»

**معماری درست:**

- **پادو = سرویس ابری آمادهٔ بیرونی** (مثل Gemini/ChatGPT). ما آن را **نمی‌سازیم و
  میزبانی نمی‌کنیم**؛ ادمینِ هر فروشگاه **خودش** تهیه می‌کند.
- **تنها چیزی که ما می‌سازیم یک «اسکیل» است** — بستهٔ دستورالعملِ کارهای روزانهٔ فروشگاه.
- **طراحی قالب هم به سرور ربطی ندارد:** پادو در محیط خودش قالب را می‌سازد، **فایل zip**
  را به ادمین می‌دهد، و ادمین از بخش «قالب‌ها»ی وردپرس نصبش می‌کند.
- **«نظارت» ویرا دقیقاً دو کار است و نه بیشتر:** ۱) به‌روزرسانی اسکیل‌ها بنا به نیاز ·
  ۲) گزارش ماهانه از فعالیت فروشگاه‌ها. کنترل زنده، دیدن گفتگوها و دخالت در کار روزمره
  **جزو نظارت نیست**.

**پیامدها:** روی سرور ما **هیچ پروسه‌ای** به‌ازای فروشگاه نیست (حافظهٔ لازم: صفر) ·
تصمیم ۴ در `DESIGN-PADO.md §۱۵` باطل شد · `DESIGN-DEPLOY-VIRA.md §۱۱` کامل
بازنویسی شد · سؤال ۴ بند ۱۵ منتفی شد · ۸ گیگِ ابتدایی راحت‌تر از تخمین قبلی است.

**باز مانده (موضوع طراحی پادو، با دستور جداگانهٔ کارفرما):** گزارش ماهانه از کجا
می‌آید؟ · اسکیل‌ها چطور توزیع و نسخه‌گذاری می‌شوند؟ · قالب اسکیل برای کدام سرویس‌ها؟

### ✅ دامنهٔ ترجمهٔ فارسی — تصمیم گرفته شد (۱۴۰۵/۰۵/۳۱)

**تصمیم کارفرما: همه‌چیز ترجمه شود، جز پیام‌های خطا.**

| دسته | تعداد | ترجمه؟ |
|---|---|---|
| همهٔ رشته‌ها (پنل مدیریت، فرانت‌اند، ماژول‌ها) | **۲۰۹۰** | ✅ بله |
| پیام‌های خطا | **۲۲۸** | ❌ نه — انگلیسی می‌مانند |
| **جمع قالب** | ۲۳۱۸ | ~۹۰٪ ترجمه می‌شود |

نمونهٔ چیزی که **انگلیسی می‌ماند**: `Account not found.` · `Activation failed.` ·
`%1$s test failed: %2$s` · `A store name is required.`

نمونهٔ چیزی که **ترجمه می‌شود**: `%s was added to your wallet.` · `%d-day free trial` ·
`%1$s likes · %2$s comments · %3$s views`

**روش تشخیص خطا:** الگوی متنی روی `msgid` (کلماتی مثل `error`, `failed`, `invalid`,
`not found`, `cannot`, `required`, `denied`, `expired`, `unauthorized`, `timeout`…).
کارفرما این تقریب را پذیرفت و نخواست فهرست ۲۲۸تایی را دستی بازبینی کند — یعنی چند
مورد مرزی (مثل `A plan priced at zero cannot be active…` که در واقع راهنمای اصلاح
است نه خطا) در دستهٔ خطا می‌مانند و ترجمه نمی‌شوند. اگر بعداً ایراد شد، همان چند
مورد را دستی جابه‌جا کنید.

> ⚠️ **این فقط دامنه است، نه اجرا.** طبق دستور کارفرما (۱۴۰۵/۰۵/۳۱) تا جواب **همهٔ**
> سؤال‌های باز داده نشود، هیچ تغییری در کد شروع نمی‌شود؛ فعلاً فقط جواب‌ها ثبت می‌شوند.

**محدودیت کد legacy:** انتخابگر پست در فرم قیف فقط محتوایی را نشان می‌دهد که از طریق همین
افزونه منتشر شده باشد. در معماری مقصد، فهرست و Insight رسمی باید از profile همان فروشگاه در
Zernio بیاید. پست‌های بیرون از دسترس endpoint رسمی با ورودی دستی ثبت می‌شوند؛ scraper یا
session مرورگر برای پرکردن این شکاف ممنوع است.

---

## وضعیت جلسهٔ ۱۴۰۵/۰۶/۰۵ — اصلاحات واقعی و بدهی‌های باقی‌مانده

این بخش وضعیت واقعی کد را ثبت می‌کند؛ هیچ قابلیت ناتمام فقط به‌دلیل وجود کلاس، جدول یا آداپتور کامل محسوب نمی‌شود.

### انجام‌شده در این جلسه

- بوت‌استرپ ریل‌وی و اتصال امن به مار‌یاد‌بی تثبیت شد.
- مستأجر نمونه، نگاشت دامنه، تشخیص مسیرمحور و داشبورد مالک فروشگاه اضافه شد.
- کاتالوگ، سفارش‌ها، فهرست‌های پیشخوان و رابط برنامه‌نویسی بر اساس `_igbz_tenant_id` محدوده‌بندی شدند.
- قرارداد حقوقی و گیت پیش از پرداخت به سرویس پرداخت متصل شد.
- گیت صادرات دادهٔ وردپرس فعال شد.
- مرکز پادو به درگاه پادو، اعتبارسنجی ZIP، پیش‌نمایش، اعمال و بازگشت قالب متصل شد و صف مجوز بر اساس مستأجر فیلتر می‌شود.
- مدیریت محصولات و بارگذاری تصویر از رابط برنامه‌نویسی اضافه شد.
- آزمون محلی: ۱۲۵۱ اظهارنظر موفق در ۲۴ کیس و ۲۴۵ فایل PHP بدون خطای نحوی.

### مواردی که هنوز باید بسته شوند

- اجرای واقعی تمام عملیات حساس از صف مجوز، نه فقط طراحی و قالب.
- تکمیل چرخهٔ پرداخت و ثبت دامنهٔ پولی.
- آزمون زندهٔ دو profile مستقل Zernio، benchmark حساب مستقل DeepInfra و آزمون شاهکار.
- تکمیل ترجمهٔ فارسی.
- ممیزی کامل مقیاس‌پذیری، صف‌ها، cache و کوئری‌های tenant-scoped.
- پیاده‌سازی کامل حافظه، Playbook و KPI پادو.
- بازسازی و اجرای آزمون ویژوال Chromium طبق قانون ۱۲.

ویرا در تمام تغییرات این جلسه خارج از محدوده بوده است.

---

## ۱۰. ممیزی نهایی پس از ادغام PR شمارهٔ ۱۰ — ۱۴۰۵/۰۶/۰۵

این بخش جدیدترین منبع حقیقت پروژه است و در تعارض با شمارش‌ها یا وضعیت‌های تاریخی بالاتر،
بر آن‌ها مقدم است. پوشهٔ `vira/` در این ممیزی بررسی یا تغییر داده نشد.

### ۱۰.۱ خط پایهٔ تأییدشده

| مورد | نتیجه |
|---|---|
| Git | PR شمارهٔ ۱۰ ادغام شده؛ شاخهٔ جلسه و `origin/main` روی `b160311` بودند |
| هستهٔ زندهٔ محلی | WordPress 7.0.4 · PHP 8.2.32 · WooCommerce 11.0.1 |
| افزونه | فعال؛ ۶ ماژول اصلی؛ DB v19؛ ۷۲/۷۲ جدول |
| کد | ۲۱۵ فایل PHP زیر `src`؛ ۱۴۳ فراخوانی ثبت مسیر REST |
| تست | ۱۲۵۱/۱۲۵۱ اظهارنظر موفق در ۲۴ کیس |
| نحو | ۲۴۵ فایل PHP؛ صفر خطا |
| نشانه‌های TODO واقعی | صفر؛ تنها تطبیق متنی موجود جملهٔ «not a TODO» است |
| قالب ترجمه | بازسازی و check شد: ۲۳۵۱ رشته، ۹ جمع، ۲۹۱۱ ارجاع از ۲۱۵ فایل |
| ویژوال | ۳۵ حالت: ۳۴ موفق، ۱ شکست ماشینی کشور تسویه؛ بازبینی چشمی یک نقص glyph نیز یافت؛ جزئیات در `visual-testing/REPORT.md` |
| HPOS | افزونه سازگاری را اعلام کرده، اما HPOS در نمونهٔ آزمون خاموش بود؛ تأیید عملی باقی است |

### ۱۰.۲ جدول تطبیق ۹ محور

| محور | آنچه واقعاً حاضر است | شکاف قطعی | وضعیت |
|---|---|---|---|
| ۱. شاخه، ادغام و استقرار | PR شمارهٔ ۱۰ ادغام؛ بررسی Preview و وضعیت Railway در PR موفق | سلامت خود استقرار تولید پس از همین merge با URL عمومی و آزمون مرورگر مستقل ثبت نشده | 🟡 |
| ۲. سلامت فنی | همهٔ تست‌ها و نحو سبز؛ سایت محلی و ۷۲ جدول بالا می‌آیند | پوشش load، race، chaos، upgrade و rollback کافی نیست | 🟡 |
| ۳. چندمستأجری و داده | کاتالوگ/سفارش/بخش‌های اصلی tenant-scoped شده‌اند | قالب پادو با `switch_theme()` سراسری است؛ بازگشت قالب شرط tenant ندارد؛ تأیید DNS مجوز شیء tenant-scoped ندارد | 🔴 |
| ۴. امنیت، احراز و رازها | OTP با CSPRNG، کول‌داون بک‌اند، قرارداد حقوقی و گیت پرداخت موجود است | ۲۲ فیلد password خارج `Settings::SECRETS` و قابل ذخیره/بازنمایش plaintext؛ گذار صف مجوز در race دقیقاً یک‌بار نیست | 🔴 |
| ۵. پادو | مرکز، Gateway قالب، صف، ZIP و ثبت artefact حاضر است | پیش‌نمایش واقعی پیاده نیست؛ دانلود امن/محدود و سیاست PHP ناقص؛ عملیات حساس دیگر، اسکیل S1، حافظه/Playbook/KPI و کلید واقعی باقی‌اند | 🔴 |
| ۶. دامنه و سرویس‌های بیرونی | زیردامنه، نگاشت و DNS واقعی حاضر است؛ آداپتورهای تاریخی سرویس‌ها وجود دارند | ثبت دامنه پیش از پرداخت فراخوانی می‌شود؛ تمدید/callback/آداپتور قراردادی ندارد؛ Zernio/DeepInfra/شاهکار/رجیسترار و درگاه‌ها با حساب واقعی تأیید نشده‌اند و آداپتورهای حذف‌شدهٔ Instagram هنوز migration نشده‌اند | 🔴 |
| ۷. مقیاس و کار پس‌زمینه | limit و صفحه‌بندی در بخش‌های زیادی هست؛ ContentScheduler تا حدی tenant-fair است | هیچ استفاده‌ای از Action Scheduler نیست؛ cronهای تجمیعی، حذف‌های نامحدود، فهرست‌های بدون سقف و batchهای بدون requeue/backpressure وجود دارند | 🔴 |
| ۸. تجربهٔ کاربری و بومی‌سازی | فروشگاه راست‌چین و صفحات اصلی قابل استفاده‌اند؛ قالب POT تازه است | `fa_IR.po/mo` کامل وجود ندارد؛ مدیریت و بخش‌هایی از ووکامرس انگلیسی‌اند؛ checkout نمونه کشور `US` می‌دهد | 🟠 |
| ۹. عملیات و آمادگی انتشار | Railway، health check، ابزار test/makepot و تصاویر ویژوال موجودند | پشتیبان/بازیابی، migration روی دادهٔ حجیم، server cron، صف قابل مشاهده، SLO/هشدار، ماتریس HPOS و Runbook انتشار نهایی بسته نشده‌اند | 🔴 |

**نتیجه:** پروژه یک پایهٔ گسترده و قابل اجرا دارد، اما با وجود محورهای قرمز بالا هنوز
«کامل» یا «آمادهٔ ورود کلیدهای واقعی/تولید» نیست. وجود کلاس، جدول، برچسب صفحه یا آداپتور
به‌تنهایی معیار تکمیل قابلیت نیست.

### ۱۰.۳ یافته‌های مقیاس‌پذیری بخش ۵.۵

۱. **صف:** در کل `igbz-suite` هیچ فراخوانی Action Scheduler وجود ندارد. سه رویداد تجمیعی
   WP-Cron پنج‌دقیقه‌ای/ساعتی/روزانه چند سرویس و تماس بیرونی را در یک درخواست اجرا می‌کنند.
   این مدل claim، مشاهده‌پذیری هر کار، retry مستقل، dead-letter و جداسازی مستأجر را ندارد.
۲. **عدالت مستأجر:** `ContentScheduler` پنجرهٔ کاندید و انتخاب گردشی دارد، اما چند sweep
   دیگر limit سراسری دارند؛ نمونه‌ها: تمدید اشتراک ۱۰۰ ردیف، انقضای VIP ۲۰۰ ردیف و
   intake مقدار ۲۵. تکرار تا تخلیه یا requeue صریح در همان سرویس‌ها وجود ندارد؛ مستأجر
   پرترافیک می‌تواند تأخیر بقیه را افزایش دهد.
۳. **پاکسازی:** حذف OTP، token، device و log با DELETE نامحدود انجام می‌شود و روی جدول
   بزرگ می‌تواند قفل/فشار I/O بسازد. الگوی لازم، batch کوچک با کلید مرتب، مکث و ادامهٔ
   زمان‌بندی‌شده است؛ همان الگویی که Action Scheduler 4 برای cleanup خود به‌کار می‌برد.
۴. **خواندن نامحدود:** چند فهرست مدیریتی/سرویسی هنوز بدون صفحه‌بندی‌اند؛ از جمله دامنه‌های
   Hub و tenant، حساب‌های FX، اعضا/دامنه‌های tenant، دستگاه‌های کاربر و برخی گزارش‌ها.
   query تک‌ردیفی یا aggregate با این مورد یکی نیست؛ فهرست‌های رشدپذیر باید limit قطعی و
   cursor/page داشته باشند.
۵. **cache و نرخ:** transient و object cache در چند مسیر به‌درستی استفاده شده، ولی شمارندهٔ
   rate limit مبتنی بر transient در اجرای هم‌زمان اتمیک نیست و در چند نمونه/گره بدون
   object cache مشترک یکپارچه نمی‌ماند. کلید، TTL، invalidation و tenant scope باید برای
   هر cache در ماتریس تست ثبت شود.
۶. **ایندکس:** Schema دارای ایندکس‌های متعدد است، اما کفایت آن‌ها با `EXPLAIN` و دادهٔ حجیم
   سنجیده نشده است. نمونهٔ نیازمند بازبینی، شمارش ساعتی OTP با `(phone, created_at)` و
   queryهای sweep با `(status, due_at/id)` است.
۷. **WooCommerce:** دسترسی سفارش عمدتاً از CRUD ووکامرس استفاده می‌کند و سازگاری HPOS
   اعلام شده است، اما نمونهٔ فعلی HPOS خاموش بود. مهاجرت دوطرفه، sync backlog، REST،
   refund و گزارش‌ها باید در حالت HPOS روشن آزموده شوند.
۸. **ظرفیت:** هیچ نتیجهٔ ثبت‌شده‌ای برای نرخ ورود سفارش/وبهوک، تعداد tenant، اندازهٔ صف،
   p95/p99، سقف دیتابیس یا رفتار قطع سرویس بیرونی وجود ندارد؛ بنابراین ادعای ظرفیت تولید
   فعلاً قابل اثبات نیست.

مبنای به‌روز این بخش: راهنمای رسمی Action Scheduler برای صف بادوام و قابل ردیابی
(<https://actionscheduler.org/>)، راهنمای مقیاس و CLI آن
(<https://actionscheduler.org/perf/> و <https://actionscheduler.org/wp-cli/>), تغییر cleanup
نسخهٔ ۴ به کار روزانهٔ batch/requeue
(<https://developer.woocommerce.com/2026/06/17/changes-to-action-scheduler/>) و راهنمای مهاجرت
HPOS فروشگاه بزرگ
(<https://developer.woocommerce.com/docs/features/orders/high-performance-order-storage/guide-large-store/>).

### ۱۰.۴ سد امنیتی رازها

مقایسهٔ ۳۶ فیلد password پنل با رجیستری ۱۷تایی `Settings::SECRETS` نشان داد فقط ۱۴
مورد مشترک‌اند و ۲۲ فیلد جاافتاده‌اند؛ سه عضو رجیستری فیلد password متناظر در این صفحه
ندارند. این شکاف شامل کلیدهای شاهکار، دامنه، FX، چند بانک، BNPL، لجستیک، مارکت‌پلیس،
ترجمه و تبلیغات است. فهرست کامل و برنامهٔ مهاجرت در `API-KEYS.md §۵` ثبت شد.

تا تکمیل رجیستری، مهاجرت plaintext، ماسک خروجی HTML، آزمون rotation/backup و اسکن نشت:

- ورود کلید واقعی در این ۲۲ فیلد ممنوع است؛
- آزمون زندهٔ شاهکار، رجیسترار، FX و سرویس‌های متناظر نباید آغاز شود؛
- کلیدی که پیش‌تر در این فیلدها وارد شده باید پس از اصلاح rotate شود.

OWASP بر رمزنگاری در حالت سکون، دسترسی حداقلی، ابطال/چرخش و ممنوعیت ثبت plaintext
تأکید دارد:
<https://cheatsheetseries.owasp.org/cheatsheets/Secrets_Management_Cheat_Sheet.html>.

### ۱۰.۵ برنامهٔ مرحله‌بندی‌شدهٔ ادامه تا تکمیل

هر بستهٔ زیر یک تغییر معنادار مستقل است و طبق قواعد پروژه فقط پس از **برنامهٔ همان بسته،
تأیید کارفرما و دستور صریح «شروع کن»** وارد کدنویسی می‌شود. پایان هر بسته: تست هدفمند،
تست یکپارچهٔ متناسب، مرورگر واقعی، اسکرین‌شات تمام‌صفحه، بازبینی چشمی، سند و commit.

#### بستهٔ صفر — بستن سدهای انتشار و امنیت

- یکپارچه‌کردن رجیستری رازها، رمزکردن ۲۲ فیلد جاافتاده و مهاجرت امن دادهٔ قدیمی؛
- جلوگیری از بازنمایش راز در HTML و افزودن تست تطبیق «هر password حتماً secret»؛
- افزودن مجوز شیء tenant-scoped به DNS و بازبینی مشابه برای همهٔ مسیرهای `{id}`؛
- اتمیک‌کردن claim/decision صف مجوز و idempotency عملیات؛
- امن‌سازی دانلود پادو: allowlist میزبان، SSRF-safe redirects، stream و سقف پاسخ، بدون
  ارسال Bearer به میزبان ثالث؛
- ممنوع‌کردن PHP در artefact تولیدی FSE و کنترل symlink/path/امضای artefact؛
- تعیین و پیاده‌سازی معماری صحیح قالب tenant-scoped؛ حذف اتکای چندفروشگاهی به
  `switch_theme()` سراسری و ساخت پیش‌نمایش واقعاً جدا؛
- رفع fixture کشور checkout و سبزکردن دوبارهٔ ۳۵/۳۵ حالت ویژوال از محیط صفر.

**دروازهٔ خروج:** صفر plaintext secret، صفر BOLA شناخته‌شده در مسیرهای هدف، تست race
دقیقاً یک‌بار، پیش‌نمایش جدا، و ۳۵/۳۵ ویژوال سبز.

#### بستهٔ یک — زیرساخت صف و مقیاس

- انتقال کارهای دیرپا/بیرونی از cron تجمیعی به Action Scheduler با group مستأجر؛
- تعریف job envelope نسخه‌دار، کلید idempotency، claim lease، retry/backoff با jitter،
  timeout، cancellation و dead-letter؛
- تبدیل cleanupها و sweepها به batchهای کوچک requeueشونده؛
- cursor pagination و سقف قطعی برای همهٔ فهرست‌های رشدپذیر؛
- ماتریس cache/rate limit، شمارندهٔ اتمیک و object cache مشترک برای چند نمونه؛
- `EXPLAIN` و اصلاح ایندکس‌ها با fixture حجیم؛ server cron/CLI برای اجرای قابل اتکا.

**دروازهٔ خروج:** عدم starvation میان tenantها، تخلیهٔ backlog پس از قطعی، عدم اجرای دوگانه،
و بودجهٔ p95/DB ثبت‌شده در آزمون بار.

#### بستهٔ دو — چرخهٔ صحیح دامنهٔ پولی

- تعریف `DomainProviderInterface` و آداپتور sandbox نسخه‌دار؛
- quote دارای قیمت و انقضا، سفارش idempotent و پرداخت؛
- فراخوانی ثبت فقط پس از تأیید قطعی پرداخت؛ جبران خودکار در شکست؛
- callback/webhook امضاشده، تمدید دستی/خودکار، اعلان انقضا و retry/dead-letter؛
- مجوز tenant در همهٔ عملیات و گزارش تطبیق سفارش/ارائه‌دهنده.

**دروازهٔ خروج:** سناریوهای ثبت، شکست پرداخت، callback تکراری/جعلی، timeout، تمدید و جبران
در sandbox واقعی موفق باشند.

#### بستهٔ سه — تکمیل S1 پادو و مرکز مجوز

- نهایی‌کردن معماری قالب با کارفرما و نوشتن اسکیل کامل `igbz-store-theme` با هفت مرجع،
  سه قرارداد خروجی یا تصمیم صریح دربارهٔ حذف انواع غیرلازم، schema و نمونهٔ golden؛
- اتصال تولید درخواست و اجراکنندهٔ قیمت، Refund، حذف انبوه، کمپین و چهار نوع انتشار؛
- ویرایش/تأیید دسته‌ای با باطل‌شدن تأیید پس از تغییر payload، سقف مالی و تأیید تک‌موردی؛
- ثبت ممیزی کامل و قابل انتساب؛ timeout، escalation و صف مرکزی با مالک/مهلت؛
- پیاده‌سازی حافظهٔ دانش/تجربه/خاطره، حافظهٔ کاری، Playbook نسخه‌دار، KPI و نگهداشت؛
- قرارداد خروجی سرویس پادو برای همهٔ نوع‌ها و آزمون‌های prompt injection/tool abuse.

**دروازهٔ خروج:** هیچ عملیات حساس خارج صف قابل اجرا نباشد، هر درخواست به نتیجهٔ اجرایی
قابل اثبات برسد و بازگشت/جبران آن آزموده شده باشد.

#### بستهٔ چهار — آزمون سرویس‌های واقعی و تاب‌آوری

پس از بستهٔ صفر، کلیدهای sandbox/واقعی با حداقل دسترسی و محیط جدا وارد شوند و برای Zernio،
DeepInfra، شاهکار، پیامک، بانک‌ها، BNPL، لجستیک، FX، مارکت‌پلیس، ترجمه و تبلیغات
قراردادهای مثبت/منفی اجرا شوند. برای هر سرویس: schema پاسخ، امضای webhook، replay، timeout،
۴۲۹/۵xx، پاسخ خراب، قطع شبکه، retry، reconciliation و هزینه ثبت شود.

**دروازهٔ خروج:** هیچ آداپتور فقط با mock «کامل» شمرده نشود؛ گزارش آزمون ارائه‌دهنده و
Runbook چرخش/قطع کلید برای هر سرویس موجود باشد.

#### بستهٔ پنج — بومی‌سازی و تجربهٔ کاربری

- ساخت `fa_IR.po/mo` برای همهٔ رشته‌ها جز پیام‌های خطا طبق تصمیم کارفرما؛
- جایگزینی وابستگی به emoji/glyphهای بی‌fallback با Dashicons/SVG محلی و واحد پول خوانا؛
- بازدسته‌بندی آمار ترجمه بر مبنای POT تازهٔ ۲۳۵۱ رشته؛
- ترجمهٔ اسکریپت‌های ووکامرس در محیط بسته‌بندی‌شده، RTL و دسترس‌پذیری پنل؛
- تست نقش‌های مدیر ارشد/مالک/کارمند/مشتری، موبایل و صفحه‌کلید؛
- جایگزینی fixtureهای تصویری صرفاً آزمایشی با سنجهٔ روشن کیفیت محتوای نمونه.

**دروازهٔ خروج:** صفحه‌های هدف فارسی/RTL، بدون متن ناخواستهٔ انگلیسی، WCAG هدف و مقایسهٔ
ویژوال تأییدشده در اندازه‌های دسکتاپ/موبایل.

#### بستهٔ شش — آمادگی عملیاتی و انتشار

- ماتریس WordPress/PHP/WooCommerce و HPOS روشن/خاموش؛ migration روی دادهٔ حجیم؛
- پشتیبان، بازیابی واقعی، rollback نسخه/DB، retention و بازیابی artefact؛
- SLI/SLO، داشبورد صف و سرویس‌ها، هشدار backlog/خطا/latency/cost و audit؛
- تست بار، soak، race، chaos و قطعی سرویس؛ ممیزی امنیتی نهایی و dependency scan؛
- staging همسان تولید، اجرای smoke مرورگر روی Railway و سپس انتشار مرحله‌ای/canary؛
- به‌روزرسانی تمام READMEها، شمارش‌ها، Runbook و هندآف.

**دروازهٔ خروج:** restore drill موفق، HPOS تأییدشده، SLO قابل سنجش، rollback آزموده و
هیچ محور قرمز/نارنجی در جدول انتشار باقی نماند.

#### پس از بک‌اند

اپ‌های فلاتر طبق تصمیم قبلی تا تکمیل بک‌اند معلق‌اند و در مخزن جدا و پس از دریافت فایل
کامل اپ ادامه می‌یابند. `vira/` نیز در تمام بسته‌های بالا خارج از محدوده است، مگر کارفرما
برای آن دستور صریح تازه بدهد.

### ۱۰.۶ پیشنهاد ترتیب شروع

ترتیب پیشنهادی بدون هم‌پوشانی: **بستهٔ صفر → یک → دو → سه → چهار → پنج → شش**. نخستین
کار کدنویسی باید بخش «رجیستری رازها و مهاجرت امن» از بستهٔ صفر باشد؛ چون ورود کلیدهای
واقعی برای چند فاز بعدی به آن وابسته است. هنوز هیچ مجوزی برای شروع کدنویسی صادر نشده است.

---

## ۱۱. برنامهٔ جامع تکمیل و تطبیق — ۱۴۰۵/۰۶/۰۵

به درخواست کارفرما، برنامهٔ کامل تطبیق دوطرفهٔ نیازها، اسناد، کد، آزمون و شواهد زنده در
فایل زیر ثبت شد:

```text
PROJECT-COMPLETION-PLAN.md
```

این برنامه، جزئیات اجرایی برنامهٔ خلاصهٔ بخش ۱۰.۵ را توسعه می‌دهد و برای اجرا بر آن مقدم
است. دامنهٔ آن افزونه، وردپرس/ووکامرس، پنل، فروشگاه، API، استقرار، آزمون و اسناد همین
مخزن است. پوشهٔ `vira/` همچنان ممنوع و اپ‌های فلاتر تا دریافت فایل کامل در مخزن جدا
معلق‌اند؛ فقط قرارداد API موردنیاز آن‌ها در دامنه است.

مرحلهٔ نخست، ساخت ماتریس ردیابی بود که در فاز ۰۱ انجام شد. تصمیم‌های معماری فاز ۰۲ و
رفع تناقض‌های سندی فاز ۰۳ پیش از نخستین برش کدنویسی قرار دارند. هر فاز فنی بعدی مجوز صریح
همان فاز را طبق قرارداد `PROJECT-COMPLETION-PLAN.md §۲۳` می‌خواهد؛ مجوز فاز ۰۱ به فازهای
بعدی تسری ندارد.

### فازبندی تک‌نوبتی برنامهٔ جامع

به درخواست کارفرما، برنامهٔ کلان به واحدهایی بازتقسیم شد که هرکدام پس از آماده‌بودن
پیش‌نیازها، در یک نوبت و بدون گزارش میانی قابل اجرا باشند. نسخهٔ اجرایی در
`PROJECT-COMPLETION-PLAN.md §۲۳` شامل ۷۶ فاز پایه و یک فاز مستقل برای هر provider فعال
است. عدد providerها پس از تصمیم معماری/دامنهٔ فاز ۰۲ مشخص می‌شود. این تقسیم‌بندی جایگزین
اجرای یک‌جای بسته‌های کلان است؛ خود بسته‌ها همچنان نقشهٔ پوشش کامل پروژه‌اند.

### ۱۱.۱ نتیجهٔ فاز ۰۱ — موجودی و ماتریس ردیابی

کارفرما پس از تأیید برنامه دستور صریح «حالا شروع کن» داد و
`امنیت و مراقبت/README.md` را مبنای امنیت انتخاب کرد. فاز ۰۱ فقط موجودی و اسناد را تغییر
داد و هیچ کد محصولی را دست نزد.

خروجی‌های مرجع:

```text
REQUIREMENTS-TRACEABILITY.md
PHASE-01-INVENTORY.json
_devenv/phase01_inventory.py
```

نتیجه‌های قابل بازتولید: ۲۱۵ فایل PHP منبع، ۱۴۳ فراخوانی ثبت REST در ۱۵ فایل، DB v19 و
۷۲ جدول، ۳۰۴ تنظیم، ۳۶ فیلد password، رجیستری ۱۷تایی راز با ۱۴ اشتراک و ۲۲ مورد جاافتاده،
۱۱ capability، سه نقش، ده shortcode، سه cron مرکزی و ۲۸ فایل تست/۲۴ کلاس اصلی. ۶۷ خانوادهٔ
نیاز با شناسهٔ یکتا به سند، کد، آزمون یا شکاف وصل شدند.

تمام طراحی‌های ریشه، ۱۲ سند `ِDoc/` و گزارش nopCommerce طبقه‌بندی شدند. اسناد `ِDoc/`
ورودی تاریخی/پیشنهادی‌اند و بدون تصمیم صریح کارفرما الزام قطعی WordPress نیستند. ده تناقض
فعال و نه تصمیم معماری/دامنه برای فاز ۰۲ ثبت شد.

**وضعیت جاری:** فاز ۰۱ تمام است. فاز ۰۲ کدنویسی ندارد، اما برای گرفتن تصمیم‌ها باید طبق
قاعدهٔ پروژه هر بار فقط یک سؤال کوتاه با گزینه و امکان پاسخ آزاد پرسیده شود. تا تکمیل این
تصمیم‌ها، فاز ۰۳ شروع نمی‌شود.

### ۱۱.۲ فاز ۰۲ — تصمیم معماری مستأجر و قالب

کارفرما گزینهٔ «چندسایتی + هستهٔ مدیریتی مشترک» را انتخاب کرد. هر فروشگاه یک زیرسایت با
دادهٔ ووکامرس، تنظیمات، فایل و قالب مستقل خواهد بود؛ کد، هاب، provision، پلن/صورتحساب و
مدیریت مرکزی در control plane مشترک IGBZ می‌مانند. سند تصمیم:

```text
ADR/ADR-0001-MULTISITE-TENANCY.md
```

معماری تک‌سایتی فعلی با `tenant_id` فقط وضعیت کد پیش از migration است و دیگر معماری نهایی
نیست. `switch_theme()` سراسری مردود است؛ جدول‌های سفارشی، نقش شبکه/سایت، HPOS، صف، cache،
فایل و دامنه باید در فازهای بعد برای Multisite طبقه‌بندی و مهاجرت شوند.

### ۱۱.۳ فاز ۰۲ — معماری نهایی پادو و اینستاگرام

تصمیم اولیهٔ ADR-0002 با ADR-0004 جانشین شد. پادو دستیار کامل ولی Playbookمحور هر فروشگاه
است؛ حساب و هزینهٔ inference آن مستقل می‌ماند. DeepInfra provider هدف نسخهٔ اول است. Zernio
تنها provider اجتماعی با حساب پرداخت مرکزی IGBZ و profile جدا برای هر فروشگاه است.
Manus، ChatPlace، ManyChat و Ayrshare از معماری هدف حذف شدند. سند تصمیم:

```text
ADR/ADR-0004-PADO-ZERNIO-SOCIAL-ARCHITECTURE.md
DESIGN-INSTAGRAM-PADO-ZERNIO.md
```

بک‌اند IGBZ مرجع هویت، مجوز، صف و اجرای قطعی است. پادو به DB، فایل، shell، wp-admin، n8n،
ویرا، کلید مرکزی Zernio یا REST عمومی WooCommerce دسترسی مستقیم ندارد. scraper و نشست
مرورگر Instagram نیز ممنوع‌اند.

### ۱۱.۴ فاز ۰۲ — بازیابی تصمیم خروجی قالب

کارفرما تصحیح کرد که نوع خروجی قبلاً جمع‌بندی شده است. پاسخ مستقیم
`DESIGN-PADO.md §۱۸، ۷-الف` ملاک قرار گرفت: **هر سه نوع به انتخاب ادمین** — قالب فرزند
بلوکی، قالب کلاسیک PHP و تمپلیت صفحه‌ساز الحاقی. مرجع تصمیم و قیود Multisite/امنیت:

```text
ADR/ADR-0003-THREE-THEME-OUTPUTS.md
```

مسیر بلوکی فاقد کد اجرایی پیش‌فرض است. PHP تولیدی نصب مستقیم ندارد و فقط پس از sandbox،
تحلیل، بازبینی انسانی مدیر شبکه، امضا، staging و تست مرورگر منتشر می‌شود. صفحه‌ساز نیز
پیش‌فرض نصب نمی‌شود و فقط نسخه‌های ازپیش‌تأییدشده را می‌پذیرد. چهار ADR معماری فاز ۰۲
ثبت شده و ADR-0004 جانشین ADR-0002 است.

### ۱۱.۵ ثبت نهایی معماری پادو و اینستاگرام — ۱۴۰۵/۰۶/۰۶

جمع‌بندی قطعی در اسناد زیر ثبت و اسناد جاری با آن هماهنگ شدند:

```text
ADR/ADR-0004-PADO-ZERNIO-SOCIAL-ARCHITECTURE.md
DESIGN-INSTAGRAM-PADO-ZERNIO.md
PROMPT-IG-GROWTH-PADO.md
```

معماری هدف عبارت است از پادوی Playbookمحور و مستقل، DeepInfra با حساب/هزینهٔ خود فروشگاه،
و Zernio به‌عنوان تنها provider اجتماعی با حساب مرکزی IGBZ و profile جدا برای هر فروشگاه.
پیشنهاد کانال Instagram مبتنی بر session در Agent Reach رد شده و کد
Manus/ChatPlace/ManyChat هنوز migration نشده است. قابلیت‌های فاقد endpoint یا آزمون واقعی
همچنان گیت/backlog هستند. هیچ کد محصول یا فایل `vira/` در این ثبت تغییر نکرد.

سه سند اصلی با Chromium واقعی در عرض ۱۴۴۰، راست‌به‌چپ و تمام‌صفحه رندر شدند؛ سرریز افقی و
خطای console نداشتند و تصاویر چشمی بازبینی شدند. مدرک:

```text
visual-testing/PADO-ZERNIO-DOCS-REPORT.md
visual-testing/phase-02-adr-0004-pado-zernio.png
visual-testing/design-instagram-pado-zernio.png
visual-testing/prompt-instagram-growth-pado.png
```
